# MacEverything 性能分析报告 R_2606031111

## 1. 本轮范围

- 任务启动时间：2026-06-03 11:11
- 报告文件：`docs/performance_ana/R_2606031111.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21184`
  - `SEG_LINES=158`
  - `LAST_LOG_TS=2026-06-03 11:11`

## 2. 执行摘要

服务正常运行（第 4 轮连续可用）。本轮整体性能为恢复后最佳：P50 ≈ 93ms，`.pdf` 降至 446ms（四轮最低）。tombstone 增长至 33,512 条（0.58%），仍在安全范围。

### 四轮横向对比

| Query | R49 冷 | R50 热 | R51 | R52 本轮 | 趋势 |
|-------|-------:|------:|-----:|--------:|------|
| `x` | 1924 | 78 | 76 | **21** | 持续改善 |
| `.pdf` | 9221 | 208 | 4091 | **446** | 高波动但本轮最佳 |
| `hello world` | 474 | 422 | 545 | **334** | 本轮最佳 |
| `path:/Users` | 221 | 578 | 237 | **201** | 稳定 |
| P50 | 189 | 83 | 185 | **93** | 本轮最佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | 备注 |
|-------|------:|------------:|---------:|--------|------|
| `x` | 5 | 21.5 | 0.0 | short-query-cache | 极快 |
| `.pdf` | 5 | 446.0 | 439.1 | advanced-trigram | 本轮最佳但仍最慢 |
| `main.cpp` | 5 | 38.9 | 29.1 | advanced-trigram | 快 |
| `*.swift` | 5 | 160.7 | 156.4 | advanced-trigram | 正常 |
| `ext:cpp` | 5 | 92.6 | 91.8 | advanced-ext-index | 正常 |
| `size:>100m` | 5 | 64.1 | 63.8 | advanced-pure-filter-soa-gcd | 快 |
| `hello world` | 5 | 333.8 | 330.6 | advanced-trigram | 改善 |
| `abc def ghi jkl mno pqr` | 0 | 121.3 | 120.9 | advanced-trigram | 正常 |
| `README` | 5 | 36.1 | 29.1 | advanced-trigram | 快 |
| `test` | 5 | 55.7 | 44.2 | advanced-trigram | 正常 |
| `MacEverything` | 5 | 12.8 | 1.1 | advanced-trigram | 极快 |
| `dm:today` | 5 | 64.1 | 64.0 | advanced-pure-filter-soa-gcd | 快 |
| `content:import` | 5 | 142.0 | 131.9 | advanced-pure-filter-soa-gcd | 正常 |
| `path:/Users` | 5 | 201.2 | 193.9 | advanced-linear-gcd | 正常 |
| `中文测试` | 0 | 243.0 | 238.4 | advanced-linear-gcd | 正常 |
| `verylongquery...` | 0 | 2.4 | 0.0 | advanced-trigram | 极快 |

**P50 ≈ 93ms，P90 ≈ 334ms，Max = 446ms。全部查询 < 500ms。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,754,527，liveRecordCount=5,721,015（tombstone=33,512，0.58%）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=36  QUERYADV_GE500=4
FLUSH=1  COMPACTION=2  SKIPFLUSH=11
DIAG_TRIGRAM_SKIP=56
```

- QUERYADV_GE500 保持 4（未新增 ≥500ms 查询，本轮 `.pdf` 446ms 未超阈值）。
- SKIPFLUSH 从 5 增至 11（WAL 条目持续不足 100）。
- DIAG_TRIGRAM_SKIP 从 55 增至 56（+1）。

## 6. 问题清单

### Medium：`.pdf` 查询仍为最大 outlier（446ms）

- 证据：四轮数据 9221 → 208 → 4091 → 446ms。波动范围 20x。
- 本轮后台 I/O 较平静，故延迟较低。结构性根因未变（trigram 候选过多）。

### Medium：`hello world` 多词查询 334ms

- 本轮为恢复后最佳，但仍是 phase2 验证成本导致。

### Low：DIAG-TRIGRAM SKIP 持续累积（56 次）

- 增速放缓（+1/轮），主要来自 UI 侧查询。

### Low：tombstone 增长

- 33,512 条（0.58%）。按当前速率（~5,000/轮），约 200 轮后可能触发 compaction。

## 7. 优化建议

### P0：补齐进程生命周期监控（承前轮）

### P1：将 `.xxx` 扩展名模式路由到 ext-index（承前轮）

### P2：多词查询 phase2 剪枝优化（承前轮）

### P2：监控 tombstone 增长速率

## 8. 下一轮重点

1. 继续监控服务稳定性和 `.pdf` 波动。
2. 追踪 tombstone 增长速率是否加速。
3. 确认 SKIPFLUSH 增速是否意味着 WAL 写入减少。

## 9. 结论

服务连续第 4 轮正常运行，本轮为恢复后性能最佳轮次。P50 ≈ 93ms，所有查询 < 500ms（首次实现）。`.pdf` 降至 446ms（四轮最佳），`x` 降至 21ms，`hello world` 降至 334ms。`.pdf` 的高波动性（208-9221ms 范围）进一步证实其与后台 I/O 负载的强相关性，结构性修复（ext-index 路由）仍是最高收益优化项。
