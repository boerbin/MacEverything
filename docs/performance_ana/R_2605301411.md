# MacEverything 性能分析报告 R_2605301411

- **任务启动时间**：2026-05-30 14:11
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~27.1h，未重启
- **状态快照 @14:11**：total `5,873,736` / live `5,391,578`
- **本轮定性**：🟢 **连续第二个洁净轮——午间停顿决定性结束**：电池零污染标志（`x`=0.45ms、`main.cpp` 71候选2.9ms、`*.swift` 9213候选**29ms**，比 R91 的 72ms 更暖），电池窗口日志零 >500ms 条目，**1-5s(23) 与 0.5-1s(16) 延迟段连续两轮冻结**。洁净间隙 **2.50×**，二层分裂 **19/19 不破**。唯一恶化项 tombstone **8.21%**（连续 25 轮单调），根因仍为 R67 阀门不可达，无新结论。

---

## 1. 查询电池（16 条，本轮洁净）

| query | path | total(ms) | phase2 | trig | cand | res |
|---|---|---|---|---|---|---|
| `x` | short-query-cache | 0.45 | 0 | 0 | 0 | 99 |
| `.pdf` | advanced-trigram | 18.88 | 17.89 | 0.83 | 10123 | 100 |
| `main.cpp` | advanced-trigram | 2.92 | 2.47 | 0.42 | 71 | 71 |
| `*.swift` | advanced-trigram | 29.30 | 29.01 | 0.27 | 9213 | 100 |
| `ext:cpp` | advanced-ext-index | 10.47 | 10.01 | 0 | 2987 | 100 |
| `size:>100m` | pure-filter-soa-gcd | 45.72 | 45.71 | 0 | 0 | 100 |
| `hello world` | advanced-trigram | 29.24 | 28.58 | 0.61 | 2340 | 100 |
| `abc def ghi jkl mno pqr` | advanced-trigram | 53.30 | 53.14 | 0.09 | 15924 | 0 |
| `README` | advanced-trigram | 18.29 | 15.12 | 3.09 | 16465 | 100 |
| `test` | advanced-trigram | 25.35 | 22.95 | 2.09 | 136266 | 100 |
| `MacEverything` | advanced-trigram | 3.78 | 1.32 | 2.42 | 6698 | 100 |
| `dm:today` | pure-filter-soa-gcd | 67.58 | 67.52 | 0 | 0 | 100 |
| `content:import` | pure-filter-soa-gcd | 166.26 | 154.91 | 0 | 0 | 100 |
| `path:/Users` | advanced-linear-gcd | 199.42 | 197.08 | 0 | 0 | 100 |
| `中文测试` | advanced-linear-gcd | 197.22 | 197.03 | 0.1 | 0 | 0 |
| `verylongquery…really` | advanced-trigram | 0.60 | 0 | 0.55 | 0 | 0 |

整批可信：所有 trigram 路径回暖（`*.swift` 29ms、`hello world` 29ms、`main.cpp` 2.9ms），无任何停顿迹象。

---

## 2. 洁净 ns/槽（电池+3× 复测取 4 样本中位，records=5,873,736）

| 路径 | 类型 | 4样本中位 | 洁净值 |
|---|---|---|---|
| `size:>100m` | 数值 | 35.7ms | **6.08** |
| `dm:today` | 数值 | 72.7ms | **12.38** |
| `content:import` | 字符串 | 182.0ms | **30.99** |
| `path:/Users` | 字符串 | 217.8ms | **37.08** |
| `中文测试` | 字符串 | 231.2ms | **39.36** |

---

## 3. 本轮头条：连续第二个洁净轮，午间停顿决定性结束

R91 已捕获午间停顿的完整生命周期（108→237→1002→618→72ms）。本轮 R92 作为停顿结束后的**第二个洁净轮**，提供了停顿确已结束的**双重确认**：

| 信号 | R90（污染）| R91（恢复）| **R92（本轮）** |
|---|---|---|---|
| `*.swift` total | 618ms | 72ms | **29ms** |
| 电池 >500ms 条目 | 有（618/861）| 无 | **无** |
| 1-5s 延迟段 | 23 | 23 | **23（冻结）** |
| 0.5-1s 延迟段 | 16 | 16 | **16（冻结）** |

`*.swift` 进一步从 72ms 回暖到 29ms（trigram 索引页重新热驻留）；延迟分布的 1-5s 与 0.5-1s 两段**连续两轮（R91/R92）零增长**，所有新增查询全部落在 0.1-0.5s 正常段。午间外部事件（12:00 起、12:41 峰、13:41 前结束）已彻底退出，系统回到稳态。

---

## 4. 绝对二层分裂：洁净数据 19/19 不破（间隙 2.50×）

- **数值层**：size 6.08、dm 12.38 → 最高数值 **dm 12.38**
- **字符串层**：content 30.99、path 37.08、中文 39.36 → 最低字符串 **content 30.99**
- **洁净间隙 = 30.99 / 12.38 = 2.50×**，无重叠。

dm（12.38）为数值天花板、size（6.08）为地板，与近轮一致。字符串层本轮排序为 content < path < 中文，且 path（37.08）与 中文（39.36）接近——再证 R79「字符串层内部细排序为逐区冷热噪声，无固定次序」。分裂依旧机制级（纯数值零解引用 ≪ 字符串解引用散布串池），不构成新结论。

---

## 5. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **455 次**（R91 443）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100，`:159` tombstone 比例阀结构性永不可达——全会话 0 触发、0 回收。

---

## 6. tombstone 趋势：连续 25 轮单调上升，稳居 8% 区间

| 轮次 | tombstone | ratio |
|---|---|---|
| R88@12:11 | 439,394 | 7.54% |
| R89@12:41 | 455,464 | 7.79% |
| R90@13:11 | 464,832 | 7.94% |
| R91@13:41 | 473,060 | 8.07% |
| **R92@14:11** | **482,158** | **8.21%** |

会话内单调递增、永不回缩。R43 参考崩溃点 12.97%（~800K），当前 8.21% 已稳居 8% 区间、过 6 成程。live 基本持平（5,391,578），约 96% 新增记录变为 tombstone。这是会话内唯一真实恶化趋势，根因明确为 R67 阀门不可达。

---

## 7. 日志健康度

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **306 条**（R91 294，+12）——增速稳定（R91 亦 +12），洁净轮特征
- Top-7 峰值 `[18582, 17928, 15749, 8205, 7544, 6811, 6061]` —— 仍与历史**逐字节相同**
- 延迟分布：>5s=**7**（冻结）、1-5s=**23**（冻结）、0.5-1s=**16**（冻结）、0.1-0.5s=260（+12）、中位 **221ms**
- 三段（>5s/1-5s/0.5-1s）连续两轮全冻结，本轮 +12 越线查询**全部落在 0.1-0.5s 段**——洁净稳态

---

## 8. 优化建议（四项 P0，本轮无新增）

1. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调 <12.97%（建议 8–10%）。当前 8.21% 已落入建议阈值区间，紧迫性最高。
2. **🔥 P0｜mlock/madvise 整体常驻热区**：午间停顿虽已消退，但连续三轮（R88/R89/R90）证明无内存保护时外部压力会反复拖慢任意路径；需把 path/name 串池、content 列、trigram 索引、候选行、数值 SoA 列整体钉入内存以抵御下次外部事件。
3. **🔥 P0｜无锁快照 flush**：启动 flush 持锁 10.3s。
4. **🔥 P0｜查询超时保护**：R88（1663ms）+ R89（1002ms）+ R90（861ms）三个真实停顿样本——超时熔断 + 降级返回是必要韧性机制。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1🔥｜CJK trigram 索引**：`中文测试` 走 linear-gcd 全扫（中位 231ms / 39.36 ns/槽）。

---

## 9. 结论

- **定性**：🟢 连续第二个洁净轮。午间停顿决定性结束——`*.swift` 从 72ms 进一步回暖到 29ms，延迟分布三段连续两轮全冻结，系统回到稳态。
- **稳健结论 19/19，间隙 2.50×**：脱离 R90 史上最窄 2.11× 后稳定在正常区间，分裂依旧机制级无重叠。
- **诚实修正纪律**：本轮无推测结论被推翻、无新结论需建立；字符串层内部 path≈中文 近并列，为 R79 已定噪声的复现。
- **唯一持续恶化项**：tombstone **8.21%**（连续 25 轮单调，稳居 8% 区间），根因 R67 阀门结构性不可达明确。这是会话内唯一真实退化，与已结束的外部停顿无关，修复紧迫性随阈值逼近而最高。
