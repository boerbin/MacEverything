# MacEverything 性能分析报告 R_2606091630

## 1. 本轮范围

- 任务启动时间：2026-06-09 16:30
- 报告文件：`docs/performance_ana/R_2606091630.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6172`
  - `SEG_LINES=5230`
  - `LAST_LOG_TS=2026-06-09 11:47`

## 2. 执行摘要

**部分恢复——1/16 >500ms，Max=825ms，P50=84ms，lockWait=0，极佳比例 5/16。** R210 的 3/16 >500ms 降至 1/16（`.pdf`=825ms 残余），但 `x`=137ms 暴露严重进程调度延迟。2 轮恢复模式（非典型 1 轮恢复）。tombstone 即将突破 26%。

| 指标 | R210 | R211 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 3/16 | **1/16** | ↓2（部分恢复） |
| Max | 1241ms | **825ms** | ↓34% |
| P50 | 86ms | **84ms** | ↓2%（持平） |
| lockWait max | 1.3ms | **0ms** | 恢复完美 |
| 极佳比例(<50ms) | 3/16 | **5/16** | ↑2 |
| tombstone | 1,805,640 (25.90%) | **1,812,772 (25.98%)** | ↑ |
| tombstone 增速 | ~391/min | **~237/min** | ↓39%（回落） |
| recordCount | 6,970,383 | **6,977,889** | +7.5K |
| liveRecordCount | 5,164,743 | **5,165,117** | +374 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 4693 | **4753** | +60 |
| COMPACTION | 2 | **2** | **不变——第 12 轮确认缺陷** |
| QUERYADV_GE500 | 48 | **49** | ↑1 |

### `.pdf`=825ms——trigram postings 残余淘汰

- **phase2=814ms，lockWait=0——纯 page cache 未完全恢复。**
- **R210=524ms → R211=825ms（↑57%）——逆势升高！**
- **说明 `.pdf` 的 trigram postings 区域在 R210 battery 中虽被部分预热，但午后持续 I/O 压力又将其淘汰。**
- **candidates=9842——需要读取大量分散的 postings 数组。**

### `x`=137ms——严重进程调度延迟！

- **phase2=0（short-query-cache 命中），但 queryTime=137ms——全是 overhead。**
- **R209=2ms → R210=16ms → R211=137ms——连续 3 轮升高！**
- **137ms overhead = 进程调度延迟（可能系统 CPU 竞争、上下文切换）。**
- **此前系列最高 R199=85ms——本轮 137ms 是新纪录！**
- **影响所有查询的 baseline——每个查询的 queryTime 都可能包含额外 ~100ms overhead。**

### `verylongquery`=12ms——异常偏高

- **此前稳定在 0.3-1.3ms——本轮 12ms（↑800%+）。**
- **phase2=0（trigram 立即返回 0 结果）——全是 overhead。**
- **与 `x`=137ms 同源——进程调度延迟影响所有查询。**

### Trigram 路径——大部分恢复

| Query | R210 | R211 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 524ms | **825ms** | ↑57% | **>500ms（逆升）** |
| `hello world` | 1241ms | **148ms** | ↓88% | 正常（恢复） |
| `*.swift` | 236ms | **74ms** | ↓69% | 正常 |
| `main.cpp` | 86ms | **38ms** | ↓56% | 极佳 |
| `abc def...` | 196ms | **191ms** | ↓3% | 正常（未恢复） |
| `README` | 54ms | **41ms** | ↓24% | 极佳 |
| `test` | 78ms | **84ms** | ↑8% | 正常 |
| `MacEverything` | 32ms | **21ms** | ↓34% | 极佳 |
| Trigram 均值 | 306ms | **178ms** | **↓42%** | 部分恢复 |

- **Trigram 均值 306→178ms（↓42%）——部分恢复。**
- **`hello world`=148ms（↓88% from 1241ms）——已恢复。**
- **`.pdf`=825ms——逆势升高，trigram postings 区域被持续 I/O 压力反复淘汰。**
- **`abc def`=191ms（↓3%）——几乎未恢复，高 candidates 查询恢复慢。**

### 全表扫描——稳定

| Query | R210 | R211 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 187ms | **190ms** | ↑2% |
| `path:/Users` | 265ms | **288ms** | ↑9% |
| `中文测试` | 273ms | **236ms** | ↓14% |
| **总和** | **725ms** | **714ms** | **↓2%** |

- **全表扫描总和 725→714ms（↓2%）——持平，正常范围。**
- **线性扫描在持续 I/O 压力下保持稳定——说明路径/名称数组的 mmap 区域被 battery 有效预热。**

### 辅助路径——大部分恢复

| Query | R210 | R211 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 16ms | **137ms** | **↑756%** | **极佳（但高 overhead）** |
| `ext:cpp` | 82ms | **34ms** | ↓59% | 极佳（恢复） |
| `size:>100m` | 742ms | **41ms** | **↓94%** | 极佳（完美恢复） |
| `dm:today` | 61ms | **63ms** | ↑3% | 正常 |
| `verylongquery...` | 1.1ms | **12ms** | ↑991% | 极佳（高 overhead） |

- **`size:>100m`=41ms（↓94% from 742ms）——SoA 区域 1 轮完美恢复。**
- **`ext:cpp`=34ms（↓59%）——ext-index 区域恢复。**

### tombstone 25.98%——即将突破 26%

- **tombstone=1,812,772（25.98%）——超阈值 0.98%，距 26% 仅 0.02%。**
- **增速 237/min（从 391/min 回落但仍然偏高）。**
- **recordCount +7.5K，liveRecordCount +374——净新增极少，大部分为 tombstone。**
- **COMPACTION=2——第 12 轮确认缺陷。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 136.8 | 0.0 | short-query-cache | 0 | 0 | 极佳（**高 overhead**） |
| `.pdf` | 5 | 824.7 | 813.6 | advanced-trigram | 0 | 9842 | **>500ms** |
| `main.cpp` | 5 | 37.5 | 26.4 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 74.2 | 69.9 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 33.7 | 32.0 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 41.2 | 40.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 148.1 | 135.5 | advanced-trigram | 0 | 3669 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 191.0 | 186.0 | advanced-trigram | 0 | 14351 | 正常 |
| `README` | 5 | 41.3 | 33.2 | advanced-trigram | 0 | 17009 | 极佳 |
| `test` | 5 | 84.4 | 76.7 | advanced-trigram | 0 | 116910 | 正常 |
| `MacEverything` | 5 | 20.7 | 1.2 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 62.6 | 62.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 189.5 | 180.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 287.9 | 282.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 235.9 | 232.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 11.7 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 84.4ms，P90 = 287.9ms，Max = 824.7ms。1/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R210 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **5/16** | 12-41ms | R210=3/16（↑2） |
| **正常**（50-500ms） | 10/16 | 63-288ms | R210=10/16（持平） |
| **超标**（>500ms） | **1/16** | 825ms | R210=3/16（↓2） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,977,889，liveRecordCount=5,165,117（tombstone=**1,812,772**，**25.98%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=377  QUERYADV_GE500=49（↑1）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4753
```

## 6. 问题清单

### P0 确认（第 12 轮）：compaction 触发机制缺陷

- **tombstone 25.98%——距 26% 仅 0.02%。**
- **COMPACTION=2——始终不变。**

### 新发现：进程调度延迟新纪录

- **`x`=137ms（phase2=0）——纯进程调度 overhead。**
- **此前最高 R199=85ms——本轮 ↑61%。**
- **午后 CPU 竞争严重——影响所有查询 baseline。**

### 观察：`.pdf` 逆势升高

- **R210=524ms → R211=825ms——不是恢复而是加剧。**
- **trigram postings 区域在持续 I/O 压力下被反复淘汰。**
- **预计下轮恢复（battery 预热效应累积）。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 12 轮确认）

### P0.5：flush rewrite 改为 COW 方式（持续建议）

### P2：madvise MADV_WILLNEED 热区维护（持续建议）

## 8. 下一轮重点

1. `.pdf`=825ms 是否恢复。
2. `x` 进程调度延迟是否消退。
3. tombstone 是否突破 26%。

## 9. 结论

**部分恢复——1/16 >500ms（`.pdf`=825ms 逆升），Max=825ms，P50=84ms，极佳 5/16。** R210 的 3/16 降至 1/16，但 `.pdf` 从 524→825ms 逆势升高（trigram postings 被持续 I/O 反复淘汰）。`x`=137ms（进程调度延迟新纪录！此前最高 85ms）。`size:>100m`=41ms 完美恢复（↓94%）。

**tombstone 25.98%（第 12 轮确认 compaction 缺陷）——距 26% 仅 0.02%。** 午后 I/O 压力导致 2 轮恢复模式（非典型），进程调度延迟叠加 page cache 淘汰双重压力。
