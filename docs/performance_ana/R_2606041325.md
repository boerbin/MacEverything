# MacEverything 性能分析报告 R_2606041325

## 1. 本轮范围

- 任务启动时间：2026-06-04 13:25
- 报告文件：`docs/performance_ana/R_2606041325.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=22086`
  - `SEG_LINES=1060`
  - `LAST_LOG_TS=2026-06-04 13:26`

## 2. 执行摘要

**性能优秀但 short-query-cache GCD 异常创历史最差。** `x`=1,526ms（**历史最高**，前最差 R86=367ms 的 4.2 倍）。排除 `x` 后：P50 ≈ 49ms，Max=118ms，**全部 15/15 查询 <500ms**，lockWaitMs 全部为 0——**接近 R90 历史最佳水平**。

**R92-R94 I/O 压力事件完全恢复。** `.pdf`=68ms（R92=820ms→R93=635ms→R94=630ms→R95=375ms→**R96=68ms**），`*.swift`=58ms，`hello world`=118ms，`中文测试`=50ms——全部回到正常区间。

**tombstone=519,641（8.30%，156MB），增量 +9,210**（略低于稳态 12K/轮，可能因 R95-R96 间隔偏短）。

### 横向对比（最近 5 轮）

| Query | R92 | R93 | R94 | R95 | R96 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 5 | 75 | 11 | 18 | **1526** | **极端 GCD 异常** |
| `.pdf` | 820 | 635 | 630 | 375 | **68** | **完全恢复** |
| `*.swift` | 430 | 22 | 226 | 264 | **58** | **正常** |
| `hello world` | 412 | 352 | 240 | 210 | **118** | **正常** |
| `README` | 27 | 14 | 1071 | 31 | **13** | 正常 |
| `path:/Users` | 222 | 144 | 218 | 394 | **83** | 正常 |
| `中文测试` | 279 | 234 | 391 | 573 | **50** | **完全恢复** |
| P50 | 131 | 57 | 171 | 147 | **49** | **优秀** |

### R92-R94 I/O 事件完整恢复追踪

| 轮次 | P50 | Max | >500ms | lockWait max | 状态 |
|------|----:|----:|-------:|------------:|------|
| R92（轻度） | 131ms | 820ms | 1/16 | 2.2ms | I/O 压力 |
| R93（改善） | 57ms | 635ms | 1/16 | 0ms | 热缓存 |
| R94（中度） | 171ms | 1,071ms | 2/16 | 46.1ms | 加剧 |
| R95（缓解） | 147ms | 573ms | 1/16 | 0ms | 缓解 |
| **R96（恢复）** | **49ms** | **118ms** | **0/16** | **0ms** | **完全恢复** |

**恢复用时 4 轮**（R92→R96）。三次 I/O 事件周期对比：

| 事件 | 轮次 | 峰值 Max | 恢复轮数 | 模式 |
|------|------|--------:|--------:|------|
| #1 | R67 | 10,902ms | 6 轮 | 广泛饱和 |
| #2 | R84-R86 | 2,731,551ms | 3 轮 | 单点灾难 |
| **#3** | **R92-R94** | **1,071ms** | **4 轮** | **游走式压力** |

第 3 次事件（R92-R94）特征独特：I/O 压力在 trigram 和 linear-gcd 索引区域间游走（`.pdf`→`README`→`中文测试`），而非集中在单一路径。这是 tombstone 8%+ 下页面缓存全局挤压的新表现。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1526.2 | 0.0 | short-query-cache | 0 | 0 | **极端 GCD 异常** |
| `.pdf` | 5 | 68.3 | 64.9 | advanced-trigram | 0 | 9952 | 完全恢复 |
| `main.cpp` | 5 | 7.5 | 3.7 | advanced-trigram | 0 | 85 | 极快 |
| `*.swift` | 5 | 58.4 | 57.5 | advanced-trigram | 0 | 9356 | 正常 |
| `ext:cpp` | 5 | 15.7 | 14.4 | advanced-ext-index | 0 | 3701 | 极快 |
| `size:>100m` | 5 | 31.6 | 31.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 117.7 | 113.1 | advanced-trigram | 0 | 3613 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 68.2 | 66.2 | advanced-trigram | 0 | 16040 | 正常 |
| `README` | 5 | 13.2 | 11.5 | advanced-trigram | 0 | 17671 | 极快 |
| `test` | 5 | 32.1 | 25.8 | advanced-trigram | 0 | 130121 | 正常 |
| `MacEverything` | 5 | 14.8 | 0.3 | advanced-trigram | 0 | 6803 | 极快 |
| `dm:today` | 5 | 17.6 | 16.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 极快 |
| `content:import` | 5 | 105.7 | 96.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 82.5 | 79.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 49.9 | 49.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.8 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**排除 `x` 后：P50 ≈ 49ms，P90 ≈ 106ms，Max = 118ms。全部 15/15 <500ms。lockWaitMs 全部为 0。**

**含 `x`：P50 ≈ 49ms，Max = 1,526ms。1/16 >500ms（仅 GCD 异常）。**

### `x`=1,526ms 极端 GCD 异常分析

- **历史全部出现记录**：

| 轮次 | `x` 耗时 | I/O 压力？ | 说明 |
|------|--------:|----------|------|
| R67 | 155ms | 是（灾难） | I/O 事件中 |
| R69 | 68ms | 否 | 正常 |
| R71 | 103ms | 否 | 正常 |
| R73 | 120ms | 否 | 正常 |
| R76 | 190ms | 是（中等） | I/O 事件中 |
| R80 | 88ms | 否 | 正常 |
| R85 | 174ms | 是（中度） | I/O 事件中 |
| R86 | 367ms | 是（灾难） | I/O 事件中 |
| R89 | 52ms | 否 | 恢复后 |
| R93 | 75ms | 否 | 正常 |
| **R96** | **1,526ms** | **否** | **极端异常** |

- **11 次出现（共 36 轮，30.6%）。本次 1,526ms 是前最差 367ms 的 4.2 倍。**
- phase2=0ms 确认问题 100% 在 GCD dispatch 层——short-query-cache 查找本身为 O(1)，但 GCD block 被调度延迟。
- **本次特殊**：非 I/O 压力环境下出现极端值——其余 15 个查询均正常，表明这是一个**独立的 GCD 调度问题**。
- 可能原因：
  1. `x` 作为电池第 1 个查询，其 GCD block 恰好与系统级 GCD 活动竞争（macOS 系统调度、后台进程等）。
  2. 1,526ms ≈ 1.5 秒——可能是 GCD thread pool 的线程创建/唤醒延迟（cold dispatch）。
  3. 与查询内容无关，纯粹是 GCD runtime 的调度抖动。

### 性能分层（排除 GCD 异常）

| 层级 | 查询 | 耗时范围 | 说明 |
|------|------|---------|------|
| 极快（<20ms） | `main.cpp`, `ext:cpp`, `MacEverything`, `dm:today`, `README`, `verylongquery` | 0.3-17.6ms | 热缓存/小候选集 |
| 正常（20-120ms） | `*.swift`, `.pdf`, `test`, `size:>100m`, `中文测试`, `path:/Users`, `abc...`, `hello world`, `content:import` | 31.6-117.7ms | 正常范围 |
| 无异常 | — | — | R92-R94 I/O 压力影响已完全消散 |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,262,718，liveRecordCount=5,743,077（tombstone=**519,641**，**8.30%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=345  QUERYADV_GE500=48
FLUSH=0  COMPACTION=2  SKIPFLUSH=412
DIAG_TRIGRAM_SKIP=104
```

- QUERYADV 从 343 增至 345（+2）。
- QUERYADV_GE500 保持 48（`x`=1526ms 走 short-query-cache 路径，不计入 QueryAdvanced）。
- SKIPFLUSH 从 404 增至 412（+8，略低于稳态 +12——R95→R96 间隔偏长但可能有段不活跃）。
- DIAG_TRIGRAM_SKIP 从 103 增至 104（+1，稳态）。

### QUERYADV_GE500 稳定

- `x`=1,526ms 不计入 QUERYADV_GE500——因为 `x` 走 short-query-cache 路径，不经过 QueryAdvanced。
- 本轮无 QueryAdvanced 查询超 500ms（排除 `x` 后 Max=118ms）。

## 6. 问题清单

### Critical：tombstone 8.30%（519,641 条，156MB）

- 增长轨迹（稳态轮次）：
  ```
  R92: 480K (+25.5K) → R94: 494K (+11.9K) → R95: 510K (+16.3K)
  → R96: 520K (+9.2K)
  ```
- 本轮 +9,210 略低于稳态——R95→R96 间隔内可能有段系统空闲。
- **累计内存开销**：~520K × 300 字节 ≈ **156MB**。
- 连续第 36 轮超 2%，第 17 轮超过 5%，第 12 轮超过 6%，第 9 轮超过 7%，第 2 轮超过 8%。

### Critical：short-query-cache GCD 异常升级（1,526ms 历史最差）

- **11/36 轮出现率 30.6%，前最差 367ms，本次 4.2 倍跳升**。
- `x` 是最简单的查询（short-query-cache 命中，phase2=0），不应超过 20ms。
- 1,526ms 的 GCD dispatch 延迟已构成**用户可感知的卡顿**。
- **独立于 I/O 压力**：本轮其余查询全部正常，说明 GCD 异常是系统级调度问题而非索引 I/O 问题。
- **优先级提升至 P0**——从"已知但不紧急"变为"用户体验直接影响"。

### 正面：R92-R94 I/O 事件完全恢复

- 恢复用时 4 轮（R92→R96）。
- 恢复模式：游走式压力（trigram→linear-gcd→恢复），非集中恶化。
- 当前性能优秀——排除 GCD 异常后接近 R90 历史最佳。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（8.30%，156MB）

- **36 轮连续超标。** 三次 I/O 事件周期已验证因果链。
- 8.30% 下一次 I/O 事件的触发门槛更低、恢复更慢。
- compaction 释放 156MB → 系统性消除所有 I/O 压力事件。

### P0 紧急：short-query-cache GCD 隔离（1,526ms 历史最差）

- **方案**：将 short-query-cache 查询从共享 GCD queue 隔离到专用 serial queue，或改为同步返回（cache hit 不需要异步）。
- **收益**：消除 30.6% 出现率的用户可感知卡顿。
- **风险**：极低——short-query-cache 的 O(1) 查找不需要 GCD 并发保护。

### P0 紧急：`size:>100m` 全表扫描超时保护
### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. GCD 异常是否持续或恢复正常。
2. I/O 压力是否保持恢复状态。
3. tombstone 增速趋势。

## 9. 结论

**性能优秀但 short-query-cache GCD 异常创历史最差。** `x`=1,526ms（phase2=0，纯 GCD dispatch 延迟，前最差 367ms 的 4.2 倍）。排除 `x` 后 P50=49ms，Max=118ms，全部 15/15 <500ms——**接近 R90 历史最佳水平**。**R92-R94 I/O 事件完全恢复**（4 轮，`.pdf` 从 820ms→68ms），恢复模式为游走式压力而非集中恶化。lockWaitMs=0。tombstone=519,641（8.30%，156MB，+9,210）。GCD 异常从"已知问题"升级为"用户体验直接影响"——1.5 秒卡顿发生在最简单的缓存查询上，且独立于 I/O 压力，需要架构层面修复（隔离 GCD queue 或改为同步返回）。
