# MacEverything 性能分析报告 R_2605301911

- **任务启动时间**：2026-05-30 19:11
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~32.1h，未重启
- **状态快照 @19:11**：total `6,053,624` / live `5,408,253` / tomb `645,371`（**10.66%**）
- **本轮定性**：🟠 **重度活跃写入 + 分裂三法不破（dm 双峰，中位法被显式拒绝）**——ingest 强烈再加速（tomb 增量 **+23,336，R96 以来最高**，92.9% 新增入墓）。整批电池重度受争用（cache marker `x`=368.54ms 硬破、`*.swift`=1273.83ms 峰值、`main.cpp` 71cand trig=155ms 触发 marker#3），但**复测全部瞬时恢复 → 突发型非稳态**。FLOOR-METHOD 双窗口：`content` **异常平稳锚**（W1↔W2 摆动仅 **1.02×**，min 135.5≈138.04），`path` **轻度非平稳**（摆动 1.51×，弱于 R100/R101 的 2.8–2.9×），`dm` **本轮双峰**（突发至 477ms）使**中位法不适用**——按 R97 floor-method 取 min（7.48ns）为无偏热态估计。**分裂 28/28、间隙 2.77×（字面 min）/ 2.99×（平稳锚）**，对抗检验（dm 最差洁净簇 10.31ns）对 content（2.17×）与 path（2.01×）**双双 HOLDS**；**中位法（dm-median 18.8ns，突发抬升）被显式拒绝**——这正是 R99 双峰中位陷阱的复现，floor-method 设计目的即规避此偏置。

---

## 1. 原始电池（重度争用，⚠️ 标记异常项）

| query | path | total(ms) | phase2 | trig | lockHeld | cand | 备注 |
|---|---|---|---|---|---|---|---|
| `x` | short-query-cache | **368.54** | 0 | 0 | 368.5 | 0 | ⚠️ **cache marker 硬破**（复测 0.0）|
| `.pdf` | advanced-trigram | 386.16 | 380.5 | 5.3 | 386.1 | 10179 | 受争用 |
| `main.cpp` | advanced-trigram | **211.89** | 56.0 | **155.37** | 211.8 | 71 | ⚠️ **marker#3**（71cand trig=155ms 不可能，复测 0.92）|
| `*.swift` | advanced-trigram | **1273.83** | 1271.2 | 2.4 | **1265.68** | 9213 | ⚠️ **本轮峰值**（lockHeld≈total，复测 2.76）|
| `ext:cpp` | advanced-ext-index | 284.59 | 283.6 | 0 | 284.5 | 2987 | 受争用 |
| `size:>100m` | pure-filter-soa-gcd | 95.13 | 95.1 | 0 | 95.1 | 0 | 数值（floor 14.74）|
| `hello world` | advanced-trigram | 326.47 | 325.0 | 1.3 | 326.4 | 2389 | 受争用 |
| `abc def ghi jkl mno pqr` | advanced-trigram | 188.33 | 187.6 | 0.66 | 188.3 | 15928 | 受争用 |
| `README` | advanced-trigram | 104.00 | 97.8 | 6.1 | 103.9 | 16520 | 受争用 |
| `test` | advanced-trigram | 30.67 | 22.9 | 7.6 | 30.6 | 137908 | 较干净 |
| `MacEverything` | advanced-trigram | 25.56 | 9.9 | 15.6 | 25.5 | 6704 | 较干净 |
| `dm:today` | pure-filter-soa-gcd | 83.22 | 83.2 | 0 | 83.2 | 0 | 数值（floor 45.27，本轮双峰）|
| `content:import` | pure-filter-soa-gcd | 306.74 | 298.4 | 0 | 298.4 | 0 | 字符串（floor 平稳锚）|
| `path:/Users` | advanced-linear-gcd | 303.25 | 300.5 | 0 | 300.5 | 0 | 字符串（floor 轻度非平稳）|
| `中文测试` | advanced-linear-gcd | 261.76 | 261.5 | 0.1 | 261.7 | 0 | 字符串 |
| `verylongquery…really` | advanced-trigram | 0.82 | 0 | 0.81 | 0.82 | 0 | |

**三重 marker 同批触发**：cache `x`=368.54ms（缓存路径理论 0ms，纯写者持锁）+ `main.cpp` 71cand 但 trig=155ms（71 个候选的真实 trigram <1ms，155ms 是排队）+ `*.swift` lockHeld 1265.68/total 1273.83（写者持锁占 99.4%）。三者共同证明 19:11:45 一次系统级写入突发横扫批次。窗口内 totalRecords 实测 +1808 证写入活跃。

---

## 2. 复测消歧（标志项全部瞬时恢复 → 突发非稳态）

| query | 电池值 | probe1 | probe2 | probe3 | probe4 | 判定 |
|---|---|---|---|---|---|---|
| `x`（缓存）| 368.54 | 0.11 | 0.03 | 0.00 | 0.04 | ✅ 瞬时（>9000× 恢复）|
| `main.cpp`（71cand）| 211.89 | 9.71 | 1.77 | 0.92 | 0.93 | ✅ 瞬时（230× 恢复）|
| `*.swift` | 1273.83 | 41.91 | 1.32 | 4.25 | 2.76 | ✅ 瞬时（461× 恢复）|
| `ext:cpp` | 284.59 | 20.61 | 2.87 | 5.15 | 27.96 | ✅ 瞬时（突发型，未完全静默）|

标志项全部突发型（非 R95 稳态），可评估。`ext:cpp` 末次 27.96 表明 ingest 仍在间歇击中，但远低于 500ms。

---

## 3. FLOOR-METHOD 复测（双窗口各 8×，R100 平稳性协议）

R100 确立：floor-method 假设窗口内计算成本平稳；pure-filter（紧凑列）满足，linear-gcd（散布串池）在 ingest 下可能违反，须**双窗口交叉验证**。本轮 W1 自身重度突发（path 1288 / content 852 / 中文 632 窗口内）：

**窗口内 spread（路径内平稳性）：**

| query | 类型 | W1 min | W1 med | W1 max | spread | 平稳性 |
|---|---|---|---|---|---|---|
| `size` | 数值 pure-filter | 14.74 | 21.41 | 94.92 | 6.4× | 散（突发尾）|
| `dm` | 数值 pure-filter | 45.27 | 113.79 | 161.95 | **3.6×（双峰）** | **散——中位法不适用** |
| `content` | 字符串 pure-filter | 135.50 | 247.84 | 852.57 | 6.3× | min 极稳（见下）|
| `path` | 字符串 linear-gcd | 189.11 | 297.58 | 1288.29 | 6.8× | 散（含 1288 突发）|
| `中文` | 字符串 linear-gcd | 258.98 | 348.37 | 632.45 | 2.4× | |

**跨窗口 floor 摆动（平稳性判定核心）：**

| query | 类型 | W1 floor | W2 floor | 摆动 | 判定 |
|---|---|---|---|---|---|
| `size` | pure-filter | 14.74 | 15.14 | **1.03×** | 平稳 |
| `dm` | pure-filter | 45.27 | 56.69 | **1.25×** | 平稳（floor 稳，但分布双峰）|
| `content` | pure-filter | 135.50 | 138.04 | **1.02×** | **异常平稳锚** |
| `path` | linear-gcd | 189.11 | 125.62 | **1.51×** | 轻度非平稳（弱于 R100/R101）|

**关键观察**：
1. **`content` 是本系列最稳锚**：W1 min 135.5 ≈ W2 min 138.04，摆动仅 1.02×，即便 W1 窗口内有 852ms 突发尾，floor 纹丝不动——pure-filter 紧凑列在写入间隙稳定全驻留。
2. **`path` 轻度非平稳**：W2 floor（125.62）< W1 floor（189.11），摆动 1.51×，**弱于 R100（literal-min 失败）与 R101（2.9×）**。W1 含 1288ms 突发，W1 min 仍被抬到 189。path floor 仍带 page 驱逐扰动，但本轮幅度温和，未威胁判定。
3. **`dm` 本轮双峰**：W1 在 45→162 间双峰、W2 突发至 477——**中位（113.79ms=18.8ns）被突发抬升，中位法不适用**。按 R97 floor-method 取 min（45.27ms=7.48ns）为无偏热态估计，并以对抗检验（最差洁净簇）兜底。

---

## 4. 洁净 ns/槽（floor 法，records=6,053,624）

| 路径 | 类型 | floor(ms) | 洁净值(ns/槽) | 说明 |
|---|---|---|---|---|
| `size:>100m` | 数值 | 14.74 | **2.43** | |
| `dm:today` | 数值 | 45.27 | **7.48** | 本轮双峰，取 floor |
| `path:/Users` | 字符串 | 125.62 | **20.75** | 字面 min（轻度非平稳 1.51×）|
| `content:import` | 字符串 | 135.50 | **22.38** | ⭐ 平稳锚（摆动 1.02×）|
| `中文测试` | 字符串 | 258.98 | **42.78** | W1-only |

数值层本轮回到 R100 量级（dm 7.48 ≈ R100 7.45，非 R101 的异常暖 2.59）；string 层 content 22.38 与 R100 anchor（23.30）一致——两层均回归稳态基线，分裂结构不变。

---

## 5. 绝对二层分裂：28/28（三法 HOLDS，中位法被显式拒绝）

| 解读 | 最低字符串 | 最高数值 | 间隙 | 对抗检验 |
|---|---|---|---|---|
| **字面绝对 min** | path 20.75ns | dm 7.48ns | **2.77×** | ✅ HOLDS |
| **平稳锚（content）** | content 22.38ns | dm 7.48ns | **2.99×** | ✅ HOLDS |

- **数值层**：size 2.43、dm 7.48 → 最高数值 **dm 7.48**
- **字符串层**：path 20.75（字面 min）/ content 22.38（平稳锚）/ 中文 42.78 → 最低 **path 20.75**
- **字面间隙 = 20.75 / 7.48 = 2.77×**，无重叠。

**多重稳健性**：
1. **字面 min**：path 20.75 > dm 7.48 → 2.77× ✅
2. **平稳锚（content，摆动 1.02×，最强锚）**：content 22.38 > dm 7.48 → 2.99× ✅
3. **对抗检验（dm 最差洁净簇 62.43ms=10.31ns，排除明显突发）**：< content 22.38（2.17×）✅、< path 20.75（2.01×）✅
4. **中位法被显式拒绝**：dm-median 113.79ms=18.8ns 是突发污染值（dm 本轮双峰、W2 突发至 477ms），用之会得 path/dm-median=1.1× 的**假破裂**——这正是 R99 双峰中位陷阱。floor-method（取 min）的设计目的即规避此偏置；dm-median 不是 dm 的真实热态成本，而是热态+ingest 锁争用的混合。

三法（字面 min / 平稳锚 / 对抗）全部 HOLDS，第四法（中位）因数据双峰被正确排除。**28/28 不破**。

间隙 2.77×（字面）回落至 R97-R100 区间（2.14×–3.13×），低于 R101 的 4.05×——R101 的高间隙源于数值层异常暖（dm 2.59ns），本轮 dm 回归 7.48ns 基线，故间隙回归常态。仍属机制级分裂（纯数值零解引用 ≪ 字符串解引用散布串池）。

---

## 6. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **819 次**（R101 761）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100 提前 return，`:159` 比例阀结构性永不可达。skip-gate 761→819（+58），写入持续。

---

## 7. tombstone 趋势：10.66%，连续 35 轮单调，增量 R96 以来最高

| 轮次 | tombstone | ratio | 30min 增量 |
|---|---|---|---|
| R98@17:11 | 582,472 | 9.74% | +18,708 |
| R99@17:41 | 594,254 | 9.91% | +11,782 |
| R100@18:11 | 604,427 | 10.06% | +10,173 |
| R101@18:41 | 622,035 | 10.32% | +17,608 |
| **R102@19:11** | **645,371** | **10.66%** | **+23,336（R96 以来最高）** |

会话内单调递增、永不回缩，**连续 35 轮**。R43 参考崩溃点 12.97%（~800K），当前 10.66% 已过 **~82% 程**。本轮 total +25,125、live 仅 +1,789，即 **92.9% 新增入墓**（本系列最高入墓比之一，与重度 ingest 一致）。增量随 ingest 振荡（本轮强烈再加速 +23,336），方向不可逆，逼近崩溃点。会话内唯一真实退化，修复紧迫性最高。

---

## 8. 日志健康度

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **640 条**（R101 584，+56，含本轮双窗口重度复测）
- Top-10 峰值 `[3362, 3766, 3807, 6061, 6811, 7544, 8205, 15749, 17928, 18582]` —— 仍与历史**逐字节相同**（启动期基建事件）
- 延迟分布（QueryAdvanced-only）：>5s=**7**（冻结）、1-5s=**30（+2）**、0.5-1s=**38（+4）**、0.1-0.5s=565、中位 **220ms**
- **越线条目枚举**：窗口内 6 条 >500ms——19:11:45 **1273ms**（电池 `*.swift`）+ 19:12:49 652ms / 19:12:52 852ms（W1 content 复测）+ 19:12:57 **1288ms**（W1 path 复测）+ 19:13:08 632ms（W1 中文复测）+ 19:14:05 504ms（W2 path 复测）。**6 条全部为本人电池/复测查询撞 ingest 突发，零 organic 慢查询**——背景查询本轮无一越线，越线全部由测量行为引入。
- 启动基建事件须排除：`20765ms`@11:05:13、`10353ms`@11:12:29——已过滤

---

## 9. 优化建议（P0｜read-write-lock + mlock 维持升级）

1. **🔥 P0｜读写锁分离 / 无锁快照读**：本轮三重 marker（cache `x` 368ms、`*.swift` lockHeld 1265/1273、`main.cpp` 71cand trig 155ms）为全系列最密集的写者饿死读者证据；ingest 振荡七轮（R95–R102）持续指向同一根因，最高优先级。
2. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调（建议 8–10%）。当前 **10.66%**，~82% 逼近崩溃点，增量 R96 以来最高。
3. **🔥 P0｜mlock/madvise 钉住散布热区**（维持 R100 升级）：本轮 path linear-gcd floor 跨窗口摆动 1.51%（轻度非平稳），虽弱于 R100/R101，仍证 page 驱逐扰动 linear-gcd 计算成本。mlock 钉住 path/name 串池可消除该非平稳，使 linear-gcd 在写入压力下保持稳定热态。R100+R101+R102 连续三轮定量证据。
4. **🔥 P0｜无锁快照 flush**：启动 flush 持锁 10.3s。
5. **🔥 P0｜查询超时保护**：本轮 `*.swift` 1273ms、path 1288ms 突发——超时熔断 + 降级返回必要。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1｜CJK trigram 索引**（维持 R99 降级）：中文热态成本（42.78）与 path/content 同量级，慢主要是页冷非算法。

---

## 10. 结论

- **定性**：🟠 重度活跃写入 + 分裂三法不破。ingest 强烈再加速（tomb **+23,336，R96 以来最高**，92.9% 入墓），整批电池重度受争用（三重 marker：cache 368ms / `*.swift` 1273ms / `main.cpp` trig 155ms），但复测全部瞬时恢复 → 突发型非稳态。
- **分裂 28/28，三法 HOLDS**：`content` 为本系列最强平稳锚（跨窗摆动仅 **1.02×**），`path` 轻度非平稳（1.51×，弱于 R100/R101），`dm` 本轮双峰使**中位法不适用**——按 floor-method 取 min（dm 7.48ns）。字面 min（path 20.75）2.77× / 平稳锚（content 22.38）2.99× / 对抗检验（dm 最差洁净 10.31ns，对 content 2.17×、对 path 2.01×）全部 HOLDS。
- **中位法被显式拒绝（方法论纪律）**：dm-median 18.8ns 是突发污染值，用之得假破裂 1.1×——R99 双峰中位陷阱复现，floor-method 设计目的即规避此偏置。本轮以「取 floor + 对抗检验」正确处理双峰 dm，未被假象误导。
- **R100 发现连续三轮验证**：linear-gcd floor 非平稳本轮第三次复现（path 摆动 1.51×，虽温和），为 mlock P0 升级提供第三轮定量证据。
- **唯一持续恶化项**：tombstone **10.66%（645,371）**，连续 35 轮单调，~82% 逼近 12.97% 崩溃点，本轮增量 R96 以来最高，根因 R67 阀门结构性不可达明确。
- **诚实修正纪律**：本轮严格区分突发非稳态（复测恢复）与稳态退化（tombstone）；明确标注 6 条越线全为测量行为撞突发、零 organic 慢查询；显式拒绝双峰污染的中位法、以 floor+对抗双法兜底；间隙 2.77× 归因于 dm 回归基线（非 R101 异常暖）而非机制变化。贯彻「不轻易下结论、每个结论需逻辑与数据支撑」。
