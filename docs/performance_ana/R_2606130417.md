# MacEverything 性能分析报告 R_2606130417

## 1. 本轮范围

- 任务启动时间：2026-06-13 04:17
- 报告文件：`docs/performance_ana/R_2606130417.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=9244`
  - `SESSION_START_TS=2026-06-10 17:02:22.835`
  - `TOTAL_LINES=11553`
  - `SEG_LINES=2310`
  - `LAST_LOG_TS=2026-06-13 04:18`

## 2. 执行摘要

**本轮前台仍无 >500ms 查询：16-query battery 中 0/16 >500ms，但 Max 从 246.5ms 升至 406.5ms。** P50 从约 57.2ms 降至约 22.8ms，P90 从约 187.7ms 升至约 344.6ms，说明大多数 trigram/indexed 查询恢复到低延迟，但扫描类尾部重新恶化。最高耗时为 `中文测试`=406.5ms，其次是 `path:/Users`=344.6ms、`content:import`=226.8ms、`dm:today`=136.3ms。lockWait 全部为 0ms，本轮没有直接锁等待证据。

后台指标进入更高风险区间：recordCount=6,019,174，liveRecordCount=5,292,078，tombstone=727,096（12.08%），较上一轮增加 6,637，已正式突破 12%。latest session `FLUSH=574`（较上一轮 +12），`SKIPFLUSH=0`。最新 flush 从 4255ms 回落到 3737ms，但仍是秒级完整重写；latest session 继续保留 2,285,536ms、896,053ms、735,223ms、107,008ms 等灾难级 flush outlier。

| 指标 | R_2606130347 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 246.5ms | **406.5ms** | ↑65% |
| P50 | ≈57.2ms | **≈22.8ms** | ↓60% |
| P90 | ≈187.7ms | **≈344.6ms** | ↑84% |
| lockWait max | 0.0ms | **0.0ms** | 持平 |
| 极佳比例(<50ms) | 8/16 | **12/16** | +4 |
| 全表扫描总和 | 737.7ms | **1149.2ms** | ↑56% |
| recordCount | 6,012,280 | **6,019,174** | +6,894 |
| liveRecordCount | 5,291,821 | **5,292,078** | +257 |
| tombstone | 720,459 (11.98%) | **727,096 (12.08%)** | +6,637 |
| 最新段 FLUSH | 562 | **574** | +12 |
| 最新段 QUERYADV_GE500 | 11 | **11** | 无新增 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 0.9 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 32.5 | 29.7 | advanced-trigram | 0.0 | 9858 | 极佳，较上一轮恢复 |
| `main.cpp` | 5 | 4.2 | 2.6 | advanced-trigram | 0.0 | 93 | 极佳 |
| `*.swift` | 5 | 21.7 | 20.6 | advanced-trigram | 0.0 | 9517 | 极佳 |
| `ext:cpp` | 5 | 9.8 | 9.3 | advanced-ext-index | 0.0 | 3736 | 极佳 |
| `size:>100m` | 5 | 35.0 | 34.9 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 23.8 | 22.7 | advanced-trigram | 0.0 | 3932 | 极佳，较上一轮恢复 |
| `abc def ghi jkl mno pqr` | 0 | 48.7 | 48.5 | advanced-trigram | 0.0 | 14588 | 极佳（接近 50ms） |
| `README` | 5 | 9.6 | 6.5 | advanced-trigram | 0.0 | 17532 | 极佳 |
| `test` | 5 | 13.2 | 10.3 | advanced-trigram | 0.0 | 117911 | 极佳 |
| `MacEverything` | 5 | 6.7 | 0.9 | advanced-trigram | 0.0 | 7004 | 极佳 |
| `dm:today` | 5 | 136.3 | 135.3 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常（偏高） |
| `content:import` | 5 | 226.8 | 214.3 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常（偏高） |
| `path:/Users` | 5 | 344.6 | 331.3 | advanced-linear-gcd | 0.0 | 0 | 正常（接近阈值） |
| `中文测试` | 0 | 406.5 | 405.4 | advanced-linear-gcd | 0.0 | 0 | 正常（本轮最高，接近超标） |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈22.8ms，P90≈344.6ms，Max=406.5ms。0/16 查询 >500ms。lockWait max=0.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **12/16** | 0.4-48.7ms | short-query-cache、trigram、ext-index 与轻量 pure-filter 表现健康 |
| 正常（50-500ms） | **4/16** | 136.3-406.5ms | pure-filter 与 linear-gcd 扫描类路径拉高 P90 和 Max |
| 超标（>500ms） | **0/16** | — | 本轮无前台超标查询，但 `中文测试` 已接近 500ms |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,019,174；
- liveRecordCount=5,292,078；
- tombstone=727,096（**12.08%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=9244
SESSION_START_TS=2026-06-10 17:02:22.835
TOTAL_LINES=11553
SEG_LINES=2310
QUERYADV=180
QUERYADV_GE500=11
FLUSH=574
SKIPFLUSH=0
COMPACTION=2
LAST_LINE=[2026-06-13 04:18:03.050] QueryAdvanced "中文测试" total=406ms | path=advanced-linear-gcd
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 11，本轮没有新增日志级慢查询。历史慢查询仍分布在 linear-gcd、trigram 与 ext-index 路径，说明前台长尾不仅来自扫描路径，也会在 trigram/postings 路径受后台 I/O 和 page-cache 状态影响时出现：

```text
[2026-06-10 17:17:09.559] QueryAdvanced "path:/Users" total=525ms | path=advanced-linear-gcd
[2026-06-10 17:25:15.434] QueryAdvanced "lllllllllllllllllllllllllllllllllllllllllllll" total=1154ms | path=advanced-trigram
[2026-06-10 17:47:13.642] QueryAdvanced "中文测试" total=716ms | path=advanced-linear-gcd
[2026-06-10 23:44:25.566] QueryAdvanced "test" total=676ms | path=advanced-trigram
[2026-06-12 09:17:32.430] QueryAdvanced "中文测试" total=555ms | path=advanced-linear-gcd
[2026-06-12 13:53:09.945] QueryAdvanced ".pdf" total=1955ms | path=advanced-trigram
[2026-06-12 13:53:12.523] QueryAdvanced "path:/Users" total=590ms | path=advanced-linear-gcd
[2026-06-12 14:48:11.578] QueryAdvanced "hello world" total=760ms | path=advanced-trigram
[2026-06-12 16:21:11.617] QueryAdvanced ".pdf" total=529ms | path=advanced-trigram
[2026-06-12 16:21:12.485] QueryAdvanced "ext:cpp" total=591ms | path=advanced-ext-index
[2026-06-12 16:21:16.446] QueryAdvanced "hello world" total=3724ms | path=advanced-trigram
```

### flush 极端长尾

最新 session 内 `FLUSH=574`，rewrite 范围 **1380ms - 2285476ms**，total 最大 **2285536ms**。

最新 flush：

```text
[2026-06-13 04:16:45.895] Flushed v6 flat index, lastEventId=355625741,
liveRecords=5292064 | timing: walSwap=0ms rewrite=3696ms sqcache=40ms total=3737ms
```

Top flush outliers：

```text
[2026-06-12 13:02:35.416] rewrite=2285476ms sqcache=6ms total=2285536ms
[2026-06-12 03:15:02.833] rewrite=895940ms sqcache=2ms total=896053ms
[2026-06-10 23:24:50.749] rewrite=725091ms sqcache=18ms total=735223ms
[2026-06-12 19:06:52.432] rewrite=106972ms sqcache=34ms total=107008ms
[2026-06-11 14:30:37.596] rewrite=58273ms sqcache=5ms total=101726ms
```

## 6. 问题清单

### P0：tombstone 已突破 12%

- 证据：tombstone=727,096（12.08%），较上一轮增加 6,637。
- 影响：无效记录比例继续升高，会放大扫描成本、索引重写成本与 compaction 压力。
- 判断：这是本轮最明确的新风险升级，说明现有 compaction/清理触发策略偏晚。

### P0：扫描类前台尾延迟重新恶化

- 证据：`中文测试`=406.5ms，`path:/Users`=344.6ms，`content:import`=226.8ms，full-scan 总和=1149.2ms。
- 影响：虽然 0/16 >500ms，但 `中文测试` 距离阈值不足 100ms，下一轮在 I/O 压力下有突破风险。

### P0：flush 高频且没有退避

- 证据：`FLUSH=574`，较上一轮 +12；`SKIPFLUSH=0`；最新 flush total=3737ms。
- 影响：秒级完整重写持续发生，会污染 page cache，并在 I/O 压力下放大为分钟级 outlier。

### P0：latest session 仍包含灾难级 flush outlier

- 证据：13:02 flush total=2,285,536ms，03:15 flush total=896,053ms，23:24 flush total=735,223ms，19:06 flush total=107,008ms。
- 影响：持久化路径缺少切分、退避和前台保护，是当前最主要的系统性风险。

### P1：trigram 路径恢复但仍需要解释波动来源

- 证据：`.pdf` 从 98.8ms 回落到 32.5ms，`hello world` 从 101.1ms 回落到 23.8ms。
- 影响：trigram 路径可从 20-30ms 波动到 100ms 左右，说明缓存或后台 I/O 对 postings 路径仍有可观影响。

## 7. 优化建议

### P0：提前或分段 compaction

- 不等待更高的全局 tombstone 阈值；结合绝对 tombstone 数、segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 验收：tombstone 率从 12%+ 回落，full-scan 总和和常规 flush rewrite 同步下降。

### P0：flush 改为增量/COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页。
- 验收：常规 flush total <200ms，latest session 不再出现分钟级 rewrite outlier。

### P0：增加 flush I/O 压力退避和前台保护

- 当最近 flush 超过阈值时，提高下一次 flush 间隔或降低 batch 频率。
- 前台查询活跃或 I/O 延迟升高时推迟大 flush。
- 持久化线程使用低 QoS，避免冲掉搜索路径 page cache。

### P0：降低 flush 频率并合并 WAL 批次

- 设置最小 flush 间隔和最大合并窗口。
- 对高 churn 目录减少完整重写次数，避免 `FLUSH` 计数持续快速增长。

### P1：拆分扫描路径耗时

- 对 `advanced-linear-gcd`、`advanced-pure-filter-soa-gcd` 记录扫描记录数、tombstone 跳过数、过滤耗时、排序耗时和结果组装耗时。
- 验收：能明确 `path:/Users`、`中文测试`、`content:import` 的主要瓶颈是扫描量、tombstone、排序还是 page-cache 冷却。

### P1：拆分 trigram/postings 路径耗时

- 对 trigram 查询记录 postings 读取、候选集交并、排序、路径加载和结果组装耗时。
- 验收：解释 `.pdf`、`hello world` 在 20-30ms 与 100ms 区间之间切换的原因。

### P2：保护搜索热数据 page cache

- 对 flush 大顺序写考虑 `F_NOCACHE` 或同类策略。
- 对 full-scan/SoA/postings 热区使用预热或访问提示，减少 flush 后冷启动波动。

## 8. 下一轮重点

1. tombstone 是否继续高于 12% 并向 12.2% 增长。
2. `中文测试` 是否突破 500ms。
3. `path:/Users` 是否重新突破 500ms。
4. 最新 flush 是否继续维持 3-4 秒级，或出现新的分钟级 outlier。
5. trigram 路径是否继续在 20-100ms 区间来回波动。

## 9. 结论

**本轮前台查询仍未超标：0/16 >500ms，P50≈22.8ms，12/16 查询低于 50ms；但 Max=406.5ms、P90≈344.6ms，扫描类路径重新成为明显尾部。** `中文测试` 与 `path:/Users` 已回到接近 500ms 的风险区间。

**后台根因继续恶化：tombstone=727,096（12.08%）已突破 12%，`FLUSH=574`，最新 flush=3737ms，latest session 仍保留 total=2,285,536ms、896,053ms、735,223ms、107,008ms 的极端 outlier。** 下一步优先级应上调 compaction 触发治理，并继续推进增量/COW flush、flush 退避、扫描路径与 trigram 路径的细粒度耗时观测。
