# MacEverything 性能分析报告 R_2606031316

## 1. 本轮范围

- 任务启动时间：2026-06-03 13:16
- 报告文件：`docs/performance_ana/R_2606031316.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21311`
  - `SEG_LINES=285`
  - `LAST_LOG_TS=2026-06-03 15:11`

## 2. 执行摘要

服务正常运行（第 12 轮连续可用）。所有查询连续第 9 轮 < 500ms（Max=436ms）。`.pdf` 从前两轮 488-490ms 回落至 349ms。性能整体处于中等水平（P50≈178ms）。

**tombstone 如预测突破 2% 临界点**：128,261 条（2.19%）。这是 11 轮以来首次超过此阈值。SKIPFLUSH=72，flush-triggered compaction 仍然完全休眠。

### 横向对比（最近 5 轮）

| Query | R56 | R57 | R58 | R59 | R60 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 5 | 4 | 254 | 19 | **14** | 稳定 |
| `.pdf` | 98 | 137 | 488 | 490 | **349** | 从逼近阈值回落 |
| `hello world` | 70 | 326 | 425 | 430 | **436** | 持续偏慢 |
| `path:/Users` | 250 | 497 | 312 | 164 | **278** | 正常波动 |
| `content:import` | 165 | 215 | 355 | 174 | **256** | 波动 |
| P50 | 52 | 76 | 163 | 176 | **178** | 中位偏慢 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 13.7 | 0.0 | short-query-cache | 0 | 0 | 快 |
| `.pdf` | 5 | 349.0 | 293.3 | advanced-trigram | 0 | 9944 | 改善 |
| `main.cpp` | 5 | 95.5 | 77.9 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 178.9 | 160.1 | advanced-trigram | 0 | 9356 | 正常 |
| `ext:cpp` | 5 | 146.8 | 142.0 | advanced-ext-index | 0 | 3702 | 正常 |
| `size:>100m` | 5 | 85.0 | 81.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 436.0 | 414.1 | advanced-trigram | 0 | 3613 | 偏慢 |
| `abc def ghi jkl mno pqr` | 0 | 329.7 | 327.6 | advanced-trigram | 0 | 15976 | 偏慢 |
| `README` | 5 | 64.3 | 53.9 | advanced-trigram | 0 | 17717 | 正常 |
| `test` | 5 | 71.3 | 64.6 | advanced-trigram | 0 | 129960 | 正常 |
| `MacEverything` | 5 | 62.2 | 3.4 | advanced-trigram | 0 | 6802 | 正常 |
| `dm:today` | 5 | 111.4 | 110.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 256.0 | 245.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 偏慢 |
| `path:/Users` | 5 | 277.7 | 271.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 214.9 | 192.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 2.0 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 178ms，P90 ≈ 349ms，Max = 436ms。全部查询连续第 9 轮 < 500ms。lockWaitMs 全部为 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,852,631，liveRecordCount=5,724,370（tombstone=**128,261**，**2.19%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=93  QUERYADV_GE500=5
FLUSH=1  COMPACTION=2  SKIPFLUSH=72
DIAG_TRIGRAM_SKIP=65
```

- QUERYADV 从 84 增至 93（本轮 battery 贡献 9 次日志记录）。
- QUERYADV_GE500 保持 5（本轮 Max=436ms，未超阈值）。
- SKIPFLUSH 从 60 增至 72（+12，每 5 分钟一次持续跳过）。
- DIAG_TRIGRAM_SKIP 从 64 增至 65（+1）。

## 6. 问题清单

### Critical：tombstone 突破 2% 临界点（2.19%，128,261 条）

- 证据：从 R49（0.07%）到 R60（2.19%），12 轮内增长 31 倍。
- 增速：本轮增量 13,403 条（前轮 14,180 条），增速稳定。
- **tombstone 增长轨迹**：
  ```
  R49: 4,118 (0.07%) → R52: 33,512 (0.58%) → R55: 67,411 (1.17%)
  → R58: 100,678 (1.73%) → R59: 114,858 (1.97%) → R60: 128,261 (2.19%)
  ```
- SKIPFLUSH=72，flush-triggered compaction 完全无法触发。
- **影响评估**：目前 2.19% 尚未对查询性能产生可测量影响（phase2 扫描仍遍历 totalRecords），但继续增长将增加内存占用和 I/O 负载。

### High：`hello world` 连续 3 轮 >400ms

- 425 → 430 → 436ms，稳定偏慢。candidates=3613，phase2 验证耗时 414ms。
- 系统 I/O 负载持续偏高时，多词 trigram 查询最先受影响。

### Medium：`abc def ghi jkl mno pqr` 延迟偏高（330ms）

- candidates=15976，phase2=328ms。六词查询本轮偏慢。

### Low：SKIPFLUSH 加速积累（72 次）

- 每 5 分钟一次，WAL 条目稳定在 68。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（已突破阈值）

- tombstone 2.19%，已超过建议的 2% 触发阈值。
- **紧急实施方案**：
  1. 定时任务（如每 10 分钟）检查 `(recordCount - liveRecordCount) / recordCount`
  2. 超过 1.5% 时触发增量 compaction（不中断服务）
  3. compaction 完成后重置计数器
- 不依赖 WAL flush 条件，独立触发路径。

### P0：补齐进程生命周期监控（承前轮）

### P1：将 `.xxx` 扩展名模式路由到 ext-index（承前轮）

### P1：`hello world` 多词查询优化

- 连续 3 轮 >400ms。考虑在 trigram 交集阶段引入更严格的剪枝，或限制 candidates 上限。

### P2：降低 SKIPFLUSH 阈值

- 当前阈值 100 导致 flush 路径长期不触发。降至 50 可使 flush 更频繁，间接触发 compaction。

## 8. 下一轮重点

1. **tombstone 增长速率** — 已超 2%，是否加速。
2. `hello world` 是否继续 >400ms。
3. `.pdf` 是否维持在 <400ms 区间。

## 9. 结论

服务连续第 12 轮正常运行，所有查询连续第 9 轮 <500ms（Max=436ms）。**tombstone 如预测突破 2% 临界点（128,261 条，2.19%）**，SKIPFLUSH=72 意味着 flush-triggered compaction 完全不可用。这是当前最紧迫的工程问题——必须引入基于占比的主动 compaction 触发机制。`.pdf` 从前两轮逼近 500ms 回落至 349ms，但 `hello world` 连续 3 轮 >400ms（436ms）。整体 P50=178ms 处于中等水平。
