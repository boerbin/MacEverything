# MacEverything 性能分析报告 R_2606101030

## 1. 本轮范围

- 任务启动时间：2026-06-10 10:30
- 报告文件：`docs/performance_ana/R_2606101030.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=8488`
  - `SEG_LINES=7546`
  - `LAST_LOG_TS=2026-06-10 07:17`

## 2. 执行摘要

**全表扫描大幅改善至 521ms——本会话近期最佳！** 0/16 >500ms，Max=202ms，P50=62ms，lockWait=0（连续 15 轮）。Trigram 均值 50ms（↑35%——轻微回调，零和反向），全表扫描总和 521ms（**↓37%——从 R246=828ms 大幅改善**）。**经典零和反向：全表扫描改善的代价是 trigram 轻微回调。** 极佳 6/16（↓2——trigram 回调所致）。

| 指标 | R246 | R247 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 313ms | **202ms** | **↓35%** |
| P50 | 42ms | **62ms** | ↑48%（零和反向） |
| lockWait max | 0ms | **0ms** | 完美（**连续 15 轮！**） |
| 极佳比例(<50ms) | 8/16 | **6/16** | ↓2（trigram 回调） |
| tombstone | 2,173,845 (29.46%) | **2,179,927 (29.51%)** | **↑——突破 29.5%！** |
| tombstone 增速 | ~231/min | **~203/min** | ↓12%（回落） |
| recordCount | 7,383,219 | **7,389,479** | +6.3K |
| liveRecordCount | 5,209,374 | **5,209,552** | +178 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 6757 | **6817** | +60 |
| COMPACTION | 2 | **2** | **不变——第 48 轮确认缺陷** |
| QUERYADV_GE500 | 60 | **60** | 不变 |

### lockWait 连续 15 轮为 0

- **R233-R247 连续 15 轮 lockWait=0——新纪录。**
- **覆盖深夜→凌晨→早间→上午全时段。**

### tombstone 突破 29.5%

- **tombstone=2,179,927（29.51%）——首次突破 29.5%。**
- **超阈值 4.51%。**
- **增速 ~203/min（↓12%——早间高峰后回落）。**

### tombstone 里程碑全景（更新）

| 里程碑 | 轮次 | tombstone | tombstone% | 距 R200 |
|--------|------|----------:|----------:|-------:|
| 首次超 25% | R200 | ~1,748K | 25.15% | 0 |
| 首次超 26% | R212 | ~1,841K | 26.14% | 12 |
| 首次超 27% | R220 | ~1,929K | 27.08% | 20 |
| 首次超 2M | R229 | 2,009,885 | 27.87% | 29 |
| 首次超 28% | R230 | 2,038,041 | 28.14% | 30 |
| 首次超 29% | R240 | 2,132,504 | 29.04% | 40 |
| **首次超 29.5%** | **R247** | **2,179,927** | **29.51%** | **47** |

### 全表扫描——大幅改善

| Query | R246 | R247 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 246ms | **175ms** | **↓29%** |
| `path:/Users` | 313ms | **144ms** | **↓54%** |
| `中文测试` | 270ms | **202ms** | **↓25%** |
| **总和** | **828ms** | **521ms** | **↓37%** |

- **全表扫描总和 828→521ms（↓37%）——三路全部大幅改善。**
- **`path:/Users`=144ms（↓54%）——路径数组回到优秀区间。**
- **`content:import`=175ms（↓29%）——SoA 内容区域持续恢复。**
- **Max=202ms（`中文测试`）——全部 <250ms，分布均衡。**

### Trigram 路径——零和回调

| Query | R246 | R247 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 52ms | **93ms** | ↑79% | 正常 |
| `hello world` | 92ms | **62ms** | ↓33% | 正常 |
| `*.swift` | 43ms | **87ms** | ↑102% | 正常 |
| `main.cpp` | 9ms | **10ms** | ↑11% | 极佳 |
| `abc def...` | 62ms | **85ms** | ↑37% | 正常 |
| `README` | 14ms | **17ms** | ↑21% | 极佳 |
| `test` | 23ms | **23ms** | 持平 | 极佳 |
| `MacEverything` | 5ms | **21ms** | ↑320%（overhead） | 极佳 |
| Trigram 均值 | 37ms | **50ms** | **↑35%（零和反向）** |

- **Trigram 均值 37→50ms（↑35%）——全表扫描改善挤压 postings 区域。**
- **`.pdf`=93ms、`*.swift`=87ms、`abc def`=85ms——大 postings 集受影响最大。**
- **4/8 trigram 极佳（↓1）——小 candidate 集不受影响。**
- **`MacEverything`=21ms（overhead=20ms，phase2=1.4ms）——调度开销，非搜索问题。**

### 辅助路径

| Query | R246 | R247 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 2ms | **1ms** | ↓50% | 极佳 |
| `ext:cpp` | 13ms | **38ms** | ↑192% | 极佳 |
| `size:>100m` | 58ms | **85ms** | ↑47% | 正常 |
| `dm:today` | 42ms | **76ms** | ↑81% | 正常 |
| `verylongquery...` | 0.2ms | **0.1ms** | ↓50% | 极佳 |

- **`ext:cpp`=38ms（↑192%）——ext-index 波动但仍极佳。**
- **`size:>100m`=85ms、`dm:today`=76ms——SoA 辅助区域轻微回调。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 0.6 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 92.9 | 90.1 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 10.4 | 7.9 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 86.6 | 81.2 | advanced-trigram | 0 | 9353 | 正常 |
| `ext:cpp` | 5 | 38.4 | 37.5 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 85.1 | 84.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 61.8 | 59.2 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 85.3 | 84.6 | advanced-trigram | 0 | 14473 | 正常 |
| `README` | 5 | 16.7 | 14.5 | advanced-trigram | 0 | 17151 | 极佳 |
| `test` | 5 | 23.1 | 18.6 | advanced-trigram | 0 | 117136 | 极佳 |
| `MacEverything` | 5 | 21.2 | 1.4 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 75.7 | 75.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 174.7 | 168.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 143.8 | 139.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 202.0 | 201.0 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.1 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 61.8ms，P90 = 174.7ms，Max = 202.0ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R246 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **6/16** | 0.1-38ms | R246=8/16（↓2） |
| **正常**（50-500ms） | 10/16 | 62-202ms | R246=8/16（↑2） |
| **超标**（>500ms） | **0/16** | — | R246=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,389,479，liveRecordCount=5,209,552（tombstone=**2,179,927**，**29.51%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=593  QUERYADV_GE500=60（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=6817
```

## 6. 问题清单

### P0 确认（第 48 轮）：compaction 触发机制缺陷

- **tombstone=2,179,927（29.51%）——首次突破 29.5%！超阈值 4.51%。**
- **48 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **连续 15 轮 lockWait=0（新纪录）。历史证据充分。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（第 48 轮确认——tombstone 突破 29.5%）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 零和是否自平衡（trigram 50ms→回调→恢复）。
2. tombstone 增速趋势（203/min 回落是否持续）。

## 9. 结论

**全表扫描大幅改善至 521ms（↓37%），Max=202ms（↓35%）。** 0/16 >500ms，P50=62ms，lockWait=0（连续 15 轮——新纪录）。经典零和反向：全表扫描三路全部改善（`path:/Users`=144ms ↓54%），代价是 trigram 均值 50ms（↑35%——仍在正常区间）。极佳 6/16（↓2）。

**tombstone 突破 29.5%（2,179,927，第 48 轮确认 compaction 缺陷）。** 增速 ~203/min（↓12%——早间高峰后回落）。COMPACTION=2 始终不变。
