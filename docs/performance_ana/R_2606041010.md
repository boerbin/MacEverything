# MacEverything 性能分析报告 R_2606041010

## 1. 本轮范围

- 任务启动时间：2026-06-04 10:10
- 报告文件：`docs/performance_ana/R_2606041010.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21978`
  - `SEG_LINES=951`
  - `LAST_LOG_TS=2026-06-04 10:10`

## 2. 执行摘要

**R86 灾难事件后完全恢复（第 3 轮）。** P50 ≈ 116ms，Max=318ms，**全部 16/16 查询 <500ms**。lockWaitMs 全部为 0。`.pdf`=225ms 回到正常波动区间。SKIPFLUSH 恢复 +12 稳态。

**tombstone 达到 441,932 条（7.23%，133MB）。** 本轮增量 +12,145——SKIPFLUSH 恢复稳态后，tombstone 增速也回归 12K/轮常态。

### 横向对比（最近 5 轮）

| Query | R85 | R86 | R87 | R88 | R89 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 174 | 367 | 14 | 6 | **52** | 正常 |
| `.pdf` | 1648 | 10989 | 474 | 353 | **225** | **完全恢复** |
| `*.swift` | 118 | 4072 | 224 | 149 | **117** | **完全恢复** |
| `hello world` | 72 | 450 | 230 | 252 | **218** | 正常 |
| `path:/Users` | 224 | 218 | 282 | 307 | **216** | 正常 |
| P50 | 104 | 361 | 114 | 112 | **116** | **完全恢复** |

### R86 灾难事件完整恢复追踪

| 轮次 | P50 | Max | >500ms | lockWait max | `.pdf` | 状态 |
|------|----:|----:|-------:|------------:|------:|------|
| **R86 (灾难)** | **361ms** | **2,731,551ms** | **5/16** | **1,010ms** | **10989ms** | **灾难** |
| R87 (恢复1) | 114ms | 474ms | 0/16 | 0ms | 474ms | 恢复中 |
| R88 (恢复2) | 112ms | 353ms | 0/16 | 0ms | 353ms | 近乎恢复 |
| **R89 (恢复3)** | **116ms** | **318ms** | **0/16** | **0ms** | **225ms** | **完全恢复** |

**恢复用时 3 轮。** 三次重大事件恢复对比：

| 事件 | 峰值 Max | 严重程度 | 恢复轮数 |
|------|--------:|---------|--------:|
| R67 | 10,902ms | 广泛 I/O 饱和（13/16 >500ms） | 6 轮 |
| R76 | 1,733ms | 中等（4/16 >500ms） | 3 轮 |
| **R86** | **2,731,551ms** | **单点极端（5/16 >500ms）** | **3 轮** |

R86 峰值为 R67 的 250 倍，但恢复同样仅需 3 轮——因为损害模式不同（单查询长锁 vs 广泛饱和）。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 52.3 | 0.0 | short-query-cache | 0 | 0 | 正常 |
| `.pdf` | 5 | 225.1 | 204.6 | advanced-trigram | 0 | 9949 | 正常 |
| `main.cpp` | 5 | 24.8 | 14.5 | advanced-trigram | 0 | 85 | 快 |
| `*.swift` | 5 | 116.8 | 113.9 | advanced-trigram | 0 | 9356 | 正常 |
| `ext:cpp` | 5 | 111.0 | 110.1 | advanced-ext-index | 0 | 3701 | 正常 |
| `size:>100m` | 5 | 92.5 | 92.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 217.9 | 212.1 | advanced-trigram | 0 | 3613 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 179.5 | 179.1 | advanced-trigram | 0 | 16011 | 正常 |
| `README` | 5 | 53.6 | 27.4 | advanced-trigram | 0 | 17679 | 正常 |
| `test` | 5 | 69.9 | 46.0 | advanced-trigram | 0 | 129957 | 正常 |
| `MacEverything` | 5 | 23.0 | 2.0 | advanced-trigram | 0 | 6803 | 极快 |
| `dm:today` | 5 | 57.8 | 57.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 183.2 | 173.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 215.6 | 202.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 318.2 | 317.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 22.8 | 0.0 | advanced-trigram | 0 | 0 | 正常 |

**P50 ≈ 116ms，P90 ≈ 318ms，Max = 318ms。全部 16/16 查询 <500ms。lockWaitMs 全部为 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,176,025，liveRecordCount=5,734,093（tombstone=**441,932**，**7.23%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=301  QUERYADV_GE500=44
FLUSH=0  COMPACTION=2  SKIPFLUSH=357
DIAG_TRIGRAM_SKIP=94
```

- QUERYADV 从 293 增至 301（+8）。
- QUERYADV_GE500 保持 44（连续第 3 轮无新增）。
- SKIPFLUSH 从 345 增至 357（**+12，恢复稳态**）。
- DIAG_TRIGRAM_SKIP 从 93 增至 94（+1）。

## 6. 问题清单

### Critical：tombstone 7.23%（441,932 条，133MB），稳态恢复

- 增长轨迹（近 5 轮）：
  ```
  R85: 406K (+23.9K) → R86: 409K (+2.7K) → R87: 417K (+8.3K)
  → R88: 430K (+12.7K) → R89: 442K (+12.1K)
  ```
- 增速恢复到稳态 ~12K/轮。R83-R85 加速期（19-24K/轮）可能对应特定系统活动窗口，非永久状态。
- **累计内存开销**：~442K × 300 字节 ≈ **133MB**。
- 连续第 29 轮超 2%，第 10 轮超过 5%，第 5 轮超过 6%，第 2 轮超过 7%。

### 趋势预测（按稳态 12K/轮）

| 未来轮次 | 预估 tombstone | 占比 | 内存 |
|---------|-------------:|-----:|-----:|
| +5 轮 | ~502K | 8.1% | 151MB |
| +10 轮 | ~562K | 8.9% | 169MB |
| +20 轮 | ~682K | 10.5% | 205MB |

### Low：`x`=52ms 轻微偏高

- 正常值 1-14ms，本轮 52ms。phase2=0ms 表明仍走 short-query-cache，但 GCD dispatch 有轻微延迟。
- 不构成异常（<100ms），仅作为 GCD 争抢的微弱信号记录。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（7.23%，133MB，R86 灾难已实证）

- **决定性证据已齐**：
  - R86 灾难：45 分钟服务不可用
  - 7.23% tombstone，133MB 内存
  - 稳态 +12K/轮永不收敛
  - 3 次重大 I/O 事件（R67、R76、R86）严重程度递增
- **修复方案**：SKIPFLUSH 分支增加 `tombstoneRatio > 0.02` 检查。
- **风险**：极低——compaction 已有实现，仅修改触发条件。

### P0 紧急：`size:>100m` 全表扫描超时保护

### P0：short-query-cache GCD 隔离（33% 异常率）

### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. 完全恢复是否持续。
2. tombstone 增速是否维持 12K/轮稳态。
3. 是否出现新 I/O 压力事件（日间活动窗口）。

## 9. 结论

**R86 灾难事件后完全恢复（第 3 轮）。** P50 ≈ 116ms，Max=318ms，全部 16/16 <500ms，lockWaitMs=0。`.pdf`=225ms 回到正常区间，`*.swift`=117ms 完全恢复。**恢复用时 3 轮**——与 R76 中等事件相同，远快于 R67 灾难的 6 轮（尽管 R86 峰值为 R67 的 250 倍）。tombstone=441,932（7.23%，133MB），增速恢复 12K/轮稳态。SKIPFLUSH 恢复 +12 稳态（357）。系统暂回正常状态，但 7.23% tombstone 意味着 **R86 级灾难可随时再次发生**——触发条件（全表扫描 + 内存压力 + 页面换出）不需要外部 I/O 事件，仅需 tombstone 继续累积即可自发触发。
