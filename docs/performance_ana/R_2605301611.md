# MacEverything 性能分析报告 R_2605301611

- **任务启动时间**：2026-05-30 16:11
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~29.1h，未重启
- **状态快照 @16:11**：total `5,945,648` / live `5,396,711`
- **本轮定性**：⚠️ **活跃摄入持续轮（第二轮）+ 突发型争用 → 分裂可洁净恢复**。摄入仍在进行（vs R95：total +24210、tomb **+23550 占新增 97.3%**、电池窗口内 totalRecords 实时增长 `5,945,918`→`5,946,078`），电池再次全程写锁争用（**每条路径 lockHeld≈total**：size 1375、abc 1331、path 1001、ext:cpp 748、*.swift 585）。**但与 R95 本质区别**：R95 摄入**稳态**、复测 30s 后仍突发不恢复 → 分裂判定挂起；**R96 摄入突发型（bursty）**、复测在两个争用层级**均洁净恢复**（标志复测 x→0.0、*.swift→1.38、ext:cpp→0.58）。**两轮独立复测（残余争用层 16:12 + 沉降热底层 16:14）双双确认二层分裂**（间隙 2.29× 与 2.24×，高度一致）→ **本轮分裂可洁净检验，战绩恢复推进 22/22**。唯一可靠恶化项 tombstone **548,937 / 9.23%**（连续 29 轮单调，**首破 9%**）。

---

## 1. 原始电池（重度写锁争用，⚠️ lockHeld≈total 全线）

| query | path | total(ms) | phase2 | lockHeld | cand | 备注 |
|---|---|---|---|---|---|---|
| `x` | short-query-cache | 5.39 | 0 | 0 | 0 | 缓存（争用轻微）|
| `.pdf` | advanced-trigram | 341.11 | 337.12 | **340.79** | 10125 | ⚠️ lockHeld≈total |
| `main.cpp` | advanced-trigram | 78.80 | 56.31 | **78.75** | 71 | ⚠️ 71 候选 78ms（marker，lockHeld 占满）|
| `*.swift` | advanced-trigram | **585.52** | 581.10 | **585.49** | 9213 | ⚠️ 写锁争用（>500ms 入日志）|
| `ext:cpp` | advanced-ext-index | **748.17** | 734.31 | **734.55** | 2987 | ⚠️ 常态 ~15ms（50× 争用）|
| `size:>100m` | pure-filter-soa-gcd | **1375.68** | 1375.02 | **1375.02** | 0 | ⚠️ **本轮最高，纯数值竟 1375ms**|
| `hello world` | advanced-trigram | 439.93 | 437.74 | **439.82** | 2342 | ⚠️ lockHeld≈total |
| `abc def ghi jkl mno pqr` | advanced-trigram | **1331.05** | 1329.96 | **1330.72** | 15925 | ⚠️ lockHeld≈total（>1s 入日志）|
| `README` | advanced-trigram | 48.54 | 41.97 | 48.41 | 16510 | 偏高 |
| `test` | advanced-trigram | 79.19 | 59.44 | 79.09 | 136419 | 偏高 |
| `MacEverything` | advanced-trigram | 23.99 | 10.71 | 23.94 | 6700 | |
| `dm:today` | pure-filter-soa-gcd | 111.27 | 111.19 | 111.19 | 0 | 数值，受争用 |
| `content:import` | pure-filter-soa-gcd | 297.27 | 278.57 | 278.57 | 0 | 字符串 |
| `path:/Users` | advanced-linear-gcd | **1001.95** | 997.41 | **997.41** | 0 | ⚠️ 纯字符串 1s（>1s 入日志）|
| `中文测试` | advanced-linear-gcd | 238.38 | 238.17 | 238.35 | 0 | 字符串，受争用 |
| `verylongquery…really` | advanced-trigram | 0.49 | 0 | 0.44 | 0 | |

**污染性质判定（同 R95，自身写锁争用）**：`size:>100m` 纯数值零候选竟 1375ms、其 lockHeld=1375.02ms 占满全部耗时——纯数值 SIMD 扫描本身从不可能 >100ms，唯一解释是查询全程在等索引写锁。`path:/Users` 同理（lockHeld 997ms）。**电池恰好落在摄入突发期**（16:12:00-16:12:11，日志确认 totalRecords 在此期间从 5,945,918 持续涨到 5,946,078）。

---

## 2. 复测消歧：两个争用层级双双洁净恢复（关键区别于 R95）

**第一轮 4× 复测（16:12，残余争用层——首探高、随即恢复）：**

| query | 电池值 | probe（4×）| median | 性质 |
|---|---|---|---|---|
| `x`（缓存）| 5.39 | 0.38 / 0.02 / 0.21 / 0.01 | 0.12 | ✅ 恢复 |
| `main.cpp`（71 cand）| 78.80 | 20.51 / 0.53 / 0.31 / 0.71 | 0.62 | ✅ 恢复 |
| `*.swift` | 585.52 | 83.88 / 6.22 / 15.97 / 1.2 | 11.10 | ✅ 恢复（首探残余）|
| `ext:cpp` | 748.17 | 79.9 / 19.04 / 13.8 / 1.29 | 16.42 | ✅ 恢复 |
| `size:>100m` | 1375.68 | 40.79 / 27.57 / 30.18 / 21.41 | **28.88** | ✅ 数值地板 |
| `dm:today` | 111.27 | 94.56 / 65.5 / 85.92 / 78.65 | **82.28** | ✅ 数值天花板 |
| `content:import` | 297.27 | 195.12 / 270.01 / 153.55 / 181.36 | **188.24** | ✅ 字符串地板 |
| `path:/Users` | 1001.95 | 341.66 / 177.41 / 188.26 / 204.65 | **196.45** | ✅ |
| `中文测试` | 238.38 | 229.87 / 286.12 / 222.46 / 241.1 | **235.49** | ✅ 字符串 |

**第二轮 4× 复测（16:14，等待 30s 沉降后——超热底层）：**

| query | settled probe（4×）| median | 性质 |
|---|---|---|---|
| `size:>100m` | 76.8 / 13.34 / 12.28 / 9.88 | **12.81** | 数值地板 |
| `dm:today` | 24.02 / 22.25 / 25.47 / 25.43 | **24.73** | 数值天花板 |
| `content:import` | 77.46 / 64.64 / 139.49 / 75.5 | **76.48** | 字符串 |
| `path:/Users` | 101.28 / 52.71 / 56.58 / 54.33 | **55.45** | 字符串地板 |
| `中文测试` | 61.1 / 66.2 / 54.65 / 52.92 | **57.88** | 字符串 |
| 标志复测 | x[0.34/0.02/0.0] main.cpp[7.14/60.25/0.54] *.swift[40.75/13.54/1.38] ext:cpp[36.12/2.72/0.58] | — | ✅ 全部洁净恢复 |

**与 R95 的决定性区别**：R95 摄入是**稳态**——30s 沉降后复测仍突发（path 648→165、content 287→182、中文 227→575→184），不能洁净恢复，故分裂判定挂起。**R96 摄入是突发型**——两个层级的复测均收敛、标志项 x→0.0/*.swift→1.38/ext:cpp→0.58 洁净，证明争用窗口短、窗口间可洁净读取。这使 R96 **可以洁净检验分裂**。

---

## 3. 洁净 ns/槽：两轮复测取值（records=5,947,377）

**第一轮（16:12，正常区间，与历史 R90-R94 对齐）：**

| 路径 | 类型 | median | 洁净值 |
|---|---|---|---|
| `size:>100m` | 数值 | 28.88ms | **4.86** |
| `dm:today` | 数值 | 82.28ms | **13.84** |
| `content:import` | 字符串 | 188.24ms | **31.65** |
| `path:/Users` | 字符串 | 196.45ms | **33.03** |
| `中文测试` | 字符串 | 235.49ms | **39.59** |

**第二轮（16:14，超热底层，绝对值约为常态半数）：**

| 路径 | 类型 | median | 洁净值 |
|---|---|---|---|
| `size:>100m` | 数值 | 12.81ms | **2.15** |
| `dm:today` | 数值 | 24.73ms | **4.16** |
| `content:import` | 字符串 | 76.48ms | **12.86** |
| `path:/Users` | 字符串 | 55.45ms | **9.32** |
| `中文测试` | 字符串 | 57.88ms | **9.73** |

**第一轮取值匹配历史正常区间**（size 4.86=R94 4.67、dm 13.84≈R90 13.56、content 31.65≈R92 30.99），作为本轮**官方洁净读数**；第二轮超热底层（30s 沉降+前序密集触达使全部页常驻）取值约半，仅作**独立确认**。

---

## 4. 绝对二层分裂：两个争用层级双双确认（22/22，间隙 2.29× / 2.24×）

| 轮次 | 最高数值 | 最低字符串 | 间隙 | 判定 |
|---|---|---|---|---|
| 第一轮（正常区间）| dm 13.84 | content 31.65 | **2.29×** | ✅ 不破 |
| 第二轮（超热底层）| dm 4.16 | path 9.32 | **2.24×** | ✅ 不破 |

- **双层级一致**：间隙 2.29× 与 2.24× 高度吻合，证明分裂是**机制级恒定差**——无论页温高低（正常 vs 超热），纯数值零解引用始终 ~2.25× 低于字符串解引用散布串池。
- **这是比 R95 更强的证据**：R95 因稳态摄入无法检验分裂；R96 突发型摄入提供了**两个独立争用层级的洁净快照**，二者**同时**确认分裂——相当于一次「双盲复测」。
- **战绩推进 22/22**（R72→R96，间隙区间 2.11×~3.49×），R95 的挂起在本轮恢复检验。
- 字符串层内部第一轮 content<path<中文、第二轮 path<中文<content，排序重排——再证 R79「字符串层内部细排序为逐区冷热噪声」。

---

## 5. 本轮头条：活跃摄入持续第二轮，突发型争用（自身根因延续）

| 证据 | R95（稳态摄入）| **R96（突发摄入）** |
|---|---|---|
| total 增量（30min）| +25055 | +24210 |
| live 增量 | +3476 | **+660**（更少净增）|
| tomb 增量 | +21579 | **+23550（占新增 97.3%）**|
| 电池窗口 totalRecords 增长 | 5,921,699→5,922,751 | **5,945,918→5,946,078** |
| lockHeld 特征 | *.swift 1290≈total | **全线 lockHeld≈total**（size 1375/abc 1331/path 1001）|
| 复测恢复 | 30s 后仍突发不恢复 | **两层级均洁净恢复** |
| 分裂判定 | 挂起 | **可洁净检验（22/22）**|

摄入**仍在进行**（R95→R96 又一个摄入密集 30min），且本轮 tomb 占新增 **97.3%**——几乎全部新记录是更新/删除（旧记录变 tombstone）。但本轮争用呈**突发型**（短窗口反复），与 R95 稳态不同，故复测可在窗口间洁净读取。**根因同 R95**：后台索引写入持锁 → 读查询被阻塞。

---

## 6. 延迟分布：1-5s/0.5-1s 继续累积（摄入争用第二轮）

| 段 | R94（冻结）| R95 | **R96** | 变化 |
|---|---|---|---|---|
| >5s | 7 | 7 | **7** | 冻结（启动事件已排除）|
| 1-5s | 23 | 24 | **27** | **+3**（size 1375/abc 1331/path 1001）|
| 0.5-1s | 16 | 22 | **24** | **+2**（*.swift 585/ext:cpp 748）|
| 0.1-0.5s | — | 325 | 330 | |
| QueryAdvanced 总数 | 334 | 378 | **388** | +10 |
| 中位 | ~220 | 221 | **222** | 稳定 |

**本轮 5 条新增 >500ms 查询（全部落在 16:12 摄入突发窗口）**：`*.swift` 585ms、`ext:cpp` 748ms、`size` 1375ms、`abc` 1331ms、`path` 1001ms——日志均带 totalRecords 实时增长戳记，与摄入窗口精确对齐。本会话 >500ms 查询累计 12 条（R95 窗口 7 + R96 窗口 5），全部由摄入突发期的电池/复测/用户查询贡献，**>5s 段与历史查询天花板（18582ms）仍未触及**。

---

## 7. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **503 次**（R95 494）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100，`:159` tombstone 比例阀结构性永不可达——全会话 0 触发、0 回收。**连续两轮活跃摄入大批生成 tombstone（R95+R96 共 +45,129），却无任何回收兜底**，使本根因紧迫性陡增。

---

## 8. tombstone 趋势：首破 9%，连续 29 轮单调

| 轮次 | tombstone | ratio |
|---|---|---|
| R92@14:11 | 482,158 | 8.21% |
| R93@14:41 | 491,103 | 8.35% |
| R94@15:11 | 503,808 | 8.54% |
| R95@15:41 | 525,387 | 8.87% |
| **R96@16:11** | **548,937** | **9.23%** |

会话内单调递增、永不回缩。R43 参考崩溃点 12.97%（~800K），当前 9.23% 已过 ~71% 程。**连续两轮摄入加速**（R94→R95 +21,579、R95→R96 +23,550，远超此前 ~12K/轮），实时读已达 **9.26%**（550,649/5,947,377）。本轮 tomb 占新增 **97.3%**——摄入几乎纯产生 tombstone。这是会话内唯一可靠的真实恶化趋势，根因 R67 阀门不可达明确，摄入加速使其紧迫性持续陡升。

---

## 9. 日志健康度

- WARN = **0**，ERROR = **0**（全会话，锁争用是性能问题非正确性问题）
- QueryAdvanced = **388**（R95 378，+10）
- 查询峰值 top-7 `[18582, 17928, 15749, 8205, 7544, 6811, 6061]` —— 仍**逐字节相同**（本轮最高查询 1375ms 远低于冻结历史峰值）
- 延迟分布：>5s=**7**（冻结）、1-5s=**27**（+3）、0.5-1s=**24**（+2）、0.1-0.5s=330、中位 **222ms**

---

## 10. 优化建议（P0 #2 读写分离持续强化）

1. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调 <12.97%（建议 8–10%）。当前 **首破 9%**（实时 9.26%），连续两轮摄入加速逼近，紧迫性最高。
2. **🔥 P0｜读写锁分离 / 无锁快照读（连续两轮实证强化）**：R95+R96 连续两轮证明自身摄入持索引写锁会饿死读查询（本轮**全线** lockHeld≈total）。**应引入读写分离**——读查询走不可变快照、写入走 copy-on-write 或双缓冲，使摄入期间读路径不被阻塞。这是连续两轮新证据共同指向的最高价值修复，优先级高于 mlock。
3. **🔥 P0｜查询超时保护 + 降级返回**：R95（1290ms）+ R96（size 1375ms / abc 1331ms / path 1001ms）——摄入争用致单查询 >1s，超时熔断必要。
4. **🔥 P0｜mlock/madvise 整体常驻热区**：对外部停顿有效；但本轮争用源自锁而非页换出，优先级让位于第 2 项。
- **P0｜HTTP 并发**：acceptLoop 单线程，摄入争用期间串行阻塞放大影响。
- **P1🔥｜CJK trigram 索引**：`中文测试` 走 linear-gcd 全扫。

---

## 11. 结论

- **定性**：⚠️ 活跃摄入持续第二轮 + 突发型争用。摄入仍在进行（tomb +23550 占新增 97.3%、电池窗口内 totalRecords 实时增长、全线 lockHeld≈total），根因同 R95（自身写锁饿死读），但争用呈突发型而非稳态。
- **诚实修正纪律（本轮亮点）**：与 R95「分裂挂起」相反，R96 突发型摄入提供**两个独立争用层级的洁净复测快照**——正常区间（dm 13.84<content 31.65，2.29×）与超热底层（dm 4.16<path 9.32，2.24×）**双双确认分裂**，间隙高度一致 → **分裂可洁净检验，战绩恢复推进 22/22**。这证明：摄入争用是否阻断分裂检验，取决于其稳态/突发性质，而非分裂机制本身动摇。
- **测量一致性**：>5s 段仍冻结于 7（启动 Phase-2 replay 20765ms 与启动 flush 10353ms 非查询，已正确排除），延续 R95 校正口径。
- **唯一可靠恶化项**：tombstone **548,937 / 9.23%**（连续 29 轮单调，**首破 9%**，实时 9.26%），连续两轮摄入加速（+21,579 / +23,550），本轮 tomb 占新增 97.3%，根因 R67 阀门结构性不可达明确。
- **最高价值修复**：连续两轮实证把 **P0 #2「读写锁分离 / 无锁快照读」**钉为最高价值修复——直接针对「写锁饿死读」根因，优先级高于 mlock。
