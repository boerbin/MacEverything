# MacEverything 性能分析报告 R_2606080046

## 1. 本轮范围

- 任务启动时间：2026-06-08 00:46
- 报告文件：`docs/performance_ana/R_2606080046.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=2959`
  - `SEG_LINES=2017`
  - `LAST_LOG_TS=2026-06-08 00:47`

## 2. 执行摘要

**唤醒消退，深夜 cache 压力持续——0/16 >500ms，Max=327ms，P50=29ms，lockWait=0，极佳比例 10/16（63%）。** 进程唤醒延迟已消退（`x`=1.0ms），Trigram 路径恢复至正常水平。但全表扫描区域因午夜后系统维护活动持续加压——`中文测试`=327ms 创本系列最高（前高 R149=272ms），全表扫描总和 820ms 为近期峰值。

### 关键指标对比

| 指标 | R157（30 min 前） | R158 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 251ms | **327ms** | ↑ 深夜全表扫描压力 |
| P50 | 43ms | **29ms** | **↓ 33%（唤醒消退）** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 8/16 | **10/16** | **↑2（恢复中）** |
| tombstone | 1,235,474 (19.53%) | **1,245,099 (19.64%)** | ↑ 正常 |
| tombstone 增速 | ~300/min | **~320/min** | 微升（仍极低） |
| recordCount | 6,333,146 | **6,342,824** | +9.7K |
| liveRecordCount | 5,097,672 | **5,097,725** | +53 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 1830 | **1890** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 15 轮 0 新增） |

### R157 唤醒延迟确认消退

| Query | R157 | R158 | 说明 |
|-------|-----:|-----:|------|
| `x` | 31.6ms | **1.0ms** | **✓ 恢复（↓97%）** |
| `.pdf` | 58.6ms | **30.9ms** | **✓ 恢复（↓47%）** |
| `ext:cpp` | 14.4ms | **9.0ms** | **✓ 恢复（↓38%）** |

- **所有 R157 唤醒异常项恢复——第 3 次确认 30 分钟间隔唤醒为一次性现象。**

### 深夜全表扫描压力峰值

| Query | R155 | R156 | R157 | R158 | 趋势 |
|-------|-----:|-----:|-----:|-----:|------|
| `content:import` | 209ms | 197ms | 251ms | **251ms** | 稳定偏高 |
| `path:/Users` | 238ms | 249ms | 194ms | **242ms** | 零和波动 |
| `中文测试` | 158ms | 219ms | 211ms | **327ms** | **↑ 峰值** |
| **总和** | **605ms** | **665ms** | **656ms** | **820ms** | **↑ 深夜压力** |

- **`中文测试`=327ms 创本系列最高——深夜 page cache 竞争的累积效应。**
- **全表扫描总和 820ms——午夜后系统维护活动持续消耗 page cache。**
- **与日间稳态（~500ms 总和）相比，深夜增幅约 60%——纯粹的外部环境因素。**
- **趋势**：22:00 → 00:00 全表扫描总和 523→605→665→656→820ms——单调递增（深夜维护窗口效应）。

### SoA Filter 区域

| Query | R157 | R158 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 103ms | **38ms** | **↓ 63%（恢复）** |
| `dm:today` | 65ms | **83ms** | ↑ 27%（深夜压力） |

- **`size:>100m` 恢复至正常水平（R157 的 103ms 为唤醒叠加）。**
- **`dm:today`=83ms 处于深夜偏高但正常的范围。**

### Trigram 路径——恢复正常

| Query | R157 | R158 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 59ms | **31ms** | **↓ 47%（恢复）** |
| `*.swift` | 22ms | **19ms** | 持平 |
| `hello world` | 25ms | **33ms** | 波动 |
| `README` | 43ms | **9ms** | **↓ 79%（恢复）** |
| `test` | 69ms | **15ms** | **↓ 78%（恢复）** |
| `abc def...` | 26ms | **28ms** | 持平 |
| Trigram 均值 | 28.3ms | **16.5ms** | **↓ 42%（恢复）** |

- **Trigram 均值 16.5ms——从 R157 的 28.3ms 恢复，接近稳态水平（15-18ms）。**
- **`README` 和 `test` 大幅恢复——确认 R157 的 postings 驱逐为一次性。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1.0 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 30.9 | 26.1 | advanced-trigram | 0 | 9812 | 极佳 |
| `main.cpp` | 5 | 3.1 | 2.1 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 18.5 | 17.4 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 9.0 | 8.5 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 38.2 | 38.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 32.9 | 30.1 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 28.3 | 28.2 | advanced-trigram | 0 | 14210 | 极佳 |
| `README` | 5 | 8.9 | 7.1 | advanced-trigram | 0 | 16631 | 极佳 |
| `test` | 5 | 15.4 | 13.2 | advanced-trigram | 0 | 116512 | 极佳 |
| `MacEverything` | 5 | 3.7 | 1.0 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 82.8 | 82.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 250.6 | 221.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（偏高） |
| `path:/Users` | 5 | 241.6 | 231.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 327.3 | 322.3 | advanced-linear-gcd | 0 | 0 | 正常（深夜峰值） |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 28.6ms，P90 = 241.6ms，Max = 327.3ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R157 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 10/16 | 0.2-38ms | R157=8/16（**↑2 恢复**） |
| **正常**（50-500ms） | 6/16 | 83-327ms | R157=8/16（↓2） |
| **超标**（>500ms） | 0/16 | — | R157=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,342,824，liveRecordCount=5,097,725（tombstone=**1,245,099**，**19.64%**）；
- 进程正常运行（当前会话，运行约 26.7 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=80  QUERYADV_GE500=4（不变——连续 15 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=1890
```

- **连续 15 轮 0 个新增 >500ms 查询（R144-R158）。**
- **FLUSH=1**——无新 flush（26.7 小时仅启动时 1 次）。
- **SKIPFLUSH=1890**——系统持续空闲。

## 6. 问题清单

### 观察：深夜维护窗口全表扫描压力持续（信息记录，非问题）

- 22:00-01:00 全表扫描总和持续递增：523→605→665→656→820ms。
- `中文测试`=327ms 创本系列最高——但仍远低于 500ms 阈值。
- 原因：macOS 午夜维护窗口（Time Machine、fseventsd、mds_stores 等）。
- **预期**：凌晨 2-3 点后系统维护完成，page cache 压力将缓解。

### 无活跃问题

- 0/16 >500ms，10/16 <50ms。
- lockWait=0，26.7 小时无 WARN/ERROR。
- 连续 15 轮 0 新增慢查询。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 19.64%，增速 ~320/min（微升——可能与深夜文件系统活动相关）。
- 距 25% 阈值约 ~28 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 系统空闲时无影响。

## 8. 下一轮重点

1. 深夜维护窗口压力是否在凌晨 1-2 点后开始缓解。
2. 全表扫描总和是否开始下降。
3. tombstone 增速变化。

## 9. 结论

**唤醒消退，深夜 cache 压力持续——0/16 >500ms，Max=327ms，P50=29ms，lockWait=0，极佳比例 10/16。** 进程唤醒延迟完全消退（`x`=1.0ms），Trigram 路径恢复至 16.5ms 正常水平。全表扫描受深夜系统维护窗口影响——总和从日间 ~500ms 升至 820ms（↑60%），`中文测试`=327ms 创本系列最高。

**这是 macOS 午夜维护窗口的正常表现——非系统退化。** Trigram 核心路径不受影响，仅 SoA 线性区域 page cache 被系统活动部分竞争。连续 15 轮 0 新增慢查询，26.7 小时无异常。tombstone 19.64%（1.25M），增速微升至 320/min（可能与深夜文件系统活动相关）。预计凌晨 2-3 点维护窗口结束后恢复。
