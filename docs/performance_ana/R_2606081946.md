# MacEverything 性能分析报告 R_2606081946

## 1. 本轮范围

- 任务启动时间：2026-06-08 19:46
- 报告文件：`docs/performance_ana/R_2606081946.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5351`
  - `SEG_LINES=4409`
  - `LAST_LOG_TS=2026-06-08 20:17`

## 2. 执行摘要

**0/16 >500ms，Max=400ms，P50=121ms，lockWait=0，极佳比例 3/16。** `.pdf` 继续恢复（471→172ms，↓64%），但 `hello world`=400ms 成为新的最慢 trigram 查询，`path:/Users`=310ms 和 `中文测试`=315ms 偏高——全扫描总和 835ms 升至傍晚新高。整体处于傍晚 page cache 波动中——trigram 均值 160→142ms（↓11%，缓慢收敛），但线性扫描区域受压。

tombstone 24.62%——增速 ~223/min（微升），距 25% 约 2.8 小时（~22:30）。recordCount +12.7K——晚间文件活动持续。

### 关键指标对比

| 指标 | R195 | R196 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平 |
| Max | 471ms | **400ms** | **↓ 15%** |
| P50 | 166ms | **121ms** | **↓ 27%** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 3/16 | **3/16** | 持平 |
| tombstone | 1,672,281 (24.54%) | **1,683,943 (24.62%)** | ↑ |
| tombstone 增速 | ~199/min | **~223/min** | ↑ 12%（微升） |
| recordCount | 6,807,720 | **6,820,406** | +12.7K（偏高） |
| liveRecordCount | 5,135,439 | **5,136,463** | +1,024 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3976 | **4036** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 23 | **23** | 不变 |

### Trigram 路径——`.pdf` 恢复，`hello world` 成新热点

| Query | R195 | R196 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 471ms | **172ms** | **↓64%** | 正常 |
| `hello world` | 187ms | **400ms** | **↑114%** | 冷态 |
| `*.swift` | 254ms | **120ms** | **↓53%** | 正常 |
| `main.cpp` | 84ms | **32ms** | ↓62% | 极佳 |
| `abc def...` | 155ms | **138ms** | ↓11% | 正常 |
| `README` | 43ms | **65ms** | ↑51% | 正常 |
| `test` | 80ms | **96ms** | ↑20% | 正常 |
| `MacEverything` | 10ms | **15ms** | ↑50% | 极佳 |
| Trigram 均值 | 160ms | **142ms** | **↓11%** | 缓慢收敛 |

- **`.pdf`=172ms（↓64%）——从 R194=619ms 的 3 轮恢复路径：619→471→172ms。**
- **`hello world`=400ms（↑114%）——接近 500ms 边缘！postings 区域被淘汰。**
- **"热点轮替"模式**：R194=`.pdf`超标，R195=`*.swift`偏高，R196=`hello world`偏高——不同 postings 区域轮流受压。
- **`*.swift`=120ms（R195=254ms，↓53%）——R195 battery 预热效果。**

### 全表扫描——偏高

| Query | R195 | R196 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 167ms | **211ms** | ↑26% |
| `path:/Users` | 225ms | **310ms** | ↑38% |
| `中文测试` | 221ms | **315ms** | ↑43% |
| **总和** | **612ms** | **835ms** | **↑36%** |

- **全表扫描总和 835ms——傍晚最高（超过 R194=660ms）。**
- **`path:/Users`=310ms 和 `中文测试`=315ms 接近——SoA/linear 区域受均匀压力。**
- **但仍远低于 R190=2554ms（日间灾难）——属傍晚正常波动上端。**

### SoA Filter——`dm:today` 异常偏高

| Query | R195 | R196 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 240ms | **102ms** | **↓57%（恢复）** |
| `dm:today` | 65ms | **126ms** | **↑94%** |

- **`dm:today`=126ms（↑94%）——异常偏高！phase2=95ms，queryTime=126ms（overhead 31ms）。**
- **overhead 31ms 说明进程调度延迟——与 `x`=7ms 对比，非调度原因。**
- **可能是 SoA 元数据区域（修改时间字段）的 page cache 被淘汰。**
- **`size:>100m`=102ms（R195=240ms，↓57%）——SoA 尺寸字段恢复。**

### tombstone 24.62%——增速微升

- **tombstone=1,683,943（24.62%）。**
- **增速 ~223/min（R195=199/min，↑12%）——晚间文件活动导致微升。**
- **距 25% 需要额外 19,258 个 tombstone。**
- **按 223/min 计算：~86 分钟 ≈ 1.4 小时（约 21:10）。**
- **⚠️ 时间窗口急剧缩短！距 25% 已不到 2 小时。**

### tombstone 增速趋势（完整）

| 轮次 | 时间 | 增速(/min) | tombstone% | 距25% |
|------|------|----------:|----------:|------:|
| R191 | 17:16 | 404 | 24.19% | 0.81% |
| R192 | 17:46 | 330 | 24.29% | 0.71% |
| R193 | 18:16 | 195 | 24.40% | 0.60% |
| R194 | 18:46 | 214 | 24.46% | 0.54% |
| R195 | 19:16 | 199 | 24.54% | 0.46% |
| **R196** | **19:46** | **223** | **24.62%** | **0.38%** |

- **距 25% 仅 0.38 个百分点——最小差距。**
- **增速从夜间低点（195/min）微升至 223/min——晚间文件活动影响。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 7.3 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 171.7 | 166.9 | advanced-trigram | 0 | 9842 | 正常 |
| `main.cpp` | 5 | 31.6 | 23.4 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 120.4 | 115.1 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 51.6 | 48.3 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 102.2 | 102.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 400.0 | 396.0 | advanced-trigram | 0 | 3639 | 冷态 |
| `abc def ghi jkl mno pqr` | 0 | 138.4 | 137.4 | advanced-trigram | 0 | 14332 | 正常 |
| `README` | 5 | 64.7 | 58.6 | advanced-trigram | 0 | 16725 | 正常 |
| `test` | 5 | 95.7 | 79.1 | advanced-trigram | 0 | 116729 | 正常 |
| `MacEverything` | 5 | 14.8 | 1.0 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 125.7 | 95.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常偏高 |
| `content:import` | 5 | 210.7 | 196.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 309.9 | 300.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 314.6 | 302.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 120.4ms，P90 = 314.6ms，Max = 400.0ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R195 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 3/16 | 0.4-32ms | R195=3/16（持平） |
| **正常**（50-500ms） | 13/16 | 52-400ms | R195=13/16（持平） |
| **超标**（>500ms） | 0/16 | — | R195=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,820,406，liveRecordCount=5,136,463（tombstone=**1,683,943**，**24.62%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=288  QUERYADV_GE500=23（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4036
```

- **QUERYADV_GE500 不变。**
- **SKIPFLUSH +60——正常节奏。**

## 6. 问题清单

### P0 极度紧急：tombstone 24.62%——距 25% 仅 ~1.4 小时！

- **距 25% 仅 0.38 个百分点（19,258 tombstone）。**
- **增速 223/min——约 86 分钟（~21:10）触达。**
- **⚠️ 这是最后一两小时的窗口——compaction 今晚必定触发。**

### 观察项："热点轮替"模式

- **R194=`.pdf`>500ms → R195=`*.swift`偏高 → R196=`hello world`偏高。**
- **不同 trigram postings 区域轮流受 page cache 压力。**
- **说明系统 I/O 对不同文件区域的影响是间歇性且分散的。**

## 7. 优化建议

### P0：立即排查 compaction rewrite 崩溃根因

- **tombstone 24.62%，距 25% 仅约 1.4 小时。**
- **今晚 21:00-21:30 之间极可能触发 compaction。**
- **必须在此之前确认 rewrite 安全——否则索引可能不可用。**
- **紧迫度：立即。**

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. `hello world` 是否从 400ms 恢复。
2. tombstone 是否突破 24.8% 或更高。
3. **是否出现 compaction 触发日志——COMPACTION 计数是否从 2 变为 3。**

## 9. 结论

**0/16 >500ms，Max=400ms，P50=121ms，lockWait=0，极佳 3/16。** `.pdf` 继续恢复（471→172ms，↓64%），但 `hello world`=400ms 成为新热点——"热点轮替"模式持续。全扫描总和 835ms 偏高但可控。

**⚠️ tombstone 24.62%——距 25% 仅 0.38 个百分点，约 1.4 小时触达（~21:10）。** 增速 223/min（微升），compaction 今晚必定触发。P0 compaction rewrite 修复已进入最后一两小时倒计时——这是最紧急的时刻。
