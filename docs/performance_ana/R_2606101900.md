# MacEverything 性能分析报告 R_2606101900

## 1. 本轮范围

- 任务启动时间：2026-06-10 19:00
- 报告文件：`docs/performance_ana/R_2606101900.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9277`
  - `SEG_LINES=8335`
  - `LAST_LOG_TS=2026-06-10 17:17`

## 2. 执行摘要

**⚠️ `path:/Users`=528ms（>500ms！post-compaction 首次超标）。lockWait=9.5ms 复现。** 1/16 >500ms，Max=528ms，P50=41.2ms，lockWait max=9.5ms（`中文测试`）。**极佳 8/16（post-compaction 新低）。** `x` 缓存稳定（3ms，连续 4 轮）。`path:/Users` 157→528ms（↑236%——page cache 大面积淘汰 + lockWait=9.5ms 叠加）。Trigram 大 postings 集持续偏高：`.pdf`=79ms, `hello world`=63ms。全表扫描总和 971ms（↑65% from R263=588ms——显著恶化）。FLUSH 38→43（+5）。COMPACTION 6→8（+2——新增 compaction 事件！）。QUERYADV_GE500=76（+1）。tombstone=72,277（1.37%——增速加快）。

| 指标 | R263 | R264 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **1/16** | **↑1（post-compaction 首次！）** |
| Max | 253ms | **528ms** | **↑109%** |
| P50 | 41ms | **41.2ms** | 持平 |
| lockWait max | 0ms | **9.5ms** | ↑（`中文测试`） |
| 极佳比例(<50ms) | 9/16 | **8/16** | ↓1（post-compaction 新低） |
| tombstone | 58,042 (1.10%) | **72,277 (1.37%)** | ↑（增速加快） |
| recordCount | 5,302,352 | **5,317,781** | +15.4K（偏高！） |
| liveRecordCount | 5,244,310 | **5,245,504** | +1,194（正常） |
| FLUSH 累计 | 38 | **43** | +5 |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 6 | **8** | **+2（新增 compaction！）** |
| QUERYADV_GE500 | 75 | **76** | **+1（`path:/Users`=528ms）** |

### ⚠️ `path:/Users`=528ms——post-compaction 首次 >500ms

- **R263=157ms → R264=528ms（↑236%）。**
- **phase2=514ms——几乎全部是搜索时间（非 overhead）。**
- **lockWait=0（此查询无锁等待）——纯粹的 page cache 淘汰导致 I/O。**
- **可能原因：**
  1. **recordCount +15.4K（偏高——大量文件系统活动冲刷了 page cache）。**
  2. **COMPACTION +2（compaction I/O 抢占了 SoA 数据区域的 page cache）。**
  3. **全表路径扫描需要遍历 5.2M+ 条记录的路径字段——I/O 密集。**
- **这是 R251（I/O 风暴 `.pdf`=2829ms）的小规模重现——同样的 page cache 淘汰模式。**
- **预计 1-round recovery（100% 历史成功率，15/15）。**

### lockWait=9.5ms（`中文测试`）

- **`中文测试` lockWait=9.5ms——flush 写锁短暂阻塞。**
- **R262=3.0ms（`ext:cpp`），R264=9.5ms（`中文测试`）——flush 锁时间在增长。**
- **FLUSH +5 期间的正常锁竞争——但趋势需关注。**
- **lockWait 序列：R257=325.7ms → R258-R261=0 → R262=3ms → R263=0 → R264=9.5ms。**

### COMPACTION +2 分析

- **COMPACTION 6→8（+2）——新增 2 次 compaction 事件。**
- **tombstone=72,277（1.37%）——远低于 25% 阈值。**
- **可能是 compaction check 日志（检查但未执行），或者 minor compaction。**
- **R261 也出现 COMPACTION +2（4→6）——可能是 flush 后常规 compaction 检查。**

### `x` 缓存——连续 4 轮稳定

| 轮次 | `x` 路径 | `x` 时间 |
|------|---------|--------:|
| R261 | short-query-cache | 17ms |
| R262 | short-query-cache | 1ms |
| R263 | short-query-cache | 2ms |
| **R264** | **short-query-cache** | **3ms** |

- **连续 4 轮 short-query-cache——完全稳定。**

### Trigram 路径——大 postings 集持续偏高

| Query | R263 | R264 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 117ms | **79ms** | ↓32%（恢复中） | 正常 |
| `hello world` | 113ms | **63ms** | ↓44%（恢复中） | 正常 |
| `*.swift` | 32ms | **41ms** | ↑28% | 极佳 |
| `main.cpp` | 9ms | **12ms** | ↑33% | 极佳 |
| `abc def...` | 41ms | **61ms** | ↑49% | 正常 |
| `README` | 8ms | **19ms** | ↑138% | 极佳 |
| `test` | 23ms | **27ms** | ↑17% | 极佳 |
| `MacEverything` | 48ms | **18ms** | ↓63% | 极佳 |
| Trigram phase2均值 | ~44ms | **~33ms** | ↓25%（恢复中） |

- **`.pdf` 117→79ms（↓32%）和 `hello world` 113→63ms（↓44%）——逐步恢复。**
- **`abc def`=61ms（↑49%——14K candidates 的 postings 页被淘汰）。**
- **phase2 均值 44→33ms（↓25%——恢复中但仍偏高）。**

### 全表扫描——显著恶化

| Query | R263 | R264 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 178ms | **183ms** | ↑3% |
| `path:/Users` | 157ms | **528ms** | **↑236%（>500ms！）** |
| `中文测试` | 253ms | **260ms** | ↑3% |
| **总和** | **588ms** | **971ms** | **↑65%** |

- **全表扫描 588→971ms（↑65%）——`path:/Users`=528ms 拉高。**
- **`content:import` 和 `中文测试` 基本持平——仅路径扫描受影响。**
- **路径扫描需要遍历完整路径字段（平均 ~80 字节/条 × 5.2M = ~400MB），page cache 缺失时 I/O 量大。**

### 零和总量

| 类别 | R263 | R264 | 变化 |
|------|-----:|-----:|------|
| Trigram 总和 | ~400ms | ~301ms | -99ms |
| 全表扫描总和 | 588ms | 971ms | +383ms |
| **Battery 总和** | ~1120ms | ~1412ms | **↑26%** |

- **Battery 总和 1120→1412ms（↑26%）——`path:/Users` 异常拉高。**
- **去除 `path:/Users` 后：总和 ≈884ms（与 R263 的 963ms 相比 ↓8%——正常）。**

### recordCount +15.4K 分析

- **R263=+8.9K → R264=+15.4K（↑73%——文件系统活动突增）。**
- **liveRecordCount +1.2K（正常——大部分新 records 替换旧版本）。**
- **tombstone 增量：72,277-58,042=14,235（vs R263=8,488）——增速 ↑68%。**
- **高 recordCount 增量导致更多 WAL entries → 更多 flush → 更多 page cache 竞争。**

### 辅助路径

| Query | R263 | R264 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 2ms | **3ms** | ↑50%（微小） | 极佳 |
| `ext:cpp` | 18ms | **24ms** | ↑33% | 极佳 |
| `size:>100m` | 41ms | **33ms** | ↓20% | 极佳 |
| `dm:today` | 54ms | **62ms** | ↑15% | 正常 |
| `verylongquery...` | 26ms | **0.5ms** | ↓98%（恢复正常） | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 2.7 | 0.0 | short-query-cache | 0 | 0 | 极佳（稳定） |
| `.pdf` | 5 | 78.6 | 74.7 | advanced-trigram | 0 | 9788 | 正常（恢复中） |
| `main.cpp` | 5 | 12.2 | 6.0 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 41.2 | 32.6 | advanced-trigram | 0 | 9405 | 极佳 |
| `ext:cpp` | 5 | 24.1 | 23.1 | advanced-ext-index | 0 | 3611 | 极佳 |
| `size:>100m` | 5 | 32.8 | 32.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 62.9 | 59.7 | advanced-trigram | 0 | 3930 | 正常（恢复中） |
| `abc def ghi jkl mno pqr` | 0 | 60.8 | 60.1 | advanced-trigram | 0 | 14456 | 正常 |
| `README` | 5 | 18.6 | 12.4 | advanced-trigram | 0 | 17431 | 极佳 |
| `test` | 5 | 26.8 | 18.1 | advanced-trigram | 0 | 117727 | 极佳 |
| `MacEverything` | 5 | 17.6 | 1.6 | advanced-trigram | 0 | 6941 | 极佳 |
| `dm:today` | 5 | 62.0 | 61.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 183.3 | 168.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | **528.1** | 514.4 | advanced-linear-gcd | 0 | 0 | **⚠️ >500ms** |
| `中文测试` | 0 | 259.6 | 248.5 | advanced-linear-gcd | 9.5 | 0 | 正常（lockWait） |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 41.2ms，P90 = 259.6ms，Max = 528.1ms。1/16 查询 >500ms。lockWait max = 9.5ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R263 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **8/16** | 0.5-41ms | R263=9/16（↓1） |
| **正常**（50-500ms） | 7/16 | 61-260ms | R263=7/16（持平） |
| **超标**（>500ms） | **1/16** | 528ms | R263=0/16（**↑1**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,317,781，liveRecordCount=5,245,504（tombstone=**72,277**，**1.37%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=685  QUERYADV_GE500=76（+1！`path:/Users`=528ms）
FLUSH=43（+5）  COMPACTION=8（+2）  SKIPFLUSH=7266
```

## 6. 问题清单

### P0：flush 写锁阻塞

- **lockWait=9.5ms（趋势：0→3→0→9.5ms——间歇性复现）。**
- **FLUSH 持续高频（+5~6/轮）。**

### P0：compaction 触发机制缺陷（根因未修复）

### P0：I/O 优先级保护

- **`path:/Users`=528ms——page cache 淘汰导致 I/O 风暴小规模重现。**
- **高 recordCount 增量（+15.4K）加剧 page cache 竞争。**

### P2：short-query-cache 重建延迟（已稳定——连续 4 轮）

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（增加独立定时检查）

### P0：进程 QoS / I/O 优先级保护

- **文件系统活动突增（+15.4K records）导致 page cache 竞争加剧。**
- **需要 I/O 优先级保护防止搜索 I/O 被 flush/compaction 抢占。**

### P2：madvise MADV_WILLNEED 热区维护

### P2：short-query-cache flush 后增量更新

## 8. 下一轮重点

1. `path:/Users` 是否 1-round recovery（预计恢复——15/15 历史成功率）。
2. lockWait 趋势（0→3→0→9.5→?）。
3. recordCount 增速是否回归正常（+15.4K 偏高）。
4. Trigram 大 postings 集是否继续恢复。

## 9. 结论

**⚠️ `path:/Users`=528ms（post-compaction 首次 >500ms）——page cache 淘汰 + 高文件系统活动（recordCount +15.4K）。** lockWait=9.5ms 复现（`中文测试`——flush 写锁）。`x` 缓存稳定（3ms，连续 4 轮）。极佳 8/16（post-compaction 新低）。Trigram 大 postings 集逐步恢复：`.pdf` 117→79ms（↓32%），`hello world` 113→63ms（↓44%）。

**全表扫描总和 588→971ms（↑65%——`path:/Users` 异常拉高）。** 去除 `path:/Users` 后整体正常。预计 1-round recovery（15/15 历史成功率）。FLUSH +5（38→43），COMPACTION +2（6→8）。tombstone=72,277（1.37%——增速加快，与 recordCount 突增相关）。
