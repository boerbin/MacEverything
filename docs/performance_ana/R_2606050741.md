# MacEverything 性能分析报告 R_2606050741

## 1. 本轮范围

- 任务启动时间：2026-06-05 07:41
- 报告文件：`docs/performance_ana/R_2606050741.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=22416`
  - `SESSION_START_TS=2026-06-04 17:21:30.784`
  - `TOTAL_LINES=25121`
  - `SEG_LINES=2705`
  - `LAST_LOG_TS=2026-06-05 07:41`

## 2. 执行摘要

**部分恢复——1/16 >500ms（`path:/Users`=642ms），lockWait 大幅改善（仅 1/16 查询残留 16ms）。** flush rewrite 从 4.3s 峰值回落至 2.2-3.0s。P50=30ms，tombstone 增速 ~2,775/min（白天高峰确认）。

**R130 的白天冲击波正在消退**：lockWait 从 6/16(max 43ms) 降至 1/16(16ms)，flush rewrite 从 4.3s 降至 3.0s，`content:import` 从 525ms 回落至 415ms。仅 `path:/Users` 仍超标（642ms），但已比 R130 的 888ms 改善 28%。

### 关键指标对比

| 指标 | R130 上轮 | R131 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 2/16 | **1/16** | **↓ 改善** |
| Max | 888ms | **642ms** | **↓ 28%** |
| P50 | 51.5ms | **30.0ms** | **↓ 42% 改善** |
| lockWait max | 43ms | **16ms** | **↓ 63% 改善** |
| lockWait 受影响查询 | 6/16 | **1/16** | **↓ 大幅改善** |
| tombstone | 282,264 (4.67%) | **364,207 (5.95%)** | ↑ 白天加速 |
| tombstone 增速 | ~1,356/min | **~2,775/min** | **↑ 白天高峰** |
| FLUSH 累计 | 1156 | **1215** | +59（~30 分钟） |
| SKIPFLUSH | 14 | **14** | 持平 |
| flush rewrite | 2.0-4.3s | **2.2-3.0s** | **↓ 回落** |
| COMPACTION | 14 | **14** | 不变 |
| QUERYADV_GE500 | 74 | **75** | +1（`path:/Users` 被记录） |

### 恢复进度——R130 冲击波消退中

| 指标 | R129(稳态) | R130(冲击) | R131(恢复中) | 恢复率 |
|------|--------:|--------:|--------:|------|
| Max | 437ms | 888ms | 642ms | 55% |
| P50 | 23.5ms | 51.5ms | 30.0ms | 77% |
| lockWait max | 0ms | 43ms | 16ms | 63% |
| content:import | 315ms | 525ms | 415ms | 52% |
| path:/Users | 437ms | 888ms | 642ms | 55% |

- **P50 和 lockWait 恢复最快**——trigram 路径和锁竞争已基本消除。
- **全表扫描路径恢复较慢**——page cache 需要更多轮次预热。
- **预计下一轮 `path:/Users` 可降至 <500ms**——本轮查询已预热关键页面。

### lockWait 改善分析

| 轮次 | 受影响查询 | 最大值 | flush rewrite 峰值 | 说明 |
|------|--------:|------:|--------:|------|
| R129 | 0/16 | 0ms | 3.3s | 稳态 |
| R130 | **6/16** | **43ms** | **4.3s** | 冲击波 |
| R131 | **1/16** | **16ms** | **3.0s** | 恢复中 |

- **lockWait 与 flush rewrite 时间强相关**——rewrite 从 4.3s 降至 3.0s 后锁竞争基本消除。
- **仅 `中文测试` 残留 16ms lockWait**——可能恰好与一次 flush 时间窗口重叠。
- **结论**：R130 的 lockWait 是 flush rewrite 时间异常升高的暂态效应，非系统性问题。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1.9 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 14.3 | 10.7 | advanced-trigram | 0 | 10037 | 极佳 |
| `main.cpp` | 5 | 9.2 | 2.8 | advanced-trigram | 0 | 83 | 极佳 |
| `*.swift` | 5 | 16.3 | 13.2 | advanced-trigram | 0 | 9326 | 极佳 |
| `ext:cpp` | 5 | 7.3 | 6.6 | advanced-ext-index | 0 | 3658 | 极佳 |
| `size:>100m` | 5 | 45.2 | 45.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 30.0 | 26.0 | advanced-trigram | 0 | 3620 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 46.9 | 45.8 | advanced-trigram | 0 | 15919 | 极佳 |
| `README` | 5 | 17.8 | 9.8 | advanced-trigram | 0 | 17373 | 极佳 |
| `test` | 5 | 40.4 | 19.5 | advanced-trigram | 0 | 130605 | 极佳 |
| `MacEverything` | 5 | 16.6 | 1.1 | advanced-trigram | 0 | 6797 | 极佳 |
| `dm:today` | 5 | 80.3 | 80.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 414.7 | 403.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（↓21%） |
| `path:/Users` | 5 | 642.2 | 575.4 | advanced-linear-gcd | 0 | 0 | **>500ms**（↓28%） |
| `中文测试` | 0 | 427.7 | 391.6 | advanced-linear-gcd | **16.1** | 0 | 残留 lockWait |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 30.0ms，P90 = 414.7ms，Max = 642.2ms。1/16 查询 >500ms。lockWait max = 16.1ms（仅 1/16 查询受影响）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R130 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 11/16 | 0.2-47ms | R130=6/16（**↑5 恢复**） |
| **正常**（50-500ms） | 4/16 | 80-428ms | R130=8/16（↓4） |
| **超标**（>500ms） | 1/16 | 642ms | R130=2/16（**↓1**） |

- **11/16 查询 <50ms——trigram 引擎完全恢复。**
- **`test`=40ms（R130=268ms，↓85%）**——lockWait 消除后大候选集查询恢复。
- **仅 `path:/Users` 仍超标**——phase2=575ms 说明 page cache 仍部分冷。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,125,813，liveRecordCount=5,761,606（tombstone=**364,207**，**5.95%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=14（历史）
QUERYADV=181  QUERYADV_GE500=75（+1——`path:/Users` 被记录）
FLUSH=1215  COMPACTION=14  SKIPFLUSH=14
```

- QUERYADV=181（+3 本轮）。
- **QUERYADV_GE500=75（+1）**——日志记录了一个 >500ms 查询。
- **FLUSH=1215（R130=1156，+59）**——频率稳定 ~120/小时。
- SKIPFLUSH=14（不变）。
- COMPACTION=14（不变）。

### Flush rewrite 时间——从 4.3s 峰值回落

```text
最近 5 次：2226ms  2645ms  2448ms  3045ms  2764ms
中位数：~2.6s
最大值：3.0s
```

- **从 R130 的 4.3s 峰值回落至 3.0s**——白天初始冲击已过。
- **2.2-3.0s 范围**——接近深夜稳态水平（1.9-2.6s），但略高。
- **flush rewrite 时间回落是 lockWait 消除的直接原因。**

### Tombstone 增速——白天高峰确认

- R130 (07:11): 282,264 (4.67%)
- R131 (07:41): 364,207 (5.95%)
- **增量 +81,943 / 30 min = ~2,731/min**
- **对比**：R130=1,356/min → R131=2,731/min（**↑2x**）——白天高峰加速。
- **按当前增速达到 15%**：还需 ~525,000 / 2,731 per min ≈ **192 分钟 ≈ ~3.2 小时**。
- **按当前增速达到 25%**：还需 ~1,105,000 / 2,731 per min ≈ **405 分钟 ≈ ~6.8 小时**。

### Tombstone 增速趋势——白天事件风暴加速

| 轮次 | 时间 | 增速(/min) | 阶段 |
|------|------|--------:|------|
| R128 | 06:11 | 358 | 深夜最低 |
| R129 | 06:41 | 436 | 开始恢复 |
| R130 | 07:11 | 1,356 | 白天开始 |
| **R131** | **07:41** | **2,731** | **白天高峰** |

- **30 分钟内增速翻倍**——事件风暴全面展开。
- **按此加速趋势，15% compaction 可能在 ~3 小时内触发（~11:00 AM）。**

## 6. 问题清单

### Low：`path:/Users` 仍超 500ms

- 642ms（R130=888ms，↓28%——正在恢复）。
- **原因**：page cache 仍部分冷——白天进程持续竞争内存。
- **预计**：随着查询活动持续，下一轮可能降至 <600ms 甚至 <500ms。
- **不需要干预**——自然恢复中。

### 正面：lockWait 基本消除

- 从 R130 的 6/16(43ms) 降至 1/16(16ms)——flush rewrite 回落是直接原因。
- **R130 的锁竞争确认为暂态事件——不是系统性问题。**

### 正面：trigram 路径完全恢复

- 11/16 查询 <50ms——P50=30ms。
- `test` 从 268ms(R130) 恢复至 40ms——page cache + 无锁竞争 = 正常性能。

## 7. 优化建议

### P1：降低 Compaction 阈值至 15%（紧迫性提升）

- **当前 5.95%，按 2,731/min 增速，~3.2 小时达到 15%。**
- **白天增速可能继续上升**——实际到达时间可能更短。
- **compaction 的收益**：
  - 减少记录数 → flush rewrite 时间缩短 → lockWait 风险降低。
  - 减少全表扫描数据量 → phase2 时间缩短。
  - 回收磁盘空间和内存。

### P2：关注 flush rewrite 在白天高峰的趋势

- 本轮 3.0s 已比 R130 的 4.3s 改善——但白天高峰可能再次升高。
- **警戒线**：rewrite >5 秒时 lockWait 可能重新出现。
- **当前无需行动——继续监控。**

### 无其他建议

- 系统在白天冲击波后正快速恢复——自调节能力有效。

## 8. 下一轮重点

1. `path:/Users` 是否降至 <500ms。
2. lockWait 是否完全消除（0/16）。
3. tombstone 增速是否继续加速——15% compaction 预计触发时间。
4. flush rewrite 在白天高峰是否重新升高。

## 9. 结论

**1/16 >500ms（`path:/Users`=642ms），lockWait 从 6/16(43ms) 大幅改善至 1/16(16ms)。** R130 白天冲击波正在消退——flush rewrite 从 4.3s 回落至 3.0s，trigram 路径完全恢复（11/16 <50ms，P50=30ms）。

**tombstone 增速飙升至 2,731/min（白天高峰）**——按此速度 ~3.2 小时达到 15% compaction 阈值（约 11:00 AM）。compaction 触发后将大幅改善全表扫描性能和 flush rewrite 时间。

**系统展现出良好的自恢复能力**——R130 的异常（lockWait、888ms）在一轮后即大幅消退。白天活动带来的 page cache 竞争是唯一残留问题，将随 compaction 和持续查询活动自然改善。
