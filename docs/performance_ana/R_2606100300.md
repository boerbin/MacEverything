# MacEverything 性能分析报告 R_2606100300

## 1. 本轮范围

- 任务启动时间：2026-06-10 03:00
- 报告文件：`docs/performance_ana/R_2606100300.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=7510`
  - `SEG_LINES=6568`
  - `LAST_LOG_TS=2026-06-09 23:47`

## 2. 执行摘要

**lockWait=286.6ms——系列新纪录！13/16 查询出现锁竞争！** 2/16 >500ms（`.pdf`=545ms，`hello world`=513ms），Max=545ms，P50=157ms。**lockWait 286.6ms 超越 R217 的 202.6ms 成为历史最高。** 13/16 查询有 lockWait（历史最广泛），说明 flush/index 写锁持有时间极长，多个查询排队等待。

| 指标 | R231 | R232 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **2/16** | **↑2** |
| Max | 208ms | **545ms** | **↑162%** |
| P50 | 56ms | **157ms** | **↑180%** |
| lockWait max | 0ms | **286.6ms** | **↑∞（系列新纪录！！）** |
| 极佳比例(<50ms) | 7/16 | **2/16** | **↓5** |
| tombstone | 2,056,422 (28.39%) | **2,073,455 (28.48%)** | ↑ |
| tombstone 增速 | ~614/min | **~567/min** | ↓8%（继续减速） |
| recordCount | 7,262,478 | **7,279,923** | +17.4K |
| liveRecordCount | 5,206,056 | **5,206,468** | +412 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5857 | **5917** | +60 |
| COMPACTION | 2 | **2** | **不变——第 33 轮确认缺陷** |
| QUERYADV_GE500 | 60 | **60** | 不变（battery 中 2 个 >500ms 尚未记入日志） |

### lockWait=286.6ms——系列新纪录！

- **`.pdf`：queryTime=545ms，phase2=181ms，lockWait=286.6ms。**
- **lockWait 占 queryTime 的 53%——超过一半时间在等锁！**
- **打破 R217=202.6ms 的历史纪录——新最高 286.6ms。**

### lockWait 历史纪录（更新）

| 轮次 | lockWait max | 触发查询 | 时段 | 受影响查询数 |
|------|------------:|---------|------|----------:|
| **R232** | **286.6ms** | **`.pdf`** | **凌晨 3:00** | **13/16** |
| R217 | 202.6ms | `.pdf` | 午后 | 3/16 |
| R227 | 65.3ms | `中文测试` | 深夜 | 1/16 |
| R229 | 46.2ms | `hello world` | 凌晨 | 3/16 |
| R215 | 18.2ms | `.pdf` | 午后 | 1/16 |

### lockWait 广度——13/16 查询受影响（历史最广）

| Query | lockWait | queryTime | phase2 | overhead |
|-------|--------:|----------:|-------:|---------:|
| `.pdf` | **286.6ms** | 545ms | 181ms | 77ms |
| `abc def...` | 19.0ms | 352ms | 323ms | 10ms |
| `*.swift` | 18.2ms | 158ms | 125ms | 15ms |
| `main.cpp` | 14.7ms | 53ms | 12ms | 26ms |
| `ext:cpp` | 9.8ms | 236ms | 218ms | 8ms |
| `test` | 4.9ms | 204ms | 184ms | 15ms |
| `content:import` | 3.4ms | 323ms | 279ms | 41ms |
| `README` | 2.4ms | 72ms | 54ms | 16ms |
| `MacEverything` | 2.3ms | 48ms | 5ms | 41ms |
| `verylongquery...` | 1.6ms | 3ms | 0ms | 1ms |
| `size:>100m` | 1.4ms | 116ms | 112ms | 3ms |
| `中文测试` | 0.5ms | 237ms | 230ms | 7ms |
| `path:/Users` | 0.1ms | 346ms | 330ms | 16ms |
| `hello world` | 0ms | 513ms | 482ms | 31ms |
| `dm:today` | 0ms | 113ms | 108ms | 5ms |
| `x` | 0ms | 6ms | 0ms | 6ms |

- **lockWait >0 的查询达 13/16——此前最高仅 3/16（R217、R229）。**
- **`.pdf` 首当其冲（286.6ms），其后多个查询仍在等锁递减。**
- **说明 flush/index 更新持有全局写锁时间极长（>286ms），后续查询排队。**
- **`hello world`=513ms 但 lockWait=0——phase2=482ms 纯计算（postings 区域 page cache 波动）。**

### lockWait 递减模式分析

lockWait 从 `.pdf`=286.6ms 递减至后续查询，符合「单次长锁持有」模型：
- 首个查询（`.pdf`）等待最长，命中锁持有全程
- 后续查询到达时锁已部分释放，等待时间递减
- `hello world` 和 `dm:today` lockWait=0——它们在锁释放后才到达

**这强化了 COW flush P0 优先级——单次 flush 可阻塞 13/16 查询。**

### Trigram 路径——受压

| Query | R231 | R232 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 186ms | **545ms** | **↑193%（>500ms！lockWait=286.6ms）** | 超标 |
| `hello world` | 208ms | **513ms** | **↑147%（>500ms！）** | 超标 |
| `*.swift` | 122ms | **158ms** | ↑30%（lockWait=18.2ms） | 正常 |
| `main.cpp` | 23ms | **53ms** | ↑130%（lockWait=14.7ms） | 正常 |
| `abc def...` | 105ms | **352ms** | ↑235%（lockWait=19.0ms） | 正常 |
| `README` | 30ms | **72ms** | ↑140%（lockWait=2.4ms） | 正常 |
| `test` | 62ms | **204ms** | ↑229%（lockWait=4.9ms） | 正常 |
| `MacEverything` | 10ms | **48ms** | ↑380%（lockWait=2.3ms） | 极佳 |
| Trigram 均值 | 93ms | **243ms** | **↑161%** | 受压 |

- **Trigram 均值 93→243ms（↑161%）——大幅恶化。**
- **`.pdf`=545ms 和 `hello world`=513ms 超过 500ms 阈值。**
- **`.pdf` 的 286.6ms lockWait 是主因（phase2=181ms 其实正常）。**
- **`hello world`=513ms 是纯 phase2（482ms）——postings 区域 page cache 被后台 I/O 淘汰。**

### 全表扫描——恶化

| Query | R231 | R232 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 66ms | **323ms** | **↑389%** |
| `path:/Users` | 109ms | **346ms** | **↑218%** |
| `中文测试` | 55ms | **237ms** | **↑331%** |
| **总和** | **230ms** | **906ms** | **↑294%** |

- **全表扫描总和 230→906ms（↑294%）——大幅恶化。**
- **`content:import`=323ms（↑389%，lockWait=3.4ms）——SoA 区域 page cache 淘汰 + 轻微锁竞争。**
- **`path:/Users`=346ms（↑218%）——路径数组 page cache 波动。**

### 辅助路径

| Query | R231 | R232 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 6ms | **6ms** | 持平 | 极佳 |
| `ext:cpp` | 54ms | **236ms** | ↑337%（lockWait=9.8ms） | 正常 |
| `size:>100m` | 36ms | **116ms** | ↑222%（lockWait=1.4ms） | 正常 |
| `dm:today` | 14ms | **113ms** | ↑707% | 正常 |
| `verylongquery...` | 0.3ms | **3ms** | ↑900%（lockWait=1.6ms） | 极佳 |

- **`dm:today`=113ms（↑707%）——SoA modTime 区域波动。**
- **`ext:cpp`=236ms（↑337%，lockWait=9.8ms）——ext-index 区域受压 + 锁竞争。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 5.5 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 545.0 | 180.6 | advanced-trigram | **286.6** | 9860 | **>500ms！lockWait 新纪录** |
| `main.cpp` | 5 | 53.2 | 12.4 | advanced-trigram | **14.7** | 82 | 正常 |
| `*.swift` | 5 | 157.7 | 125.2 | advanced-trigram | **18.2** | 9353 | 正常 |
| `ext:cpp` | 5 | 235.8 | 217.8 | advanced-ext-index | **9.8** | 3571 | 正常 |
| `size:>100m` | 5 | 115.9 | 112.2 | advanced-pure-filter-soa-gcd | **1.4** | 0 | 正常 |
| `hello world` | 5 | 513.1 | 481.8 | advanced-trigram | 0 | 3671 | **>500ms！** |
| `abc def ghi jkl mno pqr` | 0 | 352.3 | 323.4 | advanced-trigram | **19.0** | 14453 | 正常 |
| `README` | 5 | 71.8 | 54.4 | advanced-trigram | **2.4** | 17151 | 正常 |
| `test` | 5 | 203.7 | 184.2 | advanced-trigram | **4.9** | 117136 | 正常 |
| `MacEverything` | 5 | 48.4 | 4.6 | advanced-trigram | **2.3** | 6859 | 极佳 |
| `dm:today` | 5 | 112.5 | 107.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 323.0 | 278.7 | advanced-pure-filter-soa-gcd | **3.4** | 0 | 正常 |
| `path:/Users` | 5 | 346.2 | 329.8 | advanced-linear-gcd | **0.1** | 0 | 正常 |
| `中文测试` | 0 | 237.0 | 229.6 | advanced-linear-gcd | **0.5** | 0 | 正常 |
| `verylongquery...` | 0 | 3.0 | 0.0 | advanced-trigram | **1.6** | 0 | 极佳 |

**P50 = 157.7ms，P90 = 513.1ms，Max = 545.0ms。2/16 查询 >500ms。lockWait max = 286.6ms（系列新纪录！）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R231 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **2/16** | 3-48ms | R231=7/16（**↓5**） |
| **正常**（50-500ms） | 12/16 | 53-352ms | R231=9/16（↑3） |
| **超标**（>500ms） | **2/16** | 513-545ms | R231=0/16（**↑2**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,279,923，liveRecordCount=5,206,468（tombstone=**2,073,455**，**28.48%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=530  QUERYADV_GE500=60（不变——battery 中的 2 个 >500ms 尚未记入日志）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5917
```

## 6. 问题清单

### P0 确认（第 33 轮）：compaction 触发机制缺陷

- **tombstone=2,073,455（28.48%）——超阈值 3.48%。**
- **33 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞——lockWait 286.6ms 系列新纪录！

- **打破 R217=202.6ms 的历史纪录！**
- **13/16 查询受影响——此前最高仅 3/16。**
- **单次 flush 写锁持有 >286ms，阻塞几乎所有并发查询。**
- **lockWait 事件现已覆盖：午后（R215/R217）、深夜（R227）、凌晨（R229/R232）——全时段。**
- **COW flush 是唯一正确修复路径——必须消除全局写锁对查询的阻塞。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式（lockWait 286.6ms 新纪录！13/16 阻塞！）

**升级为最高优先级。** 单次 flush 写锁可阻塞 13/16 查询，最长等待 286.6ms。当前 flush 路径持有全局写锁执行完整重写，必须改为 COW（copy-on-write）方式：准备新数据→原子指针切换→释放旧数据。

### P0：修复 compaction 触发机制（第 33 轮确认）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. lockWait 是否消退（286.6ms 是否为一次性事件，预测 1 轮恢复）。
2. `hello world` phase2=482ms 是否恢复（page cache 波动）。
3. tombstone 增速趋势。

## 9. 结论

**lockWait=286.6ms 系列新纪录！13/16 查询出现锁竞争——历史最广泛。** 2/16 >500ms（`.pdf`=545ms lockWait=286.6ms，`hello world`=513ms phase2=482ms），Max=545ms（↑162%），P50=158ms（↑180%）。Trigram 均值 243ms（↑161%）。全表扫描总和 906ms（↑294%）。

**lockWait 递减模式清晰：** `.pdf`=286.6ms → `abc def`=19.0ms → ... → `hello world`=0ms。符合「单次长锁持有」模型——首个查询等待最长，后续递减。**单次 flush 写锁持有 >286ms，阻塞 13/16 查询。COW flush P0 优先级再次强化——已是本系列最强证据。**

**tombstone 28.48%（第 33 轮确认 compaction 缺陷）。** 增速 ~567/min（继续减速）。COMPACTION=2 始终不变。
