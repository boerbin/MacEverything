# MacEverything 性能分析报告 R_2606101430

## 1. 本轮范围

- 任务启动时间：2026-06-10 14:30
- 报告文件：`docs/performance_ana/R_2606101430.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=8974`
  - `SEG_LINES=8032`
  - `LAST_LOG_TS=2026-06-10 13:09`

## 2. 执行摘要

**轻微 page cache 波动——`x`=145ms 异常但非 I/O 风暴。** 1/16 >500ms（`.pdf`=530ms），Max=530ms，P50=95.6ms，lockWait=0（连续 4 轮）。Trigram 均值 125ms（↑140% from R254=52ms——page cache 部分淘汰），全表扫描总和 628ms（↑263% from R254=173ms——零和反向）。**`x`=145ms（short-query-cache——轻微系统级延迟），但 SKIPFLUSH +34（缺 26 次 tick）——存在短暂调度干扰。** 极佳 5/16（↓5）。tombstone=2,271,517（30.33%）——第 56 轮 compaction 缺陷确认。

| 指标 | R254 | R255 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **1/16** | ↑1 |
| Max | 121ms | **530ms** | ↑338% |
| P50 | 44ms | **95.6ms** | ↑117% |
| lockWait max | 0ms | **0ms** | 完美（连续 4 轮） |
| 极佳比例(<50ms) | 10/16 | **5/16** | ↓5 |
| tombstone | 2,265,964 (30.28%) | **2,271,517 (30.33%)** | ↑0.05pp |
| recordCount | 7,489,242 | **7,495,142** | +5.9K（回归正常！） |
| liveRecordCount | 5,223,278 | **5,223,625** | +347（正常） |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 7200 | **7234** | **+34（异常低！缺 26 次 tick）** |
| COMPACTION | 2 | **2** | **不变——第 56 轮确认缺陷** |
| QUERYADV_GE500 | 74 | **74** | 不变 |

### `x`=145ms 分析

- **short-query-cache 通常 <10ms（R254=2ms）。145ms 表示系统级延迟。**
- **但与 R249=153ms / R251=285ms 不同：**
  - lockWait=0（R249=11.2ms，R251=49.8ms）
  - QUERYADV_GE500 不变（R249=+4，R251=+6）
  - recordCount +5.9K 正常（R249=+8.9K，R251=+29.6K）
- **结论：一次性轻微系统级延迟（短暂 context switch），非持续 I/O 风暴。**

### SKIPFLUSH +34 异常

- **正常每轮 +60。+34 意味着缺 26 次 tick（约 13 分钟进程调度延迟）。**
- **日志时间跨度 12:17→13:09（52 分钟）——应有 ~104 次 tick，实际只有 34 次。**
- **这意味着进程在这 52 分钟内有约 35 分钟处于调度延迟。**
- **可能原因：系统进入低功耗模式、macOS App Nap 或系统级节流。**
- **与 `x`=145ms 一致——进程被暂时挂起后恢复时的延迟。**

### Trigram 路径——轻微回调

| Query | R254 | R255 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 76ms | **530ms** | ↑597% | **⚠️ >500ms** |
| `hello world` | 121ms | **131ms** | ↑8% | 正常 |
| `*.swift` | 56ms | **96ms** | ↑71% | 正常 |
| `main.cpp` | 10ms | **28ms** | ↑180% | 极佳 |
| `abc def...` | 79ms | **97ms** | ↑23% | 正常 |
| `README` | 21ms | **28ms** | ↑33% | 极佳 |
| `test` | 49ms | **87ms** | ↑78% | 正常 |
| `MacEverything` | 6ms | **5ms** | ↓17% | 极佳 |
| Trigram 均值 | 52ms | **125ms** | **↑140%** |

- **`.pdf`=530ms（↑597%——大 postings 集受 page cache 淘汰影响最大）。**
- **其余 7 个 trigram 均 <135ms——波动温和。**
- **Trigram 均值 125ms——偏高但远未达到 R249/R251 级别（612/984ms）。**
- **预计 1-round recovery。**

### 全表扫描——零和反向

| Query | R254 | R255 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 64ms | **163ms** | ↑155% |
| `path:/Users` | 65ms | **242ms** | ↑272% |
| `中文测试` | 44ms | **223ms** | ↑407% |
| **总和** | **173ms** | **628ms** | **↑263%** |

- **全表扫描 173→628ms（↑263%）——SoA 区域 page cache 部分淘汰。**
- **三路均 <250ms——尚在正常区间。**
- **典型的从优秀（R254 近期最佳）向中值回归。**

### 辅助路径

| Query | R254 | R255 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 2ms | **145ms** | ↑异常 | 正常（系统延迟） |
| `ext:cpp` | 25ms | **28ms** | ↑12% | 极佳 |
| `size:>100m` | 27ms | **42ms** | ↑56% | 极佳 |
| `dm:today` | 15ms | **115ms** | ↑667% | 正常 |
| `verylongquery...` | 0.2ms | **1.3ms** | ↑550% | 极佳 |

- **`dm:today`=115ms（↑667%——SoA filter 区域受影响）。**
- **`verylongquery`=1.3ms（偏高但仍极佳——轻微 overhead）。**

### recordCount 回归正常

- **recordCount +5.9K——**完全回归正常区间（正常 +7K）！****
- **liveRecordCount +347——正常。**
- **I/O 风暴引发的文件系统高活动已完全消退。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 145.1 | 0.0 | short-query-cache | 0 | 0 | 异常（系统延迟） |
| `.pdf` | 5 | **529.9** | 437.2 | advanced-trigram | 0 | 9888 | **⚠️ >500ms** |
| `main.cpp` | 5 | 28.2 | 23.0 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 95.6 | 94.9 | advanced-trigram | 0 | 9379 | 正常 |
| `ext:cpp` | 5 | 27.8 | 27.1 | advanced-ext-index | 0 | 3606 | 极佳 |
| `size:>100m` | 5 | 41.7 | 41.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 131.2 | 130.0 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 97.1 | 96.7 | advanced-trigram | 0 | 14507 | 正常 |
| `README` | 5 | 27.8 | 24.9 | advanced-trigram | 0 | 17164 | 极佳 |
| `test` | 5 | 87.0 | 83.7 | advanced-trigram | 0 | 117308 | 正常 |
| `MacEverything` | 5 | 4.9 | 1.6 | advanced-trigram | 0 | 6867 | 极佳 |
| `dm:today` | 5 | 115.4 | 115.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 163.2 | 153.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 242.0 | 237.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 223.1 | 221.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 1.3 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 95.6ms，P90 = 223.1ms，Max = 529.9ms。1/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R254 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **5/16** | 0.2-42ms | R254=10/16（↓5） |
| **正常**（50-500ms） | 10/16 | 87-242ms | R254=6/16（↑4） |
| **超标**（>500ms） | **1/16** | 530ms | R254=0/16（↑1） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,495,142，liveRecordCount=5,223,625（tombstone=**2,271,517**，**30.33%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=654  QUERYADV_GE500=74（不变——`.pdf`=530ms 但日志中记录为 437ms phase2）
FLUSH=1  COMPACTION=2  SKIPFLUSH=7234
```

- **SKIPFLUSH +34（异常低！缺 26 次 tick——进程调度延迟约 13 分钟）。**

## 6. 问题清单

### P0 确认（第 56 轮）：compaction 触发机制缺陷

- **tombstone=2,271,517（30.33%）——超阈值 5.33%。30% 以上第 4 轮。**
- **56 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **lockWait 连续 4 轮为 0。历史证据充分。**

### P0：I/O 优先级保护

- **SKIPFLUSH +34 新证据——进程被系统节流（可能 App Nap）。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 56 轮——tombstone 30.33%！）

### P0：flush rewrite 改为 COW 方式

### P0：进程 QoS / I/O 优先级保护

- **新证据：SKIPFLUSH +34（缺 26 次 tick）——进程被系统级节流约 13 分钟。**
- **建议：设置 `NSProcessInfoActivityIdleSystemSleepDisabled` 或 `ProcessInfo.beginActivity()` 防止 App Nap。**

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 1-round recovery 是否再次确认（从 R255 轻微回调恢复）。
2. SKIPFLUSH 是否回归 +60（App Nap 是否为一次性事件）。

## 9. 结论

**轻微 page cache 波动——非 I/O 风暴。** 1/16 >500ms（`.pdf`=530ms），Max=530ms，P50=96ms，lockWait=0（连续 4 轮）。`x`=145ms（系统级延迟——但 lockWait=0、recordCount 正常，非持续风暴）。Trigram 均值 52→125ms（↑140%），全表扫描 173→628ms（↑263%——从 R254 近期最佳向中值回归）。**SKIPFLUSH +34（缺 26 次 tick）——进程被系统节流约 13 分钟——新的 App Nap 证据。**

**tombstone 30.33%（2,271,517，第 56 轮 compaction 缺陷确认）。** recordCount +5.9K（**回归正常！**）——文件系统活动完全消退。预计 R256 1-round recovery。
