# MacEverything 性能分析报告 R_2606081746

## 1. 本轮范围

- 任务启动时间：2026-06-08 17:46
- 报告文件：`docs/performance_ana/R_2606081746.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5150`
  - `SEG_LINES=4208`
  - `LAST_LOG_TS=2026-06-08 18:17`

## 2. 执行摘要

**持续收敛——0/16 >500ms，Max=262ms，P50=93ms，lockWait=0，极佳比例 4/16（25%）。** 从 R191 继续改善，Max 从 324ms 降至 262ms（↓19%），P50 从 114ms 降至 93ms（↓18%）。Trigram 均值 149→112ms（↓25%），全表扫描总和 536→571ms（微升但均匀分布）。傍晚系统活动降低，page cache 压力缓解。tombstone 24.29%——增速降至 ~330/min（傍晚持续回落），距 25% 预计 ~3.6 小时。

### 关键指标对比

| 指标 | R191 | R192 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（连续 2 轮 0） |
| Max | 324ms | **262ms** | **↓ 19%** |
| P50 | 114ms | **93ms** | **↓ 18%** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 4/16 | **4/16** | 持平 |
| tombstone | 1,637,077 (24.19%) | **1,647,212 (24.29%)** | ↑ |
| tombstone 增速 | ~404/min | **~330/min** | **↓ 18%（傍晚回落）** |
| recordCount | 6,766,352 | **6,777,516** | +11.2K（正常） |
| liveRecordCount | 5,129,275 | **5,130,304** | +1,029 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3808 | **3868** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 22 | **22** | 不变（本轮 0 新增） |

### Trigram 路径——稳定收敛

| Query | R191 | R192 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 324ms | **193ms** | **↓41%** | 正常 |
| `hello world` | 309ms | **262ms** | ↓15% | 冷态 |
| `*.swift` | 225ms | **106ms** | **↓53%** | 正常 |
| `main.cpp` | 58ms | **25ms** | **↓57%** | 极佳 |
| `abc def...` | 114ms | **128ms** | ↑12% | 正常 |
| `README` | 61ms | **56ms** | ↓8% | 正常 |
| `test` | 81ms | **94ms** | ↑16% | 正常 |
| `MacEverything` | 20ms | **18ms** | ↓10% | 极佳 |
| Trigram 均值 | 149ms | **112ms** | **↓25%** | 收敛中 |

- **Trigram 均值 149→112ms（↓25%）——持续收敛。**
- **`.pdf`=193ms（R191=324ms，↓41%）——从冷态恢复至正常偏高。**
- **`*.swift`=106ms（R191=225ms，↓53%）——恢复速度最快。**
- **`hello world`=262ms 仍是主要慢项——高 candidates（3638）postings 区域需更多预热。**
- **`abc def...`=128ms 和 `test`=94ms 微升——postings 区域冷热不均匀的正常波动。**

### 全表扫描——正常稳定

| Query | R191 | R192 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 184ms | **152ms** | ↓17% |
| `path:/Users` | 184ms | **220ms** | ↑20% |
| `中文测试` | 168ms | **200ms** | ↑19% |
| **总和** | **536ms** | **571ms** | ↑7%（微升） |

- **全表扫描总和 536→571ms（↑7%）——微升但仍在日间正常范围。**
- **`path:/Users`=220ms（R191=184ms，↑20%）——从极佳恢复值回到日间典型水平。**
- **`content:import`=152ms（R191=184ms，↓17%）——改善。**
- **三项分布均匀（152-220ms），说明 SoA/linear 区域 page cache 稳定。**

### SoA Filter——正常

| Query | R191 | R192 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 196ms | **91ms** | **↓54%** |
| `dm:today` | 42ms | **46ms** | ↑10% |

- **`size:>100m`=91ms（R191=196ms，↓54%）——恢复至正常。**
- **`dm:today`=46ms——极佳边缘。**

### tombstone 24.29%——增速持续回落

- **tombstone=1,647,212（24.29%）——距 25% 仅 0.71 个百分点。**
- **增速 ~330/min（R191=404/min，↓18%）——傍晚系统活动降低，文件操作减少。**
- **距 25% 需要额外 48,167 个 tombstone。**
- **按 330/min 计算：~146 分钟 ≈ 2.4 小时（约 20:10-20:30）。**
- **但如果增速继续回落至夜间水平（~200/min），可能推迟至明天。**

### tombstone 增速趋势

| 轮次 | 时间 | 增速(/min) | 趋势 |
|------|------|----------:|------|
| R186 | 14:46 | 603 | 午后高峰 |
| R187 | 15:16 | 569 | 午后高峰 |
| R188 | 15:46 | 394 | 回落 |
| R189 | 16:16 | 790 | 反弹 |
| R190 | 16:46 | 549 | 回落 |
| R191 | 17:16 | 404 | 回落 |
| **R192** | **17:46** | **330** | **傍晚回落** |

- **傍晚趋势明确——从午后 600-790/min 逐步降至 330/min。**
- **如果夜间进一步降至 ~200/min，25% 可能需要 4+ 小时。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 12.2 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 192.5 | 181.6 | advanced-trigram | 0 | 9842 | 正常 |
| `main.cpp` | 5 | 24.9 | 16.8 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 106.1 | 100.3 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 67.0 | 62.0 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 91.1 | 86.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 262.1 | 252.2 | advanced-trigram | 0 | 3638 | 冷态 |
| `abc def ghi jkl mno pqr` | 0 | 128.0 | 127.0 | advanced-trigram | 0 | 14304 | 正常 |
| `README` | 5 | 55.6 | 51.7 | advanced-trigram | 0 | 16691 | 正常 |
| `test` | 5 | 93.5 | 77.3 | advanced-trigram | 0 | 116588 | 正常 |
| `MacEverything` | 5 | 18.2 | 0.8 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 46.4 | 46.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳边缘 |
| `content:import` | 5 | 151.5 | 139.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 219.6 | 215.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 199.9 | 199.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 1.1 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 93.5ms，P90 = 219.6ms，Max = 262.1ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R191 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 4/16 | 1.1-46ms | R191=4/16（持平） |
| **正常**（50-500ms） | 12/16 | 56-262ms | R191=12/16（持平） |
| **超标**（>500ms） | 0/16 | — | R191=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,777,516，liveRecordCount=5,130,304（tombstone=**1,647,212**，**24.29%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=260  QUERYADV_GE500=22（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3868
```

- **QUERYADV_GE500 不变（连续 2 轮 0 新增）。**
- **SKIPFLUSH +60——正常节奏。**

## 6. 问题清单

### P0 极度紧急：tombstone 24.29%——距 25% 仅 ~2.4 小时

- **距 25% 仅 0.71 个百分点（48,167 tombstone）。**
- **增速 330/min——预计 ~2.4 小时（约 20:10）触达。**
- **但增速持续回落中——夜间可能推迟至明天。**
- **compaction 触发后若 rewrite 崩溃，索引可能不可用。**

### 正面信号：傍晚性能稳定

- **连续 2 轮 0/16 >500ms，lockWait=0。**
- **trigram 均值持续收敛（217→149→112ms）。**
- **傍晚系统活动降低——page cache 压力缓解。**

## 7. 优化建议

### P0：立即排查 compaction rewrite 崩溃根因

- **tombstone 24.29%，今晚或明晨可能触发 compaction。**
- **必须确认 rewrite 安全——紧迫度：小时级。**

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. tombstone 增速是否继续回落至夜间水平（~200/min）。
2. trigram 均值是否从 112ms 继续收敛至 70-90ms。
3. `hello world` 是否从 262ms 恢复。

## 9. 结论

**持续收敛——0/16 >500ms，Max=262ms，P50=93ms，lockWait=0，极佳 4/16。** Trigram 均值 149→112ms（↓25%），`.pdf` 324→193ms（↓41%），`*.swift` 225→106ms（↓53%）。傍晚系统活动降低，page cache 压力持续缓解。

**tombstone 24.29%——增速持续回落至 330/min（傍晚效应）。** 距 25% 约 2.4 小时（~20:10），但如果增速继续降至夜间水平（~200/min），compaction 可能推迟至明天。无论如何，P0 compaction rewrite 崩溃修复仍是最紧急事项。
