# MacEverything 性能分析报告 R_2606092200

## 1. 本轮范围

- 任务启动时间：2026-06-09 22:00
- 报告文件：`docs/performance_ana/R_2606092200.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6935`
  - `SEG_LINES=5993`
  - `LAST_LOG_TS=2026-06-09 17:17`

## 2. 执行摘要

**1 轮恢复（第 9 次确认）——1/16 >500ms（仅 `.pdf`=561ms），Max=561ms，P50=128ms，lockWait=0，极佳比例 5/16。** R221 的灾难级状态（6/16 >500ms，Max=1698ms，overhead 扩散）完全消退。进程调度延迟从 114ms 回落至 12ms。Trigram 均值 611→173ms（↓72%）。

| 指标 | R221 | R222 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 6/16 | **1/16** | **↓5（1 轮恢复）** |
| Max | 1698ms | **561ms** | **↓67%** |
| P50 | 312ms | **128ms** | **↓59%** |
| lockWait max | 0.8ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 2/16 | **5/16** | ↑3 |
| tombstone | 1,937,740 (27.17%) | **1,947,775 (27.26%)** | ↑ |
| tombstone 增速 | ~307/min | ~332/min | ↑8%（轻微回升） |
| recordCount | 7,131,988 | **7,143,042** | +11.1K |
| liveRecordCount | 5,194,248 | **5,195,267** | +1.0K |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5353 | **5413** | +60 |
| COMPACTION | 2 | **2** | **不变——第 23 轮确认缺陷** |
| QUERYADV_GE500 | 56 | **57** | ↑1（`.pdf`=561ms） |

### 1 轮恢复模式——第 9 次确认

| 事件 | 灾难轮 | 恢复轮 | 灾难 Max | 恢复 Max | >500ms |
|------|--------|--------|---------|---------|--------|
| #1 | R176 | R177 | 2217ms | ~50ms | 0 |
| #2 | R179 | R180 | ~1500ms | ~40ms | 0 |
| #3 | R181 | R182 | ~1200ms | ~35ms | 0 |
| #4 | R190 | R191 | ~1800ms | ~45ms | 0 |
| #5 | R194 | R195 | ~1600ms | ~30ms | 0 |
| #6 | R199 | R200 | 6155ms | ~300ms | 0 |
| #7 | R208 | R209 | 3436ms | 80ms | 0 |
| #8 | R215 | R216 | 975ms | 328ms | 0 |
| **#9** | **R221** | **R222** | **1698ms** | **561ms** | **1** |

- **R221=1698ms → R222=561ms（↓67%）——模式高度稳定。**
- **R222 恢复不完全——`.pdf`=561ms 仍 >500ms——但相比灾难轮 6/16 已大幅改善。**
- **R222 是 9 次恢复中恢复幅度最小的——Max 仍 >500ms。可能是 `.pdf` postings 区域偏冷。**

### 进程调度延迟——完全消退

- **`x`=12ms（R221=114ms，↓89%）——回归正常。**
- **所有查询 overhead 回归正常——不再有 >100ms 的调度延迟。**
- **R221 的调度延迟扩散为一次性系统事件。**

### Trigram 路径——大幅恢复

| Query | R221 | R222 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 1160ms | **561ms** | ↓52% | **>500ms（残余）** |
| `hello world` | 1698ms | **297ms** | ↓83% | 正常 |
| `*.swift` | 607ms | **302ms** | ↓50% | 正常 |
| `main.cpp` | 125ms | **46ms** | ↓63% | 极佳 |
| `abc def...` | 478ms | **104ms** | ↓78% | 正常 |
| `README` | 170ms | **22ms** | ↓87% | 极佳 |
| `test` | 578ms | **50ms** | ↓91% | 极佳 |
| `MacEverything` | 69ms | **9ms** | ↓87% | 极佳 |
| Trigram 均值 | 611ms | **173ms** | **↓72%** | 大幅恢复 |

- **Trigram 均值 611→173ms（↓72%）——从灾难到正常。**
- **`.pdf`=561ms（↓52%）——仍 >500ms，postings 区域未完全预热。**
- **`test`=50ms（↓91% from 578ms）——极佳！**
- **`README`=22ms（↓87%）——极佳。**

### 全表扫描——大幅恢复

| Query | R221 | R222 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 1286ms | **177ms** | **↓86%** |
| `path:/Users` | 1016ms | **196ms** | **↓81%** |
| `中文测试` | 69ms | **256ms** | ↑271%（波动） |
| **总和** | **2371ms** | **629ms** | **↓73%** |

- **全表扫描总和 2371→629ms（↓73%）——大幅恢复。**
- **`content:import`=177ms（↓86% from 1286ms）——overhead 完全消退（R221 overhead=1167ms）。**
- **`中文测试`=256ms（↑271% from 69ms）——名称数组与 SoA 区域交替波动。**

### 辅助路径

| Query | R221 | R222 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 114ms | **12ms** | ↓89% | 极佳 |
| `ext:cpp` | 312ms | **129ms** | ↓59% | 正常 |
| `size:>100m` | 73ms | **83ms** | ↑14% | 正常 |
| `dm:today` | 25ms | **59ms** | ↑136% | 正常 |
| `verylongquery...` | 2.4ms | **0.6ms** | ↓75% | 极佳 |

### tombstone 27.26%——持续增长

- **tombstone=1,947,775（27.26%）——超阈值 2.26%。**
- **增速 ~332/min（轻微回升——可能有晚间后台任务）。**
- **recordCount +11.1K，liveRecordCount +1.0K——净新增极少，10.1K 为 tombstone。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 12.4 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 561.2 | 553.1 | advanced-trigram | 0 | 9860 | **>500ms（残余）** |
| `main.cpp` | 5 | 46.2 | 38.1 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 301.8 | 298.0 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 128.5 | 126.3 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 82.7 | 82.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 296.8 | 278.8 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 103.5 | 100.0 | advanced-trigram | 0 | 14388 | 正常 |
| `README` | 5 | 21.9 | 16.3 | advanced-trigram | 0 | 17135 | 极佳 |
| `test` | 5 | 49.6 | 46.4 | advanced-trigram | 0 | 117092 | 极佳 |
| `MacEverything` | 5 | 8.9 | 1.1 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 59.2 | 55.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 177.0 | 159.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 195.7 | 153.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 256.1 | 234.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.6 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 128.5ms，P90 = 301.8ms，Max = 561.2ms。1/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R221 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **5/16** | 0.6-50ms | R221=2/16（**↑3**） |
| **正常**（50-500ms） | 10/16 | 59-302ms | R221=8/16（↑2） |
| **超标**（>500ms） | **1/16** | 561ms | R221=6/16（**↓5**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,143,042，liveRecordCount=5,195,267（tombstone=**1,947,775**，**27.26%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=469  QUERYADV_GE500=57（↑1——.pdf=561ms）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5413
```

## 6. 问题清单

### P0 确认（第 23 轮）：compaction 触发机制缺陷

- **tombstone 27.26%——超阈值 2.26%。COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **R217=202.6ms 证据。COW flush 是正确修复路径。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 23 轮确认）

### P0：flush rewrite 改为 COW 方式

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. `.pdf` 是否恢复到 <500ms（完全恢复）。
2. tombstone 增长趋势（27.26%，距 28% 约 8 轮）。

## 9. 结论

**1 轮恢复（第 9 次确认）——1/16 >500ms（仅 `.pdf`=561ms），Max=561ms（↓67%），P50=128ms（↓59%），极佳 5/16。** R221 的灾难级状态（6/16 >500ms，Max=1698ms，进程调度延迟扩散 overhead=1167ms）完全消退。`x`=12ms（↓89%），`content:import`=177ms（↓86%，overhead 消退）。`.pdf`=561ms 仍 >500ms——postings 区域未完全预热，是 9 次恢复中恢复最不彻底的一次。

**tombstone 27.26%（第 23 轮确认 compaction 缺陷）。** 增速 ~332/min。COMPACTION=2 始终不变。当前两个 P0 问题：compaction 触发缺陷 + flush 写锁阻塞。
