# MacEverything 性能分析报告 R_2606102130

## 1. 本轮范围

- 任务启动时间：2026-06-10 21:30
- 报告文件：`docs/performance_ana/R_2606102130.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9368`
  - `SEG_LINES=8426`
  - `LAST_LOG_TS=2026-06-10 19:47`

## 2. 执行摘要

**广泛偏高——多路径 page cache 争用加剧。** 0/16 >500ms，Max=310ms，P50=88.6ms（**post-compaction 最高 P50！**），lockWait=0。**极佳 6/16（post-compaction 最低！）。** `x`=8ms（缓存稳定，连续 8 轮）。Trigram 多路径偏高：`hello world`=106ms, `test`=96ms, `abc def`=89ms。SoA 过滤也偏高：`size:>100m`=126ms, `dm:today`=109ms。全表扫描总和 795ms（↓5% from R268=834ms——轻微改善）。**flush rewrite=2421ms（持续下降趋势 3981→2688→2421ms）。** FLUSH 68→74（+6）。tombstone=150,123（2.85%——增速持续）。

| 指标 | R268 | R269 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 422ms | **310ms** | ↓27% |
| P50 | 44ms | **88.6ms** | **↑101%（post-compaction 最高！）** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 8/16 | **6/16** | **↓2（post-compaction 最低！）** |
| tombstone | 135,551 (2.56%) | **150,123 (2.85%)** | ↑（增速持续） |
| recordCount | 5,382,181 | **5,396,648** | +14.5K（连续 6 轮偏高） |
| liveRecordCount | 5,246,630 | **5,246,525** | -105（轻微净删除） |
| FLUSH 累计 | 68 | **74** | +6 |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 8 | **8** | 不变 |
| QUERYADV_GE500 | 78 | **78** | 不变 |

### flush rewrite=2421ms——持续下降趋势

**日志末行：**
```
[2026-06-10 19:47:29.618] Flushed v6 flat index, lastEventId=340315119,
liveRecords=5246526 | timing: walSwap=0ms rewrite=2421ms sqcache=3ms total=2425ms
```

| 轮次 | flush rewrite | 变化 |
|------|------------:|------|
| R265 | 2617ms | 首次捕获 |
| R266 | 3981ms | ↑52%（峰值） |
| R268 | 2688ms | ↓32% |
| **R269** | **2421ms** | **↓10%** |

- **flush rewrite 趋势：3981→2688→2421ms（持续下降）。**
- **可能原因：磁盘 I/O 负载逐渐降低（系统活动趋于平静）。**
- **但 2.4 秒仍然很高——每次 flush 仍然重写完整索引。**
- **波动范围：2.4-4.0 秒——受磁盘负载影响，但基线不变。**

### 广泛偏高——P50=89ms 分析

**8/16 查询 >50ms（post-compaction 最多）：**

| Query | R268 | R269 | 变化 | 原因 |
|-------|-----:|-----:|------|------|
| `.pdf` | 45ms | **60ms** | ↑33% | postings 页部分淘汰 |
| `hello world` | 44ms | **106ms** | **↑141%** | postings 页淘汰 |
| `abc def...` | 59ms | **89ms** | ↑51% | 大 postings 集 |
| `test` | 14ms | **96ms** | **↑586%** | 117K candidates overhead |
| `size:>100m` | 51ms | **126ms** | ↑147% | SoA 数据页淘汰 |
| `dm:today` | 85ms | **109ms** | ↑28% | SoA 数据页淘汰 |
| `content:import` | 189ms | **236ms** | ↑25% | 全表 SoA 扫描 |
| `中文测试` | 422ms | **310ms** | ↓27% | 恢复中 |

- **多路径同时偏高——不是零和翻转，是全面 page cache 争用。**
- **flush I/O（rewrite=2421ms）冲刷了 trigram postings 页和 SoA 数据页。**
- **`中文测试` 422→310ms（↓27%——反而在恢复，说明路径数据页被其他查询预热）。**

### `x` 缓存——连续 8 轮稳定

- **8ms（略高于正常 1-5ms——可能系统负载波动）。**
- **连续 8 轮 short-query-cache。**

### Trigram 路径——广泛偏高

| Query | R268 | R269 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 45ms | **60ms** | ↑33% | 正常 |
| `hello world` | 44ms | **106ms** | ↑141% | 正常 |
| `*.swift` | 66ms | **27ms** | ↓59% | 极佳 |
| `main.cpp` | 4ms | **6ms** | ↑50% | 极佳 |
| `abc def...` | 59ms | **89ms** | ↑51% | 正常 |
| `README` | 10ms | **29ms** | ↑190% | 极佳 |
| `test` | 14ms | **96ms** | ↑586% | 正常 |
| `MacEverything` | 7ms | **35ms** | ↑400% | 极佳 |
| Trigram phase2均值 | ~22ms | **~41ms** | **↑86%** |

- **phase2 均值 22→41ms（↑86%——page cache 争用加剧）。**
- **`test`=96ms（phase2=36ms 但 overhead=60ms——系统负载贡献大）。**

### 全表扫描——轻微改善

| Query | R268 | R269 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 189ms | **236ms** | ↑25% |
| `path:/Users` | 223ms | **249ms** | ↑12% |
| `中文测试` | 422ms | **310ms** | **↓27%（恢复中）** |
| **总和** | **834ms** | **795ms** | **↓5%（轻微改善）** |

- **全表扫描 834→795ms（↓5%——`中文测试` 恢复抵消了其他查询上升）。**

### tombstone 增速趋势

| 轮次 | tombstone | tombstone% | 增量 |
|------|----------:|---------:|------:|
| R265 | 90,255 | 1.72% | +17,978 |
| R266 | 106,674 | 2.03% | +16,419 |
| R267 | 121,278 | 2.31% | +14,604 |
| R268 | 135,551 | 2.56% | +14,273 |
| **R269** | **150,123** | **2.85%** | **+14,572** |

- **tombstone 增速稳定在 ~14-15K/轮。**
- **距 25% 阈值：需要 (25%-2.85%) / (~0.28%/轮) ≈ 79 轮。**

### 辅助路径

| Query | R268 | R269 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 1ms | **8ms** | ↑（系统负载） | 极佳 |
| `ext:cpp` | 12ms | **15ms** | ↑25% | 极佳 |
| `size:>100m` | 51ms | **126ms** | ↑147% | 正常 |
| `dm:today` | 85ms | **109ms** | ↑28% | 正常 |
| `verylongquery...` | 2ms | **0.4ms** | ↓80% | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 7.5 | 0.0 | short-query-cache | 0 | 0 | 极佳（稳定） |
| `.pdf` | 5 | 59.5 | 55.4 | advanced-trigram | 0 | 9788 | 正常 |
| `main.cpp` | 5 | 6.4 | 2.2 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 26.8 | 24.3 | advanced-trigram | 0 | 9405 | 极佳 |
| `ext:cpp` | 5 | 14.9 | 14.3 | advanced-ext-index | 0 | 3611 | 极佳 |
| `size:>100m` | 5 | 126.3 | 112.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 105.6 | 102.0 | advanced-trigram | 0 | 3930 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 88.6 | 87.5 | advanced-trigram | 0 | 14458 | 正常 |
| `README` | 5 | 29.1 | 18.2 | advanced-trigram | 0 | 17432 | 极佳 |
| `test` | 5 | 95.7 | 36.2 | advanced-trigram | 0 | 117728 | 正常 |
| `MacEverything` | 5 | 34.6 | 0.7 | advanced-trigram | 0 | 6943 | 极佳 |
| `dm:today` | 5 | 109.3 | 108.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 236.0 | 229.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 248.9 | 244.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 309.6 | 308.0 | advanced-linear-gcd | 0 | 0 | 正常（恢复中） |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 88.6ms，P90 = 248.9ms，Max = 309.6ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R268 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **6/16** | 0.4-35ms | R268=8/16（**↓2，post-compaction 最低！**） |
| **正常**（50-500ms） | 10/16 | 60-310ms | R268=8/16（↑2） |
| **超标**（>500ms） | **0/16** | — | R268=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,396,648，liveRecordCount=5,246,525（tombstone=**150,123**，**2.85%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=709  QUERYADV_GE500=78（不变）
FLUSH=74（+6）  COMPACTION=8  SKIPFLUSH=7266
```

**关键日志：**
```
Flushed v6 flat index | rewrite=2421ms sqcache=3ms total=2425ms
```

## 6. 问题清单

### P0（紧急）：flush rewrite 2.4-4.0 秒

- **rewrite=2421ms（下降趋势，但基线仍高）。**
- **每次 flush 冲刷 page cache → 广泛性能偏高。**

### P0：compaction 触发机制缺陷（根因未修复）

### P0：I/O 优先级保护

### 新增观察：P50 恶化趋势

- **P50 序列（post-compaction）：11→21→30→41→35→41→44→89ms。**
- **上升趋势——flush 频率和 page cache 争用在累积影响基线性能。**

## 7. 优化建议

### P0（紧急）：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. P50 是否继续上升（89ms——已超过可接受基线）。
2. flush rewrite 是否继续下降（2421ms）。
3. 极佳比例是否恢复（6/16——post-compaction 最低）。

## 9. 结论

**广泛偏高——多路径 page cache 争用加剧。** 0/16 >500ms，Max=310ms，但 P50=89ms（post-compaction 最高）。极佳仅 6/16（post-compaction 最低）。`x`=8ms（缓存稳定，连续 8 轮）。Trigram `hello world`=106ms, `test`=96ms, `abc def`=89ms（postings 页淘汰）。SoA `size:>100m`=126ms, `dm:today`=109ms（数据页淘汰）。全表扫描 795ms（↓5%——`中文测试` 422→310ms 恢复中）。

**flush rewrite=2421ms（下降趋势 3981→2688→2421ms——磁盘负载降低）。** 但基线仍高——每次 flush 重写完整索引。FLUSH +6（68→74）。tombstone=150,123（2.85%——增速 ~14K/轮稳定）。recordCount +14.5K（连续 6 轮偏高）。**P50 上升趋势需关注——flush 频率和 page cache 争用在累积影响基线。**
