# MacEverything 性能分析报告 R_2606071746

## 1. 本轮范围

- 任务启动时间：2026-06-07 17:46
- 报告文件：`docs/performance_ana/R_2606071746.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=2063`
  - `SEG_LINES=1121`
  - `LAST_LOG_TS=2026-06-07 17:47`

## 2. 执行摘要

**连续 battery 后 page cache 完全预热——0/16 >500ms，Max=384ms，P50=111ms，lockWait≈0。** 这是本会话（4 轮 battery：R141-R144）中首次实现全部 <500ms。R143 的 battery 预热在 30 分钟后成功保持——验证了"连续查询维持 cache"的结论。

**系统处于理想运行状态——lockWait=0，无 WARN/ERROR，稳定运行 19.6 小时。**

### 关键指标对比

| 指标 | R143（30 min 前） | R144 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 1/16 | **0/16** | **↓ 恢复理想** |
| Max | 957ms | **384ms** | **↓ 60%** |
| P50 | 148ms | **111ms** | **↓ 25%** |
| lockWait max | 15.7ms | **3.7ms** | ↓（微量） |
| tombstone | 1,111,175 (17.90%) | **1,121,495 (18.04%)** | ↑ 正常 |
| tombstone 增速 | ~838/min | **~344/min** | ↓ 进一步减速 |
| recordCount | 6,207,955 | **6,218,447** | +10.5K |
| liveRecordCount | 5,096,780 | **5,096,952** | +172 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 991 | **1051** | +60（30 min 正常） |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（本轮 0 新增） |

### Page Cache 行为——第四轮验证

| Query | R141 | R142 | R143 | R144 | 规律 |
|-------|-----:|-----:|-----:|-----:|------|
| `.pdf` | **834** | 300 | **957** | **233** | 奇冷偶热 ✓ |
| `path:/Users` | 282 | **2456** | 323 | **235** | 热→冷→热→**热** |
| `content:import` | 225 | **1246** | 225 | **255** | 热→冷→热→热 |

- **`.pdf` 从 R143 的 957ms 恢复至 233ms**——R143 预热的 trigram cache 保持了 30 分钟。
- **`path:/Users` 从 323ms 微降至 235ms**——连续两轮热（R143+R144）。
- **`content:import` 保持 225-255ms**——连续三轮稳定。
- **结论：连续 30 分钟间隔的 battery 已将所有路径的 page cache 维持在热状态。**

### 与历史最佳对比

| 指标 | R140（旧会话理想态） | R144 本轮 | 对比 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平 |
| Max | 259ms | **384ms** | R140 稍优 |
| P50 | 28ms | **111ms** | R140 更优（更热 cache） |
| lockWait | 0 | **0** | 持平 |
| content:import | 193ms | **255ms** | R140 稍优 |
| path:/Users | 259ms | **235ms** | **R144 更优** |

- **R144 接近 R140 的理想水平——主要差距在 trigram 路径（P50 111ms vs 28ms）。**
- **全表扫描路径已追平甚至超越 R140（path:/Users 235 < 259ms）。**
- **trigram 路径差距来源**：R140 运行 84 分钟内有 16 次 flush（持续 I/O 活动维持 cache），R144 整个会话仅 1 次 flush。

### Tombstone 变化

- R143 (17:16): 1,111,175 (17.90%)
- R144 (17:46): 1,121,495 (18.04%)
- **增量 +10,320 / 30 min = ~344/min**
- **极低增速——周末/傍晚极度空闲。**
- **距 25% compaction**：还需 ~431K / 344 ≈ **1253 分钟 ≈ 20.9 小时（~明天 14:40）。**
- **注意**：如果夜间/明天白天文件活动增加，增速会回升，实际触发时间可能更早。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 5.4 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 233.0 | 225.5 | advanced-trigram | 0 | 9812 | 正常（**R143 预热保持**） |
| `main.cpp` | 5 | 22.8 | 17.4 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 146.5 | 142.6 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 54.2 | 52.8 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 100.4 | 100.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 310.0 | 297.6 | advanced-trigram | 3.7 | 3614 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 188.0 | 186.7 | advanced-trigram | 0 | 14209 | 正常 |
| `README` | 5 | 80.9 | 73.2 | advanced-trigram | 0 | 16631 | 正常 |
| `test` | 5 | 70.5 | 56.7 | advanced-trigram | 0 | 116512 | 极佳 |
| `MacEverything` | 5 | 8.0 | 1.4 | advanced-trigram | 0 | 6861 | 极佳 |
| `dm:today` | 5 | 58.7 | 58.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 255.2 | 240.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 235.2 | 228.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 384.4 | 336.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.3 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 110.7ms，P90 = 310.0ms，Max = 384.4ms。0/16 查询 >500ms。lockWait max = 3.7ms（仅 `hello world`，可忽略）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R143 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 4/16 | 0.3-23ms | R143=3/16（↑1） |
| **正常**（50-500ms） | 12/16 | 54-384ms | R143=12/16（持平） |
| **超标**（>500ms） | 0/16 | — | R143=1/16（**↓1 消除**） |

### `中文测试` 偏高分析

- `中文测试`=384ms 是本轮 Max——但仍在正常范围（<500ms）。
- **原因**：linear-gcd 路径遍历 6.2M 记录，且排在 battery 第 15 位——前面的 `path:/Users` 已预热了部分 SoA 页面但非全部。
- **对比 R143**：285ms → 384ms（↑35%）——page cache 零和波动，`path:/Users` 在本轮更快（323→235ms），挤占了 `中文测试` 的部分页面。
- **不需关注——正常波动范围。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,218,447，liveRecordCount=5,096,952（tombstone=**1,121,495**，**18.04%**）；
- 进程正常运行（当前会话，运行约 19.6 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=37  QUERYADV_GE500=4（不变——本轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=1051
```

- **QUERYADV=37（+7 本轮）**——部分查询低于日志记录阈值未被记录。
- **QUERYADV_GE500=4（不变）**——本轮 0 个新增慢查询，所有 16 个查询 <500ms。
- **FLUSH=1**——仍无新 flush。
- **SKIPFLUSH=1051（+60）**——持续空闲。
- **COMPACTION=2**——不变。

## 6. 问题清单

### 无活跃问题——系统处于理想运行状态

- 0/16 >500ms，lockWait≈0。
- page cache 已通过连续 battery 完全预热。
- 系统 19.6 小时无 WARN/ERROR。
- tombstone 18.04%——增速极低（344/min），compaction 遥远。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- 下次 compaction 预计 ~20.9 小时后（如果增速保持当前极低水平）。
- 实际可能更早——白天文件活动恢复后增速会加快。
- R138 的崩溃根因仍未排查。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 本轮无 flush 活动——建议保持但优先级无变化。

### ~~P1：Page Cache 保持策略~~（关闭）

- **R141-R144 四轮验证结论：page cache 在连续查询时自维持，冷启动仅首次 1 查询受影响。**
- **不值得为此增加复杂度。关闭此建议。**

### P2：降低 compaction 阈值至 15%

- 当前 18.04%——15% 阈值下早已触发 compaction。

## 8. 下一轮重点

1. page cache 是否继续保持（预期是——但关注长间隔后首查询）。
2. tombstone 增速是否随晚间活动回升。
3. 系统稳定性。

## 9. 结论

**连续 battery 后 page cache 完全预热——0/16 >500ms，Max=384ms，P50=111ms，lockWait≈0。** 这是本会话 4 轮中首次全部 <500ms，验证了"连续 30 分钟间隔查询维持 cache"的结论。`.pdf` 从 R143 的 957ms 恢复至 233ms——trigram cache 保持成功。全表扫描路径持续稳定（path:/Users=235ms，接近 R140 的理想值 259ms）。

**tombstone 18.04%（1.12M），极低增速 344/min，compaction 预计 ~20.9 小时后。** 系统 19.6 小时无 WARN/ERROR，稳定性极佳。

**系统处于理想运行状态——无活跃问题。** Page cache 优化建议正式关闭——接受现状。P0 建议（compaction 崩溃根因）仍待实施但短期无触发风险。
