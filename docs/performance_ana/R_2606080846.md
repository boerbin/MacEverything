# MacEverything 性能分析报告 R_2606080846

## 1. 本轮范围

- 任务启动时间：2026-06-08 08:46
- 报告文件：`docs/performance_ana/R_2606080846.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=3989`
  - `SEG_LINES=3047`
  - `LAST_LOG_TS=2026-06-08 08:47`

## 2. 执行摘要

**全面恢复+`中文测试`创系列新低——0/16 >500ms，Max=250ms，P50=23ms，lockWait=0，极佳比例 12/16（75%）。** 全表扫描总和从 R174 的 1089ms 骤降至 546ms（↓50%）——日间启动潮已过，page cache 竞争大幅消退。`中文测试`=111ms **创本系列绝对新低**（超越 R169=141ms）！Trigram 均值 18.1ms 稳定。`dm:today`=47ms 回归极佳范围。

### 关键指标对比

| 指标 | R174（30 min 前） | R175 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 402ms | **250ms** | **↓ 38%** |
| P50 | 20ms | **23ms** | 持平（正常波动） |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 10/16 | **12/16** | **↑2** |
| tombstone | 1,371,476 (21.19%) | **1,379,443 (21.28%)** | ↑ 正常 |
| tombstone 增速 | ~283/min | **~265/min** | ↓ 回落 |
| recordCount | 6,470,737 | **6,478,806** | +8.1K |
| liveRecordCount | 5,099,261 | **5,099,363** | +102 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 2790 | **2850** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 32 轮 0 新增） |

### 全表扫描——从峰值大幅恢复

| Query | R174（峰值） | R175 本轮 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 316ms | **250ms** | **↓ 21%** |
| `path:/Users` | 371ms | **185ms** | **↓ 50%** |
| `中文测试` | 402ms | **111ms** | **↓ 72%（系列新低！）** |
| **总和** | **1089ms** | **546ms** | **↓ 50%** |

- **全表扫描总和 546ms——从本系列最高的 1089ms 骤降 50%！**
- **`中文测试`=111ms——创本系列绝对新低（超越 R169=141ms、R167=146ms）！**
- **`path:/Users`=185ms（↓50%）——linear-gcd 区域完全恢复。**
- **原因**：08:30 后日间启动潮结束，系统 I/O 竞争大幅消退，page cache 重新可用。

### 日间 page cache 压力完整周期

| 轮次 | 时间 | 全表扫总和 | Trigram 均值 | 说明 |
|------|------|----------:|----------:|------|
| R172 | 07:16 | 781ms | 14.4ms | 日间稳态 |
| R173 | 07:46 | 879ms | 38.5ms | **压力事件（trigram 受影响）** |
| R174 | 08:16 | 1089ms | 18.2ms | **峰值（trigram 恢复，SoA 最差）** |
| **R175** | **08:46** | **546ms** | **18.1ms** | **全面恢复** |

- **完整周期 ~1 小时**：压力上升（R173）→ 峰值（R174）→ 恢复（R175）。
- **Trigram 自预热成功**：R173→R174 即恢复。SoA/linear 需等外部竞争消退。
- **546ms 低于日间稳态范围（750-800ms）——post-peak 过度恢复效应（系统缓存大量文件后 I/O 减少）。**

### `中文测试`=111ms——分析

- **此前系列最低**：R169=141ms（05:46 凌晨）、R167=146ms（04:47 凌晨极静默）。
- **本轮 111ms 打破两者——在日间时段！**
- **原因**：启动潮过后，macOS unified buffer cache 中保留了大量文件系统元数据，linear-gcd 的顺序读与 readahead 协同效率极高。
- **另一可能**：R173/R174 连续两轮全表扫描将 linear-gcd 区域完全预热——后续访问直接命中。

### Trigram 路径——日间正常稳态

| Query | R174 | R175 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 17ms | **27ms** | ↑ 波动 |
| `*.swift` | 12ms | **16ms** | 持平 |
| `hello world` | 23ms | **23ms** | 持平 |
| `abc def...` | 40ms | **42ms** | 持平 |
| `README` | 10ms | **12ms** | 持平 |
| `test` | 20ms | **11ms** | ↓ 45% |
| `MacEverything` | 6ms | **2ms** | 持平 |
| Trigram 均值 | 18.2ms | **18.1ms** | **持平（日间稳态确认）** |

- **Trigram 均值 18.1ms——连续 2 轮维持在 18ms，确认为日间活跃期稳态。**
- **对比凌晨 12ms 和早晨 14ms——日间 page cache 竞争增加约 30-50% 但完全在可接受范围。**
- **所有单项 <50ms——无异常。**

### SoA Filter——`dm:today` 回归极佳

| Query | R174 | R175 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 31ms | **33ms** | 持平 |
| `dm:today` | 106ms | **47ms** | **↓ 56%（回归极佳）** |

- **`dm:today`=47ms——从本系列最高的 106ms 回落至极佳范围（<50ms）。**
- **两项总和**：R174=137ms → R175=80ms——↓ 42%。
- **SoA 时间戳区域 page cache 竞争消退确认。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 0.5 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 26.6 | 24.0 | advanced-trigram | 0 | 9812 | 极佳 |
| `main.cpp` | 5 | 4.5 | 2.5 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 15.6 | 14.4 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 7.5 | 7.0 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 32.6 | 32.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 23.4 | 22.8 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 42.3 | 42.3 | advanced-trigram | 0 | 14216 | 极佳 |
| `README` | 5 | 11.7 | 9.8 | advanced-trigram | 0 | 16631 | 极佳 |
| `test` | 5 | 10.8 | 9.1 | advanced-trigram | 0 | 116513 | 极佳 |
| `MacEverything` | 5 | 2.3 | 0.6 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 47.3 | 47.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 249.9 | 236.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 184.9 | 181.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 111.1 | 110.7 | advanced-linear-gcd | 0 | 0 | 正常（**系列新低**） |
| `verylongquery...` | 0 | 0.1 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 23.4ms，P90 = 249.9ms，Max = 249.9ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R174 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 12/16 | 0.1-47ms | R174=10/16（**↑2**） |
| **正常**（50-500ms） | 4/16 | 111-250ms | R174=6/16（↓2） |
| **超标**（>500ms） | 0/16 | — | R174=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,478,806，liveRecordCount=5,099,363（tombstone=**1,379,443**，**21.28%**）；
- 进程正常运行（当前会话，运行约 34.6 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=133  QUERYADV_GE500=4（不变——连续 32 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=2850
```

- **连续 32 轮 0 个新增 >500ms 查询（R144-R175）。**
- **FLUSH=1**——无新 flush（34.6 小时仅启动时 1 次）。
- **SKIPFLUSH=2850**——系统持续空闲。

## 6. 问题清单

### 无活跃问题

- 0/16 >500ms，12/16 <50ms（75%——恢复）。
- P50=23ms，Max=250ms，lockWait=0。
- 连续 32 轮 0 新增慢查询。
- 34.6 小时无 WARN/ERROR。
- R173-R174 page cache 压力事件已完全恢复。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 21.28%，增速 ~265/min（回落）。
- 距 25% 阈值约 ~22 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 系统空闲时无影响。

## 8. 下一轮重点

1. 全表扫描总和是否稳定在 500-600ms（post-peak 稳态）。
2. `中文测试` 新低 111ms 是否可持续。
3. Trigram 均值是否稳定在 ~18ms（日间水平）。

## 9. 结论

**全面恢复+`中文测试`创系列新低——0/16 >500ms，Max=250ms，P50=23ms，lockWait=0，极佳比例 12/16。** 全表扫描总和从峰值 1089ms 骤降至 546ms（↓50%）——日间启动潮过后 page cache 竞争大幅消退。`中文测试`=111ms 创本系列绝对新低——post-peak 过度恢复效应（连续全表扫描预热+系统 I/O 消退协同）。

**R173-R174 压力事件完整周期确认**：压力上升→峰值→恢复，总计 ~1 小时。Trigram 自预热在 1 轮内恢复，SoA/linear 需等外部竞争消退。连续 32 轮 0 新增慢查询，34.6 小时无异常。tombstone 21.28%（1.38M），增速 265/min（回落），距 25% 约 22 小时。系统恢复力验证——即使经历日间 I/O 高峰，30 分钟内全面恢复。
