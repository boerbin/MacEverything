# MacEverything 性能分析报告 R_2606101930

## 1. 本轮范围

- 任务启动时间：2026-06-10 19:30
- 报告文件：`docs/performance_ana/R_2606101930.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9300`
  - `SEG_LINES=8358`
  - `LAST_LOG_TS=2026-06-10 17:47`

## 2. 执行摘要

**⚠️⚠️ I/O 风暴！`中文测试`=731ms（>500ms），`.pdf`=452ms（接近超标）。flush rewrite=2617ms 捕获！** 2/16 >500ms，Max=731ms，P50=87.2ms，lockWait max=1.5ms。**极佳 2/16（post-compaction 最差！）。** 全面 page cache 淘汰——所有路径均显著恶化。`x`=137ms（short-query-cache 命中但 overhead 异常——系统负载导致）。**日志末行揭示：flush rewrite=2617ms（2.6 秒！）——flush 期间大量磁盘 I/O 冲刷了整个 page cache。** QUERYADV_GE500=78（+2）。FLUSH 43→50（+7！新高）。tombstone=90,255（1.72%）。recordCount +18.7K（连续 2 轮偏高）。

| 指标 | R264 | R265 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 1/16 | **2/16** | **↑1** |
| Max | 528ms | **731ms** | **↑38%** |
| P50 | 41ms | **87.2ms** | **↑113%** |
| lockWait max | 9.5ms | **1.5ms** | ↓（但系统整体慢） |
| 极佳比例(<50ms) | 8/16 | **2/16** | **↓6（post-compaction 最差！）** |
| tombstone | 72,277 (1.37%) | **90,255 (1.72%)** | ↑（增速持续偏高） |
| recordCount | 5,317,781 | **5,336,463** | **+18.7K（连续 2 轮偏高！）** |
| liveRecordCount | 5,245,504 | **5,246,208** | +704（正常） |
| FLUSH 累计 | 43 | **50** | **+7（新高！）** |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 8 | **8** | 不变 |
| QUERYADV_GE500 | 76 | **78** | **+2** |

### ⚠️⚠️ flush rewrite=2617ms——I/O 风暴根因

**日志末行（最新日志事件）：**
```
[2026-06-10 17:47:29.822] Flushed v6 flat index, lastEventId=339120161,
liveRecords=5246207 | timing: walSwap=0ms rewrite=2617ms sqcache=7ms total=2626ms
```

- **flush rewrite=2617ms——单次 flush 耗时 2.6 秒写磁盘！**
- **这是 I/O 风暴的直接原因：**
  1. **flush 将 5.2M 条记录的完整索引重写到磁盘（~400-500MB 数据量）。**
  2. **重写过程产生大量顺序 I/O，冲刷了 page cache 中的所有搜索热数据。**
  3. **battery 查询在 flush 完成后立即执行——page cache 几乎为空。**
- **这是 P0 flush COW 改造的最强有力的运行时证据：**
  - R257：lockWait=325.7ms（查询等待 flush 锁）。
  - R265：flush rewrite=2617ms（flush 本身耗时 2.6 秒 + page cache 冲刷）。
  - **两个维度都证明了当前 flush 实现的问题。**

### FLUSH +7（新高）

- **R264=+5 → R265=+7（新高！此前最高 +6）。**
- **recordCount +18.7K（连续 2 轮偏高——文件系统活动持续活跃）。**
- **更多 WAL entries → 更频繁 flush → 更多 I/O → 更多 page cache 冲刷。**
- **恶性循环：高文件系统活动 → 高 flush 频率 → page cache 竞争 → 查询性能恶化。**

### `x`=137ms——short-query-cache 命中但 overhead 异常

- **`x` 路径仍为 short-query-cache（phase2=0ms），但 queryTime=137ms。**
- **137ms overhead 原因：**
  1. **系统负载极高——CPU 调度延迟。**
  2. **flush rewrite=2617ms 期间的 I/O wait 影响了所有线程。**
  3. **或者结果序列化过程中的 page fault（结果数据页被淘汰）。**
- **这不是缓存失效——是系统级性能恶化。**

### 全面 page cache 淘汰

| Query | R264 | R265 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 79ms | **452ms** | **↑472%** | 正常（接近超标） |
| `hello world` | 63ms | **87ms** | ↑38% | 正常 |
| `*.swift` | 41ms | **80ms** | ↑95% | 正常 |
| `main.cpp` | 12ms | **14ms** | ↑17% | 极佳 |
| `abc def...` | 61ms | **112ms** | ↑84% | 正常 |
| `README` | 19ms | **11ms** | ↓42% | 极佳 |
| `test` | 27ms | **53ms** | ↑96% | 正常 |
| `MacEverything` | 18ms | **133ms** | **↑639%** | 正常 |
| Trigram phase2均值 | ~33ms | **~89ms** | **↑170%** |

- **`.pdf`=452ms（phase2=381ms——9.8K postings 的 page cache 完全淘汰）。**
- **`MacEverything`=133ms（phase2=2.4ms 但 overhead=130ms——系统负载）。**
- **仅 `main.cpp`=14ms 和 `README`=11ms 保持极佳（小 postings 集，数据少）。**

### 全表扫描——严重恶化

| Query | R264 | R265 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 183ms | **294ms** | ↑61% |
| `path:/Users` | 528ms | **291ms** | **↓45%（恢复！）** |
| `中文测试` | 260ms | **731ms** | **↑181%（>500ms！）** |
| **总和** | **971ms** | **1316ms** | **↑36%** |

- **`中文测试`=731ms（↑181%——Unicode 匹配的 SoA 扫描 + 完全 page cache miss）。**
- **`path:/Users`=291ms（↓45%——从 R264=528ms 恢复，1-round recovery 第 16 次确认！）。**
- **全表扫描总和 971→1316ms（↑36%）——严重恶化，主要由 `中文测试`=731ms 贡献。**

### 1-round recovery 第 16 次确认

- **`path:/Users`：R264=528ms（>500ms）→ R265=291ms（恢复）。**
- **16/16 = 100% 成功率。**
- **但本轮 `中文测试`=731ms 新增 >500ms——recovery 是查询级别的，不是全局的。**

### 零和总量

| 类别 | R264 | R265 | 变化 |
|------|-----:|-----:|------|
| Trigram 总和 | ~301ms | ~933ms | **+632ms** |
| 全表扫描总和 | 971ms | 1316ms | +345ms |
| **Battery 总和** | ~1412ms | ~2603ms | **↑84%** |

- **Battery 总和 1412→2603ms（↑84%）——I/O 风暴导致全面恶化。**
- **不是零和翻转——是全面恶化（trigram 和全表扫描同时恶化）。**

### 辅助路径

| Query | R264 | R265 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 3ms | **137ms** | ↑（overhead，非缓存失效） | 正常 |
| `ext:cpp` | 24ms | **45ms** | ↑88% | 极佳 |
| `size:>100m` | 33ms | **58ms** | ↑76% | 正常 |
| `dm:today` | 62ms | **104ms** | ↑68% | 正常 |
| `verylongquery...` | 0.5ms | **2ms** | ↑（lockWait=1.5ms） | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 136.8 | 0.0 | short-query-cache | 0 | 0 | 正常（overhead 异常） |
| `.pdf` | 5 | 451.6 | 381.1 | advanced-trigram | 0 | 9788 | 正常（接近超标！） |
| `main.cpp` | 5 | 13.9 | 9.4 | advanced-trigram | 0.8 | 85 | 极佳 |
| `*.swift` | 5 | 79.8 | 76.4 | advanced-trigram | 0 | 9405 | 正常 |
| `ext:cpp` | 5 | 45.2 | 43.3 | advanced-ext-index | 0 | 3611 | 极佳 |
| `size:>100m` | 5 | 57.6 | 57.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 87.2 | 84.8 | advanced-trigram | 0 | 3930 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 112.1 | 111.8 | advanced-trigram | 0 | 14459 | 正常 |
| `README` | 5 | 11.1 | 5.3 | advanced-trigram | 0 | 17431 | 极佳 |
| `test` | 5 | 53.2 | 44.0 | advanced-trigram | 0 | 117727 | 正常 |
| `MacEverything` | 5 | 132.9 | 2.4 | advanced-trigram | 0 | 6942 | 正常（overhead） |
| `dm:today` | 5 | 104.2 | 104.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 293.6 | 281.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 291.1 | 285.7 | advanced-linear-gcd | 0 | 0 | 正常（恢复！） |
| `中文测试` | 0 | **731.1** | 716.0 | advanced-linear-gcd | 0 | 0 | **⚠️ >500ms** |
| `verylongquery...` | 0 | 1.8 | 0.0 | advanced-trigram | 1.5 | 0 | 极佳 |

**P50 = 87.2ms，P90 = 293.6ms，Max = 731.1ms。2/16 查询 >500ms。lockWait max = 1.5ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R264 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **2/16** | 2-45ms | R264=8/16（**↓6**） |
| **正常**（50-500ms） | 12/16 | 53-452ms | R264=7/16（↑5） |
| **超标**（>500ms） | **2/16** | 731ms | R264=1/16（↑1） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,336,463，liveRecordCount=5,246,208（tombstone=**90,255**，**1.72%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=693  QUERYADV_GE500=78（+2！）
FLUSH=50（+7！新高）  COMPACTION=8  SKIPFLUSH=7266
```

**关键日志：**
```
Flushed v6 flat index | rewrite=2617ms sqcache=7ms total=2626ms
```

## 6. 问题清单

### P0（加强）：flush rewrite=2617ms——I/O 风暴直接证据

- **单次 flush 耗时 2.6 秒重写完整索引——冲刷所有 page cache。**
- **这是 R251（`.pdf`=2829ms）和 R264-R265（>500ms）的根因。**
- **COW flush 改造的必要性获得运行时证据支持：**
  - lockWait=325.7ms（R257）——查询等待。
  - rewrite=2617ms（R265）——flush 本身 I/O。
  - page cache 冲刷——所有查询恶化。

### P0：compaction 触发机制缺陷（根因未修复）

### P0：I/O 优先级保护

- **高 recordCount 增速（+18.7K）→ 高 flush 频率（+7）→ page cache 竞争。**

## 7. 优化建议

### P0（最高优先级）：flush rewrite 改为 COW 方式

- **当前：完整重写 5.2M 条记录索引（~400-500MB），耗时 2.6 秒。**
- **目标：COW 模式——仅写变更页（增量写入），降至 <100ms。**
- **预期效果：消除 page cache 冲刷，消除 lockWait。**

### P0：修复 compaction 触发机制

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. `中文测试`=731ms 是否 1-round recovery（预计恢复——16/16 历史成功率）。
2. `.pdf`=452ms 是否继续恶化或恢复。
3. `x`=137ms overhead 是否回归（系统负载消退后）。
4. FLUSH 频率是否回落（+7 新高——recordCount 增速相关）。

## 9. 结论

**⚠️⚠️ I/O 风暴——flush rewrite=2617ms 冲刷 page cache。** `中文测试`=731ms（>500ms），`.pdf`=452ms（接近超标）。2/16 >500ms，Max=731ms，极佳仅 2/16（post-compaction 最差）。Battery 总和 1412→2603ms（↑84%）。`x`=137ms（cache 命中但系统 overhead 异常）。`path:/Users` R264=528ms → R265=291ms（**1-round recovery 第 16 次确认，16/16=100%**）。

**flush rewrite=2617ms 是 I/O 风暴的直接根因——单次 flush 重写 5.2M 条完整索引，产生 ~500MB 磁盘 I/O，冲刷所有 page cache。** 这是 P0 COW flush 改造获得的最强运行时证据（lockWait=325.7ms + rewrite=2617ms + page cache 冲刷三重证据链）。FLUSH +7（新高），recordCount +18.7K（连续 2 轮偏高）。tombstone=90,255（1.72%）。
