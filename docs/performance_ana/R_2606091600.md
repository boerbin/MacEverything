# MacEverything 性能分析报告 R_2606091600

## 1. 本轮范围

- 任务启动时间：2026-06-09 16:00
- 报告文件：`docs/performance_ana/R_2606091600.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6105`
  - `SEG_LINES=5163`
  - `LAST_LOG_TS=2026-06-09 11:17`

## 2. 执行摘要

**午后第 2 轮 page cache 淘汰——3/16 >500ms，Max=1241ms，P50=86ms，lockWait=1.3ms，极佳比例 3/16。** `hello world`=1241ms（trigram postings 大面积被淘汰），`size:>100m`=742ms（SoA 区域被淘汰），`.pdf`=524ms（trigram postings 受压）。QUERYADV_GE500 +4（本轮新增 4 条，含 `hello world`、`size:>100m`、`.pdf` 及日志中其他查询）。

| 指标 | R209 | R210 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **3/16** | **↑3（午后 I/O 再袭）** |
| Max | 80ms | **1241ms** | **↑1451%** |
| P50 | 22ms | **86ms** | ↑291% |
| lockWait max | 0ms | **1.3ms** | ↑（轻度） |
| 极佳比例(<50ms) | 14/16 | **3/16** | **↓11** |
| tombstone | 1,793,888 (25.78%) | **1,805,640 (25.90%)** | ↑ |
| tombstone 增速 | ~88/min | **~391/min** | **↑344%（午后文件活动激增）** |
| recordCount | 6,957,926 | **6,970,383** | **+12.5K** |
| liveRecordCount | 5,164,038 | **5,164,743** | +705 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 4624 | **4693** | +69 |
| COMPACTION | 2 | **2** | **不变——第 11 轮确认缺陷** |
| QUERYADV_GE500 | 44 | **48** | **↑4** |

### `hello world`=1241ms——trigram postings 大面积淘汰

- **phase2=1235ms，lockWait=1.3ms——几乎全是 page fault。**
- **candidates=3669——中等数量，但 trigram 交集计算需要读取多个 postings 数组。**
- **lockWait=1.3ms——轻度锁竞争（可能后台 flush/更新操作）。**
- **R209=45ms → R210=1241ms（↑2658%）——从极佳到灾难。**

### `size:>100m`=742ms——SoA 区域被淘汰

- **phase2=740ms，lockWait=0——纯 page cache 淘汰。**
- **R209=23ms → R210=742ms（↑3126%）——SoA fileSize 数组被完全逐出。**
- **pure-filter-soa-gcd 路径需要全量扫描 fileSize 数组——mmap 区域被淘汰后需要从磁盘重新读取。**

### `.pdf`=524ms——trigram 受压

- **phase2=437ms，queryTime=524ms——overhead=87ms（包含 phase1 延迟）。**
- **R209=80ms → R210=524ms（↑555%）——从正常到超标。**
- **candidates=9842——trigram postings 和结果验证阶段均需要 page cache 支持。**

### 全表扫描——午后升高但可控

| Query | R209 | R210 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 67ms | **187ms** | ↑179% |
| `path:/Users` | 59ms | **265ms** | ↑349% |
| `中文测试` | 67ms | **273ms** | ↑307% |
| **总和** | **193ms** | **725ms** | **↑276%** |

- **全表扫描总和 193→725ms（↑276%）——从系列最低回升但仍在正常范围。**
- **低于 R207=884ms，远低于 R208=4252ms——不是灾难级别。**

### Trigram 路径——分化显著

| Query | R209 | R210 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 80ms | **524ms** | ↑555% | **>500ms** |
| `hello world` | 45ms | **1241ms** | **↑2658%** | **>500ms** |
| `*.swift` | 44ms | **236ms** | ↑436% | 正常 |
| `main.cpp` | 13ms | **86ms** | ↑562% | 正常 |
| `abc def...` | 27ms | **196ms** | ↑626% | 正常 |
| `README` | 10ms | **54ms** | ↑440% | 正常 |
| `test` | 13ms | **78ms** | ↑500% | 正常 |
| `MacEverything` | 7ms | **32ms** | ↑357% | 极佳 |
| Trigram 均值 | 30ms | **306ms** | **↑920%** | 大面积受压 |

- **Trigram 均值 30→306ms（↑920%）——全面恶化。**
- **2/8 trigram >500ms——`.pdf`=524ms 和 `hello world`=1241ms。**
- **`hello world` 是本轮最慢——multi-word trigram 交集需要读取多个 postings 区域。**

### tombstone 25.90%——增速激增

- **tombstone=1,805,640（25.90%）——超阈值 0.90%。**
- **增速 ~391/min（从 R209=88/min 激增——午后文件活动爆发）。**
- **recordCount +12.5K，liveRecordCount +705——净新增仅 705 条，11.8K 为 tombstone。**
- **tombstone 即将突破 26%——数据文件持续膨胀。**

### `x`=16ms——进程调度延迟

- **R209=2ms → R210=16ms——午后系统负载导致进程调度延迟。**
- **低于 R199=85ms——不是严重问题。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 16.4 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 524.2 | 437.0 | advanced-trigram | 0 | 9842 | **>500ms** |
| `main.cpp` | 5 | 86.1 | 71.3 | advanced-trigram | 0 | 82 | 正常 |
| `*.swift` | 5 | 235.6 | 229.2 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 82.4 | 80.6 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 742.1 | 739.9 | advanced-pure-filter-soa-gcd | 0 | 0 | **>500ms** |
| `hello world` | 5 | 1241.4 | 1234.9 | advanced-trigram | 1.3 | 3669 | **>500ms** |
| `abc def ghi jkl mno pqr` | 0 | 196.2 | 192.0 | advanced-trigram | 0 | 14349 | 正常 |
| `README` | 5 | 53.8 | 46.2 | advanced-trigram | 0 | 17009 | 正常 |
| `test` | 5 | 77.6 | 64.1 | advanced-trigram | 0 | 116910 | 正常 |
| `MacEverything` | 5 | 32.2 | 2.9 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 60.5 | 59.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 186.7 | 177.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 264.9 | 254.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 272.9 | 272.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 1.1 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 86.1ms，P90 = 524.2ms，Max = 1241.4ms。3/16 查询 >500ms。lockWait max = 1.3ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R209 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **3/16** | 1.1-32ms | R209=14/16（**↓11**） |
| **正常**（50-500ms） | 10/16 | 54-273ms | R209=2/16（↑8） |
| **超标**（>500ms） | **3/16** | 524-1241ms | R209=0/16（**↑3**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,970,383，liveRecordCount=5,164,743（tombstone=**1,805,640**，**25.90%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=371  QUERYADV_GE500=48（↑4——hello world, size:>100m, .pdf 及日志中其他查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4693
```

## 6. 问题清单

### P0 确认（第 11 轮）：compaction 触发机制缺陷

- **tombstone 25.90%——超阈值 0.90%，即将突破 26%。**
- **tombstone 增速 391/min——午后文件活动爆发加速膨胀。**
- **数据文件越大→page cache 覆盖率越低→淘汰事件越频繁越严重。**

### 午后 page cache 淘汰——第 2 轮

- **R208=3/16 >500ms，R209=0/16（恢复），R210=3/16 >500ms（再次淘汰）。**
- **午后 I/O 压力持续——page cache 竞争激烈。**
- **预计 1 轮恢复。**

### 新发现：`size:>100m`=742ms——SoA 区域首次 >500ms

- **此前 `size:>100m` 系列最高约 120ms——本轮 742ms 是首次 >500ms。**
- **SoA fileSize 数组被完全淘汰——说明午后 I/O 压力影响范围扩大到更多 mmap 区域。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 11 轮确认）

### P0.5：flush rewrite 改为 COW 方式（持续建议）

### P2：madvise MADV_WILLNEED 热区维护

- **SoA 区域首次被完全淘汰——可考虑定期 madvise 保持核心区域在 page cache 中。**

## 8. 下一轮重点

1. 3/16 >500ms 是否 1 轮恢复（预计是）。
2. tombstone 是否突破 26%。
3. `size:>100m` 是否恢复。

## 9. 结论

**午后第 2 轮 page cache 淘汰——3/16 >500ms，Max=1241ms（`hello world`），P50=86ms。** `size:>100m`=742ms（SoA 区域首次 >500ms），`.pdf`=524ms。Trigram 均值 30→306ms（↑920%）。QUERYADV_GE500 +4（44→48）。

**tombstone 25.90%（第 11 轮确认 compaction 缺陷）——增速激增至 391/min（午后文件活动爆发），recordCount +12.5K 中仅 705 为 live。** 数据文件持续膨胀→page cache 覆盖率下降→淘汰事件频率和严重性均升高。预计 1 轮恢复。
