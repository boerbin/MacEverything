# MacEverything 性能分析报告 R_2606091500

## 1. 本轮范围

- 任务启动时间：2026-06-09 15:00
- 报告文件：`docs/performance_ana/R_2606091500.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6015`
  - `SEG_LINES=5073`
  - `LAST_LOG_TS=2026-06-09 10:37`

## 2. 执行摘要

**午后 page cache 大面积淘汰——3/16 >500ms，Max=3436ms，P50=207ms，lockWait=3.5ms，极佳比例 1/16。** 7 轮连续 0/16 >500ms 的稳态被打破。`path:/Users`=3436ms（系列第二高，仅次 R176=2217ms... 实际已超越！），`abc def`=879ms，`test`=801ms。

| 指标 | R207 | R208 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **3/16** | **↑3（稳态打破）** |
| Max | 385ms | **3436ms** | **↑793%** |
| P50 | 79ms | **207ms** | ↑162% |
| lockWait max | 0ms | **3.5ms** | ↑（轻度） |
| 极佳比例(<50ms) | 5/16 | **1/16** | ↓4 |
| tombstone | 1,774,948 (25.59%) | **1,791,241 (25.76%)** | ↑ |
| tombstone 增速 | ~78/min | **~181/min** | ↑132%（午后回升） |
| recordCount | 6,936,250 | **6,954,650** | **+18.4K** |
| liveRecordCount | 5,161,302 | **5,163,409** | +2.1K |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 4563 | **4614** | +51 |
| COMPACTION | 2 | **2** | **不变——第 9 轮确认缺陷** |
| QUERYADV_GE500 | 41 | **44** | **↑3** |

### `path:/Users`=3436ms——系列绝对最高！

- **phase2=3425ms，lockWait=0——纯 page cache 淘汰。**
- **此前系列最高 R176=2217ms——本轮 ↑55%，新纪录！**
- **路径数组区域的 page cache 被大面积淘汰——午后系统 I/O 活动（可能 Spotlight 重索引或 Time Machine 备份）大量读取磁盘，将路径数组 mmap 区域完全逐出。**
- **3436ms 超过 3 秒——用户体感极差。**

### `abc def`=879ms 和 `test`=801ms——trigram 受压

- **`abc def`：phase2=878ms，lockWait=0，candidates=14345——trigram postings 区域被淘汰。**
- **`test`：phase2=784ms，lockWait=0，candidates=116907——高 candidates，postings 分散读取。**
- **两个高 candidates 查询同时 >500ms——说明 trigram postings 文件的大面积区域被逐出 page cache。**

### `x`=18ms——轻度进程延迟

- **R206=2.6ms → R208=17.6ms——进程调度延迟升高但可控。**
- **低于 R199=85ms——不是系统级崩溃，是 I/O 竞争。**

### `MacEverything`=145ms——异常偏高

- **phase2=1.9ms（正常），但 queryTime=145ms——overhead=143ms。**
- **lockWait=3.5ms——轻度锁竞争。**
- **145ms - 1.9ms - 3.5ms = ~140ms 无法解释的 overhead——进程调度延迟或 phase1 延迟。**

### `ext:cpp`=379ms——首次接近 400ms

- **phase2=376ms，lockWait=0——ext-index 区域 page cache 被淘汰。**
- **此前最高 R176=168ms——本轮 ↑126%。**

### 全表扫描——灾难级

| Query | R207 | R208 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 280ms | **327ms** | ↑17% |
| `path:/Users` | 219ms | **3436ms** | **↑1469%** |
| `中文测试` | 385ms | **489ms** | ↑27% |
| **总和** | **884ms** | **4252ms** | **↑381%** |

- **全表扫描总和 884→4252ms（↑381%）——`path:/Users`=3436ms 主导。**
- **仅次于 R199=6155ms（隔夜冷启动）。**

### Trigram 路径——全面受压

| Query | R207 | R208 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 157ms | **288ms** | ↑83% | 正常 |
| `hello world` | 215ms | **384ms** | ↑79% | 正常 |
| `*.swift` | 82ms | **207ms** | ↑152% | 正常 |
| `main.cpp` | 14ms | **32ms** | ↑129% | 极佳 |
| `abc def...` | 106ms | **879ms** | **↑729%** | **>500ms** |
| `README` | 58ms | **198ms** | ↑241% | 正常 |
| `test` | 74ms | **801ms** | **↑982%** | **>500ms** |
| `MacEverything` | 18ms | **145ms** | ↑706% | 正常 |
| Trigram 均值 | 91ms | **367ms** | **↑303%** | 大面积受压 |

- **Trigram 均值 91→367ms（↑303%）——全面恶化。**
- **2/8 trigram >500ms——`abc def`=879ms 和 `test`=801ms。**

### tombstone 25.76%——增速回升

- **tombstone=1,791,241（25.76%）。**
- **增速 ~181/min（从 R207=78/min 回升——午后文件活动恢复）。**
- **recordCount +18.4K——午后大量文件变更。**
- **COMPACTION=2——第 9 轮确认缺陷。**

### tombstone 增长对线性扫描的量化影响

- **tombstone 25.76% 时 `path:/Users`=3436ms。**
- **tombstone 25.43% 时（R204）`path:/Users`=375ms。**
- **0.33 个百分点的 tombstone 增长 → 线性扫描 ↑816%——但这主要是 page cache 效应，不是 tombstone 直接导致。**
- **然而 tombstone 增长增加了数据文件大小，使得 page cache 覆盖率下降——间接加剧了淘汰事件的严重性。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 17.6 | 0.0 | short-query-cache | 0 | 0 | 极佳（微延迟） |
| `.pdf` | 5 | 287.8 | 280.4 | advanced-trigram | 0 | 9842 | 正常 |
| `main.cpp` | 5 | 32.2 | 24.3 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 207.1 | 199.1 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 379.4 | 376.2 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 83.7 | 83.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 383.8 | 370.6 | advanced-trigram | 0 | 3669 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 878.9 | 878.1 | advanced-trigram | 0 | 14345 | **>500ms** |
| `README` | 5 | 198.1 | 126.0 | advanced-trigram | 0.9 | 17009 | 正常 |
| `test` | 5 | 800.8 | 784.0 | advanced-trigram | 0 | 116907 | **>500ms** |
| `MacEverything` | 5 | 145.2 | 1.9 | advanced-trigram | 3.5 | 6859 | 正常（高 overhead） |
| `dm:today` | 5 | 127.7 | 119.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 327.3 | 304.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 3435.5 | 3425.3 | advanced-linear-gcd | 0 | 0 | **>500ms（系列最高！）** |
| `中文测试` | 0 | 488.7 | 485.4 | advanced-linear-gcd | 0 | 0 | 正常（边缘） |
| `verylongquery...` | 0 | 1.1 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 207.1ms，P90 = 878.9ms，Max = 3435.5ms。3/16 查询 >500ms。lockWait max = 3.5ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R207 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **1/16** | 1.1-32ms | R207=5/16（↓4） |
| **正常**（50-500ms） | 12/16 | 84-489ms | R207=11/16（↑1） |
| **超标**（>500ms） | **3/16** | 801-3436ms | R207=0/16（**↑3**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,954,650，liveRecordCount=5,163,409（tombstone=**1,791,241**，**25.76%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=362  QUERYADV_GE500=44（+3——path:/Users, abc def, test）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4614
```

## 6. 问题清单

### P0 确认（第 9 轮）：compaction 触发机制缺陷

- **tombstone 25.76%——超阈值 0.76%。**
- **tombstone 膨胀增加了数据文件大小→page cache 覆盖率下降→淘汰事件更严重。**
- **`path:/Users`=3436ms（系列最高）可能部分归因于 tombstone 膨胀导致的文件增大。**

### 午后 page cache 大面积淘汰

- **3/16 >500ms——7 轮稳态后首次打破。**
- **系统 I/O 活动（Spotlight/Time Machine）导致多个区域同时被淘汰。**
- **预计 1 轮恢复（系列已证明 6 次）。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 9 轮确认）

- **tombstone 膨胀正在加剧 page cache 淘汰的严重性。**

### P0.5：flush rewrite 改为 COW 方式（持续建议）

## 8. 下一轮重点

1. 3/16 >500ms 是否 1 轮恢复（预计是）。
2. `path:/Users` 是否从 3436ms 恢复。
3. tombstone 增长趋势。

## 9. 结论

**午后 page cache 大面积淘汰——3/16 >500ms，Max=3436ms（`path:/Users` 系列最高！），P50=207ms。** 7 轮连续 0/16 >500ms 的稳态被打破。`abc def`=879ms，`test`=801ms。trigram 均值 91→367ms（↑303%）。全表扫描总和 4252ms（↑381%）。

**tombstone 25.76%（第 9 轮确认 compaction 缺陷）——tombstone 膨胀增加了数据文件大小，导致 page cache 覆盖率下降，淘汰事件更严重。** `path:/Users`=3436ms（系列最高）可能部分归因于此。预计 1 轮 battery 预热恢复。
