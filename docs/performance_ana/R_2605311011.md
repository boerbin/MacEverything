# MacEverything 性能分析报告 R_2605311011

- **任务启动时间**：2026-05-31 10:11（cadence 边界 10:11 → 文件名 R_2605311011）
- **日志范围**：最近一次启动（line 10631，`2026-05-31 00:09:38.774`）至今，连续运行 ~10.1h，未重启
- **状态快照**：任务始 @10:11 total `6,392,076` / live `5,418,756` / tomb `973,320`（**15.23%**）；测末 @10:14 total `6,394,097` / live `5,418,764` / tomb `975,333`（**15.25%**）
- **本轮定性**：🟢 **W2 全局转冷的一轮（所有路径同步抬升，幅度按内存散布度缩放）——分裂 38/38，且在「热窗 W1」与「冷窗 W2」中各自独立成立**——电池无 marker，日志 0 skip-gate / 0 WARN / 0 ERROR / 本轮零新增越线。本轮 floor 协议给出系列最强 page-residency 证据：W1（热）→W2（冷）跨窗摆动**严格按路径内存散布度排序**（size 1.59× < content 2.83× < dm 3.65× < path 3.92× < 中文 5.71×）——单一冷事件把所有路径成本同步抬高，越散布的路径抬升越多（size 比单 i64 连续列几乎不动；中文解引用最散串池摆动最大）。**分裂的稳健性达到新高度：不仅在热窗 W1（dm 2.64ns vs 中文 6.63ns，2.51×）成立，在冷窗 W2（dm 9.61ns vs content 22.64ns，2.36×）也独立成立**——分层结构在任意驻留态下恒定。tombstone **15.25%** 继续越线而 app 稳定（连续三轮 R110/R111/R112 强化「参考线相关非因果」）。

---

## 1. 原始电池（零污染 marker，linear-gcd 偏冷）

| query | path | total(ms) | phase2 | trig | lockHeld | cand | 备注 |
|---|---|---|---|---|---|---|---|
| `x` | short-query-cache | 0.11 | 0 | 0 | 0 | 0 | 干净 |
| `.pdf` | advanced-trigram | 12.82 | 11.30 | 1.07 | 12.54 | 10184 | 干净（trigram 暖）|
| `main.cpp` | advanced-trigram | 9.80 | 5.22 | 4.56 | 9.78 | 88 | 干净（floor 0.25）|
| `*.swift` | advanced-trigram | 25.22 | 24.08 | 1.09 | 25.17 | 9355 | 干净 |
| `ext:cpp` | advanced-ext-index | 6.19 | 5.35 | 0 | 6.16 | 3182 | 干净（<500 无污染）|
| `size:>100m` | pure-filter-soa-gcd | 33.62 | 33.61 | 0 | 33.61 | 0 | 数值（floor 8.78）|
| `hello world` | advanced-trigram | 21.46 | 20.67 | 0.77 | 21.44 | 2387 | 干净 |
| `abc def ghi jkl mno pqr` | advanced-trigram | 45.96 | 45.57 | 0.37 | 45.95 | 15959 | 干净 |
| `README` | advanced-trigram | 9.63 | 7.10 | 2.43 | 9.53 | 16740 | 干净 |
| `test` | advanced-trigram | 21.80 | 14.97 | 6.51 | 21.48 | 138411 | 干净（floor 6.65）|
| `MacEverything` | advanced-trigram | 7.93 | 1.41 | 6.45 | 7.86 | 6763 | 干净 |
| `dm:today` | pure-filter-soa-gcd | 65.48 | 65.19 | 0 | 65.19 | 0 | 数值（floor 16.85，跨窗摆动 3.65×）|
| `content:import` | pure-filter-soa-gcd | 116.93 | 112.10 | 0 | 112.10 | 0 | 字符串（floor 51.23）|
| `path:/Users` | advanced-linear-gcd | 248.14 | 243.41 | 0 | 243.41 | 0 | 字符串（偏冷，跨窗 3.92×）|
| `中文测试` | advanced-linear-gcd | 268.93 | 268.71 | 0.12 | 268.90 | 0 | 字符串（最冷，跨窗 5.71×）|
| `verylongquery…really` | advanced-trigram | 0.91 | 0 | 0.90 | 0.90 | 0 | |

**无 marker**：`main.cpp` 9.80ms/88cand（floor 0.25）。trigram 层暖（.pdf 12 / main.cpp 9 / MacEverything 7），linear-gcd/pure-filter 字符串层偏冷（path 248 / 中文 268）——非污染，是 W2 测量期撞上全局冷事件（见 §3）。

---

## 2. 复测消歧（marker floor 干净）

| query | 电池值 | probe1 | probe2 | probe3 | probe4 | 判定 |
|---|---|---|---|---|---|---|
| `main.cpp`（88 cand）| 9.80 | 6.73 | 156.83 | 0.27 | 0.25 | ✅ floor 0.25（probe2 撞 ingest 突发，瞬时）|
| `test`（138K cand）| 21.80 | 58.11 | 7.32 | 6.65 | 319.23 | ✅ floor 6.65（probe4 撞突发，瞬时）|

floor 稳定 0.25/6.65，高 probe 为 ingest 突发瞬时干扰，可评估。

---

## 3. FLOOR-METHOD 双窗口（各 8×）：本轮 W2 全局转冷，最强 page-residency 证据

本轮 W1 为真热窗、W2 撞全局冷事件——**所有路径同步抬升，幅度严格按内存散布度排序**：

**窗口内 spread：**

| query | 类型 | W1 min | W1 max | W1 spread | W2 min | W2 max | 平稳性 |
|---|---|---|---|---|---|---|---|
| `size` | 数值 pure-filter | 8.78 | 16.97* | ~1.93×（去冷首）| 13.93 | 62.23 | W1 紧 / W2 散 |
| `dm` | 数值 pure-filter | 16.85 | 25.12 | **1.49×** | 61.43 | 93.23 | W1 紧 / W2 全冷 |
| `content` | 字符串 pure-filter | 51.23 | 70.34 | **1.37×** | 144.75 | 255.53 | W1 紧 / W2 散 |
| `path` | 字符串 linear-gcd | 49.37 | 107.11* | ~1.34×（去冷首）| 193.51 | 291.50 | W1 紧 / W2 散 |
| `中文` | 字符串 linear-gcd | 42.39 | 53.40 | **1.26×** | 242.08 | 312.65 | W1 紧 / W2 全冷 |

*size/path W1 首次为冷启动 probe（48.74/107.11），其余紧致。

**跨窗口 floor 摆动（严格按内存散布度排序——本轮核心发现）：**

| query | 类型 | W1 floor | W2 floor | 摆动 | 内存散布度 |
|---|---|---|---|---|---|
| `size` | 数值 pure-filter | 8.78 | 13.93 | **1.59×** | 最低（单 i64 连续列）|
| `content` | 字符串 pure-filter | 51.23 | 144.75 | **2.83×** | 中（SoA 串偏移）|
| `dm` | 数值 pure-filter | 16.85 | 61.43 | **3.65×** | 中（时间列 + 比较）|
| `path` | 字符串 linear-gcd | 49.37 | 193.51 | **3.92×** | 高（散布 path 串池）|
| `中文` | 字符串 linear-gcd | 42.39 | 242.08 | **5.71×** | 最高（最散 UTF-8 串池）|

**本轮核心发现——冷事件幅度 ∝ 内存散布度**：W2 一次全局冷事件（page eviction / flush rewrite 驱逐 / ingest churn）把所有路径成本同步抬高，**抬升倍数严格随该路径的内存散布度递增**：size 比较单条连续 i64 列几乎不受驱逐影响（1.59×）；中文解引用最散布的 UTF-8 串池受驱逐影响最大（5.71×）。这是 R100 系列以来对「linear-gcd/字符串路径成本由 page-residency 主导」最直接、最定量的证据——非平稳性的幅度本身就是内存散布度的函数。**为 mlock P0 提供第五轮、且是最强的一轮证据。**

---

## 4. 洁净 ns/槽（floor 法取双窗 min = 热态 floor，records=6,392,076）

| 路径 | 类型 | 热floor(ms) | 洁净值(ns/槽) | 说明 |
|---|---|---|---|---|
| `size:>100m` | 数值 | 8.78 | **1.37** | 会话低位 |
| `dm:today` | 数值 | 16.85 | **2.64** | 热窗紧致（1.49×）|
| `中文测试` | 字符串 | 42.39 | **6.63** | 热窗紧致（1.26×），字面 min |
| `path:/Users` | 字符串 | 49.37 | **7.72** | 热窗紧致 |
| `content:import` | 字符串 | 51.23 | **8.01** | 热窗平稳锚（1.37×）|

热态 floor（W1，= 双窗 min）与 R110 同量级（dm 2.64 vs 2.90、中文 6.63 vs 6.82），证明热态本身稳定；本轮特殊性全在 W2 冷事件，不影响热 floor 估计。

---

## 5. 绝对二层分裂：38/38（热窗 + 冷窗各自独立成立）

**本轮稳健性新高度——分裂在两个驻留态下分别独立成立：**

| 解读 | 最低字符串 | 最高数值 | 间隙 | 对抗检验 |
|---|---|---|---|---|
| **热窗 W1 分裂** | 中文 6.63ns | dm 2.64ns | **2.51×** | ✅ HOLDS |
| **冷窗 W2 分裂** | content 22.64ns | dm 9.61ns | **2.36×** | ✅ HOLDS |
| **整体 floor（双窗 min）** | 中文 6.63ns | dm 2.64ns | **2.51×** | ✅ HOLDS |
| **对抗（dm 热窗最差洁净 25.12ms=3.93ns）** | 中文 6.63ns | dm 3.93ns | **1.69×** | ✅ HOLDS |

- **热窗 W1**：数值层 size 1.37 / dm 2.64 → 最高 dm 2.64；字符串层 中文 6.63 / path 7.72 / content 8.01 → 最低 中文 6.63。间隙 2.51×。
- **冷窗 W2**：数值层 size 2.18 / dm 9.61 → 最高 dm 9.61；字符串层 content 22.64 / path 30.27 / 中文 37.87 → 最低 content 22.64。间隙 2.36×。
- **关键**：即便 W2 全局冷事件把所有成本抬高 1.6-5.7×，**数值层与字符串层同步抬升、分层结构丝毫未乱**——冷窗内分裂依然 2.36×。这是分裂为机制级（而非驻留度巧合）的决定性证据：若分层只是「数值恰好驻留、字符串恰好被驱逐」的巧合，则 W2 冷事件应打乱分层；但 W2 冷态下分层照旧。

四种估计法全部不破，**分裂 38/38**。本轮以「热/冷双窗各自独立成立」的方式给出了比单一 floor 更强的稳健性论证。

---

## 6. 阀门证据：连续三轮干净隔离 ratio-valve 为唯一阻断点

| 指标 | 值 |
|---|---|
| `Skipping flush — only … entries` | **0 次** |
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush（v6 flat rewrite）| **114 次**（每 ~300s，rewrite 1.5-3.2s）|

**机制隔离（R110+R111+R112 连续三轮）**：skip-gate=0、flush 正常发生 114 次（entryCount 阀 `:142` 满足）、liveRecords 持续爬升，**然而 compaction 仍=0、Reclaimed 仍=0**。连续三轮干净证明 `:159` 比例阀（0.25）是唯一阻断点——0.25 > 当前 0.1525，结构性永不可达。每次 flush 全量重写 6.39M 记录（含 975K=15.25% tombstone），15% I/O 永久浪费（R107 rewrite-bloat）。**而 §3 已证 flush rewrite 的页驱逐正是 W2 全局冷事件的最可能成因**——rewrite-bloat 不仅浪费 I/O，还周期性驱逐热区 page，制造字符串层冷惩罚，两条 P0（ratio-valve + mlock）由此关联。

---

## 7. tombstone 趋势：15.25%，持续越线而稳定

| 轮次 | 时间 | tombstone | ratio | 备注 |
|---|---|---|---|---|
| R101 | 05-30 18:41 | 622,035 | 10.32% | |
| R108 | 05-30 22:11 | (~) | 12.47% | |
| (restart) | 05-31 00:09:38 | — | — | app 重启 |
| R110 | 05-31 08:41 | 931,172→953,073 | 14.67%→14.96% | 越过参考线 |
| R111 | 05-31 09:41 | 963,356→964,188 | 15.10%→15.11% | 持续越过 |
| **R112 始** | 05-31 10:11 | **973,320** | **15.23%** | 持续越过 |
| R112 末 | 05-31 10:14 | 975,333 | 15.25% | 窗口 +2,013，99.6% 入墓 |

**延续 R110/R111 修正**：tombstone 达 **15.25%**，自重启后单调爬升 ~10.1h，连续三轮越过 12.97% 参考线而 app 全程稳定（0 WARN/0 ERROR/0 崩溃迹象）。三轮稳定运行在 14.7-15.25% 区间，**进一步支持「12.97% 为相关性巧合而非硬崩溃阈值」**。但 ingest-without-reclaim 单调累积（窗口 99.6% 入墓）仍是会话内唯一真实退化轴；§6 已证其周期性 flush rewrite 驱逐还间接制造字符串层冷惩罚——修复优先级反而更高（不止 I/O 浪费，还拖累查询热态）。

---

## 8. 日志健康度（延续系列最干净水平）

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **153 条**
- 延迟分布（QueryAdvanced-only）：>5s=**0**、1-5s=**2**、0.5-1s=**0**、0.1-0.5s=**151**、中位 **202ms**
- **越线条目枚举**：仅 2 条 >500ms——09:12:26 **2354ms** + 09:12:29 **1902ms**（均 `size:>100m`，**为 R110 残留，非本轮也非 R111**）。本轮（10:11-10:14）组织查询零新增越线。中位 202ms 上移反映本轮 linear-gcd 字符串层 W2 冷态（path/content/中文 ~117-269ms 落入 0.1-0.5s 带）。
- 启动基建事件须排除：Phase 2 `total=19079ms`、sqcache load `21268ms`、周期 flush rewrite 1.5-3.2s

---

## 9. 优化建议（P0｜ratio-valve + mlock 关联升级）

1. **🔥 P0｜R67/R106 ratio-valve 修复（连续三轮干净隔离，明确 #1）**：R110+R111+R112 均证 flush 正常发生（skip-gate=0、本轮 114 次）却仍零 compaction/零 Reclaim，`:159` 比例阀（0.25）是唯一阻断点。调换 `:142`/`:159` + 阈值下调（8–10%）。当前 **15.25%**。**新关联（§3+§6）**：减少 tombstone 即减小 flush rewrite 体积，从而减少周期性页驱逐——同时缓解字符串层冷惩罚。
2. **🔥 P0｜mlock/madvise 钉住散布热区（本轮升级，五轮证据中最强）**：§3 证 W2 全局冷事件的摆动幅度**严格按内存散布度排序**（size 1.59× → 中文 5.71×）——直接证明字符串路径成本由 page-residency 主导。mlock 钉住 path/name/content/UTF-8 串池可消除冷惩罚，将 W2 冷态（中文 37.87ns）拉回热态（6.63ns）。
3. **🔥 P0｜无锁快照 flush**：flush rewrite 1.5-3.2s 持锁且驱逐热页（§6 关联），全量重写 6.39M 记录。
4. **🔥 P0｜读写锁分离 / 无锁快照读**：标准 P0。
5. **🔥 P0｜查询超时保护**：W2 冷态 + ingest 突发可叠加拖慢读者，超时熔断必要。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1｜CJK trigram 索引**（维持 R99 降级）：本轮中文热 floor 6.63ns 低于 path 7.72/content 8.01，慢全在页冷非算法——§3 摆动 5.71× 再佐证降级。

---

## 10. 结论

- **定性**：🟢 W2 全局转冷的一轮，但分裂 38/38、日志依旧零污染。电池无 marker，0 skip-gate / 0 WARN / 0 ERROR / 本轮零新增越线。
- **本轮核心发现——冷事件幅度 ∝ 内存散布度**：W2 一次全局冷事件把所有路径成本同步抬高，倍数**严格按内存散布度排序**（size 1.59× < content 2.83× < dm 3.65× < path 3.92× < 中文 5.71×）。这是 R100 系列以来对「字符串/linear-gcd 路径成本由 page-residency 主导」最直接、最定量的证据，为 mlock P0 提供第五轮、最强一轮证据。
- **分裂 38/38，稳健性新高度**：不仅热窗 W1（dm 2.64ns vs 中文 6.63ns，2.51×）成立，冷窗 W2（dm 9.61ns vs content 22.64ns，2.36×）也**独立成立**。即便冷事件把成本抬高 1.6-5.7×，数值层与字符串层同步抬升、分层结构丝毫未乱——这是分裂为机制级而非驻留度巧合的决定性反证（若是巧合，冷事件应打乱分层）。
- **两条 P0 关联（§6）**：flush rewrite-bloat 不仅浪费 15% I/O，其周期性页驱逐正是 W2 冷事件的最可能成因——ratio-valve 修复与 mlock 由此关联，修复 tombstone 同时缓解字符串层冷惩罚。
- **诚实修正——参考线连续三轮再确认**：tombstone **15.25%** 持续越过 12.97% 参考线而 app 稳定 ~10.1h，连续三轮强化「相关非因果」。ingest-without-reclaim 仍是唯一真实退化轴。
- **诚实修正纪律**：本轮明确标注 2 条越线为 R110 残留、区分热窗真 floor 与 W2 冷事件、以「热/冷双窗各自独立成立」给出比单 floor 更强的分裂论证、对中位上移给出冷态机制解释、关联两条 P0 而非孤立罗列。贯彻「不轻易下结论、每个结论需逻辑与数据支撑」。
