# MacEverything 性能分析报告 R_2606091800

## 1. 本轮范围

- 任务启动时间：2026-06-09 18:00
- 报告文件：`docs/performance_ana/R_2606091800.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6379`
  - `SEG_LINES=5437`
  - `LAST_LOG_TS=2026-06-09 13:17`

## 2. 执行摘要

**午后 I/O 压力持续——1/16 >500ms，Max=524ms，P50=179ms，lockWait=2.5ms，极佳比例 2/16。** `.pdf`=524ms（trigram postings 再次受压），`x`=180ms（进程调度延迟再破纪录！），`*.swift`=462ms（接近阈值）。**liveRecordCount 首次出现显著跳增（+9.2K）**——可能有批量新文件创建事件。

| 指标 | R213 | R214 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **1/16** | ↑1（午后压力） |
| Max | 274ms | **524ms** | ↑91% |
| P50 | 90ms | **179ms** | ↑99% |
| lockWait max | 0ms | **2.5ms** | ↑（轻度） |
| 极佳比例(<50ms) | 6/16 | **2/16** | ↓4 |
| tombstone | 1,834,629 (26.21%) | **1,849,220 (26.33%)** | ↑ |
| tombstone 增速 | ~199/min | **~486/min** | ↑144%（午后再升） |
| recordCount | 7,000,318 | **7,024,096** | **+23.8K（大幅增加）** |
| liveRecordCount | 5,165,689 | **5,174,876** | **+9.2K（显著跳增！）** |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 4873 | **4933** | +60 |
| COMPACTION | 2 | **2** | **不变——第 15 轮确认缺陷** |
| QUERYADV_GE500 | 49 | **50** | ↑1 |

### `x`=180ms——进程调度延迟再破纪录！

- **phase2=0（short-query-cache 命中），queryTime=180ms——全是 overhead。**
- **R211=137ms → R212=7ms → R213=1.4ms → R214=180ms——再次飙升。**
- **180ms 是新的系列绝对纪录！此前 R211=137ms。**
- **午后系统 CPU 竞争激烈——所有查询的 baseline 被抬高约 100-180ms。**

### `MacEverything`=142ms——overhead 异常

- **phase2=6.9ms（正常），queryTime=142ms——overhead=135ms。**
- **lockWait=0——不是锁竞争。**
- **与 `x`=180ms 同源——进程调度延迟影响所有查询。**

### `.pdf`=524ms——trigram postings 受压

- **phase2=511ms，lockWait=2.5ms——page cache 淘汰 + 轻度锁竞争。**
- **R213=59ms → R214=524ms（↑785%）——从极佳跌至超标。**
- **lockWait=2.5ms——首次在 `.pdf` 查询中观察到锁竞争。**

### `*.swift`=462ms——接近阈值

- **phase2=458ms，lockWait=0——纯 page cache 淘汰。**
- **R213=99ms → R214=462ms（↑367%）——大幅恶化。**
- **candidates=9345——与 `.pdf` 类似的 trigram postings 区域受压。**

### liveRecordCount +9.2K——异常跳增

- **此前轮次 liveRecordCount 增量通常 <1K（R212=+181，R213=+181）。**
- **本轮 +9.2K——可能有批量新文件创建事件（如解压、安装、下载）。**
- **recordCount +23.8K，tombstone 增量 = 23.8K - 9.2K = 14.6K——仍有大量 tombstone。**
- **这也解释了午后 I/O 压力加剧——大量文件操作导致磁盘竞争。**

### Trigram 路径——全面受压

| Query | R213 | R214 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 59ms | **524ms** | ↑785% | **>500ms** |
| `hello world` | 274ms | **281ms** | ↑3% | 正常 |
| `*.swift` | 99ms | **462ms** | ↑367% | 正常（接近阈值） |
| `main.cpp` | 8ms | **30ms** | ↑275% | 极佳 |
| `abc def...` | 139ms | **179ms** | ↑29% | 正常 |
| `README` | 20ms | **58ms** | ↑190% | 正常 |
| `test` | 90ms | **130ms** | ↑44% | 正常 |
| `MacEverything` | 53ms | **142ms** | ↑168% | 正常（高 overhead） |
| Trigram 均值 | 93ms | **226ms** | **↑143%** | 全面受压 |

- **Trigram 均值 93→226ms（↑143%）——全面恶化。**
- **1/8 trigram >500ms，1/8 接近阈值（462ms）。**
- **进程调度延迟（~100-180ms）叠加 page cache 淘汰——双重压力。**

### 全表扫描——偏高

| Query | R213 | R214 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 210ms | **224ms** | ↑7% |
| `path:/Users` | 272ms | **320ms** | ↑18% |
| `中文测试` | 253ms | **282ms** | ↑11% |
| **总和** | **735ms** | **826ms** | **↑12%** |

- **全表扫描总和 735→826ms（↑12%）——偏高但在正常范围。**

### `ext:cpp`=273ms——偏高

- **phase2=183ms，queryTime=273ms——overhead=90ms。**
- **R213=50ms → R214=273ms（↑446%）——ext-index 区域受压。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 179.5 | 0.0 | short-query-cache | 0 | 0 | 极佳（**overhead 纪录**） |
| `.pdf` | 5 | 524.3 | 510.5 | advanced-trigram | 2.5 | 9860 | **>500ms** |
| `main.cpp` | 5 | 29.7 | 17.8 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 462.1 | 458.4 | advanced-trigram | 0 | 9345 | 正常（接近阈值） |
| `ext:cpp` | 5 | 272.7 | 182.5 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 92.9 | 92.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 280.5 | 272.8 | advanced-trigram | 0 | 3669 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 178.6 | 169.2 | advanced-trigram | 0 | 14355 | 正常 |
| `README` | 5 | 58.2 | 53.6 | advanced-trigram | 0 | 17053 | 正常 |
| `test` | 5 | 129.8 | 117.6 | advanced-trigram | 0 | 116963 | 正常 |
| `MacEverything` | 5 | 142.3 | 6.9 | advanced-trigram | 0 | 6859 | 正常（高 overhead） |
| `dm:today` | 5 | 56.3 | 56.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 224.0 | 159.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 320.4 | 270.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 282.0 | 259.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 178.6ms，P90 = 462.1ms，Max = 524.3ms。1/16 查询 >500ms。lockWait max = 2.5ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R213 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **2/16** | 0.4-30ms | R213=6/16（↓4） |
| **正常**（50-500ms） | 13/16 | 56-462ms | R213=10/16（↑3） |
| **超标**（>500ms） | **1/16** | 524ms | R213=0/16（↑1） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,024,096，liveRecordCount=5,174,876（tombstone=**1,849,220**，**26.33%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=401  QUERYADV_GE500=50（↑1）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4933
```

## 6. 问题清单

### P0 确认（第 15 轮）：compaction 触发机制缺陷

- **tombstone 26.33%——超阈值 1.33%。**
- **COMPACTION=2——始终不变。**

### 进程调度延迟新纪录

- **`x`=180ms——新的系列绝对纪录（此前 R211=137ms）。**
- **午后 CPU 竞争导致所有查询 baseline 被抬高。**
- **与 liveRecordCount +9.2K 的大批量文件操作可能相关。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 15 轮确认）

### P0.5：flush rewrite 改为 COW 方式（持续建议）

### P2：madvise MADV_WILLNEED 热区维护（持续建议）

## 8. 下一轮重点

1. 1/16 >500ms 是否恢复。
2. 进程调度延迟是否消退。
3. tombstone 增长趋势。

## 9. 结论

**午后 I/O 压力持续——1/16 >500ms（`.pdf`=524ms），Max=524ms，P50=179ms，极佳 2/16。** `x`=180ms 进程调度延迟再破纪录（此前 137ms）。`*.swift`=462ms 接近阈值。**liveRecordCount +9.2K（异常跳增）**——可能有批量文件创建事件，导致磁盘 I/O 和 CPU 竞争加剧。

**tombstone 26.33%（第 15 轮确认 compaction 缺陷）——增速 486/min。** 午后进程调度延迟叠加 page cache 淘汰形成双重压力。预计 1 轮恢复。
