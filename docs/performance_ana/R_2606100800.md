# MacEverything 性能分析报告 R_2606100800

## 1. 本轮范围

- 任务启动时间：2026-06-10 08:00
- 报告文件：`docs/performance_ana/R_2606100800.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=8163`
  - `SEG_LINES=7221`
  - `LAST_LOG_TS=2026-06-10 04:47`

## 2. 执行摘要

**早间分化加深——trigram 改善至 29ms，但全表扫描持续攀升至 835ms。** 0/16 >500ms，Max=359ms，P50=37ms，lockWait=0（连续 10 轮）。Trigram 均值 29ms（↓24%——改善），全表扫描总和 835ms（↑6%——`path:/Users`=359ms 刷新本轮 Max）。**经典早间 page cache 分化：trigram postings 预热改善 vs SoA 大区域被系统活动淘汰。**

| 指标 | R241 | R242 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 325ms | **359ms** | ↑10%（`path:/Users` 继续攀升） |
| P50 | 43ms | **37ms** | **↓14%** |
| lockWait max | 0ms | **0ms** | 完美（**连续 10 轮！**） |
| 极佳比例(<50ms) | 9/16 | **9/16** | 持平 |
| tombstone | 2,139,613 (29.12%) | **2,146,625 (29.19%)** | ↑——接近 29.2% |
| tombstone 增速 | ~237/min | **~234/min** | ↓1%（持平） |
| recordCount | 7,347,918 | **7,355,110** | +7.2K |
| liveRecordCount | 5,208,305 | **5,208,485** | +180 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 6457 | **6517** | +60 |
| COMPACTION | 2 | **2** | **不变——第 43 轮确认缺陷** |
| QUERYADV_GE500 | 60 | **60** | 不变 |

### lockWait 连续 10 轮为 0——里程碑！

- **R233-R242 连续 10 轮 lockWait=0。**
- **此前最长连续 lockWait=0 为 R233-R240（8 轮）。**
- **10 轮覆盖深夜至早间全时段——证明 lockWait 的缺失并非时段依赖。**

### 早间 page cache 分化

- **Trigram 均值 38→29ms（↓24%）——postings 区域在重复查询中进一步预热。**
- **全表扫描总和 789→835ms（↑6%）——SoA 大区域被早间系统活动（Spotlight、Time Machine 等）逐步淘汰。**
- **这是 R235 零和博弈的反向模式：trigram 改善的同时全表扫描回升。**

### Trigram 路径——改善

| Query | R241 | R242 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 64ms | **27ms** | **↓58%** | 极佳 |
| `hello world` | 70ms | **57ms** | ↓19% | 正常 |
| `*.swift` | 62ms | **30ms** | **↓52%** | 极佳 |
| `main.cpp` | 16ms | **5ms** | ↓69% | 极佳 |
| `abc def...` | 43ms | **55ms** | ↑28% | 正常 |
| `README` | 19ms | **16ms** | ↓16% | 极佳 |
| `test` | 27ms | **38ms** | ↑41% | 极佳 |
| `MacEverything` | 5ms | **8ms** | ↑60% | 极佳 |
| Trigram 均值 | 38ms | **29ms** | **↓24%** |

- **Trigram 均值 29ms——回到 R237=23ms 级别附近。**
- **6/8 trigram 极佳（↑1 from R241）。**
- **`.pdf`=27ms（↓58%）和 `*.swift`=30ms（↓52%）大幅改善——postings 预热效果显著。**

### 全表扫描——持续攀升

| Query | R241 | R242 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 260ms | **163ms** | **↓37%** |
| `path:/Users` | 325ms | **359ms** | ↑10% |
| `中文测试` | 203ms | **313ms** | ↑54% |
| **总和** | **789ms** | **835ms** | **↑6%** |

- **全表扫描总和 789→835ms（↑6%）——持续攀升但增速放缓。**
- **`path:/Users`=359ms（↑10%）——路径数组持续受影响（本轮 Max）。**
- **`中文测试`=313ms（↑54%）——名称数组大幅回升（R240=276ms→R241=203ms→R242=313ms——波动）。**
- **`content:import`=163ms（↓37%）——SoA 内容区域反而改善——三路之间的 page cache 竞争。**

### 辅助路径——优秀

| Query | R241 | R242 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 9ms | **2ms** | ↓78% | 极佳 |
| `ext:cpp` | 16ms | **17ms** | ↑6% | 极佳 |
| `size:>100m` | 55ms | **47ms** | ↓15% | 极佳 |
| `dm:today` | 45ms | **45ms** | 持平 | 极佳 |
| `verylongquery...` | 0.1ms | **0.5ms** | ↑400% | 极佳 |

- **`size:>100m`=47ms（↓15%）——回到极佳。**
- **所有辅助路径极佳——稳定。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 2.4 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 27.0 | 24.3 | advanced-trigram | 0 | 9860 | 极佳 |
| `main.cpp` | 5 | 4.8 | 3.0 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 30.2 | 27.9 | advanced-trigram | 0 | 9353 | 极佳 |
| `ext:cpp` | 5 | 16.6 | 15.9 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 47.1 | 46.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 57.3 | 56.1 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 55.1 | 54.7 | advanced-trigram | 0 | 14468 | 正常 |
| `README` | 5 | 15.8 | 9.7 | advanced-trigram | 0 | 17151 | 极佳 |
| `test` | 5 | 37.6 | 34.1 | advanced-trigram | 0 | 117136 | 极佳 |
| `MacEverything` | 5 | 7.9 | 0.9 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 44.8 | 43.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 163.3 | 153.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 358.6 | 348.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 312.7 | 311.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 37.6ms，P90 = 312.7ms，Max = 358.6ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R241 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **9/16** | 0.5-47ms | R241=9/16（持平） |
| **正常**（50-500ms） | 7/16 | 55-359ms | R241=7/16（持平） |
| **超标**（>500ms） | **0/16** | — | R241=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,355,110，liveRecordCount=5,208,485（tombstone=**2,146,625**，**29.19%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=573  QUERYADV_GE500=60（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=6517
```

## 6. 问题清单

### P0 确认（第 43 轮）：compaction 触发机制缺陷

- **tombstone=2,146,625（29.19%）——超阈值 4.19%，持续攀升。**
- **43 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **连续 10 轮 lockWait=0（里程碑！）。历史证据充分。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（第 43 轮确认——tombstone 29.19%）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 全表扫描是否继续攀升（早间系统活动峰值时段）。
2. trigram 与全表扫描分化是否进一步加剧。

## 9. 结论

**早间分化加深——trigram 改善至 29ms，全表扫描攀升至 835ms。** 0/16 >500ms，Max=359ms（`path:/Users`），P50=37ms（↓14%），lockWait=0（**连续 10 轮里程碑！**）。经典早间 page cache 分化：trigram postings 在重复查询中预热（29ms），SoA 大区域被系统活动淘汰（835ms）。极佳 9/16 持平。

**tombstone 29.19%（2,146,625，第 43 轮确认 compaction 缺陷）。** 增速 ~234/min（持平）。COMPACTION=2 始终不变。
