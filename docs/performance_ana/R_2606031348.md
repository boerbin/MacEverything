# MacEverything 性能分析报告 R_2606031348

## 1. 本轮范围

- 任务启动时间：2026-06-03 13:48
- 报告文件：`docs/performance_ana/R_2606031348.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21402`
  - `SEG_LINES=376`
  - `LAST_LOG_TS=2026-06-03 17:11`

## 2. 执行摘要

服务正常运行（第 17 轮连续可用）。性能从前轮历史最佳大幅回落：P50 ≈ 120ms（前轮 34ms），Max=446ms。**`x` 短查询缓存出现历史最严重异常（364ms）**，为第 4 次偶发事件，本次为此前最高值（254ms R58）的 1.4 倍。`.pdf`=446ms 再次接近 500ms 阈值。所有查询连续第 14 轮 < 500ms。tombstone 增至 169,142 条（2.87%），连续第 6 轮超过 2%。

### 横向对比（最近 5 轮）

| Query | R61 | R62 | R63 | R64 | R65 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 7 | 93 | 6 | 2 | **364** | **历史最严重异常** |
| `.pdf` | 231 | 325 | 415 | 84 | **446** | 波动剧烈 |
| `*.swift` | 172 | 488 | 233 | 29 | **133** | 正常 |
| `hello world` | 252 | 297 | 274 | 120 | **222** | 正常 |
| `path:/Users` | 227 | 255 | 307 | 380 | **203** | 改善 |
| P50 | 147 | 148 | 106 | 34 | **120** | 回落 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 363.8 | 0.0 | short-query-cache | 0 | 0 | **历史最严重异常** |
| `.pdf` | 5 | 446.4 | 299.0 | advanced-trigram | 0 | 9944 | 接近阈值 |
| `main.cpp` | 5 | 107.5 | 76.7 | advanced-trigram | 0 | 85 | 偏慢 |
| `*.swift` | 5 | 132.7 | 120.9 | advanced-trigram | 0 | 9356 | 正常 |
| `ext:cpp` | 5 | 51.1 | 50.1 | advanced-ext-index | 0 | 3702 | 正常 |
| `size:>100m` | 5 | 96.2 | 83.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 221.8 | 214.3 | advanced-trigram | 0 | 3613 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 204.3 | 201.4 | advanced-trigram | 0 | 15985 | 正常 |
| `README` | 5 | 27.6 | 20.6 | advanced-trigram | 0 | 17717 | 快 |
| `test` | 5 | 59.5 | 47.8 | advanced-trigram | 0 | 129960 | 正常 |
| `MacEverything` | 5 | 31.5 | 2.4 | advanced-trigram | 0 | 6802 | 快 |
| `dm:today` | 5 | 63.0 | 62.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 195.3 | 186.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 203.1 | 196.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 291.4 | 290.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 8.4 | 0.0 | advanced-trigram | 0 | 0 | 快 |

**P50 ≈ 120ms，P90 ≈ 364ms，Max = 446ms。全部查询连续第 14 轮 < 500ms。lockWaitMs 全部为 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,895,977，liveRecordCount=5,726,835（tombstone=**169,142**，**2.87%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=131  QUERYADV_GE500=5
FLUSH=0  COMPACTION=2  SKIPFLUSH=120
DIAG_TRIGRAM_SKIP=70
```

- QUERYADV 从 123 增至 131（+8）。
- QUERYADV_GE500 保持 5（本轮 Max=446ms，未超阈值）。
- SKIPFLUSH 从 110 增至 120（+10），恢复正常增速。
- DIAG_TRIGRAM_SKIP 从 69 增至 70（+1）。

## 6. 问题清单

### Critical：`x` 短查询缓存历史最严重异常（364ms）

- **异常历史**：
  ```
  R58: 254ms → R62: 93ms → R65: 364ms（本轮）
  正常值: 2-19ms
  ```
- 17 轮中 4 次异常（23.5% 异常率），且严重程度逐步升级。
- phase2=0，lockWaitMs=0，延迟完全在缓存层面。
- **根因分析**：short-query-cache 路径不涉及磁盘 I/O 或锁等待，364ms 延迟只能来自：
  1. GCD 线程调度延迟（线程被其他任务占用）
  2. 缓存数据序列化/反序列化开销
  3. 进程级别的资源争用（内存压力、swap）
- **优先级升级**：从 P2 升至 **Critical**——364ms 接近 500ms 阈值，继续恶化将终结 <500ms 连续记录。

### Critical：tombstone 连续 6 轮超过 2%（2.87%，169,142 条）

- 增长轨迹：
  ```
  R61: 138K (2.36%) → R62: 149K (2.54%) → R63: 158K (2.69%)
  → R64: 160K (2.71%) → R65: 169K (2.87%)
  ```
- 本轮增量 9,461 条（R64 仅增 1.3K，本轮恢复正常增速）。
- **累计内存开销**：~169K × 300 字节 ≈ 51MB 额外内存。
- SKIPFLUSH=120，compaction 仍然完全不可触发。

### Medium：`.pdf` 波动剧烈（84-490ms 范围）

- 本轮 446ms，前轮 84ms。5 倍波动幅度。
- candidates=9944 不变，phase2=299ms（前轮 79ms）。
- I/O 负载变化是主因——前轮恰好处于极低 I/O 窗口。

## 7. 优化建议

### P0：调查 short-query-cache 异常（优先级升级）

- 第 4 次异常且严重性升级（364ms），异常率 23.5%。
- **建议**：
  1. 在 short-query-cache 路径增加细粒度计时日志（缓存查找、序列化、GCD 调度各阶段）。
  2. 记录异常发生时的系统负载（CPU、内存压力）。
  3. 排查缓存是否在异常时触发了重建或淘汰。

### P0：实现基于占比的主动 tombstone compaction（已超阈值 6 轮）

- tombstone 2.87%，最小实施方案不变：SKIPFLUSH 分支增加占比检查。

### P0：补齐进程生命周期监控（承前轮）

### P1：将 `.xxx`/`*.xxx` 扩展名模式路由到 ext-index（承前轮）

## 8. 下一轮重点

1. `x` 短查询缓存异常是否持续或恢复——连续恶化将是紧急问题。
2. tombstone 增速是否回到 ~10K/轮 常态。
3. `.pdf` 是否突破 500ms。

## 9. 结论

服务连续第 17 轮正常运行，所有查询连续第 14 轮 <500ms（Max=446ms）。性能从前轮历史最佳大幅回落（P50: 34→120ms），证实前轮的极低 I/O 负载是偶发窗口。**`x` 短查询缓存出现历史最严重异常（364ms，前最高 254ms）**，第 4 次偶发且严重性逐步升级，优先级从 P2 升至 Critical。tombstone 连续第 6 轮超过 2%（169,142 条，2.87%），SKIPFLUSH=120。`.pdf`=446ms 再次接近 500ms 阈值。
