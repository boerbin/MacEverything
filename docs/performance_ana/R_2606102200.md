# MacEverything 性能分析报告 R_2606102200

## 1. 本轮范围

- 任务启动时间：2026-06-10 22:00
- 报告文件：`docs/performance_ana/R_2606102200.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9387`
  - `SEG_LINES=8445`
  - `LAST_LOG_TS=2026-06-10 20:17`

## 2. 执行摘要

**`*.swift`=322ms 异常尖峰 + flush rewrite=3638ms 急升！** 0/16 >500ms，Max=322ms，P50=54.6ms（↓38% from R269=89ms），lockWait=0。**极佳 8/16（↑2 from R269=6/16——恢复）。** `x`=13ms（缓存稳定，连续 9 轮）。`*.swift`=322ms（↑1100% from R269=27ms！phase2=317ms——postings 页完全淘汰）。`.pdf`=183ms（↑207% from R269=60ms）。**全表扫描总和 742ms（↓7% from R269=795ms——持续小幅改善）。** **flush rewrite=3638ms（↑50% from R269=2421ms——急剧反弹！sqcache=164ms 异常偏高）。** FLUSH 74→80（+6）。**tombstone=166,527（3.08%——突破 3%！）。** recordCount +17.9K（连续 7 轮偏高）。

| 指标 | R269 | R270 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 310ms | **322ms** | ↑4% |
| P50 | 89ms | **54.6ms** | **↓38%（恢复）** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 6/16 | **8/16** | **↑2（恢复）** |
| tombstone | 150,123 (2.85%) | **166,527 (3.08%)** | **↑（突破 3%！）** |
| recordCount | 5,396,648 | **5,414,599** | +17.9K（连续 7 轮偏高） |
| liveRecordCount | 5,246,525 | **5,248,072** | +1,547（正常） |
| FLUSH 累计 | 74 | **80** | +6 |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 8 | **8** | 不变 |
| QUERYADV_GE500 | 78 | **78** | 不变 |

### flush rewrite=3638ms——急剧反弹！

**日志末行：**
```
[2026-06-10 20:17:31.068] Flushed v6 flat index, lastEventId=340711504,
liveRecords=5248074 | timing: walSwap=0ms rewrite=3638ms sqcache=164ms total=3803ms
```

| 轮次 | flush rewrite | sqcache | total | 变化 |
|------|------------:|--------:|------:|------|
| R265 | 2617ms | 7ms | 2626ms | 首次捕获 |
| R266 | 3981ms | 2ms | 3984ms | 峰值 |
| R268 | 2688ms | 2ms | 2692ms | 回落 |
| R269 | 2421ms | 3ms | 2425ms | 继续下降 |
| **R270** | **3638ms** | **164ms** | **3803ms** | **↑50% 急升！** |

- **flush rewrite 2421→3638ms（↑50%——急剧反弹！接近 R266 峰值 3981ms）。**
- **sqcache=164ms（↑5367% from R269=3ms——异常偏高！可能是 short-query-cache 重建开销）。**
- **total=3803ms（3.8 秒——接近历史峰值 3984ms）。**
- **flush rewrite 时间高度不稳定：2421→3638ms 的单轮 50% 跳变。**
- **与磁盘 I/O 负载强相关——系统活动是主要变量。**

### `*.swift`=322ms——异常尖峰

- **R269=27ms → R270=322ms（↑1100%！）。**
- **phase2=317ms（几乎全部是搜索时间——9,416 candidates）。**
- **9,416 candidates 通常只需 20-30ms——322ms 表明 postings 页完全被淘汰。**
- **flush rewrite=3638ms 冲刷了 `*.swift` 对应的 trigram postings 页。**
- **`.pdf`=183ms 同样受影响（phase2=176ms，9,788 candidates）。**
- **两者共性：中等规模 postings 集（~10K），数据页在 page cache 边缘。**

### P50 恢复——但分布分化

**P50=54.6ms（↓38% from R269=89ms）——但分布呈现两极分化：**

| 层级 | R269 | R270 | 典型查询 |
|------|-----:|-----:|----------|
| 极佳（<50ms） | 6/16 | **8/16** | `x`=13, `test`=30, `README`=19, `size:>100m`=40 |
| 正常偏低（50-150ms） | 4/16 | **4/16** | `dm:today`=61, `abc def`=86, `hello world`=114 |
| 正常偏高（150-350ms） | 6/16 | **4/16** | `.pdf`=183, `content:import`=214, `*.swift`=322 |

- **`test` 96→30ms, `size:>100m` 126→40ms——大幅恢复至极佳。**
- **`*.swift` 27→322ms, `.pdf` 60→183ms——大幅恶化。**
- **典型零和翻转——page cache 在 trigram postings 间轮转。**

### Trigram 路径——两极分化

| Query | R269 | R270 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 60ms | **183ms** | **↑205%** | 正常 |
| `hello world` | 106ms | **114ms** | ↑8% | 正常 |
| `*.swift` | 27ms | **322ms** | **↑1093%** | 正常（尖峰） |
| `main.cpp` | 6ms | **28ms** | ↑367% | 极佳 |
| `abc def...` | 89ms | **86ms** | ↓3% | 正常 |
| `README` | 29ms | **19ms** | ↓34% | 极佳 |
| `test` | 96ms | **30ms** | **↓69%** | 极佳 |
| `MacEverything` | 35ms | **5ms** | **↓86%** | 极佳 |
| Trigram phase2均值 | ~41ms | **~62ms** | **↑51%（被 *.swift 拉高）** |

- **去除 `*.swift` 后，trigram phase2 均值 ≈ 25ms（正常）。**
- **`*.swift`=322ms 是单点异常——postings 页被 flush I/O 淘汰。**
- **`test` 96→30ms, `MacEverything` 35→5ms——零和翻转的受益方。**

### 全表扫描——持续改善

| Query | R269 | R270 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 236ms | **214ms** | ↓9% |
| `path:/Users` | 249ms | **261ms** | ↑5% |
| `中文测试` | 310ms | **268ms** | ↓14% |
| **总和** | **795ms** | **742ms** | **↓7%（连续 2 轮改善）** |

- **全表扫描 834→795→742ms（连续 2 轮改善——page cache 逐渐稳定）。**
- **`中文测试` 422→310→268ms（连续 3 轮恢复——从 R268=422ms 峰值稳步回落）。**

### `x` 缓存——连续 9 轮稳定

- **13ms（略高——系统负载波动，非缓存问题）。**
- **连续 9 轮 short-query-cache（R261-R270）。**

### tombstone 突破 3%

| 轮次 | tombstone | tombstone% | 增量 |
|------|----------:|---------:|------:|
| R266 | 106,674 | 2.03% | +16,419 |
| R267 | 121,278 | 2.31% | +14,604 |
| R268 | 135,551 | 2.56% | +14,273 |
| R269 | 150,123 | 2.85% | +14,572 |
| **R270** | **166,527** | **3.08%** | **+16,404** |

- **tombstone 突破 3%（3.08%）——增速 +16.4K/轮（略有加速）。**
- **距 25% 阈值：(25%-3.08%) / (~0.30%/轮) ≈ 73 轮。**
- **recordCount +17.9K（连续 7 轮偏高——文件系统持续活跃）。**
- **liveRecordCount +1,547（正常——大部分新 records 是路径更新）。**

### 辅助路径

| Query | R269 | R270 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 8ms | **13ms** | ↑（系统负载） | 极佳 |
| `ext:cpp` | 15ms | **48ms** | ↑220%（偶发） | 极佳 |
| `size:>100m` | 126ms | **40ms** | **↓68%（恢复）** | 极佳 |
| `dm:today` | 109ms | **61ms** | ↓44% | 正常 |
| `verylongquery...` | 0.4ms | **0.3ms** | ↓25% | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 13.2 | 0.0 | short-query-cache | 0 | 0 | 极佳（稳定） |
| `.pdf` | 5 | 182.9 | 175.7 | advanced-trigram | 0 | 9788 | 正常 |
| `main.cpp` | 5 | 27.9 | 22.4 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 322.1 | 317.4 | advanced-trigram | 0 | 9416 | **正常（尖峰！）** |
| `ext:cpp` | 5 | 47.9 | 46.0 | advanced-ext-index | 0 | 3616 | 极佳 |
| `size:>100m` | 5 | 40.1 | 40.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳（恢复） |
| `hello world` | 5 | 114.4 | 108.7 | advanced-trigram | 0 | 3930 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 85.8 | 83.7 | advanced-trigram | 0 | 14462 | 正常 |
| `README` | 5 | 18.5 | 17.0 | advanced-trigram | 0 | 17381 | 极佳 |
| `test` | 5 | 29.8 | 25.8 | advanced-trigram | 0 | 117766 | 极佳（恢复） |
| `MacEverything` | 5 | 5.0 | 0.7 | advanced-trigram | 0 | 6943 | 极佳 |
| `dm:today` | 5 | 61.2 | 61.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 213.7 | 205.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 261.0 | 254.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 267.6 | 267.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.3 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 54.6ms，P90 = 261.0ms，Max = 322.1ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R269 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **8/16** | 0.3-48ms | R269=6/16（**↑2，恢复**） |
| **正常**（50-500ms） | 8/16 | 61-322ms | R269=10/16（↓2） |
| **超标**（>500ms） | **0/16** | — | R269=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,414,599，liveRecordCount=5,248,072（tombstone=**166,527**，**3.08%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=715  QUERYADV_GE500=78（不变）
FLUSH=80（+6）  COMPACTION=8  SKIPFLUSH=7266
```

**关键日志（flush rewrite 急升 + sqcache 异常）：**
```
Flushed v6 flat index | rewrite=3638ms sqcache=164ms total=3803ms
```

## 6. 问题清单

### P0（紧急）：flush rewrite 高度不稳定（2.4-4.0 秒）

- **2421→3638ms（↑50% 单轮急升——高度不稳定）。**
- **sqcache=164ms（正常 2-7ms——异常偏高，可能是 short-query-cache 重建开销）。**
- **total=3803ms（接近历史峰值 3984ms）。**
- **flush 时间与磁盘 I/O 负载强相关，基线不变（完整重写 ~500MB）。**

### P0：compaction 触发机制缺陷（根因未修复）

### P0：I/O 优先级保护

### 新增观察：sqcache=164ms 异常

- **正常 sqcache=2-7ms，本轮 164ms（↑23x）。**
- **可能原因：short-query-cache 在 flush 后需要重建部分数据。**
- **或者 flush I/O 导致 cache 序列化被延迟。**
- **需要关注是否持续。**

## 7. 优化建议

### P0（紧急）：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

### P2：sqcache 优化——flush 后增量更新而非全量重建

## 8. 下一轮重点

1. `*.swift`=322ms 是否 1-round recovery（预计恢复）。
2. flush rewrite 趋势（3638ms——是否继续波动）。
3. sqcache=164ms 是否复现（正常 2-7ms）。
4. tombstone 增速（3.08%——已超 3%）。

## 9. 结论

**`*.swift`=322ms 异常尖峰 + flush rewrite=3638ms 急升。** 0/16 >500ms，Max=322ms，P50=55ms（↓38% from R269=89ms——恢复）。极佳 8/16（↑2——恢复）。`x`=13ms（缓存稳定，连续 9 轮）。`*.swift`=322ms 是 postings 页被 flush I/O 完全淘汰的典型表现。`test` 96→30ms, `MacEverything` 35→5ms（零和翻转受益方）。全表扫描 742ms（↓7%——连续 2 轮改善）。

**flush rewrite=3638ms（↑50% from 2421ms——急剧反弹！sqcache=164ms 异常）。** flush 时间高度不稳定：2421-3981ms 范围，受磁盘 I/O 负载驱动。tombstone=166,527（3.08%——突破 3%）。recordCount +17.9K（连续 7 轮偏高）。P0 COW flush 改造紧急性不变——flush 时间波动是所有性能不稳定的根源。
