# MacEverything 性能分析报告 R_2605301741

- **任务启动时间**：2026-05-30 17:41
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~30.6h，未重启
- **状态快照 @17:41**：total `5,995,181` / live `5,400,927` / tomb `594,254`（**9.91%**）
- **本轮定性**：🟡 **活跃后台写入「收敛」轮（R97 型回落）**——继 R98 再强化峰值后本轮 ingest 强度回落：电池峰值 698ms→**284ms**，**缓存标志未触发**（`x`=7.01ms < 10ms 阈值），窗口内 **0 条新 >500ms 日志**，tomb 增量 +11,782（从 R98 的 +18,708 收敛）。窗口 totalRecords 实测 +321（5,995,275→5,995,596）证写入仍活跃但减弱。复测全部干净恢复（标志项 probe2-4 亚毫秒），floor-method 适用。**分裂 floor 间隙 3.139×、25/25 不破**，对抗性检验通过——但**严格披露**：本轮 floor 间隙偏宽（近 R84 3.49× 史上最宽）由 dm 单次异常洁净探针（45.39ms，低于其簇 68-99ms 达 33%）驱动；若取 dm 次低值（68ms）间隙为 2.10×，**分裂结论在两种取值下均成立**（对抗检验：dm worst-clean 16.50ns < content floor 23.77ns），但具体间隙数不宜过度解读为 regime 变化，仍属 2.11×~3.49× 既定区间内波动。

---

## 1. 原始电池（含活跃写入争用，本轮较温和）

| query | path | total(ms) | phase2 | trig | lockHeld | cand | 备注 |
|---|---|---|---|---|---|---|---|
| `x` | short-query-cache | 7.01 | 0 | 0 | 0 | 0 | 缓存标志未触发（<10ms）|
| `.pdf` | advanced-trigram | 112.48 | 110.71 | 1.62 | 112.36 | 10177 | 受争用 |
| `main.cpp` | advanced-trigram | 39.54 | 37.72 | 1.81 | 39.53 | 71 | ⚠️ marker#3 轻度（复测 0.35）|
| `*.swift` | advanced-trigram | 241.13 | 239.34 | 1.70 | 241.04 | 9213 | 受争用（复测 1.31）|
| `ext:cpp` | advanced-ext-index | 94.42 | 93.65 | 0 | 94.32 | 2987 | 受争用（复测 0.34）|
| `size:>100m` | pure-filter-soa-gcd | 84.86 | 84.83 | 0 | 84.83 | 0 | 数值（floor 16.51）|
| `hello world` | advanced-trigram | 114.27 | 111.63 | 2.59 | 114.23 | 2342 | 受争用 |
| `abc def ghi jkl mno pqr` | advanced-trigram | 104.14 | 103.63 | 0.50 | 104.13 | 15928 | 受争用 |
| `README` | advanced-trigram | 15.92 | 6.81 | 9.04 | 15.85 | 16511 | 较干净 |
| `test` | advanced-trigram | 29.05 | 19.24 | 9.67 | 28.92 | 137881 | 较干净 |
| `MacEverything` | advanced-trigram | 22.24 | 3.13 | 19.02 | 22.15 | 6703 | 较干净 |
| `dm:today` | pure-filter-soa-gcd | 60.22 | 60.04 | 0 | 60.04 | 0 | 数值（floor 45.39）|
| `content:import` | pure-filter-soa-gcd | 179.31 | 170.66 | 0 | 170.67 | 0 | 字符串（floor 142.50）|
| `path:/Users` | advanced-linear-gcd | 195.67 | 190.63 | 0 | 190.63 | 0 | 字符串（floor 149.29）|
| `中文测试` | advanced-linear-gcd | 284.85 | 284.63 | 0.10 | 284.83 | 0 | 字符串（floor 146.21）|
| `verylongquery…really` | advanced-trigram | 0.74 | 0 | 0.72 | 0.72 | 0 | |

**收敛签名（vs R98 再强化）**：电池峰值 698→284ms、无任何 >500ms、缓存标志归位（427.88→7.01ms）；但 lockHeld≈total 仍全线存在（写入未停），main.cpp 71 候选 39ms 仍轻度偏高——属 R97 型「ingest 减弱但未停」，而非真正洁净轮。

---

## 2. 复测消歧（R88 协议 + R97 FLOOR-METHOD）

**标志项复测（全部干净恢复 → 突发减弱型）：**

| query | 电池值 | probe1 | probe2 | probe3 | probe4 | 判定 |
|---|---|---|---|---|---|---|
| `x`（缓存）| 7.01 | 0.51 | 0.00 | 0.04 | 0.05 | ✅ 瞬时 |
| `main.cpp`（71 cand）| 39.54 | 4.53 | 1.58 | 0.97 | 0.35 | ✅ 瞬时（113× 恢复）|
| `*.swift` | 241.13 | 30.30 | 1.33 | 3.93 | 1.31 | ✅ 瞬时（184× 恢复）|
| `ext:cpp` | 94.42 | 17.99 | 1.46 | 0.39 | 0.34 | ✅ 瞬时（278× 恢复）|

**FLOOR-METHOD 路径 6× 复测：**

| query | 全部 6 次复测（升序）| floor(min) | 簇说明 |
|---|---|---|---|
| `size:>100m` | 16.51 / 24.83 / 39.19 / 40.25 / 47.64 / 61.1 | **16.51** | 单峰偏散 |
| `dm:today` | 45.39 / 68.01 / 70.46 / 84.48 / 87.23 / 98.95 | **45.39** | ⚠️ 45.39 低于簇 68-99 达 33% |
| `content:import` | 142.5 / 147.71 / 155.0 / 157.12 / 157.51 / 157.74 | **142.50** | 近单峰（142-158，最稳）|
| `path:/Users` | 149.29 / 157.8 / 172.24 / 192.12 / 277.9 / 290.2 | **149.29** | 双峰 |
| `中文测试` | 146.21 / 221.55 / 250.81 / 260.21 / 289.74 / 301.59 | **146.21** | 双峰 |

**dm floor 单探针风险（严格披露）**：dm 的 floor 45.39ms 比其余 5 次（68-99ms）显著低，是单点异常洁净读数。floor-method 取 min 的依据是「争用纯加性、min 最接近真热态」——但单个远低于簇的读数也可能是低侧噪声。**故本轮对 dm 同时给出保守值**：若取次低 68.01ms（11.34 ns/槽），间隙降为 2.10×。两种取值下分裂结论一致（见 §5 对抗检验），但 floor 间隙的绝对数本轮不稳健，不据此宣称分裂收窄/扩大趋势。

---

## 3. 洁净 ns/槽（floor 法，records=5,995,181）

| 路径 | 类型 | floor(ms) | 洁净值(ns/槽) | 保守值(次低) |
|---|---|---|---|---|
| `size:>100m` | 数值 | 16.51 | **2.75** | — |
| `dm:today` | 数值 | 45.39 | **7.57** | 11.34（68.01ms）|
| `content:import` | 字符串 | 142.50 | **23.77** | — |
| `path:/Users` | 字符串 | 149.29 | **24.90** | — |
| `中文测试` | 字符串 | 146.21 | **24.39** | — |

---

## 4. 本轮头条：ingest 振荡进入第二个收敛相，CJK 热态惩罚消失

R95 稳态 → R96 突发 → R97 收敛 → R98 再强化 → **R99 再收敛**——后台写入呈**振荡**而非单调，本轮回到 R97 型减弱相：

| 信号 | R97（收敛）| R98（再强化）| **R99（本轮，再收敛）** |
|---|---|---|---|
| 电池峰值 | 403ms | 698ms | **284ms** |
| 缓存标志 `x` | 正常 | 427.88ms(破) | **7.01ms（归位）** |
| 窗口内新 >500ms | 0 | 3 | **0** |
| tomb 增量(30min) | +14,827 | +18,708 | **+11,782（再收敛）** |
| 分裂判定 | 23/23 | 24/24 | **25/25** |

**新观察——CJK 热态惩罚消失（支持 R79）**：本轮字符串层三路径 floor 高度聚拢——content 23.77、中文 24.39、path 24.90，全部落在 1.13 ns/槽带内，且 **中文（平日字符串最差）本轮居中**。这进一步证实 R79：字符串层内部排序为逐区页冷热噪声，`中文测试` 在过往轮次「字符串最差」并非 linear-gcd 算法对 CJK 有固有惩罚，而是页冷——一旦进入热态（收敛轮争用低），CJK 全扫成本与 content/path 几乎无差。**这把「CJK 慢」从算法问题降级为页驻留问题**，影响 P1 CJK trigram 索引的优先级判断（热态下收益有限，仅冷态/首查受益）。

---

## 5. 绝对二层分裂：floor 法 25/25 不破

- **数值层**：size 2.75、dm 7.57（保守 11.34）→ 最高数值 **dm 7.57（保守 11.34）**
- **字符串层**：content 23.77、中文 24.39、path 24.90 → 最低字符串 **content 23.77**
- **floor 间隙 = 23.77 / 7.57 = 3.139×**（保守取值 23.77 / 11.34 = **2.10×**），无重叠。

**对抗性检验（R97 协议，决定性）**：即便取 dm **最差洁净样本** 98.95ms（16.50 ns/槽），仍 < content 地板 23.77 ns/槽——**HOLDS**。这意味着分裂结论**完全不依赖 dm floor 单探针的可信度**：无论取 dm 的 floor（45.39）、次低（68.01）还是最差（98.95），数值层天花板始终低于字符串层地板。25/25 在最不利估计下建立。

**间隙数披露**：本轮 floor 间隙 3.139× 名义上为 R84（3.49×）以来最宽，但成因是 dm 单次异常洁净探针而非机制变化；保守取值 2.10× 落在区间中部。**结论：分裂稳健（机制级，纯数值零解引用 ≪ 字符串解引用散布串池），但本轮间隙绝对值不稳健，区间 2.10×~3.14× 均在既定 2.11×~3.49× 内**——不构成趋势性新结论。

---

## 6. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **637 次**（R98 575）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100 提前 return，`:159` tombstone 比例阀（`kTombstoneCompactRatio=0.25`）结构性永不可达——全会话 0 触发、0 回收。skip-gate 575→637（+62），与持续写入一致。

---

## 7. tombstone 趋势：9.91%，连续 32 轮单调，即将破 10%

| 轮次 | tombstone | ratio | 30min 增量 |
|---|---|---|---|
| R95@15:41 | 525,387 | 8.87% | +21,579 |
| R96@16:11 | 548,937 | 9.23% | +23,550 |
| R97@16:41 | 563,764 | 9.46% | +14,827 |
| R98@17:11 | 582,472 | 9.74% | +18,708 |
| **R99@17:41** | **594,254** | **9.91%** | **+11,782（再收敛）** |

会话内单调递增、永不回缩，**连续 32 轮**。R43 参考崩溃点 12.97%（~800K），当前 9.91% 已过 **~76% 程，下一轮几乎必破 10%**。本轮 live +1,716（5,400,927）、total +14,057，即 **87.8% 新增记录变为 tombstone**——R67 阀门破损使墓碑永不回收。增量虽随 ingest 振荡（本轮收敛），但方向不可逆，逼近崩溃点。这是会话内唯一真实退化，修复紧迫性随阈值逼近持续最高。

---

## 8. 日志健康度

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **487 条**（R98 442，+45）——增速随 ingest 略升
- Top-10 峰值 `[3362, 3766, 3807, 6061, 6811, 7544, 8205, 15749, 17928, 18582]` —— 仍与历史**逐字节相同**（均为启动期基建事件，非查询）
- 延迟分布（QueryAdvanced-only）：>5s=**7**（冻结）、1-5s=**27**（冻结）、0.5-1s=**29（+1）**、0.1-0.5s=424、中位 **220ms**
- **0.5-1s +1 来源**：R99 电池窗口（17:41-17:44）实测 **0 条新 >500ms**，故 +1 来自 R98→R99 间隔期（17:14-17:41）ingest 从峰值回落途中残留的一条——本轮电池本身洁净到未新增越线条目
- 启动基建事件须排除：`20765ms`@11:05:13（Phase-2 replay）、`10353ms`@11:12:29（启动 flush）——非查询，已过滤

---

## 9. 优化建议（P0 优先级不变）

1. **🔥 P0｜读写锁分离 / 无锁快照读**：R98 缓存标志破（`x`=427ms）已是「写者饿死读者」最干净证据；本轮虽收敛但 lockHeld≈total 全线仍在——ingest 振荡四轮（R95-R99）持续指向同一争用源，读写锁分离 / RCU 快照读为最高优先级。
2. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调 <12.97%（建议 8–10%）。当前 9.91%，下轮破 10%，tombstone 永不回收逼近崩溃点。
3. **🔥 P0｜无锁快照 flush**：启动 flush 持锁 10.3s（与 #1 同属锁争用类）。
4. **🔥 P0｜查询超时保护**：R98 多个 >500ms 停顿样本——超时熔断 + 降级返回是必要韧性机制。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1｜CJK trigram 索引**（本轮**降级**）：本轮 `中文测试` floor 24.39 ns/槽与 content 23.77 几乎无差，证明 CJK linear-gcd 慢主要是页冷而非算法——热态收益有限，仅冷态/首查受益，优先级应低于锁争用类 P0。

---

## 10. 结论

- **定性**：🟡 活跃后台写入「再收敛」轮。继 R98 再强化峰值后 ingest 回落 R97 型：电池峰值 698→284ms、缓存标志归位、窗口 0 条新 >500ms、tomb 增量 +11,782 收敛。ingest 呈**振荡**（稳态→突发→收敛→再强化→再收敛）而非单调。
- **分裂 25/25 稳健，间隙数本轮不稳健（严格披露）**：floor 间隙 3.139× 名义最宽，但由 dm 单次异常洁净探针（45.39ms，低于簇 33%）驱动；保守取值 2.10×。**对抗性检验决定性**：dm 最差洁净（16.50ns）仍 < content floor（23.77ns），分裂结论不依赖单探针可信度——25/25 在最不利估计下成立，但间隙绝对值不据此宣称趋势。
- **新观察——CJK 热态惩罚消失**：收敛轮字符串层三路径 floor 聚拢 1.13 ns/槽内、中文居中，证实 R79 字符串内排序为页冷噪声，并将 P1 CJK 索引降级（热态收益有限）。
- **唯一持续恶化项**：tombstone **9.91%（594,254）**，连续 32 轮单调，~76% 逼近崩溃点、下轮必破 10%，87.8% 新增入墓，根因 R67 阀门结构性不可达明确。
- **诚实修正纪律**：本轮严格披露 dm floor 单探针风险、给出保守间隙、以对抗检验剥离单点依赖，未被 3.139× 名义最宽间隙误导宣称分裂扩大——贯彻「不轻易下结论、每个结论需逻辑与数据支撑」。
