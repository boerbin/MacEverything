# MacEverything 性能分析报告 R_2606131317

## 1. 本轮范围

- 任务启动时间：2026-06-13 13:17
- 报告文件：`docs/performance_ana/R_2606131317.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=12287`
  - `SESSION_START_TS=2026-06-13 12:03:04.944`
  - `TOTAL_LINES=12401`
  - `SEG_LINES=115`
  - `LAST_LOG_TS=2026-06-13 13:17`

## 2. 执行摘要

**本轮 16-query battery 中 0/16 >500ms，Max=346.6ms，最高项重新回到 `中文测试`。** P50≈39.6ms，P90≈245.7ms；相比 12:47，Max 从 337.4ms 小幅升至 346.6ms，P50 从约 25.0ms 升至约 39.6ms，P90 从约 279.4ms 降至约 245.7ms。所有 lockWait 均为 0ms，说明前台尾部主要仍在 phase2 扫描、过滤或 postings 处理。

后台结构性风险继续恶化：recordCount=6,201,227，liveRecordCount=5,309,867，tombstone=891,360（14.37%），较上一轮增加 6,722。最新 session `FLUSH=14`，较上一轮 +6，`SKIPFLUSH=0`；session 最大 flush total 仍为 6164ms，最新 flush total=2753ms。新 session 已持续累积完整重写次数，但没有任何可见退避。

| 指标 | R_2606131247 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 337.4ms | **346.6ms** | ↑3% |
| P50 | ≈25.0ms | **≈39.6ms** | ↑58% |
| P90 | ≈279.4ms | **≈245.7ms** | ↓12% |
| lockWait max | 0.0ms | **0.0ms** | 持平 |
| 极佳比例(<50ms) | 11/16 | **9/16** | -2 |
| 扫描/过滤路径总和 | 952.8ms | **951.3ms** | 基本持平 |
| recordCount | 6,193,947 | **6,201,227** | +7,280 |
| liveRecordCount | 5,309,309 | **5,309,867** | +558 |
| tombstone | 884,638 (14.28%) | **891,360 (14.37%)** | +6,722 |
| 最新 session FLUSH | 8 | **14** | +6 |
| 最新 session QUERYADV_GE500 | 0 | **0** | 持平 |
| 最新 flush total | 2998ms | **2753ms** | ↓8% |
| session 最大 flush total | 6164ms | **6164ms** | 持平 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 0.9 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 27.8 | 24.0 | advanced-trigram | 0.0 | 10155 | 极佳 |
| `main.cpp` | 5 | 11.7 | 10.2 | advanced-trigram | 0.0 | 103 | 极佳 |
| `*.swift` | 5 | 54.8 | 52.1 | advanced-trigram | 0.0 | 9622 | 正常，略高于 50ms |
| `ext:cpp` | 5 | 19.1 | 18.8 | advanced-ext-index | 0.0 | 3891 | 极佳 |
| `size:>100m` | 5 | 41.9 | 41.5 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 157.0 | 153.6 | advanced-trigram | 0.0 | 3932 | 正常，trigram 尾部 |
| `abc def ghi jkl mno pqr` | 0 | 53.7 | 53.6 | advanced-trigram | 0.0 | 14617 | 正常，略高于 50ms |
| `README` | 5 | 8.7 | 5.5 | advanced-trigram | 0.0 | 17582 | 极佳 |
| `test` | 5 | 37.2 | 35.1 | advanced-trigram | 0.0 | 118019 | 极佳 |
| `MacEverything` | 5 | 4.7 | 1.4 | advanced-trigram | 0.0 | 7036 | 极佳 |
| `dm:today` | 5 | 90.7 | 90.2 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 226.4 | 211.3 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常高位，pure-filter 尾部 |
| `path:/Users` | 5 | 245.7 | 239.8 | advanced-linear-gcd | 0.0 | 0 | 正常高位，linear-gcd 尾部 |
| `中文测试` | 0 | 346.6 | 344.6 | advanced-linear-gcd | 0.0 | 0 | 正常高位，本轮最高 |
| `verylongquery...` | 0 | 2.5 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈39.6ms，P90≈245.7ms，Max=346.6ms。0/16 查询 >500ms。lockWait max=0.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **9/16** | 0.9-41.9ms | short-query-cache、轻量 trigram、ext-index 与 `size:>100m` 仍健康 |
| 正常（50-500ms） | **7/16** | 53.7-346.6ms | trigram、pure-filter、linear-gcd 都有 50ms+ 项 |
| 超标（>500ms） | **0/16** | — | 本轮无正式超标查询 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,201,227；
- liveRecordCount=5,309,867；
- tombstone=891,360（**14.37%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=12287
SESSION_START_TS=2026-06-13 12:03:04.944
TOTAL_LINES=12401
SEG_LINES=115
QUERYADV=13
QUERYADV_GE500=0
FLUSH=14
SKIPFLUSH=0
COMPACTION=2
LAST_LINE=[2026-06-13 13:17:46.411] [INFO ] [SearchEngine] QueryAdvanced "中文测试" total=344ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6201225
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 0，本轮没有日志级慢查询。`中文测试` 的日志 total=344ms，与 query battery 的 346.6ms 对齐，说明当前最新 session 中 linear-gcd 的中文查询仍是最高前台尾部。

```text
[2026-06-13 13:17:46.411] QueryAdvanced "中文测试" total=344ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6201225
```

### flush 情况

最新 session 内 `FLUSH=14`，rewrite 范围 **2622ms - 4809ms**，total 最大 **6164ms**。`SKIPFLUSH=0`，说明 session 内完整 flush 持续累积，但没有任何可见 backoff。

最新 flush：

```text
[2026-06-13 13:13:23.778] Flushed v6 flat index, lastEventId=360789048,
liveRecords=5309803 | timing: walSwap=0ms rewrite=2751ms total=2753ms
```

Top flushes：

```text
[2026-06-13 12:08:25.921] rewrite=4809ms total=4812ms
[2026-06-13 12:38:27.299] rewrite=3809ms total=6164ms
[2026-06-13 12:13:24.551] rewrite=3435ms total=3437ms
[2026-06-13 12:23:24.104] rewrite=3015ms total=3018ms
[2026-06-13 12:33:24.094] rewrite=2986ms total=2989ms
```

## 6. 问题清单

### P0：tombstone 在 14% 以上继续上行

- 证据：tombstone=891,360（14.37%），较上一轮增加 6,722。
- 影响：无效记录比例继续升高，会放大扫描成本、索引重写成本与 compaction 压力。
- 判断：连续三轮位于 14% 以上并继续增长，compaction/清理策略仍未有效压低 tombstone。

### P0：linear-gcd 尾部仍是最高前台风险

- 证据：`中文测试`=346.6ms，phase2=344.6ms；`path:/Users`=245.7ms，phase2=239.8ms。
- 影响：中文与路径查询仍缺少有效候选剪枝，在 tombstone 上升和 page-cache 状态变化时会成为用户可感知尾部。
- 判断：lockWait=0ms，瓶颈主要在 phase2 扫描阶段。

### P0：new session 内 flush 持续累积且无退避

- 证据：`FLUSH=14`，较上一轮 +6；`SKIPFLUSH=0`；session 最大 flush total=6164ms。
- 影响：完整重写继续制造 I/O 与 page-cache 干扰，并可能导致 trigram 与扫描路径的周期性波动。

### P0：pure-filter 尾部仍维持 200ms+

- 证据：`content:import`=226.4ms、`dm:today`=90.7ms，主要耗时均落在 phase2。
- 影响：pure-filter 仍是稳定尾部来源，不随 linear-gcd 的高低切换而消失。

### P1：trigram 路径存在中位以上波动

- 证据：`hello world`=157.0ms，`*.swift`=54.8ms，`abc def ghi jkl mno pqr`=53.7ms。
- 影响：trigram 当前未超标，但 postings 读取、候选处理或 page-cache 状态仍会把部分查询推到 50-150ms 区间。

## 7. 优化建议

### P0：立即提前或分段 compaction

- 以 tombstone 率 14%+ 作为明确触发信号，不再等待更高阈值。
- 结合绝对 tombstone 数、segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 优先处理 tombstone 密集 segment，避免 scan/filter 路径继续扫描大量无效记录。
- 验收：tombstone 率从 14.37% 回落，`中文测试`、`path:/Users`、`content:import` 稳定 <200ms。

### P0：flush 改为增量/COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页或持锁窗口。
- 验收：常规 flush total <200ms，新 session 不再出现 3-6s 级完整 rewrite。

### P0：增加 flush I/O 压力退避和前台保护

- 当最近 flush 超过阈值时，提高下一次 flush 间隔、降低 batch 频率，或前台查询活跃时推迟大 flush。
- 持久化线程使用低 QoS，并评估大顺序写的 `F_NOCACHE`/同类策略，减少对搜索热页的冲刷。
- 验收：`SKIPFLUSH` 不再长期为 0，6s+ flush 后不再出现 200-300ms 扫描尾部。

### P0：为 advanced-linear-gcd 与 pure-filter 增加轻量剪枝

- 对中文、路径、content/filter 类查询记录扫描记录数、tombstone 跳过数和 segment 分布。
- 评估轻量字符 n-gram、路径分段索引、预过滤位图或 segment-level skip list，以减少无结果或低选择性查询的全量扫描。
- 要求保持内存增长可控，优先使用压缩或按需结构，不引入大常驻索引。
- 验收：`中文测试` 不再成为稳定 Max，`path:/Users` 与 `content:import` 稳定低于 200ms。

### P1：细分 trigram/postings 路径

- 对 `advanced-trigram` 记录 postings 读取、候选交并、候选评分、路径加载、结果组装、page-cache miss 近似指标。
- 重点解释 `hello world` 在 117-210ms 区间波动后的本轮 157.0ms 成本来源。
- 验收：trigram 查询在 flush 后仍稳定 <100ms，或至少能解释每个阶段的成本。

### P1：拆分 top-level query timing

- 对所有路径增加 queueWait、phase2、candidate materialization、path/file metadata load、sort/rank、result assembly、HTTP serialization/write 等阶段耗时。
- 验收：`queryTimeMs - phase2Ms - lockWaitMs` 不再是不可解释的大黑盒。

## 8. 下一轮重点

1. tombstone 是否继续从 14.37% 上行。
2. `中文测试` 是否继续作为 Max，或 `path:/Users` 再次切换为最高项。
3. `content:import` 是否继续维持 200ms+。
4. `hello world` 是否继续在 100-200ms 区间波动。
5. 新 session 中 `QUERYADV_GE500` 是否开始累积。
6. 新 session flush 是否继续出现 6s+ outlier。
7. `SKIPFLUSH` 是否仍为 0。

## 9. 结论

**本轮没有 >500ms 查询，Max=346.6ms，P50≈39.6ms，P90≈245.7ms。** 前台仍由 `中文测试`、`path:/Users`、`content:import` 和 `hello world` 构成尾部，主要耗时集中在 phase2，lockWait 没有贡献。

**后台根因继续恶化：tombstone=891,360（14.37%），新 session `FLUSH=14`，最大 flush total=6164ms，且 `SKIPFLUSH=0`。** 下一步仍应优先推进提前/分段 compaction、增量/COW flush、flush 前台保护/退避、advanced-linear-gcd/pure-filter 剪枝，以及 trigram 与 top-level timing 拆分。
