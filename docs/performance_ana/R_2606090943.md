# MacEverything 性能分析报告 R_2606090943

## 1. 本轮范围

- 任务启动时间：2026-06-09 09:43
- 报告文件：`docs/performance_ana/R_2606090943.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5486`
  - `SEG_LINES=4544`
  - `LAST_LOG_TS=2026-06-09 00:17`（注意：最后日志在午夜，说明凌晨至上午无活动）

## 2. 执行摘要

**⚠️ 系列最严重单轮——12/16 >500ms，Max=4514ms，lockWait=761ms，P50=1053ms。** 这是系列 200 轮中最严重的单轮事件：

| 指标 | 本轮 R199 | 此前系列最差 | 对比 |
|------|--------:|--------:|------|
| >500ms 数量 | **12/16** | 4/16 (R197/R198) | **3x** |
| Max | **4514ms** | 7193ms (R181) | 低于 R181 |
| lockWait max | **761ms** | 567ms (R197) | **↑34%（新记录）** |
| 总 lockWait 查询数 | **6/16** | 1/16 (R197) | **6x** |
| P50 | **1053ms** | 206ms (R197) | **5x** |
| QUERYADV_GE500 增量 | **+11** | +4 (R198) | **2.75x** |

**根因分析：隔夜冷启动 + flush 锁竞争复合事件。**

1. **隔夜冷启动**：最后日志 00:17，battery ~09:43——中间约 9.5 小时无活动。所有 mmap 区域的 page cache 几乎完全被系统回收。等效于全冷启动。
2. **多次 flush 锁竞争**：6/16 查询出现 lockWait（最高 761ms），说明 battery 的密集查询触发了 flush timer 的多次尝试。
3. **进程调度延迟**：`x`=85ms 说明进程在唤醒阶段仍受系统竞争影响。

### 关键指标对比

| 指标 | R198 | R199 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 4/16 | **12/16** | **↑8（系列最高！）** |
| Max | 1567ms | **4514ms** | **↑ 188%** |
| P50 | 182ms | **1053ms** | **↑ 479%** |
| lockWait max | 12ms | **761ms** | **↑ 6242%** |
| 极佳比例(<50ms) | 1/16 | **1/16** | 持平 |
| tombstone | 1,695,242 (24.71%) | **1,711,595 (24.93%)** | ↑ |
| tombstone 增速 | ~78/min | **~25/min** | **↓ 68%（凌晨极低）** |
| recordCount | 6,832,535 | **6,864,330** | +31.8K（偏高） |
| liveRecordCount | 5,137,293 | **5,152,735** | +15.4K |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 4071 | **4136** | +65 |
| COMPACTION | 2 | **2** | **不变——compaction 仍未触发！** |
| QUERYADV_GE500 | 29 | **40** | **↑11** |

### 隔夜冷启动——所有区域等效全冷

- **最后日志 00:17，battery ~09:43——9.5 小时无活动。**
- **macOS 在空闲时主动回收 page cache（尤其是 mmap 区域）。**
- **结果：所有数据区域（trigram postings、SoA 数组、路径数组、名称数组）全部冷态。**
- **这比 R176（睡眠/唤醒 ~6 小时）更严重——9.5 小时的完全空闲给系统足够时间回收一切。**

### lockWait 多点爆发——6/16 查询

| Query | lockWait (ms) | 说明 |
|-------|----------:|------|
| `size:>100m` | **760.8** | **系列绝对最高** |
| `*.swift` | **501.2** | >500ms |
| `abc def...` | 48.5 | 中度 |
| `中文测试` | 36.7 | 中度 |
| `README` | 33.9 | 中度 |
| `ext:cpp` | 29.9 | 中度 |
| `content:import` | 8.9 | 轻度 |

- **6/16 查询出现 lockWait——此前最多 1-2/16。**
- **`size:>100m` lockWait=761ms——系列绝对最高（超 R197=567ms，↑34%）。**
- **`*.swift` lockWait=501ms——第二高。**
- **多点 lockWait 说明 flush timer 在 battery 期间多次尝试获取写锁。**
- **冷启动场景下 battery 密集查询持续持有读锁（每个查询耗时长），flush 获取写锁等待时间更长。**

### `content:import`=4514ms——系列第二高单查询

- **phase2=4092ms，lockWait=9ms——主要是 SoA 全扫描的 page cache 冷态。**
- **SoA 全扫描在 9.5 小时冷启动后需要从磁盘读取整个数据区域。**
- **仅次于 R181 的 `.pdf`=7193ms。**

### `hello world`=3128ms——trigram 最严重

- **phase2=2817ms，lockWait=0——trigram postings 区域从磁盘重新加载。**
- **3639 candidates 的 postings 分散在文件各处——随机 I/O 模式，冷启动下最慢。**

### `README`=2983ms 和 `test`=2802ms

- **`README`：phase2=1951ms，lockWait=34ms，candidates=16834。**
- **`test`：phase2=2654ms，lockWait=0，candidates=116812。**
- **高 candidates 查询在冷启动下需要读取大量 postings 页——全部是磁盘 I/O。**

### `dm:today`=1913ms——SoA filter 首次 >1000ms

- **phase2=1844ms，lockWait=0。**
- **SoA 元数据（修改时间字段）的 page cache 完全冷态。**
- **此前最高 R197=648ms——本轮 2.9x。**

### 全表扫描——灾难级

| Query | R198 | R199 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 148ms | **4514ms** | **↑2950%** |
| `path:/Users` | 1072ms | **962ms** | ↓10% |
| `中文测试` | 765ms | **679ms** | ↓11% |
| **总和** | **1985ms** | **6155ms** | **↑210%** |

- **全表扫描总和 6155ms——系列绝对最高！**
- **`content:import`=4514ms 主导——SoA 区域完全冷态。**
- **`path:/Users`=962ms 和 `中文测试`=679ms 反而低于 R198——可能是 battery 前几个查询的预热效果。**

### Trigram 路径——全面冷启动

| Query | R198 | R199 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 1567ms | **722ms** | ↓54% | >500ms |
| `hello world` | 371ms | **3128ms** | **↑743%** | **>500ms** |
| `*.swift` | 680ms | **1353ms** | ↑99% | **>500ms（含 lockWait=501ms）** |
| `main.cpp` | 72ms | **575ms** | **↑699%** | **>500ms** |
| `abc def...` | 436ms | **1053ms** | ↑142% | **>500ms** |
| `README` | 105ms | **2983ms** | **↑2741%** | **>500ms** |
| `test` | 270ms | **2802ms** | **↑938%** | **>500ms** |
| `MacEverything` | 28ms | **49ms** | ↑75% | 极佳边缘 |
| Trigram 均值 | 441ms | **1333ms** | **↑202%** | 全面冷启动 |

- **8/8 trigram 查询中 7 个 >500ms——仅 `MacEverything`=49ms 因为 phase2=17ms（缓存命中或极少 postings 读取）。**
- **均值 1333ms——系列最高。**

### tombstone 24.93%——compaction 仍未触发

- **tombstone=1,711,595（24.93%）。**
- **距 25% 仅 0.07 个百分点（4,488 tombstone）。**
- **增速 ~25/min（凌晨极低——9.5 小时仅增 16,353）。**
- **但白天恢复后增速将回升至 200-400/min——预计数小时内触发。**
- **COMPACTION 仍为 2——阈值尚未触达。**

### `x`=85ms——进程调度延迟

- **`x` 使用纯内存缓存——85ms = ~83ms 调度延迟。**
- **低于 R198=119ms——但仍远高于正常（<10ms）。**
- **上午系统刚启动，后台任务（Spotlight、Time Machine）可能仍在运行。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 84.6 | 0.0 | short-query-cache | 0 | 0 | 进程延迟 |
| `.pdf` | 5 | 722.0 | 549.8 | advanced-trigram | 0 | 9842 | **>500ms** |
| `main.cpp` | 5 | 575.4 | 229.9 | advanced-trigram | 0 | 82 | **>500ms** |
| `*.swift` | 5 | 1352.7 | 320.4 | advanced-trigram | **501.2** | 9345 | **>500ms（lockWait=501ms）** |
| `ext:cpp` | 5 | 136.7 | 27.6 | advanced-ext-index | 29.9 | 3571 | 正常 |
| `size:>100m` | 5 | 2392.2 | 751.7 | advanced-pure-filter-soa-gcd | **760.8** | 0 | **>500ms（lockWait=761ms！）** |
| `hello world` | 5 | 3127.6 | 2816.7 | advanced-trigram | 0 | 3639 | **>500ms** |
| `abc def ghi jkl mno pqr` | 0 | 1053.4 | 973.4 | advanced-trigram | 48.5 | 14339 | **>500ms** |
| `README` | 5 | 2983.3 | 1951.4 | advanced-trigram | 33.9 | 16834 | **>500ms** |
| `test` | 5 | 2802.2 | 2654.4 | advanced-trigram | 0 | 116812 | **>500ms** |
| `MacEverything` | 5 | 48.7 | 17.2 | advanced-trigram | 0 | 6859 | 极佳边缘 |
| `dm:today` | 5 | 1913.3 | 1844.2 | advanced-pure-filter-soa-gcd | 0 | 0 | **>500ms** |
| `content:import` | 5 | 4514.3 | 4091.7 | advanced-pure-filter-soa-gcd | 8.9 | 0 | **>500ms（Max!）** |
| `path:/Users` | 5 | 962.2 | 954.5 | advanced-linear-gcd | 0 | 0 | **>500ms** |
| `中文测试` | 0 | 679.3 | 510.0 | advanced-linear-gcd | 36.7 | 0 | **>500ms** |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 1053.4ms，P90 = 3127.6ms，Max = 4514.3ms。12/16 查询 >500ms。lockWait max = 760.8ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R198 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 1/16 | 0.2-49ms | R198=1/16（持平） |
| **正常**（50-500ms） | 3/16 | 85-137ms | R198=11/16（**↓8**） |
| **超标**（>500ms） | **12/16** | 575-4514ms | R198=4/16（**↑8**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,864,330，liveRecordCount=5,152,735（tombstone=**1,711,595**，**24.93%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=320  QUERYADV_GE500=40（+11！系列最大单轮增量）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4136
```

- **QUERYADV_GE500 +11——系列最大单轮增量（此前最高 +4）。**
- **COMPACTION 仍为 2——25% 阈值尚未触达。**
- **FLUSH 仍为 1——虽然 lockWait 多次出现，flush 未完成新写入。**

## 6. 问题清单

### P0 极度紧急：tombstone 24.93%——仅 0.07% 距阈值

- **距 25% 仅 4,488 tombstone（0.14 个百分点）。**
- **凌晨增速 ~25/min，但白天将回升至 200-400/min。**
- **预计今天上午 10:00-12:00 之间触发 compaction。**

### P0.5 确认：lockWait=761ms——COW flush 必要性再次证实

- **6/16 查询出现 lockWait（最高 761ms，新记录）。**
- **冷启动场景下 lockWait 更严重——查询耗时长，持有读锁时间长，flush 等待更久。**
- **COW flush 可彻底消除此问题。**

### 观察项：隔夜冷启动是最严重场景

- **12/16 >500ms——比任何午后/傍晚事件都严重。**
- **系统在 9.5 小时空闲后主动回收所有 mmap page cache。**
- **但这是预期行为——冷启动后通过查询预热可在 1-2 轮恢复。**

## 7. 优化建议

### P0：立即排查 compaction rewrite 崩溃根因

- **tombstone 24.93%，今天上午极可能触发。**
- **紧迫度：立即——最后几小时窗口。**

### P0.5：flush rewrite 改为 COW 方式

- **lockWait=761ms（新记录）——冷启动场景下更严重。**
- **6/16 查询受锁竞争影响——不可接受。**

### P2：冷启动预热策略

- **新建议：进程启动后主动预热关键数据区域。**
- **使用 `madvise(MADV_WILLNEED)` 或后台查询预热 trigram/SoA/路径数组。**
- **可将冷启动 12/16 >500ms 降至 2-4/16。**

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. 冷启动是否在 1-2 轮内恢复（预计是）。
2. tombstone 是否突破 25% 触发 compaction。
3. lockWait 是否在后续轮恢复至 0。

## 9. 结论

**⚠️ 系列最严重单轮——12/16 >500ms，Max=4514ms，lockWait=761ms（新记录），P50=1053ms。** 根因：隔夜 9.5 小时无活动导致全 page cache 冷启动 + flush 锁竞争复合事件。6/16 查询出现 lockWait。QUERYADV_GE500 +11（系列最大单轮增量）。

**这是隔夜冷启动的预期最差表现——但也是最可恢复的场景。** 预计 1-2 轮 battery 预热后将显著恢复。系列此前已证明 4 次灾难性事件均 1 轮恢复。

**tombstone 24.93%——距 25% 仅 0.07 个百分点，今天上午极可能触发 compaction。** P0 compaction rewrite 修复进入最后窗口。同时 lockWait=761ms 再次证实 P0.5 COW flush 的必要性——冷启动场景下锁竞争最严重。
