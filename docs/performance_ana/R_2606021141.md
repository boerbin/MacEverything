# MacEverything 性能分析报告 R_2606021141

## 1. 本轮范围

- 任务启动时间：2026-06-02 11:41
- 报告文件：`docs/performance_ana/R_2606021141.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=10631`
  - `SESSION_START_TS=2026-05-31 00:09:38.774`
  - `TOTAL_LINES=21026`
  - `SEG_LINES=10396`
  - `LAST_LOG_TS=2026-06-01 16:57:52.124`

本轮在 R_2606021111 已确认服务持续 connection refused 的基础上继续复核。结果仍没有变化。

## 2. 执行摘要

本轮没有获得任何成功 query 样本。固定 battery 的 16 个查询全部返回 `<urlopen error [Errno 61] Connection refused>`。状态、floor/reprobe、额外 HTTP 检查同样全部 connection refused。进程检查没有列出 MacEverything 进程。

日志段与之前所有 refused 轮次完全一致：`TOTAL_LINES=21026`，最新日志停在 `2026-06-01 16:57:52.124` 的 `COW compaction done`，没有新的 `Log session started`、`WARN` 或 `ERROR`。

服务已持续不可用约 19 小时。

## 3. Query Battery 结果

所有 query 均失败（connection refused）：

| Query | 结果 |
|---|---|
| `x` | connection refused |
| `.pdf` | connection refused |
| `main.cpp` | connection refused |
| `*.swift` | connection refused |
| `ext:cpp` | connection refused |
| `size:>100m` | connection refused |
| `hello world` | connection refused |
| `abc def ghi jkl mno pqr` | connection refused |
| `README` | connection refused |
| `test` | connection refused |
| `MacEverything` | connection refused |
| `dm:today` | connection refused |
| `content:import` | connection refused |
| `path:/Users` | connection refused |
| `中文测试` | connection refused |
| 超长字符串 | connection refused |

状态、floor/reprobe、额外 HTTP 检查也全部 connection refused。

## 4. 服务可用性状态

- `localhost:19860` 没有可接受连接的 HTTP 服务；
- `pgrep -fl MacEverything` 未列出 MacEverything 进程；
- 应用日志没有写入新记录。

连续可用性失败已跨越二十一轮（从 R_2606011642 后半段开始）。

## 5. 最新日志段分析

### 5.1 汇总

```text
WARN=0 ERROR=0
QUERYADV=3906  QUERYADV_GE500=120
FLUSH=1877  FLUSH_TOTAL_GE5=1877  FLUSH_TOTAL_GE10=1877
COMPACTION=4  RECLAIM=0  SKIPFLUSH=29
```

与 R_2606021111 完全一致，无任何新增。

flush 最慢样本：`total=2064533ms`、`954403ms`、`524624ms`、`98701ms`、`37811ms`。

### 5.2 Compaction 生命周期仍是最后可见事件

| 时间 | 事件 |
|---|---|
| 16:37:27.965 | tombstone ratio 25.0006% > 25%，full compaction 触发 |
| 16:38:30.382 | Phase 2 build from 7,329,723 records |
| 16:43:05.145 | Phase 3 swap, 7,329,723 -> 5,497,248 |
| 16:57:52.124 | compaction done, replayed 615 adds / 108 deletes |

## 6. 问题清单

### Critical：服务持续不可用约 19 小时

- 证据：所有 probe 全部 connection refused，进程不存在。
- 优先级：最高。

### Critical：compaction done 后没有服务恢复证据

### High：日志无法解释进程退出

### High：full compaction 与在线服务隔离不足

### High：v6 flat index flush 极端 outlier（最慢 2064533ms）

## 7. 优化建议

### P0：优先恢复服务并补齐生命周期观测

1. 检查 macOS crash report、Console log。
2. 在关键生命周期点（app start/stop、HTTP server start/stop、fatal signal）写日志。
3. 在 compaction 各阶段后验证 HTTP server 仍在监听。

### P0：compaction 完成后必须保持或恢复 HTTP 服务

### P0：将 full compaction 改为可中断、可限速、可恢复

### P1：单独治理 flush total outlier

### P1：前移 tombstone debt 治理（8–10% 开始渐进 reclaim）

## 8. 下一轮重点

1. 先恢复或重启 MacEverything。
2. 检查 macOS crash report 定位进程退出原因。
3. 服务恢复后运行 query battery 比较 compaction 后性能。

## 9. 结论

本轮 11:41 复核显示 MacEverything HTTP 服务仍持续不可用约 19 小时。所有 probe 均 connection refused，进程不存在，日志无新增。最高优先级仍是恢复服务并补齐生命周期可观测性。
