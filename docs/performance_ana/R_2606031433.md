# MacEverything 性能分析报告 R_2606031433

## 1. 本轮范围

- 任务启动时间：2026-06-03 14:33
- 报告文件：`docs/performance_ana/R_2606031433.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21549`
  - `SEG_LINES=523`
  - `LAST_LOG_TS=2026-06-03 21:46`

## 2. 执行摘要

**tombstone 突破 4% 大关**（240,337 条，4.03%）。性能表现两极分化：`path:/Users` 和 `content:import` 完全恢复正常（213ms/219ms），但 `.pdf`=3326ms 和 `hello world`=2034ms 出现新一波 I/O 冲击。`x`=676ms 出现第 6 次缓存异常（历史第 3 高）。lockWaitMs 在多个查询上微量出现（max=15ms）。

P50 ≈ 191ms（前轮 131ms），3/16 查询 >500ms。

### 横向对比（最近 5 轮）

| Query | R68 | R69 | R70 | R71 | R72 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 545 | 15 | 7 | 96 | **676** | **第 6 次异常** |
| `.pdf` | 23901 | 502 | 877 | 728 | **3326** | 新一波冲击 |
| `*.swift` | 1657 | 390 | 266 | 508 | **253** | 恢复 |
| `hello world` | 863 | 269 | 180 | 569 | **2034** | 新一波冲击 |
| `path:/Users` | 2221 | 1370 | 816 | 880 | **213** | **完全恢复** |
| P50 | 641 | 171 | 88 | 131 | **191** | 波动 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 675.5 | 0.0 | short-query-cache | 0 | 0 | **第 6 次异常** |
| `.pdf` | 5 | 3326.0 | 3134.6 | advanced-trigram | 0.1 | 9947 | 严重退化 |
| `main.cpp` | 5 | 68.3 | 44.4 | advanced-trigram | 15.1 | 85 | 正常 |
| `*.swift` | 5 | 252.6 | 223.8 | advanced-trigram | 1.8 | 9356 | 恢复 |
| `ext:cpp` | 5 | 172.4 | 156.7 | advanced-ext-index | 10.9 | 3702 | 正常 |
| `size:>100m` | 5 | 110.0 | 109.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 2034.0 | 1997.5 | advanced-trigram | 0 | 3613 | 严重退化 |
| `abc def ghi jkl mno pqr` | 0 | 161.9 | 152.7 | advanced-trigram | 7.7 | 15993 | 正常 |
| `README` | 5 | 111.7 | 21.8 | advanced-trigram | 3.0 | 17717 | 正常 |
| `test` | 5 | 54.5 | 22.2 | advanced-trigram | 13.7 | 129962 | 正常 |
| `MacEverything` | 5 | 19.2 | 1.1 | advanced-trigram | 0 | 6802 | 极快 |
| `dm:today` | 5 | 109.1 | 101.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 218.7 | 211.4 | advanced-pure-filter-soa-gcd | 0 | 0 | **恢复正常** |
| `path:/Users` | 5 | 212.9 | 207.7 | advanced-linear-gcd | 0 | 0 | **完全恢复** |
| `中文测试` | 0 | 209.4 | 209.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 191ms，P90 ≈ 2034ms，Max = 3326ms。3/16 查询 >500ms。lockWaitMs 在 6 个查询上微量出现（max=15ms）。**

### 恢复与退化对比

| 查询 | R71 | R72 | 变化 |
|------|----:|----:|------|
| `path:/Users` | 880ms | **213ms** | 完全恢复 |
| `content:import` | 732ms | **219ms** | 完全恢复 |
| `*.swift` | 508ms | **253ms** | 恢复 |
| `.pdf` | 728ms | **3326ms** | 严重恶化 |
| `hello world` | 569ms | **2034ms** | 严重恶化 |
| `x` | 96ms | **676ms** | 恶化 |

**模式**：全表扫描（linear-gcd、pure-filter）已恢复，但特定 trigram 查询出现新一波 I/O 冲击。这表明不是全局 I/O 压力，而是**特定文件/页面的 I/O 争用**。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,969,186，liveRecordCount=5,728,849（tombstone=**240,337**，**4.03%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=198  QUERYADV_GE500=32
FLUSH=0  COMPACTION=2  SKIPFLUSH=193
DIAG_TRIGRAM_SKIP=77
```

- QUERYADV 从 188 增至 198（+10）。
- QUERYADV_GE500 从 30 增至 32（+2，`.pdf` 3326ms + `hello world` 2034ms）。
- SKIPFLUSH 从 186 增至 193（+7，增速放缓——可能 I/O 争用影响定时器）。
- DIAG_TRIGRAM_SKIP 从 76 增至 77（+1）。

## 6. 问题清单

### Critical：tombstone 突破 4%（240,337 条，4.03%）

- 增长轨迹：
  ```
  R67: 195K (3.29%) → R68: 199K (3.36%) → R69: 213K (3.58%)
  → R70: 222K (3.73%) → R71: 230K (3.87%) → R72: 240K (4.03%)
  ```
- 本轮增量 9,944 条。连续第 13 轮超 2%。
- **累计内存开销**：~240K × 300 字节 ≈ 72MB。
- **查询开销**：4.03% 额外 phase2 扫描。
- SKIPFLUSH=193，compaction 完全不可触发。

### Critical：`x` 短查询缓存异常频率和严重性持续升级

- **异常历史**（23 轮中 6 次，26% 异常率）：
  ```
  R58: 254ms → R62: 93ms → R65: 364ms → R67: 600ms → R68: 545ms → R72: 676ms
  ```
- 趋势：异常值逐步升高（93→254→364→545→600→676ms）。
- 本轮特征：phase2=0，lockWaitMs=0，但其他查询有轻微 lockWait（15ms），暗示有并发写入活动。
- **可能根因**：缓存路径虽不涉及磁盘 I/O，但受 GCD 线程调度影响。当 FSWatcher 触发密集写入时，GCD 线程池被占用，缓存查询的 dispatch 被延迟。

### High：`.pdf` 和 `hello world` 新一波 I/O 冲击

- `.pdf`=3326ms（前轮 728ms），`hello world`=2034ms（前轮 569ms）。
- 与 R67-R68 不同：`path:/Users` 和 `content:import` 已恢复，`*.swift` 也恢复。
- **不是全局 I/O 压力**——是特定 trigram 索引页面的 I/O 争用。
- lockWaitMs 微量出现（6 个查询，max=15ms），表明有并发写操作但不严重。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（已突破 4%）

- tombstone 4.03%，240K 条。72MB 额外内存。
- **实施已极度紧迫**——每多拖一轮增加 ~10K 条、~3MB 内存、~0.17% 扫描开销。

### P0：将 `.pdf`/`*.swift` 路由到 ext-index

- `.pdf` 连续第 4 轮 >500ms（502→877→728→3326ms）。
- `*.swift` 本轮恢复（253ms），但 `.pdf` 和 `*.swift` 共享 trigram 路径，ext-index 可彻底解决。

### P0：short-query-cache 并发写入隔离

- `x` 第 6 次异常（676ms），趋势升级。
- **建议**：将缓存查询从共享 GCD 队列隔离到独立高优先级队列，避免被 FSWatcher 写入调度挤占。

### P1：实现 phase2 超时降级（承前轮）

## 8. 下一轮重点

1. `.pdf` 和 `hello world` 的新一波冲击是否消退。
2. `x` 缓存异常是否连续——本轮为非 I/O 压力期间的高值。
3. tombstone 增速——4% 后是否加速。

## 9. 结论

**tombstone 突破 4% 大关**（240,337 条，4.03%），连续第 13 轮超 2%，SKIPFLUSH=193。性能两极分化：`path:/Users` 和 `content:import` 完全恢复正常，但 `.pdf`=3326ms 和 `hello world`=2034ms 出现新一波 I/O 冲击——这次不是全局 I/O 压力，而是特定 trigram 索引页面的争用。`x`=676ms 出现第 6 次缓存异常（23 轮中 6 次，26%），异常值趋势持续升级（93→254→364→545→600→676ms），建议将缓存查询隔离到独立高优先级 GCD 队列。
