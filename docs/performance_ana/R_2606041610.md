# MacEverything 性能分析报告 R_2606041610

## 1. 本轮范围

- 任务启动时间：2026-06-04 16:10
- 报告文件：`docs/performance_ana/R_2606041610.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=22303`
  - `SEG_LINES=1277`
  - `LAST_LOG_TS=2026-06-04 16:12`

## 2. 执行摘要

**tombstone 第三次爆发——系统突破 1GB 内存负担。** tombstone 从 2,382,609 (29.30%) 增至 **3,472,840 (37.65%，~1,042MB)**——**单轮 +1,090,231 条**。**3/16 查询 >500ms**（`*.swift`=980ms，`中文测试`=567ms，`hello world`=529ms）。**lockWait 显著恶化：最高 60.8ms**（R101=15.3ms 的 4 倍）——**R86 式灾难预警信号**。P50 ≈ 203ms，Max=980ms。

**SKIPFLUSH 连续第 2 轮 +56**——FSWatcher 事件风暴以恒定速率持续。**FLUSH=1**——历史首次出现非零 flush（可能为边界条件触发）。

### 关键指标对比

| 指标 | R100 | R101 上轮 | R102 本轮 | 变化 |
|------|--------:|--------:|--------:|------|
| >500ms | 2/16 | 2/16 | **3/16** | ↑ 恶化 |
| Max | 2,887ms | 766ms | **980ms** | ↑ 反弹 |
| P50 | 222ms | 216ms | **203ms** | 持平 |
| lockWait max | 19.7ms | 15.3ms | **60.8ms** | **↑↑↑ 4 倍** |
| lockWait 受影响查询 | 13/16 | 14/16 | **15/16** | **↑ 几乎全部** |
| tombstone | 800K (12.21%) | 2,383K (29.30%) | **3,473K (37.65%)** | **↑↑↑ 持续** |
| tombstone 增量 | +231.4K | +1,583.0K | **+1,090.2K** | 持续爆发 |
| tombstone 内存 | 240MB | 714MB | **~1,042MB** | **突破 1GB** |

### tombstone 爆发三轮追踪

| 轮次 | 增量 | 累计 | 比率 | 内存 | 说明 |
|------|-----:|-----:|-----:|-----:|------|
| ≤R99 稳态 | ~12K/轮 | 568K | 8.98% | 170MB | 正常增长 |
| R100 | +231K | 800K | 12.21% | 240MB | 第 1 波爆发 |
| R101 | +1,583K | 2,383K | 29.30% | 714MB | 第 2 波爆发 |
| **R102** | **+1,090K** | **3,473K** | **37.65%** | **~1,042MB** | **第 3 波——突破 1GB** |

**三轮累计 +2,904K tombstone（从 568K 到 3,473K）**——事件源（大规模系统活动）仍未停止，但增量从 +1,583K 略降至 +1,090K（下降 31%），可能接近尾声。

### 横向对比（最近 5 轮）

| Query | R98 | R99 | R100 | R101 | R102 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 11 | 949 | 37 | 14 | **22** | 正常 |
| `.pdf` | 342 | 8495 | 840 | 766 | **484** | **恢复中** |
| `*.swift` | 518 | 430 | 2887 | 409 | **980** | **反弹** |
| `hello world` | 312 | 3042 | 327 | 458 | **529** | **>500ms** |
| `path:/Users` | 379 | 569 | 228 | 627 | **443** | 恢复中 |
| `中文测试` | 295 | 312 | 282 | 206 | **567** | **>500ms** |
| P50 | 155 | 376 | 222 | 216 | **203** | 持平 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 21.9 | 0.0 | short-query-cache | 0 | 0 | 正常 |
| `.pdf` | 5 | 483.9 | 471.6 | advanced-trigram | 0 | 9952 | 接近 500ms |
| `main.cpp` | 5 | 80.2 | 53.0 | advanced-trigram | 6.6 | 85 | lockWait |
| `*.swift` | 5 | 979.9 | 852.3 | advanced-trigram | **60.8** | 9356 | **极端+lockWait最高** |
| `ext:cpp` | 5 | 205.0 | 132.9 | advanced-ext-index | **45.4** | 3701 | **lockWait显著** |
| `size:>100m` | 5 | 482.0 | 459.8 | advanced-pure-filter-soa-gcd | 10.5 | 0 | 接近 500ms |
| `hello world` | 5 | 528.7 | 459.9 | advanced-trigram | **40.5** | 3613 | **>500ms+lockWait** |
| `abc def ghi jkl mno pqr` | 0 | 242.2 | 213.0 | advanced-trigram | 8.5 | 16042 | lockWait |
| `README` | 5 | 200.0 | 162.4 | advanced-trigram | 7.4 | 17679 | lockWait |
| `test` | 5 | 102.8 | 44.3 | advanced-trigram | **27.6** | 130129 | lockWait |
| `MacEverything` | 5 | 58.1 | 4.2 | advanced-trigram | 7.9 | 6803 | lockWait |
| `dm:today` | 5 | 84.7 | 75.0 | advanced-pure-filter-soa-gcd | 3.0 | 0 | lockWait |
| `content:import` | 5 | 182.7 | 165.0 | advanced-pure-filter-soa-gcd | 4.2 | 0 | lockWait |
| `path:/Users` | 5 | 442.6 | 421.5 | advanced-linear-gcd | 1.1 | 0 | 偏高+lockWait |
| `中文测试` | 0 | 566.9 | 509.3 | advanced-linear-gcd | 14.8 | 0 | **>500ms** |
| `verylongquery...` | 0 | 26.8 | 0.0 | advanced-trigram | 25.7 | 0 | lockWait |

**P50 ≈ 203ms，P90 ≈ 529ms，Max = 980ms。3/16 查询 >500ms。lockWaitMs 出现在 15/16 查询中，最高 60.8ms。**

### lockWait 显著恶化分析

| lockWaitMs 范围 | 查询数 | R101 同范围 | 说明 |
|----------------|------:|----------:|------|
| 0ms | 1 | 2 | 仅 `x` |
| 0.1-10.5ms | 8 | 7 | 轻微竞争 |
| 14.8-27.6ms | 3 | 5 | 中度竞争 |
| **40.5-60.8ms** | **3** | **0** | **显著竞争——新出现** |

- **关键变化**：R101 最高 15.3ms → R102 出现 3 个 >40ms 的查询（`*.swift`=60.8ms，`ext:cpp`=45.4ms，`hello world`=40.5ms）。
- **60.8ms 是 R86 灾难（lockWait=1,010ms）前的典型升级信号**。
- **R86 升级路径回顾**：R84(lockWait<5ms) → R85(lockWait~50ms) → R86(lockWait=1,010ms, 灾难)。**本轮 60.8ms 已到达 R85 水平。**

### `*.swift`=980ms 分析

- phase2=852ms（87%），lockWait=**60.8ms**（6%），其余=67ms。
- R100=2,887ms → R101=409ms → **R102=980ms**——波动但趋势恶化。
- **60.8ms lockWait 是本轮最显著的新信号**：
  - 之前 lockWait 最高 19.7ms（R100），本轮跳至 60.8ms。
  - FSWatcher 事件写入速率极高——单条查询执行期间有 60.8ms 被阻塞在锁等待上。
  - 如果事件源继续，lockWait 将持续增长直至触发 R86 式灾难。

### `中文测试`=567ms 分析

- phase2=509ms（90%），lockWait=14.8ms——linear-gcd 全表扫描。
- R101=206ms → **R102=567ms**——linear 路径再次受压。
- recordCount 已达 9.22M——linear 扫描工作量比 R99（6.31M）增加 46%。
- **37.65% tombstone 意味着 linear 扫描了超过三分之一的无效记录。**

### `.pdf`=484ms + `size:>100m`=482ms 接近 500ms

- `.pdf` R99=8,495ms → R100=840ms → R101=766ms → **R102=484ms**——持续恢复，但仍在危险区域。
- `size:>100m` R101=226ms → **R102=482ms**——全表扫描 9.22M 记录的成本显著上升。
- **两个查询均距 500ms 仅 16-18ms**——下一轮极可能突破。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=9,223,782，liveRecordCount=5,750,942（tombstone=**3,472,840**，**37.65%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=401  QUERYADV_GE500=61
FLUSH=1  COMPACTION=2  SKIPFLUSH=566
DIAG_TRIGRAM_SKIP=110
```

- QUERYADV 从 391 增至 401（+10）。
- QUERYADV_GE500 从 58 增至 61（**+3，即 `*.swift`=980ms，`中文测试`=567ms，`hello world`=529ms**）。
- SKIPFLUSH 从 510 增至 566（**+56，连续第 2 轮 +56——事件风暴恒定速率**）。
- FLUSH 从 0 增至 **1**——**历史首次出现非零 flush**。
- DIAG_TRIGRAM_SKIP 从 109 增至 110（+1）。

### SKIPFLUSH +56 连续恒定

| 轮次间隔 | SKIPFLUSH 增量 | 说明 |
|----------|------------:|------|
| R99→R100 | +12 | 稳态 |
| R100→R101 | **+56** | 事件风暴开始 |
| **R101→R102** | **+56** | **恒定——事件源持续** |

- 连续 2 轮精确 +56——FSWatcher 事件以恒定速率涌入，事件源是持续性活动（非一次性操作）。
- 可能来源：持续运行的构建系统、持续索引服务（Spotlight mdworker）、或后台同步服务。

### FLUSH=1 历史首次非零

- **连续 41 轮 FLUSH=0 后首次出现 FLUSH=1**。
- 可能原因：某个时刻 WAL pending changes 恰好累积到 100 条阈值。
- **但 1 次 flush 对 3.47M tombstone 毫无意义**——需要约 34,728 次 flush 才能清理全部 tombstone。
- 此事件确认 flush 机制并非完全失效——只是在高频事件流下极难触发。

## 6. 问题清单

### Critical：tombstone 突破 1GB——37.65%（3,472,840 条，~1,042MB）

- **单轮 +1,090,231 条——连续第 3 轮爆发式增长。**
- **从 R99 的 568K (8.98%) 到 R102 的 3,473K (37.65%)——3 轮内增长 6.1 倍。**
- **~1,042MB 额外内存占用——突破 1GB**。
  - R95 (150MB) → I/O 游走
  - R99 (170MB) → 全路径饱和
  - R100 (240MB) → lockWait 广泛
  - R101 (714MB) → 系统危机
  - **R102 (1,042MB) → 1GB+ 内存负担**
- recordCount 9.22M 中 3.47M 是 tombstone——**每次查询扫描 37.65% 无效记录**。
- 连续第 42 轮超 2%，首轮超过 37%。

### Critical：lockWait 升级至 60.8ms——R86 灾难预警

- **R101 最高 15.3ms → R102 最高 60.8ms——4 倍跳升。**
- **3 个查询 lockWait >40ms**——历史首次出现如此多的高 lockWait。
- **R86 升级路径对比**：
  - R84: lockWait <5ms
  - R85: lockWait ~50ms ← **R102 已到达此水平**
  - R86: lockWait=1,010ms, Max=2,731,551ms（灾难）
- **如果事件源持续，下一轮 lockWait 可能达到数百毫秒——触发 R86 式灾难。**

### Critical：I/O 压力扩散（3/16 >500ms + 2 个接近 500ms）

- `*.swift`=980ms，`中文测试`=567ms，`hello world`=529ms——3/16 >500ms。
- `.pdf`=484ms，`size:>100m`=482ms——距 500ms 仅 16-18ms。
- **实质上 5/16 查询处于危险区域。**

### Critical：SKIPFLUSH +56 连续恒定——事件风暴持续

- 连续 2 轮精确 +56——事件源是持续性活动。
- tombstone 增量从 +1,583K 降至 +1,090K（下降 31%），但仍远超正常水平（+12K）的 90 倍。

### High：FLUSH=1 杯水车薪

- 41 轮来首次触发 flush，但 1 次 flush 对 3.47M tombstone 完全无效。
- 确认了 flush 阈值机制在高频事件流下的极低触发概率。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（37.65%，~1GB——系统存活级别）

- **42 轮连续超标。** tombstone 3 轮内从 568K 增长到 3,473K，内存从 170MB 增至 1GB+。
- **1GB+ tombstone 是系统级负担**——占用大量页面缓存空间，挤压所有索引和数据结构。
- **compaction 是唯一出路**：释放 ~1GB 后系统可恢复正常运行。
- 修复方案：定期 compaction（tombstoneRatio > 0.02 时触发），或在 SKIPFLUSH 分支增加强制 compaction 路径。

### P0 紧急：修复 WAL flush 阈值机制

- 41 轮 FLUSH=0 后首次 FLUSH=1——确认机制极难触发。
- 方案：增加时间触发器（每 N 秒强制 flush），或基于 tombstoneRatio 触发。

### P0 紧急：lockWait 升级防护

- 60.8ms lockWait 已到达 R85 水平——下一步可能是 R86 式灾难（1,010ms）。
- 方案：FSWatcher 事件批量写入（减少锁获取次数），或查询/写入分离到不同锁。

### P0 紧急：`size:>100m` 全表扫描超时保护
### P0 紧急：short-query-cache GCD 隔离
### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. **lockWait 是否继续升级**——60.8ms 是否向 R86 的 1,010ms 方向发展。
2. tombstone 增量是否开始下降（事件源是否停止）。
3. `.pdf` 和 `size:>100m` 是否突破 500ms。

## 9. 结论

**tombstone 第三次爆发——突破 1GB 内存负担。** tombstone 从 2,383K (29.30%) 增至 **3,473K (37.65%，~1,042MB)**——**单轮 +1,090K 条**（连续第 3 轮爆发，3 轮累计从 568K 增长 6.1 倍至 3,473K）。`*.swift`=980ms，`中文测试`=567ms，`hello world`=529ms，3/16 >500ms，另有 `.pdf`=484ms 和 `size:>100m`=482ms 接近 500ms。**lockWait 显著恶化至 60.8ms**（R101=15.3ms 的 4 倍，3 个查询 >40ms）——**已到达 R85 水平，距 R86 灾难仅一步之遥**。15/16 查询有 lockWait。SKIPFLUSH 连续第 2 轮 +56（事件风暴恒定速率持续）。**FLUSH=1 历史首次非零**——但对 3.47M tombstone 杯水车薪。P50=203ms，Max=980ms。**系统运行在 ~1GB tombstone 负担下**——recordCount 9.22M 中 37.65% 为无效记录，linear 查询扫描效率降低近 40%。lockWait 60.8ms 是 R86 灾难的明确前兆——**如果事件源持续，下一轮极可能触发灾难级性能崩溃。**
