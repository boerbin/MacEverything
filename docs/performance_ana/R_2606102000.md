# MacEverything 性能分析报告 R_2606102000

## 1. 本轮范围

- 任务启动时间：2026-06-10 20:00
- 报告文件：`docs/performance_ana/R_2606102000.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9314`
  - `SEG_LINES=8372`
  - `LAST_LOG_TS=2026-06-10 18:17`

## 2. 执行摘要

**⭐ 完美 1-round recovery！0/16 >500ms，Max=162ms，lockWait=0。极佳 12/16。但 flush rewrite=3981ms（3.9 秒——新纪录！）。** `x`=2ms（缓存稳定，连续 5 轮）。Trigram 全面恢复：`.pdf` 452→39ms（↓91%），`hello world` 87→35ms（↓60%）。全表扫描 1316→441ms（↓66%）。`中文测试`=162ms（↓78% from R265=731ms——**1-round recovery 第 17 次确认，17/17=100%**）。FLUSH 50→55（+5）。tombstone=106,674（2.03%——增速持续偏高）。

**⚠️ 日志末行：flush rewrite=3981ms（超越 R265=2617ms——新系列纪录！）。flush 重写时间在加速恶化。**

| 指标 | R265 | R266 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 2/16 | **0/16** | **↓2（完美恢复！）** |
| Max | 731ms | **162ms** | **↓78%** |
| P50 | 87ms | **27.8ms** | **↓68%** |
| lockWait max | 1.5ms | **0ms** | 恢复为 0 |
| 极佳比例(<50ms) | 2/16 | **12/16** | **↑10** |
| tombstone | 90,255 (1.72%) | **106,674 (2.03%)** | ↑（增速持续偏高） |
| recordCount | 5,336,463 | **5,352,582** | +16.1K（连续 3 轮偏高！） |
| liveRecordCount | 5,246,208 | **5,245,908** | -300（轻微净删除） |
| FLUSH 累计 | 50 | **55** | +5 |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 8 | **8** | 不变 |
| QUERYADV_GE500 | 78 | **78** | 不变 |

### ⭐ 1-round recovery 第 17 次确认

- **R265（2/16 >500ms, 极佳 2/16）→ R266（0/16 >500ms, 极佳 12/16）。**
- **17/17 = 100% 成功率——铁律级别的恢复模式。**
- **完整记录：R176→R177, R179→R180, R181→R182, R190→R191, R194→R195, R199→R200, R208→R209, R215→R216, R221→R222, R227→R228, R232→R233, R244→R245, R252→R254(3轮), R255→R256, R264→R265(path), R265→R266。**

### ⚠️⚠️ flush rewrite=3981ms——新系列纪录！

**日志末行：**
```
[2026-06-10 18:17:31.158] Flushed v6 flat index, lastEventId=339438535,
liveRecords=5245910 | timing: walSwap=0ms rewrite=3981ms sqcache=2ms total=3984ms
```

| 轮次 | flush rewrite | 说明 |
|------|------------:|------|
| R265 | 2617ms | 首次捕获 |
| **R266** | **3981ms** | **新纪录！↑52%** |

- **flush rewrite 从 2617ms → 3981ms（↑52%——加速恶化）。**
- **可能原因：**
  1. **recordCount +16.1K（连续 3 轮偏高）——索引体积持续增长。**
  2. **tombstone=106,674（2.03%）——虽然比例低，但绝对数量增加了索引文件大小。**
  3. **磁盘 I/O 带宽饱和——其他系统活动竞争磁盘。**
- **3.9 秒 flush = 3.9 秒写锁持有时间——期间所有查询被阻塞。**
- **P0 COW flush 改造已从"建议"升级为"紧急"——flush 时间在逐轮恶化。**

### Trigram 路径——全面恢复

| Query | R265 | R266 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 452ms | **39ms** | **↓91%** | 极佳 |
| `hello world` | 87ms | **35ms** | ↓60% | 极佳 |
| `*.swift` | 80ms | **24ms** | ↓70% | 极佳 |
| `main.cpp` | 14ms | **6ms** | ↓57% | 极佳 |
| `abc def...` | 112ms | **28ms** | ↓75% | 极佳 |
| `README` | 11ms | **9ms** | ↓18% | 极佳 |
| `test` | 53ms | **15ms** | ↓72% | 极佳 |
| `MacEverything` | 133ms | **10ms** | ↓92% | 极佳 |
| Trigram phase2均值 | ~89ms | **~17ms** | **↓81%** |

- **全部 8/8 trigram 极佳——从 R265 的全面恶化中完美恢复。**
- **phase2 均值 89→17ms（↓81%）——page cache 完全预热。**
- **`.pdf` 452→39ms（↓91%——最大幅度恢复）。**

### 全表扫描——大幅恢复

| Query | R265 | R266 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 294ms | **151ms** | ↓49% |
| `path:/Users` | 291ms | **128ms** | ↓56% |
| `中文测试` | 731ms | **162ms** | **↓78%** |
| **总和** | **1316ms** | **441ms** | **↓66%** |

- **全表扫描 1316→441ms（↓66%——大幅恢复）。**
- **`中文测试` 731→162ms（↓78%——完美 1-round recovery）。**
- **稳态区间：400-600ms 确认（本轮 441ms 处于区间低端）。**

### 零和总量

| 类别 | R265 | R266 | 变化 |
|------|-----:|-----:|------|
| Trigram 总和 | ~933ms | ~167ms | **-766ms** |
| 全表扫描总和 | 1316ms | 441ms | -875ms |
| **Battery 总和** | ~2603ms | ~715ms | **↓73%** |

- **Battery 总和 2603→715ms（↓73%）——从 I/O 风暴完美恢复。**
- **非零和——两者同时大幅恢复（page cache 全面预热）。**

### recordCount 持续偏高——tombstone 增速加快

| 轮次 | recordCount Δ | tombstone | tombstone% |
|------|------------:|----------:|---------:|
| R262 | +7.2K | 49,554 | 0.94% |
| R263 | +8.9K | 58,042 | 1.10% |
| R264 | +15.4K | 72,277 | 1.37% |
| R265 | +18.7K | 90,255 | 1.72% |
| **R266** | **+16.1K** | **106,674** | **2.03%** |

- **连续 3 轮 recordCount +15K 以上——文件系统活动异常活跃期。**
- **tombstone 增速：(106,674-58,042)/4轮 ≈ 12,158/轮。**
- **liveRecordCount -300（净删除）——新 records 主要是路径更新（旧版本变 tombstone）。**
- **按当前增速，tombstone 达到 25% 阈值需要 ~100 轮——短期无 compaction 风险。**
- **但 tombstone 增长会增大索引文件 → 增加 flush rewrite 时间 → 恶性循环。**

### 辅助路径

| Query | R265 | R266 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 137ms | **2ms** | ↓99%（恢复） | 极佳 |
| `ext:cpp` | 45ms | **6ms** | ↓87% | 极佳 |
| `size:>100m` | 58ms | **29ms** | ↓50% | 极佳 |
| `dm:today` | 104ms | **72ms** | ↓31% | 正常 |
| `verylongquery...` | 2ms | **0.2ms** | ↓90% | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1.5 | 0.0 | short-query-cache | 0 | 0 | 极佳（稳定） |
| `.pdf` | 5 | 39.1 | 36.7 | advanced-trigram | 0 | 9788 | 极佳（恢复！） |
| `main.cpp` | 5 | 5.8 | 1.3 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 23.9 | 21.6 | advanced-trigram | 0 | 9405 | 极佳 |
| `ext:cpp` | 5 | 5.6 | 5.1 | advanced-ext-index | 0 | 3611 | 极佳 |
| `size:>100m` | 5 | 29.2 | 28.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 34.6 | 32.2 | advanced-trigram | 0 | 3930 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 27.8 | 27.4 | advanced-trigram | 0 | 14457 | 极佳 |
| `README` | 5 | 9.1 | 6.0 | advanced-trigram | 0 | 17432 | 极佳 |
| `test` | 5 | 15.2 | 10.2 | advanced-trigram | 0 | 117727 | 极佳 |
| `MacEverything` | 5 | 9.8 | 1.4 | advanced-trigram | 0 | 6942 | 极佳 |
| `dm:today` | 5 | 72.4 | 71.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 150.8 | 142.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 128.4 | 123.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 162.0 | 159.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 27.8ms，P90 = 150.8ms，Max = 162.0ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R265 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **12/16** | 0.2-39ms | R265=2/16（**↑10**） |
| **正常**（50-500ms） | 4/16 | 72-162ms | R265=12/16（↓8） |
| **超标**（>500ms） | **0/16** | — | R265=2/16（**↓2**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,352,582，liveRecordCount=5,245,908（tombstone=**106,674**，**2.03%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=696  QUERYADV_GE500=78（不变）
FLUSH=55（+5）  COMPACTION=8  SKIPFLUSH=7266
```

**关键日志（flush rewrite 新纪录）：**
```
Flushed v6 flat index | rewrite=3981ms sqcache=2ms total=3984ms
```

## 6. 问题清单

### P0（紧急）：flush rewrite 加速恶化

- **R265=2617ms → R266=3981ms（↑52%——新纪录）。**
- **3.9 秒写锁——期间所有查询阻塞。**
- **趋势在恶化：recordCount 持续增长 → 索引体积增大 → flush 时间增加。**

### P0：compaction 触发机制缺陷（根因未修复）

### P0：I/O 优先级保护

### P2：tombstone 增速加快（2.03%，~12K/轮）

## 7. 优化建议

### P0（紧急！）：flush rewrite 改为 COW 方式

- **flush rewrite 从 2617ms → 3981ms——趋势在恶化。**
- **当前实现：完整重写 5.2M+ 条记录索引。**
- **COW 模式：仅写变更页（增量写入），预期 <100ms。**
- **效果：消除 3.9 秒写锁 + 消除 page cache 冲刷。**

### P0：修复 compaction 触发机制

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. flush rewrite 时间趋势（2617→3981→?）。
2. recordCount 增速是否回归正常（连续 3 轮 +15K 以上）。
3. tombstone 增速趋势（2.03%，按 ~12K/轮增长）。

## 9. 结论

**⭐ 完美 1-round recovery（第 17 次，17/17=100%）——0/16 >500ms，Max=162ms，lockWait=0，极佳 12/16。** `.pdf` 452→39ms（↓91%），`中文测试` 731→162ms（↓78%）。Battery 总和 2603→715ms（↓73%）。`x`=2ms（缓存稳定，连续 5 轮）。

**⚠️ flush rewrite=3981ms（新纪录！↑52% from R265=2617ms）——趋势在加速恶化。** recordCount 连续 3 轮 +15K 以上 → 索引体积增大 → flush 时间增加 → 恶性循环。tombstone=106,674（2.03%）。**P0 COW flush 改造从"建议"升级为"紧急"——flush 重写时间正在以 ~50%/轮的速度恶化。**
