# MacEverything 性能分析报告 R_2606051311

## 1. 本轮范围

- 任务启动时间：2026-06-05 13:11
- 报告文件：`docs/performance_ana/R_2606051311.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=25825`
  - `SESSION_START_TS=2026-06-05 11:47:42.275`
  - `TOTAL_LINES=25897`
  - `SEG_LINES=73`
  - `LAST_LOG_TS=2026-06-05 13:11`

## 2. 执行摘要

**全面恢复至理想水平——0/16 >500ms，Max=259ms，P50=28ms，lockWait=0。flush rewrite 完全恢复至 2.2-3.8s（正常水平）。** 这是崩溃重启后的第二轮稳态监控，系统性能已超越崩溃前多数轮次。

**recordCount 5.22M（崩溃前 6.82M 的 77%）——更紧凑的索引带来全面性能提升。**

### 关键指标对比

| 指标 | R139 上轮 | R140 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（极佳） |
| Max | 337ms | **259ms** | **↓ 23% 改善** |
| P50 | 29ms | **28ms** | 持平（极佳） |
| lockWait max | 14ms | **0ms** | **↓ 完全消除** |
| tombstone | 97,125 (1.88%) | **141,536 (2.71%)** | ↑ 正常增长 |
| tombstone 增速 | ~783/min | **~1,480/min** | ↑ 白天正常 |
| recordCount | 5,169,595 | **5,215,400** | +46K（正常） |
| liveRecordCount | 5,072,470 | **5,073,864** | +1.4K |
| FLUSH 累计 | 10 | **16** | +6（正常） |
| SKIPFLUSH | 0 | **0** | 持平 |
| flush rewrite | 3.0-7.3s | **2.2-3.8s** | **↓ 完全恢复** |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 0 | **0** | 持平（连续 3 轮 0 慢查询） |

### Flush rewrite 完全恢复

```text
最近 5 次：2369ms  2643ms  2227ms  2791ms  3795ms
中位数：~2.6s
```

- **从 R138 初期的 3-11s → R139 的 3-7s → R140 的 2.2-3.8s——完全恢复至正常水平。**
- **2,227ms 最低值接近历史最佳（R133=2,052ms）。**
- **3,795ms 峰值在正常范围内——不会引发明显 lockWait。**
- **恢复原因**：新会话稳定 → 每次 flush 增量回归正常 → rewrite 数据量正常。

### Tombstone 增速——白天正常水平

- R139 (12:41): 97,125 (1.88%)
- R140 (13:11): 141,536 (2.71%)
- **增量 +44,411 / 30 min = ~1,480/min**
- **从 R139 的 783/min 上升至 1,480/min——白天正常活动水平。**
- **远低于崩溃前的事件风暴（R135=10,449/min, R137=8,942/min）。**
- **按当前增速达到 25%**：还需 ~1,146,000 / 1,480 ≈ **774 分钟 ≈ ~12.9 小时（~明天凌晨 2:00）。**

### 全表扫描性能——page cache 持续预热

| Query | R138 | R139 | R140 | 趋势 |
|-------|-----:|-----:|-----:|------|
| `content:import` | 389ms | 258ms | **193ms** | **↓ 持续改善** |
| `path:/Users` | 210ms | 241ms | **259ms** | ↑ 零和波动 |
| `中文测试` | 284ms | 337ms | **225ms** | **↓ 大幅改善** |
| **全表扫描总和** | **883ms** | **836ms** | **676ms** | **↓ 23% 改善** |

- **全表扫描总和 676ms（R138=883ms，↓23%）**——page cache 逐步预热。
- **`content:import` 从 389ms 持续降至 193ms**——3 轮改善 50%。
- **`中文测试` 从 337ms 降至 225ms**——page cache 覆盖增加。
- **`path:/Users`=259ms 微升——零和效应但绝对值正常。**

### 与历史最佳（R133, 旧会话）对比

| 指标 | R133（历史最佳） | R140 本轮 | 对比 |
|------|--------:|--------:|------|
| Max | 67ms | **259ms** | R133 更优（page cache 极限） |
| P50 | 13ms | **28ms** | R133 更优 |
| content:import | 67ms | **193ms** | R133 更优（3x） |
| path:/Users | 56ms | **259ms** | R133 更优（5x） |
| recordCount | 6,201,772 | **5,215,400** | **R140 更少 16%** |
| lockWait | 0 | **0** | 持平 |

- **R133 的 page cache 极限性能（全部 <67ms）尚未达到——但 R140 正在快速逼近。**
- **R140 的 recordCount 比 R133 少 16%——理论上全表扫描应更快，差距来自 page cache 预热不足。**
- **预计 2-3 轮后可接近 R133 水平。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 2 | 1.0 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 26.4 | 13.9 | advanced-trigram | 0 | 9878 | 极佳 |
| `main.cpp` | 5 | 7.4 | 2.2 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 21.1 | 16.1 | advanced-trigram | 0 | 9325 | 极佳 |
| `ext:cpp` | 5 | 9.6 | 8.5 | advanced-ext-index | 0 | 3569 | 极佳 |
| `size:>100m` | 5 | 28.3 | 28.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 40.4 | 34.1 | advanced-trigram | 0 | 3620 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 58.2 | 57.3 | advanced-trigram | 0 | 14126 | 正常 |
| `README` | 5 | 16.1 | 10.6 | advanced-trigram | 0 | 16887 | 极佳 |
| `test` | 5 | 46.6 | 24.5 | advanced-trigram | 0 | 116157 | 极佳 |
| `MacEverything` | 5 | 22.2 | 1.5 | advanced-trigram | 0 | 6796 | 极佳 |
| `dm:today` | 5 | 52.3 | 52.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 192.8 | 180.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（↓25% vs R139） |
| `path:/Users` | 5 | 258.5 | 254.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 224.5 | 219.1 | advanced-linear-gcd | 0 | 0 | 正常（↓33% vs R139） |
| `verylongquery...` | 0 | 0.7 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 27.5ms，P90 = 224.5ms，Max = 258.5ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R139 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 11/16 | 1-47ms | R139=11/16（持平） |
| **正常**（50-500ms） | 5/16 | 52-259ms | R139=5/16（持平） |
| **超标**（>500ms） | 0/16 | — | R139=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,215,400，liveRecordCount=5,073,864（tombstone=**141,536**，**2.71%**）；
- 进程正常运行（新会话，运行约 84 分钟）。

## 5. 最新日志段分析

```text
WARN=1（FSEvents replay failed — 启动时）
ERROR=0
QUERYADV=9  QUERYADV_GE500=0（连续 3 轮 0 慢查询）
FLUSH=16  COMPACTION=2  SKIPFLUSH=0
```

- QUERYADV=9（+3 本轮）。
- **QUERYADV_GE500=0**——连续 3 轮（R138-R140）0 个慢查询，本会话至今 0 个。
- **FLUSH=16（+6）**——频率稳定 ~120/小时。
- COMPACTION=2（不变——auto-compaction 初始化检查，无触发）。
- SKIPFLUSH=0。

## 6. 问题清单

### 无活跃问题——系统处于理想运行状态

- 0/16 >500ms，lockWait=0，P50=28ms。
- flush rewrite 完全恢复至 2.2-3.8s。
- tombstone 2.71%——远低于 25% 阈值。
- **连续 3 轮 0 个 >500ms 查询——新会话至今 0 个慢查询。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（R138 发现，不变）

- 短期无触发风险（下次 compaction ~12.9 小时后），但根因需要排查。

### P0：flush rewrite 改为 COW 方式（持续建议）

- 当前 flush rewrite 2.2-3.8s 且 lockWait=0——系统运行良好。
- **但架构风险仍在——下一次 I/O 风暴或事件风暴时 lockWait 将重现。**
- **COW flush 是消除系统性风险的根本方案。**

### P1：降低 compaction 阈值至 15%（持续建议）

- 25% 阈值使 tombstone 堆积周期过长 → flush rewrite 退化窗口过大。

## 8. 下一轮重点

1. 全表扫描性能是否继续向 R133（67ms）水平逼近。
2. flush rewrite 是否稳定维持 <3s。
3. tombstone 增速趋势。
4. 系统稳定性。

## 9. 结论

**全面恢复至理想水平——0/16 >500ms，Max=259ms，P50=28ms，lockWait=0。** flush rewrite 完全恢复至 2.2-3.8s（中位数 2.6s），连续 3 轮 0 个慢查询。全表扫描持续改善：content:import 从 389ms(R138)→193ms，中文测试从 337ms→225ms——page cache 逐步预热中。

**tombstone 2.71%（141K），增速 1,480/min（白天正常），下次 25% compaction 约 12.9 小时后（~明天凌晨 2:00）。** recordCount 5.22M（崩溃前 6.82M 的 77%）——更紧凑的索引带来全面提速。

**系统处于理想运行状态——无活跃问题。** P0 建议（compaction 崩溃根因 + COW flush）待实施但短期无触发风险。全表扫描 page cache 预计 2-3 轮后接近 R133（67ms）的历史极限水平。
