# MacEverything 性能分析报告 R_2606131548

## 1. 本轮范围

- 任务启动时间：2026-06-13 15:48
- 报告文件：`docs/performance_ana/R_2606131548.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=12287`
  - `SESSION_START_TS=2026-06-13 12:03:04.944`
  - `TOTAL_LINES=12606`
  - `SEG_LINES=320`
  - `LAST_LOG_TS=2026-06-13 15:48`

## 2. 执行摘要

**本轮 16-query battery 中 0/16 >500ms，Max 从 344.4ms 小幅回落到 322.4ms，P90 从约 286.6ms 大幅回落到约 135.2ms。** 最高项为 `中文测试`=322.4ms，其次是 `path:/Users`=178.8ms、`content:import`=135.2ms、`dm:today`=72.8ms、`size:>100m`=57.0ms。P50≈24.8ms，所有 lockWait 均为 0ms。

相比 15:17，本轮前台明显恢复：P50 从约 30.2ms 降至约 24.8ms，P90 从约 286.6ms 降至约 135.2ms，scan/filter 路径总和从 1069.7ms 降至 766.2ms。需要注意的是，前台恢复并不代表根因解除：recordCount=6,236,436，liveRecordCount=5,312,133，tombstone=925,303（14.84%），较上一轮增加 7,169；最新 session `FLUSH=44`，较上一轮 +6，`SKIPFLUSH=0`，session 最大 flush total 仍为 6164ms，并新增 15:33 的 3694ms flush。

| 指标 | R_2606131517 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 344.4ms | **322.4ms** | ↓6% |
| P50 | ≈30.2ms | **≈24.8ms** | ↓18% |
| P90 | ≈286.6ms | **≈135.2ms** | ↓53% |
| lockWait max | 0.0ms | **0.0ms** | 持平 |
| 极佳比例(<50ms) | 10/16 | **10/16** | 持平 |
| 扫描/过滤路径总和 | 1069.7ms | **766.2ms** | ↓28% |
| recordCount | 6,229,711 | **6,236,436** | +6,725 |
| liveRecordCount | 5,311,577 | **5,312,133** | +556 |
| tombstone | 918,134 (14.74%) | **925,303 (14.84%)** | +7,169 |
| 最新 session FLUSH | 38 | **44** | +6 |
| 最新 session QUERYADV_GE500 | 0 | **0** | 持平 |
| 最新 flush total | 2733ms | **2741ms** | 基本持平 |
| session 最大 flush total | 6164ms | **6164ms** | 持平 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 2.2 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 23.9 | 19.4 | advanced-trigram | 0.0 | 10157 | 极佳 |
| `main.cpp` | 5 | 6.1 | 3.8 | advanced-trigram | 0.0 | 103 | 极佳 |
| `*.swift` | 5 | 25.7 | 24.6 | advanced-trigram | 0.0 | 9622 | 极佳 |
| `ext:cpp` | 5 | 13.7 | 13.3 | advanced-ext-index | 0.0 | 3891 | 极佳 |
| `size:>100m` | 5 | 57.0 | 56.6 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常，略高于 50ms |
| `hello world` | 5 | 52.1 | 51.5 | advanced-trigram | 0.0 | 3932 | 正常，刚过 50ms |
| `abc def ghi jkl mno pqr` | 0 | 49.1 | 48.5 | advanced-trigram | 0.0 | 14619 | 极佳，贴近 50ms |
| `README` | 5 | 9.5 | 8.6 | advanced-trigram | 0.0 | 17586 | 极佳 |
| `test` | 5 | 17.1 | 14.0 | advanced-trigram | 0.0 | 118021 | 极佳 |
| `MacEverything` | 5 | 7.7 | 1.5 | advanced-trigram | 0.0 | 7038 | 极佳 |
| `dm:today` | 5 | 72.8 | 72.6 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 135.2 | 125.5 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常高位，较上一轮回落 |
| `path:/Users` | 5 | 178.8 | 172.2 | advanced-linear-gcd | 0.0 | 0 | 正常高位，较上一轮明显回落 |
| `中文测试` | 0 | 322.4 | 321.7 | advanced-linear-gcd | 0.0 | 0 | 正常高位，本轮最高 |
| `verylongquery...` | 0 | 2.3 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈24.8ms，P90≈135.2ms，Max=322.4ms。0/16 查询 >500ms。lockWait max=0.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **10/16** | 2.2-49.1ms | short-query-cache、轻量 trigram、ext-index 整体健康 |
| 正常（50-500ms） | **6/16** | 52.1-322.4ms | pure-filter 与 linear-gcd 仍构成主要尾部 |
| 超标（>500ms） | **0/16** | — | 本轮无正式超标查询 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,236,436；
- liveRecordCount=5,312,133；
- tombstone=925,303（**14.84%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=12287
SESSION_START_TS=2026-06-13 12:03:04.944
TOTAL_LINES=12606
SEG_LINES=320
QUERYADV=33
QUERYADV_GE500=0
FLUSH=44
SKIPFLUSH=0
COMPACTION=2
LAST_LINE=[2026-06-13 15:48:07.904] [INFO ] [SearchEngine] QueryAdvanced "中文测试" total=322ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6236436
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 0，本轮没有日志级慢查询。`中文测试` 的日志 total=322ms，与 query battery 的 322.4ms 对齐；`path:/Users` 和 `content:import` 相比 15:17 回落明显，但仍处于 100ms+ 区间。

```text
[2026-06-13 15:48:07.904] QueryAdvanced "中文测试" total=322ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6236436
```

### flush 情况

最新 session 内 `FLUSH=44`，rewrite 范围 **2470ms - 4809ms**，total 最大 **6164ms**。`SKIPFLUSH=0`，说明 session 内完整 flush 从上一轮的 38 次继续累积到 44 次，但没有任何可见 backoff。

最新 flush：

```text
[2026-06-13 15:43:23.920] Flushed v6 flat index, lastEventId=362002033,
liveRecords=5312100 | timing: walSwap=0ms rewrite=2739ms total=2741ms
```

Top flushes：

```text
[2026-06-13 12:08:25.921] rewrite=4809ms total=4812ms
[2026-06-13 13:48:25.498] rewrite=4395ms total=4396ms
[2026-06-13 12:38:27.299] rewrite=3809ms total=6164ms
[2026-06-13 15:33:24.838] rewrite=3691ms total=3694ms
[2026-06-13 13:43:24.572] rewrite=3471ms total=3472ms
```

## 6. 问题清单

### P0：tombstone 已突破 925k 且继续上行

- 证据：tombstone=925,303（14.84%），较上一轮增加 7,169。
- 影响：无效记录比例继续升高，会放大扫描成本、索引重写成本与 compaction 压力。
- 判断：本轮前台延迟改善与 tombstone 继续恶化同时发生，说明前台波动更可能来自缓存/系统状态，而不是根因解除。

### P0：new session 内 flush 已累积到 44 次且无退避

- 证据：`FLUSH=44`，较上一轮 +6；`SKIPFLUSH=0`；session 最大 flush total=6164ms，并新增 15:33 的 3694ms flush。
- 影响：完整重写持续制造 I/O 与 page-cache 干扰，容易让 linear-gcd、pure-filter、trigram 在不同采样点出现尾部抬升。
- 判断：flush 频率继续按固定节奏累积，未看到对 3-6s rewrite 的保护机制。

### P0：linear-gcd 仍是最高前台尾部

- 证据：`中文测试`=322.4ms，phase2=321.7ms；`path:/Users`=178.8ms，phase2=172.2ms。
- 影响：`path:/Users` 已回落到 200ms 以下，但中文无结果查询仍超过 300ms，用户可感知。
- 判断：lockWait=0ms，主要瓶颈仍在 phase2 扫描阶段。

### P1：pure-filter 回落但仍构成中位以上成本

- 证据：`content:import`=135.2ms，`dm:today`=72.8ms，`size:>100m`=57.0ms。
- 影响：相比上一轮 296.4ms 有明显改善，但 pure-filter 仍贡献 50-150ms 区间的稳定成本。

### P1：trigram 多数健康但贴近 50ms 边界

- 证据：`hello world`=52.1ms，`abc def ghi jkl mno pqr`=49.1ms，`.pdf`=23.9ms。
- 影响：trigram 本轮整体健康，但多词和大候选查询仍接近 50ms 边界。

## 7. 优化建议

### P0：立即提前或分段 compaction

- 以 tombstone 率 14%+ 或绝对 tombstone 900k+ 作为明确触发信号，不再等待更高阈值。
- 结合 segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 优先处理 tombstone 密集 segment，避免 scan/filter 路径继续扫描大量无效记录。
- 验收：tombstone 率从 14.84% 回落，`中文测试`、`content:import`、`path:/Users` 稳定 <200ms。

### P0：flush 改为增量/COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页或持锁窗口。
- 验收：常规 flush total <200ms，新 session 不再出现 3-6s 级完整 rewrite。

### P0：增加 flush I/O 压力退避和前台保护

- 当最近 flush 超过阈值时，提高下一次 flush 间隔、降低 batch 频率，或前台查询活跃时推迟大 flush。
- 持久化线程使用低 QoS，并评估大顺序写的 `F_NOCACHE`/同类策略，减少对搜索热页的冲刷。
- 验收：`SKIPFLUSH` 不再长期为 0，3-6s flush 后不再出现 300ms+ scan/filter 尾部。

### P0：为 advanced-linear-gcd 与 pure-filter 增加轻量剪枝

- 对中文、路径、content/filter 类查询记录扫描记录数、tombstone 跳过数和 segment 分布。
- 评估轻量字符 n-gram、路径分段索引、预过滤位图或 segment-level skip list，以减少无结果或低选择性查询的全量扫描。
- 要求保持内存增长可控，优先使用压缩或按需结构，不引入大常驻索引。
- 验收：`中文测试` 不再稳定落在 300ms+ 区间，`content:import` 与 `path:/Users` 稳定低于 200ms。

### P1：细分 scan/filter 与 top-level timing

- 对 pure-filter 和 linear-gcd 记录扫描记录数、跳过 tombstone 数、命中 live 数、segment 数、metadata 加载耗时和结果组装耗时。
- 对所有路径增加 queueWait、phase2、candidate materialization、path/file metadata load、sort/rank、HTTP serialization/write 等阶段耗时。
- 验收：`中文测试` 的 322.4ms 能归因到具体阶段，而不是只看到 phase2 大黑盒。

### P1：细分 trigram/postings 路径

- 对 `advanced-trigram` 记录 postings 读取、候选交并、候选评分、路径加载、结果组装、page-cache miss 近似指标。
- 重点解释 `hello world` 与 `abc def ghi jkl mno pqr` 在 50ms 附近的成本来源。
- 验收：trigram 查询在 flush 后仍稳定 <50ms，或至少能解释每个阶段的成本。

## 8. 下一轮重点

1. tombstone 是否继续从 925,303 上行并接近 930k。
2. `中文测试` 是否继续维持 300ms+，或重新接近 400-500ms。
3. `path:/Users` 是否能稳定保持 <200ms。
4. `content:import` 是否能继续保持在 150ms 以下。
5. scan/filter 路径总和是否继续低于 800ms，还是回到 1000ms+。
6. 新 session 中 `QUERYADV_GE500` 是否开始累积。
7. 新 session flush 是否继续出现 4-6s outlier。
8. `SKIPFLUSH` 是否仍为 0。

## 9. 结论

**本轮没有 >500ms 查询，Max=322.4ms，P50≈24.8ms，P90≈135.2ms，前台相比 15:17 明显恢复。** `path:/Users` 和 `content:import` 均明显回落，但 `中文测试` 仍是 300ms+ 的 linear-gcd 尾部，主要耗时仍在 phase2 扫描。

**后台根因继续恶化：tombstone=925,303（14.84%），新 session `FLUSH=44`，最大 flush total=6164ms，且 `SKIPFLUSH=0`。** 下一步仍应优先推进提前/分段 compaction、增量/COW flush、flush 前台保护/退避、advanced-linear-gcd/pure-filter 剪枝，以及 scan/filter、trigram 与 top-level timing 拆分。
