# MacEverything 性能分析报告 R_2606101630

## 1. 本轮范围

- 任务启动时间：2026-06-10 16:30
- 报告文件：`docs/performance_ana/R_2606101630.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9074`
  - `SEG_LINES=8132`
  - `LAST_LOG_TS=2026-06-10 14:47`

## 2. 执行摘要

**Post-compaction 持续恢复——全面收敛中。** 0/16 >500ms，Max=260ms，P50=12.3ms，lockWait=0（连续 2 轮）。**极佳 12/16（与 R258 持平）。** Trigram phase2 均值 ~3.3ms（与 R258=2.7ms 持平——极致）。`x` 仍走 advanced-linear-gcd（260ms——**缓存第 3 轮未重建**）。全表扫描总和 542ms（↓14% from R258=627ms——持续恢复）。**FLUSH 从 9→15（+6！连续 2 轮 +6）。** tombstone=19,217（0.37%——健康）。

| 指标 | R258 | R259 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 297ms | **260ms** | ↓12% |
| P50 | 21ms | **12.3ms** | ↓41% |
| lockWait max | 0ms | **0ms** | 完美（连续 2 轮） |
| 极佳比例(<50ms) | 12/16 | **12/16** | 持平 |
| tombstone | 11,334 (0.22%) | **19,217 (0.37%)** | ↑（正常增长） |
| recordCount | 5,252,092 | **5,260,815** | +8.7K（正常） |
| liveRecordCount | 5,240,758 | **5,241,598** | +840（正常） |
| FLUSH 累计 | 9 | **15** | **+6（连续 2 轮 +6！）** |
| SKIPFLUSH | 7266 | **7266** | +0（flush 占用所有 tick） |
| COMPACTION | 4 | **4** | 不变 |
| QUERYADV_GE500 | 75 | **75** | 不变 |

### `x` 查询——第 3 轮仍未恢复 short-query-cache

| 轮次 | `x` 路径 | `x` 时间 | 说明 |
|------|---------|--------:|------|
| R256 | short-query-cache | 1ms | compaction 前 |
| R257 | advanced-linear-gcd | 587ms | 缓存失效 |
| R258 | advanced-linear-gcd | 297ms | 第 2 轮未重建 |
| **R259** | **advanced-linear-gcd** | **260ms** | **第 3 轮未重建** |

- **连续 3 轮 `x` 走 linear-gcd——确认 short-query-cache 重建机制有延迟或缺陷。**
- **260ms（↓13% from 297ms）——page cache 预热逐步改善全表扫描速度。**
- **可能原因：**
  1. **cache 重建依赖特定查询频率或累计次数——battery 每轮只查 `x` 一次。**
  2. **cache 在 flush 时被重新失效——FLUSH +6 每轮导致缓存反复清空。**
  3. **cache 重建需要后台定时器——但进程被 App Nap 节流。**
- **推荐：检查 short-query-cache 的失效与重建逻辑，特别是 flush 后是否需要重新填充。**

### FLUSH 连续 +6 分析

- **R257→R258：+6，R258→R259：+6——连续 2 轮每个 30s tick 都 flush。**
- **SKIPFLUSH +0——没有跳过任何 tick。**
- **tombstone 增速：11,334→19,217（+7,883 / ~30min = ~263/min）。**
- **recordCount +8.7K（正常范围）。**
- **持续 flush 是因为 FSEvents 产生的 WAL entries 持续超过 100 阈值。**
- **这是 compaction 后的正常行为——之前 57 轮 entries 从未超过 100（SKIPFLUSH），现在因为索引紧凑了，每次 FSEvents 产生更多有效 WAL entries。**
- **但持续 flush 也意味着持续的写锁风险——再次强调 COW flush 的必要性。**

### Trigram 路径——极致且稳定

| Query | R258 | R259 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 4ms | **3ms** | ↓25% | 极佳 |
| `hello world` | 15ms | **6ms** | ↓60% | 极佳 |
| `*.swift` | 3ms | **2ms** | ↓33% | 极佳 |
| `main.cpp` | 12ms | **5ms** | ↓58% | 极佳 |
| `abc def...` | 19ms | **4ms** | ↓79% | 极佳 |
| `README` | 27ms | **12ms** | ↓56% | 极佳 |
| `test` | 21ms | **18ms** | ↓14% | 极佳 |
| `MacEverything` | 25ms | **10ms** | ↓60% | 极佳 |
| Trigram phase2均值 | ~2.7ms | **~3.3ms** | 持平 |

- **全部 8/8 trigram 极佳——连续 3 轮（R257 去 lockWait 后也是）。**
- **queryTime 收敛：大部分 trigram 降至 <20ms（R258 有几个 20-27ms 的 overhead 已消失）。**

### 全表扫描——持续恢复

| Query | R258 | R259 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 158ms | **152ms** | ↓4% |
| `path:/Users` | 244ms | **174ms** | **↓29%** |
| `中文测试` | 225ms | **216ms** | ↓4% |
| **总和** | **627ms** | **542ms** | **↓14%** |

- **全表扫描 627→542ms（↓14%）——持续恢复中。**
- **`path:/Users`=174ms（↓29%——路径数组 page cache 恢复）。**
- **预计 1-2 轮后接近 200-300ms 稳态。**

### 辅助路径

| Query | R258 | R259 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 297ms | **260ms** | ↓12%（仍 linear-gcd） | 正常 |
| `ext:cpp` | 1ms | **1ms** | 持平 | 极佳 |
| `size:>100m` | 21ms | **17ms** | ↓19% | 极佳 |
| `dm:today` | 101ms | **59ms** | ↓42% | 正常 |
| `verylongquery...` | 0.2ms | **0.7ms** | ↑250%（微小波动） | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 259.7 | 253.0 | advanced-linear-gcd | 0 | 0 | 正常（缓存未重建） |
| `.pdf` | 5 | 3.4 | 2.4 | advanced-trigram | 0 | 9783 | 极佳 |
| `main.cpp` | 5 | 5.2 | 0.0 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 2.4 | 0.8 | advanced-trigram | 0 | 9401 | 极佳 |
| `ext:cpp` | 5 | 0.6 | 0.3 | advanced-ext-index | 0 | 3612 | 极佳 |
| `size:>100m` | 5 | 16.7 | 16.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 5.8 | 3.7 | advanced-trigram | 0 | 3929 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 4.0 | 3.6 | advanced-trigram | 0 | 14444 | 极佳 |
| `README` | 5 | 12.3 | 6.0 | advanced-trigram | 0 | 17428 | 极佳 |
| `test` | 5 | 17.8 | 9.2 | advanced-trigram | 0 | 117720 | 极佳 |
| `MacEverything` | 5 | 9.5 | 0.9 | advanced-trigram | 0 | 6921 | 极佳 |
| `dm:today` | 5 | 58.8 | 58.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 151.9 | 142.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 173.8 | 170.3 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 216.0 | 214.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.7 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 12.3ms，P90 = 173.8ms，Max = 259.7ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R258 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **12/16** | 0.6-18ms | R258=12/16（持平） |
| **正常**（50-500ms） | 4/16 | 59-260ms | R258=4/16（持平） |
| **超标**（>500ms） | **0/16** | — | R258=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,260,815，liveRecordCount=5,241,598（tombstone=**19,217**，**0.37%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=667  QUERYADV_GE500=75（不变）
FLUSH=15（+6！连续 2 轮）  COMPACTION=4  SKIPFLUSH=7266
```

## 6. 问题清单

### P0：flush 写锁阻塞（持续 flush 加剧风险）

- **连续 2 轮 FLUSH +6——每个 30s tick 都在 flush。**
- **虽然当前 lockWait=0，但 flush 频率高意味着写锁争用概率增大。**

### P0：compaction 触发机制缺陷（根因未修复）

- **tombstone 0.37%——短期无风险。但机制缺陷仍在。**

### 新增观察项：short-query-cache 重建缺陷

- **连续 3 轮 `x` 走 linear-gcd——缓存未重建。**
- **可能原因：flush 导致缓存反复失效。**
- **建议调查 flush 后 cache 失效/重建逻辑。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式（持续高频 flush 加剧风险）

### P0：修复 compaction 触发机制（增加独立定时检查）

### P0：进程 QoS / I/O 优先级保护

### P1（新增）：调查 short-query-cache 重建机制

- **compaction 后 + 高频 flush 导致缓存 3 轮无法重建。**
- **每次 flush 是否失效 cache？是否需要显式 cache warm-up？**

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. `x` 是否恢复为 short-query-cache（第 4 轮观察——若仍未恢复则确认为缺陷）。
2. 全表扫描是否继续收敛（542ms→预期 <400ms）。
3. FLUSH 频率是否持续 +6。

## 9. 结论

**Post-compaction 持续恢复——trigram 极致稳定，全表扫描收敛中。** 0/16 >500ms，Max=260ms，P50=12ms，lockWait=0（连续 2 轮）。极佳 12/16。Trigram 全部 8/8 极佳（连续 3 轮）。全表扫描 627→542ms（↓14%——持续恢复）。**`x` 第 3 轮仍走 linear-gcd（260ms）——short-query-cache 重建确认有延迟，可能与高频 flush 失效有关。**

**FLUSH +6（连续 2 轮——9→15）——compaction 后 WAL 持续活跃。** tombstone=19,217（0.37%——健康）。持续高频 flush 虽然保持索引新鲜，但增加了写锁风险——再次强调 COW flush 的必要性。
