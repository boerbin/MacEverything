# MacEverything 性能分析报告 R_2606101300

## 1. 本轮范围

- 任务启动时间：2026-06-10 13:00
- 报告文件：`docs/performance_ana/R_2606101300.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=8802`
  - `SEG_LINES=7860`
  - `LAST_LOG_TS=2026-06-10 11:17`

## 2. 执行摘要

**I/O 风暴消退，trigram 大幅恢复——但 tombstone 突破 30%！** 2/16 >500ms（`.pdf`=560ms，`path:/Users`=690ms），Max=690ms，P50=124.8ms，lockWait=0（恢复）。Trigram 均值 211ms（↓79% from R251=984ms——大幅恢复）。**经典零和反转：全表扫描 1140ms（↑253% from R251=323ms）——trigram 恢复挤压 SoA 区域。** `x`=5ms（恢复——系统级阻塞完全消退）。**⚠️ tombstone=2,241,343（30.04%）——首次突破 30%！** 第 53 轮 compaction 缺陷确认。

| 指标 | R251 | R252 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 7/16 | **2/16** | **↓5** |
| Max | 3097ms | **690ms** | **↓78%** |
| P50 | 313ms | **124.8ms** | **↓60%** |
| lockWait max | 49.8ms | **0ms** | **恢复为 0** |
| 极佳比例(<50ms) | 1/16 | **3/16** | ↑2 |
| tombstone | 2,226,784 (29.95%) | **2,241,343 (30.04%)** | **⚠️ 突破 30%！** |
| recordCount | 7,442,309 | **7,459,878** | +17.6K（回落但仍偏高） |
| liveRecordCount | 5,215,525 | **5,218,535** | +3.0K（回落但仍偏高） |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 7021 | **7080** | +59（正常化） |
| COMPACTION | 2 | **2** | **不变——第 53 轮确认缺陷** |
| QUERYADV_GE500 | 71 | **73** | +2 |

### ⚠️⚠️ 里程碑：tombstone 首次突破 30%

- **tombstone=2,241,343（30.04%）——从 R200 首次确认缺陷（25.15%）增长 4.89 个百分点。**
- **COMPACTION=2 始终不变——53 轮中零次自动 compaction 触发。**
- **以当前增速，tombstone 将持续不可逆增长，直至手动修复触发机制。**

| 里程碑 | 轮次 | tombstone | tombstone% | 距 R200 |
|--------|------|----------:|----------:|-------:|
| 首次超 25% | R200 | ~1,748K | 25.15% | 0 |
| 首次超 26% | R212 | ~1,841K | 26.14% | 12 |
| 首次超 27% | R220 | ~1,929K | 27.08% | 20 |
| 首次超 2M | R229 | 2,009,885 | 27.87% | 29 |
| 首次超 28% | R230 | 2,038,041 | 28.14% | 30 |
| 首次超 29% | R240 | 2,132,504 | 29.04% | 40 |
| 首次超 29.5% | R247 | 2,179,927 | 29.51% | 47 |
| **首次超 30%** | **R252** | **2,241,343** | **30.04%** | **52** |

### I/O 风暴消退证据

| 指标 | R251 | R252 | 结论 |
|------|-----:|-----:|------|
| `x` (cache) | 285ms | **5ms** | **恢复——系统阻塞消退** |
| `verylongquery` | 0.5ms | **0.5ms** | 正常 |
| lockWait max | 49.8ms | **0ms** | **恢复** |
| SKIPFLUSH delta | +62 | **+59** | **正常化（≈60）** |
| recordCount delta | +29.6K | **+17.6K** | **回落（仍 2.5× 正常）** |

- **R249→R251 三轮 I/O 风暴已完全消退。**
- **`x`=5ms（vs R251=285ms）——纯内存操作恢复正常。**
- **SKIPFLUSH +59（vs R251=+62）——进程调度正常。**
- **但 recordCount +17.6K 仍偏高（正常 +7K），文件系统活动尚未完全平息。**

### 经典零和反转

**R251（I/O 风暴期）：trigram 984ms / 全表扫描 323ms（SoA 预热）。**
**R252（恢复期）：trigram 211ms / 全表扫描 1140ms（SoA 被挤压）。**

| 路径 | R251 | R252 | 变化 | 原因 |
|------|-----:|-----:|------|------|
| Trigram 均值 | 984ms | **211ms** | **↓79%** | postings 重新加载到 page cache |
| 全表扫描总和 | 323ms | **1140ms** | **↑253%** | SoA 被 postings 挤压出 page cache |
| 零和总量 | 1307ms | **1351ms** | ↑3% | **总量守恒** |

- **零和总量 1307→1351ms（↑3%）——几乎完美守恒！**
- **这是 page cache 容量限制下的必然结果。**

### Trigram 路径——大幅恢复

| Query | R251 | R252 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 2829ms | **560ms** | ↓80% | **⚠️ >500ms（仍未完全恢复）** |
| `hello world` | 1905ms | **389ms** | ↓80% | 正常 |
| `*.swift` | 810ms | **295ms** | ↓64% | 正常 |
| `main.cpp` | 134ms | **69ms** | ↓48% | 正常 |
| `abc def...` | 800ms | **219ms** | ↓73% | 正常 |
| `README` | 93ms | **61ms** | ↓34% | 正常 |
| `test` | 1230ms | **76ms** | **↓94%** | 正常 |
| `MacEverything` | 73ms | **23ms** | ↓69% | 极佳 |
| Trigram 均值 | 984ms | **211ms** | **↓79%** |

- **Trigram 均值 984→211ms——大幅恢复但仍高于稳态（30-50ms）。**
- **`.pdf`=560ms（仍 >500ms）——大 postings 集（9860 candidates）恢复最慢。**
- **`test`=76ms（↓94%！从 1230ms 恢复）——phase2=59ms 回归正常。**
- **1/8 trigram 极佳（`MacEverything`=23ms）。**

### 全表扫描——零和恶化

| Query | R251 | R252 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 103ms | **167ms** | ↑62% |
| `path:/Users` | 147ms | **690ms** | **↑369%** |
| `中文测试` | 73ms | **282ms** | ↑286% |
| **总和** | **323ms** | **1140ms** | **↑253%** |

- **全表扫描总和 323→1140ms（↑253%）——R251 的 SoA 预热效应消失。**
- **`path:/Users`=690ms（>500ms！↑369%）——路径数组被 postings 挤压出 page cache。**
- **预计 1-2 轮后全表扫描将回归 500-700ms 正常区间。**

### 辅助路径

| Query | R251 | R252 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 285ms | **5ms** | ↓98%（恢复） | 极佳 |
| `ext:cpp` | 3097ms | **125ms** | ↓96%（恢复） | 正常 |
| `size:>100m` | 1244ms | **64ms** | ↓95%（恢复） | 正常 |
| `dm:today` | 313ms | **97ms** | ↓69% | 正常 |
| `verylongquery...` | 0.5ms | **0.5ms** | 持平 | 极佳 |

- **`ext:cpp`=125ms（↓96% from 3097ms——从系列记录恢复！）。**
- **`size:>100m`=64ms（↓95% from 1244ms——SoA size 区域恢复）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 5.1 | 0.0 | short-query-cache | 0 | 0 | 极佳（恢复） |
| `.pdf` | 5 | **559.5** | 553.4 | advanced-trigram | 0 | 9860 | **⚠️ >500ms** |
| `main.cpp` | 5 | 69.1 | 49.0 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 294.8 | 281.7 | advanced-trigram | 0 | 9379 | 正常 |
| `ext:cpp` | 5 | 124.8 | 123.6 | advanced-ext-index | 0 | 3606 | 正常 |
| `size:>100m` | 5 | 64.2 | 63.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 389.4 | 385.4 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 218.5 | 217.9 | advanced-trigram | 0 | 14493 | 正常 |
| `README` | 5 | 61.0 | 54.3 | advanced-trigram | 0 | 17162 | 正常 |
| `test` | 5 | 76.3 | 58.9 | advanced-trigram | 0 | 117300 | 正常 |
| `MacEverything` | 5 | 22.7 | 0.6 | advanced-trigram | 0 | 6867 | 极佳 |
| `dm:today` | 5 | 96.8 | 78.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 167.2 | 155.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | **690.3** | 680.8 | advanced-linear-gcd | 0 | 0 | **⚠️ >500ms** |
| `中文测试` | 0 | 282.0 | 279.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 124.8ms，P90 = 559.5ms，Max = 690.3ms。2/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R251 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **3/16** | 0.5-23ms | R251=1/16（↑2） |
| **正常**（50-500ms） | 11/16 | 61-389ms | R251=8/16（↑3） |
| **超标**（>500ms） | **2/16** | 560-690ms | R251=7/16（**↓5**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,459,878，liveRecordCount=5,218,535（tombstone=**2,241,343**，**30.04%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=639  QUERYADV_GE500=73（+2）
FLUSH=1  COMPACTION=2  SKIPFLUSH=7080
```

## 6. 问题清单

### P0 确认（第 53 轮）：compaction 触发机制缺陷

- **⚠️ tombstone=2,241,343（30.04%）——首次突破 30%！超阈值 5.04%。**
- **53 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **lockWait 恢复为 0。R249-R251 的 lockWait 已确认为系统级事件。**

### P0（从 P1 升级）：I/O 优先级保护

- **R249-R251 三轮 I/O 风暴证据充分。虽已消退，但无防护机制。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 53 轮——tombstone 突破 30%！）

### P0：flush rewrite 改为 COW 方式

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 全表扫描能否从 1140ms 回落（零和自平衡——预计 1-2 轮）。
2. tombstone 增速是否继续加速（30.04%，增速仍偏高）。
3. `.pdf` 能否恢复至 <500ms。

## 9. 结论

**I/O 风暴完全消退——系统恢复正常运行。** `x`=5ms（↓98%），lockWait=0，SKIPFLUSH +59（正常）。Trigram 大幅恢复 984→211ms（↓79%），7/16→2/16 >500ms。**经典零和反转：trigram 恢复挤压全表扫描 323→1140ms（↑253%）——零和总量守恒（1307→1351ms，↑3%）。** `path:/Users`=690ms（>500ms——被 postings 挤压出 page cache）。

**⚠️ tombstone 首次突破 30%！** 2,241,343（30.04%）——从 R200=25.15% 增长 4.89pp，53 轮中零次自动 compaction 触发。recordCount +17.6K（回落但仍 2.5× 正常——文件系统活动尚未完全平息）。
