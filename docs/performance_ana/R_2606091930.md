# MacEverything 性能分析报告 R_2606091930

## 1. 本轮范围

- 任务启动时间：2026-06-09 19:30
- 报告文件：`docs/performance_ana/R_2606091930.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6587`
  - `SEG_LINES=5645`
  - `LAST_LOG_TS=2026-06-09 14:47`

## 2. 执行摘要

**lockWait=202.6ms 系列绝对纪录！** 1/16 >500ms（`.pdf`=598ms），但 `x`=474ms（进程调度延迟第 2 高）。`.pdf` 的 598ms 中 lockWait=202.6ms 占 34%——**flush 或索引更新操作持有写锁超过 200ms！** 这是 COW flush（P0.5）修复的直接证据。

| 指标 | R216 | R217 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **1/16** | ↑1 |
| Max | 328ms | **598ms** | ↑82% |
| P50 | 47ms | **180ms** | ↑283% |
| lockWait max | 0ms | **202.6ms** | **↑∞（系列绝对纪录！）** |
| 极佳比例(<50ms) | 9/16 | **4/16** | ↓5 |
| tombstone | 1,884,334 (26.67%) | **1,897,337 (26.77%)** | ↑ |
| tombstone 增速 | ~413/min | **~433/min** | ↑5% |
| recordCount | 7,064,457 | **7,086,789** | +22.3K |
| liveRecordCount | 5,180,123 | **5,189,452** | **+9.3K** |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5053 | **5113** | +60 |
| COMPACTION | 2 | **2** | **不变——第 18 轮确认缺陷** |
| QUERYADV_GE500 | 52 | **53** | ↑1 |

### lockWait=202.6ms——系列绝对纪录！P0.5 升级证据

- **`.pdf` 查询：phase2=341ms，lockWait=202.6ms，queryTime=598ms。**
- **lockWait 占 queryTime 的 34%——三分之一时间在等锁。**
- **此前系列最高 lockWait=18.2ms（R215）——本轮 ↑1013%！**
- **含义：某个后台操作（flush 或索引更新）在 `.pdf` 查询期间持有写锁超过 200ms。**
- **lockWait=202.6ms 已接近用户可感知延迟——如果用户在此刻搜索，会有 200ms 的无响应等待。**
- **这是 COW flush 方案（P0.5）的最强直接证据——flush 操作不应持有全局写锁。**

### lockWait 历史纪录

| 轮次 | lockWait max | 触发查询 | 说明 |
|------|------------:|---------|------|
| R199 | 761ms* | — | *lockHeldMs，非 lockWaitMs |
| R201-R213 | 0ms | — | 无锁竞争 |
| R214 | 2.5ms | `.pdf` | 轻度 |
| R215 | 18.2ms | `.pdf` | 显著 |
| R216 | 0ms | — | 恢复 |
| **R217** | **202.6ms** | **`.pdf`** | **系列绝对纪录！** |

### `x`=474ms——进程调度延迟第 2 高

- **phase2=0（cache 命中），queryTime=474ms——全是 overhead。**
- **仅低于 R215=620ms——系列第 2 高。**
- **系统级 CPU 竞争再次出现——晚间也有高负载。**

### `.pdf`=598ms——lockWait 主导

- **phase2=341ms（trigram postings 部分受压）。**
- **lockWait=202.6ms——等锁占总时间 34%。**
- **即使没有 lockWait，phase2=341ms + overhead ≈ 400ms——仍在正常范围。**
- **lockWait=202.6ms 是 .pdf 超标的直接原因。**

### liveRecordCount +9.3K——第 2 次大幅跳增

- **R214=+9.2K，R217=+9.3K——两次大幅跳增。**
- **可能有周期性批量文件操作（如构建产出、日志轮转）。**
- **recordCount +22.3K——大量文件变更。**

### Trigram 路径——受压但可控

| Query | R216 | R217 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 110ms | **598ms** | ↑444% | **>500ms（lockWait 主导）** |
| `hello world` | 328ms | **401ms** | ↑22% | 正常 |
| `*.swift` | 62ms | **225ms** | ↑263% | 正常 |
| `main.cpp` | 18ms | **41ms** | ↑128% | 极佳 |
| `abc def...` | 203ms | **154ms** | ↓24% | 正常 |
| `README` | 46ms | **44ms** | ↓4% | 极佳 |
| `test` | 141ms | **78ms** | ↓45% | 正常 |
| `MacEverything` | 27ms | **12ms** | ↓56% | 极佳 |
| Trigram 均值 | 117ms | **194ms** | **↑66%** | 受压 |

- **Trigram 均值 117→194ms（↑66%）——受压。**
- **`.pdf`=598ms 中 lockWait=202.6ms 占主导——如果去除 lockWait，phase2=341ms 在正常范围。**
- **`README`=44ms，`MacEverything`=12ms——部分查询仍极佳。**

### 全表扫描——正常

| Query | R216 | R217 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 126ms | **180ms** | ↑43% |
| `path:/Users` | 115ms | **198ms** | ↑72% |
| `中文测试` | 50ms | **195ms** | ↑290% |
| **总和** | **291ms** | **573ms** | **↑97%** |

- **全表扫描总和 291→573ms（↑97%）——从优秀回到午后常态。**
- **`中文测试`=195ms（从极佳 50ms 回升——page cache 零和效应）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 474.4 | 0.0 | short-query-cache | 0 | 0 | 正常（**调度延迟第 2 高**） |
| `.pdf` | 5 | 597.8 | 340.9 | advanced-trigram | **202.6** | 9860 | **>500ms（lockWait 纪录！）** |
| `main.cpp` | 5 | 41.2 | 36.1 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 225.1 | 220.5 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 60.6 | 57.8 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 277.1 | 275.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 401.3 | 394.4 | advanced-trigram | 0 | 3669 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 153.8 | 152.8 | advanced-trigram | 0 | 14371 | 正常 |
| `README` | 5 | 43.8 | 38.8 | advanced-trigram | 0 | 17099 | 极佳 |
| `test` | 5 | 78.0 | 60.4 | advanced-trigram | 0 | 117051 | 正常 |
| `MacEverything` | 5 | 11.6 | 1.4 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 70.9 | 69.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 180.1 | 172.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 197.5 | 168.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 195.4 | 182.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 1.3 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 180.1ms，P90 = 474.4ms，Max = 597.8ms。1/16 查询 >500ms。lockWait max = 202.6ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R216 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **4/16** | 1.3-44ms | R216=9/16（↓5） |
| **正常**（50-500ms） | 11/16 | 61-474ms | R216=7/16（↑4） |
| **超标**（>500ms） | **1/16** | 598ms | R216=0/16（↑1） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,086,789，liveRecordCount=5,189,452（tombstone=**1,897,337**，**26.77%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=426  QUERYADV_GE500=53（↑1）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5113
```

## 6. 问题清单

### P0 确认（第 18 轮）：compaction 触发机制缺陷

- **tombstone 26.77%——超阈值 1.77%。**

### P0.5 升级证据：lockWait=202.6ms

- **锁持有时间超过 200ms——用户可感知延迟。**
- **COW flush 方案是唯一正确的修复路径。**

### 进程调度延迟——第 2 高

- **`x`=474ms——仅低于 R215=620ms。**
- **晚间系统负载仍然偏高。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 18 轮确认）

### P0.5→P0：flush rewrite 改为 COW 方式（**建议升级至 P0**）

- **lockWait=202.6ms 是直接的用户体验损害证据。**
- **flush 操作持有全局写锁 >200ms——任何并发查询都会被阻塞。**
- **COW 方案：flush 时生成数据快照，在快照上写盘，完成后原子替换——写锁时间从 200ms+ 降至 <1ms。**

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. lockWait 是否回落（202.6ms 是一次性事件还是趋势）。
2. `x` 进程调度延迟是否消退。
3. tombstone 增长趋势。

## 9. 结论

**lockWait=202.6ms 系列绝对纪录——`.pdf`=598ms 中 34% 时间在等锁。** 1/16 >500ms，`x`=474ms（调度延迟第 2 高）。flush 或索引更新操作持有全局写锁超过 200ms——**建议 COW flush（P0.5）升级至 P0 优先级**。如果去除 lockWait，`.pdf` phase2=341ms 在正常范围。

**tombstone 26.77%（第 18 轮确认 compaction 缺陷）。** liveRecordCount +9.3K（第 2 次大幅跳增——周期性批量文件操作）。recordCount +22.3K。当前有两个 P0 级问题：compaction 触发缺陷和 flush 写锁阻塞。
