# MacEverything 性能分析报告 R_2606081146

## 1. 本轮范围

- 任务启动时间：2026-06-08 11:46
- 报告文件：`docs/performance_ana/R_2606081146.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=4338`
  - `SEG_LINES=3396`
  - `LAST_LOG_TS=2026-06-08 12:17`

## 2. 执行摘要

**I/O 风暴 1 轮恢复——0/16 >500ms，Max=386ms，P50=105ms，lockWait=0，极佳比例 4/16（25%）。** 从 R179 的 6/16 >500ms、lockWait=64ms 恢复至 0/16 >500ms、lockWait=0——确认为一次性事件。Trigram 均值从 375ms 降至 147ms（↓61%），全表扫描总和从 3422ms 降至 965ms（↓72%）。但 trigram 仍远高于正常稳态（12-18ms）——恢复仍在进行中。tombstone 增速开始回落（679/min，R179=818/min）。

### 关键指标对比

| 指标 | R179（I/O 风暴） | R180 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 6/16 | **0/16** | **↓6（恢复！）** |
| Max | 1538ms | **386ms** | **↓ 75%** |
| P50 | 280ms | **105ms** | **↓ 63%** |
| lockWait max | 63.7ms | **0ms** | **恢复完美** |
| 极佳比例(<50ms) | 0/16 | **4/16** | **↑4** |
| tombstone | 1,459,175 (22.22%) | **1,478,091 (22.44%)** | ↑ |
| tombstone 增速 | ~818/min | **~679/min** | **↓ 17%（追赶减速！）** |
| recordCount | 6,565,559 | **6,585,907** | +20.3K（追赶减速） |
| liveRecordCount | 5,106,384 | **5,107,816** | +1,432 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 3089 | **3148** | +59 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 13 | **13** | 不变（本轮 0 新增） |

### 恢复进度——R179→R180

| 路径 | R179（风暴） | R180 本轮 | 恢复率 | 目标值 |
|------|-----:|-----:|------|------|
| `content:import` | 1538ms | **266ms** | ↓ 83% | ~200ms |
| `path:/Users` | 1145ms | **386ms** | ↓ 66% | ~250ms |
| `中文测试` | 739ms | **313ms** | ↓ 58% | ~200ms |
| `.pdf`(trigram) | 447ms | **329ms** | ↓ 26% | ~17ms |
| `hello world`(trigram) | 985ms | **199ms** | ↓ 80% | ~15ms |
| lockWait max | 63.7ms | **0ms** | ↓ 100% | 0ms ✓ |
| `x`(cache) | 72ms | **15ms** | ↓ 79% | ~3ms |

- **lockWait 完全恢复——flush timer 竞争已消退。**
- **`x`=15ms（R179=72ms）——进程调度延迟大幅改善但仍高于正常（~3ms）。**
- **SoA/linear 恢复良好——`content:import`=266ms 接近正常水平。**
- **trigram 恢复较慢——`.pdf`=329ms 仍远高于正常（17ms）。**

### 全表扫描——大幅恢复

| Query | R179 | R180 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 1538ms | **266ms** | **↓ 83%** |
| `path:/Users` | 1145ms | **386ms** | **↓ 66%** |
| `中文测试` | 739ms | **313ms** | **↓ 58%** |
| **总和** | **3422ms** | **965ms** | **↓ 72%** |

- **全表扫描总和 965ms——从 3422ms 大幅恢复。**
- **仍高于日间稳态（750-800ms）——完全恢复需更多轮。**
- **`path:/Users`=386ms 最高——linear-gcd 区域仍受日间 page cache 竞争。**

### Trigram 路径——部分恢复，不均匀

| Query | R179 | R180 | 变化 | 正常值 |
|-------|-----:|-----:|------|------|
| `.pdf` | 447ms | **329ms** | ↓ 26% | ~17ms |
| `*.swift` | 241ms | **269ms** | ↑ 12%（未恢复） | ~15ms |
| `hello world` | 985ms | **199ms** | ↓ 80% | ~15ms |
| `abc def...` | 284ms | **61ms** | **↓ 79%** | ~30ms |
| `README` | 313ms | **17ms** | **↓ 95%（完全恢复！）** | ~10ms |
| `test` | 280ms | **36ms** | **↓ 87%（接近恢复！）** | ~14ms |
| `MacEverything` | 78ms | **9ms** | **↓ 88%（恢复）** | ~3ms |
| Trigram 均值 | 375ms | **147ms** | **↓ 61%** | 12-18ms |

- **恢复极其不均匀**：`README`=17ms（完全恢复），`.pdf`=329ms（几乎未恢复）。
- **原因**：不同 postings 区域在 page cache 中的恢复取决于：
  1. R179 battery 是否触及该区域的 postings（是→已预热）
  2. 该区域的 postings 是否被日间系统活动再次淘汰（是→又变冷）
- **`.pdf`=329ms + `*.swift`=269ms——这两个查询的 postings 区域与日间活跃文件类型重叠，被系统活动持续竞争。**
- **`README`/`test`/`MacEverything`——这些 postings 区域未被日间活动竞争，R179 预热后保持住了。**

### SoA Filter——恢复中

| Query | R179 | R180 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 465ms | **112ms** | **↓ 76%** |
| `dm:today` | 233ms | **79ms** | **↓ 66%** |

- **`size:>100m`=112ms（R179=465ms）——大幅恢复但仍偏高（正常 ~30ms）。**
- **`dm:today`=79ms（R179=233ms）——回到日间正常范围。**
- **两项总和**：R179=698ms → R180=191ms——↓ 73%。

### tombstone 追赶减速信号

- **增速 679/min（R179=818/min，↓17%）——追赶开始减速！**
- **recordCount +20.3K（R179=+27.1K，↓25%）——FSEvents 积压正在消化。**
- **tombstone 22.44%（1.48M）——距 25% 约 13-16 小时。**
- **预计后续几轮增速将继续回落至正常水平（~300/min）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 15.0 | 0.0 | short-query-cache | 0 | 0 | 恢复中 |
| `.pdf` | 5 | 328.8 | 325.1 | advanced-trigram | 0 | 9841 | 冷态 |
| `main.cpp` | 5 | 49.9 | 34.0 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 269.4 | 266.2 | advanced-trigram | 0 | 9345 | 冷态 |
| `ext:cpp` | 5 | 105.4 | 103.9 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 111.5 | 111.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 199.2 | 195.9 | advanced-trigram | 0 | 3626 | 冷态 |
| `abc def ghi jkl mno pqr` | 0 | 60.6 | 59.4 | advanced-trigram | 0 | 14218 | 正常 |
| `README` | 5 | 16.7 | 15.0 | advanced-trigram | 0 | 16634 | **极佳（完全恢复）** |
| `test` | 5 | 36.1 | 27.8 | advanced-trigram | 0 | 116529 | **极佳（接近恢复）** |
| `MacEverything` | 5 | 9.1 | 0.9 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 79.0 | 77.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 265.9 | 245.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 385.9 | 376.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 313.0 | 312.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.8 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 105.4ms，P90 = 328.8ms，Max = 385.9ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R179 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 4/16 | 0.8-49.9ms | R179=0/16（**↑4**） |
| **正常**（50-500ms） | 12/16 | 61-386ms | R179=10/16（↑2） |
| **超标**（>500ms） | **0/16** | — | R179=6/16（**↓6**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,585,907，liveRecordCount=5,107,816（tombstone=**1,478,091**，**22.44%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=179  QUERYADV_GE500=13（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3148
```

- **本轮 0 新增 >500ms 查询。**
- **SKIPFLUSH +59——正常节奏。**

## 6. 问题清单

### 观察项：trigram 部分 postings 持续冷态

- **`.pdf`=329ms、`*.swift`=269ms——与日间活跃文件类型 postings 区域重叠，被系统活动持续竞争。**
- **`README`=17ms、`test`=36ms——完全恢复——这些 postings 区域未被竞争。**
- **结论**：部分 trigram postings 可能长期处于冷态——取决于与日间系统 I/O 的重叠程度。

### tombstone 22.44%——追赶减速中

- **增速 679/min（↓17% from R179），追赶正在消化。**
- **距 25% 约 13-16 小时。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（紧迫）

- tombstone 22.44%（1.48M），距 25% 约 13-16 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- R179 lockWait=64ms 已证明问题——本轮恢复至 0 但根因未消除。

### P2：唤醒预热策略（持续建议）

- 本轮数据进一步证明：部分 trigram postings 可能长期无法通过自预热恢复——需要主动策略。

## 8. 下一轮重点

1. `.pdf`/`*.swift` trigram postings 是否继续恢复或稳定在冷态。
2. tombstone 增速是否继续回落（信号追赶完成）。
3. 全表扫描总和是否恢复至日间稳态（750-800ms）。

## 9. 结论

**I/O 风暴 1 轮恢复——0/16 >500ms，Max=386ms，P50=105ms，lockWait=0，极佳比例 4/16。** R179 的重度事件确认为一次性——lockWait 完全恢复，>500ms 归零。SoA/linear 通过 R179 battery 预热恢复良好。Trigram 恢复极其不均匀：`README`/`test` 完全恢复（postings 区域未被日间活动竞争），`.pdf`/`*.swift` 持续冷态（与系统活跃文件类型重叠）。

**新发现**：trigram 恢复不仅取决于 battery 预热，还取决于 postings 区域是否与日间系统 I/O 活跃区域重叠——重叠区域可能长期冷态。tombstone 22.44%（1.48M），追赶减速至 679/min（信号消化中），距 25% 约 13-16 小时。P0 compaction 修复日益紧迫。
