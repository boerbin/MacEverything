# MacEverything 性能分析报告 R_2605302141

- **任务启动时间**：2026-05-30 21:41
- **日志范围**：最新会话（line 10206，`2026-05-30 20:50:36`，R106 重启后的当前会话），任务时已运行 ~51min。**与 R106 同会话，处于重启后暖机阶段**。
- **状态快照 @21:41**：total `6,167,918` / live `5,412,142` / tomb `755,776`（**12.25%**）；@21:43 复测 tomb `756,904`（12.27%）。
- **本轮定性**：🟡 **重启后暖机 + ingest 争用突发 + flush 重写-膨胀恶性循环显形 + tombstone 逼近崩溃参考**。本轮处于 R106 重启会话的暖机段（~51min），整体较 R106 冷态回暖但未达 R105 长跑暖态。三个观测：(1) **ingest 争用突发**——floor 双窗口内 dm 单次 608ms、path 850ms、content 538ms 突发，骑在稳定 floor 之上（R97 加性签名，非 R100 非平稳）；(2) **flush 重写-膨胀恶性循环直接显形**——新会话已 **10 次 flush 全为纯重写、零 compaction**，每次写全量 6.1M 记录（含 756K tombstone），重写代价 3.3-30s，R67 使 tombstone 永不回收，每次 flush 都被迫重写死记录；(3) tombstone **12.25%**，距 12.97% 崩溃参考仅 ~0.72pp，本轮增量 **+14,686（较 20K+ 平台回落，可能 taper）**。分裂经 **floor（2.92×）+ median（2.43×）双成立**；对抗检验因 dm 争用突发长尾**失败**（与 R106 同构，标注为突发方差伪影、非机制违背）。**全部组织查询零 organic 越线**（9 条 >500ms 全为本轮 floor 探测、唯一 >5s 的 png 系 R106 Phase-2 尾遗留）。

---

## 1. 原始电池（暖机段，markers 全部洁净恢复）

| query | path | total(ms) | phase2 | trig | lockHeld | cand | 备注 |
|---|---|---|---|---|---|---|---|
| `x` | short-query-cache | 0.13 | 0 | 0 | 0 | 0 | 缓存 |
| `.pdf` | advanced-trigram | 35.13 | 32.81 | 2.25 | 35.08 | 10181 | 洁净 |
| `main.cpp` | advanced-trigram | 47.68 | 5.38 | 42.24 | 47.62 | 74 | 暖机（复测 0.29，无 marker）|
| `*.swift` | advanced-trigram | 181.01 | 54.51 | 126.29 | 180.81 | 9237 | 暖机抬升（复测 4.75）|
| `ext:cpp` | advanced-ext-index | 19.33 | 18.78 | 0 | 19.29 | 3020 | 洁净 |
| `size:>100m` | pure-filter-soa-gcd | 51.11 | 51.10 | 0 | 51.10 | 0 | 数值（floor 14.35）|
| `hello world` | advanced-trigram | 36.00 | 30.86 | 5.08 | 35.95 | 2374 | 洁净 |
| `abc def ghi jkl mno pqr` | advanced-trigram | 147.23 | 132.03 | 15.16 | 147.21 | 15959 | 暖机 |
| `README` | advanced-trigram | 18.01 | 10.20 | 7.76 | 17.96 | 16700 | 洁净 |
| `test` | advanced-trigram | 32.29 | 17.73 | 14.46 | 32.19 | 137940 | 洁净 |
| `MacEverything` | advanced-trigram | 111.39 | 1.15 | 110.20 | 111.35 | 6722 | 暖机抬升（复测 2.52）|
| `dm:today` | pure-filter-soa-gcd | 87.85 | 87.71 | 0 | 87.71 | 0 | 数值（floor 45.5）|
| `content:import` | pure-filter-soa-gcd | 371.65 | 360.39 | 0 | 360.39 | 0 | 字符串（floor 132.9）|
| `path:/Users` | advanced-linear-gcd | 161.60 | 152.54 | 0 | 152.54 | 0 | 字符串（floor 143.27）|
| `中文测试` | advanced-linear-gcd | 241.44 | 241.15 | 0.15 | 241.40 | 0 | 字符串（floor 203.79）|
| `verylongquery…really` | advanced-trigram | 1.34 | 0 | 1.28 | 1.29 | 0 | |

**markers 全部洁净**：ext:cpp 19ms<500、x 0.13ms<10、main.cpp 74cand 47.7ms<100。`*.swift` 181ms / `MacEverything` 111ms 暖机抬升，复测分别恢复至 4.75 / 2.52（trigram 索引页暖机伪影，非稳态）。

---

## 2. 复测消歧（标志项全部瞬时恢复）

| query | 电池值 | probe1 | probe2 | probe3 | probe4 | 判定 |
|---|---|---|---|---|---|---|
| `x` | 0.13 | 0.16 | 0.0 | 0.04 | 0.55 | ✅ 瞬时 |
| `*.swift`（181ms）| 181.01 | 48.36 | 4.75 | 31.65 | 8.43 | ✅ 暖机伪影（38× 恢复）|
| `MacEverything`（111ms）| 111.39 | 61.08 | 2.83 | 2.52 | 5.04 | ✅ 暖机伪影（44× 恢复）|
| `main.cpp`（74 cand）| 47.68 | 17.59 | 0.29 | 4.36 | 0.64 | ✅ 瞬时（无 marker）|

trigram 暖机抬升项干净恢复，确认为重启后页暖机（非争用、非算法）。

---

## 3. FLOOR-METHOD 双窗口（各 8×，R100 平稳性协议）

**窗口内 spread（含 ingest 争用突发）：**

| query | 类型 | W1 min | W1 max | W2 min | W2 max | 突发标记 |
|---|---|---|---|---|---|---|
| `size` | 数值 | 14.35 | 52.54 | 24.58 | 64.47 | — |
| `dm` | 数值 | 45.5 | **608.44** | 51.6 | 191.27 | ⚠️ W1 单次 608 突发 |
| `content` | 字符串 | 199.31 | 538.94 | 132.9 | 359.16 | W1 单次 538 |
| `path` | 字符串 | 255.68 | **850.06** | 143.27 | 512.91 | ⚠️ W1 722/850 突发 |
| `中文` | 字符串 | 203.79 | 534.6 | — | — | |

**跨窗口 floor 摆动（floor 稳定，突发为加性）：**

| query | 类型 | W1 floor | W2 floor | 摆动 | 判定 |
|---|---|---|---|---|---|
| `size` | 数值 | 14.35 | 24.58 | 1.71× | 稳 |
| `dm` | 数值 | 45.5 | 51.6 | **1.13×** | **稳** |
| `content` | 字符串 | 199.31 | 132.9 | 1.50× | 稳 |
| `path` | 字符串 | 255.68 | 143.27 | **1.78×** | 较稳（< R100 2.3× 阈值）|

**关键判定**：所有 floor 跨窗口摆动 ≤1.78×，**低于 R100 非平稳阈值（2.3-3.0×）**。dm 608、path 850、content 538 等突发**骑在稳定 floor 之上**——这是 R97 加性争用突发（突发在 21:42-21:44 同时击中所有路径，证明是全局写者持锁事件），**非 R100 单路径非平稳**。floor 法在本轮有效。

---

## 4. 洁净 ns/槽（floor 法，records=6,167,918）

| 路径 | 类型 | floor(ms) | 洁净值(ns/槽) | 说明 |
|---|---|---|---|---|
| `size:>100m` | 数值 | 14.35 | **2.33** | 暖机回落（R106 1.90）|
| `dm:today` | 数值 | 45.5 | **7.38** | 暖机（R105 7.65、R106 6.28）|
| `content:import` | 字符串 | 132.9 | **21.55** | 最低字符串 |
| `path:/Users` | 字符串 | 143.27 | **23.23** | |
| `中文测试` | 字符串 | 203.79 | **33.04** | |

重启后暖机段，floor 介于 R106 冷态与 R105 长跑暖态之间（content 21.55 vs R106 16.37、path 23.23 vs R106 16.79）——暖机进行中，仍清晰分层。

---

## 5. 二层分裂：floor+median 双成立，对抗检验因 dm 突发失败（诚实标注）

| 估计法 | 最高数值 | 最低字符串 | 间隙 | 判定 |
|---|---|---|---|---|
| **floor（R97 主估计）** | dm 7.38ns | content 21.55ns | **2.92×** | ✅ HOLDS |
| **median** | dm 15.92ns | content 38.68ns | **2.43×** | ✅ HOLDS |
| **对抗（dm 突发-worst 含 608/191/188）** | — | — | 重叠 | ❌ FAILS（突发伪影）|

- **数值层**：size 2.33、dm 7.38 → 最高数值 **dm 7.38**
- **字符串层**：content 21.55（最低）、path 23.23、中文 33.04 → 最低 **content 21.55**
- **floor 间隙 = 21.55 / 7.38 = 2.92×**，无重叠。
- **median 间隙**：dm median 98.2ms（15.92ns）vs content median 238.6ms（38.68ns）= **2.43×**，无重叠。

**多重稳健性（诚实分级）**：
1. **floor**：dm 7.38 < content 21.55 → 2.92× ✅（R97 主估计量，floor 跨窗口稳定）
2. **median**：dm 15.92 < content 38.68 → 2.43× ✅（中心趋势分离）
3. **对抗（dm 突发-worst）**：dm W1 含 608/170/154、W2 含 191/188 突发，与 content floor 132.9（21.55ns）重叠 → **FAILS** ❌

**诚实判定**：本轮 dm 在 21:42-21:44 与 path/content **同时**出现争用突发（608/850/538ms），属全局写者持锁事件的加性污染。floor（最干净页驻留估计）+ median（中心趋势）双双清晰分离 2.92×/2.43×；对抗检验因 dm 突发长尾失败，与 **R106 同构**（最强检验因单层方差失效，floor/中心仍成立）。**故本轮记为 floor+median 双成立（33/33 by floor），对抗检验突发态推迟**——不宣称干净 literal-min，亦不宣称破裂。

---

## 6. 阀门证据：flush 重写-膨胀恶性循环显形（R67 持续）

### 6.1 新会话 10 次 flush 全为纯重写

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries` | **0**（WAL 重建越过阈值）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 `Flushed v6` | **10 次（全部纯重写）** |

**10 次 flush 重写代价（写全量含 tombstone）：**

```
20:56:13  rewrite=29947ms  liveRecords=5,401,300  ← 重启首次（冷）
21:00:54  rewrite=10813ms
21:06:07  rewrite=23944ms
21:10:48  rewrite= 4431ms
21:15:46  rewrite= 3287ms
21:20:49  rewrite= 5787ms
21:25:48  rewrite= 4757ms
21:30:49  rewrite= 5594ms
21:35:50  rewrite= 6735ms
21:40:50  rewrite= 6200ms
```

### 6.2 R67 恶性循环本轮直接量化

**R106 确认了 R67 比例阀根因（25% > 12.97% 崩溃参考永不可达）。本轮新增观测——flush 重写-膨胀恶性循环**：

- flush 每 5min 触发一次（auto-compaction 300s 间隔），但**从不 compaction**，每次都把全量 6.1M 记录（含 **756K tombstone**）逐字节重写。
- 重写代价 **3.3-30s**（启动期冷态 29947ms，暖态稳定 4-7s）——代价主要由页缓存状态主导，**非单调随 tombstone 上升**（诚实修正：R106 推测「随 tombstone 单调上升」过强，实测暖态稳定 4-7s）。
- **但绝对量随 tombstone 膨胀确凿**：每次 flush 都被迫重写 756K 个死记录，浪费 ~12% 的 I/O。R67 不回收 → tombstone 累积 → 每次 flush 重写更多死记录 → I/O 浪费递增。这是 R67 在 I/O 维度的直接代价，本轮首次量化（10 次 flush 累计重写 ~60M 死记录-槽）。

---

## 7. tombstone 趋势：12.25%，逼近崩溃参考

| 轮次 | tombstone | ratio | 增量 |
|---|---|---|---|
| R103@19:41 | 671,592 | 11.06% | +26,221 |
| R104@20:11 | 695,550 | 11.41% | +23,958 |
| R105@20:41 | 717,223 | 11.72% | +21,673 |
| R106@21:11 | 741,090 | 12.05% | +23,867（穿越重启）|
| **R107@21:41** | **755,776** | **12.25%** | **+14,686（回落，可能 taper）** |

- 连续单调递增，**穿越重启不回缩**（R106 已证）。
- **本轮增量 +14,686 较前 6 轮 20K+ 平台明显回落**——可能 ingest taper，或重启后新 WAL 尚未累积同等删除量。需后续轮次确认是否为趋势拐点（不过度解读）。
- 距 R43 崩溃参考 **12.97% 仅 ~0.72pp**（~44,700 tombstone）。按本轮 +14,686/30min ≈ +29K/h，**~1.5h 触线**（与 R106 估计一致）。**会话内唯一真实退化，修复紧迫性维持峰值。**

---

## 8. 日志健康度（新会话 line 10206 起，~51min）

- WARN = **0**，ERROR = **0**
- QueryAdvanced（total>100ms）= **112 条**（R106 58，+54 含本轮电池+双窗口探测）
- 延迟分布（QueryAdvanced-only）：>5s=**1**（png 5729ms，R106 Phase-2 尾遗留）、1-5s=0、0.5-1s=**8**、0.1-0.5s=103、中位 **226ms**、max 5729ms
- **越线条目枚举（9 条 >500ms，全部为本轮 floor 探测或遗留）**：
  - `png` 5729ms @20:51:26（R106 Phase-2 尾，遗留）
  - dm 608 @21:42:56 / content 538 @21:42:58 / path 722,507,850 @21:43:09-12 / 中文 505,534 @21:43:16-19 / path 512 @21:44:08——**全部在 21:42-21:44 本轮 floor 探测窗口内**
- **零 organic 慢查询**——本轮组织查询无任何非探测越线。8 条 0.5-1s 全为 floor 探测撞 21:43 全局争用突发。

---

## 9. 优化建议（P0｜R67 紧迫性维持峰值）

1. **🔥 P0｜R67 flush 比例阀修复（维持 R106 最高优先级）**：R106 重启双重确认比例阀 0.25 > 崩溃参考 0.1297 永不可达。本轮新增 I/O 维度证据——10 次 flush 全纯重写、零 compaction，每次重写 756K 死记录。修复须双管齐下：(a) 调换 `:142`/`:159` 顺序；(b) 阈值下调至 8-10%。当前 **12.25%，~1.5h 触线**，最紧迫。
2. **🔥 P0｜无锁快照 flush（本轮升级）**：flush 重写-膨胀恶性循环显形——每 5min flush 重写全量含 756K tombstone，启动期持锁 29947ms。无锁快照 flush 可消除 flush 持锁 + 配合 R67 修复消除死记录重写。
3. **🔥 P0｜读写锁分离 / 无锁快照读**：本轮 21:43 全局争用突发（dm/path/content/中文 同时 500-850ms）再证写者饿死读者，根因不变。
4. **🔥 P0｜mlock/madvise 钉住散布热区**（维持 R100 升级）：本轮暖机段 floor 仍较 R105 长跑暖态偏高（content 21.55 vs 18.80），mlock 可加速重启后暖机收敛。R100-R107 连续证据。
5. **🔥 P0｜查询超时保护**：21:43 突发 path 850ms、png 5729ms——超时熔断+降级返回必要。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1｜CJK trigram 索引**（维持 R99 降级）：中文暖机 floor 33.04 与 path/content 同量级，慢主要是页冷非算法。

---

## 10. 结论

- **定性**：🟡 重启后暖机 + ingest 争用突发 + flush 重写-膨胀恶性循环显形。本轮处于 R106 重启会话暖机段（~51min），floor 介于冷态（R106）与长跑暖态（R105）之间。
- **flush 重写-膨胀恶性循环本轮直接量化（新增观测）**：10 次 flush 全纯重写、零 compaction，每次写全量含 756K tombstone。重写代价暖态稳定 4-7s（诚实修正 R106「随 tombstone 单调上升」过强），但绝对 I/O 浪费随 tombstone 膨胀确凿——R67 在 I/O 维度的代价首次量化。
- **分裂 33/33 by floor+median，对抗检验突发态推迟**：floor（dm 7.38 < content 21.55 = 2.92×）+ median（15.92 < 38.68 = 2.43×）双双成立；对抗检验因 dm 在 21:43 与 path/content 同时争用突发（608/850/538）而失败——诚实标注为全局写者持锁加性污染、非机制违背，与 R106 同构。floor 跨窗口稳定（≤1.78× < R100 阈值），突发为加性。
- **唯一持续恶化项**：tombstone **12.25%（755,776）**，距 12.97% 崩溃参考仅 ~0.72pp，~1.5h 触线。本轮增量 +14,686 较 20K+ 平台回落，**可能 taper 拐点但不过度解读**，待后续确认。根因 R67 比例阀已由 R106 重启决定性证实。
- **诚实修正纪律**：本轮(1)明确同会话暖机（非新重启）；(2)修正 R106「flush 代价随 tombstone 单调」推测（实测暖态稳定）；(3)floor/median 成立 vs 对抗失败（突发伪影、非机制）；(4)9 条 >500ms 全标注为探测/遗留、零 organic；(5)tombstone 增量回落标注「可能 taper、不过度解读」。贯彻「不轻易下结论、每个结论需逻辑与数据支撑」。
