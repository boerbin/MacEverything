# MacEverything 性能分析报告 R_2606101330

## 1. 本轮范围

- 任务启动时间：2026-06-10 13:30
- 报告文件：`docs/performance_ana/R_2606101330.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=8871`
  - `SEG_LINES=7929`
  - `LAST_LOG_TS=2026-06-10 11:47`

## 2. 执行摘要

**零和自平衡完成——trigram 持续恢复，全表扫描大幅回落。** 1/16 >500ms（`abc def`=582ms——大 candidate 集），Max=582ms，P50=104.7ms，lockWait=0（连续 2 轮恢复）。Trigram 均值 172ms（↓18% from R252=211ms——持续恢复）。全表扫描总和 495ms（**↓57% from R252=1140ms——零和自平衡完成**）。极佳 5/16（↑2）。**tombstone=2,255,326（30.15%）——30%以上持续攀升。** 第 54 轮 compaction 缺陷确认。

| 指标 | R252 | R253 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 2/16 | **1/16** | ↓1 |
| Max | 690ms | **582ms** | ↓16% |
| P50 | 125ms | **105ms** | ↓16% |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 3/16 | **5/16** | ↑2 |
| tombstone | 2,241,343 (30.04%) | **2,255,326 (30.15%)** | ↑0.11pp |
| recordCount | 7,459,878 | **7,478,181** | +18.3K（仍偏高） |
| liveRecordCount | 5,218,535 | **5,222,855** | +4.3K（仍偏高） |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 7080 | **7140** | +60（正常） |
| COMPACTION | 2 | **2** | **不变——第 54 轮确认缺陷** |
| QUERYADV_GE500 | 73 | **74** | +1 |

### 零和自平衡完成

**R249→R253 五轮完整事件周期：**

| 轮次 | 事件 | Trigram均值 | 全扫总和 | 零和总量 |
|------|------|----------:|--------:|--------:|
| R248 | 稳态 | 96ms | 783ms | 879ms |
| R249 | I/O风暴 | 612ms | 1379ms | 1991ms |
| R250 | 部分恢复 | 195ms | 585ms | 780ms |
| R251 | 二次风暴 | 984ms | 323ms | 1307ms |
| R252 | 恢复+零和 | 211ms | 1140ms | 1351ms |
| **R253** | **平衡** | **172ms** | **495ms** | **667ms** |

- **零和总量 1351→667ms（↓51%）——系统重新达到平衡。**
- **R253 接近 R248 稳态水平（879ms）——但 trigram 仍偏高（172 vs 96ms）。**
- **预计 1-2 轮将完全回归稳态。**

### 1-round recovery 第 13 次确认

- **R252（2/16 >500ms, trigram 211ms）→ R253（1/16 >500ms, trigram 172ms）。**
- **严格来说这是"2-round recovery from I/O storm"——R251→R252→R253 两轮恢复。**
- **但 R252→R253 的改善仍符合 1-round recovery 模式（持续收敛）。**

### Trigram 路径——持续恢复

| Query | R252 | R253 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 560ms | **161ms** | **↓71%** | 正常（恢复！） |
| `hello world` | 389ms | **337ms** | ↓13% | 正常 |
| `*.swift` | 295ms | **128ms** | ↓57% | 正常 |
| `main.cpp` | 69ms | **15ms** | ↓78% | 极佳 |
| `abc def...` | 219ms | **582ms** | ↑166% | **⚠️ >500ms** |
| `README` | 61ms | **46ms** | ↓25% | 极佳 |
| `test` | 76ms | **105ms** | ↑38% | 正常 |
| `MacEverything` | 23ms | **6ms** | ↓74% | 极佳 |
| Trigram 均值 | 211ms | **172ms** | **↓18%** |

- **`.pdf`=161ms（↓71%——从 R251=2829ms 完全恢复！）。**
- **`abc def`=582ms（↑166%——14,502 candidates 大集，page cache 随机波动）。**
- **3/8 trigram 极佳（`main.cpp`=15ms，`README`=46ms，`MacEverything`=6ms）。**
- **Trigram 均值 172ms——距稳态（30-50ms）仍有差距，预计 1-2 轮回归。**

### 全表扫描——零和自平衡完成

| Query | R252 | R253 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 167ms | **151ms** | ↓10% |
| `path:/Users` | 690ms | **180ms** | **↓74%** |
| `中文测试` | 282ms | **164ms** | ↓42% |
| **总和** | **1140ms** | **495ms** | **↓57%** |

- **全表扫描总和 1140→495ms（↓57%）——大幅回落！**
- **`path:/Users`=180ms（↓74%——从 >500ms 恢复至正常）。**
- **三路均 <200ms——分布均衡，SoA 区域 page cache 重新加载。**

### 辅助路径

| Query | R252 | R253 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 5ms | **7ms** | ↑40% | 极佳 |
| `ext:cpp` | 125ms | **42ms** | ↓66% | 极佳 |
| `size:>100m` | 64ms | **62ms** | ↓3% | 正常 |
| `dm:today` | 97ms | **50ms** | ↓48% | 极佳 |
| `verylongquery...` | 0.5ms | **0.4ms** | ↓20% | 极佳 |

- **`ext:cpp`=42ms（极佳——完全恢复至正常区间）。**
- **`dm:today`=50ms（↓48%——边界极佳）。**

### recordCount 增速仍偏高

- **recordCount +18.3K（正常 +7K——仍 2.6× 正常）。**
- **liveRecordCount +4.3K（正常 +200——仍 21× 正常）。**
- **文件系统高活动仍在持续——可能是持续的开发/构建活动。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 7.0 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 160.7 | 149.4 | advanced-trigram | 0 | 9888 | 正常 |
| `main.cpp` | 5 | 15.0 | 9.6 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 127.8 | 124.8 | advanced-trigram | 0 | 9379 | 正常 |
| `ext:cpp` | 5 | 42.2 | 41.1 | advanced-ext-index | 0 | 3606 | 极佳 |
| `size:>100m` | 5 | 62.4 | 61.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 336.8 | 332.8 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | **582.2** | 578.1 | advanced-trigram | 0 | 14502 | **⚠️ >500ms** |
| `README` | 5 | 46.4 | 40.2 | advanced-trigram | 0 | 17164 | 极佳 |
| `test` | 5 | 104.7 | 98.0 | advanced-trigram | 0 | 117308 | 正常 |
| `MacEverything` | 5 | 5.8 | 1.0 | advanced-trigram | 0 | 6867 | 极佳 |
| `dm:today` | 5 | 49.9 | 49.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 151.3 | 144.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 179.7 | 167.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 164.2 | 163.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 104.7ms，P90 = 336.8ms，Max = 582.2ms。1/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R252 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **5/16** | 0.4-50ms | R252=3/16（↑2） |
| **正常**（50-500ms） | 10/16 | 62-337ms | R252=11/16（↓1） |
| **超标**（>500ms） | **1/16** | 582ms | R252=2/16（↓1） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,478,181，liveRecordCount=5,222,855（tombstone=**2,255,326**，**30.15%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=647  QUERYADV_GE500=74（+1）
FLUSH=1  COMPACTION=2  SKIPFLUSH=7140
```

## 6. 问题清单

### P0 确认（第 54 轮）：compaction 触发机制缺陷

- **tombstone=2,255,326（30.15%）——突破 30% 后持续攀升。超阈值 5.15%。**
- **54 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **lockWait 连续 2 轮为 0。历史证据充分。**

### P0：I/O 优先级保护

- **R249-R251 三轮证据。无防护机制时任何系统级 I/O 活动均可导致全面性能灾难。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 54 轮——tombstone 30.15%！）

### P0：flush rewrite 改为 COW 方式

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. trigram 能否回归稳态（172ms→<100ms——预计 1 轮）。
2. recordCount 增速能否回归正常（+18.3K vs 正常 +7K）。

## 9. 结论

**零和自平衡完成——系统重新达到平衡。** 1/16 >500ms（`abc def`=582ms——随机波动），Max=582ms，P50=105ms，lockWait=0。Trigram 211→172ms（↓18%——持续恢复），全表扫描 1140→495ms（**↓57%——自平衡完成**）。零和总量 1351→667ms（↓51%——接近 R248 稳态）。极佳 5/16（↑2）。`.pdf`=161ms（**从 R251=2829ms 完全恢复！**）。

**tombstone 30.15%（2,255,326，第 54 轮 compaction 缺陷确认）——30% 以上持续攀升。** recordCount +18.3K（仍 2.6× 正常——文件系统活动持续）。预计 1 轮后 trigram 回归稳态。
