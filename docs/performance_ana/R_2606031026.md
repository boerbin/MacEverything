# MacEverything 性能分析报告 R_2606031026

## 1. 本轮范围

- 任务启动时间：2026-06-03 10:26
- 报告文件：`docs/performance_ana/R_2606031026.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21131`
  - `SEG_LINES=105`
  - `LAST_LOG_TS=2026-06-03 10:26:38.416`

## 2. 执行摘要

服务正常运行（第 2 轮连续可用），query battery 全部完成。与前轮（R_2606031021，冷启动后首次）相比性能大幅提升——缓存预热效果显著。

| 指标 | R_2606031021（冷） | R_2606031026（热） | 变化 |
|------|------------------:|------------------:|------|
| `.pdf` | 9221ms | 208ms | **-97.7%** |
| `x` | 1924ms | 78ms | **-95.9%** |
| `hello world` | 474ms | 422ms | -11.0% |
| `path:/Users` | 221ms | 578ms | +161%（退化） |
| P50 | ~189ms | ~83ms | **-56%** |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | 备注 |
|-------|------:|------------:|---------:|--------|------|
| `x` | 5 | 77.8 | 0.0 | short-query-cache | 缓存命中，比冷启动快 25x |
| `.pdf` | 5 | 207.7 | 167.6 | advanced-trigram | 缓存热后大幅改善 |
| `main.cpp` | 5 | 41.1 | 38.1 | advanced-trigram | 快 |
| `*.swift` | 5 | 71.2 | 68.1 | advanced-trigram | 快 |
| `ext:cpp` | 5 | 83.7 | 78.9 | advanced-ext-index | 正常 |
| `size:>100m` | 5 | 181.5 | 181.4 | advanced-pure-filter-soa-gcd | 正常 |
| `hello world` | 5 | 422.2 | 397.6 | advanced-trigram | 较慢但稳定 |
| `abc def ghi jkl mno pqr` | 0 | 83.2 | 82.8 | advanced-trigram | 正常 |
| `README` | 5 | 51.6 | 47.7 | advanced-trigram | 快 |
| `test` | 5 | 36.4 | 32.3 | advanced-trigram | 快 |
| `MacEverything` | 5 | 25.8 | 3.2 | advanced-trigram | 极快 |
| `dm:today` | 5 | 61.8 | 60.3 | advanced-pure-filter-soa-gcd | 快 |
| `content:import` | 5 | 258.1 | 241.8 | advanced-pure-filter-soa-gcd | 正常 |
| `path:/Users` | 5 | 577.6 | 371.3 | advanced-linear-gcd | 本轮最慢（退化） |
| `中文测试` | 0 | 259.4 | 255.1 | advanced-linear-gcd | 线性扫描 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 极快 |

**P50 ≈ 83ms，P90 ≈ 422ms，Max = 578ms。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,724,982，liveRecordCount=5,718,746；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=18  QUERYADV_GE500=2
FLUSH=1  COMPACTION=2  SKIPFLUSH=2
DIAG_TRIGRAM_SKIP=54
```

与前轮相比：QueryAdvanced 从 12 增长到 18（本轮 battery 贡献），SKIPFLUSH 增加 1 次。DIAG-TRIGRAM SKIP 增加 1 次。

## 6. 问题清单

### High：`path:/Users` 查询退化（221ms → 578ms）

- 证据：phase2=371ms（前轮 206ms），走 advanced-linear-gcd 路径。
- 可能原因：concurrent FSWatcher 写入增加 lock contention，或 GCD 线性扫描受 I/O 负载影响。

### Medium：`hello world` 仍稳定在 422ms

- 证据：前轮 474ms，本轮 422ms。trigram candidates=3612，phase2 扫描成本仍高。
- 这是多词查询的 phase2 验证成本，属于结构性瓶颈。

### Medium：DIAG-TRIGRAM SKIP 持续累积（54 次）

- 前轮 53 次，本轮 54 次。常见词搜索仍 fallback 到线性扫描。

### Low：FSEvents replay 失败（启动时）

- 仍为本 session 的已知问题，不影响运行中性能。

## 7. 优化建议

### P0：补齐进程生命周期监控（承前轮）

### P1：调查 `path:/Users` 退化根因

- 线性扫描路径受并发写入影响大。考虑为 `path:` 前缀查询引入路径前缀索引。

### P1：优化 `hello world` 类多词 trigram 查询

- 3612 candidates 的 phase2 验证耗时 398ms。考虑在 trigram 交集阶段引入更严格的剪枝。

### P2：调查 DIAG-TRIGRAM SKIP 根因（承前轮）

## 8. 下一轮重点

1. 继续监控服务稳定性。
2. 追踪 `path:/Users` 退化是否为偶发波动。
3. 观察 DIAG-TRIGRAM SKIP 是否继续增长。

## 9. 结论

服务连续第 2 轮正常运行。缓存预热后性能大幅改善：`.pdf` 从 9221ms 降至 208ms（-97.7%），`x` 从 1924ms 降至 78ms（-95.9%），P50 从 189ms 降至 83ms。但 `path:/Users` 出现退化（221→578ms），需追踪。DIAG-TRIGRAM SKIP 持续累积。最高优先级仍是补齐进程生命周期监控。
