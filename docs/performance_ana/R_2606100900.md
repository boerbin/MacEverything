# MacEverything 性能分析报告 R_2606100900

## 1. 本轮范围

- 任务启动时间：2026-06-10 09:00
- 报告文件：`docs/performance_ana/R_2606100900.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=8296`
  - `SEG_LINES=7354`
  - `LAST_LOG_TS=2026-06-10 05:47`

## 2. 执行摘要

**早间活动冲击——全面回调！** 0/16 >500ms，Max=322ms，P50=84ms，lockWait=0（连续 12 轮）。Trigram 均值 99ms（↑219%——早间 page cache 大面积淘汰），全表扫描总和 825ms（↑43%）。**`hello world`=295ms（↑370%）——单查询最大波动，postings 区域被大幅淘汰。** 极佳仅 5/16（本会话近期最低），但 Max=322ms 仍远低于 500ms 阈值——系统仍在正常范围内。

| 指标 | R243 | R244 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 212ms | **322ms** | ↑52% |
| P50 | 38ms | **84ms** | ↑121%（早间冲击） |
| lockWait max | 0ms | **0ms** | 完美（连续 12 轮） |
| 极佳比例(<50ms) | 9/16 | **5/16** | **↓4** |
| tombstone | 2,152,953 (29.25%) | **2,160,078 (29.33%)** | ↑——接近 29.4% |
| tombstone 增速 | ~211/min | **~237/min** | ↑12%（早间加速） |
| recordCount | 7,361,605 | **7,368,961** | +7.4K |
| liveRecordCount | 5,208,652 | **5,208,883** | +231 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 6577 | **6637** | +60 |
| COMPACTION | 2 | **2** | **不变——第 45 轮确认缺陷** |
| QUERYADV_GE500 | 60 | **60** | 不变 |

### 早间活动冲击分析

- **极佳 9/16→5/16（↓4）——本会话 R236 以来最低。**
- **R236 极佳 4/16（凌晨 5:00）→ R237 极佳 11/16（凌晨 5:30）——1 轮恢复。**
- **预期 R245 将出现类似恢复（1-round recovery pattern，已确认 11+ 次）。**
- **早间活动（Spotlight 索引、系统守护进程、用户登录）大面积淘汰 page cache。**

### Trigram 路径——全面回调

| Query | R243 | R244 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 48ms | **121ms** | ↑152% | 正常 |
| `hello world` | 63ms | **295ms** | **↑368%** | 正常（最大波动） |
| `*.swift` | 39ms | **120ms** | ↑208% | 正常 |
| `main.cpp` | 3ms | **21ms** | ↑600% | 极佳 |
| `abc def...` | 51ms | **125ms** | ↑145% | 正常 |
| `README` | 12ms | **35ms** | ↑192% | 极佳 |
| `test` | 28ms | **82ms** | ↑193% | 正常 |
| `MacEverything` | 7ms | **5ms** | ↓29% | 极佳 |
| Trigram 均值 | 31ms | **99ms** | **↑219%** |

- **Trigram 均值 31→99ms（↑219%）——从优秀回到正常区间。**
- **`hello world`=295ms（↑368%）——postings 区域被大幅淘汰，需要从磁盘重新加载。**
- **仅 3/8 trigram 极佳——`main.cpp`、`README`、`MacEverything`（小 candidate 集不受 page cache 影响）。**
- **这确认了 page cache 淘汰主要影响大 postings 集——candidate 数越多，受影响越大。**

### 全表扫描——同步回调

| Query | R243 | R244 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 212ms | **231ms** | ↑9% |
| `path:/Users` | 197ms | **272ms** | ↑38% |
| `中文测试` | 167ms | **322ms** | ↑93% |
| **总和** | **576ms** | **825ms** | **↑43%** |

- **全表扫描总和 576→825ms（↑43%）——与 R242=835ms 接近。**
- **`中文测试`=322ms（↑93%——本轮 Max）——名称数组受影响最大。**
- **`path:/Users`=272ms（↑38%）——路径数组同步回调。**
- **三路均在 231-322ms——分布相对均匀，非单点极端。**

### 辅助路径——回调

| Query | R243 | R244 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 1ms | **2ms** | ↑100% | 极佳 |
| `ext:cpp` | 29ms | **42ms** | ↑45% | 极佳 |
| `size:>100m` | 37ms | **85ms** | ↑130% | 正常 |
| `dm:today` | 123ms | **75ms** | ↓39% | 正常 |
| `verylongquery...` | 1ms | **0.5ms** | ↓50% | 极佳 |

- **`size:>100m`=85ms（↑130%）——SoA size 区域波动。**
- **`dm:today`=75ms（↓39%）——反向改善，page cache 重分配效应。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1.7 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 121.4 | 119.1 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 20.7 | 16.7 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 119.8 | 116.3 | advanced-trigram | 0 | 9353 | 正常 |
| `ext:cpp` | 5 | 42.2 | 41.0 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 84.6 | 84.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 294.8 | 292.3 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 124.8 | 119.2 | advanced-trigram | 0 | 14470 | 正常 |
| `README` | 5 | 35.4 | 28.2 | advanced-trigram | 0 | 17151 | 极佳 |
| `test` | 5 | 81.6 | 78.6 | advanced-trigram | 0 | 117136 | 正常 |
| `MacEverything` | 5 | 5.1 | 1.0 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 75.2 | 74.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 230.9 | 222.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 272.2 | 261.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 321.6 | 320.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 84.6ms，P90 = 294.8ms，Max = 321.6ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R243 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **5/16** | 0.5-42ms | R243=9/16（**↓4**） |
| **正常**（50-500ms） | 11/16 | 75-322ms | R243=7/16（↑4） |
| **超标**（>500ms） | **0/16** | — | R243=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,368,961，liveRecordCount=5,208,883（tombstone=**2,160,078**，**29.33%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=584  QUERYADV_GE500=60（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=6637
```

## 6. 问题清单

### P0 确认（第 45 轮）：compaction 触发机制缺陷

- **tombstone=2,160,078（29.33%）——超阈值 4.33%，持续攀升。**
- **45 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **连续 12 轮 lockWait=0。历史证据充分。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（第 45 轮确认——tombstone 29.33%）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 是否出现 1-round recovery（R244→R245 极佳回升——已有 11+ 次先例）。
2. 早间活动峰值是否已过（9:00-9:30 通常为工作日高峰）。

## 9. 结论

**早间活动冲击——极佳 5/16（↓4），trigram 99ms（↑219%），全表扫描 825ms（↑43%）。** 0/16 >500ms，Max=322ms，P50=84ms，lockWait=0（连续 12 轮）。`hello world`=295ms 为单查询最大波动——postings 区域被早间系统活动大面积淘汰。但 Max=322ms 仍远低于 500ms 阈值，系统仍在正常范围。基于 11+ 次 1-round recovery 先例，预期 R245 将回升。

**tombstone 29.33%（2,160,078，第 45 轮确认 compaction 缺陷）。** 增速 ~237/min（早间加速↑12%）。COMPACTION=2 始终不变。
