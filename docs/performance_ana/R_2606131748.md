# MacEverything 性能分析报告 R_2606131748

## 1. 本轮范围

- 任务启动时间：2026-06-13 17:48
- 报告文件：`docs/performance_ana/R_2606131748.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=12287`
  - `SESSION_START_TS=2026-06-13 12:03:04.944`
  - `TOTAL_LINES=12768`
  - `SEG_LINES=482`
  - `LAST_LOG_TS=2026-06-13 17:48`

## 2. 执行摘要

**本轮 16-query battery 中 0/16 >500ms，Max 从 270.4ms 小幅升至 283.1ms，P50 从约 17.9ms 升至约 24.9ms，P90 从约 141.9ms 升至约 228.7ms。** 最高项仍为 `中文测试`=283.1ms，其次是 `path:/Users`=228.7ms、`content:import`=172.4ms、`dm:today`=94.0ms、`hello world`=63.2ms。所有 lockWait 均为 0ms。

相比 17:18，本轮前台整体仍未出现正式慢查询，但中高位尾部重新抬升：`content:import` 从 141.9ms 升至 172.4ms，`path:/Users` 从 209.8ms 升至 228.7ms，`中文测试` 从 270.4ms 升至 283.1ms，`hello world` 从 47.2ms 升至 63.2ms。后台结构性风险继续恶化：recordCount=6,266,883，liveRecordCount=5,314,105，tombstone=952,778（15.20%），较上一轮增加 7,196；最新 session `FLUSH=68`，`SKIPFLUSH=0`，最新 flush total=4086ms，并新增 17:23 的 5320ms flush。

| 指标 | R_2606131718 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 270.4ms | **283.1ms** | ↑5% |
| P50 | ≈17.9ms | **≈24.9ms** | ↑39% |
| P90 | ≈141.9ms | **≈228.7ms** | ↑61% |
| lockWait max | 0.0ms | **0.0ms** | 持平 |
| 极佳比例(<50ms) | 11/16 | **11/16** | 持平 |
| 扫描/过滤路径总和 | 710.6ms | **811.5ms** | ↑14% |
| recordCount | 6,259,184 | **6,266,883** | +7,699 |
| liveRecordCount | 5,313,602 | **5,314,105** | +503 |
| tombstone | 945,582 (15.11%) | **952,778 (15.20%)** | +7,196 |
| 最新 session FLUSH | 62 | **68** | +6 |
| 最新 session QUERYADV_GE500 | 0 | **0** | 持平 |
| 最新 flush total | 3491ms | **4086ms** | ↑17% |
| session 最大 flush total | 9092ms | **9092ms** | 持平 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 0.2 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 26.4 | 23.8 | advanced-trigram | 0.0 | 10158 | 极佳 |
| `main.cpp` | 5 | 15.2 | 8.6 | advanced-trigram | 0.0 | 103 | 极佳 |
| `*.swift` | 5 | 16.2 | 15.4 | advanced-trigram | 0.0 | 9622 | 极佳 |
| `ext:cpp` | 5 | 8.9 | 8.7 | advanced-ext-index | 0.0 | 3891 | 极佳 |
| `size:>100m` | 5 | 33.3 | 33.2 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 63.2 | 62.1 | advanced-trigram | 0.0 | 3932 | 正常，较上一轮小幅抬升 |
| `abc def ghi jkl mno pqr` | 0 | 39.9 | 39.8 | advanced-trigram | 0.0 | 14624 | 极佳，较上一轮回落 |
| `README` | 5 | 7.9 | 5.4 | advanced-trigram | 0.0 | 17589 | 极佳 |
| `test` | 5 | 23.4 | 20.5 | advanced-trigram | 0.0 | 118022 | 极佳 |
| `MacEverything` | 5 | 4.3 | 2.4 | advanced-trigram | 0.0 | 7040 | 极佳 |
| `dm:today` | 5 | 94.0 | 93.8 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 172.4 | 161.4 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常高位，较上一轮抬升 |
| `path:/Users` | 5 | 228.7 | 220.9 | advanced-linear-gcd | 0.0 | 0 | 正常高位，较上一轮抬升 |
| `中文测试` | 0 | 283.1 | 280.0 | advanced-linear-gcd | 0.0 | 0 | 正常高位，本轮最高 |
| `verylongquery...` | 0 | 1.9 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈24.9ms，P90≈228.7ms，Max=283.1ms。0/16 查询 >500ms。lockWait max=0.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **11/16** | 0.2-39.9ms | short-query-cache、轻量 trigram、ext-index 与轻量 pure-filter 仍健康 |
| 正常（50-500ms） | **5/16** | 63.2-283.1ms | trigram、pure-filter、linear-gcd 均有中高位项 |
| 超标（>500ms） | **0/16** | — | 本轮无正式超标查询 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,266,883；
- liveRecordCount=5,314,105；
- tombstone=952,778（**15.20%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=12287
SESSION_START_TS=2026-06-13 12:03:04.944
TOTAL_LINES=12768
SEG_LINES=482
QUERYADV=47
QUERYADV_GE500=0
FLUSH=68
SKIPFLUSH=0
COMPACTION=2
LAST_QUERYADV=[2026-06-13 17:48:03.583] [INFO ] [SearchEngine] QueryAdvanced "中文测试" total=280ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6266884
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 0，本轮没有日志级慢查询。`中文测试` 的日志 total=280ms，与 query battery 的 283.1ms 对齐；但 P90 回升至约 228.7ms，说明用户可感知的中高位尾部仍在 scan/filter 与 linear-gcd 路径聚集。

```text
[2026-06-13 17:48:03.583] QueryAdvanced "中文测试" total=280ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6266884
```

### flush 情况

最新 session 内 `FLUSH=68`，rewrite 范围仍覆盖秒级完整重写，total 最大 **9092ms**。`SKIPFLUSH=0`，说明完整 flush 从上一轮 62 次继续累积到 68 次；最新 flush total=4086ms，并在 17:23 记录到新的 5320ms flush。后台没有看到可解释的退避或跳过行为。

最新 flush：

```text
[2026-06-13 17:43:25.257] Flushed v6 flat index, lastEventId=362938772,
liveRecords=5314073 | timing: walSwap=0ms rewrite=4085ms total=4086ms
```

Top flushes：

```text
[2026-06-13 16:43:30.341] rewrite=9087ms total=9092ms
[2026-06-13 12:38:27.299] rewrite=3809ms total=6164ms
[2026-06-13 16:23:26.885] rewrite=5714ms total=5716ms
[2026-06-13 16:28:26.841] rewrite=5670ms total=5672ms
[2026-06-13 17:23:26.451] rewrite=5318ms total=5320ms
```

## 6. 问题清单

### P0：tombstone 继续突破 950k，比例升至 15.20%

- 证据：tombstone=952,778（15.20%），较上一轮增加 7,196。
- 影响：无效记录比例继续升高，会放大 scan/filter、linear-gcd 与 full-index rewrite 的实际工作量。
- 判断：前台没有 >500ms 慢查询不代表根因缓解；tombstone 与 flush 仍在同步恶化。

### P0：full-index flush 继续每轮 +6 且没有 backoff

- 证据：`FLUSH=68`，较上一轮 +6；`SKIPFLUSH=0`；最新 flush total=4086ms，session 最大 total=9092ms。
- 影响：秒级完整重写持续制造 I/O 和 page-cache 干扰，可能解释 P90 与 scan/filter 总和重新抬升。
- 判断：后台持久化策略仍是当前最高优先级风险。

### P0：linear-gcd 仍稳定占据最高尾部

- 证据：`中文测试`=283.1ms，phase2=280.0ms；`path:/Users`=228.7ms，phase2=220.9ms。
- 影响：中文与路径类查询仍明显高于轻量 trigram 和 ext-index，属于用户可感知延迟。
- 判断：lockWait=0ms，瓶颈在 phase2 扫描/过滤本身，不是锁等待。

### P1：pure-filter 中高位重新抬升

- 证据：`content:import` 从 141.9ms 升至 172.4ms，`dm:today`=94.0ms。
- 影响：pure-filter 虽未超过 linear-gcd，但仍是 P90 前后的稳定成本来源。

### P1：trigram 整体健康但仍受系统状态波动

- 证据：`hello world`=63.2ms，较上一轮 47.2ms 小幅抬升；`abc def ghi jkl mno pqr` 回落至 39.9ms。
- 影响：trigram 没有复现 16:48 的 223.1ms 高点，但仍需要解释 flush 后 page-cache/I/O 对 postings 路径的扰动。

## 7. 优化建议

### P0：为 full-index flush 增加硬性前台保护与退避

- 对 flush rewrite 超过 2s/5s/8s 建立分级退避：延后下一次 flush、降低 batch 频率、前台查询活跃时禁止大 rewrite。
- 持久化线程使用低 QoS，并评估大顺序写的 `F_NOCACHE` 或同类策略，减少对搜索热页的冲刷。
- 在日志中记录触发、跳过和退避原因，避免 `SKIPFLUSH=0` 长期无法解释。
- 验收：出现 5s+ flush 后下一周期必须看到 backoff 或 skip 证据；常规查询 P90 不再随 flush 抬升。

### P0：将 full-index flush 改为增量或 COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页或持锁窗口。
- 验收：常规 flush total <200ms，新 session 不再出现 3-9s 级完整 rewrite。

### P0：提前或分段 compaction，先控制 tombstone 增长

- 以 tombstone 率 14%+ 或绝对 tombstone 900k+ 作为明确触发信号；当前 15.20% 已超过应急阈值。
- 结合 segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 优先处理 tombstone 密集 segment，避免 scan/filter 与 flush rewrite 继续放大。
- 验收：tombstone 率从 15.20% 回落；`中文测试`、`path:/Users`、`content:import` 稳定 <200ms。

### P0：为 advanced-linear-gcd 与 pure-filter 增加轻量剪枝

- 对中文、路径、content/filter 类查询记录扫描记录数、tombstone 跳过数和 segment 分布。
- 评估轻量字符 n-gram、路径分段索引、预过滤位图或 segment-level skip list，以减少无结果或低选择性查询的全量扫描。
- 要求保持内存增长可控，优先使用压缩或按需结构，不引入大常驻索引。
- 验收：`中文测试` 与 `path:/Users` 稳定低于 200ms，`content:import` 不再维持 150ms+。

### P1：细分 trigram/postings 与 scan/filter timing

- 对 `advanced-trigram` 记录 postings 读取、候选交并、候选评分、路径加载、结果组装和 page-cache miss 近似指标。
- 对 pure-filter 和 linear-gcd 记录扫描记录数、跳过 tombstone 数、命中 live 数、segment 数、metadata 加载耗时和结果组装耗时。
- 对所有路径增加 queueWait、candidate materialization、sort/rank、HTTP serialization/write 等 top-level timing。
- 验收：`queryTimeMs - phase2Ms - lockWaitMs` 不再是不可解释的大黑盒。

## 8. 下一轮重点

1. tombstone 是否继续从 952,778 上行并逼近 1,000,000。
2. `FLUSH` 是否继续每 30 分钟 +6。
3. `SKIPFLUSH` 是否仍为 0。
4. 是否继续出现 5s+ flush，尤其是 17:23 后是否还有新 outlier。
5. `中文测试` 是否继续维持 280ms+ 或重新接近 400ms。
6. `path:/Users` 是否能回落到 200ms 以下。
7. `content:import` 是否继续从 170ms 上行。
8. 新 session 中 `QUERYADV_GE500` 是否开始累积。

## 9. 结论

**本轮没有正式慢查询：0/16 >500ms，Max=283.1ms，P50≈24.9ms。** 但 P90 回升到约 228.7ms，`中文测试`、`path:/Users`、`content:import` 构成主要中高位尾部，且瓶颈几乎全部落在 phase2，而不是 lockWait。

**后台根因继续恶化：tombstone=952,778（15.20%），最新 session `FLUSH=68`，最大 flush total=9092ms，最新 flush total=4086ms，且 `SKIPFLUSH=0`。** 下一步最高优先级仍是 full-index flush 前台保护/退避与增量/COW flush，同时推进提前/分段 compaction、advanced-linear-gcd/pure-filter 剪枝，以及更细粒度 timing 拆分。
