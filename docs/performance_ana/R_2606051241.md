# MacEverything 性能分析报告 R_2606051241

## 1. 本轮范围

- 任务启动时间：2026-06-05 12:41
- 报告文件：`docs/performance_ana/R_2606051241.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=25825`
  - `SESSION_START_TS=2026-06-05 11:47:42.275`
  - `TOTAL_LINES=25881`
  - `SEG_LINES=57`
  - `LAST_LOG_TS=2026-06-05 12:41`

## 2. 执行摘要

**崩溃恢复后首轮稳态监控——0/16 >500ms，P50=29ms，Max=337ms，lockWait 仅 1/16（14ms）。** 系统在 R138 崩溃重启后快速恢复至优秀水平。tombstone 从 1.43%（73K）微升至 1.88%（97K），增速 ~783/min（白天正常低值）。flush rewrite 从初期的 3-11s 回落至 3.0-7.3s，正在向正常水平收敛。

### 关键指标对比

| 指标 | R138 上轮 | R139 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（极佳） |
| Max | 389ms | **337ms** | **↓ 改善** |
| P50 | 33ms | **29ms** | **↓ 改善** |
| lockWait max | 0ms | **14ms** | ↑ 微量（仅 1/16） |
| tombstone | 73,533 (1.43%) | **97,125 (1.88%)** | ↑ 正常增长 |
| tombstone 增速 | — | **~783/min** | 白天正常低值 |
| recordCount | 5,144,577 | **5,169,595** | +25K（正常） |
| liveRecordCount | 5,071,044 | **5,072,470** | +1.4K |
| FLUSH 累计 | 4 | **10** | +6（正常） |
| SKIPFLUSH | 0 | **0** | 持平 |
| flush rewrite | 3.3-10.9s | **3.0-7.3s** | **↓ 改善中** |
| COMPACTION | 0 | **2** | +2（auto-compaction 启动） |
| QUERYADV_GE500 | 0 | **0** | 持平（完美） |

### Tombstone 增速——白天正常低值

- R138 (12:11): 73,533 (1.43%)
- R139 (12:41): 97,125 (1.88%)
- **增量 +23,592 / 30 min = ~783/min**
- **对比崩溃前的事件风暴（R135=10,449/min, R137=8,942/min）——大幅回落。**
- **白天正常低值——事件风暴已完全消退。**
- **按当前增速达到 25%**：还需 ~1,190,000 / 783 ≈ **1,520 分钟 ≈ ~25 小时**。
- **下一次 compaction 非常遥远——至少明天中午之后。**

### Flush rewrite 持续恢复

```text
最近 5 次：2958ms  4398ms  3170ms  3258ms  7335ms
中位数：~3.3s
```

- **从 R138 的 3.3-10.9s 回落至 3.0-7.3s——中位数从 7.7s 降至 3.3s（↓57%）。**
- **2,958ms 已接近正常水平（~2.5s）。**
- **7,335ms 峰值仍偏高——但总体趋势向好。**
- **原因**：新会话稳定后每次 flush 增量减小 → rewrite 数据量更少。

### lockWait 分析——极轻微

| Query | lockWaitMs | queryTimeMs | 说明 |
|-------|----------:|------------:|------|
| `hello world` | 14 | 39ms | lockWait 占 36%，但绝对值极低 |

- **仅 1/16 查询出现 lockWait，且仅 14ms——完全不影响用户体验。**
- **恰好与一次 flush 时间窗口重叠——概率事件。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 2 | 5.2 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 32.0 | 15.2 | advanced-trigram | 0 | 9878 | 极佳 |
| `main.cpp` | 5 | 11.6 | 2.9 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 15.4 | 11.8 | advanced-trigram | 0 | 9325 | 极佳 |
| `ext:cpp` | 5 | 18.0 | 17.4 | advanced-ext-index | 0 | 3569 | 极佳 |
| `size:>100m` | 5 | 27.6 | 27.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 38.9 | 22.2 | advanced-trigram | 13.9 | 3620 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 42.9 | 41.9 | advanced-trigram | 0 | 14126 | 极佳 |
| `README` | 5 | 20.7 | 12.5 | advanced-trigram | 0 | 16887 | 极佳 |
| `test` | 5 | 44.1 | 32.2 | advanced-trigram | 0 | 116155 | 极佳 |
| `MacEverything` | 5 | 14.4 | 0.7 | advanced-trigram | 0 | 6796 | 极佳 |
| `dm:today` | 5 | 68.1 | 68.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 258.4 | 244.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（↓33% vs R138） |
| `path:/Users` | 5 | 241.2 | 235.6 | advanced-linear-gcd | 0 | 0 | 正常（↑15% vs R138） |
| `中文测试` | 0 | 337.3 | 331.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 1.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 29.0ms，P90 = 258.4ms，Max = 337.3ms。0/16 查询 >500ms。lockWait max = 13.9ms（仅 1/16 查询受影响）。**

### 与 R138 对比——全面稳定

| Query | R138 | R139 | 变化 | 说明 |
|-------|-----:|-----:|------|------|
| `.pdf` | 25ms | **32ms** | ↑ 28% | 正常波动 |
| `content:import` | 389ms | **258ms** | **↓ 33%** | page cache 预热 |
| `path:/Users` | 210ms | **241ms** | ↑ 15% | page cache 零和 |
| `中文测试` | 284ms | **337ms** | ↑ 19% | page cache 零和 |
| `test` | 52ms | **44ms** | ↓ 15% | 改善 |

- **`content:import` 从 389ms 降至 258ms（↓33%）**——page cache 逐步预热。
- **`path:/Users` 和 `中文测试` 微升**——page cache 零和效应（content:import 预热 → 挤占其他路径）。
- **全表扫描路径总和**：R138=884ms → R139=837ms（↓5%）——总体改善。

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R138 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 11/16 | 2-44ms | R138=10/16（**↑1**） |
| **正常**（50-500ms） | 5/16 | 68-337ms | R138=6/16（↓1） |
| **超标**（>500ms） | 0/16 | — | R138=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,169,595，liveRecordCount=5,072,470（tombstone=**97,125**，**1.88%**）；
- 进程正常运行（新会话，运行 ~54 分钟）。

## 5. 最新日志段分析

```text
WARN=1（FSEvents replay failed — 启动时）
ERROR=0
QUERYADV=6  QUERYADV_GE500=0
FLUSH=10  COMPACTION=2  SKIPFLUSH=0
```

- QUERYADV=6（+3 本轮）。
- **QUERYADV_GE500=0**——连续两轮 0 个慢查询。
- **FLUSH=10（+6）**——频率正常（~120/小时）。
- **COMPACTION=2**——auto-compaction 在 11:56 启动，已完成初始 compaction 检查。
- SKIPFLUSH=0。

## 6. 问题清单

### 无活跃问题——系统处于崩溃恢复后的稳态

- 0/16 >500ms，lockWait 微量（14ms，1/16）。
- tombstone 1.88%——远低于 25% 阈值。
- flush rewrite 中位数 3.3s——正在向 ~2.5s 正常水平收敛。

### 持续关注：flush rewrite 7.3s 峰值

- 偶发的 7s+ flush rewrite 仍然存在——可能在高负载时引发 lockWait。
- **但当前频率和幅度远低于崩溃前——暂不需要干预。**

### 持续关注：compaction rewrite 崩溃根因（R138 发现）

- 上一会话的崩溃根因尚未确认——需要代码层面排查。
- **当前会话 tombstone 仅 1.88%，距 25% compaction 约 25 小时——短期内无崩溃风险。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（R138 新增，不变）

- 虽然短期内不会再次触发 compaction，但根因未解决。
- 建议在 IndexPersistence rewrite 步骤添加内存/磁盘监控日志。

### P0：flush rewrite 改为 COW 方式（持续建议）

- flush rewrite 仍是唯一的 lockWait 来源。
- 7.3s 峰值时任何查询都可能被阻塞。

### P1：降低 compaction 阈值至 15%

- 25% 阈值导致 tombstone 大量堆积 → flush rewrite 膨胀 → 间接导致崩溃。
- 15% 更安全——compaction 更频繁但更轻量。

## 8. 下一轮重点

1. flush rewrite 是否继续向 ~2.5s 收敛。
2. `content:import` page cache 预热进展。
3. tombstone 增速是否维持低位。
4. 系统稳定性——无崩溃。

## 9. 结论

**崩溃恢复后首轮稳态监控——0/16 >500ms，P50=29ms，Max=337ms，lockWait 仅 14ms（1/16）。** 系统在 R138 崩溃重启后快速恢复至优秀水平。11/16 查询 <50ms，全表扫描路径稳步预热（content:import 从 389ms↓258ms）。

**tombstone 1.88%（97K），增速 783/min（白天正常低值），距 25% compaction 约 25 小时。** flush rewrite 中位数 3.3s（从 R138 初期的 7.7s 大幅改善），正在向 ~2.5s 正常水平收敛。

**系统处于稳定恢复阶段——无活跃问题。** P0 建议（compaction 崩溃根因排查 + COW flush）仍待实施，但短期内无触发风险。下一轮重点：flush rewrite 是否完全恢复至正常水平。
