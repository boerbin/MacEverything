# MacEverything 性能分析报告 R_2606091130

## 1. 本轮范围

- 任务启动时间：2026-06-09 11:30
- 报告文件：`docs/performance_ana/R_2606091130.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5743`
  - `SEG_LINES=4801`
  - `LAST_LOG_TS=2026-06-09 02:17`（注意：最后非 battery 日志仍在 02:17）

## 2. 执行摘要

**连续第 3 轮系列顶尖——0/16 >500ms，Max=381ms，P50=37ms，lockWait=0，极佳比例 12/16（新纪录！）。** 稳态持续。`dm:today` 回落至 46ms（极佳），全部 trigram 查询连续第 3 轮 <50ms。全表扫描总和微升至 974ms——线性扫描区域（`path:/Users`=379ms，`中文测试`=381ms）受上午 I/O 轻度影响。

| 指标 | R202 | R203 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平（连续 3 轮） |
| Max | 325ms | **381ms** | ↑17%（波动） |
| P50 | 22ms | **37ms** | ↑68%（波动） |
| lockWait max | 0ms | **0ms** | 完美（连续 5 轮） |
| 极佳比例(<50ms) | 10/16 | **12/16** | **↑2（新纪录！）** |
| tombstone | 1,748,016 (25.30%) | **1,754,182 (25.37%)** | ↑（持续增长） |
| tombstone 增速 | ~211/min | **~205/min** | ↓3%（稳定） |
| recordCount | 6,909,037 | **6,915,259** | +6.2K |
| liveRecordCount | 5,161,021 | **5,161,077** | +56 |
| FLUSH 累计 | 1 | **1** | **不变** |
| SKIPFLUSH | 4315 | **4374** | +59 |
| COMPACTION | 2 | **2** | **不变——第 4 轮连续确认缺陷** |
| QUERYADV_GE500 | 41 | **41** | 不变（0 新增，连续 3 轮） |

### 极佳比例 12/16——新纪录

| 轮次 | 极佳比例 | 备注 |
|------|--------:|------|
| R201 | 11/16 | 此前纪录 |
| R202 | 10/16 | dm:today=102ms |
| **R203** | **12/16** | **新纪录！** |

- **12/16 查询 <50ms——仅 `content:import`(214ms)、`path:/Users`(379ms)、`中文测试`(381ms)、`verylongquery`(0.3ms) 不在此列（`verylongquery` 实际 0.3ms 也是极佳，但它因 0 results 直接短路）。**
- **实际上 13/16 <50ms（含 `verylongquery`=0.3ms）。**

### Trigram 路径——连续第 3 轮全部极佳

| Query | R202 | R203 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 22ms | **27ms** | ↑23% | 极佳 |
| `hello world` | 35ms | **36ms** | ↑3% | 极佳 |
| `*.swift` | 13ms | **23ms** | ↑77% | 极佳 |
| `main.cpp` | 6ms | **4ms** | ↓33% | 极佳 |
| `abc def...` | 24ms | **41ms** | ↑71% | 极佳 |
| `README` | 7ms | **23ms** | ↑229% | 极佳 |
| `test` | 17ms | **40ms** | ↑135% | 极佳 |
| `MacEverything` | 5ms | **9ms** | ↑80% | 极佳 |
| Trigram 均值 | 16ms | **25ms** | ↑56% | 全部极佳 |

- **8/8 trigram 全部 <50ms——连续第 3 轮。**
- **均值 16→25ms（↑56%）——微升但仍极佳。**
- **`README`=23ms（↑229%）和 `test`=40ms（↑135%）——上午 I/O 压力导致轻度波动，但都仍在极佳范围。**

### 全表扫描——线性扫描区域受压

| Query | R202 | R203 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 195ms | **214ms** | ↑10% |
| `path:/Users` | 218ms | **379ms** | ↑74% |
| `中文测试` | 325ms | **381ms** | ↑17% |
| **总和** | **738ms** | **974ms** | **↑32%** |

- **全表扫描总和 738→974ms（↑32%）——线性扫描区域受上午 I/O 影响。**
- **`path:/Users`=379ms（↑74%）——路径数组区域 page cache 被部分淘汰。**
- **`中文测试`=381ms（↑17%）——名称数组区域同步受压。**
- **但仍远低于灾难级别（R199=6155ms）——正常日间波动上端。**

### tombstone 25.37%——第 4 轮确认 compaction 缺陷

| 轮次 | tombstone% | 超阈值 | COMPACTION | FLUSH |
|------|----------:|-------:|----------:|------:|
| R200 | 25.15% | +0.15% | 2 | 1 |
| R201 | 25.23% | +0.23% | 2 | 1 |
| R202 | 25.30% | +0.30% | 2 | 1 |
| **R203** | **25.37%** | **+0.37%** | **2** | **1** |

- **tombstone 连续 4 轮超过 25% 阈值——从 25.15% 增至 25.37%。**
- **COMPACTION 始终为 2——没有任何 compaction 被触发。**
- **增速 ~205/min（稳定日间水平）。**
- **按此速度，每小时增长 ~0.18 个百分点——1 天后约 29.7%。**
- **死记录浪费的内存将持续增长直到进程重启或 flush 实际执行。**

### `x`=1.9ms——进程状态极佳

- **R199=85ms → R200=4.6ms → R201=2.9ms → R202=8.5ms → R203=1.9ms。**
- **1.9ms 是系列近期最低——进程调度延迟接近 0。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1.9 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 27.0 | 25.6 | advanced-trigram | 0 | 9842 | 极佳 |
| `main.cpp` | 5 | 4.2 | 2.8 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 22.7 | 21.5 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 7.5 | 6.2 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 43.3 | 43.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 36.4 | 34.0 | advanced-trigram | 0 | 3669 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 40.6 | 40.4 | advanced-trigram | 0 | 14344 | 极佳 |
| `README` | 5 | 22.5 | 21.2 | advanced-trigram | 0 | 17006 | 极佳 |
| `test` | 5 | 40.3 | 37.5 | advanced-trigram | 0 | 116884 | 极佳 |
| `MacEverything` | 5 | 8.9 | 0.8 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 46.4 | 46.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 213.7 | 199.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 378.8 | 367.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 381.0 | 380.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.3 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 36.4ms，P90 = 378.8ms，Max = 381.0ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R202 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **12/16** | 0.3-46ms | R202=10/16（**↑2，新纪录！**） |
| **正常**（50-500ms） | 4/16 | 214-381ms | R202=6/16（↓2） |
| **超标**（>500ms） | **0/16** | — | R202=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,915,259，liveRecordCount=5,161,077（tombstone=**1,754,182**，**25.37%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=335  QUERYADV_GE500=41（不变——连续 3 轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4374
```

## 6. 问题清单

### P0 确认（第 4 轮）：compaction 触发机制缺陷

- **tombstone 25.37%（超阈值 0.37%），COMPACTION 仍为 2。**
- **按 ~205/min 增速，每天增长 ~4.3 个百分点。**
- **如果不修复，1 周后 tombstone 将达 ~55%——一半以上记录为死记录。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 4 轮确认——最高优先级）

- **在 skipflush 路径中增加 tombstone 阈值检查。**
- **或：添加独立的 compaction timer（每 N 分钟检查一次 tombstone 比例）。**
- **或：降低 flush entries 阈值（从 100 降至 10 或 1），使 flush 更频繁执行。**

### P0.5：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. 全表扫描线性区域是否从 ~380ms 回落。
2. tombstone 增长趋势和 COMPACTION/FLUSH 计数。
3. 稳态是否维持。

## 9. 结论

**连续第 3 轮系列顶尖——0/16 >500ms，Max=381ms，P50=37ms，极佳比例 12/16（新纪录）。** Trigram 全部 <50ms（连续第 3 轮），`x`=1.9ms（进程状态极佳）。线性扫描区域受上午 I/O 轻度影响（`path:/Users`=379ms，`中文测试`=381ms），但仍远低于灾难级别。

**⚠️ compaction 触发缺陷连续第 4 轮确认——tombstone 25.37%（超阈值 0.37%），COMPACTION=2，FLUSH=1。** 按当前增速（~205/min），每天增长 ~4.3 个百分点。如不修复，tombstone 将持续增长直到进程重启。这是当前唯一的 P0 问题——查询性能已处于系列最优，但底层数据膨胀问题必须通过代码修复解决。
