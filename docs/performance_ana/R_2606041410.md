# MacEverything 性能分析报告 R_2606041410

## 1. 本轮范围

- 任务启动时间：2026-06-04 14:10
- 报告文件：`docs/performance_ana/R_2606041410.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=22122`
  - `SEG_LINES=1096`
  - `LAST_LOG_TS=2026-06-04 14:11`

## 2. 执行摘要

**I/O 压力回迁 trigram 路径。** `*.swift`=518ms（>500ms，trigram 路径再次受影响）。`.pdf`=342ms（偏高但 <500ms）。linear-gcd 路径改善（`中文测试`=295ms，R97 的 563ms 下降 48%）。**1/16 查询 >500ms。lockWaitMs 最高 0.7ms（极轻微）。** P50 ≈ 155ms，Max=518ms。

**I/O 压力游走模式再次确认**：R92-R94 压力在 trigram 路径 → R95-R97 转移到 linear-gcd → **R98 回迁到 trigram 路径**。压力在两个路径间交替，反映 tombstone 8.73% 下页面缓存两个区域无法同时维持。

**tombstone=549,520（8.73%，165MB），增量 +21,994**——**日间加速增长**，仅次于 R92 的 +25,546。

### 横向对比（最近 5 轮）

| Query | R94 | R95 | R96 | R97 | R98 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 11 | 18 | 1526 | 5 | **11** | 正常 |
| `.pdf` | 630 | 375 | 68 | 113 | **342** | 偏高 |
| `*.swift` | 226 | 264 | 58 | 80 | **518** | **>500ms** |
| `hello world` | 240 | 210 | 118 | 205 | **312** | 偏高 |
| `README` | 1071 | 31 | 13 | 36 | **63** | 正常 |
| `path:/Users` | 218 | 394 | 83 | 458 | **379** | 偏高 |
| `中文测试` | 391 | 573 | 50 | 563 | **295** | **改善** |
| P50 | 171 | 147 | 49 | 113 | **155** | 偏高 |

### I/O 压力游走完整追踪

| 阶段 | 轮次 | 受影响路径 | 受影响查询 | 说明 |
|------|------|-----------|-----------|------|
| 第 1 波 | R92-R94 | trigram | `.pdf`, `README` | 日间 I/O 压力 |
| 转移 | R95, R97 | linear-gcd | `中文测试`, `path:/Users` | 缓存区域切换 |
| 恢复 | R96 | — | — | 热缓存 |
| **第 2 波** | **R98** | **trigram** | **`*.swift`** | **压力回迁** |

**游走机制**：当 R97 的查询电池预热了 linear-gcd 数据后，这些页面占用了缓存空间，挤出了 trigram 索引区域的冷页面。下一轮（R98）trigram 查询遇到冷页面而 linear 查询因预热而正常——**零和博弈**。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 10.9 | 0.0 | short-query-cache | 0 | 0 | 正常 |
| `.pdf` | 5 | 342.1 | 331.4 | advanced-trigram | 0 | 9952 | 偏高 |
| `main.cpp` | 5 | 31.1 | 21.4 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 518.1 | 503.7 | advanced-trigram | 0 | 9356 | **>500ms** |
| `ext:cpp` | 5 | 301.6 | 298.7 | advanced-ext-index | 0.7 | 3701 | **偏高** |
| `size:>100m` | 5 | 147.8 | 143.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 312.3 | 309.0 | advanced-trigram | 0 | 3613 | 偏高 |
| `abc def ghi jkl mno pqr` | 0 | 155.8 | 148.8 | advanced-trigram | 0 | 16038 | 正常 |
| `README` | 5 | 63.1 | 54.0 | advanced-trigram | 0 | 17671 | 正常 |
| `test` | 5 | 64.9 | 51.3 | advanced-trigram | 0 | 130121 | 正常 |
| `MacEverything` | 5 | 24.6 | 2.8 | advanced-trigram | 0 | 6803 | 正常 |
| `dm:today` | 5 | 60.9 | 58.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 240.2 | 230.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 378.6 | 370.1 | advanced-linear-gcd | 0 | 0 | 偏高 |
| `中文测试` | 0 | 294.6 | 259.7 | advanced-linear-gcd | 0 | 0 | 改善 |
| `verylongquery...` | 0 | 1.5 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 155ms，P90 ≈ 379ms，Max = 518ms。1/16 查询 >500ms。lockWaitMs 最高 0.7ms。**

### `*.swift`=518ms 分析

- 历史数据：R92=430ms, R93=22ms, R94=226ms, R95=264ms, R96=58ms, R97=80ms, **R98=518ms**。
- candidates=9,356, phase2Ms=504ms——大量候选集 + 冷 trigram 索引页面。
- 与 `.pdf`（candidates=9,952, 342ms）对比：`*.swift` 候选集略少但耗时更高——说明 `*.swift` 涉及的特定 trigram 索引区域比 `.pdf` 更冷。

### `ext:cpp`=302ms + lockWait=0.7ms 分析

- 正常值 10-96ms，本轮 302ms——**3 倍偏差**。
- lockWait=0.7ms（极轻微），phase2Ms=299ms——几乎全部是 phase2 扫描开销。
- ext-index 路径通常极快（直接从扩展名索引查找），302ms 表明 ext-index 页面也被部分换出。
- **新信号**：I/O 压力开始影响 ext-index 路径——之前 38 轮中 ext-index 从未受影响。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,293,734，liveRecordCount=5,744,214（tombstone=**549,520**，**8.73%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=361  QUERYADV_GE500=50
FLUSH=0  COMPACTION=2  SKIPFLUSH=430
DIAG_TRIGRAM_SKIP=106
```

- QUERYADV 从 352 增至 361（+9）。
- QUERYADV_GE500 从 49 增至 50（**+1，即 `*.swift`=518ms**）——**里程碑：第 50 次 >=500ms 查询**。
- SKIPFLUSH 从 418 增至 430（**+12，恢复稳态**）。
- DIAG_TRIGRAM_SKIP 从 105 增至 106（+1，稳态）。

### QUERYADV_GE500=50 里程碑

- 50 次 >=500ms 查询分布：
  - 前 44 次：R60-R91（31 轮，1.42 次/轮）
  - **后 6 次：R92-R98（7 轮，0.86 次/轮）**
- R92 以来近 7 轮贡献了 6 次——**频率接近每轮 1 次 >=500ms 查询**。

## 6. 问题清单

### Critical：tombstone 8.73%（549,520 条，165MB），日间加速增长

- 增长轨迹：
  ```
  R95: 510K (+16.3K) → R96: 520K (+9.2K) → R97: 528K (+7.9K)
  → R98: 550K (+22.0K)
  ```
- **本轮 +22.0K——日间加速**，与 R92 的 +25.5K 同级别。
- **累计内存开销**：~550K × 300 字节 ≈ **165MB**。
- 连续第 38 轮超 2%，第 19 轮超过 5%，第 14 轮超过 6%，第 11 轮超过 7%，第 4 轮超过 8%。
- **按 +15K/轮（含日间加速）预测**：+5 轮 ~625K（9.5%，188MB），+10 轮 ~700K（10.4%，210MB）。

### Critical：I/O 压力游走模式——全路径受影响

- **新发现**：ext-index 路径首次受影响（`ext:cpp`=302ms，正常 10-96ms）。
- 此前仅 trigram 和 linear-gcd 两个路径交替受压，现在扩展到第 3 个路径。
- 意味着 165MB tombstone 的缓存挤压已开始影响**所有索引路径**。
- **唯一未受影响路径**：short-query-cache（O(1) 无 I/O）和 pure-filter-soa-gcd（SoA 布局高效）。

### High：>=500ms 查询频率趋近每轮 1 次

- R92-R98（7 轮）：6 次 >=500ms 查询（0.86 次/轮）。
- 对比 R60-R91（31 轮）：44 次（1.42 次/轮，含灾难事件）。
- **排除灾难事件后，R92-R98 的频率已超过历史正常水平**。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（8.73%，165MB，全路径受影响）

- **38 轮连续超标。** I/O 压力已扩展至全部 3 个索引路径（trigram, linear-gcd, ext-index）。
- **日间加速增长**（+22K/轮）加速恶化。
- compaction 释放 165MB → 所有索引路径缓存稳定 → 消除游走式 I/O 压力。

### P0 紧急：short-query-cache GCD 隔离（30.6% 异常率，峰值 1,526ms）
### P0 紧急：`size:>100m` 全表扫描超时保护
### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. I/O 压力游走方向（trigram 是否持续受压或回迁 linear-gcd）。
2. ext-index 路径是否继续偏高。
3. tombstone 增速是否维持日间加速水平。

## 9. 结论

**I/O 压力回迁 trigram 路径——游走模式再确认。** `*.swift`=518ms（>500ms），`.pdf`=342ms（偏高）。linear-gcd 改善（`中文测试`=295ms，R97 的 563ms 下降 48%）。**ext-index 路径首次受影响**（`ext:cpp`=302ms，正常 10-96ms）——I/O 压力从 2 个路径扩展至 3 个路径，标志 165MB tombstone 的缓存挤压已影响全部索引路径。1/16 查询 >500ms，lockWait=0.7ms。P50=155ms。tombstone=549,520（8.73%，165MB，+22.0K 日间加速）。QUERYADV_GE500 达到 50 次里程碑。**缓存空间的零和博弈**——trigram 预热挤出 linear 数据，linear 预热挤出 trigram 数据——只有 compaction 释放 165MB 才能打破这个死循环。
