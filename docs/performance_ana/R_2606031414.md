# MacEverything 性能分析报告 R_2606031414

## 1. 本轮范围

- 任务启动时间：2026-06-03 14:14
- 报告文件：`docs/performance_ana/R_2606031414.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21490`
  - `SEG_LINES=464`
  - `LAST_LOG_TS=2026-06-03 19:41`

## 2. 执行摘要

**I/O 压力大幅缓解，性能显著恢复**。P50 ≈ 171ms（前两轮 641ms/1131ms），大部分查询回到正常区间。lockWaitMs 基本归零（仅 `content:import`=0.5ms）。

但仍有 2 个残留问题：`.pdf`=502ms **首次正式突破 500ms 阈值**（此前最高 490ms），`path:/Users`=1370ms 仍显著偏高。

tombstone 突破 200K 和 3.5% 双重里程碑：212,906 条（3.58%），连续第 10 轮超过 2%。

### 横向对比（最近 5 轮）

| Query | R65 | R66 | R67 | R68 | R69 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 364 | 11 | 600 | 545 | **15** | **恢复正常** |
| `.pdf` | 446 | 329 | 10902 | 23901 | **502** | **首次 >500ms（正式）** |
| `*.swift` | 133 | 150 | 1101 | 1657 | **390** | 恢复 |
| `hello world` | 222 | 359 | 1644 | 863 | **269** | 恢复 |
| `path:/Users` | 203 | 269 | 3753 | 2221 | **1370** | 部分恢复 |
| P50 | 120 | 127 | 1131 | 641 | **171** | **大幅恢复** |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 15.3 | 0.0 | short-query-cache | 0 | 0 | 恢复正常 |
| `.pdf` | 5 | 502.3 | 480.7 | advanced-trigram | 0 | 9947 | **首次 >500ms** |
| `main.cpp` | 5 | 475.0 | 91.5 | advanced-trigram | 0 | 85 | 偏慢 |
| `*.swift` | 5 | 390.2 | 376.1 | advanced-trigram | 0 | 9356 | 恢复中 |
| `ext:cpp` | 5 | 121.8 | 119.7 | advanced-ext-index | 0 | 3702 | 正常 |
| `size:>100m` | 5 | 132.2 | 132.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 269.4 | 259.2 | advanced-trigram | 0 | 3613 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 138.7 | 136.8 | advanced-trigram | 0 | 15989 | 正常 |
| `README` | 5 | 79.7 | 40.6 | advanced-trigram | 0 | 17717 | 正常 |
| `test` | 5 | 93.2 | 45.9 | advanced-trigram | 0 | 129962 | 正常 |
| `MacEverything` | 5 | 115.9 | 1.0 | advanced-trigram | 0 | 6802 | 正常 |
| `dm:today` | 5 | 75.3 | 75.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 204.2 | 193.2 | advanced-pure-filter-soa-gcd | 0.5 | 0 | 正常 |
| `path:/Users` | 5 | 1369.9 | 1355.1 | advanced-linear-gcd | 0 | 0 | **仍严重偏慢** |
| `中文测试` | 0 | 265.2 | 263.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.7 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 171ms，P90 ≈ 502ms，Max = 1370ms。14/16 查询 <500ms。lockWaitMs 基本归零。**

### I/O 压力恢复进度

| 指标 | R67（峰值） | R68 | R69 本轮 | 恢复状态 |
|------|----------:|----:|--------:|---------|
| P50 | 1131ms | 641ms | **171ms** | 恢复 |
| Max | 10,902ms | 23,901ms | **1370ms** | 大幅改善 |
| >500ms 查询数 | 13/16 | 10/16 | **2/16** | 近乎恢复 |
| lockWaitMs max | 583ms | 2269ms | **0.5ms** | 恢复 |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,940,802，liveRecordCount=5,727,896（tombstone=**212,906**，**3.58%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=173  QUERYADV_GE500=23
FLUSH=0  COMPACTION=2  SKIPFLUSH=162
DIAG_TRIGRAM_SKIP=74
```

- QUERYADV 从 162 增至 173（+11）。
- QUERYADV_GE500 从 22 增至 23（+1，`.pdf` 502ms）。
- SKIPFLUSH 从 146 增至 162（+16，恢复正常增速）。
- DIAG_TRIGRAM_SKIP 从 73 增至 74（+1）。

## 6. 问题清单

### Critical：tombstone 突破 200K 和 3.5%（212,906 条，3.58%）

- 增长轨迹：
  ```
  R64: 160K (2.71%) → R65: 169K (2.87%) → R66: 185K (3.13%)
  → R67: 195K (3.29%) → R68: 199K (3.36%) → R69: 213K (3.58%)
  ```
- 本轮增量 13,683 条（I/O 压力缓解后增速恢复）。
- **累计内存开销**：~213K × 300 字节 ≈ 64MB 额外内存。
- **查询开销**：3.58% 额外 phase2 扫描——虽非 I/O 压力事件的根因，但在 I/O 压力下会放大退化。
- SKIPFLUSH=162，compaction 完全不可触发。连续第 10 轮超 2%。

### High：`.pdf` 首次正式突破 500ms（502ms）

- 21 轮追踪中首次 >500ms（此前最高 490ms R59）。
- candidates=9947，phase2=481ms。I/O 压力尾部残留效应。
- **结构性问题**：trigram 路径处理 ~10K candidates 在正常负载下约 80-300ms，但波动范围可达 500ms+。

### Medium：`path:/Users` 恢复缓慢（1370ms）

- R67=3753ms → R68=2221ms → R69=1370ms。持续改善但仍远超正常（164-380ms）。
- linear-gcd 全表扫描对系统缓存状态高度敏感。

### Medium：`main.cpp` 偏慢（475ms）

- 正常区间 12-108ms。phase2=92ms 正常，但总延迟 475ms。
- 额外 383ms 可能来自 trigram 查找阶段的 I/O 残留。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（连续 10 轮超标，突破 3.5%）

- tombstone 3.58%，**每轮浪费 ~64MB 内存和 3.58% phase2 扫描**。
- 最小实施方案：SKIPFLUSH 分支增加占比检查，>2% 触发 compaction。

### P0：实现 phase2 超时降级（承前轮——R68 证实 24s 阻塞）

### P0：将 `.pdf`/`*.swift` 路由到 ext-index（优先级升至 P0——`.pdf` 已突破 500ms）

- `.pdf` 首次 >500ms，而 `ext:cpp` 同样在 I/O 压力下仅 184ms。
- 路由到 ext-index 可将 `.pdf` 延迟降至 ~50-130ms，彻底消除 500ms 风险。

### P1：short-query-cache 诊断日志（承前轮——本轮恢复正常）

## 8. 下一轮重点

1. `.pdf` 是否回到 <500ms——本轮 502ms 是 I/O 残留还是新常态。
2. `path:/Users` 是否继续恢复。
3. tombstone 增速——是否继续以 ~13K/轮 增长。

## 9. 结论

I/O 压力大幅缓解，性能显著恢复（P50: 641→171ms），14/16 查询回到 <500ms，lockWaitMs 基本归零。`.pdf`=502ms **首次正式突破 500ms 阈值**，结构性修复（ext-index 路由）优先级升至 P0。`path:/Users`=1370ms 仍在恢复中。tombstone 突破 200K 和 3.5% 双重里程碑（212,906 条，3.58%），连续第 10 轮超 2%，SKIPFLUSH=162。R67-R68 的 I/O 压力事件暴露了三个系统性弱点：缺少 phase2 超时降级、`.pdf` 未走 ext-index、tombstone 持续积累放大 I/O 压力影响。
