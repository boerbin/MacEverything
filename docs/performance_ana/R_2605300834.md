# MacEverything 性能分析报告 R_2605300834

**时间**: 2026-05-30 08:34  
**索引规模**: 5,755,626 总记录 / 5,384,838 活跃记录  
**Tombstone**: 370,788 (**6.44%**)  
**日志**: session 起于 11:04:48（同一长会话，已运行 ~21.5h），WARN=0 / ERROR=0  
**轮次**: 第 81 轮（重启后第 29 轮，同一 11:04:48 长会话）  

---

## 🟢 新发现轮：页温度是「分区」的，非全局——trigram 候选区冷 / 线性扫描区暖 同轮并存

**前 7 轮（R75–R80）反复在追问线性/纯过滤路径的细微方差排序，结论是只有「绝对二层分裂」（纯数值 ≪ 字符串 full-eval）能扛过全部样本。本轮（08:34）提供一个此前未单独坐实的正交证据：页温度不是单一全局标量，而是按访问区域独立升降的。** 本轮电池里 trigram/ext-index 路径（散落候选行随机访问）明显转冷——`ext:cpp` phase2 **120.33ms**（2987 候选，40μs/候选）、`hello world` **173.64ms**、`*.swift` **100.2ms**、`abc…`(6词) **291.26ms**、`.pdf` **52.68ms**，五个查询 phase2>50ms；与此**同一电池、相隔数秒**，顺序扫描的线性/纯过滤路径却刷出**八轮最暖读数**——`content:import` **23.56 ns/槽**（历史新低）、`path:/Users` **25.74 ns/槽**（历史新低）、`中文测试` 33.38（接近历史低）。**散落候选区「冷」与顺序 SoA 扫描区「暖」在同一秒并存，方向相反**——这只能解释为：两区在 OS 页缓存里独立驻留、独立淘汰。trigram 走 posting list 点跳到分散的 SoA 行（随机、对淘汰敏感）；linear/pure-filter 顺序遍历 SoA 列（readahead 友好、易保持驻留）。**这把「冷缓存根因」从「全局页温度」精化为「分区页温度」，并强化 mlock/madvise P0：必须同时钉住散落候选行与顺序 SoA 列两个区——它们不会一起暖、也不会一起冷。** 绝对二层分裂本轮仍成立（8/8）：数值层 size 11.17 / dm 13.23 ≪ 字符串层 content 23.56 / path 25.74 / 中文 33.38（层间隙 1.78×，无重叠）。Tombstone 越过 **6.44%**（连涨 14 轮）；安全阀整会话 321 次 skip「78 entries」、0 触发、0 回收——R67 根因在 21.5h 持续硬证。

---

## 1. 本轮查询电池（08:34，limit=100，混合温度）

| 查询 | qTimeMs | totalMs | phase2Ms | trigMs | sortMs | lockHeldMs | candidates | results | searchPath |
|------|---------|---------|----------|--------|--------|-----------|-----------|---------|-----------|
| `"x"` | 37.79 | 2.66 | 0 | 0 | 0 | 0 | 0 | 99 | short-query-cache |
| `".pdf"` | 59.42 | 55.45 | 52.68 | 2.57 | 0.06 | 55.30 | 10,116 | 100 | advanced-trigram |
| `"main.cpp"` | 33.80 | 20.07 | 14.08 | 5.96 | 0.01 | 20.05 | **71** | 71 | advanced-trigram |
| `"*.swift"` | 109.31 | 103.14 | 100.20 | 2.90 | 0.02 | 103.11 | 9,213 | 100 | advanced-trigram |
| `"ext:cpp"` | 125.48 | 121.38 | **120.33** | 0 | 0.02 | 121.29 | 2,987 | 100 | advanced-ext-index |
| `"size:>100m"` | 81.59 | 64.28 | 64.26 | 0 | 0 | 64.26 | 0 | 100 | advanced-pure-filter-soa-gcd |
| `"hello world"` | 199.21 | 175.69 | **173.64** | 1.93 | 0.01 | 175.57 | 2,340 | 100 | advanced-trigram |
| `"abc…"`(6词) | 292.91 | 292.81 | **291.26** | 1.52 | 0 | 292.78 | 15,917 | 0 | advanced-trigram |
| `"README"` | 32.58 | 22.78 | 20.45 | 2.28 | 0.02 | 22.74 | 16,457 | 100 | advanced-trigram |
| `"test"` | 33.78 | 28.95 | 19.89 | 8.95 | 0.09 | 28.85 | **136,229** | 100 | advanced-trigram |
| `"MacEverything"` | 22.29 | 11.63 | 2.52 | 9.09 | 0.02 | 11.61 | 6,693 | 100 | advanced-trigram |
| `"dm:today"` | 81.45 | 76.17 | 76.12 | 0 | 0.01 | 76.12 | 0 | 100 | advanced-pure-filter-soa-gcd |
| `"content:import"` | 137.35 | 135.59 | 127.41 | 0 | 8.07 | 127.41 | 0 | 100 | advanced-pure-filter-soa-gcd |
| `"path:/Users"` | 152.73 | 148.13 | 145.36 | 0 | 2.74 | 145.37 | 0 | 100 | advanced-linear-gcd |
| `"中文测试"` | 192.60 | 192.15 | 191.81 | 0.24 | 0 | 192.13 | 0 | 0 | advanced-linear-gcd |
| `"verylong"`(43w) | 1.42 | 1.42 | 0 | 1.40 | 0 | 1.41 | 0 | 0 | advanced-trigram |

**本轮 trigram 侧偏冷**（5 个查询 phase2>50ms，`abc…` 291ms 最冷），**linear/pure-filter 侧偏暖**（content/path 刷新八轮新低）。lockHeld/total 仍 ~99%（结构性持锁全程，lockWait=0）。

---

## 2. 🔑 分区页温度：散落候选区 vs 顺序扫描区，同轮反向

把本轮 trigram phase2 与 R76 全暖基线对照，再看同轮 linear ns/槽：

| 查询 | 路径 | R76 暖 phase2 | R81 phase2 | 相对冷化 | 区域 |
|------|------|--------------|-----------|---------|------|
| `ext:cpp` | ext-index（散落候选） | 5.01ms | **120.33ms** | **24×** | 散落 |
| `hello world` | trigram（散落候选） | 13.81ms | **173.64ms** | 12.6× | 散落 |
| `*.swift` | trigram（散落候选） | 18.06ms | 100.20ms | 5.5× | 散落 |
| `abc…`(6词) | trigram（散落候选） | 33.60ms | 291.26ms | 8.7× | 散落 |
| `.pdf` | trigram（散落候选） | 14.42ms | 52.68ms | 3.7× | 散落 |
| `content:import` | pure-filter（顺序 SoA） | — | **23.56 ns/槽（新低）** | — | 顺序 |
| `path:/Users` | linear（顺序 SoA+串池） | — | **25.74 ns/槽（新低）** | — | 顺序 |

### 解读

- **散落候选区冷、顺序扫描区暖，在同一电池（数秒内）并存**——两区温度方向相反，证明页缓存驻留是**按区域**的，不是全局一个温度。
- **机制**：trigram/ext-index 经 posting list 点跳到分散 SoA 行（随机访问，单页只用几条记录，最易被淘汰）；linear/pure-filter 顺序遍历整列 SoA（OS readahead 友好 + 大块顺序触达，最易保持驻留）。隔夜散落候选页被淘汰，但顺序扫描区因 readahead/周期扫盘持续触达而留暖。
- **对 P0 的影响**：mlock/madvise **必须同时覆盖两区**（散落候选行 + 顺序 SoA 列/串池）。只钉一区，另一区仍会独立转冷——本轮就是「顺序区暖但散落区冷」的活样本，R76 是「两区皆暖」，二者互补。

---

## 3. 🔑 绝对二层分裂：八样本 8/8 不破（唯一稳健结论）

将 ns/槽（=totalMs×1e6/总记录数）八读数（R75-01:11 / R75-01:14 / R76 / R77 / R78 / R79 / R80 / R81）并列：

| 查询 | 层 | 11 | 14 | R76 | R77 | R78 | R79 | R80 | **R81** | 极差 |
|------|----|----|----|-----|-----|-----|-----|-----|---------|------|
| `size:>100m` | 数值 | 7.95 | 8.18 | 8.58 | 9.22 | 7.63 | 6.06 | 16.16 | **11.17** | 2.67× |
| `dm:today` | 数值 | 12.8 | 20.0 | 17.7 | 22.3 | 16.8 | 9.96 | 12.23 | **13.23** | 2.24× |
| `content:import` | 字符串 | 28.2 | 35.9 | 28.4 | 31.7 | 38.7 | 26.6 | 27.79 | **23.56** | 1.64× |
| `path:/Users` | 字符串 | 36.3 | 45.0 | 29.8 | 33.6 | 36.7 | 33.7 | 31.47 | **25.74** | 1.75× |
| `中文测试` | 字符串 | 54.1 | 31.2 | 43.5 | 48.2 | 41.4 | 39.4 | 53.15 | **33.38** | 1.74× |

### 解读（8/8 全样本一致）

- **每一轮，最高数值层 < 最低字符串层**：R81 数值层 max=dm 13.23 < 字符串层 min=content 23.56（层间隙 1.78×）。八轮层间隙 1.42×–2.67×，从未重叠。**这是七轮反复证伪后唯一扛住全部样本的结论**：纯数值路径（单 i64 列比较、零解引用）≪ 字符串 full-eval 路径（解引用散落串池）。
- **本轮字符串层刷新相对低位**：content 23.56 / path 25.74 均为八轮新低——与 §2「顺序扫描区暖」自洽（字符串 full-eval 走顺序 SoA+串池，本轮恰暖）。
- **R76「数值最稳」排序、R79「1.5–2.2× 方差界」均已被先前轮次证伪**（R80 size 16.16=2.67× 已破界）；本轮不新增证伪，size 11.17 落在区间内。层内方差仍由页温度主导，无固定排序——只有**层间绝对分裂**是机制级稳健量。

---

## 4. trigram/候选 每候选成本：本轮与候选数「反相关」（冷态特征）

| 查询 | candidates | phase2Ms | 每候选 (μs) | 温度 |
|------|-----------|----------|------------|------|
| `test` | 136,229 | 19.89 | **0.146** | 暖 |
| `MacEverything` | 6,693 | 2.52 | 0.377 | 暖 |
| `README` | 16,457 | 20.45 | 1.243 | 温 |
| `.pdf` | 10,116 | 52.68 | 5.208 | 冷 |
| `*.swift` | 9,213 | 100.20 | 10.875 | 冷 |
| `abc…`(6词) | 15,917 | 291.26 | 18.30 | 冷 |
| `ext:cpp` | 2,987 | 120.33 | 40.28 | 很冷 |
| `hello world` | 2,340 | 173.64 | 74.21 | 很冷 |
| `main.cpp` | 71 | 14.08 | **198.3** | 很冷 |

**每候选成本与候选数反相关**（高候选 0.146μs ↔ 低候选 198μs，跨度 1356×）：高候选查询命中**热门/常驻**行（频繁访问文件，页留暖），低候选查询命中**稀有/散落**行（页冷）。这正是 mode-A 冷缓存特征（对照 R63 `main.cpp` 71 候选冷态 1665ms），**每候选成本由候选行页驻留主导，与候选数本身无关**。

---

## 5. 🔴 安全阀整会话不可达（R67 根因 21.5h 硬证）

| 证据 | 值 | 含义 |
|------|-----|------|
| 实际 flush | **1 次**（11:12:29，rewrite=10331ms，持锁 10.3s） | 整 21.5h 仅启动后那一次 |
| Skipping flush | **321 次**，全 `only 78 entries (threshold=100)` | pending 冻结 78<100，命中 `IndexPersistence.cpp:142` 早退 |
| `triggering full compaction` | **0 次** | tombstone 比例阀（`:159`，0.25）从未求值——结构性不可达 |
| `Reclaimed` | **0 次** | 整会话零回收 |
| tombstone 走势 | 0（11:12 flush 后）→ **6.44%** | 单调累积、无任何在线回收 |

`:142` 的 `entryCount()<kCompactThreshold` 早退把 `:159` 的 tombstone 比例阀挡在门外——21.5h 内 321 skip / 0 trigger / 0 reclaim，硬证根因为「检查顺序」缺陷，而非阈值未达。

---

## 6. Tombstone 趋势（任务起点快照）

| 轮次 | 时刻 | Tombstone | 比例 | Δ | 区间增速 |
|------|------|-----------|------|---|------|
| R76 | 01:41 | 326,635 | 5.72% | — | — |
| R77 | 02:11 | 334,545 | 5.85% | +7,910 | — |
| R78 | 02:41 | 341,283 | 5.96% | +6,738 | — |
| R79 | 03:11 | 348,021 | 6.07% | +6,738 | — |
| R80 | 04:50 | 355,489 | 6.19% | +7,468 | — |
| R80(log) | 08:24 | 366,288 | 6.37% | +10,799 | ~3.0K/h（隔夜低） |
| **R81** | **08:34** | **370,788** | **6.44%** | **+4,500** | **~27K/h（晨间激增）** |

**14 轮严格单调递增。** R80-08:24→R81-08:34 仅 10min 增 4,500（~27K/h）——与隔夜 04:50→08:24 的 ~3.0K/h 形成鲜明对比，**再次坐实 R80「tombstone 增速与活动量成正比」**：隔夜低活动 ~3K/h，晨间用户活动恢复即跳到 ~27K/h。增速非恒定背景过程，故不宜给单一 ETA；当前 6.44%，距 R43 崩溃前 12.97% 约半程，按昼夜混合速率粗估 ~1.5 天量级（受当日活动量支配，仅作量级参考）。

---

## 7. 索引状态

| 指标 | R80(log 08:24) | R81(08:34) | Δ |
|------|-----|-----|---|
| 总记录 | 5,740,655 | 5,755,626 | +14,971 |
| 活跃记录 | ~5,374,367 | 5,384,838 | +10,471 |
| Tombstone | 366,288 | 370,788 | +4,500 |
| Tombstone 比例 | 6.37% | **6.44%** | +0.07pp |
| 实际 flush | 1 | 1 | +0 |
| Skip flush | ~318 | 321 | +3 |
| QueryAdvanced(>100ms) | ~199 | 202 | +3 |

---

## 8. 日志延迟分布（QueryAdvanced=202，整会话）

| 区间 | 计数 |
|------|------|
| 100–200ms | 66 |
| 200–500ms | 94 |
| 500–1000ms | 14 |
| 1–5s | 21 |
| **>5s** | **7** |

**Top 7 峰值与 R76 逐字节相同**：18582 / 17928 / 15749 / 8205 / 7544 / 6811 / 6061 ms——均为会话早段冷缓存 trigram 历史尖峰，被冻结在过去。R76→R81 增量（182→202，+20）全部落在 100–500ms 带（53+87→66+94），>1s 各档（21/7）零增长——**本轮暖态电池未新增任何 >1s 条目**，但 trigram 侧多个查询逼近 100–300ms（§1），印证散落候选区本轮转冷。

---

## 9. 数据窗口说明

本轮 status / battery / log 分析均在 **08:34 同一分钟内**完成（无 R80 那种 ~3.5h 时间涂抹），ns/槽 一律以 status 时刻总记录 5,755,626 为分母，无跨时外推。

---

## 10. 优化建议

| 优先级 | 项目 | 状态 |
|--------|------|------|
| **P0🔥** | 修复 flush 检查顺序（tombstone 安全阀前置/并联）+ 阈值下调 <12.97% | R67 根因；21.5h「321 skip/0 trigger/0 reclaim」硬证 |
| **P0🔥** | 关键数据页 mlock 常驻 + 候选页预读——**须同钉散落候选区 + 顺序 SoA 列两区** | R81 新证：两区独立淘汰（散落冷/顺序暖同轮并存），单钉一区不足 |
| **P0🔥** | 无锁快照 flush | lockHeld/total ~99% + 启动 flush rewrite 持锁 10.3s |
| **P0🔥** | 查询超时保护 | 历史 .pdf 峰值 18.6s；本轮 trigram 侧多查询 100–291ms |
| **P0** | HTTP 并发处理 | R64 单线程 acceptLoop |
| **P1🔥** | 字符串 full-eval 覆盖层方差治理 | 八样本层内方差由页温度主导（顺序区，本轮恰暖刷新低） |
| P1 | CJK trigram 索引化 | `中文测试` 无 trigram → 0 命中全比对，字符串层最大暴露面 |
| P1 | 日志 total 含 materialization | R64/R72 |

---

## 11. 总结评分

| 维度 | 评分 | 趋势 |
|------|------|------|
| 查询 (trigram, 暖) | 7/10 | ↓ (本轮散落候选区转冷：ext:cpp 120ms/hello 173ms/*.swift 100ms) |
| 查询 (线性/过滤) | 7/10 | ↑ (顺序扫描区本轮最暖，content/path 刷八轮新低 23.56/25.74 ns/槽) |
| 延迟稳定性 | 5/10 | → (暖态本身稳，分区温度波动 + 历史 .pdf 18.6s 均在) |
| 索引健康度 | 3/10 | ↓ (6.44%, 14 轮单调，安全阀 21.5h 零触发零回收) |
| 系统稳定性 | 5/10 | → (代码级缺陷已定位待修) |

**第 81 轮结论**：**🟢 新发现轮——页温度是「分区」的，非全局**。本轮捕获到一个正交于前七轮的证据：同一电池、相隔数秒，trigram/ext-index 的**散落候选区明显转冷**（`ext:cpp` 120ms=40μs/候选、`hello world` 173ms、`*.swift` 100ms、`abc…` 291ms、`.pdf` 52ms，五查询 phase2>50ms），而 linear/pure-filter 的**顺序扫描区却刷新八轮最暖**（`content:import` 23.56、`path:/Users` 25.74 ns/槽，均历史新低）。两区温度同秒反向，只能解释为页缓存按访问区域独立驻留/淘汰——trigram 点跳散落 SoA 行（随机、易淘汰），linear 顺序遍历整列（readahead 友好、易留暖）。**这把冷缓存根因从「全局页温度」精化为「分区页温度」，并把 mlock P0 的要求收紧为「须同钉散落候选区 + 顺序 SoA 列两区」**——R76（两区皆暖）与 R81（散落冷/顺序暖）互为印证。绝对二层分裂八样本 8/8 不破（数值 size 11.17/dm 13.23 ≪ 字符串 content 23.56/path 25.74/中文 33.38，层间隙 1.78×），仍是七轮证伪后唯一机制级稳健结论。每候选成本与候选数反相关（高候选 0.146μs ↔ 低候选 198μs，1356× 跨度，命中热门行=暖/稀有行=冷）再证 mode-A。Tombstone 6.44%（连涨 14 轮）；R80-08:24→R81-08:34 十分钟增 4,500（~27K/h 晨间激增 vs 隔夜 ~3K/h），**再坐实 tombstone 增速与活动量成正比**；安全阀 321 skip「78 entries」/0 触发/0 回收，启动唯一 flush 持锁 10.3s——R67 + 无锁 flush 双 P0 在 21.5h 持续硬证。**工程优先级维持四项 P0🔥（R67 检查顺序、mlock 双区常驻【本轮强化】、无锁 flush、查询超时）；字符串覆盖层方差与 CJK trigram 为 P1🔥。**
