# MacEverything 性能分析报告 R_2606031218

## 1. 本轮范围

- 任务启动时间：2026-06-03 12:18
- 报告文件：`docs/performance_ana/R_2606031218.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21229`
  - `SEG_LINES=203`
  - `LAST_LOG_TS=2026-06-03 12:41`

## 2. 执行摘要

服务正常运行（第 7 轮连续可用）。本轮为恢复后综合性能最佳轮次：P50 ≈ 56ms，Max = 272ms，`.pdf` 降至 115ms（**历史最佳**），`hello world` 降至 128ms（历史最佳）。所有查询连续第 4 轮 < 500ms。lockWaitMs=0（前轮 223ms 已消失）。tombstone 继续加速增长至 67,411 条（1.17%）。

### 七轮横向对比

| Query | R49 冷 | R50 热 | R51 | R52 | R53 | R54 | R55 本轮 | 趋势 |
|-------|-------:|------:|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 1924 | 78 | 76 | 21 | 6.5 | 16 | **4** | 极快 |
| `.pdf` | 9221 | 208 | 4091 | 446 | 229 | 313 | **115** | **历史最佳** |
| `hello world` | 474 | 422 | 545 | 334 | 177 | 270 | **128** | **历史最佳** |
| `path:/Users` | 221 | 578 | 237 | 201 | 438 | 132 | **167** | 稳定 |
| P50 | 189 | 83 | 185 | 93 | 65 | 95 | **56** | **历史最佳** |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 4.2 | 0.0 | short-query-cache | 0 | 0 | 极快 |
| `.pdf` | 5 | 115.2 | 107.2 | advanced-trigram | 0 | 9944 | **历史最佳** |
| `main.cpp` | 5 | 8.8 | 3.0 | advanced-trigram | 0 | 85 | 极快 |
| `*.swift` | 5 | 37.0 | 34.6 | advanced-trigram | 0 | 9356 | 快 |
| `ext:cpp` | 5 | 35.9 | 34.8 | advanced-ext-index | 0 | 3702 | 快 |
| `size:>100m` | 5 | 68.6 | 68.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 127.5 | 126.0 | advanced-trigram | 0 | 3613 | **历史最佳** |
| `abc def ghi jkl mno pqr` | 0 | 58.9 | 58.6 | advanced-trigram | 0 | 15983 | 快 |
| `README` | 5 | 28.2 | 21.4 | advanced-trigram | 0 | 17717 | 快 |
| `test` | 5 | 46.0 | 34.9 | advanced-trigram | 0 | 129957 | 正常（无 lockWait） |
| `MacEverything` | 5 | 7.4 | 0.9 | advanced-trigram | 0 | 6802 | 极快 |
| `dm:today` | 5 | 40.2 | 40.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 快 |
| `content:import` | 5 | 188.9 | 179.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 166.5 | 160.0 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 271.9 | 240.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 56ms，P90 ≈ 189ms，Max = 272ms。全部查询连续第 4 轮 < 500ms。所有查询 lockWaitMs = 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,790,141，liveRecordCount=5,722,730（tombstone=67,411，**1.17%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=59  QUERYADV_GE500=5
FLUSH=1  COMPACTION=2  SKIPFLUSH=29
DIAG_TRIGRAM_SKIP=60
```

- QUERYADV 从 54 增至 59（本轮 battery 贡献 5 次日志记录）。
- QUERYADV_GE500 保持 5（本轮无新增 ≥500ms 查询）。
- SKIPFLUSH 从 23 增至 29（+6，WAL 条目仍不足 100 阈值）。
- DIAG_TRIGRAM_SKIP 从 59 增至 60（+1，本轮 `中文测试` 查询）。

## 6. 问题清单

### High：tombstone 加速增长（1.17%，67,411 条）

- 证据：七轮 tombstone 轨迹 4,118 → 6,236 → 17,015 → 33,512 → 44,758 → 58,558 → **67,411**。
- 本轮增量 8,853 条（前轮 13,800 条，增速略缓但总量持续攀升）。
- 占比 1.17%，已超过 1% 观察阈值。
- recordCount 每轮增长约 9,000-14,000 条，liveRecordCount 相对稳定（~5,722K），说明增长主要来自 FSWatcher 捕获的变更（删除+重建文件条目）。

### Medium：前轮 lockWaitMs=223ms 为偶发事件

- 证据：本轮 16 个查询全部 lockWaitMs=0。
- 前轮 `test` 509ms（lockWaitMs=223ms）已证实为偶发竞争，非趋势性问题。

### Low：`.pdf` 查询仍有结构性风险

- 本轮 115ms 为历史最佳，但 candidates=9944（未变），在 I/O 竞争时仍可能飙升至秒级。

### Low：DIAG-TRIGRAM SKIP 持续累积（60 次）

- 增速稳定（+1/轮），`中文测试` 的 trigram 在索引中未找到匹配。

## 7. 优化建议

### P0：补齐进程生命周期监控（承前轮）

### P1：主动 tombstone 回收策略

- tombstone 已达 1.17%（67,411 条）。建议实现基于占比的主动 compaction 触发（如 2% 阈值），而非仅依赖 WAL flush 后触发。
- 当前 SKIPFLUSH 持续增加（WAL <100 条目），意味着 flush-triggered compaction 路径长期不会被激活。

### P1：将 `.xxx` 扩展名模式路由到 ext-index（承前轮）

### P2：多词查询 phase2 剪枝优化（承前轮）

- `hello world` 本轮 128ms 已显著改善，优先级可降低。

## 8. 下一轮重点

1. 追踪 tombstone 增长速率——已超 1%，是否会继续加速。
2. 确认 lockWaitMs=0 是否持续（前轮为偶发）。
3. 监控 SKIPFLUSH 积累是否导致 compaction 路径永久休眠。

## 9. 结论

服务连续第 7 轮正常运行，本轮为恢复后综合性能最佳：P50=56ms，Max=272ms，所有查询连续第 4 轮 <500ms。`.pdf`=115ms 和 `hello world`=128ms 均为历史最佳。前轮 lockWaitMs=223ms 已证实为偶发。最大关切是 tombstone 持续加速增长至 67,411 条（1.17%），而 SKIPFLUSH 持续积累意味着 flush-triggered compaction 路径可能长期休眠，需引入基于占比的主动 compaction 机制。
