# MacEverything 性能分析报告 R_2606031249

## 1. 本轮范围

- 任务启动时间：2026-06-03 12:49
- 报告文件：`docs/performance_ana/R_2606031249.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21239`
  - `SEG_LINES=213`
  - `LAST_LOG_TS=2026-06-03 13:11`

## 2. 执行摘要

服务正常运行（第 8 轮连续可用）。本轮再次刷新多项历史最佳：P50 ≈ 52ms，`.pdf` 首次降至 100ms 以下（98ms），`hello world`=70ms，Max=250ms。所有查询连续第 5 轮 < 500ms，lockWaitMs 全部为 0。tombstone 继续增长至 75,220 条（1.30%）。

### 八轮横向对比

| Query | R49 冷 | R50 | R51 | R52 | R53 | R54 | R55 | R56 本轮 | 趋势 |
|-------|-------:|----:|-----:|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 1924 | 78 | 76 | 21 | 6.5 | 16 | 4 | **5** | 极快稳定 |
| `.pdf` | 9221 | 208 | 4091 | 446 | 229 | 313 | 115 | **98** | **首次<100ms** |
| `hello world` | 474 | 422 | 545 | 334 | 177 | 270 | 128 | **70** | **历史最佳** |
| `path:/Users` | 221 | 578 | 237 | 201 | 438 | 132 | 167 | **250** | 波动 |
| P50 | 189 | 83 | 185 | 93 | 65 | 95 | 56 | **52** | **历史最佳** |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 4.6 | 0.0 | short-query-cache | 0 | 0 | 极快 |
| `.pdf` | 5 | 98.3 | 96.5 | advanced-trigram | 0 | 9944 | **首次<100ms** |
| `main.cpp` | 5 | 10.6 | 4.0 | advanced-trigram | 0 | 85 | 极快 |
| `*.swift` | 5 | 61.6 | 58.0 | advanced-trigram | 0 | 9356 | 快 |
| `ext:cpp` | 5 | 52.6 | 51.5 | advanced-ext-index | 0 | 3702 | 快 |
| `size:>100m` | 5 | 39.7 | 39.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 快 |
| `hello world` | 5 | 69.7 | 68.3 | advanced-trigram | 0 | 3613 | **历史最佳** |
| `abc def ghi jkl mno pqr` | 0 | 47.1 | 47.0 | advanced-trigram | 0 | 15984 | 快 |
| `README` | 5 | 17.3 | 14.4 | advanced-trigram | 0 | 17717 | 快 |
| `test` | 5 | 23.4 | 18.1 | advanced-trigram | 0 | 129960 | 快 |
| `MacEverything` | 5 | 8.8 | 0.8 | advanced-trigram | 0 | 6802 | 极快 |
| `dm:today` | 5 | 67.2 | 65.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 165.1 | 150.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 249.5 | 234.4 | advanced-linear-gcd | 0 | 0 | 波动 |
| `中文测试` | 0 | 183.8 | 183.0 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.3 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 52ms，P90 ≈ 183ms，Max = 250ms。全部查询连续第 5 轮 < 500ms。lockWaitMs 全部为 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,798,182，liveRecordCount=5,722,962（tombstone=75,220，**1.30%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=62  QUERYADV_GE500=5
FLUSH=1  COMPACTION=2  SKIPFLUSH=35
DIAG_TRIGRAM_SKIP=61
```

- QUERYADV 从 59 增至 62（本轮 battery 贡献 3 次日志记录）。
- QUERYADV_GE500 保持 5（本轮无新增 ≥500ms 查询）。
- SKIPFLUSH 从 29 增至 35（+6，每 5 分钟一次，WAL 条目稳定在 68）。
- DIAG_TRIGRAM_SKIP 从 60 增至 61（+1，本轮 `中文测试`）。

## 6. 问题清单

### High：tombstone 持续加速增长（1.30%，75,220 条）

- 证据：八轮轨迹 4,118 → 6,236 → 17,015 → 33,512 → 44,758 → 58,558 → 67,411 → **75,220**。
- 本轮增量 7,809 条。总量 75K，占比 1.30%。
- liveRecordCount 稳定在 ~5,722K，recordCount 持续增长（5,798K），差值即 tombstone。
- SKIPFLUSH 持续（WAL=68 < 100 阈值），flush-triggered compaction 路径休眠。

### Medium：`path:/Users` 持续波动

- 八轮数据：221 → 578 → 237 → 201 → 438 → 132 → 167 → 250ms。
- 走 advanced-linear-gcd 路径，受系统 I/O 负载和 GCD 调度影响。
- 标准差约 150ms，波动属结构性特征。

### Low：DIAG-TRIGRAM SKIP 持续累积（61 次）

- 增速稳定（+1/轮），均为 `中文测试` 查询触发。

## 7. 优化建议

### P0：补齐进程生命周期监控（承前轮）

### P1：主动 tombstone 回收策略（承前轮，升级紧迫度）

- tombstone 占比 1.30% 且 SKIPFLUSH 持续，compaction 路径休眠。
- 建议方案：定时检查 tombstone 占比，超过 2% 时触发增量 compaction，不依赖 WAL flush。
- 备选：降低 SKIPFLUSH 阈值（当前 100 → 50），使 flush 更频繁触发。

### P1：将 `.xxx` 扩展名模式路由到 ext-index（承前轮）

### P2：`path:/Users` 线性扫描路径优化

- 考虑引入路径前缀索引，减少对 GCD 线性扫描的依赖。

## 8. 下一轮重点

1. 追踪 tombstone 增长——是否会在 2% 前触发自然 compaction。
2. 观察 `.pdf` 是否能持续保持 <200ms。
3. 监控 lockWaitMs 是否再次出现。

## 9. 结论

服务连续第 8 轮正常运行，再次刷新多项历史最佳。P50=52ms（历史最佳），Max=250ms，`.pdf` 首次降至 100ms 以下（98ms），`hello world`=70ms（历史最佳）。所有查询连续第 5 轮 <500ms，lockWaitMs 全部为 0。最大关切仍是 tombstone 持续增长至 75,220 条（1.30%），而 SKIPFLUSH 持续积累（35 次）意味着 flush-triggered compaction 路径处于休眠状态。建议引入基于占比的主动 compaction 触发机制。
