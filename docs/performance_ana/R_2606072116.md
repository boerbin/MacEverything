# MacEverything 性能分析报告 R_2606072116

## 1. 本轮范围

- 任务启动时间：2026-06-07 21:16
- 报告文件：`docs/performance_ana/R_2606072116.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=2511`
  - `SEG_LINES=1569`
  - `LAST_LOG_TS=2026-06-07 21:17`

## 2. 执行摘要

**正常波动——0/16 >500ms，Max=178ms，P50=24ms，lockWait=0。** 极佳比例从 R150 的 13/16 回落至 11/16，主因是 30 分钟空闲后进程唤醒延迟（`x` 从 0.6→24.5ms，phase2=0）和 SoA page cache 部分 LRU 驱逐（`size:>100m` 从 20→144ms）。仍处于稳态范围内。

### 关键指标对比

| 指标 | R150（30 min 前） | R151 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 215ms | **178ms** | **↓ 17%** |
| P50 | 16ms | **24ms** | ↑ 正常波动 |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 13/16 | **11/16** | ↓2（波动） |
| tombstone | 1,173,932 (18.72%) | **1,183,173 (18.84%)** | ↑ 正常 |
| tombstone 增速 | ~282/min | **~308/min** | 持平（极低） |
| recordCount | 6,270,743 | **6,280,283** | +9.5K |
| liveRecordCount | 5,096,811 | **5,097,110** | +299 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 1413 | **1470** | +57 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 8 轮 0 新增） |

### 稳态确认——P50 连续 6 轮 <33ms

| 轮次 | P50 | Max | 极佳比例 | 状态 |
|------|----:|----:|------:|------|
| R146 | 20ms | 221ms | 11/16 | 极限 |
| R147 | 32ms | 156ms | 10/16 | 波动 |
| R148 | 14ms | 199ms | 12/16 | 极限 |
| R149 | 23ms | 272ms | 11/16 | 稳态 |
| R150 | 16ms | 215ms | 13/16 | 新高 |
| **R151** | **24ms** | **178ms** | **11/16** | **正常波动** |

- **P50 在 14-32ms 范围波动（6 轮均值 ~22ms）——稳态持续。**
- **Max 从 R149=272ms 持续下降至 178ms——全表扫描总成本趋降。**

### 进程唤醒延迟分析（本轮新观察）

| Query | R150 queryTime | R150 overhead | R151 queryTime | R151 overhead | 说明 |
|-------|------:|------:|------:|------:|------|
| `x` | 0.6ms | 0.6ms | **24.5ms** | **24.5ms** | 30min 空闲后进程唤醒 |
| `ext:cpp` | 4.0ms | 0.4ms | **24.1ms** | **20.4ms** | ext-index 代码路径冷加载 |
| `main.cpp` | 1.8ms | 0.6ms | 5.9ms | 1.1ms | 正常（第 3 个查询已热） |

- **`x` query：phase2=0ms，queryTime=24.5ms——全部 24.5ms 是进程级开销。**
- **原因**：macOS 对 30 分钟无活动的后台进程执行 App Nap / 调度降级，首次请求需唤醒进程、恢复代码页和调度优先级。
- **影响**：仅影响 30 分钟间隔后的首个查询（<25ms），不影响用户体验。
- **结论**：正常行为，无需优化。

### SoA Page Cache 部分驱逐

| Query | R150 | R151 | 变化 | 说明 |
|-------|-----:|-----:|------|------|
| `size:>100m` | 20ms | **144ms** | ↑ 7.1x | SoA filter 区域部分驱逐 |
| `dm:today` | 31ms | **56ms** | ↑ 1.8x | 同区域轻微影响 |
| `content:import` | 110ms | **157ms** | ↑ 1.4x | 轻微 |
| `path:/Users` | 215ms | **140ms** | **↓ 35%** | 改善 |
| `中文测试` | 174ms | **178ms** | 持平 | — |

- **`size:>100m` 从 20→144ms（7x）——SoA filter 区域的 page cache 被 30 分钟间隔内的其他系统活动部分驱逐。**
- **`path:/Users` 同时改善 215→140ms——零和效应（LRU 重新分配）。**
- **全表扫描总和**：R150=499ms → R151=474ms（**↓5%，持续下降趋势**）。

### Trigram 路径——稳定

| Query | R150 | R151 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 11ms | **30ms** | ↑ 波动 |
| `*.swift` | 15ms | **19ms** | ↑ 微量 |
| `hello world` | 18ms | **37ms** | ↑ 波动 |
| `README` | 8ms | **6ms** | ↓ |
| `test` | 32ms | **18ms** | **↓ 44%** |
| Trigram 均值 | 12.0ms | **16.4ms** | ↑ 37%（含唤醒开销） |

- **Trigram 均值 16.4ms（R150=12.0ms）——上浮主要受首查询唤醒延迟影响。**
- **去除 `x` 异常后 Trigram 均值 ~14ms——仍接近 R133 水平。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 24.5 | 0.0 | short-query-cache | 0 | 0 | 唤醒延迟 |
| `.pdf` | 5 | 30.0 | 27.7 | advanced-trigram | 0 | 9812 | 极佳 |
| `main.cpp` | 5 | 5.9 | 4.8 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 19.0 | 15.3 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 24.1 | 3.7 | advanced-ext-index | 0 | 3571 | 唤醒残余 |
| `size:>100m` | 5 | 144.4 | 144.0 | advanced-pure-filter-soa-gcd | 0 | 0 | page cache 驱逐 |
| `hello world` | 5 | 37.0 | 36.5 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 22.4 | 22.2 | advanced-trigram | 0 | 14209 | 极佳 |
| `README` | 5 | 6.1 | 5.3 | advanced-trigram | 0 | 16631 | 极佳 |
| `test` | 5 | 18.1 | 16.0 | advanced-trigram | 0 | 116512 | 极佳 |
| `MacEverything` | 5 | 2.0 | 0.6 | advanced-trigram | 0 | 6861 | 极佳 |
| `dm:today` | 5 | 55.5 | 53.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 156.7 | 127.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 140.2 | 136.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 177.9 | 177.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.1 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 24.3ms，P90 = 144.4ms，Max = 177.9ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R150 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 11/16 | 0.1-37ms | R150=13/16（↓2 波动） |
| **正常**（50-500ms） | 5/16 | 56-178ms | R150=3/16（↑2） |
| **超标**（>500ms） | 0/16 | — | R150=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,280,283，liveRecordCount=5,097,110（tombstone=**1,183,173**，**18.84%**）；
- 进程正常运行（当前会话，运行约 23.2 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=59  QUERYADV_GE500=4（不变——连续 8 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=1470
```

- **连续 8 轮 0 个新增 >500ms 查询（R144-R151）。**
- **FLUSH=1**——无新 flush（23.2 小时仅启动时 1 次）。
- **SKIPFLUSH=1470**——系统持续空闲。

## 6. 问题清单

### 观察：30 分钟空闲后进程唤醒延迟（信息记录，非问题）

- `x` query 从 0.6ms 升至 24.5ms（phase2 仍为 0ms）。
- macOS App Nap / 调度降级对后台进程的正常行为。
- 仅影响首个查询，对用户体验无感知影响。

### 无活跃问题——系统稳定

- 0/16 >500ms，11/16 <50ms。
- P50=24ms，lockWait=0。
- 连续 8 轮 0 个新增慢查询。
- 23.2 小时无 WARN/ERROR。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 18.84%，增速 ~308/min。
- 距 25% 阈值约 ~33 小时（按当前增速）。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 系统空闲时无影响。

### P2：降低 compaction 阈值至 15%

- 保持建议。

## 8. 下一轮重点

1. 唤醒延迟是否为一次性现象（下轮 `x` 是否回到 <1ms）。
2. `size:>100m` page cache 是否恢复。
3. 稳态是否维持。

## 9. 结论

**正常波动——0/16 >500ms，Max=178ms，P50=24ms，lockWait=0。** 极佳比例从 R150 的 13/16（81%）回落至 11/16（69%），主因是两个一次性因素：(1) 30 分钟空闲后进程唤醒延迟（`x`=24.5ms，phase2=0）；(2) SoA filter 区域 page cache 部分 LRU 驱逐（`size:>100m` 20→144ms）。

**全表扫描总和持续下降：R149=533ms → R150=499ms → R151=474ms。** P50=24ms 处于 6 轮稳态范围（14-32ms）。tombstone 18.84%（1.18M），增速 308/min。系统 23.2 小时无异常——稳态持续，无需干预。
