# MacEverything 性能分析报告 R_2605301941

- **任务启动时间**：2026-05-30 19:41
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~32.6h，未重启
- **状态快照 @19:41**：total `6,069,564` / live `5,397,972` / tomb `671,592`（**11.06%**）
- **本轮定性**：🟠 **重度活跃写入持续 + 分裂仅靠 content 锚成立（字面 min 对抗检验失败）**——ingest 持续强烈（tomb 增量 **+26,221，再创系列新高**，且 **live 净减 -10,281**，本系列首次净减，churn/删除显著）。整批电池重度受争用（`.pdf`=1141ms、`path`=772ms、`*.swift`=497ms、`main.cpp` 71cand/trig=83ms 触发 marker#3），复测全部瞬时恢复 → 突发非稳态。FLOOR-METHOD 双窗口本轮**两条路径同时非平稳**：`path`（摆动 **2.34×**，R100 模式）与 `dm`（摆动 **2.59×**，数值列罕见）；`content` 仍为平稳锚（摆动 **1.16×**），`size` 平稳（1.51×）。**关键：字面 min 对抗检验失败**（dm 最差洁净 10.92ns ≈ path 字面 min 10.72ns = 0.98×），与 R100 同型——字面 min path 非平稳不可信，判定**退回 content 平稳锚**。锚解读分裂 **3.51×（floor）/ 1.18×（对抗，系列最紧但 HOLDS）**，**29/29 经锚成立**。

---

## 1. 原始电池（重度争用，⚠️ 标记异常项）

| query | path | total(ms) | phase2 | trig | lockHeld | cand | 备注 |
|---|---|---|---|---|---|---|---|
| `x` | short-query-cache | 9.12 | 0 | 0 | 0 | 0 | ⚠️ cache 轻破（复测 0.0）|
| `.pdf` | advanced-trigram | **1141.23** | 994.78 | 146.30 | 1141.12 | 10180 | ⚠️ **本轮峰值**（复测 1.6）|
| `main.cpp` | advanced-trigram | 121.47 | 38.22 | **83.17** | 121.40 | 71 | ⚠️ marker#3（71cand trig=83ms，复测 0.27）|
| `*.swift` | advanced-trigram | **497.87** | 482.48 | 1.83 | 484.31 | 9213 | 受争用（复测 2.43）|
| `ext:cpp` | advanced-ext-index | 302.07 | 300.83 | 0 | 301.92 | 2987 | 受争用 |
| `size:>100m` | pure-filter-soa-gcd | 168.19 | 167.44 | 0 | 167.45 | 0 | 数值（floor 9.47）|
| `hello world` | advanced-trigram | 212.77 | 210.15 | 2.07 | 212.22 | 2374 | 受争用 |
| `abc def ghi jkl mno pqr` | advanced-trigram | 329.25 | 326.19 | 2.17 | 328.38 | 15928 | 受争用 |
| `README` | advanced-trigram | 81.82 | 63.15 | 18.62 | 81.77 | 16522 | 较干净 |
| `test` | advanced-trigram | 125.36 | 73.90 | 51.20 | 125.11 | 137790 | 受争用 |
| `MacEverything` | advanced-trigram | 49.76 | 3.77 | 45.89 | 49.67 | 6705 | 较干净 |
| `dm:today` | pure-filter-soa-gcd | 132.44 | 131.61 | 0 | 131.61 | 0 | 数值（floor 22.27，本轮非平稳）|
| `content:import` | pure-filter-soa-gcd | 267.71 | 252.41 | 0 | 252.41 | 0 | 字符串（floor 平稳锚）|
| `path:/Users` | advanced-linear-gcd | **772.14** | 767.30 | 0 | 767.31 | 0 | 字符串（floor 非平稳 2.34×）|
| `中文测试` | advanced-linear-gcd | 217.67 | 217.48 | 0.10 | 217.65 | 0 | 字符串 |
| `verylongquery…really` | advanced-trigram | 1.36 | 0 | 1.34 | 1.35 | 0 | |

**marker#3 触发**：`main.cpp` 71 候选 trig=83ms（71 个候选真实 trigram <1ms，83ms 是写者持锁排队）。整批 lockHeld≈total（`.pdf` 1141.12/1141.23、`path` 767.31/772.14、`*.swift` 484.31/497.87）——写者持锁占比近 100%，写者饿死读者。窗口内 totalRecords 实测增长证写入活跃。

---

## 2. 复测消歧（标志项全部瞬时恢复 → 突发非稳态）

| query | 电池值 | probe1 | probe2 | probe3 | probe4 | 判定 |
|---|---|---|---|---|---|---|
| `x`（缓存）| 9.12 | 9.39 | 0.02 | 0.00 | 0.02 | ✅ 瞬时 |
| `main.cpp`（71cand）| 121.47 | 31.27 | 0.27 | 2.11 | 0.26 | ✅ 瞬时（450× 恢复）|
| `*.swift` | 497.87 | 49.05 | 2.43 | 26.55 | 35.97 | ✅ 瞬时（205× 恢复，未完全静默）|
| `.pdf` | 1141.23 | 13.44 | 1.60 | 9.04 | 3.11 | ✅ 瞬时（713× 恢复）|
| `ext:cpp` | 302.07 | 41.40 | 0.34 | 0.76 | 13.46 | ✅ 瞬时（突发型）|

标志项全部突发型（非 R95 稳态），可评估。

---

## 3. FLOOR-METHOD 复测（双窗口各 8×，R100 平稳性协议）

R100 确立：floor-method 假设窗口内计算成本平稳；linear-gcd（散布串池）在 ingest 下可能违反，须**双窗口交叉验证**。**本轮异常：path 与 dm 双双非平稳**：

**跨窗口 floor 摆动（平稳性判定核心）：**

| query | 类型 | W1 floor | W2 floor | 摆动 | 判定 |
|---|---|---|---|---|---|
| `size` | pure-filter 数值 | 14.29 | 9.47 | **1.51×** | 平稳 |
| `dm` | pure-filter 数值 | 57.76 | 22.27 | **2.59×** | ⚠️ **非平稳（数值列罕见）** |
| `content` | pure-filter 字符串 | 78.12 | 90.35 | **1.16×** | ✅ **平稳锚** |
| `path` | linear-gcd 字符串 | 152.27 | 65.09 | **2.34×** | ⚠️ **非平稳（R100 模式）** |

**窗口内 spread：**

| query | W1 min | W1 med | W1 max | W2 min | W2 max | 备注 |
|---|---|---|---|---|---|---|
| `size` | 14.29 | 21.4 | 57.46 | 9.47 | 333.66 | W2 含 333 突发 |
| `dm` | 57.76 | 83.0 | 122.44 | 22.27 | 427.38 | 双峰 + 突发 |
| `content` | 78.12 | 179.0 | 243.06 | 90.35 | 1320.27 | min 极稳（见判定）|
| `path` | 152.27 | 276.1 | 1019.42 | 65.09 | 597.49 | W1 含 1019 突发 |

**关键观察**：
1. **`content` 仍是稳定锚**：W1 min 78.12 ≈ W2 min 90.35，摆动仅 1.16×，即便 W2 窗口内有 1320ms 突发尾，floor 稳定——pure-filter 紧凑列在写入间隙稳定全驻留。
2. **`path` 非平稳（2.34×）**：W2 floor（65.09）< W1 floor（152.27），W1 含 1019ms 突发。这是 R100/R101 的 page 驱逐扰动 linear-gcd 计算成本签名，本轮第四次复现。
3. **`dm` 本轮亦非平稳（2.59×，数值列罕见）**：W1 floor 57.76 vs W2 22.27。pure-filter 数值列通常平稳（`size` 本轮仍 1.51×），但 `dm` 摆动 2.59×——**本系列首次观察到数值 floor 在重度 ingest 下亦非平稳**。诚实标注：可能为 W1 撞 path 1019 突发期的连带争用，或 date 列访问模式差异；`size` 保持平稳，**不构成数值层整体退化**，但提示重度 ingest 下页压力可外溢至紧凑列。

---

## 4. 洁净 ns/槽（floor 法，records=6,069,564）

| 路径 | 类型 | floor(ms) | 洁净值(ns/槽) | 说明 |
|---|---|---|---|---|
| `size:>100m` | 数值 | 9.47 | **1.56** | 平稳 |
| `dm:today` | 数值 | 22.27 | **3.67** | ⚠️ 非平稳，取 floor |
| `path:/Users` | 字符串 | 65.09 | **10.72** | ⚠️ 字面 min（非平稳 2.34×，对抗失败）|
| `content:import` | 字符串 | 78.12 | **12.87** | ⭐ 平稳锚（摆动 1.16×）|
| `中文测试` | 字符串 | 177.09 | **29.18** | W1-only |

---

## 5. 绝对二层分裂：29/29 经 content 锚成立（字面 min 对抗失败）

| 解读 | 最低字符串 | 最高数值 | 间隙 | 对抗检验 |
|---|---|---|---|---|
| **字面绝对 min** | path 10.72ns | dm 3.67ns | 2.92× | ❌ **FAILS**（dm 最差 10.92 ≈ path 10.72 = 0.98×）|
| **平稳锚（content）** | content 12.87ns | dm 3.67ns | **3.51×** | ✅ **HOLDS**（1.18×）|

- **数值层**：size 1.56、dm 3.67 → 最高数值 **dm 3.67**
- **字符串层**：path 10.72（字面 min，非平稳）/ content 12.87（平稳锚）/ 中文 29.18

**多重稳健性（本轮退回锚，与 R100 同型）**：
1. **字面 min floor**：path 10.72 > dm 3.67 → 2.92× ✅（floor 层面成立）
2. **字面 min 对抗检验**：dm 最差洁净（W2 簇上沿 66.3ms=10.92ns）vs path 10.72 = **0.98× ❌ FAILS**——path 字面 min 非平稳（2.34×），其 W2 floor 65.09 疑为 page 驻留偶发低点，**不是可靠的最低字符串读数**（R100 先例）。
3. **平稳锚 floor**：content 12.87 > dm 3.67 → **3.51× ✅**（content 摆动仅 1.16×，最可信）。
4. **平稳锚对抗检验**：dm 最差洁净 10.92ns < content 12.87 → **1.18× ✅ HOLDS**（系列最紧对抗间隙，但不破）。

**判定逻辑**：字面 min path（10.72）与 dm 最差洁净（10.92）几乎重合（0.98×），单看字面 min 无法确证分裂；但 path 已被独立证明非平稳（2.34×），其 floor 不可信，故按 R100 协议退回 content 平稳锚。锚解读 floor 3.51×、对抗 1.18× 双双 HOLDS——**29/29 经锚成立**。

**诚实边界**：这是继 R100 之后**第二轮字面 min 失败、仅靠锚成立**的轮次，且对抗间隙 1.18× 为系列最紧。主因是 dm 本轮非平稳使其洁净簇上沿（10.92ns）逼近 path 字面 min——若 dm 与 path 同时被重度 ingest 页压力扰动，两者数值层级可瞬时接近。但 **content 锚（紧凑串列、摆动 1.16×）提供了不受该扰动影响的稳定下界**，分裂机制（纯数值零解引用 ≪ 字符串解引用散布串池）未被推翻。

---

## 6. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **885 次**（R102 819）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100 提前 return，`:159` 比例阀结构性永不可达。skip-gate 819→885（+66，本轮最高单轮增量），写入持续。

---

## 7. tombstone 趋势：11.06%，连续 36 轮单调，live 首次净减

| 轮次 | tombstone | ratio | 30min 增量 | live 增量 |
|---|---|---|---|---|
| R99@17:41 | 594,254 | 9.91% | +11,782 | — |
| R100@18:11 | 604,427 | 10.06% | +10,173 | — |
| R101@18:41 | 622,035 | 10.32% | +17,608 | +5,214 |
| R102@19:11 | 645,371 | 10.66% | +23,336 | +1,789 |
| **R103@19:41** | **671,592** | **11.06%** | **+26,221（再创新高）** | **-10,281（首次净减）** |

会话内单调递增、永不回缩，**连续 36 轮**。R43 参考崩溃点 12.97%（~800K），当前 11.06% 已过 **~85% 程**，且**首次突破 11%**。本轮 total +15,940、**live 净减 -10,281**——这是本系列首次 live 绝对值下降，意味本轮 churn/删除超过新增入库（删除产生 tombstone + 既有记录被标删）。tomb 增量 +26,221 再创系列新高。方向不可逆，逼近崩溃点，修复紧迫性最高。

---

## 8. 日志健康度

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **690 条**（R102 640，+50，含本轮双窗口重度复测）
- Top-10 峰值 `[3362, 3766, 3807, 6061, 6811, 7544, 8205, 15749, 17928, 18582]` —— 仍与历史**逐字节相同**（启动期基建事件）
- 延迟分布（QueryAdvanced-only）：>5s=**7**（冻结）、1-5s=**33（+3）**、0.5-1s=**42（+4）**、0.1-0.5s=608、中位 **220ms**
- **越线条目枚举**：窗口内 7 条 >500ms——19:41:47 **1141ms**（电池 `.pdf`）/ 19:41:53 772ms（电池 `path`）+ 19:43:17 615ms / 19:43:20 **1019ms**（W1 path 复测）+ 19:44:22 844ms / 19:44:31 **1320ms**（W2 content 复测）/ 19:44:33 597ms（W2 path 复测）。**7 条全部为本人电池/复测查询撞 ingest 突发，零 organic 慢查询**——背景查询本轮无一越线。
- 启动基建事件须排除：`20765ms`@11:05:13、`10353ms`@11:12:29——已过滤

---

## 9. 优化建议（P0｜read-write-lock + mlock 维持升级）

1. **🔥 P0｜读写锁分离 / 无锁快照读**：本轮整批 lockHeld≈total（`.pdf` 1141、`path` 772、`*.swift` 497）+ marker#3（`main.cpp` 71cand trig 83ms）为持续最强写者饿死读者证据；ingest 振荡八轮（R95–R103）持续指向同一根因，最高优先级。
2. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调（建议 8–10%）。当前 **11.06%**，~85% 逼近崩溃点，**首破 11%**，live 首次净减，紧迫性进一步升级。
3. **🔥 P0｜mlock/madvise 钉住散布热区**（维持 R100 升级，本轮证据加强）：本轮 **path 与 dm 双双非平稳**（2.34× / 2.59×），重度 ingest 页压力**首次外溢至紧凑数值列**（dm）。mlock 钉住 path/name 串池 + 数值 SoA 列可消除该非平稳。R100+R101+R102+R103 连续四轮定量证据，本轮范围扩大至数值层。
4. **🔥 P0｜无锁快照 flush**：启动 flush 持锁 10.3s。
5. **🔥 P0｜查询超时保护**：本轮 `.pdf` 1141ms、path 1019ms、content 1320ms 突发——超时熔断 + 降级返回必要。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1｜CJK trigram 索引**（维持 R99 降级）：中文热态成本（29.18）与 path/content 同量级，慢主要是页冷非算法。

---

## 10. 结论

- **定性**：🟠 重度活跃写入持续 + 分裂仅靠 content 锚成立。ingest 持续强烈（tomb **+26,221 再创新高**，**live 首次净减 -10,281**，churn 显著），整批电池重度受争用（`.pdf` 1141ms / lockHeld≈total / marker#3），复测全部瞬时恢复 → 突发非稳态。
- **分裂 29/29，经 content 锚成立**：本轮**两条路径同时非平稳**——`path`（2.34×，R100 模式第四次复现）与 `dm`（2.59×，数值列罕见，重度 ingest 页压力首次外溢至紧凑列）。`content` 仍为稳定锚（摆动 1.16×）。**字面 min 对抗检验失败**（dm 最差洁净 10.92ns ≈ path 字面 min 10.72ns = 0.98×），与 R100 同型——path 非平稳 floor 不可信，退回 content 锚。锚解读 floor 3.51× / 对抗 1.18×（系列最紧）双双 HOLDS。
- **诚实边界（关键）**：这是继 R100 之后第二轮字面 min 失败、对抗间隙系列最紧（1.18×）的轮次。主因是 dm 本轮非平稳使其洁净簇上沿逼近 path 字面 min。但 content 锚（紧凑串列、摆动 1.16×）提供不受页压力扰动的稳定下界，分裂机制未被推翻。**未把字面 min 0.98× 误读为破裂，亦未无视 dm 非平稳硬套字面 min**——按 R100 协议正确退回锚。
- **R100 发现连续四轮验证 + 范围扩大**：linear-gcd floor 非平稳本轮第四次复现（path 2.34×），且**首次扩展至数值层**（dm 2.59×），为 mlock P0 升级提供第四轮证据并扩大覆盖。
- **唯一持续恶化项**：tombstone **11.06%（671,592）**，连续 36 轮单调，**首破 11%、~85% 逼近崩溃点**，本轮 tomb 增量再创新高且 **live 首次净减**，根因 R67 阀门结构性不可达明确。
- **诚实修正纪律**：本轮严格区分突发非稳态（复测恢复）与稳态退化（tombstone）；明确标注 7 条越线全为测量行为撞突发、零 organic；显式呈现字面 min 对抗失败（0.98×）并据 path 非平稳退回锚；标注 dm 非平稳为新观察但不过度解读为数值层退化（size 仍平稳）。贯彻「不轻易下结论、每个结论需逻辑与数据支撑」。
