# MacEverything 性能分析报告 R_2605301341

- **任务启动时间**：2026-05-30 13:41
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~26.6h，未重启
- **状态快照 @13:41**：total `5,864,416` / live `5,391,356`
- **本轮定性**：🟢 **洁净恢复轮 + 午间停顿完全消退 + tombstone 首破 8%**——连续三轮污染（R88/R89/R90）后**首个洁净轮**：电池零污染标志（`x`=1.34ms、`main.cpp` 71候选16ms、`*.swift` 9213候选**72ms**），午间停顿走完完整生命周期 108→237→1002(峰)→618→**72ms** 回到基线（甚至低于 R87 的 108ms）。电池窗口日志确认**无任何 >500ms 条目**，1-5s/0.5-1s 延迟段均**未增**。洁净间隙从 R90 史上最窄 2.11× **恢复到 2.88×**，二层分裂 **18/18 不破**。唯一恶化项 tombstone **首破 8%（8.07%）**，根因仍为 R67 阀门不可达。

---

## 1. 查询电池（16 条，本轮洁净）

| query | path | total(ms) | phase2 | trig | cand | res |
|---|---|---|---|---|---|---|
| `x` | short-query-cache | 1.34 | 0 | 0 | 0 | 99 |
| `.pdf` | advanced-trigram | 33.14 | 31.39 | 1.61 | 10122 | 100 |
| `main.cpp` | advanced-trigram | 16.44 | 15.35 | 1.06 | 71 | 71 |
| `*.swift` | advanced-trigram | 71.74 | 70.42 | 1.28 | 9213 | 100 |
| `ext:cpp` | advanced-ext-index | 23.89 | 23.52 | 0 | 2987 | 100 |
| `size:>100m` | pure-filter-soa-gcd | 35.61 | 35.57 | 0 | 0 | 100 |
| `hello world` | advanced-trigram | 68.55 | 67.40 | 1.02 | 2340 | 100 |
| `abc def ghi jkl mno pqr` | advanced-trigram | 38.53 | 38.36 | 0.08 | 15924 | 0 |
| `README` | advanced-trigram | 13.23 | 7.93 | 5.2 | 16464 | 100 |
| `test` | advanced-trigram | 20.63 | 14.0 | 6.31 | 136265 | 100 |
| `MacEverything` | advanced-trigram | 11.53 | 1.64 | 9.84 | 6697 | 100 |
| `dm:today` | pure-filter-soa-gcd | 57.56 | 57.49 | 0 | 0 | 100 |
| `content:import` | pure-filter-soa-gcd | 164.36 | 155.71 | 0 | 0 | 100 |
| `path:/Users` | advanced-linear-gcd | 162.52 | 158.12 | 0 | 0 | 100 |
| `中文测试` | advanced-linear-gcd | 221.50 | 221.20 | 0.14 | 0 | 0 |
| `verylongquery…really` | advanced-trigram | 0.72 | 0 | 0.7 | 0 | 0 |

无污染标志：`x`<10ms、`main.cpp` 71 候选 16ms 合理、`*.swift` 9213 候选 72ms 回到正常区、`ext:cpp` 24ms。整批可信。

---

## 2. 洁净 ns/槽（电池+3× 复测取 4 样本中位，records=5,864,416）

本轮无污染，但仍按纪律做 3× 复测确认稳定性（标志复测 `x`=0.01、`*.swift`=2.02 均正常）：

| 路径 | 类型 | 4样本中位 | 洁净值 |
|---|---|---|---|
| `size:>100m` | 数值 | 34.6ms | **5.91** |
| `dm:today` | 数值 | 57.8ms | **9.86** |
| `content:import` | 字符串 | 166.5ms | **28.40** |
| `path:/Users` | 字符串 | 177.8ms | **30.31** |
| `中文测试` | 字符串 | 269.5ms | **45.95** |

---

## 3. 本轮头条：午间停顿走完完整生命周期（外部事件实锤）

把 `*.swift`（trigram，候选恒定 ~9213）五轮并排，外部停顿的「爬升→达峰→消退」完整曲线一目了然：

| 时刻 | `*.swift` total | 性质 |
|---|---|---|
| 11:41（R87）| 108ms | 正常 |
| 12:11（R88）| 237ms | 轻度停顿（起）|
| 12:41（R89）| **1002ms** | **重度停顿（峰）** |
| 13:11（R90）| 618ms | 回落中 |
| **13:41（R91）** | **72ms** | **完全恢复** |

候选数恒定下耗时从 1002ms 一路回落到 72ms（甚至低于初始 108ms 基线），**完整刻画了一次外部进程的生命周期**（疑似午间备份 / Spotlight 重建索引于 12:00 启动、12:41 达峰、13:41 前结束）。这是「午间停顿是运行环境而非 MacEverything 代码回归」判断的**最终实锤**——若是代码退化，绝不会自行恢复到优于初始的水平。

**日志佐证**：本轮电池窗口（05-30 13:41-13:42）经检索**无任何 >500ms 条目**；延迟分布的 1-5s 段（23）、0.5-1s 段（16）相比 R90 **均未增加**，证明本轮零新增停顿。

---

## 4. 绝对二层分裂：洁净数据 18/18 不破（间隙恢复 2.88×）

- **数值层**：size 5.91、dm 9.86 → 最高数值 **dm 9.86**
- **字符串层**：content 28.40、path 30.31、中文 45.95 → 最低字符串 **content 28.40**
- **洁净间隙 = 28.40 / 9.86 = 2.88×**，无重叠。

**与 R90 对照印证机制级稳健**：R90 因「双向挤压」（dm 真冷 13.56 + content 偏暖 28.56）把间隙压到史上最窄 2.11×；本轮 dm 退冷回 9.86（数值天花板回到中位）、content 持平 28.40，间隙自然回弹到 2.88×。**两轮对照证明 2.11× 是冷热组合的极端取值、而非分裂在弱化**——分裂在 2.11×（R90 最窄）到 3.49×（R84 最宽）的全区间一律不破。机制不变：纯数值零解引用恒低于字符串解引用散布串池。

**数值层内部**：dm（9.86）本轮仍为天花板但已退冷（R90 真冷 13.56 → R91 中位 9.86），size（5.91）仍为地板。字符串层 content（28.40）与 path（30.31）几乎并列、仅差 1.9 ns/槽——再证 R79「字符串层内部细排序为逐区冷热噪声」。均为 R81 分区机制复现，不构成新结论。

---

## 5. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **443 次**（R90 431）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100，`:159` tombstone 比例阀结构性永不可达——全会话 0 触发、0 回收。

---

## 6. tombstone 趋势：首破 8%，连续 24 轮单调上升

| 轮次 | tombstone | ratio |
|---|---|---|
| R87@11:41 | 427,763 | 7.36% |
| R88@12:11 | 439,394 | 7.54% |
| R89@12:41 | 455,464 | 7.79% |
| R90@13:11 | 464,832 | 7.94% |
| **R91@13:41** | **473,060** | **8.07%** |

会话内单调递增、永不回缩。R43 参考崩溃点 12.97%（~800K），当前 **首破 8%**、过 6 成程。live 基本持平（5,391,356），约 96% 新增记录变为 tombstone。这是会话内唯一的真实恶化趋势，与外部午间停顿（已消退）无关，根因明确为 R67 阀门不可达。

---

## 7. 日志健康度

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **294 条**（R90 282，+12）——增速放缓（R90 为 +21），与本轮洁净一致
- Top-7 峰值 `[18582, 17928, 15749, 8205, 7544, 6811, 6061]` —— 仍与历史**逐字节相同**
- 延迟分布：>5s=**7**（冻结）、1-5s=**23**（R90 23，**未增**）、0.5-1s=**16**（R90 16，**未增**）、0.1-0.5s=248（+12）、中位 **221ms**
- 本轮 +12 越线查询**全部落在 0.1-0.5s 段**（正常查询），无任何新增 >500ms——洁净轮的标志特征

---

## 8. 优化建议（四项 P0，本轮无新增）

1. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调 <12.97%（建议 8–10%）。当前 **首破 8%**，已落入建议阈值区间下沿，紧迫性最高。
2. **🔥 P0｜mlock/madvise 整体常驻热区**：连续三轮（R88/R89/R90）午间停顿命中证明无内存保护时外部压力会反复拖慢任意路径；本轮虽恢复，但需把 path/name 串池、content 列、trigram 索引、候选行、数值 SoA 列整体钉入内存以抵御下次外部事件。
3. **🔥 P0｜无锁快照 flush**：启动 flush 持锁 10.3s。
4. **🔥 P0｜查询超时保护**：R88（1663ms）+ R89（1002ms）+ R90（861ms）三个真实停顿样本——超时熔断 + 降级返回是抵御外部停顿的必要韧性机制。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1🔥｜CJK trigram 索引**：`中文测试` 走 linear-gcd 全扫（中位 269ms / 45.95 ns/槽，本轮字符串最差）。

---

## 9. 结论

- **定性**：🟢 洁净恢复轮。连续三轮污染后首个洁净轮，午间停顿走完完整生命周期（108→237→1002→618→72ms），最终实锤其为外部环境事件而非代码回归。
- **稳健结论 18/18，间隙恢复 2.88×**：与 R90 史上最窄 2.11× 对照，证明窄间隙是「双向挤压」的极端冷热取值、而非分裂弱化——分裂在 2.11×~3.49× 全区间一律不破，是机制级恒定差。
- **诚实修正纪律**：本轮无推测结论被推翻、无新结论需建立；dm 退冷回中位、content/path 近并列，均为 R81/R79 已定机制的复现。
- **唯一持续恶化项**：tombstone **首破 8%**（8.07%，连续 24 轮单调），根因 R67 阀门结构性不可达明确，与已消退的外部停顿无关。修复紧迫性随阈值逼近而最高。
