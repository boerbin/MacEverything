# MacEverything 性能分析报告 R_2606120947

## 1. 本轮范围

- 任务启动时间：2026-06-12 09:47
- 报告文件：`docs/performance_ana/R_2606120947.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=9244`
  - `SESSION_START_TS=2026-06-10 17:02:22.835`
  - `TOTAL_LINES=10090`
  - `SEG_LINES=847`
  - `LAST_LOG_TS=2026-06-12 09:47`

## 2. 执行摘要

**`中文测试` 完成 1-round recovery，但 full-scan 热区仍偏冷；后台 flush/tombstone 风险继续累积。** 本轮 16-query battery 中 0/16 >500ms，Max=326.9ms，P50≈38.6ms，lockWait=0。极佳 11/16。`中文测试` 从上一轮 556.5ms 回落到 244.0ms，latest-session 慢查询数量保持 5，无新增 >500ms；但 `path:/Users` 上升到 326.9ms，full-scan 总和为 751.5ms，仍高于健康低位区间。

最新运行段日志显示：`FLUSH=250`（较上一轮 +12），`SKIPFLUSH=0`，`QUERYADV_GE500=5` 无新增。最新 flush 为 rewrite=2295ms、total=2303ms，维持 2 秒级完整重写。历史灾难 outlier 仍存在：rewrite 最大 895,940ms，total 最大 896,053ms。tombstone=448,750（7.85%），继续上升。

| 指标 | R_2606120917 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 1/16 | **0/16** | **-1** |
| Max | 556.5ms | **326.9ms** | ↓41% |
| P50 | 28.3ms | **38.6ms** | ↑36% |
| lockWait max | 0ms | **0ms** | 持平 |
| 极佳比例(<50ms) | 12/16 | **11/16** | ↓1 |
| 全表扫描总和 | 1013.4ms | **751.5ms** | ↓26% |
| recordCount | 5,705,804 | **5,717,547** | +11,743 |
| liveRecordCount | 5,266,332 | **5,268,797** | +2,465 |
| tombstone | 439,472 (7.70%) | **448,750 (7.85%)** | +9,278 |
| 最新段 FLUSH | 238 | **250** | +12 |
| 最新段 QUERYADV_GE500 | 5 | **5** | 无新增 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 0.8 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 82.0 | 74.3 | advanced-trigram | 0.0 | 9824 | 正常（偏高） |
| `main.cpp` | 5 | 6.7 | 1.2 | advanced-trigram | 0.0 | 85 | 极佳 |
| `*.swift` | 5 | 26.5 | 24.8 | advanced-trigram | 0.0 | 9430 | 极佳 |
| `ext:cpp` | 5 | 45.1 | 42.8 | advanced-ext-index | 0.0 | 3616 | 极佳 |
| `size:>100m` | 5 | 36.8 | 36.7 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 29.5 | 28.7 | advanced-trigram | 0.0 | 3930 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 43.9 | 42.8 | advanced-trigram | 0.0 | 14492 | 极佳 |
| `README` | 5 | 28.5 | 18.4 | advanced-trigram | 0.0 | 17380 | 极佳 |
| `test` | 5 | 24.0 | 19.6 | advanced-trigram | 0.0 | 117812 | 极佳 |
| `MacEverything` | 5 | 14.5 | 1.3 | advanced-trigram | 0.0 | 6946 | 极佳 |
| `dm:today` | 5 | 87.7 | 87.3 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常（偏高） |
| `content:import` | 5 | 180.6 | 171.6 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `path:/Users` | 5 | 326.9 | 318.9 | advanced-linear-gcd | 0.0 | 0 | 正常（偏高） |
| `中文测试` | 0 | 244.0 | 243.7 | advanced-linear-gcd | 0.0 | 0 | 正常（恢复） |
| `verylongquery...` | 0 | 40.3 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈38.6ms，P90≈244.0ms，Max=326.9ms。0/16 查询 >500ms。lockWait=0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **11/16** | 0.8-45.1ms | short-query-cache、主要 trigram、ext-index 与 size filter 仍健康 |
| 正常（50-500ms） | **5/16** | 82.0-326.9ms | `.pdf`、`dm:today` 与 full-scan 路径偏高 |
| 超标（>500ms） | **0/16** | — | 本轮无前台超时 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=5,717,547；
- liveRecordCount=5,268,797；
- tombstone=448,750（**7.85%**）。

## 5. 最新日志段分析

```text
SESSION_START_LINE=9244
SESSION_START_TS=2026-06-10 17:02:22.835
TOTAL_LINES=10090
SEG_LINES=847
QUERYADV=66
QUERYADV_GE500=5
FLUSH=250
SKIPFLUSH=0
COMPACTION=2
```

### 慢查询证据

最新 session 内慢查询数量仍为 5 条，本轮未新增 `>=500ms` 慢查询：

```text
[2026-06-10 17:17:09.559] QueryAdvanced "path:/Users" total=525ms | path=advanced-linear-gcd
[2026-06-10 17:25:15.434] QueryAdvanced "lllllllllllllllllllllllllllllllllllllllllllll" total=1154ms | path=advanced-trigram
[2026-06-10 17:47:13.642] QueryAdvanced "中文测试" total=716ms | path=advanced-linear-gcd
[2026-06-10 23:44:25.566] QueryAdvanced "test" total=676ms | path=advanced-trigram
[2026-06-12 09:17:32.430] QueryAdvanced "中文测试" total=555ms | path=advanced-linear-gcd
```

### flush 极端长尾

最新 session 内 `FLUSH=250`，rewrite 范围 **1440ms - 895940ms**，total 最大 **896053ms**。

最新 flush：

```text
[2026-06-12 09:47:02.526] Flushed v6 flat index, lastEventId=347362955,
liveRecords=5268784 | timing: walSwap=0ms rewrite=2295ms sqcache=6ms total=2303ms
```

Top flush outliers：

```text
[2026-06-12 03:15:02.833] rewrite=895940ms sqcache=2ms total=896053ms
[2026-06-10 23:24:50.749] rewrite=725091ms sqcache=18ms total=735223ms
[2026-06-11 14:30:37.596] rewrite=58273ms sqcache=5ms total=101726ms
[2026-06-10 21:16:27.157] rewrite=32497ms sqcache=5ms total=230084ms
[2026-06-10 23:26:19.499] rewrite=27557ms sqcache=10ms total=27834ms
```

## 6. 问题清单

### P0：flush 完整重写仍有分钟级灾难长尾

- 证据：rewrite 最大 895,940ms，total 最大 896,053ms。
- 最新 flush 为 2303ms，仍是秒级完整重写。
- 根因：完整索引重写在 APFS COW、系统 I/O 压力、睡眠/唤醒或后台任务竞争下被放大。

### P0：flush 频率继续上升且没有退避

- 证据：最新 session `FLUSH=250`，较上一轮 +12；`SKIPFLUSH=0`。
- 影响：高频完整重写持续污染 page cache，增加 full-scan 慢查询概率。

### P0：tombstone 继续增长

- 证据：tombstone=448,750（7.85%），较上一轮增加 9,278。
- 影响：索引体积、flush rewrite 成本和扫描成本继续扩大。

### P1：full-scan 热区仍不稳定

- 证据：`中文测试` 556.5ms → 244.0ms 完成恢复，但 `path:/Users` 231.4ms → 326.9ms；full-scan 总和仍为 751.5ms。
- 影响：虽然本轮没有 >500ms，但 full-scan 热区仍受 page cache 波动影响。

### P1：trigram 中等 postings 偶发偏高

- 证据：`.pdf`=82.0ms（9,824 candidates），高于上一轮 40.7ms。
- 判断：postings 页可能被 flush I/O 部分淘汰。

## 7. 优化建议

### P0：flush 改为增量/COW 页写入

- 分离 stable segment 与 delta segment；常规 flush 只追加 delta。
- 后台低优先级合并，不阻塞前台查询。
- 验收：flush rewrite max <200ms，latest session 不再出现分钟级 outlier。

### P0：为 flush 增加 I/O 压力退避和前台保护

- flush 超过阈值后降低下一轮频率。
- 前台查询活跃时推迟大 flush。
- 持久化线程使用更低 QoS，减少对搜索热路径的干扰。

### P0：降低 flush 频率并合并 WAL 批次

- 引入最小 flush 间隔和最大合并窗口。
- 对高 churn 场景减少完整重写次数。

### P1：提前或分段 compaction

- 不等待全局 25% tombstone 阈值。
- 按 segment 或绝对 tombstone 数触发局部 compaction。

### P2：保护搜索热数据 page cache

- 对 flush 大顺序写考虑 `F_NOCACHE` 或同类策略。
- 对搜索热区使用 `madvise` 预热/保持。

## 8. 下一轮重点

1. `path:/Users` 是否继续上升并接近 500ms。
2. `中文测试` 是否稳定保持 <300ms。
3. latest session 慢查询计数是否继续保持 5。
4. FLUSH 增长速度是否放缓（当前 250）。
5. tombstone 是否继续上升（当前 7.85%）。

## 9. 结论

**本轮 `中文测试` 从 556.5ms 恢复到 244.0ms，前台 >500ms 查询归零。** 但 `path:/Users` 上升到 326.9ms，full-scan 总和仍为 751.5ms，说明 SoA/path 热区还未完全稳定。整体前台表现可接受：P50≈38.6ms，极佳 11/16，lockWait=0。

**后台持久化风险继续累积：FLUSH=250、SKIPFLUSH=0、tombstone=448,750（7.85%），最新 flush=2303ms，历史最大 rewrite 895,940ms。** 优先级仍是 COW/增量 flush、I/O 退避、降低 flush 频率、提前或分段 compaction。
