# MacEverything 性能分析报告 R_2606041210

## 1. 本轮范围

- 任务启动时间：2026-06-04 12:10
- 报告文件：`docs/performance_ana/R_2606041210.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=22075`
  - `SEG_LINES=1049`
  - `LAST_LOG_TS=2026-06-04 12:11`

## 2. 执行摘要

**I/O 压力未升级为灾难——R84-R86 模式被打断。** `中文测试`=573ms（>500ms，新受影响查询），但 `.pdf`=375ms（从 630ms 恢复），`README`=31ms（从 1,071ms 恢复）。**1/16 查询 >500ms（R94 为 2/16）。lockWaitMs 全部为 0（R94 为 46.1ms）。** P50 ≈ 147ms，Max=573ms。

**R94 预警的灾难阶段未到来**——关键原因：lockWait 归零 + trigram 路径查询恢复。受影响查询从 trigram 路径（`.pdf`, `README`）转移到 linear-gcd 路径（`中文测试`），说明 I/O 压力在不同索引区域间游走而非集中恶化。

**tombstone 突破 8% 里程碑**：510,431 条（8.16%，153MB），增量 +16,262。

### 横向对比（最近 5 轮）

| Query | R91 | R92 | R93 | R94 | R95 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 12 | 5 | 75 | 11 | **18** | 正常 |
| `.pdf` | 192 | 820 | 635 | 630 | **375** | **恢复中** |
| `*.swift` | 125 | 430 | 22 | 226 | **264** | 波动 |
| `hello world` | 273 | 412 | 352 | 240 | **210** | 改善 |
| `README` | 126 | 27 | 14 | 1071 | **31** | **完全恢复** |
| `path:/Users` | 314 | 222 | 144 | 218 | **394** | 偏高 |
| `中文测试` | — | 279 | 234 | 391 | **573** | **新 >500ms** |
| P50 | 126 | 131 | 57 | 171 | **147** | 正常波动 |

### I/O 压力事件演化——未升级为灾难

| 轮次 | P50 | Max | >500ms | lockWait max | 状态 |
|------|----:|----:|-------:|------------:|------|
| R92（轻度） | 131ms | 820ms | 1/16 | 2.2ms | I/O 压力 |
| R93（改善） | 57ms | 635ms | 1/16 | 0ms | 热缓存掩盖 |
| R94（中度） | 171ms | 1,071ms | 2/16 | 46.1ms | 加剧 |
| **R95（缓解）** | **147ms** | **573ms** | **1/16** | **0ms** | **压力缓解** |

**与 R84-R86 模式对比**：
- R84→R85→R86：轻度→中度→灾难（连续 3 轮恶化）
- R92→R93→R94→R95：轻度→改善→中度→**缓解**（第 4 轮压力下降）

**差异关键**：R95 lockWait=0 表明 FSWatcher 活动暂时降低（可能用户暂停了密集文件操作），打断了恶化螺旋。但这不意味安全——tombstone 8.16% 的结构性风险不变。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 17.6 | 0.0 | short-query-cache | 0 | 0 | 正常 |
| `.pdf` | 5 | 374.6 | 371.2 | advanced-trigram | 0 | 9952 | **恢复中** |
| `main.cpp` | 5 | 71.2 | 64.5 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 264.4 | 248.7 | advanced-trigram | 0 | 9356 | 波动 |
| `ext:cpp` | 5 | 136.8 | 135.7 | advanced-ext-index | 0 | 3701 | 正常 |
| `size:>100m` | 5 | 79.5 | 78.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 210.0 | 189.4 | advanced-trigram | 0 | 3613 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 158.7 | 157.7 | advanced-trigram | 0 | 16036 | 正常 |
| `README` | 5 | 30.8 | 28.1 | advanced-trigram | 0 | 17671 | **完全恢复** |
| `test` | 5 | 25.7 | 15.1 | advanced-trigram | 0 | 130121 | 极快 |
| `MacEverything` | 5 | 22.9 | 1.6 | advanced-trigram | 0 | 6803 | 极快 |
| `dm:today` | 5 | 77.8 | 77.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 181.1 | 170.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 394.1 | 382.8 | advanced-linear-gcd | 0 | 0 | 偏高 |
| `中文测试` | 0 | 572.9 | 335.2 | advanced-linear-gcd | 0 | 0 | **>500ms** |
| `verylongquery...` | 0 | 2.0 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 147ms，P90 ≈ 394ms，Max = 573ms。1/16 查询 >500ms。lockWaitMs 全部为 0。**

### `中文测试`=573ms 分析

- **历史数据**：R89=318ms, R90=52ms, R91=386ms, R92=279ms, R93=234ms, R94=391ms, **R95=573ms**。
- 走 advanced-linear-gcd 路径（全表线性扫描），candidates=0（无结果）。
- queryTimeMs=573ms，phase2Ms=335ms——**queryTimeMs - phase2Ms = 238ms 为非 phase2 开销**。
- 对比 `path:/Users`（同为 linear-gcd）：394ms, phase2=383ms, 非 phase2=11ms。
- **`中文测试` 的 238ms 非 phase2 开销异常**——可能原因：
  1. Unicode 处理（中文字符需要额外的编码/解码开销）。
  2. linear-gcd 路径的初始化/dispatch 开销在系统负载下放大。
  3. 与 I/O 压力叠加：linear 扫描 6.25M 记录时遇到部分冷页面。

### trigram 路径恢复分析

| 查询 | R94 | R95 | 变化 | 说明 |
|------|----:|----:|-----:|------|
| `.pdf` | 630ms | 375ms | -40% | trigram 索引页面恢复缓存 |
| `README` | 1,071ms | 31ms | -97% | 完全恢复 |
| `test` | 182ms | 26ms | -86% | 完全恢复 |
| `MacEverything` | 74ms | 23ms | -69% | 完全恢复 |

R94 的查询电池预热了 trigram 索引区域，加上 lockWait=0（FSWatcher 活动降低），trigram 路径查询全面恢复。但 linear-gcd 路径（需要扫描全部 6.25M 记录）因数据量更大，缓存覆盖更难完整。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,252,324，liveRecordCount=5,741,893（tombstone=**510,431**，**8.16%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=343  QUERYADV_GE500=48
FLUSH=0  COMPACTION=2  SKIPFLUSH=404
DIAG_TRIGRAM_SKIP=103
```

- QUERYADV 从 335 增至 343（+8）。
- QUERYADV_GE500 保持 48（本轮 `中文测试`=573ms 但在日志写入时间窗口内可能未计入）。
- SKIPFLUSH 从 392 增至 404（**+12，恢复稳态**）。
- DIAG_TRIGRAM_SKIP 从 102 增至 103（+1，稳态）。

## 6. 问题清单

### Critical：tombstone 突破 8% 里程碑（510,431 条，153MB）

- 增长轨迹（稳态轮次）：
  ```
  R91: 454K (+10.5K) → R92: 480K (+25.5K) → R94: 494K (+11.9K)
  → R95: 510K (+16.3K)
  ```
- 本轮 +16,262——高于稳态 12K/轮，日间用户活动持续加速。
- **累计内存开销**：~510K × 300 字节 ≈ **153MB**。
- 连续第 35 轮超 2%，第 16 轮超过 5%，第 11 轮超过 6%，第 8 轮超过 7%，**首轮超过 8%**。

### 里程碑完整追踪

| 里程碑 | 轮次 | 值 | 内存 | 间隔 |
|--------|------|----:|-----:|-----:|
| 突破 2% | R60 | 100K | 30MB | — |
| 突破 3% | R66 | 185K | 56MB | 6 轮 |
| 突破 4% | R72 | 240K | 72MB | 6 轮 |
| 突破 5% | R79 | 306K | 92MB | 7 轮 |
| 突破 100MB | R82 | 340K | 102MB | 3 轮 |
| 突破 6% | R84 | 382K | 115MB | 5 轮 |
| 灾难事件 #1 | R86 | 409K | 123MB | 2 轮 |
| 突破 7% | R88 | 430K | 129MB | 2 轮 |
| I/O 事件 #2 | R92 | 480K | 144MB | 4 轮 |
| **突破 8%** | **R95** | **510K** | **153MB** | **3 轮** |

**每个百分点突破所需轮次**：6→6→7→5→2→3——**加速中**，因为基数增大但增量稳定。

### Critical：I/O 压力事件暂时缓解但结构性风险不变

- R92-R94 恶化周期被打断（R95 lockWait=0，>500ms 降至 1/16）。
- 但打断原因是外部的（用户活动暂减），非系统层面修复。
- tombstone 8.16% 意味着缓存挤压更严重——**下次事件的触发门槛更低**。

### Medium：`中文测试` 首次突破 500ms（573ms）

- 走 linear-gcd 全表扫描路径。
- queryTimeMs - phase2Ms = 238ms 异常高（`path:/Users` 仅 11ms）。
- Unicode 处理开销 + I/O 压力叠加。
- linear-gcd 路径在 tombstone 8.16% 下需扫描额外 510K 条死记录。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（8.16%，153MB，两次 I/O 事件周期已验证）

- **35 轮连续超标。** 两个完整的 I/O 事件周期（R84-R86, R92-R94）已验证因果链。
- **8% 里程碑**——每增 1% 间隔从 6-7 轮缩短至 2-3 轮。
- compaction 后释放 153MB → 消除 I/O 压力事件根因 + 消除 linear-gcd 额外扫描 510K 条记录。
- **预估收益**：正常状态 P50 从 100-170ms 降至 60-100ms，I/O 事件完全消除。

### P0 紧急：`size:>100m` 全表扫描超时保护
### P0：short-query-cache GCD 隔离
### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P1：linear-gcd 路径 Unicode 查询优化
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. I/O 压力是否反弹（`中文测试` 是否持续 >500ms，trigram 查询是否再次恶化）。
2. tombstone 增速是否维持 >12K/轮（日间加速）。
3. lockWait 是否再次出现。

## 9. 结论

**I/O 压力缓解——R84-R86 恶化模式被打断。** `中文测试`=573ms（>500ms，新受影响查询），但 `.pdf`=375ms（恢复），`README`=31ms（完全恢复）。1/16 查询 >500ms（R94 的 2/16 下降）。lockWaitMs=0（R94 的 46.1ms 归零）。P50=147ms。**R92-R94 恶化周期在第 4 轮（R95）被打断**——与 R84-R86 的第 3 轮灾难不同。打断原因为外部（用户活动暂减）而非系统修复。**tombstone 突破 8% 里程碑**（510,431 条，153MB，+16,262），每百分点突破间隔从 6-7 轮缩短至 2-3 轮。I/O 压力从 trigram 路径转移到 linear-gcd 路径——问题在不同索引区域间游走，反映 153MB tombstone 对全局页面缓存的持续挤压。
