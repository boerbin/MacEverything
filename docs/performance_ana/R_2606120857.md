# MacEverything 性能分析报告 R_2606120857

## 1. 本轮范围

- 任务启动时间：2026-06-12 08:57
- 报告文件：`docs/performance_ana/R_2606120857.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=9244`
  - `SESSION_START_TS=2026-06-10 17:02:22.835`
  - `TOTAL_LINES=9946`
  - `SEG_LINES=703`
  - `LAST_LOG_TS=2026-06-12 08:57`

## 2. 执行摘要

**前台整体仍健康，但 `中文测试` 回升到 311.8ms，全表扫描明显变慢；后台 flush/tombstone 风险继续累积。** 本轮 16-query battery 中 0/16 >500ms，Max=311.8ms，P50≈23.7ms，lockWait=0。极佳 12/16，保持健康。Trigram 绝大多数仍在 5-55ms 区间，`test` 从上一轮 109.6ms 恢复到 18.5ms；但全表扫描总和升至 687.6ms（`content:import`=182.1ms, `path:/Users`=193.7ms, `中文测试`=311.8ms），较上一轮 478.1ms 上升 44%。

最新运行段日志显示：`FLUSH=230`（较上一轮 +16），`SKIPFLUSH=0`，`QUERYADV_GE500=4` 无新增。最新 flush 为 rewrite=2986ms、total=2990ms，较上一轮最新 flush 1940ms 回升 54%。历史灾难 outlier 仍存在：rewrite 最大 895,940ms，total 最大 896,053ms。tombstone=433,294（7.60%），继续上升。

| 指标 | R_2606120818 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 174.4ms | **311.8ms** | ↑79% |
| P50 | 24.1ms | **23.7ms** | 基本持平 |
| lockWait max | 0ms | **0ms** | 持平 |
| 极佳比例(<50ms) | 10/16 | **12/16** | ↑2 |
| 全表扫描总和 | 478.1ms | **687.6ms** | ↑44% |
| recordCount | 5,681,395 | **5,698,719** | +17,324 |
| liveRecordCount | 5,264,311 | **5,265,425** | +1,114 |
| tombstone | 417,084 (7.34%) | **433,294 (7.60%)** | +16,210 |
| 最新段 FLUSH | 214 | **230** | +16 |
| 最新段 QUERYADV_GE500 | 4 | **4** | 无新增 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 2.4 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 23.0 | 20.3 | advanced-trigram | 0.0 | 9822 | 极佳 |
| `main.cpp` | 5 | 5.0 | 1.6 | advanced-trigram | 0.0 | 85 | 极佳 |
| `*.swift` | 5 | 24.3 | 22.5 | advanced-trigram | 0.0 | 9419 | 极佳 |
| `ext:cpp` | 5 | 9.3 | 9.2 | advanced-ext-index | 0.0 | 3616 | 极佳 |
| `size:>100m` | 5 | 46.0 | 43.0 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 33.4 | 32.1 | advanced-trigram | 0.0 | 3930 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 54.5 | 54.0 | advanced-trigram | 0.0 | 14489 | 正常 |
| `README` | 5 | 10.2 | 4.2 | advanced-trigram | 0.0 | 17396 | 极佳 |
| `test` | 5 | 18.5 | 11.9 | advanced-trigram | 0.0 | 117789 | 极佳（恢复） |
| `MacEverything` | 5 | 13.1 | 1.3 | advanced-trigram | 0.0 | 6944 | 极佳 |
| `dm:today` | 5 | 44.8 | 44.6 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `content:import` | 5 | 182.1 | 166.7 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `path:/Users` | 5 | 193.7 | 185.2 | advanced-linear-gcd | 0.0 | 0 | 正常 |
| `中文测试` | 0 | 311.8 | 311.3 | advanced-linear-gcd | 0.0 | 0 | 正常（回升） |
| `verylongquery...` | 0 | 1.9 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈23.7ms，P90≈193.7ms，Max=311.8ms。0/16 查询 >500ms。lockWait=0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **12/16** | 1.9-46.0ms | short-query-cache、主要 trigram、ext-index 与多数 filter 均健康 |
| 正常（50-500ms） | **4/16** | 54.5-311.8ms | `中文测试` 和 full-scan 查询回升 |
| 超标（>500ms） | **0/16** | — | 本轮无前台超时 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=5,698,719；
- liveRecordCount=5,265,425；
- tombstone=433,294（**7.60%**）。

## 5. 最新日志段分析

```text
SESSION_START_LINE=9244
SESSION_START_TS=2026-06-10 17:02:22.835
TOTAL_LINES=9946
SEG_LINES=703
QUERYADV=60
QUERYADV_GE500=4
FLUSH=230
SKIPFLUSH=0
COMPACTION=2
```

### 慢查询证据

最新 session 内慢查询数量仍为 4 条，本轮未新增 `>=500ms` 慢查询：

```text
[2026-06-10 17:17:09.559] QueryAdvanced "path:/Users" total=525ms | path=advanced-linear-gcd
[2026-06-10 17:25:15.434] QueryAdvanced "lllllllllllllllllllllllllllllllllllllllllllll" total=1154ms | path=advanced-trigram
[2026-06-10 17:47:13.642] QueryAdvanced "中文测试" total=716ms | path=advanced-linear-gcd
[2026-06-10 23:44:25.566] QueryAdvanced "test" total=676ms | path=advanced-trigram
```

### flush 极端长尾

最新 session 内 `FLUSH=230`，rewrite 范围 **1440ms - 895940ms**，total 最大 **896053ms**。

最新 flush：

```text
[2026-06-12 08:56:58.991] Flushed v6 flat index, lastEventId=346818297,
liveRecords=5265419 | timing: walSwap=0ms rewrite=2986ms sqcache=2ms total=2990ms
```

Top flush outliers：

```text
[2026-06-12 03:15:02.833] rewrite=895940ms sqcache=2ms total=896053ms
[2026-06-10 23:24:50.749] rewrite=725091ms sqcache=18ms total=735223ms
[2026-06-11 14:30:37.596] rewrite=58273ms sqcache=5ms total=101726ms
[2026-06-10 21:16:27.157] rewrite=32497ms sqcache=5ms total=230084ms
[2026-06-10 23:26:19.499] rewrite=27557ms sqcache=10ms total=27834ms
```

## 6. 问题清单

### P0：flush 完整重写仍有分钟级灾难长尾

- 证据：rewrite 最大 895,940ms，total 最大 896,053ms。
- 最新 flush 从 1940ms 回升到 2986ms，说明即便没有分钟级 outlier，常规 flush 仍在 2-3 秒级波动。
- 根因：完整索引重写在 APFS COW、系统 I/O 压力、睡眠/唤醒或后台任务竞争下被放大。

### P0：flush 频率继续上升且没有退避

- 证据：最新 session `FLUSH=230`，较上一轮 +16；`SKIPFLUSH=0`。
- 影响：高频完整重写持续污染 page cache，并扩大下一次长尾出现概率。

### P0：tombstone 继续增长

- 证据：tombstone=433,294（7.60%），较上一轮增加 16,210。
- 影响：索引体积、flush rewrite 成本和扫描成本继续扩大。

### P1：全表扫描回升

- 证据：全表扫描总和 478.1ms → 687.6ms；`中文测试` 150.9ms → 311.8ms。
- 影响：当前未超 500ms，但已重新接近此前慢查询形态。
- 可能原因：flush 频率上升导致 SoA/path 数据页被部分淘汰。

## 7. 优化建议

### P0：flush 改为增量/COW 页写入

- 分离 stable segment 与 delta segment；常规 flush 只追加 delta。
- 后台低优先级合并，不阻塞前台查询。
- 验收：flush rewrite max <200ms，latest session 不再出现分钟级 outlier。

### P0：为 flush 增加 I/O 压力退避

- flush 超过阈值后降低下一轮频率。
- 前台查询活跃时推迟大 flush。
- 持久化线程使用更低 QoS，减少对搜索热路径的干扰。

### P0：降低 flush 频率并合并 WAL 批次

- 引入最小 flush 间隔和最大合并窗口。
- 对高 churn 场景减少完整重写次数。

### P1：提前或分段 compaction

- 不等待全局 25% tombstone 阈值。
- 按 segment 或绝对 tombstone 数触发局部 compaction。

### P2：保护搜索热数据 page cache

- 对 flush 大顺序写考虑 `F_NOCACHE` 或同类策略。
- 对搜索热区使用 `madvise` 预热/保持。

## 8. 下一轮重点

1. `中文测试` 是否继续上升并接近 500ms。
2. full-scan 总和是否回落到 400-500ms 区间。
3. latest session 慢查询计数是否仍维持 4，无新增 >500ms。
4. FLUSH 增长速度是否放缓（当前 230，上一轮 +16）。
5. tombstone 是否继续快速上升（当前 7.60%）。

## 9. 结论

**本轮前台查询整体仍健康：0/16 >500ms，P50≈23.7ms，极佳 12/16。** 但 Max 回升到 311.8ms，主要来自 `中文测试` 和 full-scan 路径，说明 page cache/SoA 热区出现部分回落。

**后台持久化风险继续累积：latest session FLUSH=230、SKIPFLUSH=0、tombstone=433,294（7.60%），最新 flush 回升到 2990ms，历史最大 rewrite 仍为 895,940ms。** 优先级仍是 COW/增量 flush、I/O 退避、降低 flush 频率、提前或分段 compaction。
