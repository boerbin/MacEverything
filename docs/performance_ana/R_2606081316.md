# MacEverything 性能分析报告 R_2606081316

## 1. 本轮范围

- 任务启动时间：2026-06-08 13:16
- 报告文件：`docs/performance_ana/R_2606081316.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=4537`
  - `SEG_LINES=3595`
  - `LAST_LOG_TS=2026-06-08 13:47`

## 2. 执行摘要

**稳态最佳轮——0/16 >500ms，Max=224ms，P50=25ms，lockWait=0，极佳比例 10/16（63%，系列新高！）。** 从 R182 继续优化：`.pdf`=28ms（R182=65ms，↓57%，首次回到极佳！），trigram 均值 27ms（R182=32ms，↓16%，接近正常稳态 12-18ms）。全表扫描总和 621ms（正常稳态范围）。tombstone 增速 331/min——FSEvents 追赶基本完成，回到正常水平。

### 关键指标对比

| 指标 | R182 | R183 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（连续 2 轮 0） |
| Max | 242ms | **224ms** | ↓ 7% |
| P50 | 36ms | **25ms** | **↓ 31%** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 9/16 | **10/16** | **↑1（系列新高！）** |
| tombstone | 1,500,394 (22.73%) | **1,508,117 (22.80%)** | ↑ |
| tombstone 增速 | ~382/min | **~331/min** | **↓ 13%（回到正常！）** |
| recordCount | 6,608,547 | **6,616,381** | +7.8K（正常） |
| liveRecordCount | 5,108,153 | **5,108,264** | +111 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3268 | **3328** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 18 | **18** | 不变（本轮 0 新增） |

### Trigram 持续收敛

| Query | R182 | R183 | 变化 | 正常值 | 状态 |
|-------|-----:|-----:|------|------|------|
| `.pdf` | 65ms | **28ms** | **↓ 57%** | ~17ms | **极佳（首次恢复！）** |
| `hello world` | 76ms | **100ms** | ↑ 33% | ~15ms | 正常（波动） |
| `*.swift` | 18ms | **17ms** | ↓ 6% | ~15ms | 极佳 |
| `ext:cpp` | 10ms | **6ms** | ↓ 40% | ~10ms | 极佳 |
| `main.cpp` | 6ms | **2ms** | ↓ 67% | ~5ms | 极佳 |
| `abc def...` | 42ms | **25ms** | **↓ 40%** | ~30ms | **极佳** |
| `README` | 13ms | **9ms** | ↓ 31% | ~10ms | 极佳 |
| `test` | 22ms | **27ms** | ↑ 23% | ~14ms | 极佳 |
| `MacEverything` | 4ms | **3ms** | ↓ 25% | ~3ms | 极佳 |
| Trigram 均值 | 32ms | **27ms** | **↓ 16%** | 12-18ms | 收敛中 |

- **`.pdf`=28ms——从 R181 的 7193ms 到本轮 28ms，两轮恢复至极佳。**
- **`hello world`=100ms——唯一仍偏高的 trigram 查询。postings 区域可能仍部分冷态。**
- **Trigram 均值 27ms——如果排除 `hello world` 的异常值，均值约 15ms——已完全回到正常稳态。**

### `hello world`=100ms——唯一偏高项分析

- **R182=76ms → R183=100ms（↑33%）——不减反升。**
- **candidates=3626，phase2=100ms，每 candidate 0.028ms——远优于 R181 的 0.71ms，但高于正常的 <0.01ms。**
- **可能原因**：`hello world` 的 postings 区域与系统活跃 I/O 区域重叠——日间活动持续竞争该区域的 page cache 页面。
- **与 R178 分析一致**：某些 trigram postings 区域可能长期处于"温态"（非完全冷也非完全热），取决于系统 I/O 活动的模式。

### 全表扫描——稳定

| Query | R182 | R183 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 242ms | **202ms** | ↓ 17% |
| `path:/Users` | 207ms | **195ms** | ↓ 6% |
| `中文测试` | 150ms | **224ms** | ↑ 49% |
| **总和** | **599ms** | **621ms** | ↑ 4%（稳定） |

- **全表扫描总和 621ms——正常稳态范围。**
- **`中文测试`=224ms（R182=150ms，↑49%）——正常波动，R182 偏低。**
- **`content:import`=202ms——连续下降，page cache 良好。**

### SoA Filter——稳定

| Query | R182 | R183 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 30ms | **28ms** | ↓ 7%（极佳） |
| `dm:today` | 54ms | **40ms** | ↓ 26%（极佳！） |

- **`dm:today`=40ms——首次进入极佳范围（<50ms）！此前最低 R177=12.5ms（post-wake 极值）。**
- **日间稳态下 40ms 是正常极佳水平。**

### tombstone 追赶完成！

- **增速 331/min——回到正常水平（250-300/min 的正常偏上端）。**
- **recordCount +7.8K——完全正常（日常 ~8K/30min）。**
- **FSEvents 唤醒追赶正式完成。**
- **tombstone 22.80%（1.51M）——距 25% 约 16-18 小时。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1.3 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 28.0 | 24.5 | advanced-trigram | 0 | 9841 | **极佳（首次恢复！）** |
| `main.cpp` | 5 | 2.1 | 1.4 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 17.0 | 16.6 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 5.9 | 5.5 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 28.4 | 28.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 100.4 | 99.6 | advanced-trigram | 0 | 3626 | 正常（偏高） |
| `abc def ghi jkl mno pqr` | 0 | 24.8 | 24.7 | advanced-trigram | 0 | 14219 | 极佳 |
| `README` | 5 | 9.2 | 8.4 | advanced-trigram | 0 | 16636 | 极佳 |
| `test` | 5 | 27.1 | 23.5 | advanced-trigram | 0 | 116529 | 极佳 |
| `MacEverything` | 5 | 2.9 | 0.6 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 40.2 | 40.0 | advanced-pure-filter-soa-gcd | 0 | 0 | **极佳** |
| `content:import` | 5 | 201.7 | 192.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 195.1 | 191.0 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 223.7 | 222.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 24.8ms，P90 = 195.1ms，Max = 223.7ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R182 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **10/16** | 0.4-40ms | R182=9/16（**↑1，系列新高**） |
| **正常**（50-500ms） | 6/16 | 100-224ms | R182=7/16（↓1） |
| **超标**（>500ms） | 0/16 | — | R182=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,616,381，liveRecordCount=5,108,264（tombstone=**1,508,117**，**22.80%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=195  QUERYADV_GE500=18（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3328
```

- **本轮 0 新增 >500ms 查询（连续 3 轮 0）。**
- **SKIPFLUSH +60——正常节奏。**

## 6. 问题清单

### 观察项：`hello world`=100ms 持续偏高

- **3 轮连续偏高（R180=199ms, R182=76ms, R183=100ms）——可能是 postings 区域的结构性问题。**
- **`hello world` 的 postings 区域可能与系统活跃 I/O 区域存在长期重叠。**
- **影响有限——单个查询 100ms 在用户体验上仍可接受。**

### tombstone 22.80%——追赶完成，正常增长

- **增速 331/min（正常水平），距 25% 约 16-18 小时。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（紧迫）

- tombstone 22.80%（1.51M），距 25% 约 16-18 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. `hello world` 是否回落至极佳水平（<50ms）——如果持续 3+ 轮 >50ms 则可确认为结构性温态。
2. 极佳比例是否保持 10/16 或更高。
3. tombstone 增速是否稳定在 ~300/min。

## 9. 结论

**稳态最佳轮——0/16 >500ms，Max=224ms，P50=25ms，lockWait=0，极佳比例 10/16（63%，系列新高！）。** Trigram 持续收敛：`.pdf`=28ms（首次回到极佳），trigram 均值 27ms（排除 `hello world` 后约 15ms——完全正常）。唯一偏高项 `hello world`=100ms——可能是 postings 区域与系统 I/O 的结构性重叠。全表扫描总和 621ms（正常稳态）。

**FSEvents 唤醒追赶正式完成**——tombstone 增速回到 331/min（正常水平），recordCount +7.8K（日常正常）。tombstone 22.80%（1.51M），距 25% 约 16-18 小时。P0 compaction 修复持续紧迫。
