# MacEverything 性能分析报告 R_2606031335

## 1. 本轮范围

- 任务启动时间：2026-06-03 13:35
- 报告文件：`docs/performance_ana/R_2606031335.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21376`
  - `SEG_LINES=350`
  - `LAST_LOG_TS=2026-06-03 16:43`

## 2. 执行摘要

服务正常运行（第 15 轮连续可用）。性能改善：P50 ≈ 106ms，Max=415ms。`x` 恢复正常（6ms），`*.swift` 从前轮 488ms 回落至 233ms。所有查询连续第 12 轮 < 500ms。tombstone 增至 158,357 条（2.69%），连续第 4 轮超过 2%。SKIPFLUSH 突破 100 次大关。

### 横向对比（最近 5 轮）

| Query | R59 | R60 | R61 | R62 | R63 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 19 | 14 | 7 | 93 | **6** | 恢复正常 |
| `.pdf` | 490 | 349 | 231 | 325 | **415** | 波动 |
| `*.swift` | 226 | 179 | 172 | 488 | **233** | 恢复 |
| `hello world` | 430 | 436 | 252 | 297 | **274** | 改善 |
| `path:/Users` | 164 | 278 | 227 | 255 | **307** | 波动 |
| P50 | 176 | 178 | 147 | 148 | **106** | 改善 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 5.8 | 0.0 | short-query-cache | 0 | 0 | 极快 |
| `.pdf` | 5 | 415.1 | 394.0 | advanced-trigram | 0 | 9944 | 偏慢 |
| `main.cpp` | 5 | 34.7 | 23.0 | advanced-trigram | 0 | 85 | 快 |
| `*.swift` | 5 | 232.6 | 228.9 | advanced-trigram | 0 | 9356 | 恢复 |
| `ext:cpp` | 5 | 137.3 | 133.6 | advanced-ext-index | 0 | 3702 | 正常 |
| `size:>100m` | 5 | 105.5 | 104.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 273.8 | 264.8 | advanced-trigram | 0 | 3613 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 90.2 | 88.7 | advanced-trigram | 0 | 15982 | 快 |
| `README` | 5 | 17.1 | 13.2 | advanced-trigram | 0 | 17717 | 快 |
| `test` | 5 | 36.6 | 29.8 | advanced-trigram | 0 | 129960 | 快 |
| `MacEverything` | 5 | 18.3 | 1.7 | advanced-trigram | 0 | 6802 | 快 |
| `dm:today` | 5 | 52.8 | 52.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 快 |
| `content:import` | 5 | 178.8 | 164.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 307.3 | 300.1 | advanced-linear-gcd | 0 | 0 | 偏慢 |
| `中文测试` | 0 | 195.8 | 193.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 3.1 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 106ms，P90 ≈ 307ms，Max = 415ms。全部查询连续第 12 轮 < 500ms。lockWaitMs 全部为 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,883,598，liveRecordCount=5,725,241（tombstone=**158,357**，**2.69%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=119  QUERYADV_GE500=5
FLUSH=1  COMPACTION=2  SKIPFLUSH=109
DIAG_TRIGRAM_SKIP=68
```

- QUERYADV 从 111 增至 119（+8）。
- QUERYADV_GE500 保持 5（本轮 Max=415ms）。
- **SKIPFLUSH 突破 100 次大关**：从 96 增至 109（+13）。7.4 小时内 109 次跳过。
- DIAG_TRIGRAM_SKIP 从 67 增至 68（+1）。

## 6. 问题清单

### Critical：tombstone 连续 4 轮超过 2%（2.69%，158,357 条）

- 增长轨迹：
  ```
  R60: 128K (2.19%) → R61: 138K (2.36%) → R62: 149K (2.54%) → R63: 158K (2.69%)
  ```
- 每轮增量约 10K 条，增速稳定。
- **累计内存开销**：~158K × 300 字节 ≈ 47MB 额外内存。
- **查询开销**：phase2 扫描 totalRecords 包含 158K tombstone（2.69% 额外扫描）。
- SKIPFLUSH=109，flush-triggered compaction 完全不可用。

### Medium：`.pdf` 波动仍大（231-490ms 范围）

- 本轮 415ms，candidates=9944 不变。I/O 负载时可轻松突破 500ms。

### Low：SKIPFLUSH 突破 100 次

- 7.4 小时内 109 次跳过，WAL 条目稳定在 68。
- 表明文件系统变动率低但持续产生新 tombstone（通过 FSWatcher 捕获的临时文件创建/删除）。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（已超阈值 4 轮）

- tombstone 2.69%，增速稳定约 10K/轮。
- **最小实施方案**：在 `IndexPersistence` 的 SKIPFLUSH 分支中增加一行检查：
  ```
  if (tombstoneRatio > 0.02) triggerCompaction();
  ```
- 复用现有 compaction 代码路径，零新增复杂度。

### P0：补齐进程生命周期监控（承前轮）

### P1：将 `.xxx`/`*.xxx` 扩展名模式路由到 ext-index（承前轮）

### P2：short-query-cache 偶发延迟根因（承前轮，优先级降低——本轮正常）

## 8. 下一轮重点

1. tombstone 增长——预计约 7 轮后达 3.5%。
2. `.pdf` 是否会在高 I/O 负载下突破 500ms。
3. SKIPFLUSH 累积速率是否变化。

## 9. 结论

服务连续第 15 轮正常运行，所有查询连续第 12 轮 <500ms（Max=415ms）。`x` 和 `*.swift` 均从前轮异常恢复正常。tombstone 连续第 4 轮超过 2%（158,357 条，2.69%），SKIPFLUSH 突破 100 次大关（109 次），进一步确认 flush-triggered compaction 完全不可用。最小修复方案：在 SKIPFLUSH 分支增加一行 tombstone 占比检查即可触发现有 compaction 流程。
