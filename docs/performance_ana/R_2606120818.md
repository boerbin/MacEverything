# MacEverything 性能分析报告 R_2606120818

## 1. 本轮范围

- 任务启动时间：2026-06-12 08:18
- 报告文件：`docs/performance_ana/R_2606120818.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=9244`
  - `SESSION_START_TS=2026-06-10 17:02:22.835`
  - `TOTAL_LINES=9862`
  - `SEG_LINES=619`
  - `LAST_LOG_TS=2026-06-12 08:18`

## 2. 执行摘要

**前台查询仍健康，但较上一轮略回落；后台 flush/tombstone 风险继续累积。** 本轮 16-query battery 中 0/16 >500ms，Max=174.4ms，P50≈24.1ms，lockWait=0。极佳 10/16，较 R_2606120755 的 12/16 下降 2 个。`x`=1.9ms，short-query-cache 稳定；trigram 大多仍在 8-30ms 区间，但 `test`=109.6ms、`abc def...`=78.9ms 偏高。全表扫描总和 478.1ms（`content:import`=174.4ms, `path:/Users`=152.8ms, `中文测试`=150.9ms），较上一轮 418.5ms 回升 14%，但仍处于健康区间。

最新运行段日志显示：`FLUSH=214`（较上一轮 +9），`SKIPFLUSH=0`，`QUERYADV_GE500=4` 无新增。最新 flush 为 rewrite=1940ms，总体回到 2 秒级，但历史灾难 outlier 仍存在：rewrite 最大 895,940ms，total 最大 896,053ms。tombstone=417,084（7.34%），继续上升。

| 指标 | R_2606120755 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 146.9ms | **174.4ms** | ↑19% |
| P50 | 18.3ms | **24.1ms** | ↑32% |
| lockWait max | 0ms | **0ms** | 持平 |
| 极佳比例(<50ms) | 12/16 | **10/16** | ↓2 |
| 全表扫描总和 | 418.5ms | **478.1ms** | ↑14% |
| recordCount | 5,669,455 | **5,681,395** | +11,940 |
| liveRecordCount | 5,262,957 | **5,264,311** | +1,354 |
| tombstone | 406,498 (7.17%) | **417,084 (7.34%)** | +10,586 |
| 最新段 FLUSH | 205 | **214** | +9 |
| 最新段 QUERYADV_GE500 | 4 | **4** | 无新增 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 1.9 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 14.0 | 12.3 | advanced-trigram | 0.0 | 9821 | 极佳 |
| `main.cpp` | 5 | 8.2 | 1.9 | advanced-trigram | 0.0 | 85 | 极佳 |
| `*.swift` | 5 | 17.4 | 14.9 | advanced-trigram | 0.0 | 9419 | 极佳 |
| `ext:cpp` | 5 | 7.6 | 6.5 | advanced-ext-index | 0.0 | 3616 | 极佳 |
| `size:>100m` | 5 | 23.9 | 23.8 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 26.5 | 25.3 | advanced-trigram | 0.0 | 3930 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 78.9 | 77.7 | advanced-trigram | 0.0 | 14486 | 正常（偏高） |
| `README` | 5 | 24.2 | 13.4 | advanced-trigram | 0.0 | 17394 | 极佳 |
| `test` | 5 | 109.6 | 88.5 | advanced-trigram | 0.0 | 117787 | 正常（偏高） |
| `MacEverything` | 5 | 12.4 | 0.8 | advanced-trigram | 0.0 | 6944 | 极佳 |
| `dm:today` | 5 | 53.7 | 53.5 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 174.4 | 167.8 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `path:/Users` | 5 | 152.8 | 147.7 | advanced-linear-gcd | 0.0 | 0 | 正常 |
| `中文测试` | 0 | 150.9 | 150.4 | advanced-linear-gcd | 0.0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈24.1ms，P90≈152.8ms，Max=174.4ms。0/16 查询 >500ms。lockWait=0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **10/16** | 0.2-26.5ms | short-query-cache、主要 trigram、ext-index 与 size filter 仍健康 |
| 正常（50-500ms） | **6/16** | 53.7-174.4ms | 大 candidates trigram 与 full-scan 查询偏高 |
| 超标（>500ms） | **0/16** | — | 本轮无前台超时 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=5,681,395；
- liveRecordCount=5,264,311；
- tombstone=417,084（**7.34%**）。

## 5. 最新日志段分析

```text
SESSION_START_LINE=9244
SESSION_START_TS=2026-06-10 17:02:22.835
TOTAL_LINES=9862
SEG_LINES=619
QUERYADV=57
QUERYADV_GE500=4
FLUSH=214
SKIPFLUSH=0
COMPACTION=2
```

### 慢查询证据

最新 session 内慢查询数量仍为 4 条，本轮未新增 `>=500ms` 慢查询：

```text
[2026-06-10 17:17:09.559] QueryAdvanced "path:/Users" total=525ms | path=advanced-linear-gcd
[2026-06-10 17:25:15.434] QueryAdvanced "lllllllllllllllllllllllllllllllllllllllllllll" total=1154ms | path=advanced-trigram
[2026-06-10 17:47:13.642] QueryAdvanced "中文测试" total=716ms | path=advanced-linear-gcd
[2026-06-10 23:44:25.566] QueryAdvanced "test" total=676ms | path=advanced-trigram
```

### flush 极端长尾

最新 session 内 `FLUSH=214`，rewrite 范围 **1440ms - 895940ms**，total 最大 **896053ms**。

最新 flush：

```text
[2026-06-12 08:16:58.089] Flushed v6 flat index, lastEventId=346377988,
liveRecords=5264291 | timing: walSwap=0ms rewrite=1940ms sqcache=2ms total=1945ms
```

Top flush outliers：

```text
[2026-06-12 03:15:02.833] rewrite=895940ms sqcache=2ms total=896053ms
[2026-06-10 23:24:50.749] rewrite=725091ms sqcache=18ms total=735223ms
[2026-06-11 14:30:37.596] rewrite=58273ms sqcache=5ms total=101726ms
[2026-06-10 21:16:27.157] rewrite=32497ms sqcache=5ms total=230084ms
[2026-06-10 23:26:19.499] rewrite=27557ms sqcache=10ms total=27834ms
```

## 6. 问题清单

### P0：flush 完整重写仍有分钟级灾难长尾

- 证据：rewrite 最大 895,940ms，total 最大 896,053ms。
- 最新 flush 回到 1940ms，但这只是当前 I/O 压力较低时的表现；结构性风险仍未消除。
- 根因：完整索引重写在 APFS COW、系统 I/O 压力、睡眠/唤醒或后台任务竞争下被放大。

### P0：flush 频率继续上升且没有退避

- 证据：最新 session `FLUSH=214`，较上一轮 +9；`SKIPFLUSH=0`。
- 影响：即使单次 flush 是 2 秒级，高频完整重写仍会持续污染 page cache，并扩大长尾概率。

### P0：tombstone 继续增长

- 证据：tombstone=417,084（7.34%），较上一轮增加 10,586。
- 影响：索引体积、flush rewrite 成本和扫描成本继续扩大。

### P1：大 candidates trigram 偶发偏高

- 证据：`test`=109.6ms（117,787 candidates），`abc def...`=78.9ms（14,486 candidates）。
- 影响：当前仍在正常区间，但在 flush 后 page cache 受扰时可能成为慢查询候选。

## 7. 优化建议

### P0：flush 改为增量/COW 页写入

- 分离 stable segment 与 delta segment；常规 flush 只追加 delta。
- 后台低优先级合并，不阻塞前台查询。
- 验收：flush rewrite max <200ms，latest session 不再出现分钟级 outlier。

### P0：为 flush 增加 I/O 压力退避

- flush 超过阈值后降低下一轮频率。
- 前台查询活跃时推迟大 flush。
- 持久化线程使用更低 QoS，减少对搜索热路径的干扰。

### P0：降低 flush 频率并合并 WAL 批次

- 引入最小 flush 间隔和最大合并窗口。
- 对高 churn 场景减少完整重写次数。

### P1：提前或分段 compaction

- 不等待全局 25% tombstone 阈值。
- 按 segment 或绝对 tombstone 数触发局部 compaction。

### P2：保护搜索热数据 page cache

- 对 flush 大顺序写考虑 `F_NOCACHE` 或同类策略。
- 对搜索热区使用 `madvise` 预热/保持。

## 8. 下一轮重点

1. `test` 和 `abc def...` 是否继续偏高。
2. latest session 慢查询计数是否仍维持 4，无新增 >500ms。
3. 最新 flush 是否继续维持 2 秒级，还是再次出现长尾。
4. tombstone 是否继续上升（当前 7.34%）。

## 9. 结论

**本轮前台查询仍健康：0/16 >500ms，P50≈24.1ms，Max=174.4ms，极佳 10/16。** 相比上一轮略回落，但仍明显优于此前 I/O 风暴阶段。全表扫描总和 478.1ms，处于健康区间。

**后台持久化风险继续累积：latest session FLUSH=214，SKIPFLUSH=0，tombstone=417,084（7.34%），flush rewrite 最大仍为 895,940ms。** 最新 flush 约 1.94s，说明瞬时状态恢复，但完整重写索引的 P0 结构性问题仍未解决。优先级仍是 COW/增量 flush、I/O 退避、降低 flush 频率、提前或分段 compaction。
