# MacEverything 性能分析报告 R_2606101830

## 1. 本轮范围

- 任务启动时间：2026-06-10 18:30
- 报告文件：`docs/performance_ana/R_2606101830.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9165`
  - `SEG_LINES=8223`
  - `LAST_LOG_TS=2026-06-10 16:47`

## 2. 执行摘要

**Trigram 大 postings 集回退——`.pdf`=117ms, `hello world`=113ms（零和加剧）。`x` 缓存稳定（2ms，连续 3 轮）。** 0/16 >500ms，Max=253ms，P50=40.6ms，lockWait=0。**极佳 9/16（↓2——post-compaction 最低）。** Trigram 大 postings 集 page cache 淘汰加剧：`.pdf` 31→117ms（↑277%），`hello world` 21→113ms（↑438%）。全表扫描总和 587ms（↑4% from R262=567ms——持平）。**`verylongquery`=26ms（异常——通常 <1ms）。** FLUSH 32→38（+6，连续 5 轮 +6）。tombstone=58,042（1.10%——健康）。

| 指标 | R262 | R263 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 204ms | **253ms** | ↑24% |
| P50 | 21ms | **40.6ms** | ↑93% |
| lockWait max | 3.0ms | **0ms** | 恢复为 0 |
| 极佳比例(<50ms) | 11/16 | **9/16** | ↓2（post-compaction 最低） |
| tombstone | 49,554 (0.94%) | **58,042 (1.10%)** | ↑（正常增长） |
| recordCount | 5,293,491 | **5,302,352** | +8.9K（正常） |
| liveRecordCount | 5,243,937 | **5,244,310** | +373（正常） |
| FLUSH 累计 | 32 | **38** | +6（连续 5 轮 +6） |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 6 | **6** | 不变 |
| QUERYADV_GE500 | 75 | **75** | 不变 |

### `x` 缓存稳定——连续 3 轮确认

| 轮次 | `x` 路径 | `x` 时间 |
|------|---------|--------:|
| R261 | short-query-cache | 17ms |
| R262 | short-query-cache | 1ms |
| **R263** | **short-query-cache** | **2ms** |

- **连续 3 轮 short-query-cache——缓存完全稳定。**
- **P2 缺陷已确认为"重建延迟"问题，非持久性故障。**

### ⚠️ Trigram 大 postings 集回退

| Query | candidates | R262 | R263 | 变化 | 状态 |
|-------|----------:|-----:|-----:|------|------|
| `.pdf` | 9,783 | 31ms | **117ms** | **↑277%** | 正常 |
| `hello world` | 3,929 | 21ms | **113ms** | **↑438%** | 正常 |
| `*.swift` | 9,402 | 10ms | **32ms** | ↑220% | 极佳 |
| `main.cpp` | 85 | 5ms | **9ms** | ↑80% | 极佳 |
| `abc def...` | 14,453 | 50ms | **41ms** | ↓18% | 极佳 |
| `README` | 17,430 | 11ms | **8ms** | ↓27% | 极佳 |
| `test` | 117,723 | 55ms | **23ms** | ↓58% | 极佳 |
| `MacEverything` | 6,923 | 11ms | **48ms** | ↑336% | 极佳 |
| Trigram phase2均值 | | ~17ms | **~44ms** | **↑159%** |

- **`.pdf`=117ms（phase2=111ms）和 `hello world`=113ms（phase2=110ms）——大 postings 集的 page cache 被系统活动淘汰。**
- **对比：`test`=23ms（117K candidates 但只读取少量 postings 页——三元组交集快速裁剪）。**
- **`README`=8ms（17K candidates）和 `abc def`=41ms（14K candidates）——不同 trigram 组合的 postings 分布差异。**
- **phase2 均值 17→44ms（↑159%）——大幅回退，主要由 `.pdf` 和 `hello world` 贡献。**
- **这是典型的 page cache 抖动——预计下轮 battery 查询重新预热后恢复。**

### `verylongquery`=26ms 异常

- **R262=0.6ms → R263=26ms（↑4233%）。**
- **phase2=0ms（trigram 查找为空——无匹配 trigram，立即返回）。**
- **26ms 全部是 overhead（锁获取 + 结果序列化 + 网络延迟）——非搜索引擎性能问题。**
- **可能原因：系统瞬时负载（CPU 调度延迟）或 GC-like 暂停。**
- **单次异常，不影响整体评估。**

### 全表扫描——稳定

| Query | R262 | R263 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 169ms | **178ms** | ↑5% |
| `path:/Users` | 194ms | **157ms** | ↓19% |
| `中文测试` | 204ms | **253ms** | ↑24% |
| **总和** | **567ms** | **588ms** | **↑4%（持平）** |

- **全表扫描 567→588ms（↑4%——基本持平）。**
- **`中文测试`=253ms（↑24%）——Unicode 匹配的 SoA 扫描偏慢。**
- **稳态区间确认：400-600ms（post-compaction 正常水平）。**

### 辅助路径

| Query | R262 | R263 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 1ms | **2ms** | ↑100%（微小） | 极佳 |
| `ext:cpp` | 11ms | **18ms** | ↑64% | 极佳 |
| `size:>100m` | 29ms | **41ms** | ↑41% | 极佳 |
| `dm:today` | 44ms | **54ms** | ↑23% | 正常 |
| `verylongquery...` | 0.6ms | **26ms** | ↑异常（overhead） | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 2.4 | 0.0 | short-query-cache | 0 | 0 | 极佳（稳定） |
| `.pdf` | 5 | 116.9 | 111.4 | advanced-trigram | 0 | 9783 | 正常（page cache 淘汰） |
| `main.cpp` | 5 | 9.3 | 2.9 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 31.7 | 30.3 | advanced-trigram | 0 | 9402 | 极佳 |
| `ext:cpp` | 5 | 18.4 | 13.1 | advanced-ext-index | 0 | 3612 | 极佳 |
| `size:>100m` | 5 | 40.7 | 37.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 113.2 | 109.7 | advanced-trigram | 0 | 3929 | 正常（page cache 淘汰） |
| `abc def ghi jkl mno pqr` | 0 | 40.5 | 40.0 | advanced-trigram | 0 | 14453 | 极佳 |
| `README` | 5 | 8.3 | 5.2 | advanced-trigram | 0 | 17430 | 极佳 |
| `test` | 5 | 23.1 | 12.1 | advanced-trigram | 0 | 117723 | 极佳 |
| `MacEverything` | 5 | 47.6 | 0.7 | advanced-trigram | 0 | 6923 | 极佳 |
| `dm:today` | 5 | 54.2 | 49.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 177.8 | 168.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 156.7 | 152.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 253.0 | 252.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 26.4 | 0.0 | advanced-trigram | 0 | 0 | 极佳（overhead 异常） |

**P50 = 40.6ms，P90 = 177.8ms，Max = 253.0ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R262 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **9/16** | 2-48ms | R262=11/16（↓2） |
| **正常**（50-500ms） | 7/16 | 54-253ms | R262=5/16（↑2） |
| **超标**（>500ms） | **0/16** | — | R262=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,302,352，liveRecordCount=5,244,310（tombstone=**58,042**，**1.10%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=682  QUERYADV_GE500=75（不变）
FLUSH=38（+6，连续 5 轮 +6）  COMPACTION=6  SKIPFLUSH=7266
```

## 6. 问题清单

### P0：flush 写锁阻塞

- **FLUSH 连续 5 轮 +6——高频 flush 持续。**

### P0：compaction 触发机制缺陷（根因未修复）

### P2：short-query-cache 重建延迟（已稳定——连续 3 轮）

### 观察项：trigram 大 postings 集 page cache 抖动

- **`.pdf` 和 `hello world` 在 <30ms 和 >100ms 之间交替——page cache LRU 淘汰。**
- **非引擎缺陷——系统级 page cache 管理行为。**
- **madvise MADV_WILLNEED 可缓解。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（增加独立定时检查）

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护（缓解 trigram postings 抖动）

### P2：short-query-cache flush 后增量更新

## 8. 下一轮重点

1. `.pdf` 和 `hello world` 是否恢复（page cache 预热）。
2. `verylongquery` 异常是否复现（单次 vs 系统性）。
3. FLUSH +6 是否持续（连续 5 轮）。

## 9. 结论

**Trigram 大 postings 集 page cache 抖动——`.pdf`=117ms, `hello world`=113ms（↑277%/↑438%）。`x` 缓存稳定（2ms，连续 3 轮）。** 0/16 >500ms，Max=253ms，P50=41ms，lockWait=0。极佳 9/16（↓2——post-compaction 最低，page cache 抖动导致）。全表扫描 588ms（↑4%——持平，稳态 400-600ms 确认）。

**Page cache 抖动是系统级行为，非引擎缺陷——madvise MADV_WILLNEED 可缓解。** `verylongquery`=26ms 为单次 overhead 异常（phase2=0ms）。FLUSH +6（连续 5 轮，32→38）。tombstone=58,042（1.10%——健康）。recordCount +8.9K（正常）。
