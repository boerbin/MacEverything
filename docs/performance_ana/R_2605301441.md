# MacEverything 性能分析报告 R_2605301441

- **任务启动时间**：2026-05-30 14:41
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~27.6h，未重启
- **状态快照 @14:41**：total `5,883,063` / live `5,391,960`
- **本轮定性**：🟢 **连续第三个洁净轮——稳态固化**：电池零污染标志（`x`=2.43ms、`main.cpp` 71候选10ms、`*.swift` 9213候选**26ms**，比 R92 的 29ms 再暖一档、`ext:cpp` 15ms）。电池窗口（14:41/14:42）日志零 >500ms 条目，**三段延迟（>5s/1-5s/0.5-1s）连续第三轮全冻结**（7/23/16），本轮 +13 越线查询全部落 0.1-0.5s 正常段。洁净间隙 **2.67×**，二层分裂 **20/20 不破**。唯一恶化项 tombstone **8.35%**（连续 26 轮单调），根因仍为 R67 阀门不可达，无新结论。

---

## 1. 查询电池（16 条，本轮洁净）

| query | path | total(ms) | phase2 | trig | cand | res |
|---|---|---|---|---|---|---|
| `x` | short-query-cache | 2.43 | 0 | 0 | 0 | 99 |
| `.pdf` | advanced-trigram | 21.51 | 20.71 | 0.60 | 10124 | 100 |
| `main.cpp` | advanced-trigram | 10.26 | 8.57 | 1.66 | 71 | 71 |
| `*.swift` | advanced-trigram | 26.03 | 25.61 | 0.39 | 9213 | 100 |
| `ext:cpp` | advanced-ext-index | 14.67 | 14.30 | 0 | 2987 | 100 |
| `size:>100m` | pure-filter-soa-gcd | 37.46 | 37.44 | 0 | 0 | 100 |
| `hello world` | advanced-trigram | 40.79 | 39.94 | 0.78 | 2340 | 100 |
| `abc def ghi jkl mno pqr` | advanced-trigram | 92.87 | 91.93 | 0.90 | 15924 | 0 |
| `README` | advanced-trigram | 19.59 | 16.34 | 3.14 | 16466 | 100 |
| `test` | advanced-trigram | 13.09 | 9.05 | 3.91 | 136267 | 100 |
| `MacEverything` | advanced-trigram | 12.79 | 1.96 | 10.77 | 6699 | 100 |
| `dm:today` | pure-filter-soa-gcd | 71.27 | 71.21 | 0 | 0 | 100 |
| `content:import` | pure-filter-soa-gcd | 180.99 | 167.31 | 0 | 0 | 100 |
| `path:/Users` | advanced-linear-gcd | 223.25 | 217.62 | 0 | 0 | 100 |
| `中文测试` | advanced-linear-gcd | 216.63 | 216.36 | 0.14 | 0 | 0 |
| `verylongquery…really` | advanced-trigram | 0.32 | 0 | 0.30 | 0 | 0 |

整批可信：所有 trigram 路径暖驻留（`*.swift` 26ms、`hello world` 41ms、`main.cpp` 10ms），无任何停顿迹象。

---

## 2. 洁净 ns/槽（电池+3× 复测取 4 样本中位，records=5,883,063）

本轮无污染，但仍按纪律做 3× 复测（标志复测 `x`=0.02、`ext:cpp`=0.34 均正常恢复）：

| 路径 | 类型 | 4样本中位 | 洁净值 |
|---|---|---|---|
| `size:>100m` | 数值 | 36.49ms | **6.20** |
| `dm:today` | 数值 | 71.77ms | **12.20** |
| `content:import` | 字符串 | 191.93ms | **32.62** |
| `path:/Users` | 字符串 | 227.31ms | **38.64** |
| `中文测试` | 字符串 | 234.79ms | **39.91** |

注：`size` 复测首样本 131.95ms 为单点抖动（其余样本 16–37ms），已被 4 样本中位吸收，未触发污染标志（`x`/`ext:cpp` 均恢复常态），不影响判定。

---

## 3. 本轮头条：连续第三个洁净轮，稳态固化

R91 捕获午间停顿恢复、R92 双重确认其结束，本轮 R93 作为停顿结束后的**第三个洁净轮**，提供了系统已回归并**稳定锁定在稳态**的三重确认：

| 信号 | R91（恢复）| R92 | **R93（本轮）** |
|---|---|---|---|
| `*.swift` total | 72ms | 29ms | **26ms** |
| 电池 >500ms 条目 | 无 | 无 | **无** |
| 1-5s 延迟段 | 23 | 23 | **23（冻结）** |
| 0.5-1s 延迟段 | 16 | 16 | **16（冻结）** |

`*.swift` 从 R91 的 72ms → R92 的 29ms → 本轮 26ms，trigram 索引页稳定热驻留；三段延迟（>5s=7、1-5s=23、0.5-1s=16）**连续三轮零增长**，所有新增查询持续落在 0.1-0.5s 正常段。午间外部事件（12:00 起、12:41 峰、13:41 前结束）确已彻底退出，系统进入并锁定稳态。

---

## 4. 绝对二层分裂：洁净数据 20/20 不破（间隙 2.67×）

- **数值层**：size 6.20、dm 12.20 → 最高数值 **dm 12.20**
- **字符串层**：content 32.62、path 38.64、中文 39.91 → 最低字符串 **content 32.62**
- **洁净间隙 = 32.62 / 12.20 = 2.67×**，无重叠。

dm（12.20）连续两轮（R92 12.38 / R93 12.20）稳居数值天花板、size（6.20）为地板，数值层方向稳定。字符串层本轮排序为 content < path < 中文，且 path（38.64）与 中文（39.91）接近（仅差 1.3 ns/槽）——再证 R79「字符串层内部细排序为逐区冷热噪声，无固定次序」（与 R92 的 path 37.08≈中文 39.36 同形）。分裂依旧机制级（纯数值零解引用 ≪ 字符串解引用散布串池），不构成新结论。

---

## 5. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **467 次**（R92 455）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100，`:159` tombstone 比例阀结构性永不可达——全会话 0 触发、0 回收。

---

## 6. tombstone 趋势：连续 26 轮单调上升，稳居 8% 区间

| 轮次 | tombstone | ratio |
|---|---|---|
| R89@12:41 | 455,464 | 7.79% |
| R90@13:11 | 464,832 | 7.94% |
| R91@13:41 | 473,060 | 8.07% |
| R92@14:11 | 482,158 | 8.21% |
| **R93@14:41** | **491,103** | **8.35%** |

会话内单调递增、永不回缩。R43 参考崩溃点 12.97%（~800K），当前 8.35% 已稳居 8% 区间、过 6 成程。live 基本持平（5,391,960），约 96% 新增记录变为 tombstone。这是会话内唯一真实恶化趋势，根因明确为 R67 阀门不可达。

---

## 7. 日志健康度

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **319 条**（R92 306，+13）——增速稳定（R92 亦 +12），洁净轮特征
- Top-7 峰值 `[18582, 17928, 15749, 8205, 7544, 6811, 6061]` —— 仍与历史**逐字节相同**
- 延迟分布：>5s=**7**（冻结）、1-5s=**23**（冻结）、0.5-1s=**16**（冻结）、0.1-0.5s=273（+13）、中位 **220ms**
- 三段（>5s/1-5s/0.5-1s）连续三轮全冻结，本轮 +13 越线查询**全部落在 0.1-0.5s 段**——稳态固化的标志特征

---

## 8. 优化建议（四项 P0，本轮无新增）

1. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调 <12.97%（建议 8–10%）。当前 8.35% 已落入建议阈值区间，紧迫性最高。
2. **🔥 P0｜mlock/madvise 整体常驻热区**：午间停顿虽已消退三轮，但连续三轮（R88/R89/R90）曾证明无内存保护时外部压力会反复拖慢任意路径；需把 path/name 串池、content 列、trigram 索引、候选行、数值 SoA 列整体钉入内存以抵御下次外部事件。
3. **🔥 P0｜无锁快照 flush**：启动 flush 持锁 10.3s。
4. **🔥 P0｜查询超时保护**：R88（1663ms）+ R89（1002ms）+ R90（861ms）三个真实停顿样本——超时熔断 + 降级返回是必要韧性机制。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1🔥｜CJK trigram 索引**：`中文测试` 走 linear-gcd 全扫（中位 235ms / 39.91 ns/槽）。

---

## 9. 结论

- **定性**：🟢 连续第三个洁净轮，稳态固化。`*.swift` 从 29ms 进一步暖到 26ms，三段延迟连续三轮全冻结，系统已回归并锁定稳态。
- **稳健结论 20/20，间隙 2.67×**：稳定在正常区间（2.11×~3.49× 全区间一律不破），分裂依旧机制级无重叠。
- **诚实修正纪律**：本轮无推测结论被推翻、无新结论需建立；字符串层内部 path≈中文 近并列为 R79 已定噪声的复现，size 复测单点抖动 131.95ms 被中位吸收，均如实标注。
- **唯一持续恶化项**：tombstone **8.35%**（连续 26 轮单调，稳居 8% 区间），根因 R67 阀门结构性不可达明确。这是会话内唯一真实退化，与已结束的外部停顿无关，修复紧迫性随阈值逼近而最高。
