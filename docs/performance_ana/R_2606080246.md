# MacEverything 性能分析报告 R_2606080246

## 1. 本轮范围

- 任务启动时间：2026-06-08 02:46
- 报告文件：`docs/performance_ana/R_2606080246.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=3219`
  - `SEG_LINES=2277`
  - `LAST_LOG_TS=2026-06-08 02:47`

## 2. 执行摘要

**Trigram 极限+全表扫描升高——0/16 >500ms，Max=302ms，P50=10ms，lockWait=0，极佳比例 12/16（75%）。** P50=10ms 再创本会话绝对新低（超越 R161=11ms）！Trigram 均值 11.5ms 匹配 R156 会话最佳。然而全表扫描总和从 778ms 升至 822ms——逼近 R158 维护峰值（820ms），凌晨 2-3 点有第二波系统活动。Trigram 核心路径不受影响。

### 关键指标对比

| 指标 | R162（30 min 前） | R163 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 341ms | **302ms** | **↓ 11%** |
| P50 | 13ms | **10ms** | **↓ 23%（新会话绝对最低）** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 12/16 | **12/16** | 持平 |
| tombstone | 1,271,062 (19.96%) | **1,279,935 (20.07%)** | **↑ 突破 20%** |
| tombstone 增速 | ~275/min | **~296/min** | 持平 |
| recordCount | 6,369,131 | **6,378,120** | +9.0K |
| liveRecordCount | 5,098,069 | **5,098,185** | +116 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 2070 | **2130** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 20 轮 0 新增） |

### P50=10ms——新会话绝对最低

| 轮次 | 时间 | P50 | Trigram 均值 | 状态 |
|------|------|----:|------:|------|
| R156 | 23:46 | 14ms | 11.5ms | 前极限 |
| R159 | 01:16 | 12ms | 11.7ms | 维护结束谷底 |
| R161 | 01:50 | 11ms | 12.1ms | 前新低 |
| **R163** | **02:46** | **10ms** | **11.5ms** | **绝对新低** |

- **P50=10ms 首次跌破 11ms——连续 5 轮维持在 10-13ms 极限带。**
- **Trigram 均值 11.5ms 匹配 R156 会话最佳——postings page cache 完全热化。**

### 全表扫描——凌晨第二波压力

| Query | R162 | R163 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 186ms | **219ms** | ↑ 18% |
| `path:/Users` | 252ms | **302ms** | ↑ 20% |
| `中文测试` | 341ms | **301ms** | ↓ 12% |
| **总和** | **778ms** | **822ms** | **↑ 6%** |

- **全表扫描总和 822ms——匹配 R158 的维护窗口峰值（820ms）。**
- **但 Max 仅 302ms（R158=327ms）——压力更均匀分布。**
- **`path:/Users`=302ms vs `中文测试`=301ms——两项几乎完全相等，表明 SoA 线性区域 page cache 被均匀竞争。**
- **原因推测**：凌晨 2:30-3:00 macOS 第二波轻量维护活动（Photos ML 分析、iCloud 同步收尾等）。
- **预计**：凌晨 3:30 后系统完全静默，全表扫描将开始回落。

### 深夜全表扫描完整趋势（新增 R163 数据点）

| 轮次 | 时间 | 总和 | P50 | 说明 |
|------|------|-----:|----:|------|
| R158 | 00:46 | 820ms | 29ms | 维护峰值 |
| R159 | 01:16 | 416ms | 12ms | 谷底 |
| R160 | 01:46 | 733ms | 21ms | 回弹 |
| R161 | 01:50 | 693ms | 11ms | 回落 |
| R162 | 02:16 | 778ms | 13ms | 二次升高 |
| **R163** | **02:46** | **822ms** | **10ms** | **二次峰值** |

- **双峰模式确认**：第一峰 820ms（00:46），谷底 416ms（01:16），第二峰 822ms（02:46）。
- **关键区别**：第一峰时 P50=29ms（trigram 也受影响）；第二峰时 P50=10ms（仅 SoA 受影响）。
- **结论**：第二波系统活动强度与第一波相当，但仅影响 SoA 线性扫描区域——trigram postings 已完全锁定在 page cache 中。

### Trigram 路径——会话最佳水平

| Query | R162 | R163 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 13ms | **10ms** | ↓ 23% |
| `*.swift` | 12ms | **8ms** | **↓ 33%** |
| `hello world` | 15ms | **13ms** | ↓ 13% |
| `README` | 16ms | **10ms** | **↓ 38%** |
| `test` | 11ms | **16ms** | ↑ 波动 |
| `abc def...` | 26ms | **32ms** | ↑ 波动 |
| `MacEverything` | 2ms | **3ms** | 持平 |
| Trigram 均值 | 13.4ms | **11.5ms** | **↓ 14%** |

- **Trigram 均值 11.5ms——匹配 R156 会话最佳记录。**
- **`*.swift`=8ms 匹配 R156 最佳。**
- **结论**：高频 battery 运行保持 trigram postings 完全热化——即使 SoA 区域被系统活动竞争，trigram 路径完全不受影响。**

### SoA Filter

| Query | R162 | R163 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 56ms | **26ms** | **↓ 54%（恢复）** |
| `dm:today` | 34ms | **54ms** | ↑ 59%（零和波动） |

- **两项总和**：R162=90ms → R163=80ms——总体略降，正常零和抖动。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 0.8 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 10.2 | 9.4 | advanced-trigram | 0 | 9812 | 极佳 |
| `main.cpp` | 5 | 1.3 | 0.8 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 7.8 | 7.5 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 5.6 | 5.2 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 25.7 | 25.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 12.8 | 12.4 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 31.7 | 30.5 | advanced-trigram | 0 | 14211 | 极佳 |
| `README` | 5 | 9.5 | 8.6 | advanced-trigram | 0 | 16631 | 极佳 |
| `test` | 5 | 15.9 | 14.0 | advanced-trigram | 0 | 116513 | 极佳 |
| `MacEverything` | 5 | 2.6 | 0.8 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 53.8 | 53.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 219.3 | 189.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 301.8 | 295.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 301.0 | 300.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 9.5ms，P90 = 219.3ms，Max = 301.8ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R162 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 12/16 | 0.2-32ms | R162=12/16（持平） |
| **正常**（50-500ms） | 4/16 | 54-302ms | R162=4/16（持平） |
| **超标**（>500ms） | 0/16 | — | R162=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,378,120，liveRecordCount=5,098,185（tombstone=**1,279,935**，**20.07%**）；
- 进程正常运行（当前会话，运行约 28.6 小时）。
- **tombstone 突破 20% 里程碑。**

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=95  QUERYADV_GE500=4（不变——连续 20 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=2130
```

- **连续 20 轮 0 个新增 >500ms 查询（R144-R163）——里程碑！**
- **FLUSH=1**——无新 flush（28.6 小时仅启动时 1 次）。
- **SKIPFLUSH=2130**——系统持续空闲。

## 6. 问题清单

### 里程碑：tombstone 突破 20%

- tombstone=1,279,935（20.07%）。
- 增速 ~296/min（稳定）。
- 距 25% compaction 阈值约 ~28 小时。
- **无风险——compaction 距离充裕，性能无可观测影响。**

### 观察：凌晨双峰 page cache 竞争模式（信息记录）

- 第一峰 820ms（00:46）+ 第二峰 822ms（02:46）——间隔 2 小时。
- macOS 深夜维护呈双峰分布：主窗口 ~22:30-01:00 + 轻量窗口 ~02:00-03:00。
- 仅影响 SoA 线性区域；第二峰时 trigram 路径完全不受影响（P50=10ms）。
- **预计 03:00 后回落。**

### 无活跃问题

- 0/16 >500ms，12/16 <50ms。
- P50=10ms（绝对新低），lockWait=0。
- **连续 20 轮 0 新增慢查询（里程碑）。**
- 28.6 小时无 WARN/ERROR。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 20.07%（已突破 20%），增速 ~296/min。
- 距 25% 阈值约 ~28 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 系统空闲时无影响。

## 8. 下一轮重点

1. 凌晨 3 点后全表扫描是否开始回落。
2. tombstone 增速是否稳定。
3. P50 是否继续维持 10-13ms 极限区间。

## 9. 结论

**Trigram 极限+全表扫描第二峰——0/16 >500ms，Max=302ms，P50=10ms（绝对新低），lockWait=0，极佳比例 12/16。** Trigram 均值 11.5ms 匹配 R156 会话最佳。全表扫描总和 822ms（第二峰，匹配 R158 第一峰 820ms），但关键区别：第二峰时 trigram 路径完全不受影响——postings 已牢固驻留 page cache。

**双里程碑达成**：(1) P50=10ms 首次跌破 11ms；(2) 连续 20 轮 0 新增 >500ms 查询。tombstone 突破 20%（1.28M，20.07%），增速 296/min，距 25% 约 28 小时——安全裕量充足。28.6 小时无异常。系统核心路径处于历史最优状态。
