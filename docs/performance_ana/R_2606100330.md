# MacEverything 性能分析报告 R_2606100330

## 1. 本轮范围

- 任务启动时间：2026-06-10 03:30
- 报告文件：`docs/performance_ana/R_2606100330.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=7577`
  - `SEG_LINES=6635`
  - `LAST_LOG_TS=2026-06-10 00:17`

## 2. 执行摘要

**1 轮恢复——第 11 次确认！lockWait 286.6ms→0ms，0/16 >500ms，Max=334ms，P50=76ms，极佳 5/16。** R232 的系列新纪录 lockWait=286.6ms（13/16 阻塞）完全消退。全表扫描偏高（`content:import`=334ms），trigram 路径恢复良好。

| 指标 | R232 | R233 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 2/16 | **0/16** | **↓2（恢复）** |
| Max | 545ms | **334ms** | **↓39%** |
| P50 | 158ms | **76ms** | **↓52%** |
| lockWait max | 286.6ms | **0ms** | **↓100%（完全消退）** |
| 极佳比例(<50ms) | 2/16 | **5/16** | **↑3** |
| tombstone | 2,073,455 (28.48%) | **2,085,892 (28.60%)** | ↑ |
| tombstone 增速 | ~567/min | **~414/min** | ↓27%（继续减速） |
| recordCount | 7,279,923 | **7,292,592** | +12.7K |
| liveRecordCount | 5,206,468 | **5,206,700** | +232 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5917 | **5977** | +60 |
| COMPACTION | 2 | **2** | **不变——第 34 轮确认缺陷** |
| QUERYADV_GE500 | 60 | **60** | 不变 |

### 1 轮恢复——第 11 次确认

历次 1 轮恢复模式：

| 灾难轮 | lockWait max | >500ms | 恢复轮 | lockWait | >500ms |
|--------|------------:|-------:|--------|--------:|-------:|
| R176 | — | 6/16 | R177 | 0 | 1/16 |
| R179 | — | 2/16 | R180 | 0 | 0/16 |
| R181 | — | 3/16 | R182 | 0 | 0/16 |
| R190 | — | 2/16 | R191 | 0 | 0/16 |
| R194 | — | 1/16 | R195 | 0 | 0/16 |
| R199 | — | 2/16 | R200 | 0 | 0/16 |
| R208 | — | 1/16 | R209 | 0 | 0/16 |
| R215 | 18.2ms | 1/16 | R216 | 0 | 0/16 |
| R221 | — | 6/16 | R222 | 0 | 1/16 |
| R227 | 65.3ms | 1/16 | R228 | 0 | 0/16 |
| **R232** | **286.6ms** | **2/16** | **R233** | **0** | **0/16** |

- **即使 lockWait 达到系列新纪录 286.6ms（13/16 阻塞），仍然 1 轮恢复。**
- **说明 lockWait 是一次性事件（单次 flush），不会造成持续性能退化。**
- **但单次事件本身的影响范围极大（13/16 阻塞），COW flush 仍为 P0。**

### Trigram 路径——恢复

| Query | R232 | R233 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 545ms | **125ms** | **↓77%** | 正常 |
| `hello world` | 513ms | **128ms** | **↓75%** | 正常 |
| `*.swift` | 158ms | **76ms** | ↓52% | 正常 |
| `main.cpp` | 53ms | **10ms** | **↓81%** | 极佳 |
| `abc def...` | 352ms | **70ms** | **↓80%** | 正常 |
| `README` | 72ms | **23ms** | **↓68%** | 极佳 |
| `test` | 204ms | **41ms** | **↓80%** | 极佳 |
| `MacEverything` | 48ms | **4ms** | **↓92%** | 极佳 |
| Trigram 均值 | 243ms | **60ms** | **↓75%** | 恢复 |

- **Trigram 均值 243→60ms（↓75%）——大幅恢复！**
- **`.pdf`=125ms（↓77%，R232 lockWait=286.6ms 消退）。**
- **`hello world`=128ms（↓75%，R232 phase2=482ms page cache 恢复）。**
- **`test`=41ms（↓80%）——回到极佳。**

### 全表扫描——偏高

| Query | R232 | R233 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 323ms | **334ms** | ↑3%（偏高） |
| `path:/Users` | 346ms | **207ms** | ↓40% |
| `中文测试` | 237ms | **181ms** | ↓24% |
| **总和** | **906ms** | **722ms** | **↓20%** |

- **全表扫描总和 906→722ms（↓20%）——部分恢复但仍偏高。**
- **`content:import`=334ms（↑3%）——SoA 区域 page cache 尚未完全恢复。**
- **`path:/Users`=207ms（↓40%）——路径数组部分恢复。**

### 辅助路径

| Query | R232 | R233 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 6ms | **19ms** | ↑217% | 极佳（偏高） |
| `ext:cpp` | 236ms | **36ms** | **↓85%** | 极佳 |
| `size:>100m` | 116ms | **64ms** | ↓45% | 正常 |
| `dm:today` | 113ms | **116ms** | ↑3% | 正常（偏高） |
| `verylongquery...` | 3ms | **0.8ms** | ↓73% | 极佳 |

- **`ext:cpp`=36ms（↓85%）——ext-index 区域恢复。**
- **`x`=19ms——cache 路径偏高（通常 2-6ms），与 R230=18ms 类似。**
- **`dm:today`=116ms（偏高）——SoA modTime 区域 page cache 未完全恢复。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 18.7 | 0.0 | short-query-cache | 0 | 0 | 极佳（偏高） |
| `.pdf` | 5 | 124.5 | 118.3 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 10.4 | 4.6 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 76.2 | 75.3 | advanced-trigram | 0 | 9353 | 正常 |
| `ext:cpp` | 5 | 36.1 | 35.3 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 64.1 | 64.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 128.3 | 125.8 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 69.8 | 68.8 | advanced-trigram | 0 | 14454 | 正常 |
| `README` | 5 | 22.9 | 17.7 | advanced-trigram | 0 | 17151 | 极佳 |
| `test` | 5 | 41.4 | 37.6 | advanced-trigram | 0 | 117136 | 极佳 |
| `MacEverything` | 5 | 4.1 | 1.0 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 116.0 | 115.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 333.5 | 325.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（偏高） |
| `path:/Users` | 5 | 206.9 | 200.3 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 181.3 | 179.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.8 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 76.2ms，P90 = 206.9ms，Max = 333.5ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R232 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **5/16** | 0.8-41ms | R232=2/16（**↑3**） |
| **正常**（50-500ms） | 11/16 | 64-334ms | R232=12/16（↓1） |
| **超标**（>500ms） | **0/16** | — | R232=2/16（**↓2**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,292,592，liveRecordCount=5,206,700（tombstone=**2,085,892**，**28.60%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=536  QUERYADV_GE500=60（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5977
```

## 6. 问题清单

### P0 确认（第 34 轮）：compaction 触发机制缺陷

- **tombstone=2,085,892（28.60%）——超阈值 3.60%。**
- **34 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **R233 lockWait=0——1 轮恢复。但 R232=286.6ms（系列新纪录，13/16 阻塞）。**
- **一次性事件但影响极大——COW flush 为 P0。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式（R232 lockWait=286.6ms 系列新纪录）

### P0：修复 compaction 触发机制（第 34 轮确认）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 全表扫描是否继续恢复（`content:import`=334ms 偏高）。
2. `x`=19ms 的 cache 路径开销是否消退。
3. tombstone 增速趋势。

## 9. 结论

**1 轮恢复——第 11 次确认！** R232 的 lockWait=286.6ms（系列新纪录，13/16 阻塞，2/16 >500ms）→ R233 lockWait=0，0/16 >500ms，Max=334ms（↓39%），P50=76ms（↓52%），极佳 5/16。Trigram 均值 60ms（↓75%——大幅恢复）。全表扫描总和 722ms（↓20%——部分恢复，`content:import`=334ms 偏高）。

**即使面对历史最严重的锁竞争事件（286.6ms，13/16 阻塞），系统仍然 1 轮恢复——但单次事件的破坏力极大，COW flush 仍为最高优先级修复项。**

**tombstone 28.60%（第 34 轮确认 compaction 缺陷）。** 增速继续减速至 ~414/min。COMPACTION=2 始终不变。
