# MacEverything 性能分析报告 R_2606100030

## 1. 本轮范围

- 任务启动时间：2026-06-10 00:30
- 报告文件：`docs/performance_ana/R_2606100030.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=7252`
  - `SEG_LINES=6310`
  - `LAST_LOG_TS=2026-06-09 20:16`

## 2. 执行摘要

**lockWait=65.3ms + `path:/Users`=720ms——深夜异常事件。** 1/16 >500ms，Max=720ms，P50=135ms，极佳比例 3/16。从 R226 的深夜稳态（Max=166ms，极佳 9/16）突然恶化。**`中文测试` lockWait=65.3ms——系列第 2 高（仅低于 R217=202.6ms）。** `path:/Users`=720ms（phase2=705ms——路径数组 page cache 大面积淘汰）。

| 指标 | R226 | R227 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **1/16** | ↑1 |
| Max | 166ms | **720ms** | **↑334%** |
| P50 | 56ms | **135ms** | ↑141% |
| lockWait max | 0ms | **65.3ms** | **↑∞（系列第 2 高！）** |
| 极佳比例(<50ms) | 9/16 | **3/16** | ↓6 |
| tombstone | 1,985,093 (27.57%) | **1,994,643 (27.64%)** | ↑ |
| tombstone 增速 | ~418/min | ~319/min | ↓24%（深夜减速） |
| recordCount | 7,182,935 | **7,192,835** | +9.9K |
| liveRecordCount | 5,197,842 | **5,198,192** | +350 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5653 | **5699** | +46（间隔缩短——SKIPFLUSH 周期非固定 60） |
| COMPACTION | 2 | **2** | **不变——第 28 轮确认缺陷** |
| QUERYADV_GE500 | 59 | **60** | ↑1（`path:/Users`=720ms） |

### lockWait=65.3ms——系列第 2 高

- **`中文测试`：queryTime=290ms，phase2=224ms，lockWait=65.3ms。**
- **lockWait 占 queryTime 的 22%——五分之一时间在等锁。**
- **系列纪录：R217=202.6ms > **R227=65.3ms** > R215=18.2ms。**
- **深夜出现锁竞争——可能有定时后台任务（如 Time Machine、Spotlight）。**

### lockWait 历史纪录

| 轮次 | lockWait max | 触发查询 | 时段 |
|------|------------:|---------|------|
| R217 | **202.6ms** | `.pdf` | 午后 |
| **R227** | **65.3ms** | **`中文测试`** | **深夜** |
| R215 | 18.2ms | `.pdf` | 午后 |
| R214 | 2.5ms | `.pdf` | 午后 |

- **深夜首次出现显著 lockWait——R214-R216 均为午后。**
- **可能是定时任务触发了大量 FSEvents，导致索引更新持有写锁。**

### `path:/Users`=720ms——路径数组 page cache 淘汰

- **phase2=705ms，lockWait=0——纯 page cache 淘汰。**
- **此前 R226 `path:/Users`=73ms——10 倍恶化。**
- **路径数组是连续内存区域，但规模大（~7M records × 路径长度），对 page cache 压力大。**
- **深夜出现 page cache 淘汰——与 lockWait=65.3ms 一致——可能有后台 I/O 密集任务。**

### Trigram 路径——受压

| Query | R226 | R227 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 166ms | **217ms** | ↑31% | 正常 |
| `hello world` | 161ms | **372ms** | ↑131% | 正常 |
| `*.swift` | 98ms | **135ms** | ↑38% | 正常 |
| `main.cpp` | 23ms | **30ms** | ↑30% | 极佳 |
| `abc def...` | 122ms | **147ms** | ↑20% | 正常 |
| `README` | 38ms | **96ms** | ↑153% | 正常 |
| `test` | 47ms | **119ms** | ↑153% | 正常 |
| `MacEverything` | 12ms | **19ms** | ↑58% | 极佳 |
| Trigram 均值 | 83ms | **142ms** | **↑71%** | 受压 |

- **Trigram 均值 83→142ms（↑71%）——受压但全部 <400ms。**
- **`hello world`=372ms——本轮 trigram 最高。**

### 全表扫描——恶化（路径数组淘汰）

| Query | R226 | R227 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 73ms | **193ms** | ↑164% |
| `path:/Users` | 73ms | **720ms** | **↑886%（>500ms！）** |
| `中文测试` | 48ms | **290ms** | ↑504%（lockWait=65.3ms） |
| **总和** | **194ms** | **1203ms** | **↑520%** |

- **全表扫描总和 194→1203ms（↑520%）——从本会话最低到大幅恶化。**
- **`path:/Users`=720ms 主导——路径数组 page cache 被后台 I/O 淘汰。**

### 辅助路径

| Query | R226 | R227 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 5ms | **13ms** | ↑160% | 极佳 |
| `ext:cpp` | 56ms | **88ms** | ↑57% | 正常 |
| `size:>100m` | 69ms | **78ms** | ↑13% | 正常 |
| `dm:today` | 15ms | **66ms** | ↑340% | 正常 |
| `verylongquery...` | 0.4ms | **0.2ms** | ↓50% | 极佳 |

### tombstone 27.64%——接近 2M

- **tombstone=1,994,643（27.64%）——超阈值 2.64%。**
- **距 2M 仅差 5,357——预计下 1-2 轮突破。**
- **增速 ~319/min（深夜减速）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 13.1 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 216.9 | 212.5 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 29.7 | 18.8 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 135.1 | 132.0 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 87.6 | 85.8 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 78.3 | 75.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 372.0 | 368.6 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 146.6 | 146.2 | advanced-trigram | 0 | 14400 | 正常 |
| `README` | 5 | 96.1 | 89.0 | advanced-trigram | 0 | 17138 | 正常 |
| `test` | 5 | 119.1 | 110.6 | advanced-trigram | 0 | 117092 | 正常 |
| `MacEverything` | 5 | 18.9 | 1.3 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 66.0 | 65.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 192.6 | 179.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 720.3 | 704.7 | advanced-linear-gcd | 0 | 0 | **>500ms** |
| `中文测试` | 0 | 290.2 | 223.5 | advanced-linear-gcd | **65.3** | 0 | 正常（lockWait!） |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 135.1ms，P90 = 372.0ms，Max = 720.3ms。1/16 查询 >500ms。lockWait max = 65.3ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R226 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **3/16** | 0.2-30ms | R226=9/16（↓6） |
| **正常**（50-500ms） | 12/16 | 66-372ms | R226=7/16（↑5） |
| **超标**（>500ms） | **1/16** | 720ms | R226=0/16（↑1） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,192,835，liveRecordCount=5,198,192（tombstone=**1,994,643**，**27.64%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=496  QUERYADV_GE500=60（↑1）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5699
```

## 6. 问题清单

### P0 确认（第 28 轮）：compaction 触发机制缺陷

- **tombstone 27.64%——超阈值 2.64%。即将突破 2M。**

### P0：flush 写锁阻塞——深夜也出现

- **R227 lockWait=65.3ms（深夜！）。此前 lockWait 事件仅出现在午后。**
- **说明锁竞争不仅限于高负载时段——定时后台任务也可触发。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 28 轮确认）

### P0：flush rewrite 改为 COW 方式

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. lockWait 是否消退（65.3ms 是否为一次性事件）。
2. `path:/Users` 是否恢复。
3. tombstone 是否突破 2M。

## 9. 结论

**lockWait=65.3ms + `path:/Users`=720ms——深夜异常事件。** 1/16 >500ms，Max=720ms（↑334%），P50=135ms。**lockWait=65.3ms 为系列第 2 高（仅低于 R217=202.6ms）——首次在深夜出现显著锁竞争**，可能由定时后台任务（Time Machine/Spotlight）触发。`path:/Users`=720ms（phase2=705ms）——路径数组 page cache 被后台 I/O 淘汰。

**tombstone 27.64%（第 28 轮确认 compaction 缺陷）——距 2M 仅差 5,357。** 预计下 1-2 轮突破 2M 里程碑。
