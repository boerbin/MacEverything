# MacEverything 性能分析报告 R_2606091900

## 1. 本轮范围

- 任务启动时间：2026-06-09 19:00
- 报告文件：`docs/performance_ana/R_2606091900.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6518`
  - `SEG_LINES=5576`
  - `LAST_LOG_TS=2026-06-09 14:17`

## 2. 执行摘要

**1 轮完美恢复——0/16 >500ms，Max=328ms，P50=47ms，lockWait=0，极佳比例 9/16。** R215 的系统级资源竞争（`x`=620ms，lockWait=18.2ms，3/16 >500ms）完全消退。第 8 次确认 1 轮恢复模式。`中文测试`=50ms（首次极佳！）。

| 指标 | R215 | R216 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 3/16 | **0/16** | **↓3（1 轮恢复）** |
| Max | 975ms | **328ms** | **↓66%** |
| P50 | 191ms | **47ms** | **↓75%** |
| lockWait max | 18.2ms | **0ms** | **完美（↓100%）** |
| 极佳比例(<50ms) | 3/16 | **9/16** | **↑6** |
| tombstone | 1,871,930 (26.55%) | **1,884,334 (26.67%)** | ↑ |
| tombstone 增速 | ~757/min | **~413/min** | ↓45%（回落） |
| recordCount | 7,050,190 | **7,064,457** | +14.3K |
| liveRecordCount | 5,178,260 | **5,180,123** | +1.9K |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 4993 | **5053** | +60 |
| COMPACTION | 2 | **2** | **不变——第 17 轮确认缺陷** |
| QUERYADV_GE500 | 52 | **52** | 不变（恢复） |

### 1 轮恢复模式——第 8 次确认

| 事件 | 灾难轮 | 恢复轮 | 灾难 Max | 恢复 Max |
|------|--------|--------|---------|---------|
| #1 | R176 | R177 | 2217ms | ~50ms |
| #2 | R179 | R180 | ~1500ms | ~40ms |
| #3 | R181 | R182 | ~1200ms | ~35ms |
| #4 | R190 | R191 | ~1800ms | ~45ms |
| #5 | R194 | R195 | ~1600ms | ~30ms |
| #6 | R199 | R200 | 6155ms | ~300ms |
| #7 | R208 | R209 | 3436ms | 80ms |
| **#8** | **R215** | **R216** | **975ms** | **328ms** |

- **R215=975ms → R216=328ms（↓66%）——模式高度稳定。**
- **R215 是系统级三重压力（调度+cache+锁），R216 完全消退——系统自愈能力优秀。**

### 进程调度延迟——完全消退

- **`x`=4.3ms（R215=620ms，↓99.3%）——回归稳态。**
- **lockWait=0（R215=18.2ms，↓100%）——后台索引更新压力消退。**
- **系统级资源竞争为一次性事件——可能是某个大型后台任务完成。**

### `中文测试`=50ms——首次极佳！

- **phase2=49.5ms——线性扫描极快。**
- **此前最低 R209=67ms——本轮 ↓25%。**
- **名称数组区域 page cache 完全温热。**

### Trigram 路径——大幅恢复

| Query | R215 | R216 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 975ms | **110ms** | ↓89% | 正常（恢复） |
| `hello world` | 330ms | **328ms** | ↓1% | 正常（持平） |
| `*.swift` | 208ms | **62ms** | ↓70% | 正常 |
| `main.cpp` | 314ms | **18ms** | ↓94% | 极佳 |
| `abc def...` | 131ms | **203ms** | ↑55% | 正常（波动） |
| `README` | 47ms | **46ms** | ↓2% | 极佳 |
| `test` | 181ms | **141ms** | ↓22% | 正常 |
| `MacEverything` | 19ms | **27ms** | ↑42% | 极佳 |
| Trigram 均值 | 276ms | **117ms** | **↓58%** | 大幅恢复 |

- **Trigram 均值 276→117ms（↓58%）——大幅恢复。**
- **`.pdf`=110ms（↓89% from 975ms）——从灾难到正常。**
- **`hello world`=328ms 和 `abc def`=203ms 偏高——postings 区域尚未完全预热。**

### 全表扫描——优秀

| Query | R215 | R216 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 191ms | **126ms** | ↓34% |
| `path:/Users` | 192ms | **115ms** | ↓40% |
| `中文测试` | 163ms | **50ms** | **↓69%** |
| **总和** | **546ms** | **291ms** | **↓47%** |

- **全表扫描总和 546→291ms（↓47%）——优秀！**
- **接近 R209=193ms 的系列最低水平。**
- **线性扫描区域 page cache 完全温热。**

### 辅助路径——全面恢复

| Query | R215 | R216 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 620ms | **4ms** | ↓99% | 极佳 |
| `ext:cpp` | 56ms | **23ms** | ↓59% | 极佳 |
| `size:>100m` | 648ms | **28ms** | **↓96%** | 极佳 |
| `dm:today` | 120ms | **32ms** | ↓73% | 极佳 |
| `verylongquery...` | 0.2ms | **0.7ms** | ↑250% | 极佳 |

- **`size:>100m`=28ms（↓96% from 648ms）——SoA 区域 1 轮完美恢复。**
- **`ext:cpp`=23ms——ext-index 恢复。**

### tombstone 26.67%——第 17 轮确认 compaction 缺陷

- **tombstone=1,884,334（26.67%）——超阈值 1.67%。**
- **增速降至 ~413/min（从 757/min 回落——系统活动减缓）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 4.3 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 109.7 | 105.8 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 18.2 | 14.6 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 62.1 | 60.6 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 22.6 | 22.1 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 28.1 | 27.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 327.9 | 324.3 | advanced-trigram | 0 | 3669 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 202.7 | 197.5 | advanced-trigram | 0 | 14361 | 正常 |
| `README` | 5 | 46.2 | 43.4 | advanced-trigram | 0 | 17052 | 极佳 |
| `test` | 5 | 140.5 | 130.5 | advanced-trigram | 0 | 116991 | 正常 |
| `MacEverything` | 5 | 26.6 | 2.7 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 31.8 | 30.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 125.7 | 109.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 114.7 | 113.0 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 49.6 | 49.5 | advanced-linear-gcd | 0 | 0 | 极佳 |
| `verylongquery...` | 0 | 0.7 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 46.2ms，P90 = 202.7ms，Max = 327.9ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R215 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **9/16** | 0.7-50ms | R215=3/16（**↑6**） |
| **正常**（50-500ms） | 7/16 | 62-328ms | R215=10/16（↓3） |
| **超标**（>500ms） | **0/16** | — | R215=3/16（**↓3**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,064,457，liveRecordCount=5,180,123（tombstone=**1,884,334**，**26.67%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=418  QUERYADV_GE500=52（不变——恢复）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5053
```

## 6. 问题清单

### P0 确认（第 17 轮）：compaction 触发机制缺陷

- **tombstone 26.67%——超阈值 1.67%。**
- **COMPACTION=2——始终不变。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 17 轮确认）

### P0.5：flush rewrite 改为 COW 方式（持续建议）

### P1：进程 QoS 优先级设置（R215 新增）

### P2：madvise MADV_WILLNEED 热区维护（持续建议）

## 8. 下一轮重点

1. 是否恢复到稳态（极佳 10+/16）。
2. tombstone 增长趋势。

## 9. 结论

**1 轮完美恢复（第 8 次确认）——0/16 >500ms，Max=328ms（↓66%），P50=47ms（↓75%），极佳 9/16。** R215 的系统级三重压力（`x`=620ms，lockWait=18.2ms，3/16 >500ms）完全消退。`.pdf` 975→110ms（↓89%），`size:>100m` 648→28ms（↓96%），`x` 620→4ms（↓99%）。`中文测试`=50ms（首次极佳！）。全表扫描总和 291ms（接近系列最低）。

**tombstone 26.67%（第 17 轮确认 compaction 缺陷）。** 增速降至 413/min（系统活动减缓）。1 轮恢复模式高度稳定——系统不存在持久性退化。
