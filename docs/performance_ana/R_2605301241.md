# MacEverything 性能分析报告 R_2605301241

- **任务启动时间**：2026-05-30 12:41
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~25.6h，未重启
- **状态快照 @12:41**：total `5,846,444` / live `5,390,980`
- **本轮定性**：⚠️ **连续第二轮污染 + R88 协议首次复用验证**——午间系统停顿再现且**递增**：`*.swift` 呈 108ms(11:41)→237ms(12:11)→**1002ms(12:41)** 的逐轮恶化签名，`main.cpp` 仅 71 候选却 237ms、缓存 `x` 21.49ms。**直接套用 R88 固化的复测协议**，3× 复测确认全部恢复（`*.swift`→6ms、`main.cpp`→1.6ms、`x`→0.05ms），洁净后 size 回到 **4.36 ns/槽**。二层分裂洁净数据 **16/16 不破**，间隙 2.30×。

---

## 1. 原始电池（含污染，⚠️ 标记异常项）

| query | path | total(ms) | phase2 | trig | cand | 备注 |
|---|---|---|---|---|---|---|
| `x` | short-query-cache | **21.49** | 0 | 0 | 0 | ⚠️ 缓存常态 <1ms |
| `.pdf` | advanced-trigram | 96.96 | 92.79 | 3.57 | 10120 | |
| `main.cpp` | advanced-trigram | **237.62** | 217.71 | 19.64 | 71 | ⚠️ 71 候选 237ms 不可能 |
| `*.swift` | advanced-trigram | **1002.40** | 985.10 | 16.85 | 9213 | ⚠️ **午间停顿（复测 6ms）** |
| `ext:cpp` | advanced-ext-index | 77.78 | 77.46 | 0 | 2987 | 略高（复测 2–21ms）|
| `size:>100m` | pure-filter-soa-gcd | **104.80** | 104.75 | 0 | 0 | ⚠️ 受污染（复测 19–37ms）|
| `hello world` | advanced-trigram | 143.02 | 139.69 | 3.01 | 2340 | |
| `abc def ghi jkl mno pqr` | advanced-trigram | 79.28 | 79.10 | 0.1 | 15923 | |
| `README` | advanced-trigram | 15.06 | 12.89 | 2.08 | 16462 | 复测一致，未污染 |
| `test` | advanced-trigram | 38.87 | 29.44 | 9.2 | 136263 | |
| `MacEverything` | advanced-trigram | 11.73 | 1.69 | 9.99 | 6696 | |
| `dm:today` | pure-filter-soa-gcd | 56.02 | 55.96 | 0 | 0 | 数值，复测更高（真冷）|
| `content:import` | pure-filter-soa-gcd | 166.58 | 154.24 | 0 | 0 | 字符串，复测一致 |
| `path:/Users` | advanced-linear-gcd | 231.71 | 227.69 | 0 | 0 | 字符串，复测一致 |
| `中文测试` | advanced-linear-gcd | 224.88 | 224.65 | 0.11 | 0 | 字符串 |
| `verylongquery…really` | advanced-trigram | 0.45 | 0 | 0.39 | 0 | |

---

## 2. 复测消歧（套用 R88 协议）

**污染标志项（确认瞬时，全部恢复）：**

| query | 电池值 | probe1 | probe2 | probe3 | 判定 |
|---|---|---|---|---|---|
| `x`（缓存）| 21.49 | 0.43 | 0.05 | 0.11 | ✅ 瞬时 |
| `main.cpp`（71 cand）| 237.62 | 6.02 | 2.15 | 1.61 | ✅ 瞬时 |
| `*.swift` | 1002.40 | 17.13 | 6.06 | 7.40 | ✅ 瞬时（午间停顿）|
| `ext:cpp` | 77.78 | 21.4 | 7.31 | 1.88 | ✅ 恢复常态 |

**数值/纯过滤项（4× 取中位，区分污染 vs 真冷）：**

| query | 电池值 | 复测中位 | 判定 |
|---|---|---|---|
| `size:>100m` | 104.80 | **25.5** | ⚠️ 受污染，恢复数值地板 |
| `dm:today` | 56.02 | **83.2** | 真冷（复测一致偏高，非污染）|
| `content:import` | 166.58 | **191.0** | 真实（复测一致）|
| `path:/Users` | 231.71 | **212.5** | 真实（复测一致）|

注：`dm` 复测（71–100ms）反而**高于**电池（56ms），说明电池恰好抓到 dm 的暖瞬间，复测抓到其冷态——dm 本轮真冷，是逐区温度（非污染）。污染只砸在与停顿同时执行的 `size`/`*.swift`/`main.cpp`/`x` 上。

---

## 3. 洁净 ns/槽（records=5,846,444）

| 路径 | 类型 | 洁净值 | 来源 |
|---|---|---|---|
| `size:>100m` | 数值 | **4.36** | 复测中位 25.5ms（数值地板）|
| `dm:today` | 数值 | **14.24** | 复测中位 83ms（本轮真冷）|
| `content:import` | 字符串 | **32.67** | 复测中位 191ms |
| `path:/Users` | 字符串 | **36.35** | 复测中位 212ms |
| `中文测试` | 字符串 | **38.46** | 电池 225ms（复测模式一致）|

---

## 4. 绝对二层分裂：洁净数据 16/16 不破

- **数值层**：size 4.36、dm 14.24 → 最高数值 **dm 14.24**
- **字符串层**：content 32.67、path 36.35、中文 38.46 → 最低字符串 **content 32.67**
- **洁净间隙 = 32.67 / 14.24 = 2.30×**，无重叠。

**数值层内部温度互换**：本轮 dm（14.24）为高位、size（4.36）为地板，与 R87（size 冷、dm 暖）方向相反——再次复现 R81 分区机制下探到单个数值列粒度（size 列与 dm 列独立冷热）。此为补充观察，不构成新结论。机制不变：纯数值零解引用恒低于字符串解引用散布串池。

---

## 5. 环境信号：午间停顿正在递增（外部，非 MacEverything 回归）

把 `*.swift`（trigram，候选固定 9213）三轮日志并排：

| 时刻 | `*.swift` total | 性质 |
|---|---|---|
| 11:41（R87）| 108ms | 正常 |
| 12:11（R88）| 237ms | 轻度停顿 |
| **12:41（R89）** | **1002ms** | **重度停顿** |

候选数恒定（9213）下耗时翻 9 倍，且同批 `ext:cpp`/缓存 `x` 同时被拖慢——证明是**系统级停顿**（疑似午间外部进程：Spotlight 重建索引 / 备份 / 其他应用内存压力），非 MacEverything 自身退化（数据冷化无法解释 ext-index 和缓存查询变慢）。停顿出现在 12:00–13:00 窗口且逐轮加重。

**重要边界**：这是 MacEverything 所处**运行环境**的信号，不是代码回归。但它直接强化了应用的**韧性需求**——应用应在外部系统停顿下优雅降级，对应下文 P0 的查询超时保护与 mlock。

---

## 6. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **419 次**（R88 408）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100，`:159` tombstone 比例阀结构性永不可达——全会话 0 触发、0 回收。

---

## 7. tombstone 趋势：连续 22 轮单调上升

| 轮次 | tombstone | ratio |
|---|---|---|
| R85@10:41 | 409,024 | 7.06% |
| R86@11:11 | 417,598 | 7.19% |
| R87@11:41 | 427,763 | 7.36% |
| R88@12:11 | 439,394 | 7.54% |
| **R89@12:41** | **455,464** | **7.79%** |

会话内单调递增。R43 参考崩溃点 12.97%（~800K），当前 7.79% 已过半程。

---

## 8. 日志健康度

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **261 条**（R88 246）——连续午间污染推高越线计数
- Top-7 峰值 `[18582, 17928, 15749, 8205, 7544, 6811, 6061]` —— 仍与历史**逐字节相同**；本轮 1002ms `*.swift` 停顿低于这些冻结历史峰值
- 延迟分布：>5s=**7**（冻结）、1-5s=**23**（R88 22，+1 即本轮 1002ms `*.swift`）、0.5-1s=14、0.1-0.5s=217
- 唯一新增 >1s 项为午间停顿致 `*.swift` 1002ms，已复测证实瞬时

---

## 9. 优化建议（四项 P0，连续两轮污染强化第 2、4 项）

1. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调 <12.97%（建议 8–10%）。当前 7.79% 紧迫性持续上升。
2. **🔥 P0｜mlock/madvise 整体常驻热区（连续两轮污染强化）**：午间停顿连续两轮命中，证明无内存保护时外部系统压力会反复拖慢任意路径。需把 path/name 串池、content 列、trigram 索引、候选行、数值 SoA 列整体钉入内存，减小被换出/停顿波及面。
3. **🔥 P0｜无锁快照 flush**：启动 flush 持锁 10.3s。
4. **🔥 P0｜查询超时保护（连续两轮实证强化）**：R88（1663ms）+ R89（1002ms）连续两轮真实停顿——超时熔断 + 降级返回是抵御外部系统停顿的必要韧性机制，已有两个真实触发样本。
- **P0｜HTTP 并发**：acceptLoop 单线程，停顿期间串行阻塞放大影响。
- **P1🔥｜CJK trigram 索引**：`中文测试` 走 linear-gcd 全扫（225ms）。

---

## 10. 结论

- **定性**：⚠️ 连续第二轮污染 + 协议复用验证。午间系统停顿递增（`*.swift` 108→237→1002ms），R88 固化的复测协议首次复用即成功消歧。
- **诚实修正连续生效**：连续两轮均未把污染样本误记为机制异常；洁净数据上二层分裂 **16/16 不破、间隙 2.30×**。
- **新环境信号（明确边界）**：捕获午间 12:00–13:00 外部系统停顿且逐轮加重——属运行环境而非代码回归，但强化应用韧性需求（超时保护 + mlock）。
- **诚实修正纪律**：无推测结论被推翻、无新结论需建立；dm 与 size 数值层内部温度互换，为 R81 分区机制列粒度复现。
- **唯一持续恶化项**：tombstone 7.79%（连续 22 轮单调），根因 R67 阀门不可达明确。
