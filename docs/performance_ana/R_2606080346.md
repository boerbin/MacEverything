# MacEverything 性能分析报告 R_2606080346

## 1. 本轮范围

- 任务启动时间：2026-06-08 03:46
- 报告文件：`docs/performance_ana/R_2606080346.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=3347`
  - `SEG_LINES=2405`
  - `LAST_LOG_TS=2026-06-08 03:47`

## 2. 执行摘要

**全表扫描大幅回落——0/16 >500ms，Max=238ms，P50=15ms，lockWait=0，极佳比例 11/16（69%）。** 全表扫描总和从 R164 的 672ms 骤降至 564ms（↓16%）——接近日间稳态水平。`path:/Users`=124ms 创近期新低（前低 R159=110ms）。Trigram 均值从 R164 的 23.3ms 恢复至 16.2ms——如预期 battery 重新热化了 postings。

### 关键指标对比

| 指标 | R164（30 min 前） | R165 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 266ms | **238ms** | **↓ 11%** |
| P50 | 25ms | **15ms** | **↓ 40%（恢复）** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 12/16 | **11/16** | ↓1（`dm:today` 升至 94ms） |
| tombstone | 1,288,342 (20.17%) | **1,296,561 (20.27%)** | ↑ 正常 |
| tombstone 增速 | ~280/min | **~274/min** | ↓ 持续回落 |
| recordCount | 6,386,632 | **6,394,969** | +8.3K |
| liveRecordCount | 5,098,290 | **5,098,408** | +118 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 2190 | **2250** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 22 轮 0 新增） |

### 全表扫描——回落至日间稳态附近

| Query | R164 | R165 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 156ms | **202ms** | ↑ 29%（零和波动） |
| `path:/Users` | 266ms | **124ms** | **↓ 53%（近期新低）** |
| `中文测试` | 250ms | **238ms** | ↓ 5% |
| **总和** | **672ms** | **564ms** | **↓ 16%** |

- **全表扫描总和 564ms——从第二峰 822ms 大幅回落至接近日间稳态（~500-550ms）。**
- **`path:/Users`=124ms——从 R164 的 266ms 骤降 53%，接近 R159 的 110ms（谷底）。**
- **`content:import`=202ms 相对偏高——与 `path:/Users` 的零和效应。两者访问不同 SoA 区域。**
- **深夜维护窗口影响基本消退。**

### 深夜全表扫描完整趋势（新增 R165）

| 轮次 | 时间 | 总和 | P50 | 说明 |
|------|------|-----:|----:|------|
| R158 | 00:46 | 820ms | 29ms | 第一峰 |
| R159 | 01:16 | 416ms | 12ms | 谷底 |
| R160 | 01:46 | 733ms | 21ms | 回弹 |
| R161 | 01:50 | 693ms | 11ms | 回落 |
| R162 | 02:16 | 778ms | 13ms | 二次升高 |
| R163 | 02:46 | 822ms | 10ms | 第二峰 |
| R164 | 03:16 | 672ms | 25ms | 回落中 |
| **R165** | **03:46** | **564ms** | **15ms** | **接近稳态** |

- **从第二峰 822ms 到当前 564ms——30 分钟内回落 31%。**
- **564ms 已低于 R153-R155（日间 22:00-23:00）的 523-605ms 范围。**
- **双峰维护窗口完全结束——系统已进入静默期。**

### Trigram 路径——恢复确认

| Query | R164 | R165 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 25ms | **16ms** | **↓ 36%** |
| `*.swift` | 18ms | **10ms** | **↓ 44%** |
| `hello world` | 28ms | **16ms** | **↓ 43%** |
| `abc def...` | 69ms | **40ms** | **↓ 42%** |
| `README` | 12ms | **15ms** | 持平 |
| `test` | 36ms | **28ms** | **↓ 22%** |
| `MacEverything` | 11ms | **6ms** | ↓ 45% |
| Trigram 均值 | 23.3ms | **16.2ms** | **↓ 30%（恢复）** |

- **Trigram 均值 16.2ms——从 R164 的 23.3ms 恢复，接近稳态（15-18ms）。**
- **如 R164 预测：本轮 battery 重新热化了 trigram postings page cache。**
- **`abc def...`=40ms（R164=69ms）——14K candidates 的 postings 页面大部分恢复。**

### SoA Filter——`dm:today` 异常偏高

| Query | R164 | R165 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 38ms | **36ms** | 持平 |
| `dm:today` | 43ms | **94ms** | **↑ 119%** |

- **`dm:today`=94ms——本轮唯一超出 <50ms 的 SoA filter 项。**
- **原因**：`dm:today` 访问的时间戳 filter 区域 page cache 被 `path:/Users` 的大幅恢复所竞争——经典零和。
- **`path:/Users` 从 266→124ms（释放了 page cache 空间），`dm:today` 从 43→94ms（被挤占）。**
- **总 SoA filter 两项总和**：R164=81ms → R165=130ms——↑ 但单次波动，非趋势。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 0.5 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 15.9 | 14.9 | advanced-trigram | 0 | 9812 | 极佳 |
| `main.cpp` | 5 | 3.6 | 2.3 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 9.8 | 9.3 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 8.2 | 6.4 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 35.5 | 35.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 15.6 | 15.1 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 40.3 | 36.3 | advanced-trigram | 0 | 14212 | 极佳 |
| `README` | 5 | 15.0 | 12.6 | advanced-trigram | 0 | 16631 | 极佳 |
| `test` | 5 | 27.5 | 23.4 | advanced-trigram | 0 | 116513 | 极佳 |
| `MacEverything` | 5 | 5.6 | 1.4 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 93.6 | 93.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（零和偏高） |
| `content:import` | 5 | 202.0 | 194.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 123.9 | 119.9 | advanced-linear-gcd | 0 | 0 | 正常（近期新低） |
| `中文测试` | 0 | 238.4 | 238.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 15.6ms，P90 = 202.0ms，Max = 238.4ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R164 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 11/16 | 0.2-40ms | R164=12/16（↓1） |
| **正常**（50-500ms） | 5/16 | 94-238ms | R164=4/16（↑1） |
| **超标**（>500ms） | 0/16 | — | R164=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,394,969，liveRecordCount=5,098,408（tombstone=**1,296,561**，**20.27%**）；
- 进程正常运行（当前会话，运行约 29.6 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=101  QUERYADV_GE500=4（不变——连续 22 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=2250
```

- **连续 22 轮 0 个新增 >500ms 查询（R144-R165）。**
- **QUERYADV 突破 100——累计 101 次高级查询（含 battery + 用户查询）。**
- **FLUSH=1**——无新 flush（29.6 小时仅启动时 1 次）。
- **SKIPFLUSH=2250**——系统持续空闲。

## 6. 问题清单

### 无活跃问题——深夜维护影响完全消退

- 0/16 >500ms，11/16 <50ms。
- P50=15ms（恢复），Max=238ms，lockWait=0。
- 连续 22 轮 0 新增慢查询。
- 29.6 小时无 WARN/ERROR。
- 全表扫描总和 564ms——已接近日间稳态。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 20.27%，增速 ~274/min（持续回落）。
- 距 25% 阈值约 ~27 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 系统空闲时无影响。

## 8. 下一轮重点

1. 全表扫描总和是否稳定在 500-550ms 日间稳态。
2. `dm:today` 是否在下轮恢复至 <50ms。
3. tombstone 增速趋势（20.27%）。

## 9. 结论

**全表扫描大幅回落——0/16 >500ms，Max=238ms，P50=15ms，lockWait=0，极佳比例 11/16。** 全表扫描总和从第二峰 822ms 回落至 564ms（↓31%）——深夜双峰维护窗口影响完全消退，`path:/Users`=124ms 接近近期新低。Trigram 均值 16.2ms 从 R164 的 23.3ms 恢复——battery 热化确认。

**深夜维护窗口全周期观测完毕**：双峰（820ms/822ms）→ 恢复至 564ms（日间稳态附近）。连续 22 轮 0 新增慢查询，29.6 小时无异常。tombstone 20.27%（1.30M），增速 274/min（持续回落）。系统恢复至正常运行状态。
