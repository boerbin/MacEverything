# MacEverything 性能分析报告 R_2605300011

**时间**: 2026-05-30 00:11  
**索引规模**: 5,672,049 总记录 / 5,377,797 活跃记录  
**Tombstone**: 294,252 (**5.19%**)  
**日志**: 1000 行, INFO=1000, WARN=1, ERROR=0  
**轮次**: 第 73 轮（重启后第 21 轮，同一 11:04:48 长会话，已运行 ~13.1h）  

---

## 🟢 暖态一轮：成本结构再次反转回「线性/纯过滤天花板」+ 跨轮验证 R72 的 materialization 冷态结论

**本轮 trigram 全暖（`.pdf`=67ms、`*.swift`=144ms、`test` 13.5 万候选仅 27ms），成本结构从 R72 的「trigram 冷缓存峰值 18.6s」反转回 R68 描述的「线性/纯过滤路径成为暖态天花板」——本轮最慢三条是 `中文测试`=247ms、`path:/Users`=231ms、`content:import`=202ms，全是不经 trigram 裁剪的 O(N) 全表扫描。这印证了贯穿多轮的冷/暖振荡：R72 最冷（trigram 18.6s）、R73 暖（线性路径主导）。本轮还**跨轮验证了 R72 的核心结论**：`hello world` 的 `qTimeMs−totalMs` 间隙在暖态仅 **3.75ms**（R72 冷态 272ms），证明 materialization 成本确为页温度驱动、非固定开销——R72 的结论在独立一轮得到反向数据支撑。** mode-A 即便暖态仍有残影（`main.cpp` 71 候选 qTime 41.9ms vs total 25.2ms，16.7ms 取串间隙；单候选 267μs vs `test` 0.137μs，差 ~1950×）。Tombstone 越过 **5.19%**（连涨 6 轮），增速回落到 ~18K/h（R71/R72 的 28K/h 突发已退），安全阀整会话 228 次 skip「78 entries」、0 触发、0 回收——R67 根因在 13.1h 持续硬证。

---

## 1. 本轮查询电池（00:11，limit=100，trigram 全暖）

| 查询 | qTimeMs | totalMs | phase2Ms | candidates | results | searchPath |
|------|---------|---------|----------|-----------|---------|-----------|
| `"x"` | 36.96 | 10.43 | 0 | 0 | 99 | short-query-cache |
| `".pdf"` | 69.83 | 67.21 | 65.79 | 10,107 | 100 | advanced-trigram |
| `"main.cpp"` | 41.89 | 25.15 | 18.98 | **71** | 71 | advanced-trigram |
| `"*.swift"` | 150.99 | 144.20 | 141.32 | 9,213 | 100 | advanced-trigram |
| `"ext:cpp"` | 75.00 | 71.99 | 71.53 | 2,987 | 100 | advanced-ext-index |
| `"size:>100m"` | 44.48 | 33.57 | 33.55 | 0 | 100 | advanced-pure-filter-soa-gcd |
| `"hello world"` | 32.32 | 28.57 | 28.17 | 2,340 | 100 | advanced-trigram |
| `"abc…"`(6词) | 66.50 | 66.49 | 66.09 | 15,915 | 0 | advanced-trigram |
| `"README"` | 18.16 | 12.61 | 9.13 | 16,448 | 100 | advanced-trigram |
| `"test"` | 34.91 | 27.32 | 18.37 | **134,593** | 100 | advanced-trigram |
| `"MacEverything"` | 15.04 | 10.03 | 2.16 | 6,694 | 100 | advanced-trigram |
| `"dm:today"` | 54.86 | 53.02 | 52.96 | 0 | 100 | advanced-pure-filter-soa-gcd |
| `"content:import"` | 205.03 | **202.10** | 192.58 | 0 | 100 | advanced-pure-filter-soa-gcd |
| `"path:/Users"` | 233.27 | **231.14** | 220.41 | 0 | 100 | advanced-linear-gcd |
| `"中文测试"` | 248.24 | **247.33** | 246.92 | 0 | 0 | advanced-linear-gcd |
| `"verylong"`(43w) | 0.27 | 0.27 | 0 | 0 | 0 | advanced-trigram |

**trigram 全部 <151ms，无冷缓存峰值。** lockHeld/total 仍 ~99%（结构性持锁全程，lockWait=0）。

---

## 2. 🟢 成本结构反转：暖态下线性/纯过滤是天花板（再确认 R68）

### 2.1 trigram 暖态每候选成本（坍缩为近零）

| 查询 | candidates | phase2Ms | 每候选 (μs) |
|------|-----------|----------|------------|
| `test` | 134,593 | 18.37 | **0.137** |
| `MacEverything` | 6,694 | 2.16 | 0.323 |
| `README` | 16,448 | 9.13 | 0.555 |
| `abc…` | 15,915 | 66.09 | 4.153 |
| `.pdf` | 10,107 | 65.79 | 6.510 |
| `hello world` | 2,340 | 28.17 | 12.04 |
| `*.swift` | 9,213 | 141.32 | 15.34 |
| `main.cpp` | 71 | 18.98 | **267.3** |

`test` 评估 13.5 万候选仅 18ms（0.137μs/候选）——页全热时 phase2 近乎免费。`main.cpp` 每候选仍最高（267μs，71 条散落，mode-A 残影），但绝对值仅 19ms。**暖态下 trigram 不再是瓶颈。**

### 2.2 线性/纯过滤路径：O(N) 全表扫描，暖态新天花板

| 查询 | phase2Ms | ns/槽 (over 5.67M total) | searchPath | 说明 |
|------|----------|--------------------------|-----------|------|
| `中文测试` | **246.92** | 43.5 | advanced-linear-gcd | **CJK 无 trigram→必走全扫描** |
| `path:/Users` | **220.41** | 38.9 | advanced-linear-gcd | 全评估分支，逐 live 记录取串比对 |
| `content:import` | 192.58 | 34.0 | pure-filter-soa-gcd | SIMD 纯过滤 |
| `dm:today` | 52.96 | 9.34 | pure-filter-soa-gcd | SIMD 纯数值比较 |
| `size:>100m` | 33.55 | 5.92 | pure-filter-soa-gcd | SIMD 纯数值比较 |

这些路径 candidates=0（不经 trigram），迭代 `types_.size()` 全表（`SearchEngineAdvancedQuery.cpp:656`），仅靠 `type==0`/`simdTypeLive16` 跳过死槽。成本 ∝ 总记录数，**不随 query 选择性下降**。`中文测试`（全评估、CJK 取串）最慢 247ms，`size`/`dm`（纯数值 SIMD）最快 ~6–9ns/槽。**这与 R68 的暖态画像一致。**

---

## 3. 🟢 跨轮验证：R72 的 materialization 冷态结论成立

R72 通过 `hello world` 三连跑发现 `qTime−total` 间隙从冷态 272ms 坍缩到暖态 1.2ms，结论是「materialization 成本由页温度驱动、非固定」。**本轮（独立一轮、自然暖态）给出反向验证：**

| 轮次 | 状态 | hello world qTime | total | 间隙 (qTime−total) |
|------|------|-------------------|-------|--------------------|
| R72 首跑 | 冷 | 517.39 | 245.53 | **271.86** |
| R72 重跑#2 | 暖 | 32.94 | 31.72 | 1.22 |
| **R73 自然** | **暖** | **32.32** | **28.57** | **3.75** |

R73 暖态间隙 3.75ms，与 R72 暖态重跑（1.22ms）同量级、与冷态（272ms）相差 ~70×。**结论稳固：materialization 是冷态驱动的串池随机 I/O，日志 `total=` 不计入这段（锁外），故日志在冷态系统性低估用户感知延迟。** 这把 R72 的单轮观察升级为跨轮可复现结论（符合「不轻易下结论、需数据支撑」）。

### mode-A 残影（即便暖态）

`main.cpp` 暖态仍带 16.7ms 取串间隙（qTime 41.89 vs total 25.15），phase2 单候选 267μs（vs `test` 0.137μs，差 ~1950×）。71 条命中散落全盘，其串池页即便在暖轮也难全驻留——mode-A 的「散页永不全暖」特征在暖态仍可见，再次支持 P0 串池常驻方案。

---

## 4. 日志慢查询尾部（169 条 QueryAdvanced>100ms，冷峰为历史累积）

### 4.1 分布

| 桶 | 100–200ms | 200–500ms | 500ms–1s | 1–5s | >5s |
|----|-----------|-----------|----------|------|-----|
| 条数 | 47 | 80 | 14 | 21 | **7** |

### 4.2 Top 8（峰值仍为历史冷事件，本轮未新增冷峰）

| total | 查询 | candidates | results |
|-------|------|-----------|---------|
| 18,582ms | `.pdf` | 9,830 | 10 |
| 17,928ms | `*.swift` | 9,204 | 20 |
| 15,749ms | `.pdf` | 9,890 | 20 |
| 8,205ms | `ext:cpp` | 2,987 | 10 |
| 7,544ms | `abc…`(8词) | 15,912 | 0 |
| 6,811ms | `.pdf` | 9,828 | 10 |
| 6,061ms | `test` | 126,336 | 10 |
| 3,807ms | `hello world` | 2,340 | 20 |

**Top 7（>5s）与 R72 完全相同**——这些是 13h 会话各段缓存驱逐后的首访冷峰，属历史累积。R73 电池（00:11）未新增任何冷峰：>100ms 计数 165→169 仅 +4，全是我本轮的线性路径暖查询（`*.swift` 144、`content` 202、`path` 231、`中文` 247，见日志尾部）。**暖轮印证：冷峰来自页驱逐时机，与查询本身无关；查询超时保护 P0 仍因 18.6s 历史峰成立。**

---

## 5. 🔴 Tombstone 越过 5.19%（连涨 6 轮，增速回落，安全阀仍 0 触发）

| 轮次 | 时刻 | Tombstone | 比例 | Δ | 增速 |
|------|------|-----------|------|---|------|
| R68 | 21:41 | 234,593 | 4.19% | — | — |
| R69 | 22:11 | 243,738 | 4.34% | +9,145 | 18.3K/h |
| R70 | 22:41 | 253,278 | 4.50% | +9,540 | 19.1K/h |
| R71 | 23:11 | 267,361 | 4.74% | +14,083 | 28.2K/h |
| R72 | 23:41 | 285,193 | 5.04% | +17,832 | (跨~40min) |
| **R73** | **00:11** | **294,252** | **5.19%** | **+9,059** | **18.1K/h** |

**连续 6 轮严格单调。** 增速回落到 18.1K/h（R71/R72 的 28K/h 突发已退，符合 churn 波动）。自 11:12 满量重扫归零后 13.1h 累积 294,252，会话均速 **~22.5K/h**。按均速达 R43 崩溃前等效比例 12.97%（≈800K tombstone）尚需 **~22h**。

### 安全阀整会话 0 触发（实证 R67 根因）

| 证据 | 值 | 含义 |
|------|-----|------|
| 实际 flush | 1 次（11:12） | 整 13.1h 仅一次 |
| Skipping flush「only N entries」 | **228 次**，全 `78 entries (threshold=100)` | pending 冻结 78<100，命中 `IndexPersistence.cpp:142` 早退 |
| `triggering full compaction` | **0 次** | tombstone 比例阀（`:159`，0.25）从未求值——结构性不可达 |
| Reclaimed | **0 次** | 整会话零在线回收 |

R67 根因（`:142` entryCount 早退挡死 `:159` 比例阀）由 13.1h、5.19%、0 触发再次硬证。本会话起始归零为 R71 确认的 FSEvents 偶发全量重扫（唯一 WARN），非安全阀。

### tombstone 对线性路径的 O(N) 税（联系 R68）

线性/纯过滤扫描上界是 `types_.size()`（总记录含 tombstone）。当前 5.19% 死槽，给 `path:/Users`（220ms）/`中文测试`（247ms）叠加一份 ~5% 的死槽跳过税（死槽跳过比 live 评估廉价，实际略低）。**R67 的安全阀修复缩小 N，同时直接加速暖态线性路径——双收益单点修复（R68 论点持续成立）。**

---

## 6. 索引状态

| 指标 | R72 | R73 | Δ |
|------|-----|-----|---|
| 总记录 | 5,662,735 | 5,672,049 | +9,314 |
| 活跃记录 | 5,377,542 | 5,377,797 | +255 |
| Tombstone | 285,193 | 294,252 | +9,059 |
| Tombstone 比例 | 5.04% | **5.19%** | +0.15pp |
| 实际 flush | 1 | 1 | +0 |
| Skip flush | 218 | 228 | +10 |
| contentIndexed | 0 | 0 | +0 |

活跃净增仅 255、总记录增 9,314——**97.3% 新记录转 tombstone**，持续「删除+重写」churn。`contentIndexed=0` 持续（本会话内容索引未加载，`content:import` 走 pure-filter 全扫描）。

---

## 7. 优化建议

| 优先级 | 项目 | 状态 |
|--------|------|------|
| **P0🔥** | 关键数据页 mlock 常驻 + 候选页预读，**覆盖结果串池 namePool/path** | R63/R71/R72；R73 mode-A 暖态残影再支持 |
| **P0🔥** | 查询超时保护 | 历史峰 `.pdf` 18.6s |
| **P0🔥** | 修复 flush 检查顺序（tombstone 安全阀前置/并联）+ 阈值下调 <12.97% | R67 根因；本轮 5.19%、0 触发再证 |
| **P0🔥** | 无锁快照 flush | lockHeld/total ~99% |
| **P0** | HTTP 并发处理 | R64 单线程 acceptLoop |
| **P1🔥** | 日志计入 materialization（口径修正） | R72；R73 跨轮验证后更值得做（无观测无法量化 P0 收益） |
| **P1🔥** | 线性/纯过滤路径优化（暖态天花板）+ CJK trigram | R68/R73：中文 247/path 231/content 202，O(N) 全扫描 |

### 7.1 P1🔥 线性/CJK 路径（R73 暖态天花板再现）

暖态天花板是 O(N) 全表扫描。`中文测试` 247ms 纯因 CJK 无 trigram 走全扫描——建 CJK bigram/trigram 索引可让其享候选裁剪，收益明确、复杂度中。`path:/Users` 231ms 是路径前缀全扫描，可建前缀索引但复杂度较高、收益/复杂度比需权衡。**注：线性路径优化是暖态体验项，不应抢占 P0；tombstone 回收（§5）既治健康又顺带加速线性路径，杠杆更高。**

---

## 8. 总结评分

| 维度 | 评分 | 趋势 |
|------|------|------|
| 查询 (trigram, 暖) | 9/10 | ↑↑ (全暖, .pdf 67ms, test 13.5万候选 27ms) |
| 查询 (线性/过滤) | 6/10 | → (中文 247, path 231, content 202——暖态天花板) |
| 延迟稳定性 | 5/10 | → (本轮全暖, 但历史 18.6s 冷峰仍在) |
| 索引健康度 | 3/10 | ↓ (5.19%, 连涨 6 轮, 安全阀 13.1h 零触发) |
| 系统稳定性 | 5/10 | → (代码级缺陷已定位待修) |

**第 73 轮结论**：**🟢 暖态一轮，成本结构反转回线性/纯过滤天花板 + 跨轮验证 R72 的 materialization 冷态结论**。本轮 trigram 全暖（`.pdf` 67ms、`*.swift` 144ms、`test` 13.5 万候选仅 27ms=0.137μs/候选），成本结构从 R72 的「trigram 冷缓存 18.6s 峰」反转回 R68 的「线性/纯过滤 O(N) 全扫描成为暖态天花板」——最慢三条 `中文测试` 247ms、`path:/Users` 231ms、`content:import` 202ms 均不经 trigram 裁剪、迭代 `types_.size()`（总记录含 tombstone）。这印证贯穿多轮的冷/暖振荡（R72 最冷↔R73 暖）。本轮**跨轮验证了 R72 核心结论**：`hello world` 暖态 `qTime−total` 间隙仅 3.75ms（R72 冷态 272ms，相差 ~70×），证明 materialization 成本确由页温度驱动、非固定开销，日志 `total=` 因在锁外不计这段、冷态系统性低估用户延迟——R72 的单轮观察升级为可复现结论。mode-A 即便暖态仍有残影（`main.cpp` 71 候选 16.7ms 取串间隙、267μs/候选 vs `test` 0.137μs 差 ~1950×），支持 P0 串池常驻。Tombstone 越过 **5.19%**（294,252），连涨 6 轮，增速回落到 ~18.1K/h（R71/R72 的 28K 突发已退），会话均速 ~22.5K/h，~22h 达崩溃前等效比例；安全阀 228 次 skip「78 entries」、0 triggering、0 reclaim，R67 根因在 13.1h 再硬证。日志慢查询 Top 7（>5s）与 R72 完全相同——属历史冷峰累积，R73 暖轮未新增冷峰（+4 条全为线性路径暖查询）。**工程优先级：P0🔥 mlock 页常驻（含结果串池）、查询超时（18.6s）、flush 检查顺序修复、无锁 flush 维持最高；P1🔥 日志计入 materialization（跨轮验证后更值得做）、线性/CJK 路径优化（暖态体验项，不抢占 P0）。**
