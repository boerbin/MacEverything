# MacEverything 性能分析报告 R_2606092130

## 1. 本轮范围

- 任务启动时间：2026-06-09 21:30
- 报告文件：`docs/performance_ana/R_2606092130.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6866`
  - `SEG_LINES=5924`
  - `LAST_LOG_TS=2026-06-09 16:47`

## 2. 执行摘要

**灾难轮——6/16 >500ms，Max=1698ms，P50=311ms，lockWait=0，极佳比例 2/16。** 连续 3 轮 0/16 后突然恶化。`hello world`=1698ms（phase2=1071ms），`content:import`=1286ms（**phase2=119ms，overhead=1167ms——进程调度延迟灾难级！**），`.pdf`=1160ms，`path:/Users`=1016ms，`*.swift`=607ms，`test`=578ms。**lockWait=0——不是锁竞争，是 page cache 淘汰 + 进程调度延迟双重打击。**

| 指标 | R220 | R221 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **6/16** | **↑6（灾难）** |
| Max | 415ms | **1698ms** | **↑309%** |
| P50 | 130ms | **311ms** | ↑139% |
| lockWait max | 0ms | **0.8ms** | 微量（可忽略） |
| 极佳比例(<50ms) | 5/16 | **2/16** | ↓3 |
| tombstone | 1,928,515 (27.08%) | **1,937,740 (27.17%)** | ↑（持续） |
| tombstone 增速 | ~341/min | ~307/min | ↓10%（晚间减速） |
| recordCount | 7,122,394 | **7,131,988** | +9.6K |
| liveRecordCount | 5,193,879 | **5,194,248** | +369 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5293 | **5353** | +60 |
| COMPACTION | 2 | **2** | **不变——第 22 轮确认缺陷** |
| QUERYADV_GE500 | 53 | **56** | **↑3（新增 >500ms 日志）** |

### 进程调度延迟——多查询扩散！

本轮最关键发现：**进程调度延迟不再局限于 `x`，而是扩散到多个查询的 overhead 中。**

| Query | queryTimeMs | phase2Ms | overhead | 说明 |
|-------|----------:|--------:|---------:|------|
| `content:import` | 1286 | 119 | **1167ms** | **灾难级！phase2 仅 119ms** |
| `hello world` | 1698 | 1071 | **627ms** | 大量 overhead |
| `path:/Users` | 1016 | 431 | **586ms** | 大量 overhead |
| `.pdf` | 1160 | 747 | **413ms** | 显著 overhead |
| `test` | 578 | 321 | **256ms** | 显著 overhead |
| `x` | 114 | 0 | **114ms** | 全是调度延迟 |
| `ext:cpp` | 312 | 225 | 87ms | 中度 |
| `*.swift` | 607 | 531 | 77ms | 中度 |
| `MacEverything` | 69 | 6 | 63ms | 中度 |

- **`content:import`=1286ms 中 phase2=119ms（仅 9%！），overhead=1167ms——进程在查询执行前后被调度走了 1.2 秒。**
- **这说明系统 CPU 正在被严重竞争——进程频繁被抢占。**
- **`x`=114ms（R220=7ms，↑1529%）——进程调度延迟回升。**

### 进程调度延迟历史（`x` queryTime）

| 轮次 | `x` ms | 说明 |
|------|------:|------|
| R218 | 12 | 正常 |
| R219 | 8 | 正常 |
| R220 | 7 | 正常 |
| **R221** | **114** | **回升——系统负载增加** |

### Page Cache 淘汰——Trigram 路径大面积恶化

| Query | R220 | R221 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 229ms | **1160ms** | ↑407% | **>500ms** |
| `hello world` | 415ms | **1698ms** | ↑309% | **>500ms（本轮最高！）** |
| `*.swift` | 130ms | **607ms** | ↑367% | **>500ms** |
| `main.cpp` | 41ms | **125ms** | ↑205% | 正常 |
| `abc def...` | 147ms | **478ms** | ↑225% | 正常（接近 500ms） |
| `README` | 51ms | **170ms** | ↑233% | 正常 |
| `test` | 132ms | **578ms** | ↑338% | **>500ms** |
| `MacEverything` | 16ms | **69ms** | ↑331% | 正常 |
| Trigram 均值 | 145ms | **611ms** | **↑321%** | **灾难级** |

- **Trigram 均值 145→611ms（↑321%）——从持续恢复到灾难。**
- **6/8 trigram 查询 >100ms——大面积 page cache 淘汰。**
- **`hello world`=1698ms 是本会话系列中第 3 高纪录（仅低于 R199=6155ms 和 R208=3436ms 级别）。**

### 全表扫描——overhead 主导

| Query | R220 | R221 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 294ms | **1286ms** | ↑337%（**overhead=1167ms！**） |
| `path:/Users` | 285ms | **1016ms** | ↑256%（overhead=586ms） |
| `中文测试` | 171ms | **69ms** | **↓60%（极佳！）** |
| **总和** | **750ms** | **2371ms** | **↑216%** |

- **全表扫描总和 750→2371ms（↑216%）——灾难。**
- **`content:import`=1286ms 中 phase2=119ms——查询本身很快，但进程被调度走了 1167ms！**
- **`中文测试`=69ms（↓60%）——名称数组区域 cache 温热，与 SoA 区域形成对比。**

### 辅助路径

| Query | R220 | R221 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 7ms | **114ms** | ↑1529% | 正常（调度延迟） |
| `ext:cpp` | 47ms | **312ms** | ↑564% | 正常 |
| `size:>100m` | 52ms | **73ms** | ↑40% | 正常 |
| `dm:today` | 55ms | **25ms** | ↓55% | 极佳 |
| `verylongquery...` | 2.7ms | **2.4ms** | ↓11% | 极佳 |

- **`size:>100m`=73ms——SoA fileSize 数组未受严重影响（连续区域）。**
- **`dm:today`=25ms（极佳）——SoA modTime 数组预热。**

### tombstone 27.17%——持续增长

- **tombstone=1,937,740（27.17%）——超阈值 2.17%。**
- **增速 ~307/min（晚间减速）。**
- **recordCount +9.6K，liveRecordCount +369——净新增极少，9.2K 为 tombstone。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 113.8 | 0.0 | short-query-cache | 0 | 0 | 正常（调度延迟） |
| `.pdf` | 5 | 1159.9 | 746.9 | advanced-trigram | 0 | 9860 | **>500ms** |
| `main.cpp` | 5 | 125.4 | 68.2 | advanced-trigram | 0 | 82 | 正常 |
| `*.swift` | 5 | 607.2 | 530.6 | advanced-trigram | 0 | 9345 | **>500ms** |
| `ext:cpp` | 5 | 311.9 | 224.7 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 72.6 | 71.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 1698.3 | 1071.3 | advanced-trigram | 0 | 3671 | **>500ms（本轮最高）** |
| `abc def ghi jkl mno pqr` | 0 | 477.8 | 448.4 | advanced-trigram | 0 | 14387 | 正常（接近 500ms） |
| `README` | 5 | 170.3 | 137.1 | advanced-trigram | 0 | 17137 | 正常 |
| `test` | 5 | 577.6 | 321.4 | advanced-trigram | 0 | 117078 | **>500ms** |
| `MacEverything` | 5 | 69.2 | 6.1 | advanced-trigram | 0 | 6859 | 正常 |
| `dm:today` | 5 | 24.5 | 21.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 1286.2 | 119.3 | advanced-pure-filter-soa-gcd | 0 | 0 | **>500ms（overhead=1167ms！）** |
| `path:/Users` | 5 | 1016.0 | 430.5 | advanced-linear-gcd | 0 | 0 | **>500ms** |
| `中文测试` | 0 | 68.6 | 55.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 2.4 | 0.0 | advanced-trigram | 0.8 | 0 | 极佳 |

**P50 = 311.9ms，P90 = 1159.9ms，Max = 1698.3ms。6/16 查询 >500ms。lockWait ≈ 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R220 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **2/16** | 2.4-25ms | R220=5/16（↓3） |
| **正常**（50-500ms） | 8/16 | 69-478ms | R220=11/16（↓3） |
| **超标**（>500ms） | **6/16** | 578-1698ms | R220=0/16（**↑6**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,131,988，liveRecordCount=5,194,248（tombstone=**1,937,740**，**27.17%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=461  QUERYADV_GE500=56（↑3——.pdf, hello world, *.swift/test/content:import/path:中部分被日志记录）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5353
LAST_LOG: DIAG-TRIGRAM key="中文测试" allFound=false（trigram 索引未完全命中——线性扫描 fallback）
```

## 6. 问题清单

### P0 确认（第 22 轮）：compaction 触发机制缺陷

- **tombstone 27.17%——超阈值 2.17%。COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **R217=202.6ms 证据。COW flush 是正确修复路径。**

### 新发现：进程调度延迟扩散

- **`content:import`=1286ms 中 phase2=119ms，overhead=1167ms——进程调度延迟不再局限于 `x`。**
- **多个查询出现 100-1167ms 的 overhead——进程被频繁抢占。**
- **P1 进程 QoS 优先级设置的必要性再次增强。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 22 轮确认）

### P0：flush rewrite 改为 COW 方式

### P1：进程 QoS 优先级设置（本轮强化证据）

- **`content:import` overhead=1167ms 证明进程在查询执行期间被频繁调度走。**
- **设置 `QOS_CLASS_USER_INTERACTIVE` 或 `DISPATCH_QUEUE_PRIORITY_HIGH` 可减少被抢占的概率。**

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 是否 1 轮恢复（预期高概率——此前 8 次确认 1 轮恢复模式）。
2. tombstone 增长趋势。
3. 进程调度延迟是否消退。

## 9. 结论

**灾难轮——6/16 >500ms（从连续 3 轮 0/16 突然恶化），Max=1698ms（`hello world`），P50=311ms。** 关键新发现：**进程调度延迟扩散——`content:import`=1286ms 中 phase2 仅 119ms（9%），overhead=1167ms！** 进程在查询执行期间被频繁调度走。这不再是 `x` 独有的问题——`hello world` overhead=627ms，`path:/Users` overhead=586ms，`.pdf` overhead=413ms。lockWait≈0——不是锁竞争。

**tombstone 27.17%（第 22 轮确认 compaction 缺陷）。** QUERYADV_GE500=56（↑3）。基于此前 8 次确认的 1 轮恢复模式，预期下轮恢复。P1 进程 QoS 优先级设置的必要性因本轮 overhead 扩散证据而增强。
