# MacEverything 性能分析报告 R_2606102230

## 1. 本轮范围

- 任务启动时间：2026-06-10 22:30
- 报告文件：`docs/performance_ana/R_2606102230.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9406`
  - `SEG_LINES=8464`
  - `LAST_LOG_TS=2026-06-10 20:47`

## 2. 执行摘要

**`hello world`=362ms 尖峰 + flush rewrite=2179ms（系列新低！）。** 0/16 >500ms，Max=362ms，P50=68.8ms（↑26% from R270=55ms），lockWait=0。**极佳 5/16（post-compaction 新低！）。** `x`=18ms（缓存稳定，连续 10 轮）。`hello world`=362ms（↑216% from R270=114ms——postings 页淘汰尖峰）。`*.swift` 322→40ms（**1-round recovery 第 18 次，18/18=100%**）。全表扫描总和 782ms（↑5% from R270=742ms——轻微回升，稳态区间内）。`中文测试`=175ms（**post-compaction 最佳！** 连续 4 轮恢复 422→310→268→175ms）。**flush rewrite=2179ms（↓40% from R270=3638ms——系列新低！sqcache=12ms 回归正常）。** FLUSH 80→86（+6）。tombstone=183,038（3.38%——增速持续）。

| 指标 | R270 | R271 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 322ms | **362ms** | ↑12% |
| P50 | 55ms | **68.8ms** | ↑25% |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 8/16 | **5/16** | **↓3（post-compaction 新低！）** |
| tombstone | 166,527 (3.08%) | **183,038 (3.38%)** | ↑（增速持续） |
| recordCount | 5,414,599 | **5,430,735** | +16.1K（连续 8 轮偏高） |
| liveRecordCount | 5,248,072 | **5,247,697** | -375（轻微净删除） |
| FLUSH 累计 | 80 | **86** | +6 |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 8 | **8** | 不变 |
| QUERYADV_GE500 | 78 | **78** | 不变 |

### flush rewrite=2179ms——系列新低！

**日志末行：**
```
[2026-06-10 20:47:29.513] Flushed v6 flat index, lastEventId=341010874,
liveRecords=5247698 | timing: walSwap=0ms rewrite=2179ms sqcache=12ms total=2192ms
```

| 轮次 | flush rewrite | sqcache | total | 变化 |
|------|------------:|--------:|------:|------|
| R265 | 2617ms | 7ms | 2626ms | 首次捕获 |
| R266 | 3981ms | 2ms | 3984ms | 峰值 |
| R268 | 2688ms | 2ms | 2692ms | 回落 |
| R269 | 2421ms | 3ms | 2425ms | 下降 |
| R270 | 3638ms | 164ms | 3803ms | 急升 |
| **R271** | **2179ms** | **12ms** | **2192ms** | **↓40%（系列新低！）** |

- **flush rewrite 3638→2179ms（↓40%——急剧回落，系列最低值！）。**
- **sqcache 164→12ms（回归正常——R270 的 164ms 是一次性异常）。**
- **flush rewrite 波动范围确认：2179-3981ms（近 2x 波动——完全由磁盘 I/O 负载驱动）。**
- **2179ms 仍然是 2.2 秒——基线未改善（完整重写 ~500MB 索引）。**

### `hello world`=362ms——postings 页淘汰尖峰

- **R270=114ms → R271=362ms（↑217%！）。**
- **phase2=297ms（3,930 candidates——正常 30-40ms，本轮 page cache 完全 miss）。**
- **与 R270 的 `*.swift`=322ms 同一模式——中等规模 postings 集被 flush I/O 淘汰。**
- **每轮不同查询被随机命中——取决于 flush 时 page cache 替换的具体页面。**

### `*.swift` 322→40ms——1-round recovery 第 18 次

- **R270=322ms → R271=40ms（↓88%——完美恢复至极佳）。**
- **18/18 = 100% 成功率——铁律级别。**
- **完整记录：R176→R177, R179→R180, R181→R182, R190→R191, R194→R195, R199→R200, R208→R209, R215→R216, R221→R222, R227→R228, R232→R233, R244→R245, R252→R254, R255→R256, R264→R265(path), R265→R266, R269→R270(P50), R270→R271(*.swift)。**

### `中文测试`=175ms——post-compaction 最佳！

| 轮次 | `中文测试` | 趋势 |
|------|----------:|------|
| R268 | 422ms | 峰值 |
| R269 | 310ms | ↓27% |
| R270 | 268ms | ↓14% |
| **R271** | **175ms** | **↓35%（连续 4 轮恢复！）** |

- **422→310→268→175ms（连续 4 轮恢复——page cache 逐轮预热）。**
- **175ms 是 post-compaction 最佳值——SoA 数据区域 page cache 充分预热。**

### 极佳比例——post-compaction 新低

**仅 5/16 极佳（<50ms）——post-compaction 最低！**

| 极佳查询 | 时间 |
|----------|-----:|
| `x` | 18ms |
| `main.cpp` | 30ms |
| `*.swift` | 40ms |
| `ext:cpp` | 27ms |
| `verylongquery...` | 0.2ms |

**11/16 查询 >50ms——广泛偏高的原因：**
- **flush rewrite=2179ms 虽然较低，但仍然冲刷了部分 page cache。**
- **`hello world`=362ms 拉高了 Max。**
- **多个查询落在 50-110ms 的"正常偏低"区间。**

### Trigram 路径——零和翻转

| Query | R270 | R271 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 183ms | **153ms** | ↓16% | 正常 |
| `hello world` | 114ms | **362ms** | **↑217%（尖峰）** | 正常 |
| `*.swift` | 322ms | **40ms** | **↓88%（恢复！）** | 极佳 |
| `main.cpp` | 28ms | **30ms** | ↑7% | 极佳 |
| `abc def...` | 86ms | **61ms** | ↓29% | 正常 |
| `README` | 19ms | **83ms** | ↑337% | 正常 |
| `test` | 30ms | **57ms** | ↑90% | 正常 |
| `MacEverything` | 5ms | **62ms** | ↑1140% | 正常 |
| Trigram phase2均值 | ~62ms | **~70ms** | ↑13%（被 hello world 拉高） |

- **去除 `hello world`=362ms 后，trigram phase2 均值 ≈ 37ms（正常）。**
- **`MacEverything` 5→62ms（phase2=6ms 但 overhead=56ms——系统负载）。**
- **典型零和翻转：`*.swift` 恢复，`hello world` 和 `README` 恶化。**

### 全表扫描——稳态区间

| Query | R270 | R271 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 214ms | **304ms** | ↑42% |
| `path:/Users` | 261ms | **303ms** | ↑16% |
| `中文测试` | 268ms | **175ms** | **↓35%（恢复）** |
| **总和** | **742ms** | **782ms** | ↑5%（稳态区间内） |

- **全表扫描 742→782ms（↑5%——正常波动，稳态 600-900ms 区间内）。**
- **`中文测试` 持续恢复抵消了 `content:import` 和 `path:/Users` 的回升。**

### `x` 缓存——连续 10 轮稳定

- **18ms（偏高——系统负载波动，R270 也是 13ms）。**
- **连续 10 轮 short-query-cache（R261-R271）。**

### tombstone 增速趋势

| 轮次 | tombstone | tombstone% | 增量 |
|------|----------:|---------:|------:|
| R267 | 121,278 | 2.31% | +14,604 |
| R268 | 135,551 | 2.56% | +14,273 |
| R269 | 150,123 | 2.85% | +14,572 |
| R270 | 166,527 | 3.08% | +16,404 |
| **R271** | **183,038** | **3.38%** | **+16,511** |

- **tombstone 增速 +16.5K/轮（略有加速——recordCount +16.1K 同步）。**
- **距 25% 阈值：(25%-3.38%) / (~0.30%/轮) ≈ 72 轮。**

### 辅助路径

| Query | R270 | R271 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 13ms | **18ms** | ↑（系统负载） | 极佳 |
| `ext:cpp` | 48ms | **27ms** | ↓44% | 极佳 |
| `size:>100m` | 40ms | **76ms** | ↑90% | 正常 |
| `dm:today` | 61ms | **110ms** | ↑80% | 正常 |
| `verylongquery...` | 0.3ms | **0.2ms** | ↓33% | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 18.4 | 0.0 | short-query-cache | 0 | 0 | 极佳（稳定） |
| `.pdf` | 5 | 153.2 | 134.8 | advanced-trigram | 0 | 9788 | 正常 |
| `main.cpp` | 5 | 29.5 | 20.1 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 40.0 | 35.0 | advanced-trigram | 0 | 9416 | 极佳（恢复！） |
| `ext:cpp` | 5 | 26.9 | 24.6 | advanced-ext-index | 0 | 3616 | 极佳 |
| `size:>100m` | 5 | 75.8 | 75.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 361.7 | 296.6 | advanced-trigram | 0 | 3930 | **正常（尖峰！）** |
| `abc def ghi jkl mno pqr` | 0 | 60.5 | 54.1 | advanced-trigram | 0 | 14459 | 正常 |
| `README` | 5 | 82.8 | 73.5 | advanced-trigram | 0 | 17381 | 正常 |
| `test` | 5 | 57.2 | 40.3 | advanced-trigram | 0 | 117766 | 正常 |
| `MacEverything` | 5 | 61.7 | 5.6 | advanced-trigram | 0 | 6943 | 正常（overhead） |
| `dm:today` | 5 | 110.0 | 109.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 304.2 | 234.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 303.1 | 293.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 174.5 | 173.8 | advanced-linear-gcd | 0 | 0 | 正常（**最佳！**） |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 68.8ms，P90 = 303.1ms，Max = 361.7ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R270 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **5/16** | 0.2-40ms | R270=8/16（**↓3，post-compaction 新低！**） |
| **正常**（50-500ms） | 11/16 | 57-362ms | R270=8/16（↑3） |
| **超标**（>500ms） | **0/16** | — | R270=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,430,735，liveRecordCount=5,247,697（tombstone=**183,038**，**3.38%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=721  QUERYADV_GE500=78（不变）
FLUSH=86（+6）  COMPACTION=8  SKIPFLUSH=7266
```

**关键日志（flush rewrite 系列新低）：**
```
Flushed v6 flat index | rewrite=2179ms sqcache=12ms total=2192ms
```

## 6. 问题清单

### P0（紧急）：flush rewrite 2.2-4.0 秒（高度不稳定）

- **rewrite=2179ms（系列新低——但仍是 2.2 秒完整重写）。**
- **波动范围 2179-3981ms（近 2x——完全由磁盘 I/O 负载驱动）。**
- **每次 flush 冲刷 page cache → 随机查询出现尖峰。**

### P0：compaction 触发机制缺陷（根因未修复）

### P0：I/O 优先级保护

### 观察：极佳比例持续走低

- **极佳比例序列（post-compaction）：12→10→8→6→8→5/16。**
- **下降趋势——flush 频率和 tombstone 增长持续侵蚀基线性能。**

## 7. 优化建议

### P0（紧急）：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. `hello world`=362ms 是否 1-round recovery。
2. 极佳比例是否恢复（5/16——post-compaction 新低）。
3. flush rewrite 趋势（2179ms 新低——是否持续低位）。
4. tombstone 增速（3.38%——加速中）。

## 9. 结论

**`hello world`=362ms 尖峰 + flush rewrite=2179ms 系列新低。** 0/16 >500ms，Max=362ms，P50=69ms。极佳仅 5/16（post-compaction 新低——下降趋势明显）。`x`=18ms（缓存稳定，连续 10 轮）。`*.swift` 322→40ms（**1-round recovery 第 18 次，18/18=100%**）。`中文测试`=175ms（**post-compaction 最佳——连续 4 轮恢复 422→175ms**）。全表扫描 782ms（稳态区间内）。

**flush rewrite=2179ms（↓40% from 3638ms——系列新低！sqcache=12ms 回归正常）。** 波动范围确认 2179-3981ms（近 2x）。tombstone=183,038（3.38%——增速 +16.5K/轮）。recordCount +16.1K（连续 8 轮偏高）。**极佳比例下降趋势是核心信号——flush 频率和 tombstone 增长在持续侵蚀基线性能。**
