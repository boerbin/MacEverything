# MacEverything 性能分析报告 R_2606101800

## 1. 本轮范围

- 任务启动时间：2026-06-10 18:00
- 报告文件：`docs/performance_ana/R_2606101800.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9147`
  - `SEG_LINES=8205`
  - `LAST_LOG_TS=2026-06-10 16:17`

## 2. 执行摘要

**post-compaction 收敛——`x` 缓存稳定，trigram 零和恢复中，lockWait 微弱复现。** 0/16 >500ms，Max=204ms，P50=21.0ms，lockWait max=3.0ms（`ext:cpp` 单点，轻微）。**极佳 11/16。** `x`=1ms（short-query-cache 稳定——连续 2 轮确认恢复）。Trigram 零和恢复：`.pdf` 67→31ms（↓54%），`hello world` 59→21ms（↓64%）。全表扫描 427→567ms（↑33%——零和回摆）。FLUSH 26→32（+6，恢复 +6 节奏）。tombstone=49,554（0.94%——健康）。

| 指标 | R261 | R262 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 159ms | **204ms** | ↑28%（零和回摆） |
| P50 | 30ms | **21.0ms** | ↓30% |
| lockWait max | 0ms | **3.0ms** | ↑（`ext:cpp` 单点） |
| 极佳比例(<50ms) | 11/16 | **11/16** | 持平 |
| tombstone | 42,626 (0.81%) | **49,554 (0.94%)** | ↑（正常增长） |
| recordCount | 5,286,245 | **5,293,491** | +7.2K（正常） |
| liveRecordCount | 5,243,619 | **5,243,937** | +318（正常） |
| FLUSH 累计 | 26 | **32** | +6（恢复 +6 节奏） |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 6 | **6** | 不变 |
| QUERYADV_GE500 | 75 | **75** | 不变 |

### `x` 缓存稳定确认

| 轮次 | `x` 路径 | `x` 时间 |
|------|---------|--------:|
| R257-R260 | advanced-linear-gcd | 587→246ms |
| R261 | short-query-cache | 17ms |
| **R262** | **short-query-cache** | **1ms** |

- **连续 2 轮 short-query-cache——缓存稳定。**
- **1ms vs R261=17ms（↓94%）——首轮重建后的 cache warm-up 完成。**
- **P2 缺陷确认：重建后缓存稳定维持，问题仅在重建延迟（4 轮）。**

### lockWait=3.0ms（`ext:cpp` 单点）

- **`ext:cpp` lockWait=3.0ms——非常轻微，仅 1/16 查询受影响。**
- **FLUSH +6 期间偶发的短暂写锁——正常现象。**
- **与 R257=325.7ms（flush+compaction）完全不同——单次 flush 的写锁时间在可接受范围。**

### Trigram 路径——零和恢复中

| Query | R261 | R262 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 67ms | **31ms** | **↓54%（恢复中）** | 极佳 |
| `hello world` | 59ms | **21ms** | **↓64%（恢复中）** | 极佳 |
| `*.swift` | 36ms | **10ms** | ↓72% | 极佳 |
| `main.cpp` | 15ms | **5ms** | ↓67% | 极佳 |
| `abc def...` | 30ms | **50ms** | ↑67%（大 postings 集） | 正常 |
| `README` | 6ms | **11ms** | ↑83% | 极佳 |
| `test` | 14ms | **55ms** | ↑293%（117K candidates） | 正常 |
| `MacEverything` | 7ms | **11ms** | ↑57% | 极佳 |
| Trigram phase2均值 | ~25ms | **~17ms** | **↓32%** |

- **`.pdf` 和 `hello world` 恢复最快——battery 首轮查询预热了 postings 页。**
- **`test`=55ms 和 `abc def`=50ms 因候选集大（117K/14K），phase2 偏高。**
- **Trigram phase2 均值 25→17ms（↓32%）——正在收敛回极致水平。**
- **预计再 1 轮完全恢复至 <10ms 均值。**

### 全表扫描——零和回摆

| Query | R261 | R262 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 118ms | **169ms** | ↑43% |
| `path:/Users` | 150ms | **194ms** | ↑29% |
| `中文测试` | 159ms | **204ms** | ↑28% |
| **总和** | **427ms** | **567ms** | **↑33%** |

- **全表扫描 427→567ms（↑33%）——零和回摆（trigram 恢复抢占了 SoA 数据页）。**
- **正常波动——post-compaction 全表扫描稳态在 400-650ms 区间。**

### 零和总量守恒验证

| 类别 | R261 | R262 | 变化 |
|------|-----:|-----:|------|
| Trigram 总和 | ~234ms | ~194ms | -40ms |
| 全表扫描总和 | 427ms | 567ms | +140ms |
| **Battery 总和** | ~765ms | ~846ms | **↑11%** |

- **零和总量 765→846ms（↑11%）——轻微上升，正常波动范围。**

### 辅助路径

| Query | R261 | R262 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 17ms | **1ms** | ↓94%（缓存预热完成） | 极佳 |
| `ext:cpp` | 12ms | **11ms** | ↓8%（lockWait=3ms） | 极佳 |
| `size:>100m` | 29ms | **29ms** | 持平 | 极佳 |
| `dm:today` | 46ms | **44ms** | ↓4% | 极佳 |
| `verylongquery...` | 0.2ms | **0.6ms** | ↑200%（微小波动） | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1.1 | 0.0 | short-query-cache | 0 | 0 | 极佳（稳定） |
| `.pdf` | 5 | 31.4 | 23.9 | advanced-trigram | 0 | 9783 | 极佳（恢复中） |
| `main.cpp` | 5 | 5.1 | 1.4 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 9.9 | 7.8 | advanced-trigram | 0 | 9402 | 极佳 |
| `ext:cpp` | 5 | 11.0 | 7.1 | advanced-ext-index | 3.0 | 3612 | 极佳（lockWait=3ms） |
| `size:>100m` | 5 | 28.6 | 28.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 21.0 | 19.8 | advanced-trigram | 0 | 3929 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 50.3 | 49.9 | advanced-trigram | 0 | 14452 | 正常 |
| `README` | 5 | 10.6 | 5.4 | advanced-trigram | 0 | 17430 | 极佳 |
| `test` | 5 | 54.9 | 30.2 | advanced-trigram | 0 | 117720 | 正常 |
| `MacEverything` | 5 | 11.0 | 0.8 | advanced-trigram | 0 | 6923 | 极佳 |
| `dm:today` | 5 | 44.4 | 44.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 168.8 | 150.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 193.6 | 184.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 204.1 | 200.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.6 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 21.0ms，P90 = 193.6ms，Max = 204.1ms。0/16 查询 >500ms。lockWait max = 3.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R261 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **11/16** | 0.6-44ms | R261=11/16（持平） |
| **正常**（50-500ms） | 5/16 | 50-204ms | R261=5/16（持平） |
| **超标**（>500ms） | **0/16** | — | R261=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,293,491，liveRecordCount=5,243,937（tombstone=**49,554**，**0.94%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=677  QUERYADV_GE500=75（不变）
FLUSH=32（+6，恢复 +6 节奏）  COMPACTION=6  SKIPFLUSH=7266
```

## 6. 问题清单

### P0：flush 写锁阻塞

- **lockWait=3ms 复现（轻微）——FLUSH +6 持续高频。**

### P0：compaction 触发机制缺陷（根因未修复）

### P2：short-query-cache 重建延迟（已恢复，监控中）

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（增加独立定时检查）

### P0：进程 QoS / I/O 优先级保护

### P2：short-query-cache flush 后增量更新

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. Trigram 是否完全恢复至 <10ms 均值。
2. 零和总量是否稳定在 800ms 左右。
3. tombstone 增速（0.94%——正常，增速 ~230/min）。

## 9. 结论

**Post-compaction 收敛——`x` 缓存稳定（1ms，连续 2 轮），trigram 零和恢复中。** 0/16 >500ms，Max=204ms，P50=21ms，lockWait max=3ms（`ext:cpp` 单点，轻微）。极佳 11/16。Trigram 零和恢复：`.pdf` 67→31ms（↓54%），`hello world` 59→21ms（↓64%），phase2 均值 25→17ms（↓32%）。全表扫描 427→567ms（↑33%——零和回摆，正常）。

**FLUSH +6（恢复 +6 节奏，26→32）。** tombstone=49,554（0.94%——健康）。recordCount +7.2K（正常）。系统处于 post-compaction 稳定收敛阶段。
