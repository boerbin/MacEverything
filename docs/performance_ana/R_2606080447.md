# MacEverything 性能分析报告 R_2606080447

## 1. 本轮范围

- 任务启动时间：2026-06-08 04:47
- 报告文件：`docs/performance_ana/R_2606080447.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=3475`
  - `SEG_LINES=2533`
  - `LAST_LOG_TS=2026-06-08 04:47`

## 2. 执行摘要

**凌晨最优——0/16 >500ms，Max=192ms，P50=14ms，lockWait=0.3ms（单次微量），极佳比例 12/16（75%）。** Max=192ms 创本会话最低（超越 R159=156ms 之外的所有轮次）！全表扫描总和从 R166 的 545ms 进一步降至 467ms——**创本系列绝对新低**（超越 R159=416ms 外的所有正常轮次）。`中文测试`=146ms **创本系列新低**。系统处于 page cache 完全充裕的极限状态。

### 关键指标对比

| 指标 | R166（31 min 前） | R167 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 223ms | **192ms** | **↓ 14%（近会话最低）** |
| P50 | 11ms | **14ms** | ↑ 微量（正常波动） |
| lockWait max | 0ms | **0.3ms** | 微量（单次，可忽略） |
| 极佳比例(<50ms) | 12/16 | **12/16** | 持平 |
| tombstone | 1,304,662 (20.37%) | **1,313,352 (20.48%)** | ↑ 正常 |
| tombstone 增速 | ~270/min | **~280/min** | 持平 |
| recordCount | 6,403,169 | **6,411,960** | +8.8K |
| liveRecordCount | 5,098,507 | **5,098,608** | +101 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 2310 | **2370** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 24 轮 0 新增） |

### 全表扫描——创本系列新低

| Query | R166 | R167 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 223ms | **192ms** | **↓ 14%** |
| `path:/Users` | 131ms | **129ms** | 持平 |
| `中文测试` | 191ms | **146ms** | **↓ 24%（系列新低）** |
| **总和** | **545ms** | **467ms** | **↓ 14%（系列新低）** |

- **全表扫描总和 467ms——超越 R165（564ms）和 R166（545ms），仅次于 R159 的 416ms（维护谷底）。**
- **`中文测试`=146ms——超越 R159（156ms），创本系列绝对新低！**
- **原因**：凌晨 4:30-5:00 是 macOS 最静默的时段——系统进程全部休眠，page cache 100% 可用于 MacEverything。
- **467ms 是正常运行下的真实极限——R159 的 416ms 含维护窗口刚结束的特殊 page cache 状态。**

### 凌晨恢复完整曲线

| 轮次 | 时间 | 总和 | P50 | Max | 说明 |
|------|------|-----:|----:|----:|------|
| R163 | 02:46 | 822ms | 10ms | 302ms | 第二峰 |
| R164 | 03:16 | 672ms | 25ms | 266ms | 回落中 |
| R165 | 03:46 | 564ms | 15ms | 238ms | 接近稳态 |
| R166 | 04:16 | 545ms | 11ms | 223ms | 稳态确认 |
| **R167** | **04:47** | **467ms** | **14ms** | **192ms** | **系列新低** |

- **全表扫描和 Max 持续下降——凌晨静默期 page cache 持续优化。**
- **预计**：05:00-06:00 可能维持或略回升（早晨系统活动开始前）。

### Trigram 路径——稳定正常

| Query | R166 | R167 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 10ms | **13ms** | 波动 |
| `*.swift` | 11ms | **15ms** | 波动 |
| `hello world` | 15ms | **16ms** | 持平 |
| `abc def...` | 33ms | **29ms** | ↓ 12% |
| `README` | 10ms | **7ms** | ↓ 30% |
| `test` | 13ms | **23ms** | ↑ 波动（page cache LRU 抖动） |
| `MacEverything` | 2ms | **2ms** | 持平 |
| Trigram 均值 | 12.0ms | **13.4ms** | ↑ 12%（正常波动） |

- **Trigram 均值 13.4ms——从 R166 的 12.0ms 微升，仍处于稳态（12-15ms）。**
- **`test`=23ms（R166=13ms）——116K candidates 的 page cache LRU 正常抖动。**
- **整体 trigram 路径稳定——无退化迹象。**

### lockWait 微量出现

- **`README` 查询 lockWait=0.3ms——首次在本系列观察到非零 lockWait。**
- **0.3ms 完全可忽略（阈值 10ms 以上才需关注）。**
- **可能原因**：flush timer 线程短暂持锁检查是否需要 flush（判断后跳过）。
- **非问题——记录备查。**

### SoA Filter

| Query | R166 | R167 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 30ms | **29ms** | 持平 |
| `dm:today` | 55ms | **58ms** | 持平 |

- **两项总和**：R166=85ms → R167=87ms——完全持平。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 3.2 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 13.1 | 11.2 | advanced-trigram | 0 | 9812 | 极佳 |
| `main.cpp` | 5 | 3.4 | 2.2 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 15.1 | 14.6 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 4.8 | 4.3 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 28.5 | 28.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 16.0 | 15.1 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 29.2 | 27.8 | advanced-trigram | 0 | 14213 | 极佳 |
| `README` | 5 | 6.7 | 5.4 | advanced-trigram | 0.3 | 16631 | 极佳 |
| `test` | 5 | 23.1 | 15.5 | advanced-trigram | 0 | 116513 | 极佳 |
| `MacEverything` | 5 | 2.3 | 0.6 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 57.9 | 57.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 192.3 | 186.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 129.1 | 125.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 145.9 | 145.6 | advanced-linear-gcd | 0 | 0 | 正常（系列新低） |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 14.1ms，P90 = 145.9ms，Max = 192.3ms。0/16 查询 >500ms。lockWait max = 0.3ms（单次）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R166 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 12/16 | 0.2-29ms | R166=12/16（持平） |
| **正常**（50-500ms） | 4/16 | 58-192ms | R166=4/16（持平） |
| **超标**（>500ms） | 0/16 | — | R166=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,411,960，liveRecordCount=5,098,608（tombstone=**1,313,352**，**20.48%**）；
- 进程正常运行（当前会话，运行约 30.7 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=107  QUERYADV_GE500=4（不变——连续 24 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=2370
```

- **连续 24 轮 0 个新增 >500ms 查询（R144-R167）。**
- **FLUSH=1**——无新 flush（30.7 小时仅启动时 1 次）。
- **SKIPFLUSH=2370**——系统持续空闲。

## 6. 问题清单

### 无活跃问题——系统处于最优运行状态

- 0/16 >500ms，12/16 <50ms（75%）。
- P50=14ms，Max=192ms（近会话最低），lockWait=0.3ms（可忽略）。
- 全表扫描总和 467ms——系列正常运行新低。
- 连续 24 轮 0 新增慢查询。
- 30.7 小时无 WARN/ERROR。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 20.48%，增速 ~280/min。
- 距 25% 阈值约 ~26 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 系统空闲时无影响。

## 8. 下一轮重点

1. 全表扫描总和是否在早晨 5-6 点维持 <500ms。
2. lockWait 是否再次出现（判断是否为偶发）。
3. tombstone 增速趋势。

## 9. 结论

**凌晨最优——0/16 >500ms，Max=192ms（近会话最低），P50=14ms，极佳比例 12/16。** 全表扫描总和 467ms 创本系列正常运行新低（仅次于 R159 维护谷底的 416ms）。`中文测试`=146ms 创系列绝对新低。凌晨 4:30-5:00 是 macOS 最静默时段——page cache 完全可用。

**连续 24 轮 0 新增慢查询，30.7 小时无异常。** lockWait=0.3ms 首次出现（单次，可忽略——flush timer 检查）。tombstone 20.48%（1.31M），增速 280/min，距 25% 约 26 小时。系统性能处于理论极限——page cache 零竞争+trigram 完全热化+SoA 线性区域完全驻留。
