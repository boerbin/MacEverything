# MacEverything 性能分析报告 R_2605291614

**时间**: 2026-05-29 16:14  
**索引规模**: 5,466,962 总记录 / 5,353,196 活跃记录  
**Tombstone**: 113,766 (2.08%)  
**日志**: 317 行, INFO=316, WARN=1, ERROR=0  
**轮次**: 第 59 轮（重启后第 7 轮）  

---

## 🟢 完全恢复 — R58 锁事件确认为瞬态

**R58 的 3 个超时 + 巨额锁等待全部消失。本轮零锁等待，所有查询（除 .pdf）回到良好区间：`test` 6061→61ms, `ext:cpp` 8205→57ms, `abc def` 7544→137ms。Flush 检查节奏恢复正常（无 13 分钟间隙）。Tombstone 增速降至 ~9K/h。R57→R58→R59 完成一个完整的"文件系统爆发 → 压缩锁爆发 → 恢复"周期。**

---

## 1. 本轮查询耗时

| 查询 | totalMs | trigramMs | phase2Ms | lockMs | searchPath |
|------|---------|-----------|---------|--------|------------|
| `"x"` | 23 | 0 | 0 | 0 | short-query-cache |
| `".pdf"` | **1295** | 0 | **1294** | 0 | advanced-trigram |
| `"main.cpp"` | 38 | 28 | 10 | 0 | advanced-trigram |
| `"*.swift"` | 215 | 3 | 211 | 0 | advanced-trigram |
| `"ext:cpp"` | 57 | 0 | 56 | 0 | advanced-ext-index |
| `"size:>100m"` | 75 | 0 | 74 | 0 | advanced-pure-filter-soa-gcd |
| `"hello world"` | 75 | 0 | 74 | 0 | advanced-trigram |
| `"abc def ghi..."` | 137 | 0 | 133 | 0 | advanced-trigram |
| `"README"` | 20 | 5 | 14 | 0 | advanced-trigram |
| `"test"` | 61 | 28 | 32 | 0 | advanced-trigram |
| `"MacEverything"` | 26 | 20 | 5 | 0 | advanced-trigram |
| `"dm:today"` | 84 | 0 | 84 | 0 | advanced-pure-filter-soa-gcd |
| `"content:import"` | 161 | 0 | 155 | 0 | advanced-pure-filter-soa-gcd |
| `"path:/Users"` | 438 | 0 | 430 | 0 | advanced-linear-gcd |
| `"中文测试"` | 216 | 0 | 216 | 0 | advanced-linear-gcd |
| `"very long query..."` | 25 | 1 | 23 | 0 | advanced-trigram |

### 与 R58 对比（锁事件 → 恢复）

| 查询 | R58 | R59 | 变化 | lockMs |
|------|-----|-----|------|--------|
| `x` | **TIMEOUT** | **23** | 恢复 | 0 |
| `.pdf` | **TIMEOUT** | **1295** | 恢复 | 0 |
| `main.cpp` | **TIMEOUT** | **38** | 恢复 | 0 |
| `ext:cpp` | 8205 | **57** | **-99.3%** | 0 |
| `abc def` | 7544 | **137** | **-98.2%** | 0 |
| `test` | 6061 | **61** | **-99.0%** | 0 |
| `README` | 1862 | **20** | **-98.9%** | 0 |
| `*.swift` | 3766 | **215** | **-94.3%** | 0 |
| `dm:today` | 1187 | **84** | **-92.9%** | 0 |

**所有 lockMs 归零**。R58 命中锁窗口的查询（位置 1-12）全部回到良好水平——确认 R58 是瞬态压缩锁事件，而非持续性退化。

---

## 2. R58 锁事件确认为瞬态

### 证据 1: Flush 检查节奏恢复正常

| 时刻 | 间隔 | 状态 |
|------|------|------|
| 15:38:54 | — | R58 异常前最后一次 |
| 15:52:04 | **+790s (13.2min)** | **R58 压缩占用窗口** |
| 15:57:11 | +307s | 恢复 |
| 15:59:35 | +144s | 正常 |
| 16:03:27 → 16:14:41 | 71~303s | 正常波动 |

15:52 之后再无多分钟级间隙——持久化线程恢复正常周期检查。**R58 的 13.2 分钟静默是一次性压缩批处理窗口，已结束。**

### 证据 2: 零锁等待

R59 全部 16 条查询 lockMs=0。压缩不再持锁阻塞查询。

### 证据 3: Tombstone 增速三连降

| 区间 | 速率 | 说明 |
|------|------|------|
| R56→R57 | ~52K/h | 文件系统爆发峰值 |
| R57→R58 | ~23K/h | 消退中 |
| **R58→R59** | **~9K/h** | **接近正常基线（16K/h 以下）** |

文件系统活动爆发已基本结束，tombstone 增速回落到正常范围以下。

---

## 3. 完整周期复盘：R57 → R58 → R59

本轮标志着一个完整的因果周期闭合，三轮数据互为印证：

```
R57 (15:11)  外部文件系统爆发
             ├─ tombstone 增速 52K/h（96.9% 新记录为 tombstone）
             ├─ I/O 驱逐缓存：.pdf 6811ms, content 1765ms, 中文 736ms
             └─ 压缩首次获工作：lock=799ms（单查询）
                          ↓
R58 (15:50)  压缩锁爆发（tombstone 累积至 110K）
             ├─ 持久化线程 13.2min 静默
             ├─ 3 查询超时 + lock 高达 5713ms
             └─ 反证：content/中文 恢复 → 纯锁事件
                          ↓
R59 (16:14)  完全恢复
             ├─ 零锁等待，查询 -94~99%
             ├─ flush 节奏正常，tombstone 增速 9K/h
             └─ 压缩工作完成，系统回到稳态
```

**这个周期是 P0 工程论断的完整实证**：
1. **文件系统爆发不可控**（外部因素）→ 必然周期性产生 tombstone 累积
2. **tombstone 累积触发压缩**→ 当前压缩**持锁**→ 必然阻塞查询
3. **无查询超时保护**→ 阻塞表现为客户端超时（R58 重演 R43）

只要"压缩持锁"和"无查询超时"两个根因存在，**此周期必然重复**。R56 的 I/O 抖动、R57 的爆发、R58 的锁爆发都是同一结构性缺陷的不同触发强度。

---

## 4. .pdf 残留偏高分析

R59 唯一未完全恢复的是 `.pdf`（1295ms, phase2=1294）：

| 轮次 | .pdf phase2 | 状态 |
|------|-----------|------|
| R55（稳定） | 373 | 最佳 |
| R56 | 2215 | I/O 驱逐 |
| R57 | 6000 | 爆发峰值 |
| R58 | TIMEOUT | 锁阻塞 |
| **R59** | **1294** | 恢复中（仍 3.5x R55） |

`.pdf` 候选集最大（trigram "pdf" 匹配极广），phase2 候选过滤始终最重，对缓存状态最敏感。R59 已无锁、无 I/O 爆发，但 .pdf 候选页面尚未完全回到文件系统缓存——预期下轮继续回落至 R55 的 ~400ms 水平。这不是锁问题，是高候选查询的缓存预热残留。

---

## 5. 索引状态

| 指标 | R58 | R59 | Δ |
|------|-----|-----|---|
| 总记录 | 5,462,721 | 5,466,962 | +4,241 |
| 活跃记录 | 5,352,609 | 5,353,196 | +587 |
| Tombstone | 110,112 | 113,766 | +3,654 |
| Tombstone 比例 | 2.02% | **2.08%** | +0.06pp |
| Flush | 1 | 1 | +0 |
| Skip | 67 | 69 | +2 |

Tombstone 2.08%，增速 9K/h——三轮持续放缓（52K→23K→9K/h）。系统回到低活跃稳态。

---

## 6. 优化建议（维持 R58 紧急升级）

| 优先级 | 项目 | 状态 |
|--------|------|------|
| **P0🔥** | 查询超时保护 | R58 重演 R43，最紧急 |
| **P0🔥** | 无锁快照 flush + 压缩 | 周期闭合证明两者必须同时无锁化 |
| **P0** | 压缩持锁限流（单批 ≤50ms + 查询让锁） | R58 新增，本轮验证必要性 |
| **P0** | 崩溃自动恢复 | 24.5h 停机教训 |
| **P1** | 压缩 lock hold duration 日志 | 确证压缩持锁时长 |
| P1 | CJK trigram/path 索引化 | 中 |

**周期闭合强化了 P0 论证**：R59 的恢复不是"问题解决"，而是"本次爆发耗尽"。结构性缺陷（压缩持锁 + 无超时）仍在，下次文件系统爆发将重复 R57→R58 周期。优化建议优先级不降。

---

## 7. 总结

| 维度 | 评分 | 趋势 |
|------|------|------|
| 查询 (trigram) | 8/10 | ↑↑↑ (.pdf 1295ms 仍偏高，其余优秀) |
| 查询 (线性/过滤) | 8/10 | ↑↑ (content 161ms, 中文 216ms 良好) |
| 延迟稳定性 | **10/10** | ↑↑↑ (零锁等待，从 3 超时恢复) |
| 索引健康度 | 8/10 | → (2.08%, 增速 9K/h 放缓) |
| 系统稳定性 | 7/10 | ↑↑ (本次爆发结束，但结构缺陷未除) |

**第 59 轮结论**: **🟢 完全恢复，R58 锁事件确认为瞬态**。R58 的 3 个超时 + 巨额锁等待（5713ms）全部消失：`test` 6061→61ms (-99%), `ext:cpp` 8205→57ms (-99.3%), `abc def` 7544→137ms (-98.2%)，**全部 16 条查询 lockMs=0**。三条证据确认压缩锁事件结束：① flush 检查节奏恢复正常（15:52 后无多分钟间隙）；② 零锁等待；③ tombstone 增速三连降（52K→23K→**9K/h**）。**R57→R58→R59 完成一个完整因果周期**：文件系统爆发 → tombstone 累积至 110K → 压缩持锁爆发（13.2min 线程静默 + 3 超时）→ 工作完成后恢复。这个周期是 P0 工程论断的完整实证——只要"压缩持锁 + 无查询超时"两个根因存在，下次文件系统爆发必然重复此周期。唯一残留偏高的 `.pdf`（1295ms, phase2=1294）是高候选查询的缓存预热残留、非锁问题，预期下轮回落。**优化优先级维持 R58 紧急升级：查询超时保护 + 无锁化（flush+压缩）+ 压缩持锁限流。**
