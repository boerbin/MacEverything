# MacEverything 性能分析报告 R_2606121448

## 1. 本轮范围

- 任务启动时间：2026-06-12 14:48
- 报告文件：`docs/performance_ana/R_2606121448.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=9244`
  - `SESSION_START_TS=2026-06-10 17:02:22.835`
  - `TOTAL_LINES=10520`
  - `SEG_LINES=1277`
  - `LAST_LOG_TS=2026-06-12 14:48`

## 2. 执行摘要

**本轮 full-scan 明显恢复，但 trigram 路径新增慢查询：`hello world` 达到 763.1ms。** 16-query battery 中 1/16 >500ms，Max=763.1ms，P50≈85.3ms，lockWait max=8.5ms。相比 R_2606121417，`content:import` 从 259.1ms 降到 107.2ms，`path:/Users` 从 258.0ms 降到 144.6ms，`中文测试` 从 227.8ms 降到 63.3ms，full-scan 总和从 744.9ms 降至 315.1ms；但 `.pdf`、`*.swift`、`ext:cpp`、`size:>100m` 与 `hello world` 同步回升，说明 postings/filter 热区仍受后台 I/O 影响。

最新运行段日志显示：`QUERYADV_GE500=8`，较上一轮 +1，新增慢查询为本轮 `hello world` total=760ms。`FLUSH=336`（较上一轮 +12），`SKIPFLUSH=0`。最新 flush 为 rewrite=2901ms、total=2952ms，较上一轮 4664ms 回落但仍是秒级完整重写；latest session 内仍保留 13:02 的 38 分钟级灾难 flush：rewrite=2,285,476ms、total=2,285,536ms。tombstone=529,062（9.11%），已经突破 9%。

| 指标 | R_2606121417 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **1/16** | **+1** |
| Max | 259.1ms | **763.1ms** | ↑195% |
| P50 | ≈31.2ms | **≈85.3ms** | ↑173% |
| lockWait max | 0ms | **8.5ms** | ↑8.5ms |
| 极佳比例(<50ms) | 12/16 | **6/16** | -6 |
| 全表扫描总和 | 744.9ms | **315.1ms** | ↓58% |
| recordCount | 5,791,935 | **5,805,261** | +13,326 |
| liveRecordCount | 5,274,808 | **5,276,199** | +1,391 |
| tombstone | 517,127 (8.93%) | **529,062 (9.11%)** | +11,935 |
| 最新段 FLUSH | 324 | **336** | +12 |
| 最新段 QUERYADV_GE500 | 7 | **8** | **+1** |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 10.6 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 313.9 | 307.8 | advanced-trigram | 0.0 | 9848 | 正常（偏高） |
| `main.cpp` | 5 | 27.9 | 21.6 | advanced-trigram | 0.0 | 85 | 极佳 |
| `*.swift` | 5 | 230.3 | 205.8 | advanced-trigram | 8.5 | 9430 | 正常（偏高） |
| `ext:cpp` | 5 | 214.8 | 205.8 | advanced-ext-index | 0.0 | 3616 | 正常（偏高） |
| `size:>100m` | 5 | 216.8 | 202.4 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常（偏高） |
| `hello world` | 5 | 763.1 | 747.3 | advanced-trigram | 0.0 | 3930 | **超标** |
| `abc def ghi jkl mno pqr` | 0 | 136.8 | 136.4 | advanced-trigram | 0.0 | 14526 | 正常 |
| `README` | 5 | 20.7 | 16.7 | advanced-trigram | 0.0 | 17505 | 极佳 |
| `test` | 5 | 11.2 | 5.8 | advanced-trigram | 0.0 | 117816 | 极佳 |
| `MacEverything` | 5 | 12.0 | 0.8 | advanced-trigram | 0.0 | 6945 | 极佳 |
| `dm:today` | 5 | 12.7 | 12.2 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `content:import` | 5 | 107.2 | 94.7 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常（恢复） |
| `path:/Users` | 5 | 144.6 | 141.0 | advanced-linear-gcd | 0.0 | 0 | 正常（恢复） |
| `中文测试` | 0 | 63.3 | 61.7 | advanced-linear-gcd | 0.0 | 0 | 正常（恢复） |
| `verylongquery...` | 0 | 0.8 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈85.3ms，P90≈313.9ms，Max=763.1ms。1/16 查询 >500ms。lockWait max=8.5ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **6/16** | 0.8-27.9ms | short-query-cache、小候选 trigram 与 dm filter 健康 |
| 正常（50-500ms） | **9/16** | 63.3-313.9ms | full-scan 恢复，但 postings/filter 多路径偏高 |
| 超标（>500ms） | **1/16** | 763.1ms | `hello world` trigram 明确超标 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=5,805,261；
- liveRecordCount=5,276,199；
- tombstone=529,062（**9.11%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=9244
SESSION_START_TS=2026-06-10 17:02:22.835
TOTAL_LINES=10520
SEG_LINES=1277
QUERYADV=100
QUERYADV_GE500=8
FLUSH=336
SKIPFLUSH=0
COMPACTION=2
```

### 慢查询证据

最新 session 内慢查询数量从 7 增至 8，本轮新增 `hello world`：

```text
[2026-06-10 17:17:09.559] QueryAdvanced "path:/Users" total=525ms | path=advanced-linear-gcd
[2026-06-10 17:25:15.434] QueryAdvanced "lllllllllllllllllllllllllllllllllllllllllllll" total=1154ms | path=advanced-trigram
[2026-06-10 17:47:13.642] QueryAdvanced "中文测试" total=716ms | path=advanced-linear-gcd
[2026-06-10 23:44:25.566] QueryAdvanced "test" total=676ms | path=advanced-trigram
[2026-06-12 09:17:32.430] QueryAdvanced "中文测试" total=555ms | path=advanced-linear-gcd
[2026-06-12 13:53:09.945] QueryAdvanced ".pdf" total=1955ms | path=advanced-trigram
[2026-06-12 13:53:12.523] QueryAdvanced "path:/Users" total=590ms | path=advanced-linear-gcd
[2026-06-12 14:48:11.578] QueryAdvanced "hello world" total=760ms | path=advanced-trigram
```

### flush 极端长尾

最新 session 内 `FLUSH=336`，rewrite 范围 **1440ms - 2285476ms**，total 最大 **2285536ms**。

最新 flush：

```text
[2026-06-12 14:46:31.998] Flushed v6 flat index, lastEventId=349640520,
liveRecords=5276192 | timing: walSwap=0ms rewrite=2901ms sqcache=48ms total=2952ms
```

Top flush outliers：

```text
[2026-06-12 13:02:35.416] rewrite=2285476ms sqcache=6ms total=2285536ms
[2026-06-12 03:15:02.833] rewrite=895940ms sqcache=2ms total=896053ms
[2026-06-10 23:24:50.749] rewrite=725091ms sqcache=18ms total=735223ms
[2026-06-11 14:30:37.596] rewrite=58273ms sqcache=5ms total=101726ms
[2026-06-10 21:16:27.157] rewrite=32497ms sqcache=5ms total=230084ms
```

## 6. 问题清单

### P0：trigram 路径新增前台慢查询

- 证据：`hello world`=763.1ms，日志 `QueryAdvanced "hello world" total=760ms`。
- 影响：交互式搜索出现明确用户可感知卡顿。
- 判断：同轮 `.pdf`=313.9ms、`*.swift`=230.3ms、`abc def...`=136.8ms，说明 postings/trigram 热区整体偏冷。

### P0：38 分钟级 flush outlier 仍是最新 session 最大风险

- 证据：`rewrite=2285476ms`、`total=2285536ms`。
- 影响：后台持久化长时间占用 I/O，极可能导致随后多轮 query 热区反复冷却。
- 根因：完整索引重写在 APFS COW、系统 I/O 压力、睡眠/唤醒或后台任务竞争下被极端放大。

### P0：flush 频率继续上升且没有退避

- 证据：最新 session `FLUSH=336`，较上一轮 +12；`SKIPFLUSH=0`。
- 影响：高频完整重写持续污染 page cache，并扩大下一次慢查询风险。

### P0：tombstone 已突破 9%

- 证据：tombstone=529,062（9.11%），较上一轮增加 11,935。
- 影响：索引体积、flush rewrite 成本和扫描成本继续扩大。

### P1：full-scan 恢复但整体稳定性不足

- 证据：full-scan 总和 744.9ms → 315.1ms，但 latest session 中仍有多条历史 full-scan 慢查询。
- 影响：当前 full-scan 好转，但仍可能在后续 flush 后再次反弹。

## 7. 优化建议

### P0：flush 改为增量/COW 页写入

- 分离 stable segment 与 delta segment；常规 flush 只追加 delta。
- 后台低优先级合并，不阻塞前台查询。
- 验收：flush rewrite max <200ms，latest session 不再出现分钟级 outlier。

### P0：为 flush 增加 I/O 压力退避和前台保护

- flush 超过阈值后降低下一轮频率。
- 前台查询活跃时推迟大 flush。
- 持久化线程使用更低 QoS，减少对搜索热路径的干扰。

### P0：降低 flush 频率并合并 WAL 批次

- 引入最小 flush 间隔和最大合并窗口。
- 对高 churn 场景减少完整重写次数。

### P1：提前或分段 compaction

- 不等待全局 25% tombstone 阈值。
- 按 segment 或绝对 tombstone 数触发局部 compaction。
- 验收：tombstone 率从 9%+ 回落，并且常规 flush rewrite 时间下降。

### P2：保护搜索热数据 page cache

- 对 flush 大顺序写考虑 `F_NOCACHE` 或同类策略。
- 对搜索热区使用 `madvise` 预热/保持。

## 8. 下一轮重点

1. `hello world` 是否从 763ms 恢复到 <100ms。
2. `.pdf` 与 `*.swift` 是否继续偏高。
3. latest session 慢查询计数是否继续高于 8。
4. 最新 flush 是否维持 2-3 秒级，还是再次升至 4 秒以上。
5. tombstone 是否继续高于 9.2%。

## 9. 结论

**本轮 full-scan 表现大幅恢复，但 trigram 路径新增慢查询：`hello world`=763.1ms，latest session 慢查询数增至 8。** 这说明后台 I/O/page-cache 干扰已经从 full-scan 波动扩展到 postings/trigram 热区。

**后台结构性风险继续恶化：FLUSH=336、SKIPFLUSH=0、tombstone=529,062（9.11%），latest session 仍包含 total=2,285,536ms 的 flush outlier。** 当前最优先的修复仍是 COW/增量 flush、I/O 退避、降低 flush 频率、提前或分段 compaction。
