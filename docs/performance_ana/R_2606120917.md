# MacEverything 性能分析报告 R_2606120917

## 1. 本轮范围

- 任务启动时间：2026-06-12 09:17
- 报告文件：`docs/performance_ana/R_2606120917.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=9244`
  - `SESSION_START_TS=2026-06-10 17:02:22.835`
  - `TOTAL_LINES=10014`
  - `SEG_LINES=771`
  - `LAST_LOG_TS=2026-06-12 09:17`

## 2. 执行摘要

**`中文测试` 再次突破 500ms，前台慢查询重新出现；后台 flush/tombstone 风险继续累积。** 本轮 16-query battery 中 1/16 >500ms，Max=556.5ms，P50≈28.3ms，lockWait=0。极佳 12/16，trigram 与 short-query-cache 仍整体健康，但 full-scan 路径显著恶化：`content:import`=225.5ms、`path:/Users`=231.4ms、`中文测试`=556.5ms，全表扫描总和升至 1013.4ms，较上一轮 687.6ms 上升 47%。

最新运行段日志显示：`QUERYADV_GE500=5`，较上一轮 +1，新增慢查询正是本轮 `中文测试` total=555ms。`FLUSH=238`（较上一轮 +8），`SKIPFLUSH=0`。最新 flush 为 rewrite=2352ms、total=2361ms，较上一轮 2990ms 回落，但仍是秒级完整重写。历史灾难 outlier 仍存在：rewrite 最大 895,940ms，total 最大 896,053ms。tombstone=439,472（7.70%），继续上升。

| 指标 | R_2606120857 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **1/16** | **+1** |
| Max | 311.8ms | **556.5ms** | ↑78% |
| P50 | 23.7ms | **28.3ms** | ↑19% |
| lockWait max | 0ms | **0ms** | 持平 |
| 极佳比例(<50ms) | 12/16 | **12/16** | 持平 |
| 全表扫描总和 | 687.6ms | **1013.4ms** | ↑47% |
| recordCount | 5,698,719 | **5,705,804** | +7,085 |
| liveRecordCount | 5,265,425 | **5,266,332** | +907 |
| tombstone | 433,294 (7.60%) | **439,472 (7.70%)** | +6,178 |
| 最新段 FLUSH | 230 | **238** | +8 |
| 最新段 QUERYADV_GE500 | 4 | **5** | **+1** |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 9.7 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 40.7 | 30.3 | advanced-trigram | 0.0 | 9824 | 极佳 |
| `main.cpp` | 5 | 4.1 | 1.5 | advanced-trigram | 0.0 | 85 | 极佳 |
| `*.swift` | 5 | 23.2 | 22.5 | advanced-trigram | 0.0 | 9419 | 极佳 |
| `ext:cpp` | 5 | 8.0 | 7.9 | advanced-ext-index | 0.0 | 3616 | 极佳 |
| `size:>100m` | 5 | 38.1 | 37.9 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 28.2 | 26.6 | advanced-trigram | 0.0 | 3930 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 31.2 | 31.1 | advanced-trigram | 0.0 | 14488 | 极佳 |
| `README` | 5 | 9.1 | 6.0 | advanced-trigram | 0.0 | 17397 | 极佳 |
| `test` | 5 | 28.3 | 18.5 | advanced-trigram | 0.0 | 117791 | 极佳 |
| `MacEverything` | 5 | 6.5 | 1.2 | advanced-trigram | 0.0 | 6944 | 极佳 |
| `dm:today` | 5 | 103.7 | 99.4 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 225.5 | 208.5 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `path:/Users` | 5 | 231.4 | 221.0 | advanced-linear-gcd | 0.0 | 0 | 正常 |
| `中文测试` | 0 | 556.5 | 555.3 | advanced-linear-gcd | 0.0 | 0 | **超标** |
| `verylongquery...` | 0 | 1.1 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈28.3ms，P90≈231.4ms，Max=556.5ms。1/16 查询 >500ms。lockWait=0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **12/16** | 1.1-40.7ms | short-query-cache、主要 trigram、ext-index 与 size filter 仍健康 |
| 正常（50-500ms） | **3/16** | 103.7-231.4ms | `dm:today`、`content:import`、`path:/Users` 偏高 |
| 超标（>500ms） | **1/16** | 556.5ms | `中文测试` full-scan 再次超标 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=5,705,804；
- liveRecordCount=5,266,332；
- tombstone=439,472（**7.70%**）。

## 5. 最新日志段分析

```text
SESSION_START_LINE=9244
SESSION_START_TS=2026-06-10 17:02:22.835
TOTAL_LINES=10014
SEG_LINES=771
QUERYADV=63
QUERYADV_GE500=5
FLUSH=238
SKIPFLUSH=0
COMPACTION=2
```

### 慢查询证据

最新 session 内慢查询数量从 4 增至 5，本轮新增 `中文测试`：

```text
[2026-06-10 17:17:09.559] QueryAdvanced "path:/Users" total=525ms | path=advanced-linear-gcd
[2026-06-10 17:25:15.434] QueryAdvanced "lllllllllllllllllllllllllllllllllllllllllllll" total=1154ms | path=advanced-trigram
[2026-06-10 17:47:13.642] QueryAdvanced "中文测试" total=716ms | path=advanced-linear-gcd
[2026-06-10 23:44:25.566] QueryAdvanced "test" total=676ms | path=advanced-trigram
[2026-06-12 09:17:32.430] QueryAdvanced "中文测试" total=555ms | path=advanced-linear-gcd
```

### flush 极端长尾

最新 session 内 `FLUSH=238`，rewrite 范围 **1440ms - 895940ms**，total 最大 **896053ms**。

最新 flush：

```text
[2026-06-12 09:16:58.605] Flushed v6 flat index, lastEventId=347008412,
liveRecords=5266321 | timing: walSwap=0ms rewrite=2352ms sqcache=6ms total=2361ms
```

Top flush outliers：

```text
[2026-06-12 03:15:02.833] rewrite=895940ms sqcache=2ms total=896053ms
[2026-06-10 23:24:50.749] rewrite=725091ms sqcache=18ms total=735223ms
[2026-06-11 14:30:37.596] rewrite=58273ms sqcache=5ms total=101726ms
[2026-06-10 21:16:27.157] rewrite=32497ms sqcache=5ms total=230084ms
[2026-06-10 23:26:19.499] rewrite=27557ms sqcache=10ms total=27834ms
```

## 6. 问题清单

### P0：full-scan 路径重新出现 >500ms 慢查询

- 证据：`中文测试`=556.5ms，日志 `QueryAdvanced "中文测试" total=555ms`。
- 影响：前台用户可感知卡顿重新出现。
- 判断：这是 R_2606120857 中 `中文测试`=311.8ms 后继续恶化，说明 SoA/path 数据页再次被 flush I/O 或系统内存压力淘汰。

### P0：flush 完整重写仍有分钟级灾难长尾

- 证据：rewrite 最大 895,940ms，total 最大 896,053ms。
- 最新 flush 为 2361ms，仍是秒级完整重写。
- 根因：完整索引重写在 APFS COW、系统 I/O 压力、睡眠/唤醒或后台任务竞争下被放大。

### P0：flush 频率继续上升且没有退避

- 证据：最新 session `FLUSH=238`，较上一轮 +8；`SKIPFLUSH=0`。
- 影响：高频完整重写持续污染 page cache，增加 full-scan 慢查询概率。

### P0：tombstone 继续增长

- 证据：tombstone=439,472（7.70%），较上一轮增加 6,178。
- 影响：索引体积、flush rewrite 成本和扫描成本继续扩大。

### P1：全表扫描总和突破 1 秒

- 证据：全表扫描总和 687.6ms → 1013.4ms。
- 影响：虽然只有一个查询超标，但 full-scan 热区整体已经明显降温。

## 7. 优化建议

### P0：flush 改为增量/COW 页写入

- 分离 stable segment 与 delta segment；常规 flush 只追加 delta。
- 后台低优先级合并，不阻塞前台查询。
- 验收：flush rewrite max <200ms，latest session 不再出现分钟级 outlier，`中文测试` 不再因 flush 后 page cache 冷却而超 500ms。

### P0：为 flush 增加 I/O 压力退避和前台保护

- flush 超过阈值后降低下一轮频率。
- 前台查询活跃时推迟大 flush。
- 持久化线程使用更低 QoS，减少对搜索热路径的干扰。

### P0：降低 flush 频率并合并 WAL 批次

- 引入最小 flush 间隔和最大合并窗口。
- 对高 churn 场景减少完整重写次数。

### P1：提前或分段 compaction

- 不等待全局 25% tombstone 阈值。
- 按 segment 或绝对 tombstone 数触发局部 compaction。

### P2：保护搜索热数据 page cache

- 对 flush 大顺序写考虑 `F_NOCACHE` 或同类策略。
- 对搜索热区使用 `madvise` 预热/保持。

## 8. 下一轮重点

1. `中文测试` 是否 1-round recovery 回落到 <300ms。
2. full-scan 总和是否从 1013ms 回落到 400-700ms 区间。
3. latest session 慢查询计数是否继续增加。
4. FLUSH 增长速度是否放缓（当前 238）。
5. tombstone 是否继续上升（当前 7.70%）。

## 9. 结论

**本轮前台出现明确回归：`中文测试`=556.5ms，latest session 慢查询数从 4 增至 5。** 其他路径仍健康：P50≈28.3ms，极佳 12/16，trigram 大多 <50ms。但 full-scan 总和升至 1013.4ms，说明 SoA/path 热区被明显冷却。

**后台持久化风险继续累积：FLUSH=238、SKIPFLUSH=0、tombstone=439,472（7.70%），最新 flush=2361ms，历史最大 rewrite 895,940ms。** 当前最重要的修复仍是 COW/增量 flush、I/O 退避、降低 flush 频率、提前或分段 compaction。
