# MacEverything 性能分析报告 R_2606081916

## 1. 本轮范围

- 任务启动时间：2026-06-08 19:16
- 报告文件：`docs/performance_ana/R_2606081916.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5282`
  - `SEG_LINES=4340`
  - `LAST_LOG_TS=2026-06-08 19:47`

## 2. 执行摘要

**`.pdf` 从 619ms 恢复至 471ms（刚好 <500ms，第五次确认 1 轮恢复），0/16 >500ms，Max=471ms，P50=166ms，lockWait=0，极佳比例 3/16。** 但整体比 R194 略差——P50 从 96ms 升至 166ms（↑73%），trigram 均值从 200ms 降至 160ms（↓20%，部分恢复），全表扫描总和 660→612ms（↓7%，持平）。`size:>100m`=240ms 和 `*.swift`=254ms 偏高——SoA/trigram 区域仍受轻度 page cache 压力。

tombstone 24.54%——增速 ~199/min（夜间水平维持），距 25% 约 3.9 小时（~23:10）。**注意：recordCount +17.9K，liveRecordCount +4.4K——晚间出现一波文件活动。**

### 关键指标对比

| 指标 | R194 | R195 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 1/16 | **0/16** | **↓1（.pdf 恢复）** |
| Max | 619ms | **471ms** | **↓ 24%** |
| P50 | 96ms | **166ms** | ↑ 73% |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 4/16 | **3/16** | ↓1 |
| tombstone | 1,658,821 (24.46%) | **1,672,281 (24.54%)** | ↑ |
| tombstone 增速 | ~214/min | **~199/min** | ↓ 7%（稳定） |
| recordCount | 6,789,819 | **6,807,720** | **+17.9K（偏高！）** |
| liveRecordCount | 5,130,998 | **5,135,439** | **+4,441** |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3916 | **3976** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 23 | **23** | 不变（本轮 0 新增） |

### `.pdf` 第五次确认 1 轮恢复

| 轮次 | .pdf (ms) | 恢复轮 | 恢复值 | 恢复率 |
|------|--------:|--------|------:|------:|
| R181=7193ms | 7193 | R182 | 180ms | 97% |
| R184=575ms | 575 | R185 | 134ms | 77% |
| R190=630ms | 630 | R191 | 324ms | 49% |
| **R194=619ms** | 619 | **R195** | **471ms** | **24%** |

- **619→471ms（↓24%）——恢复但幅度较小，仍在 >400ms 冷态。**
- **前三次恢复率 49-97%，本轮仅 24%——说明 postings 区域受到持续压力，未完全恢复。**
- **但已降至 <500ms——从"超标"回到"正常"。**

### Trigram 路径——部分恢复

| Query | R194 | R195 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 619ms | **471ms** | **↓24%** | 冷态（恢复中） |
| `hello world` | 373ms | **187ms** | **↓50%** | 正常 |
| `*.swift` | 138ms | **254ms** | **↑84%** | 冷态 |
| `main.cpp` | 70ms | **84ms** | ↑20% | 正常 |
| `abc def...` | 101ms | **155ms** | ↑53% | 正常 |
| `README` | 31ms | **43ms** | ↑39% | 极佳 |
| `test` | 255ms | **80ms** | **↓69%** | 正常 |
| `MacEverything` | 15ms | **10ms** | ↓33% | 极佳 |
| Trigram 均值 | 200ms | **160ms** | **↓20%** | 部分恢复 |

- **Trigram 均值 200→160ms（↓20%）——部分恢复但仍偏高。**
- **`hello world`=187ms（↓50%）和 `test`=80ms（↓69%）恢复良好。**
- **`*.swift`=254ms（↑84%）——R194 battery 预热了 `.pdf`/`test` 但未覆盖 `*.swift` 的 postings 区域。**
- **不均匀恢复——不同 postings 区域被交替预热和淘汰。**

### 全表扫描——稳定偏高

| Query | R194 | R195 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 177ms | **167ms** | ↓6% |
| `path:/Users` | 311ms | **225ms** | ↓28% |
| `中文测试` | 172ms | **221ms** | ↑28% |
| **总和** | **660ms** | **612ms** | ↓7%（持平） |

- **全表扫描总和 660→612ms（↓7%）——基本持平。**
- **`path:/Users`=225ms（↓28%）——继续从 R190=2043ms 恢复。**
- **`中文测试`=221ms（↑28%）——名称数组区域轻度被淘汰。**

### SoA Filter——`size:>100m` 偏高

| Query | R194 | R195 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 82ms | **240ms** | **↑193%** |
| `dm:today` | 47ms | **65ms** | ↑38% |

- **`size:>100m`=240ms（↑193%）——SoA 全扫描区域受显著 page cache 压力。**
- **与 `*.swift`=254ms 一起说明傍晚有一波 I/O 影响了多个数据区域。**

### tombstone 24.54%——晚间文件活动波动

- **tombstone=1,672,281（24.54%）。**
- **增速 ~199/min（R194=214/min，↓7%）——夜间水平维持。**
- **但 recordCount +17.9K（R194=+6.4K，↑180%）——晚间出现一波文件活动。**
- **liveRecordCount +4,441——有新文件创建（vs R194 的 +462）。**
- **距 25% 需要额外 30,919 个 tombstone。**
- **按 199/min 计算：~155 分钟 ≈ 2.6 小时（约 21:45）。**
- **但如果晚间文件活动持续，增速可能回升。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 19.7 | 0.0 | short-query-cache | 0 | 0 | 极佳（调度延迟） |
| `.pdf` | 5 | 470.7 | 454.6 | advanced-trigram | 0 | 9842 | 冷态（恢复中） |
| `main.cpp` | 5 | 83.8 | 66.8 | advanced-trigram | 0 | 82 | 正常 |
| `*.swift` | 5 | 253.7 | 232.9 | advanced-trigram | 0 | 9345 | 冷态 |
| `ext:cpp` | 5 | 125.6 | 123.5 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 239.7 | 238.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常偏高 |
| `hello world` | 5 | 186.9 | 173.7 | advanced-trigram | 0 | 3639 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 155.0 | 154.4 | advanced-trigram | 0 | 14327 | 正常 |
| `README` | 5 | 43.2 | 35.0 | advanced-trigram | 0 | 16725 | 极佳 |
| `test` | 5 | 79.8 | 72.1 | advanced-trigram | 0 | 116729 | 正常 |
| `MacEverything` | 5 | 9.6 | 1.5 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 64.9 | 64.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 166.9 | 153.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 225.4 | 218.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 220.5 | 218.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 166.9ms，P90 = 253.7ms，Max = 470.7ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R194 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 3/16 | 0.2-43ms | R194=4/16（↓1） |
| **正常**（50-500ms） | 13/16 | 65-471ms | R194=11/16（↑2） |
| **超标**（>500ms） | **0/16** | — | R194=1/16（**↓1**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,807,720，liveRecordCount=5,135,439（tombstone=**1,672,281**，**24.54%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=280  QUERYADV_GE500=23（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3976
```

- **QUERYADV_GE500 不变。**
- **SKIPFLUSH +60——正常节奏。**

## 6. 问题清单

### P0 紧急：tombstone 24.54%——距 25% 约 2.6 小时

- **距 25% 仅 0.46 个百分点（30,919 tombstone）。**
- **增速 199/min——约 2.6 小时（~21:45）触达。**
- **晚间 recordCount +17.9K 说明仍有文件活动——增速可能回升。**

### 观察项：晚间 I/O 波动

- **recordCount +17.9K 远高于前 2 轮（+5.9K/+6.4K）。**
- **可能是系统维护、应用更新、或用户文件操作。**
- **导致多个区域 page cache 压力——`*.swift`=254ms，`size:>100m`=240ms。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因

- **tombstone 24.54%，今晚极可能触发 compaction。**
- **紧迫度：小时级——最后几小时窗口。**

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. `.pdf` 是否从 471ms 继续恢复至 <300ms。
2. tombstone 增速是否因晚间文件活动回升。
3. 是否出现 compaction 相关日志。

## 9. 结论

**`.pdf` 从 619ms 恢复至 471ms（第五次确认 1 轮恢复），0/16 >500ms，Max=471ms，P50=166ms，lockWait=0。** 整体处于傍晚 page cache 波动状态——trigram 均值从 200ms 部分恢复至 160ms，`*.swift`=254ms 和 `size:>100m`=240ms 说明晚间 I/O 活动影响了多个数据区域。recordCount +17.9K 异常偏高——晚间出现一波文件活动。

**tombstone 24.54%——距 25% 仅 0.46 个百分点，约 2.6 小时触达（~21:45）。** 增速 199/min（夜间水平），但晚间文件活动可能推高增速。P0 compaction rewrite 修复进入最后几小时倒计时。
