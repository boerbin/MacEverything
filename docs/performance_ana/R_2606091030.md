# MacEverything 性能分析报告 R_2606091030

## 1. 本轮范围

- 任务启动时间：2026-06-09 10:30
- 报告文件：`docs/performance_ana/R_2606091030.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5616`
  - `SEG_LINES=4674`
  - `LAST_LOG_TS=2026-06-09 01:17`

## 2. 执行摘要

**系列最优轮——0/16 >500ms，Max=383ms，P50=26ms，lockWait=0，极佳比例 11/16。** 这是 200+ 轮系列中性能最好的一轮：所有指标全面恢复至最优水平，`hello world` 从 R199=3128ms → R200=1318ms → R201=38ms 完成 2 轮完全恢复。

| 指标 | R200 | R201 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 1/16 | **0/16** | **↓1（全清！）** |
| Max | 1318ms | **383ms** | **↓71%** |
| P50 | 48ms | **26ms** | **↓46%** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 8/16 | **11/16** | **↑3（系列最优！）** |
| tombstone | 1,734,304 (25.15%) | **1,741,672 (25.23%)** | ↑（继续增长） |
| tombstone 增速 | ~1336/min | **~245/min** | ↓82%（回落至正常日间水平） |
| recordCount | 6,895,065 | **6,902,530** | +7.5K |
| liveRecordCount | 5,160,761 | **5,160,858** | +97 |
| FLUSH 累计 | 1 | **1** | **不变！** |
| SKIPFLUSH | 4195 | **4255** | +60 |
| COMPACTION | 2 | **2** | **不变——tombstone 25.23% 仍未触发！** |
| QUERYADV_GE500 | 41 | **41** | 不变（本轮 0 新增） |

### 系列最优的证据

| 指标 | 此前系列最优 | R201 本轮 | 对比 |
|------|--------:|--------:|------|
| 极佳比例 | 8/16 (R200) | **11/16** | **新纪录** |
| P50 | 48ms (R200) | **26ms** | **新纪录** |
| Max（0/16 >500ms 轮中） | 400ms (R196) | **383ms** | **新纪录** |
| Trigram 均值 | 142ms (R196) | **18ms** | **新纪录（↓87%）** |
| 全表扫描总和 | 443ms (R200) | **798ms** | R200 更优 |

### `hello world` 完成 2 轮恢复

| 轮次 | hello world (ms) | 变化 |
|------|--------:|------|
| R199（灾难） | 3128ms | — |
| R200（恢复 1） | 1318ms | ↓58% |
| **R201（恢复 2）** | **38ms** | **↓97%** |

- **从 3128ms 到 38ms——2 轮完成 98.8% 恢复。**
- **`hello world` 的 3669 candidates postings 区域需要 2 次 battery 预热才能完全温态。**
- **这与高 candidates 查询的 postings 分散性一致——postings 分布在文件多处，需要多次读取才能全部进入 page cache。**

### 全面 Trigram 极佳——系列首次

| Query | R200 | R201 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 181ms | **26ms** | **↓86%** | 极佳 |
| `hello world` | 1318ms | **38ms** | **↓97%** | 极佳 |
| `*.swift` | 116ms | **16ms** | **↓86%** | 极佳 |
| `main.cpp` | 26ms | **3ms** | ↓88% | 极佳 |
| `abc def...` | 42ms | **33ms** | ↓21% | 极佳 |
| `README` | 11ms | **7ms** | ↓36% | 极佳 |
| `test` | 24ms | **13ms** | ↓46% | 极佳 |
| `MacEverything` | 11ms | **3ms** | ↓73% | 极佳 |
| Trigram 均值 | 216ms | **17ms** | **↓92%** | **全部极佳！** |

- **8/8 trigram 查询全部 <50ms——系列首次！**
- **Trigram 均值 216→17ms（↓92%）——系列绝对最优。**
- **证明 R200 battery 预热了 `hello world` 的残留冷区域，R201 时所有 trigram postings 完全在 page cache 中。**

### 全表扫描——正常偏高

| Query | R200 | R201 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 131ms | **151ms** | ↑15% |
| `path:/Users` | 134ms | **265ms** | ↑98% |
| `中文测试` | 178ms | **383ms** | ↑115% |
| **总和** | **443ms** | **798ms** | ↑80% |

- **全表扫描总和 443→798ms（↑80%）——R200 的极优恢复后出现轻度回退。**
- **`中文测试`=383ms（↑115%）——名称数组区域 page cache 被部分淘汰。**
- **`path:/Users`=265ms（↑98%）——路径数组区域同样受压。**
- **但仍远低于 R199=6155ms——属于正常日间波动范围。**
- **上午 IDE/Spotlight/用户操作占用大量 I/O，线性扫描区域受间歇性影响。**

### SoA Filter——稳定

| Query | R200 | R201 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 61ms | **26ms** | ↓57% |
| `dm:today` | 53ms | **37ms** | ↓30% |

- **两项均 <50ms——SoA 区域完全在 page cache 中。**

### tombstone 25.23%——持续增长，compaction 仍未触发

- **tombstone=1,741,672（25.23%）。**
- **增速 ~245/min（从 R200 的 ~1336/min 回落——上午脉冲已过）。**
- **距 R200 新增 7,368 tombstone（~30 分钟）。**
- **COMPACTION 仍为 2——确认 R200 的发现：compaction 触发机制存在缺陷。**

### 连续第 2 轮确认 compaction 触发机制缺陷

| 轮次 | tombstone% | COMPACTION | FLUSH | SKIPFLUSH |
|------|----------:|----------:|------:|----------:|
| R199 | 24.93% | 2 | 1 | 4136 |
| R200 | 25.15% | 2 | 1 | 4195 |
| **R201** | **25.23%** | **2** | **1** | **4255** |

- **tombstone 从 24.93% → 25.15% → 25.23%——连续 2 轮超过 25% 阈值。**
- **COMPACTION 始终为 2，FLUSH 始终为 1。**
- **SKIPFLUSH 持续增长（4136 → 4195 → 4255）——flush timer 不断检查但始终跳过。**
- **结论确认：compaction 检查绑定在 flush 执行路径上，skipflush 时不检查。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 2.9 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 26.2 | 24.0 | advanced-trigram | 0 | 9842 | 极佳 |
| `main.cpp` | 5 | 2.8 | 1.9 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 15.6 | 15.1 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 5.8 | 5.5 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 26.0 | 25.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 38.1 | 37.6 | advanced-trigram | 0 | 3669 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 32.6 | 32.5 | advanced-trigram | 0 | 14344 | 极佳 |
| `README` | 5 | 7.2 | 6.0 | advanced-trigram | 0 | 17006 | 极佳 |
| `test` | 5 | 13.3 | 11.6 | advanced-trigram | 0 | 116884 | 极佳 |
| `MacEverything` | 5 | 2.6 | 0.6 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 36.6 | 34.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 151.2 | 140.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 264.5 | 258.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 382.6 | 382.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 26.0ms，P90 = 264.5ms，Max = 382.6ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R200 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **11/16** | 0.2-38ms | R200=8/16（**↑3，系列最优！**） |
| **正常**（50-500ms） | 5/16 | 61-383ms | R200=7/16（↓2） |
| **超标**（>500ms） | **0/16** | — | R200=1/16（**↓1**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,902,530，liveRecordCount=5,160,858（tombstone=**1,741,672**，**25.23%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=329  QUERYADV_GE500=41（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4255
```

- **QUERYADV_GE500 不变——本轮 0 新增（全部 <500ms）。**
- **FLUSH 仍为 1——3 天无新 flush。**
- **COMPACTION 仍为 2——tombstone 25.23% 仍未触发。**

## 6. 问题清单

### P0 确认：compaction 触发机制缺陷——连续第 2 轮验证

- **tombstone 25.23%（超阈值 0.23%），COMPACTION 仍为 2。**
- **FLUSH=1（3 天），SKIPFLUSH=4255——flush 因 entries < 100 持续跳过。**
- **compaction 检查绑定在 flush 执行路径上——skipflush 时不检查 tombstone 比例。**
- **tombstone 将持续增长——内存浪费 + 扫描效率下降。**
- **修复方案：在 skipflush 路径中也执行 tombstone 阈值检查，或独立 timer。**

### 观察项：全表扫描日间波动

- `中文测试`=383ms，`path:/Users`=265ms——上午 I/O 压力导致线性扫描区域轻度受压。
- 但远低于灾难级别——属正常日间波动。

## 7. 优化建议

### P0（确认）：修复 compaction 触发机制

- **连续 2 轮 tombstone >25% 但 compaction 未触发。**
- **修复方案：skipflush 路径中增加 tombstone 阈值检查。**

### P0.5：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. 全表扫描是否恢复至 <200ms 水平。
2. tombstone 增长趋势和 COMPACTION 计数。
3. 是否出现新的 FLUSH 事件。

## 9. 结论

**系列最优轮——0/16 >500ms，Max=383ms，P50=26ms，极佳比例 11/16（系列最优）。** `hello world` 完成 2 轮恢复（3128→1318→38ms，↓98.8%），8/8 trigram 全部 <50ms（系列首次），trigram 均值 17ms（系列最优）。冷启动灾难完全消散。

**⚠️ compaction 触发缺陷连续第 2 轮确认——tombstone 25.23%（超阈值 0.23%），COMPACTION 仍为 2。** FLUSH=1（3 天），SKIPFLUSH=4255。compaction 检查绑定在 flush 执行路径上，skipflush 时不检查 tombstone 比例。这是当前最紧急的 P0 问题——tombstone 将持续增长直到下次 flush 实际执行或进程重启。
