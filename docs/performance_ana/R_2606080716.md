# MacEverything 性能分析报告 R_2606080716

## 1. 本轮范围

- 任务启动时间：2026-06-08 07:16
- 报告文件：`docs/performance_ana/R_2606080716.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=3795`
  - `SEG_LINES=2853`
  - `LAST_LOG_TS=2026-06-08 07:17`

## 2. 执行摘要

**零和翻转——`dm:today`回归极佳+`中文测试`升至371ms——0/16 >500ms，Max=371ms，P50=14ms，lockWait=0，极佳比例 13/16（81%↑）。** `dm:today`从 R171 的 88ms 骤降至 35.7ms（SoA 时间戳区域回归 page cache），同时 `中文测试` 从 271ms 升至 371ms——经典零和 LRU 翻转。净效果积极：极佳比例从 12/16 升至 13/16。

### 关键指标对比

| 指标 | R171（30 min 前） | R172 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 271ms | **371ms** | ↑ 零和翻转（`中文测试`） |
| P50 | 12ms | **14ms** | ↑ 微量（正常波动） |
| lockWait max | 0.6ms | **0ms** | 恢复完美 |
| 极佳比例(<50ms) | 12/16 | **13/16** | **↑1（`dm:today` 回归极佳）** |
| tombstone | 1,346,387 (20.89%) | **1,354,085 (20.98%)** | ↑ 正常 |
| tombstone 增速 | ~273/min | **~257/min** | ↓ 回落 |
| recordCount | 6,445,437 | **6,453,240** | +7.8K |
| liveRecordCount | 5,099,050 | **5,099,155** | +105 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 2610 | **2670** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 29 轮 0 新增） |

### 零和翻转——`dm:today` ↔ `中文测试`

| Query | R171 | R172 | 变化 | 说明 |
|-------|-----:|-----:|------|------|
| `dm:today` | 88ms | **36ms** | **↓ 60%** | SoA 时间戳区域回归 page cache |
| `中文测试` | 271ms | **371ms** | **↑ 37%** | linear-gcd 区域被 LRU 淘汰 |

- **经典零和 LRU 翻转**：SoA 时间戳 filter 区域（dm:today）与 linear-gcd 扫描区域（中文测试）争夺相同 page cache 空间。
- **R171 时**：`dm:today` 区域被竞争（88ms），`中文测试` 区域驻留（271ms）。
- **R172 时**：`dm:today` 区域回归（36ms），`中文测试` 区域被挤出（371ms）。
- **净效果积极**：`dm:today` 进入极佳范围（<50ms），整体极佳比例 ↑1。
- **371ms 仍远低于 500ms 阈值——无问题。**

### 全表扫描——日间稳态

| Query | R171 | R172 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 244ms | **168ms** | **↓ 31%** |
| `path:/Users` | 252ms | **242ms** | 持平 |
| `中文测试` | 271ms | **371ms** | ↑ 37%（零和翻转） |
| **总和** | **767ms** | **781ms** | **↑ 2%（持平）** |

- **全表扫描总和 781ms——与 R171（767ms）基本持平。**
- **`content:import`=168ms（R171=244ms，↓31%）——与 `dm:today` 改善同步（SoA 区域部分共享）。**
- **`中文测试`=371ms（R171=271ms，↑37%）——零和翻转的代价。**
- **总和稳定——page cache 总量不变，仅内部分配翻转。**

### 日间全表扫描趋势

| 时段 | 轮次 | 总和 | 说明 |
|------|------|-----:|------|
| 04:47 | R167 | 467ms | 凌晨极静默 |
| 05:46 | R169 | 509ms | 早晨前 |
| 06:16 | R170 | 693ms | 早晨开始 |
| 06:46 | R171 | 767ms | 日间活跃 |
| **07:16** | **R172** | **781ms** | **日间稳态** |

- **全表扫描总和 767→781ms——趋于稳定，日间稳态区间确认为 750-800ms。**
- **从凌晨 467ms 到日间 781ms——增幅 67%，完全由 page cache 竞争驱动。**

### Trigram 路径——正常稳态

| Query | R171 | R172 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 17ms | **13ms** | ↓ 24% |
| `*.swift` | 11ms | **19ms** | ↑ 波动 |
| `hello world` | 12ms | **15ms** | 持平 |
| `abc def...` | 26ms | **30ms** | 持平 |
| `README` | 8ms | **7ms** | 持平 |
| `test` | 14ms | **14ms** | 持平 |
| `MacEverything` | 2ms | **2ms** | 持平 |
| Trigram 均值 | 12.1ms | **14.4ms** | ↑ 19%（正常稳态波动） |

- **Trigram 均值 14.4ms——从 R171 的 12.1ms 微升，完全处于正常稳态范围（12-16ms）。**
- **`*.swift`=19ms（R171=11ms，↑）——9.3K candidates 的 postings 页面 LRU 正常抖动。**
- **`.pdf`=13ms（R171=17ms，↓24%）——postings 区域内部 LRU 重排。**
- **结论**：trigram 路径整体稳定——个别项波动但均值处于正常日间水平。

### SoA Filter——`dm:today` 大幅改善

| Query | R171 | R172 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 30ms | **32ms** | 持平 |
| `dm:today` | 88ms | **36ms** | **↓ 60%（回归极佳）** |

- **`dm:today`=36ms——从 R171 的 88ms 大幅回落至极佳范围。**
- **原因**：SoA 时间戳 filter 区域的 page cache 页面被日间活动短暂挤出后，后续访问重新预热。**
- **两项总和**：R171=118ms → R172=68ms——**↓ 42%**。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 4.8 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 13.2 | 11.7 | advanced-trigram | 0 | 9812 | 极佳 |
| `main.cpp` | 5 | 3.9 | 1.6 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 19.2 | 17.8 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 4.3 | 3.8 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 31.9 | 31.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 14.8 | 14.2 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 29.9 | 29.4 | advanced-trigram | 0 | 14216 | 极佳 |
| `README` | 5 | 6.8 | 5.9 | advanced-trigram | 0 | 16631 | 极佳 |
| `test` | 5 | 14.3 | 12.5 | advanced-trigram | 0 | 116513 | 极佳 |
| `MacEverything` | 5 | 2.4 | 0.7 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 35.7 | 35.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 167.7 | 152.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 242.4 | 236.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 371.2 | 370.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.3 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 14.3ms，P90 = 242.4ms，Max = 371.2ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R171 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 13/16 | 0.3-36ms | R171=12/16（**↑1**） |
| **正常**（50-500ms） | 3/16 | 168-371ms | R171=4/16（↓1） |
| **超标**（>500ms） | 0/16 | — | R171=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,453,240，liveRecordCount=5,099,155（tombstone=**1,354,085**，**20.98%**）；
- 进程正常运行（当前会话，运行约 33.1 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=122  QUERYADV_GE500=4（不变——连续 29 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=2670
```

- **连续 29 轮 0 个新增 >500ms 查询（R144-R172）。**
- **FLUSH=1**——无新 flush（33.1 小时仅启动时 1 次）。
- **SKIPFLUSH=2670**——系统持续空闲。

## 6. 问题清单

### 无活跃问题

- 0/16 >500ms，13/16 <50ms（81%——新高）。
- P50=14ms（日间稳态），lockWait=0。
- 连续 29 轮 0 新增慢查询。
- 33.1 小时无 WARN/ERROR。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 20.98%，增速 ~257/min（回落）。
- 距 25% 阈值约 ~24 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 系统空闲时无影响。

## 8. 下一轮重点

1. 零和翻转是否持续（`dm:today` vs `中文测试`）或回摆。
2. 日间全表扫描总和是否稳定在 750-800ms。
3. tombstone 增速趋势（20.98%，接近 21%）。

## 9. 结论

**零和翻转净效果积极——0/16 >500ms，Max=371ms，P50=14ms，lockWait=0，极佳比例 13/16（81%新高）。** `dm:today` 从 88ms 骤降至 36ms（SoA 时间戳区域回归 page cache），代价是 `中文测试` 从 271ms 升至 371ms——经典 LRU 零和翻转。全表扫描总和 781ms（↑2%）趋于稳定——日间稳态区间确认为 750-800ms。

**连续 29 轮 0 新增慢查询，33.1 小时无异常。** Trigram 均值 14.4ms 处于正常日间稳态。tombstone 20.98%（1.35M），增速 257/min（回落），距 25% 约 24 小时。系统持续稳定运行——零和翻转是 page cache 正常的 LRU 替换行为，不影响用户体验（所有路径均 <500ms）。
