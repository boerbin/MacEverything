# MacEverything 性能分析报告 R_2606041740

## 1. 本轮范围

- 任务启动时间：2026-06-04 17:40
- 报告文件：`docs/performance_ana/R_2606041740.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=22416`
  - `SESSION_START_TS=2026-06-04 17:21:30.784`
  - `TOTAL_LINES=22469`
  - `SEG_LINES=54`
  - `LAST_LOG_TS=2026-06-04 17:42`

## 2. 执行摘要

**重启后第 2 轮——tombstone 已开始重新积累，`x` 路由异常持续，lockWait 异常飙升。** `x`=8,097ms（仍被路由到 advanced-linear-gcd 而非 short-query-cache）。**lockWait 异常飙升至 90.8ms**（`*.swift`）——重启后仅 20 分钟。**7/16 查询 >500ms**。P50 ≈ 387ms，Max=8,097ms。

**tombstone 已从 0.02% 增至 3.08%**（177,645 条，~53MB）——**重启后仅约 20 分钟即突破 3%**。

### 关键指标

| 指标 | R104（重启冷缓存） | R105 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 9/16 | **7/16** | 改善（部分预热） |
| Max | 4,573ms | **8,097ms** | ↑ `x` 路由异常 |
| P50（排除 `x`） | — | **283ms** | — |
| lockWait max | 818.9ms | **90.8ms** | ↓ 但仍异常高 |
| lockWait 受影响查询 | 1/16 | **13/16** | **↑↑ 重新广泛** |
| tombstone | 1,205 (0.02%) | **177,645 (3.08%)** | **↑↑ 快速重积** |

### tombstone 重新积累速度

| 时间点 | tombstone | 比率 | 说明 |
|--------|--------:|-----:|------|
| 17:21（重启） | 1,205 | 0.02% | WAL replay 清零 |
| **17:40（~20 分钟后）** | **177,645** | **3.08%** | **快速重积** |

- **20 分钟内 +176,440 条**——约 8,822 条/分钟。
- 对比前会话稳态：~12,000 条/30 分钟（400 条/分钟）。
- **当前积累速度是正常稳态的 22 倍**——可能原因：
  1. 全盘扫描后的 FSWatcher 追赶积压事件。
  2. 系统后台活动（Spotlight、Time Machine）仍在持续。
  3. 前会话事件风暴的根源（大规模系统进程）仍在运行。

### 横向对比（排除 `x` 路由异常后）

| Query | R96（历史最佳） | R103（崩溃前） | R105 本轮 | 说明 |
|-------|-----:|-----:|--------:|------|
| `.pdf` | 68 | 1725 | **284** | 恢复良好 |
| `*.swift` | 58 | 366 | **387** | 偏高+lockWait |
| `hello world` | 118 | 629 | **250** | 恢复良好 |
| `path:/Users` | 83 | 1833 | **903** | **仍 >500ms** |
| `中文测试` | 50 | 444 | **455** | 偏高 |
| `content:import` | 106 | 283 | **1141** | **异常偏高** |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 8097.3 | 7882.5 | advanced-linear-gcd | 2.6 | 0 | **路由异常持续** |
| `.pdf` | 5 | 283.6 | 28.4 | advanced-trigram | 14.4 | 10037 | 正常+lockWait |
| `main.cpp` | 5 | 1036.0 | 8.5 | advanced-trigram | 9.1 | 83 | **>500ms GCD** |
| `*.swift` | 5 | 386.9 | 27.3 | advanced-trigram | **90.8** | 9326 | **lockWait 最高** |
| `ext:cpp` | 5 | 136.6 | 13.0 | advanced-ext-index | **65.9** | 3658 | **lockWait 显著** |
| `size:>100m` | 5 | 72.5 | 54.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 250.3 | 26.2 | advanced-trigram | 9.6 | 3619 | 正常+lockWait |
| `abc def ghi jkl mno pqr` | 0 | 216.8 | 195.1 | advanced-trigram | 10.5 | 15992 | lockWait |
| `README` | 5 | 426.9 | 17.6 | advanced-trigram | 3.5 | 17404 | 偏高 |
| `test` | 5 | 935.4 | 121.3 | advanced-trigram | 2.5 | 130561 | **>500ms** |
| `MacEverything` | 5 | 2144.6 | 46.4 | advanced-trigram | 6.6 | 6794 | **>500ms GCD** |
| `dm:today` | 5 | 116.3 | 91.9 | advanced-pure-filter-soa-gcd | 2.0 | 0 | 正常 |
| `content:import` | 5 | 1140.5 | 1087.1 | advanced-pure-filter-soa-gcd | 19.2 | 0 | **>500ms I/O** |
| `path:/Users` | 5 | 903.0 | 855.3 | advanced-linear-gcd | 0 | 0 | **>500ms I/O** |
| `中文测试` | 0 | 454.7 | 425.6 | advanced-linear-gcd | 0 | 0 | 偏高 |
| `verylongquery...` | 0 | 12.0 | 0.0 | advanced-trigram | 11.9 | 0 | lockWait |

**P50 ≈ 387ms，P90 ≈ 1,141ms，Max = 8,097ms。7/16 查询 >500ms。lockWaitMs 出现在 13/16 查询中，最高 90.8ms。**

### `x`=8,097ms 路由异常分析（第 2 轮）

- searchPath=advanced-linear-gcd（应为 short-query-cache）。
- phase2=7,883ms——全表扫描 5.93M 记录。
- **R104 也出现此问题**（4,573ms）——连续 2 轮 `x` 未命中短查询缓存。
- **原因**：重启后短查询缓存从磁盘加载，但全盘扫描新增了大量记录，缓存被标记为过期。缓存重建需要等待下一次 `x` 查询后的缓存更新周期。
- **预计**：当缓存更新完成后 `x` 将恢复正常。

### lockWait 90.8ms 分析

- `*.swift`：queryTimeMs=387ms，phase2=27ms，lockWait=**90.8ms**——lockWait 占 23%。
- `ext:cpp`：queryTimeMs=137ms，phase2=13ms，lockWait=**65.9ms**——lockWait 占 48%。
- **lockWait 在 tombstone 仅 3.08% 时即达到 90.8ms——超过 R102（60.8ms，tombstone 37.65%）**。
- **原因**：重启后 FSWatcher 正在追赶全盘扫描期间积压的文件系统事件——事件密度极高，导致频繁锁获取。
- **与 R100-R103 的区别**：R100-R103 的 lockWait 由持续事件风暴驱动；本轮由 FSWatcher 追赶积压驱动——属于重启后的暂态行为，预计后续轮次将降低。

### 非 phase2 开销异常

| 查询 | queryTimeMs | phase2Ms | 差值 | 占比 |
|------|----------:|--------:|-----:|-----:|
| `main.cpp` | 1,036ms | 9ms | **1,027ms** | 99% |
| `MacEverything` | 2,145ms | 46ms | **2,099ms** | 98% |
| `test` | 935ms | 121ms | **814ms** | 87% |

- 三个查询的 phase2 均正常（9-121ms），但 queryTimeMs 极高——**差值来自 GCD dispatch 延迟**。
- 与 R104 冷缓存模式一致——GCD 线程池在重启后尚未完全预热。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,931,878，liveRecordCount=5,754,233（tombstone=**177,645**，**3.08%**）；
- 进程正常运行。

## 5. 最新日志段分析（新会话，起始行 22416）

```text
WARN=0  ERROR=0
QUERYADV=23  QUERYADV_GE500=11
FLUSH=3  COMPACTION=2  SKIPFLUSH=0
DIAG_TRIGRAM_SKIP=0
```

- QUERYADV=23（R104=11 +12 本轮）。
- QUERYADV_GE500=11（R104=5 +6 本轮——冷缓存+GCD 延迟所致）。
- **FLUSH=3**——**新会话已触发 3 次 flush**（前会话 43 轮仅 1 次）。
- **COMPACTION=2**——启动时触发了 2 次 compaction。
- SKIPFLUSH=0——**无跳过 flush**。

### FLUSH=3 + SKIPFLUSH=0 分析

- **前会话**：FLUSH=1, SKIPFLUSH=667——阈值 100 近乎失效。
- **新会话**：FLUSH=3, SKIPFLUSH=0——阈值机制正常工作。
- **原因**：新会话启动后 FSWatcher 追赶事件以更大批次到达（全盘扫描触发大量事件），单次 pending changes 超过 100 → 正常触发 flush。
- **但 FLUSH=3 仅清理了约 300 条**——对 177,645 条 tombstone 仍然杯水车薪。

## 6. 问题清单

### Critical：tombstone 快速重新积累——3.08%（177,645 条，20 分钟）

- **重启后仅 20 分钟即从 0.02% 增至 3.08%——积累速度是正常稳态的 22 倍。**
- **按当前速度预测**：1 小时 ~530K（~8.5%），2 小时 ~1.06M（~16%），4 小时可能回到 R100 水平。
- **原因**：FSWatcher 追赶积压 + 系统后台活动持续。
- **紧迫性**：如果不实现 compaction，前会话 43 轮的恶化周期将在数小时内重演。

### Critical：lockWait 90.8ms——重启后仍异常

- tombstone 仅 3.08% 时 lockWait 已达 90.8ms——**超过 R102 的 60.8ms（tombstone 37.65%）**。
- 原因：FSWatcher 追赶积压，事件密度极高。
- **预计**：追赶完成后 lockWait 将降低——但需要监控。

### Critical：`x` 路由异常持续（第 2 轮）

- `x` 连续 2 轮被路由到 advanced-linear-gcd 而非 short-query-cache。
- phase2=7,883ms——8 秒响应时间对用户体验影响极大。
- 短查询缓存可能需要手动刷新或等待自动更新周期。

### High：GCD dispatch 延迟仍在恢复

- `main.cpp`、`MacEverything`、`test` 的 queryTimeMs-phase2Ms 差值达 800-2,100ms。
- GCD 线程池重启后预热不完全——预计后续轮次改善。

### 正面：FLUSH 机制在新会话中正常工作

- FLUSH=3, SKIPFLUSH=0——前会话的 FLUSH=1/SKIPFLUSH=667 异常未重现。
- 但 flush 频率仍不足以对抗 8,822 条/分钟的 tombstone 积累速度。

## 7. 优化建议

### P0 紧急：实现运行时 tombstone compaction

- **20 分钟 0%→3%，数小时内将回到危机水平。** 崩溃清零只是暂时缓解。
- **方案**：tombstoneRatio > 2% 时触发后台 compaction。
- **紧迫性**：按当前 22 倍速度，留给修复的时间窗口仅数小时。

### P0 紧急：修复 `x` 短查询缓存路由异常

- 连续 2 轮 `x` 被路由到 linear-gcd 全表扫描（8 秒）。
- 短查询缓存启动后应立即对所有单字符查询有效。

### P1：lockWait 升级防护
### P1：GCD 线程池预热
### P2：phase2 超时降级

## 8. 下一轮重点

1. `x` 路由是否恢复到 short-query-cache。
2. lockWait 是否从 90.8ms 开始下降（FSWatcher 追赶是否完成）。
3. tombstone 积累速度是否减缓（从 22 倍稳态回归正常）。
4. GCD dispatch 延迟是否消退。

## 9. 结论

**重启后第 2 轮——tombstone 快速重积至 3.08%，lockWait 异常飙升至 90.8ms。** `x`=8,097ms（路由异常持续，被路由到 linear-gcd 全表扫描）。`main.cpp`=1,036ms，`MacEverything`=2,145ms，`content:import`=1,141ms，`test`=935ms，`path:/Users`=903ms——7/16 >500ms。P50=387ms。**lockWait 90.8ms 在 tombstone 仅 3.08% 时出现**——超过 R102 的 60.8ms（tombstone 37.65%）——原因是 FSWatcher 追赶重启期间积压的文件系统事件。13/16 查询有 lockWait。**tombstone 20 分钟内从 0.02% 增至 3.08%（+176K，8,822 条/分钟——正常稳态 22 倍）**——按此速度数小时内将回到崩溃前水平。FLUSH=3（新会话 flush 机制正常工作，前会话 43 轮仅 1 次），但 3 次 flush 对 177K tombstone 杯水车薪。**运行时 compaction 的修复窗口仅数小时**——否则系统将重演 R60-R103 的 43 轮恶化→崩溃周期。
