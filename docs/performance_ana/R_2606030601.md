# MacEverything 性能分析报告 R_2606030601

## 1. 本轮范围

- 任务启动时间：2026-06-03 06:01
- 报告文件：`docs/performance_ana/R_2606030601.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=10631`
  - `SESSION_START_TS=2026-05-31 00:09:38.774`
  - `TOTAL_LINES=21026`
  - `SEG_LINES=10396`
  - `LAST_LOG_TS=2026-06-01 16:57:52.124`

## 2. 执行摘要

状态端点 connection refused。进程不存在。日志无新增（21026 行不变）。

服务已持续不可用约 37.1 小时（自 2026-06-01 16:57 COW compaction done 之后）。

## 3. Query Battery 结果

所有 16 个 query 均 connection refused（首个失败后 early-exit）。状态端点 connection refused。

## 4. 服务可用性状态

- `localhost:19860` 无 HTTP 服务；
- MacEverything 应用进程不存在；
- 应用日志无新记录。

连续可用性失败已跨越四十五轮。

## 5. 最新日志段分析

```text
WARN=0 ERROR=0
QUERYADV=3906  QUERYADV_GE500=120
FLUSH=1877  FLUSH_TOTAL_GE5=1877  FLUSH_TOTAL_GE10=1877
COMPACTION=4  RECLAIM=0  SKIPFLUSH=29
```

与前轮完全一致。Compaction 生命周期仍是最后可见事件（16:37 触发 → 16:57 done）。

## 6. 问题清单

### Critical：服务持续不可用约 37.1 小时

- 证据：所有 probe 全部 connection refused，进程不存在。

### Critical：compaction done 后没有服务恢复证据

### High：日志无法解释进程退出

### High：full compaction 与在线服务隔离不足

### High：v6 flat index flush 极端 outlier（最慢 2064533ms）

## 7. 优化建议

### P0：优先恢复服务并补齐生命周期观测

### P0：compaction 完成后必须保持或恢复 HTTP 服务

### P0：将 full compaction 改为可中断、可限速、可恢复

### P1：单独治理 flush total outlier

### P1：前移 tombstone debt 治理

## 8. 下一轮重点

1. 先恢复或重启 MacEverything。
2. 检查 macOS crash report 定位进程退出原因。
3. 服务恢复后运行 query battery 比较 compaction 后性能。

## 9. 结论

本轮 06:01 复核显示 MacEverything HTTP 服务仍持续不可用约 37.1 小时。所有 probe 均 connection refused，进程不存在，日志无新增。最高优先级仍是恢复服务并补齐生命周期可观测性。
