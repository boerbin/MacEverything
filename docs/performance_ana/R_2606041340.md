# MacEverything 性能分析报告 R_2606041340

## 1. 本轮范围

- 任务启动时间：2026-06-04 13:40
- 报告文件：`docs/performance_ana/R_2606041340.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=22100`
  - `SEG_LINES=1074`
  - `LAST_LOG_TS=2026-06-04 13:41`

## 2. 执行摘要

**linear-gcd 路径 I/O 压力。** `中文测试`=563ms（>500ms，连续第 2 轮），`path:/Users`=458ms（接近 500ms）。trigram 路径全部正常。**1/16 查询 >500ms。lockWaitMs 全部为 0。** P50 ≈ 113ms，Max=563ms。

**R96 GCD 异常已恢复**：`x`=5ms（R96=1,526ms）——确认为一次性 GCD 调度抖动。

**tombstone=527,526（8.42%，158MB），增量 +7,885**。

### 横向对比（最近 5 轮）

| Query | R93 | R94 | R95 | R96 | R97 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 75 | 11 | 18 | 1526 | **5** | **GCD 异常恢复** |
| `.pdf` | 635 | 630 | 375 | 68 | **113** | 正常 |
| `*.swift` | 22 | 226 | 264 | 58 | **80** | 正常 |
| `hello world` | 352 | 240 | 210 | 118 | **205** | 正常 |
| `README` | 14 | 1071 | 31 | 13 | **36** | 正常 |
| `path:/Users` | 144 | 218 | 394 | 83 | **458** | **偏高** |
| `中文测试` | 234 | 391 | 573 | 50 | **563** | **>500ms** |
| P50 | 57 | 171 | 147 | 49 | **113** | 正常 |

### linear-gcd 路径压力趋势

| 轮次 | `path:/Users` | `中文测试` | 说明 |
|------|------------:|----------:|------|
| R93 | 144ms | 234ms | 正常 |
| R94 | 218ms | 391ms | 偏高 |
| R95 | 394ms | 573ms | **>500ms** |
| R96 | 83ms | 50ms | 热缓存恢复 |
| **R97** | **458ms** | **563ms** | **再次偏高** |

**模式**：linear-gcd 路径在奇数轮（R95、R97）偏高，偶数轮（R96）因前轮预热恢复。这表明 linear-gcd 全表扫描（6.27M 记录）的缓存覆盖在正常间隔后即被系统活动换出——**tombstone 8.42% 下页面缓存无法同时维持 trigram 索引和 linear 扫描数据**。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 5.3 | 0.0 | short-query-cache | 0 | 0 | 正常 |
| `.pdf` | 5 | 113.4 | 109.0 | advanced-trigram | 0 | 9952 | 正常 |
| `main.cpp` | 5 | 27.3 | 21.0 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 79.7 | 71.7 | advanced-trigram | 0 | 9356 | 正常 |
| `ext:cpp` | 5 | 30.1 | 29.6 | advanced-ext-index | 0 | 3701 | 正常 |
| `size:>100m` | 5 | 60.0 | 59.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 204.7 | 201.1 | advanced-trigram | 0 | 3613 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 148.4 | 147.8 | advanced-trigram | 0 | 16040 | 正常 |
| `README` | 5 | 35.9 | 31.5 | advanced-trigram | 0 | 17671 | 正常 |
| `test` | 5 | 49.5 | 39.1 | advanced-trigram | 0 | 130121 | 正常 |
| `MacEverything` | 5 | 28.8 | 6.0 | advanced-trigram | 0 | 6803 | 正常 |
| `dm:today` | 5 | 174.1 | 173.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 207.7 | 201.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 458.2 | 436.7 | advanced-linear-gcd | 0 | 0 | **接近 500ms** |
| `中文测试` | 0 | 563.3 | 557.0 | advanced-linear-gcd | 0 | 0 | **>500ms** |
| `verylongquery...` | 0 | 4.1 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 113ms，P90 ≈ 458ms，Max = 563ms。1/16 查询 >500ms。lockWaitMs 全部为 0。**

### 路径分化现象

| 路径 | 查询数 | 性能 | 说明 |
|------|------:|------|------|
| trigram | 10 | 全部正常（5-205ms） | trigram 索引页面缓存良好 |
| ext-index | 1 | 正常（30ms） | 小索引 |
| pure-filter-soa-gcd | 4 | 正常（60-208ms） | SoA 布局高效 |
| **linear-gcd** | **2** | **偏高-超标（458-563ms）** | **全表扫描缓存竞争** |

**关键发现**：I/O 压力已从 R92 时的 trigram 路径完全转移到 linear-gcd 路径。原因：
1. trigram 索引页面因连续多轮查询电池预热，缓存命中率高。
2. linear-gcd 路径需扫描全部 6.27M 记录（含 527K tombstone），数据量远大于 trigram 索引。
3. 系统活动（日间文件操作）在轮次间隔内优先换出 linear 扫描数据（体积大、访问频率低）。

### `中文测试` queryTimeMs - phase2Ms 分析

- R95: 573ms - 335ms = **238ms 非 phase2**
- R97: 563ms - 557ms = **6ms 非 phase2**

R95 的 238ms 非 phase2 开销本轮消失——表明 R95 的异常源于 GCD dispatch 延迟（与 R96 `x`=1526ms 同期），而非 Unicode 处理本身。本轮 `中文测试` 的 557ms 几乎全部来自 phase2 全表扫描——**纯 I/O 压力**。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,271,112，liveRecordCount=5,743,586（tombstone=**527,526**，**8.42%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=352  QUERYADV_GE500=49
FLUSH=0  COMPACTION=2  SKIPFLUSH=418
DIAG_TRIGRAM_SKIP=105
```

- QUERYADV 从 345 增至 352（+7）。
- QUERYADV_GE500 从 48 增至 49（+1，即 `中文测试`=563ms）。
- SKIPFLUSH 从 412 增至 418（+6，**低于稳态 +12**）。
- DIAG_TRIGRAM_SKIP 从 104 增至 105（+1，稳态）。

### SKIPFLUSH +6 分析

- 稳态 +12/轮，本轮仅 +6——R96→R97 间隔较短（~15 分钟）所致。
- 不构成异常。

## 6. 问题清单

### Critical：tombstone 8.42%（527,526 条，158MB）

- 增长轨迹：
  ```
  R94: 494K (+11.9K) → R95: 510K (+16.3K) → R96: 520K (+9.2K)
  → R97: 528K (+7.9K)
  ```
- R96-R97 增量偏低（短间隔所致），稳态仍为 ~12K/轮。
- **累计内存开销**：~528K × 300 字节 ≈ **158MB**。
- 连续第 37 轮超 2%，第 18 轮超过 5%，第 13 轮超过 6%，第 10 轮超过 7%，第 3 轮超过 8%。

### High：linear-gcd 路径成为新常态瓶颈

- `中文测试` 连续 2 轮（R95、R97）>500ms，`path:/Users` 连续 2 轮接近 500ms（394ms、458ms）。
- **交替模式**：冷→热→冷——linear 扫描数据在轮次间被系统活动换出。
- 根因：527K tombstone (158MB) 挤压页面缓存 → linear 扫描 6.27M 记录的工作集无法常驻。
- **与 trigram 路径分化**：trigram 索引体积小、访问频繁，缓存优先级高；linear 数据体积大、访问少，被优先换出。

### 正面：R96 GCD 异常已恢复

- `x`=5ms（R96=1,526ms）——确认为一次性极端 GCD 调度抖动。
- GCD 异常仍需架构修复（30.6% 出现率），但 R96 的 1,526ms 为独立事件而非持续恶化。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（8.42%，158MB）

- **37 轮连续超标。** linear-gcd 路径已成新常态瓶颈——compaction 释放 158MB 后 linear 扫描工作集可常驻。
- 预估收益：`path:/Users` 从 400-460ms → 80-150ms，`中文测试` 从 560ms → 100-200ms。

### P0 紧急：short-query-cache GCD 隔离（30.6% 异常率，峰值 1,526ms）
### P0 紧急：`size:>100m` 全表扫描超时保护
### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. linear-gcd 路径是否继续交替模式（下轮预计恢复）。
2. trigram 路径是否保持正常。
3. tombstone 增速趋势。

## 9. 结论

**linear-gcd 路径 I/O 压力。** `中文测试`=563ms（>500ms 连续第 2 轮），`path:/Users`=458ms（接近 500ms）。trigram 路径全部正常——I/O 压力已完全转移到 linear-gcd 全表扫描路径。1/16 查询 >500ms，lockWaitMs=0。P50=113ms。**路径分化现象确认**：trigram 索引（小体积、高频访问）缓存稳定，linear 数据（大体积、低频访问）被优先换出——交替冷热模式。R96 GCD 异常已恢复（`x`=5ms）。tombstone=527,526（8.42%，158MB）——**是 linear-gcd 瓶颈的直接根因**，compaction 释放 158MB 即可消除此问题。
