# MacEverything 性能分析报告 R_2606101600

## 1. 本轮范围

- 任务启动时间：2026-06-10 16:00
- 报告文件：`docs/performance_ana/R_2606101600.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9057`
  - `SEG_LINES=8115`
  - `LAST_LOG_TS=2026-06-10 14:17`

## 2. 执行摘要

**Post-compaction 稳定化——trigram 极速，`x` 缓存仍未重建。** 0/16 >500ms，Max=297ms，P50=20.7ms，lockWait=0（compaction 完成后恢复）。**极佳 12/16。** Trigram phase2 均值 ~2.7ms（compaction 后索引紧凑——极致性能）。`x` 仍走 advanced-linear-gcd（297ms——缓存尚未重建）。**FLUSH 从 3→9（+6！）——compaction 后大量新记录涌入触发连续 flush。** tombstone=11,334（0.22%——健康水平）。

| 指标 | R257 | R258 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 1/16 | **0/16** | ↓1 |
| Max | 587ms | **297ms** | ↓49% |
| P50 | 35ms | **20.7ms** | ↓41% |
| lockWait max | 325.7ms | **0ms** | **恢复为 0！** |
| 极佳比例(<50ms) | 10/16 | **12/16** | ↑2 |
| tombstone | 1,971 (0.04%) | **11,334 (0.22%)** | ↑（正常增长） |
| recordCount | 5,241,736 | **5,252,092** | +10.4K |
| liveRecordCount | 5,239,765 | **5,240,758** | +993 |
| FLUSH 累计 | 3 | **9** | **+6！** |
| SKIPFLUSH | 7266 | **7266** | +0（flush 占用了所有 tick） |
| COMPACTION | 4 | **4** | 不变（tombstone 0.22%<25%） |
| QUERYADV_GE500 | 75 | **75** | 不变 |

### FLUSH +6 分析

- **FLUSH 从 3→9（+6 次 flush！）——57 轮中仅 1 次 flush，现在单轮 6 次！**
- **原因：compaction 后 WAL 从零开始积累。大量新记录涌入（recordCount +10.4K）使 WAL entries 频繁达到 100 阈值。**
- **SKIPFLUSH +0——说明每个 30s tick 都触发了 flush（没有跳过）。**
- **这证实了 compaction 后文件系统活动仍然活跃——新记录持续涌入。**
- **COMPACTION=4 不变——因为 tombstone 0.22% 远低于 25% 阈值。**

### `x` 查询——缓存仍未重建

- **R257：`x` → advanced-linear-gcd → 587ms（首次缓存失效后的全表扫描）。**
- **R258：`x` → advanced-linear-gcd → 297ms（仍未恢复为 short-query-cache）。**
- **297ms vs 587ms——page cache 预热使全表扫描加速 49%，但仍走 linear-gcd 路径。**
- **short-query-cache 可能需要特定条件才能重建（如累计查询次数或定时器）。**
- **如果 R259 仍然走 linear-gcd，说明 cache 重建机制可能有延迟。**

### Trigram 路径——compaction 后极致性能

| Query | R257 | R258 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 6ms | **4ms** | ↓33% | 极佳 |
| `hello world` | 4ms | **15ms** | ↑275%（overhead） | 极佳 |
| `*.swift` | 48ms* | **3ms** | ↓94%（无lockWait） | 极佳 |
| `main.cpp` | 337ms* | **12ms** | ↓96%（无lockWait） | 极佳 |
| `abc def...` | 2ms | **19ms** | ↑850%（overhead） | 极佳 |
| `README` | 3ms | **27ms** | ↑800%（overhead） | 极佳 |
| `test` | 17ms | **21ms** | ↑24% | 极佳 |
| `MacEverything` | 5ms | **25ms** | ↑400%（overhead） | 极佳 |
| Trigram phase2均值 | ~2.6ms* | **~2.7ms** | **持平——极致！** |

*R257 标注 * 的去除 lockWait 后的 phase2 值。

- **Trigram phase2 均值 ~2.7ms——compaction 后索引紧凑效应持续。**
- **queryTime 高于 phase2 的部分是系统 overhead（锁获取、结果序列化等）。**
- **全部 8/8 trigram 极佳——连续 2 轮（R257 去 lockWait 后也是）。**

### 全表扫描——正常

| Query | R257 | R258 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 183ms | **158ms** | ↓14% |
| `path:/Users` | 285ms | **244ms** | ↓14% |
| `中文测试` | 238ms | **225ms** | ↓5% |
| **总和** | **706ms** | **627ms** | **↓11%** |

- **全表扫描 706→627ms（↓11%）——缓慢恢复中。**
- **compaction 后记录数少了 30%（5.2M vs 7.5M），全表扫描应该更快。**
- **当前 627ms 仍偏高——page cache 预热尚未完全完成。**
- **预计 1-2 轮后回归 200-300ms 区间。**

### 辅助路径

| Query | R257 | R258 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 587ms | **297ms** | ↓49%（仍走 linear-gcd） | 正常 |
| `ext:cpp` | 2ms | **1ms** | ↓50% | 极佳 |
| `size:>100m` | 35ms | **21ms** | ↓40% | 极佳 |
| `dm:today` | 72ms | **101ms** | ↑40% | 正常 |
| `verylongquery...` | 0.1ms | **0.2ms** | ↑100% | 极佳 |

- **`ext:cpp`=1ms（极致——compaction 后 ext-index 更紧凑）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 297.3 | 293.0 | advanced-linear-gcd | 0 | 0 | 正常（缓存未重建） |
| `.pdf` | 5 | 4.1 | 2.1 | advanced-trigram | 0 | 9783 | 极佳 |
| `main.cpp` | 5 | 12.1 | 0.1 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 2.9 | 0.9 | advanced-trigram | 0 | 9401 | 极佳 |
| `ext:cpp` | 5 | 0.9 | 0.4 | advanced-ext-index | 0 | 3612 | 极佳 |
| `size:>100m` | 5 | 20.6 | 20.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 15.4 | 3.2 | advanced-trigram | 0 | 3929 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 19.1 | 5.6 | advanced-trigram | 0 | 14437 | 极佳 |
| `README` | 5 | 26.8 | 0.7 | advanced-trigram | 0 | 17428 | 极佳 |
| `test` | 5 | 20.7 | 8.1 | advanced-trigram | 0 | 117720 | 极佳 |
| `MacEverything` | 5 | 25.1 | 0.8 | advanced-trigram | 0 | 6921 | 极佳 |
| `dm:today` | 5 | 101.3 | 60.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 158.4 | 143.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 244.0 | 220.3 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 224.8 | 223.3 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 20.7ms，P90 = 224.8ms，Max = 297.3ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R257 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **12/16** | 0.2-27ms | R257=10/16（↑2） |
| **正常**（50-500ms） | 4/16 | 101-297ms | R257=5/16（↓1） |
| **超标**（>500ms） | **0/16** | — | R257=1/16（↓1） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,252,092，liveRecordCount=5,240,758（tombstone=**11,334**，**0.22%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=663  QUERYADV_GE500=75（不变）
FLUSH=9（+6！）  COMPACTION=4  SKIPFLUSH=7266
```

## 6. 问题清单

### P0：compaction 触发机制缺陷（根因未修复）

- **虽然 R257 自然触发，但机制缺陷仍存在。**
- **当前 tombstone 0.22%——短期无风险，但未来可能再次陷入 57 轮不触发的循环。**

### P0：flush 写锁阻塞

- **R257 lockWait=325.7ms（系列记录）确认了 P0 优先级。**
- **R258 lockWait=0——因为 compaction 已完成。但问题在于下次 flush/compaction 时必然复现。**

### 观察：`x` 查询缓存重建延迟

- **连续 2 轮（R257-R258）`x` 走 linear-gcd 而非 short-query-cache。**
- **可能是 cache 重建需要特定触发条件。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（增加独立定时检查）

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

### 新增观察项：short-query-cache 重建机制

- **compaction 后缓存失效→linear-gcd 回退——是否有自动重建？延迟多久？**

## 8. 下一轮重点

1. `x` 是否恢复为 short-query-cache（第 3 轮观察）。
2. 全表扫描是否继续恢复（627ms→预期 200-300ms）。
3. FLUSH 频率是否持续（+6 是否为暂态）。

## 9. 结论

**Post-compaction 稳定化——trigram 极致，全表扫描恢复中。** 0/16 >500ms，Max=297ms，P50=21ms，lockWait=0（恢复！）。极佳 12/16。Trigram phase2 均值 ~2.7ms（compaction 后索引紧凑——极致性能）。`x` 仍走 linear-gcd（297ms——缓存第 2 轮未重建）。全表扫描 627ms（↓11%——缓慢恢复）。

**FLUSH +6（3→9）——compaction 后 WAL 重新活跃，大量新记录涌入触发连续 flush。** tombstone=11,334（0.22%——健康）。COMPACTION=4 不变（tombstone 远低于阈值）。
