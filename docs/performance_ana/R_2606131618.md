# MacEverything 性能分析报告 R_2606131618

## 1. 本轮范围

- 任务启动时间：2026-06-13 16:18
- 报告文件：`docs/performance_ana/R_2606131618.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=12287`
  - `SESSION_START_TS=2026-06-13 12:03:04.944`
  - `TOTAL_LINES=12646`
  - `SEG_LINES=360`
  - `LAST_LOG_TS=2026-06-13 16:18`

## 2. 执行摘要

**本轮 16-query battery 中 0/16 >500ms，但 Max 从 322.4ms 升至 418.3ms，前台尾部再次接近 500ms 阈值。** 最高项为 `path:/Users`=418.3ms，其次是 `中文测试`=279.0ms、`content:import`=187.5ms、`dm:today`=65.6ms、`hello world`=57.0ms。P50≈24.6ms，P90≈187.5ms，所有 lockWait 均为 0ms。

相比 15:48，中位层基本稳定：P50 从约 24.8ms 到约 24.6ms；但尾部明显恶化，Max 上升约 30%，P90 从约 135.2ms 升至约 187.5ms，scan/filter 路径总和从 766.2ms 升至 984.1ms。后台结构性风险继续恶化：recordCount=6,244,350，liveRecordCount=5,312,636，tombstone=931,714（14.92%），较上一轮增加 6,411；最新 session `FLUSH=50`，较上一轮 +6，`SKIPFLUSH=0`，session 最大 flush total 仍为 6164ms。

| 指标 | R_2606131548 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 322.4ms | **418.3ms** | ↑30% |
| P50 | ≈24.8ms | **≈24.6ms** | 基本持平 |
| P90 | ≈135.2ms | **≈187.5ms** | ↑39% |
| lockWait max | 0.0ms | **0.0ms** | 持平 |
| 极佳比例(<50ms) | 10/16 | **10/16** | 持平 |
| 扫描/过滤路径总和 | 766.2ms | **984.1ms** | ↑28% |
| recordCount | 6,236,436 | **6,244,350** | +7,914 |
| liveRecordCount | 5,312,133 | **5,312,636** | +503 |
| tombstone | 925,303 (14.84%) | **931,714 (14.92%)** | +6,411 |
| 最新 session FLUSH | 44 | **50** | +6 |
| 最新 session QUERYADV_GE500 | 0 | **0** | 持平 |
| 最新 flush total | 2741ms | **2942ms** | ↑7% |
| session 最大 flush total | 6164ms | **6164ms** | 持平 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 0.6 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 25.4 | 22.9 | advanced-trigram | 0.0 | 10158 | 极佳 |
| `main.cpp` | 5 | 5.5 | 4.5 | advanced-trigram | 0.0 | 103 | 极佳 |
| `*.swift` | 5 | 23.9 | 23.2 | advanced-trigram | 0.0 | 9622 | 极佳 |
| `ext:cpp` | 5 | 15.0 | 14.3 | advanced-ext-index | 0.0 | 3891 | 极佳 |
| `size:>100m` | 5 | 33.7 | 33.6 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 57.0 | 56.4 | advanced-trigram | 0.0 | 3932 | 正常，略高于 50ms |
| `abc def ghi jkl mno pqr` | 0 | 43.0 | 42.9 | advanced-trigram | 0.0 | 14622 | 极佳 |
| `README` | 5 | 6.2 | 5.2 | advanced-trigram | 0.0 | 17587 | 极佳 |
| `test` | 5 | 18.4 | 16.6 | advanced-trigram | 0.0 | 118022 | 极佳 |
| `MacEverything` | 5 | 4.5 | 2.1 | advanced-trigram | 0.0 | 7038 | 极佳 |
| `dm:today` | 5 | 65.6 | 65.4 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 187.5 | 175.1 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常高位，较上一轮上升 |
| `path:/Users` | 5 | 418.3 | 410.8 | advanced-linear-gcd | 0.0 | 0 | 正常高位，本轮最高，接近 500ms |
| `中文测试` | 0 | 279.0 | 278.5 | advanced-linear-gcd | 0.0 | 0 | 正常高位 |
| `verylongquery...` | 0 | 1.0 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈24.6ms，P90≈187.5ms，Max=418.3ms。0/16 查询 >500ms。lockWait max=0.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **10/16** | 0.6-43.0ms | short-query-cache、轻量 trigram、ext-index 与轻量 pure-filter 整体健康 |
| 正常（50-500ms） | **6/16** | 57.0-418.3ms | pure-filter、trigram 尾部与 linear-gcd 贡献主要延迟 |
| 超标（>500ms） | **0/16** | — | 本轮无正式超标查询，但 `path:/Users` 已重新接近阈值 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,244,350；
- liveRecordCount=5,312,636；
- tombstone=931,714（**14.92%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=12287
SESSION_START_TS=2026-06-13 12:03:04.944
TOTAL_LINES=12646
SEG_LINES=360
QUERYADV=36
QUERYADV_GE500=0
FLUSH=50
SKIPFLUSH=0
COMPACTION=2
LAST_LINE=[2026-06-13 16:18:04.783] [INFO ] [SearchEngine] QueryAdvanced "中文测试" total=278ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6244350
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 0，本轮没有日志级慢查询。`中文测试` 的日志 total=278ms，与 query battery 的 279.0ms 对齐；但本轮最高项是 `path:/Users`=418.3ms，说明 linear-gcd 尾部在路径与中文查询之间继续摆动，并再次接近 500ms。

```text
[2026-06-13 16:18:04.783] QueryAdvanced "中文测试" total=278ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6244350
```

### flush 情况

最新 session 内 `FLUSH=50`，rewrite 范围 **2470ms - 4809ms**，total 最大 **6164ms**。`SKIPFLUSH=0`，说明 session 内完整 flush 从上一轮的 44 次继续累积到 50 次，但没有任何可见 backoff。

最新 flush：

```text
[2026-06-13 16:13:24.110] Flushed v6 flat index, lastEventId=362233757,
liveRecords=5312611 | timing: walSwap=0ms rewrite=2939ms total=2942ms
```

Top flushes：

```text
[2026-06-13 12:08:25.921] rewrite=4809ms total=4812ms
[2026-06-13 13:48:25.498] rewrite=4395ms total=4396ms
[2026-06-13 12:38:27.299] rewrite=3809ms total=6164ms
[2026-06-13 15:33:24.838] rewrite=3691ms total=3694ms
[2026-06-13 13:43:24.572] rewrite=3471ms total=3472ms
```

## 6. 问题清单

### P0：前台尾部重新接近 500ms

- 证据：`path:/Users`=418.3ms，phase2=410.8ms；`中文测试`=279.0ms，phase2=278.5ms。
- 影响：虽然仍未超过 500ms，但路径查询已经进入明显用户可感知区间，下一轮或后台 I/O 状态变化时容易越线。
- 判断：lockWait=0ms，瓶颈主要在 phase2 的 linear-gcd 扫描阶段。

### P0：tombstone 已突破 930k 且继续上行

- 证据：tombstone=931,714（14.92%），较上一轮增加 6,411。
- 影响：无效记录比例继续升高，会放大扫描成本、索引重写成本与 compaction 压力。
- 判断：前台尾部抬升与 tombstone 继续增长同向出现，说明 scan/filter 路径仍受数据膨胀影响。

### P0：new session 内 flush 已累积到 50 次且无退避

- 证据：`FLUSH=50`，较上一轮 +6；`SKIPFLUSH=0`；session 最大 flush total=6164ms，最新 flush total=2942ms。
- 影响：完整重写持续制造 I/O 与 page-cache 干扰，容易让 linear-gcd、pure-filter、trigram 在不同采样点出现尾部抬升。
- 判断：flush 频率继续按固定节奏累积，未看到对 3-6s rewrite 的保护机制。

### P0：scan/filter 路径总和重新接近 1s

- 证据：扫描/过滤路径总和从 766.2ms 升至 984.1ms；`content:import`=187.5ms、`path:/Users`=418.3ms、`中文测试`=279.0ms。
- 影响：尾部不是单个查询孤立抖动，而是 pure-filter 与 linear-gcd 系列整体再次抬升。

### P1：trigram 多数健康但仍有 50ms+ 项

- 证据：`hello world`=57.0ms，`.pdf`=25.4ms，`abc def ghi jkl mno pqr`=43.0ms。
- 影响：trigram 当前不是主要瓶颈，但多词查询仍可能略高于 50ms，应继续拆分 postings 与候选处理成本。

## 7. 优化建议

### P0：立即提前或分段 compaction

- 以 tombstone 率 14%+ 或绝对 tombstone 900k+ 作为明确触发信号，不再等待更高阈值。
- 结合 segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 优先处理 tombstone 密集 segment，避免 scan/filter 路径继续扫描大量无效记录。
- 验收：tombstone 率从 14.92% 回落，`path:/Users`、`中文测试`、`content:import` 稳定 <200ms。

### P0：flush 改为增量/COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页或持锁窗口。
- 验收：常规 flush total <200ms，新 session 不再出现 3-6s 级完整 rewrite。

### P0：增加 flush I/O 压力退避和前台保护

- 当最近 flush 超过阈值时，提高下一次 flush 间隔、降低 batch 频率，或前台查询活跃时推迟大 flush。
- 持久化线程使用低 QoS，并评估大顺序写的 `F_NOCACHE`/同类策略，减少对搜索热页的冲刷。
- 验收：`SKIPFLUSH` 不再长期为 0，3-6s flush 后不再出现 400ms+ linear-gcd 尾部。

### P0：为 advanced-linear-gcd 与 pure-filter 增加轻量剪枝

- 对中文、路径、content/filter 类查询记录扫描记录数、tombstone 跳过数和 segment 分布。
- 评估轻量字符 n-gram、路径分段索引、预过滤位图或 segment-level skip list，以减少无结果或低选择性查询的全量扫描。
- 要求保持内存增长可控，优先使用压缩或按需结构，不引入大常驻索引。
- 验收：`path:/Users` 不再接近 500ms，`中文测试` 与 `content:import` 稳定低于 200ms。

### P1：细分 scan/filter 与 top-level timing

- 对 pure-filter 和 linear-gcd 记录扫描记录数、跳过 tombstone 数、命中 live 数、segment 数、metadata 加载耗时和结果组装耗时。
- 对所有路径增加 queueWait、phase2、candidate materialization、path/file metadata load、sort/rank、HTTP serialization/write 等阶段耗时。
- 验收：`path:/Users` 的 418.3ms 能归因到具体阶段，而不是只看到 phase2 大黑盒。

### P1：细分 trigram/postings 路径

- 对 `advanced-trigram` 记录 postings 读取、候选交并、候选评分、路径加载、结果组装、page-cache miss 近似指标。
- 重点解释 `hello world` 在 50ms+ 的成本来源。
- 验收：trigram 查询在 flush 后仍稳定 <50ms，或至少能解释每个阶段的成本。

## 8. 下一轮重点

1. `path:/Users` 是否正式超过 500ms。
2. tombstone 是否继续从 931,714 上行并接近 940k。
3. scan/filter 路径总和是否突破 1000ms。
4. `中文测试` 是否重新接近 400ms+。
5. `content:import` 是否继续上升到 200ms+。
6. 新 session 中 `QUERYADV_GE500` 是否开始累积。
7. 新 session flush 是否继续出现 4-6s outlier。
8. `SKIPFLUSH` 是否仍为 0。

## 9. 结论

**本轮没有 >500ms 查询，但 Max=418.3ms、P90≈187.5ms，前台尾部较 15:48 明显抬升。** `path:/Users` 是本轮最高项，耗时几乎全部来自 phase2 linear-gcd 扫描；`中文测试` 与 `content:import` 也维持中高位。

**后台根因继续恶化：tombstone=931,714（14.92%），新 session `FLUSH=50`，最大 flush total=6164ms，且 `SKIPFLUSH=0`。** 下一步仍应优先推进提前/分段 compaction、增量/COW flush、flush 前台保护/退避、advanced-linear-gcd/pure-filter 剪枝，以及 scan/filter、trigram 与 top-level timing 拆分。
