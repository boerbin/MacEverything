# MacEverything 性能分析报告 R_2606081246

## 1. 本轮范围

- 任务启动时间：2026-06-08 12:46
- 报告文件：`docs/performance_ana/R_2606081246.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=4473`
  - `SEG_LINES=3531`
  - `LAST_LOG_TS=2026-06-08 13:17`

## 2. 执行摘要

**灾难性事件 1 轮完全恢复——0/16 >500ms，Max=242ms，P50=35ms，lockWait=0，极佳比例 9/16（56%，系列最高！）。** 从 R181 的 `.pdf`=7193ms（系列最高）恢复至 65ms（↓99.1%），`hello world`=4166ms→76ms（↓98.2%）。所有 trigram 查询回到正常水平——证明 R181 的选择性淘汰是一次性事件，R181 battery 本身完成了受影响 postings 区域的预热。全表扫描总和 599ms（↓18% from R181=733ms，系列新低！）。tombstone 增速回落至 382/min——追赶基本完成。

### 关键指标对比

| 指标 | R181（选择性淘汰） | R182 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 5/16 | **0/16** | **↓5（完全恢复）** |
| Max | 7193ms | **242ms** | **↓ 97%** |
| P50 | 221ms | **35ms** | **↓ 84%** |
| lockWait max | 2.5ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 4/16 | **9/16** | **↑5（系列最高！）** |
| tombstone | 1,491,439 (22.62%) | **1,500,394 (22.73%)** | ↑ |
| tombstone 增速 | ~445/min | **~382/min** | **↓ 14%（追赶接近正常）** |
| recordCount | 6,599,451 | **6,608,547** | +9.1K（接近正常） |
| liveRecordCount | 5,108,012 | **5,108,153** | +141 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3208 | **3268** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 18 | **18** | 不变（本轮 0 新增） |

### Trigram 恢复——R181 battery 预热效果验证

| Query | R181（灾难） | R182 本轮 | 恢复率 | 正常值 |
|-------|-----:|-----:|------|------|
| `.pdf` | 7193ms | **65ms** | **↓ 99.1%** | ~17ms |
| `hello world` | 4166ms | **76ms** | **↓ 98.2%** | ~15ms |
| `*.swift` | 1791ms | **18ms** | **↓ 99.0%（完全恢复！）** | ~15ms |
| `ext:cpp` | 726ms | **10ms** | **↓ 98.6%（完全恢复！）** | ~10ms |
| `main.cpp` | 228ms | **6ms** | **↓ 97.4%（极佳）** | ~5ms |
| `README` | 25ms | **13ms** | ↓ 48% | ~10ms |
| `test` | 40ms | **22ms** | ↓ 45% | ~14ms |
| `MacEverything` | 7ms | **4ms** | ↓ 39%（极佳） | ~3ms |
| Trigram 均值 | 1647ms | **32ms** | **↓ 98.1%** | 12-18ms |

**关键验证**：
1. **R181 battery 完成了受影响 postings 区域的预热**——一次 battery 即可恢复，即使是 7193ms 级别的灾难。
2. **`*.swift`=18ms、`ext:cpp`=10ms——从 1791ms/726ms 完全恢复至极佳水平。**
3. **`.pdf`=65ms、`hello world`=76ms——大幅恢复但尚未完全回到极佳（<50ms）**——这两个查询的 postings 区域最大，需要更多轮次完全预热。
4. **Trigram 均值 32ms——接近正常稳态（12-18ms），是 R177 以来最佳。**

### 全表扫描——系列新低！

| Query | R181 | R182 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 290ms | **242ms** | ↓ 17% |
| `path:/Users` | 215ms | **207ms** | ↓ 4% |
| `中文测试` | 229ms | **150ms** | **↓ 34%** |
| **总和** | **733ms** | **599ms** | **↓ 18%（系列新低！）** |

- **全表扫描总和 599ms——超越 R177 的 238ms 之后的第二低值。**
- **`中文测试`=150ms（R181=229ms，↓34%）——显著改善。**
- **全扫描区域 page cache 状态良好——日间系统活动未严重影响。**

### SoA Filter——恢复

| Query | R181 | R182 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 675ms | **30ms** | **↓ 96%（极佳！）** |
| `dm:today` | 114ms | **54ms** | ↓ 53% |

- **`size:>100m`=30ms（R181=675ms）——完全恢复至极佳水平。**
- **`dm:today`=54ms——正常范围。**

### 极佳比例 9/16——系列最高！

极佳查询（<50ms）：
1. `verylongquery`=0.4ms
2. `x`=0.8ms
3. `MacEverything`=3.6ms
4. `main.cpp`=5.5ms
5. `ext:cpp`=9.8ms
6. `README`=13.3ms
7. `*.swift`=18.1ms
8. `test`=21.8ms
9. `size:>100m`=29.6ms

- **9/16=56%——此前最高 R177=6/16（38%）。**
- **包含 3 个从 >500ms 恢复的查询（`*.swift`、`ext:cpp`、`size:>100m`）。**

### tombstone 追赶接近完成

- **增速 382/min（R181=445/min，↓14%）——接近正常水平（250-300/min）。**
- **recordCount +9.1K（R181=+13.5K，↓33%）——接近日常正常值（~8K/30min）。**
- **tombstone 22.73%（1.50M）——距 25% 约 15-18 小时。**
- **追赶预计下一轮完全回到正常增速。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 0.8 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 65.2 | 61.6 | advanced-trigram | 0 | 9841 | 正常（恢复中） |
| `main.cpp` | 5 | 5.5 | 3.5 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 18.1 | 16.4 | advanced-trigram | 0 | 9345 | **极佳（完全恢复）** |
| `ext:cpp` | 5 | 9.8 | 9.1 | advanced-ext-index | 0 | 3571 | **极佳（完全恢复）** |
| `size:>100m` | 5 | 29.6 | 29.5 | advanced-pure-filter-soa-gcd | 0 | 0 | **极佳（完全恢复）** |
| `hello world` | 5 | 75.5 | 73.7 | advanced-trigram | 0 | 3626 | 正常（恢复中） |
| `abc def ghi jkl mno pqr` | 0 | 41.8 | 41.7 | advanced-trigram | 0 | 14218 | 极佳 |
| `README` | 5 | 13.3 | 12.0 | advanced-trigram | 0 | 16636 | 极佳 |
| `test` | 5 | 21.8 | 17.8 | advanced-trigram | 0 | 116529 | 极佳 |
| `MacEverything` | 5 | 3.6 | 0.8 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 54.2 | 54.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 241.8 | 227.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 207.4 | 204.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 150.1 | 149.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 35.7ms，P90 = 207.4ms，Max = 241.8ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R181 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **9/16** | 0.4-42ms | R181=4/16（**↑5，系列最高**） |
| **正常**（50-500ms） | 7/16 | 54-242ms | R181=7/16（持平） |
| **超标**（>500ms） | **0/16** | — | R181=5/16（**↓5**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,608,547，liveRecordCount=5,108,153（tombstone=**1,500,394**，**22.73%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=192  QUERYADV_GE500=18（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3268
```

- **本轮 0 新增 >500ms 查询。**
- **SKIPFLUSH +60——正常节奏。**

## 6. 问题清单

### 已解决：R181 选择性 trigram 淘汰——1 轮完全恢复

- **确认为一次性事件。**
- **R181 battery 本身完成了受影响 postings 的预热。**
- **恢复模式与 R176→R177（睡眠/唤醒恢复）和 R179→R180（I/O 风暴恢复）一致——1 轮 battery 恢复。**

### 观察项：`.pdf`=65ms、`hello world`=76ms 尚未回到极佳

- **已从 7193ms/4166ms 恢复 99%，但仍高于正常（17ms/15ms）。**
- **预计再 1 轮可回到极佳水平。**

### tombstone 22.73%——追赶接近完成

- **增速 382/min（正常 250-300/min）——基本正常化。**
- **距 25% 约 15-18 小时。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（紧迫）

- tombstone 22.73%（1.50M），距 25% 约 15-18 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

- R181→R182 的恢复模式证明：battery 预热有效，但被动预热（等待查询触及）不如主动维护。
- 建议对高频 postings 区域定期 `madvise(MADV_WILLNEED)` 保持热态。

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. `.pdf`/`hello world` 是否继续恢复至极佳水平（<50ms）。
2. 极佳比例是否保持 9/16 或更高。
3. tombstone 增速是否回到正常水平（250-300/min）。

## 9. 结论

**灾难性事件 1 轮完全恢复——0/16 >500ms，Max=242ms，P50=36ms，lockWait=0，极佳比例 9/16（56%，系列最高！）。** R181 的 `.pdf`=7193ms 在本轮恢复至 65ms（↓99.1%），所有 trigram 查询回到正常——证明 R181 的选择性淘汰是一次性事件。全表扫描总和 599ms（系列新低）。

**恢复模式总结（本系列 3 次灾难性事件）**：

| 事件 | 类型 | Max | 恢复轮数 |
|------|------|----:|------:|
| R176→R177 | 睡眠/唤醒冷启动 | 2217ms | **1 轮** |
| R179→R180 | I/O 风暴 | 1538ms | **1 轮** |
| R181→R182 | 选择性 trigram 淘汰 | 7193ms | **1 轮** |

**三次灾难性事件均在 1 轮 battery 后恢复——系统自恢复能力强健。** 但 P2 热区维护策略仍有必要——被动等待查询预热不如主动维护。tombstone 22.73%（1.50M），追赶基本完成（382/min），距 25% 约 15-18 小时。P0 compaction 修复持续紧迫。
