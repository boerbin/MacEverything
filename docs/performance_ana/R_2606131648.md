# MacEverything 性能分析报告 R_2606131648

## 1. 本轮范围

- 任务启动时间：2026-06-13 16:48
- 报告文件：`docs/performance_ana/R_2606131648.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=12287`
  - `SESSION_START_TS=2026-06-13 12:03:04.944`
  - `TOTAL_LINES=12688`
  - `SEG_LINES=402`
  - `LAST_LOG_TS=2026-06-13 16:48`

## 2. 执行摘要

**本轮 16-query battery 中 0/16 >500ms，Max 从 418.3ms 回落到 328.4ms，但 trigram 尾部明显恶化，且最新 flush 出现 9.092s 极端 outlier。** 最高项为 `中文测试`=328.4ms，其次是 `path:/Users`=240.0ms、`hello world`=223.1ms、`content:import`=179.1ms、`abc def ghi jkl mno pqr`=147.3ms。P50≈39.3ms，P90≈223.1ms，所有 lockWait 均为 0ms。

相比 16:18，Max 下降约 21%，但 P50 从约 24.6ms 升至约 39.3ms，P90 从约 187.5ms 升至约 223.1ms，极佳查询数从 10/16 降至 7/16。扫描/过滤路径总和从 984.1ms 降至 850.5ms，但 trigram 系列显著抬升：`hello world`=223.1ms、`abc def ghi jkl mno pqr`=147.3ms、`*.swift`=91.3ms。后台结构性风险进一步恶化：recordCount=6,253,003，liveRecordCount=5,313,303，tombstone=939,700（15.03%），较上一轮增加 7,986；最新 session `FLUSH=56`，`SKIPFLUSH=0`，session 最大 flush total 从 6164ms 升至 **9092ms**。

| 指标 | R_2606131618 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 418.3ms | **328.4ms** | ↓21% |
| P50 | ≈24.6ms | **≈39.3ms** | ↑60% |
| P90 | ≈187.5ms | **≈223.1ms** | ↑19% |
| lockWait max | 0.0ms | **0.0ms** | 持平 |
| 极佳比例(<50ms) | 10/16 | **7/16** | -3 |
| 扫描/过滤路径总和 | 984.1ms | **850.5ms** | ↓14% |
| recordCount | 6,244,350 | **6,253,003** | +8,653 |
| liveRecordCount | 5,312,636 | **5,313,303** | +667 |
| tombstone | 931,714 (14.92%) | **939,700 (15.03%)** | +7,986 |
| 最新 session FLUSH | 50 | **56** | +6 |
| 最新 session QUERYADV_GE500 | 0 | **0** | 持平 |
| 最新 flush total | 2942ms | **9092ms** | ↑209% |
| session 最大 flush total | 6164ms | **9092ms** | ↑47% |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 0.7 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 19.8 | 18.4 | advanced-trigram | 0.0 | 10158 | 极佳 |
| `main.cpp` | 5 | 14.1 | 9.4 | advanced-trigram | 0.0 | 103 | 极佳 |
| `*.swift` | 5 | 91.3 | 90.1 | advanced-trigram | 0.0 | 9622 | 正常，trigram 抬升 |
| `ext:cpp` | 5 | 27.0 | 26.5 | advanced-ext-index | 0.0 | 3891 | 极佳 |
| `size:>100m` | 5 | 30.8 | 30.6 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 223.1 | 218.9 | advanced-trigram | 0.0 | 3932 | 正常高位，本轮 trigram 最高 |
| `abc def ghi jkl mno pqr` | 0 | 147.3 | 146.8 | advanced-trigram | 0.0 | 14623 | 正常高位，trigram 抬升 |
| `README` | 5 | 28.7 | 20.1 | advanced-trigram | 0.0 | 17588 | 极佳 |
| `test` | 5 | 47.8 | 40.6 | advanced-trigram | 0.0 | 118022 | 极佳，接近 50ms |
| `MacEverything` | 5 | 11.4 | 1.8 | advanced-trigram | 0.0 | 7038 | 极佳 |
| `dm:today` | 5 | 73.2 | 73.0 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 179.1 | 170.2 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常高位 |
| `path:/Users` | 5 | 240.0 | 229.9 | advanced-linear-gcd | 0.0 | 0 | 正常高位 |
| `中文测试` | 0 | 328.4 | 317.1 | advanced-linear-gcd | 0.0 | 0 | 正常高位，本轮最高 |
| `verylongquery...` | 0 | 5.0 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈39.3ms，P90≈223.1ms，Max=328.4ms。0/16 查询 >500ms。lockWait max=0.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **7/16** | 0.7-47.8ms | 轻量 trigram、ext-index、short-query-cache 仍健康，但极佳比例下降 |
| 正常（50-500ms） | **9/16** | 73.2-328.4ms | trigram、pure-filter、linear-gcd 全部出现中高位尾部 |
| 超标（>500ms） | **0/16** | — | 本轮无正式超标查询 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,253,003；
- liveRecordCount=5,313,303；
- tombstone=939,700（**15.03%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=12287
SESSION_START_TS=2026-06-13 12:03:04.944
TOTAL_LINES=12688
SEG_LINES=402
QUERYADV=41
QUERYADV_GE500=0
FLUSH=56
SKIPFLUSH=0
COMPACTION=2
LAST_LINE=[2026-06-13 16:48:13.137] [INFO ] [SearchEngine] QueryAdvanced "中文测试" total=317ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6253002
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 0，本轮没有日志级慢查询。`中文测试` 的日志 total=317ms，与 query battery 的 328.4ms 同量级；本轮更突出的变化是 trigram 查询整体抬升，尤其是 `hello world`=223.1ms 与 `abc def ghi jkl mno pqr`=147.3ms。

```text
[2026-06-13 16:48:13.137] QueryAdvanced "中文测试" total=317ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6253002
```

### flush 情况

最新 session 内 `FLUSH=56`，rewrite 范围 **2470ms - 9087ms**，total 最大 **9092ms**。`SKIPFLUSH=0`，说明 session 内完整 flush 从上一轮的 50 次继续累积到 56 次，并且在 16:43 出现了新的 9.092s outlier，但没有任何可见 backoff。

最新 flush：

```text
[2026-06-13 16:43:30.341] Flushed v6 flat index, lastEventId=362489135,
liveRecords=5313264 | timing: walSwap=0ms rewrite=9087ms total=9092ms
```

Top flushes：

```text
[2026-06-13 16:43:30.341] rewrite=9087ms total=9092ms
[2026-06-13 16:23:26.885] rewrite=5714ms total=5716ms
[2026-06-13 16:28:26.841] rewrite=5670ms total=5672ms
[2026-06-13 12:08:25.921] rewrite=4809ms total=4812ms
[2026-06-13 16:38:25.783] rewrite=4621ms total=4624ms
```

## 6. 问题清单

### P0：full-index flush 出现 9.092s 新 outlier 且无退避

- 证据：最新 flush total=9092ms，rewrite=9087ms；`FLUSH=56`，较上一轮 +6；`SKIPFLUSH=0`。
- 影响：9s 级完整重写会显著冲刷 page cache，并可能解释本轮 trigram 的中高位抬升。
- 判断：flush 从 3-6s 级进一步恶化到 9s 级，且仍没有可见 backoff，后台持久化策略是本轮最高优先级风险。

### P0：tombstone 突破 15%

- 证据：tombstone=939,700（15.03%），较上一轮增加 7,986。
- 影响：无效记录比例继续升高，会放大扫描成本、索引重写成本与 compaction 压力。
- 判断：连续多轮上行后已突破 15%，说明当前 compaction/清理策略未能控制数据膨胀。

### P0：trigram 尾部明显恶化

- 证据：`hello world`=223.1ms，`abc def ghi jkl mno pqr`=147.3ms，`*.swift`=91.3ms。
- 影响：trigram 从此前的健康/中位状态进入明显用户可感知区间，说明问题不只局限于 scan/filter。
- 判断：结合最新 9.092s flush，trigram postings/候选处理可能受到 page-cache 或 I/O 干扰。

### P0：linear-gcd 仍维持 240-330ms 尾部

- 证据：`中文测试`=328.4ms，phase2=317.1ms；`path:/Users`=240.0ms，phase2=229.9ms。
- 影响：虽然没有 16:18 的 418.3ms 高点，但中文与路径查询仍明显高于轻量查询。
- 判断：lockWait=0ms，瓶颈主要在 phase2 扫描阶段。

### P1：pure-filter 仍处于 70-180ms 区间

- 证据：`content:import`=179.1ms，`dm:today`=73.2ms，`size:>100m`=30.8ms。
- 影响：pure-filter 仍是稳定的中位以上成本来源。

## 7. 优化建议

### P0：为 full-index flush 增加硬性前台保护与退避

- 对 flush rewrite 超过 2s/5s/8s 建立分级退避：延后下一次 flush、降低 batch 频率、前台查询活跃时禁止大 rewrite。
- 持久化线程使用低 QoS，并评估大顺序写的 `F_NOCACHE`/同类策略，减少对搜索热页的冲刷。
- 在日志中记录触发/跳过 flush 的原因，避免 `SKIPFLUSH=0` 长期无法解释。
- 验收：9s 级 flush 不再出现；出现 5s+ flush 后下一周期必须看到 backoff 或 skip 证据。

### P0：flush 改为增量/COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页或持锁窗口。
- 验收：常规 flush total <200ms，新 session 不再出现 3-9s 级完整 rewrite。

### P0：立即提前或分段 compaction

- 以 tombstone 率 14%+ 或绝对 tombstone 900k+ 作为明确触发信号；当前 15.03% 已超过应急阈值。
- 结合 segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 优先处理 tombstone 密集 segment，避免 scan/filter 与 flush rewrite 继续放大。
- 验收：tombstone 率从 15.03% 回落，`中文测试`、`path:/Users`、`content:import` 稳定 <200ms。

### P0：为 advanced-linear-gcd 与 pure-filter 增加轻量剪枝

- 对中文、路径、content/filter 类查询记录扫描记录数、tombstone 跳过数和 segment 分布。
- 评估轻量字符 n-gram、路径分段索引、预过滤位图或 segment-level skip list，以减少无结果或低选择性查询的全量扫描。
- 要求保持内存增长可控，优先使用压缩或按需结构，不引入大常驻索引。
- 验收：`中文测试` 与 `path:/Users` 稳定低于 200ms，`content:import` 不再接近 200ms。

### P1：细分 trigram/postings 路径

- 对 `advanced-trigram` 记录 postings 读取、候选交并、候选评分、路径加载、结果组装、page-cache miss 近似指标。
- 重点解释 `hello world` 的 223.1ms、`abc def ghi jkl mno pqr` 的 147.3ms 是否由 postings 读取、候选处理还是 page-cache 状态导致。
- 验收：trigram 查询在 5s+ flush 后仍稳定 <100ms，或至少能解释每个阶段的成本。

### P1：细分 scan/filter 与 top-level timing

- 对 pure-filter 和 linear-gcd 记录扫描记录数、跳过 tombstone 数、命中 live 数、segment 数、metadata 加载耗时和结果组装耗时。
- 对所有路径增加 queueWait、phase2、candidate materialization、path/file metadata load、sort/rank、HTTP serialization/write 等阶段耗时。
- 验收：`queryTimeMs - phase2Ms - lockWaitMs` 不再是不可解释的大黑盒。

## 8. 下一轮重点

1. 是否继续出现 5s-9s 级 flush outlier。
2. `SKIPFLUSH` 是否仍为 0。
3. tombstone 是否继续从 939,700 上行并突破 950k。
4. `hello world` 是否继续维持 200ms+。
5. `abc def ghi jkl mno pqr` 是否继续维持 100ms+。
6. `中文测试` 是否继续维持 300ms+ 或接近 500ms。
7. 新 session 中 `QUERYADV_GE500` 是否开始累积。
8. scan/filter 与 trigram 尾部是否同时抬升。

## 9. 结论

**本轮虽然没有 >500ms 查询，Max=328.4ms，但 P50≈39.3ms、P90≈223.1ms，trigram 尾部明显恶化。** `hello world`=223.1ms、`abc def ghi jkl mno pqr`=147.3ms、`*.swift`=91.3ms，说明中高位延迟已从 scan/filter 扩散到 trigram 路径。

**后台根因显著恶化：tombstone=939,700（15.03%）首次突破 15%，新 session `FLUSH=56`，最新 flush total=9092ms，且 `SKIPFLUSH=0`。** 下一步最高优先级应是 full-index flush 前台保护/退避与增量/COW flush，同时推进提前/分段 compaction、advanced-linear-gcd/pure-filter 剪枝，以及 trigram/postings 与 top-level timing 拆分。
