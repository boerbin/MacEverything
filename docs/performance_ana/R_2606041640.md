# MacEverything 性能分析报告 R_2606041640

## 1. 本轮范围

- 任务启动时间：2026-06-04 16:40
- 报告文件：`docs/performance_ana/R_2606041640.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=22373`
  - `SEG_LINES=1347`
  - `LAST_LOG_TS=2026-06-04 16:41`

## 2. 执行摘要

**tombstone 第四次爆发——逼近半数记录为无效。** tombstone 从 3,472,840 (37.65%) 增至 **5,028,362 (46.64%，~1,508MB)**——**单轮 +1,555,522 条**。**3/16 查询 >500ms**（`path:/Users`=1,833ms，`.pdf`=1,725ms，`hello world`=629ms）。lockWait 最高 48.4ms（R102=60.8ms，略降）。P50 ≈ 205ms，Max=1,833ms。

**SKIPFLUSH +59**——连续第 3 轮事件风暴，速率微增。**事件源仍未停止。**

### 关键指标对比

| 指标 | R101 | R102 上轮 | R103 本轮 | 变化 |
|------|--------:|--------:|--------:|------|
| >500ms | 2/16 | 3/16 | **3/16** | 持平 |
| Max | 766ms | 980ms | **1,833ms** | **↑↑ 恶化** |
| P50 | 216ms | 203ms | **205ms** | 持平 |
| lockWait max | 15.3ms | 60.8ms | **48.4ms** | 略降 |
| lockWait 受影响查询 | 14/16 | 15/16 | **15/16** | 持平 |
| tombstone | 2,383K (29.30%) | 3,473K (37.65%) | **5,028K (46.64%)** | **↑↑↑ 持续** |
| tombstone 增量 | +1,583.0K | +1,090.2K | **+1,555.5K** | **反弹** |
| tombstone 内存 | 714MB | ~1,042MB | **~1,508MB** | **1.5GB** |

### tombstone 爆发四轮追踪

| 轮次 | 增量 | 累计 | 比率 | 内存 | 说明 |
|------|-----:|-----:|-----:|-----:|------|
| ≤R99 稳态 | ~12K/轮 | 568K | 8.98% | 170MB | 正常增长 |
| R100 | +231K | 800K | 12.21% | 240MB | 第 1 波爆发 |
| R101 | +1,583K | 2,383K | 29.30% | 714MB | 第 2 波 |
| R102 | +1,090K | 3,473K | 37.65% | ~1,042MB | 第 3 波（突破 1GB） |
| **R103** | **+1,556K** | **5,028K** | **46.64%** | **~1,508MB** | **第 4 波（1.5GB）** |

**四轮累计 +4,460K tombstone**（从 568K 到 5,028K，增长 8.8 倍）。增量从 R102 的 +1,090K 反弹至 +1,556K——**事件源非但未停止，反而加速**。

### Max 从 980ms 跳至 1,833ms——I/O 压力升级

- R101(766ms) → R102(980ms) → **R103(1,833ms)**——Max 连续 3 轮上升。
- `path:/Users`=1,833ms 是 linear-gcd 路径的历史极端值（前最差 R95=573ms 的 3.2 倍）。
- **1.5GB tombstone 下 linear 全表扫描成本极端**：10.78M 条记录中 5.03M 为 tombstone——**扫描效率仅 53%**。

### 横向对比（最近 5 轮）

| Query | R99 | R100 | R101 | R102 | R103 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 949 | 37 | 14 | 22 | **22** | 正常 |
| `.pdf` | 8495 | 840 | 766 | 484 | **1725** | **反弹恶化** |
| `*.swift` | 430 | 2887 | 409 | 980 | **366** | 波动 |
| `hello world` | 3042 | 327 | 458 | 529 | **629** | **>500ms** |
| `path:/Users` | 569 | 228 | 627 | 443 | **1833** | **极端恶化** |
| `中文测试` | 312 | 282 | 206 | 567 | **444** | 偏高 |
| P50 | 376 | 222 | 216 | 203 | **205** | 稳定 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 21.7 | 0.0 | short-query-cache | 0 | 0 | 正常 |
| `.pdf` | 5 | 1724.6 | 1658.8 | advanced-trigram | 0 | 9952 | **极端 I/O** |
| `main.cpp` | 5 | 220.5 | 132.1 | advanced-trigram | **48.4** | 85 | **lockWait 最高** |
| `*.swift` | 5 | 365.6 | 315.9 | advanced-trigram | 23.5 | 9356 | lockWait |
| `ext:cpp` | 5 | 136.1 | 115.6 | advanced-ext-index | 7.5 | 3701 | lockWait |
| `size:>100m` | 5 | 369.2 | 362.3 | advanced-pure-filter-soa-gcd | 4.9 | 0 | 偏高 |
| `hello world` | 5 | 628.8 | 588.1 | advanced-trigram | 13.7 | 3613 | **>500ms** |
| `abc def ghi jkl mno pqr` | 0 | 180.7 | 152.9 | advanced-trigram | 9.4 | 16043 | lockWait |
| `README` | 5 | 58.0 | 20.6 | advanced-trigram | 17.1 | 17679 | lockWait |
| `test` | 5 | 54.8 | 34.6 | advanced-trigram | 4.3 | 130129 | lockWait |
| `MacEverything` | 5 | 36.1 | 2.5 | advanced-trigram | 2.7 | 6803 | lockWait |
| `dm:today` | 5 | 188.7 | 142.7 | advanced-pure-filter-soa-gcd | 6.5 | 0 | lockWait |
| `content:import` | 5 | 283.3 | 255.0 | advanced-pure-filter-soa-gcd | 0.4 | 0 | lockWait |
| `path:/Users` | 5 | 1832.9 | 1797.7 | advanced-linear-gcd | 5.0 | 0 | **极端 I/O** |
| `中文测试` | 0 | 443.8 | 406.6 | advanced-linear-gcd | 22.9 | 0 | 偏高+lockWait |
| `verylongquery...` | 0 | 22.4 | 0.0 | advanced-trigram | 11.1 | 0 | lockWait |

**P50 ≈ 205ms，P90 ≈ 629ms，Max = 1,833ms。3/16 查询 >500ms。lockWaitMs 出现在 15/16 查询中，最高 48.4ms。**

### `path:/Users`=1,833ms 深度分析

- phase2=1,798ms（98%），lockWait=5.0ms——几乎纯 phase2 全表扫描 I/O。
- 历史趋势：

| 轮次 | `path:/Users` | recordCount | 说明 |
|------|------------:|----------:|------|
| R96 | 83ms | 6.26M | 热缓存 |
| R97 | 458ms | 6.27M | 冷缓存 |
| R99 | 569ms | 6.31M | I/O 事件 |
| R101 | 627ms | 8.13M | tombstone 爆发中 |
| **R103** | **1,833ms** | **10.78M** | **极端** |

- **recordCount 增长 72%（6.26M→10.78M），延迟增长 22 倍（83ms→1,833ms）**——非线性恶化。
- 原因：recordCount 增长不仅增加扫描量，更严重的是 1.5GB tombstone 挤出了大量页面缓存，导致 phase2 扫描遭遇大量页面故障。

### `.pdf`=1,725ms 分析

- phase2=1,659ms（96%），lockWait=0ms——纯 trigram 索引 I/O。
- R100=840ms → R101=766ms → R102=484ms → **R103=1,725ms**——**恢复趋势被打断，强力反弹**。
- R102 的 484ms 是因为连续查询电池预热了 trigram 页面。本轮预热效果被 1.5GB tombstone 的缓存挤压覆盖——**系统已无法通过预热维持缓存稳定性**。

### `main.cpp`=220ms + lockWait=48.4ms 异常

- `main.cpp` 正常值 7-56ms，本轮 220ms——**4 倍偏差**。
- lockWait=48.4ms 是本轮最高——一个仅 85 候选集的简单查询被锁等待占去 22%。
- **lockWait 分布异常**：最高 lockWait 不再出现在 `*.swift` 等大候选集查询，而是在小候选集查询——说明 FSWatcher 事件写入的时机是随机的，任何查询都可能遭遇长锁。

### lockWait 分布

| lockWaitMs 范围 | 查询数 | R102 同范围 | 说明 |
|----------------|------:|----------:|------|
| 0ms | 1 | 1 | 仅 `x` |
| 0.4-13.7ms | 9 | 8 | 轻微竞争 |
| 17.1-23.5ms | 3 | 3 | 中度竞争 |
| **48.4ms** | **1** | 3(>40ms) | 显著竞争 |

- R102 有 3 个 >40ms（最高 60.8ms），R103 仅 1 个 >40ms（48.4ms）——**峰值略降但分布转为随机性**。
- lockWait 48.4ms 出现在 `main.cpp`（85 候选集）而非大查询——**不可预测性增加**。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=10,780,933，liveRecordCount=5,752,571（tombstone=**5,028,362**，**46.64%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=412  QUERYADV_GE500=64
FLUSH=1  COMPACTION=2  SKIPFLUSH=625
DIAG_TRIGRAM_SKIP=111
```

- QUERYADV 从 401 增至 412（+11）。
- QUERYADV_GE500 从 61 增至 64（**+3，即 `path:/Users`=1,833ms，`.pdf`=1,725ms，`hello world`=629ms**）。
- SKIPFLUSH 从 566 增至 625（**+59，连续第 3 轮事件风暴**）。
- FLUSH 保持 1（本轮未新增 flush）。
- DIAG_TRIGRAM_SKIP 从 110 增至 111（+1）。

### SKIPFLUSH 事件风暴持续且微增

| 轮次间隔 | SKIPFLUSH 增量 | 说明 |
|----------|------------:|------|
| ≤R99→R100 | +12 | 稳态 |
| R100→R101 | +56 | 事件风暴开始 |
| R101→R102 | +56 | 恒定 |
| **R102→R103** | **+59** | **微增——事件源加速** |

- 从 +56 微增至 +59——事件源非但未衰减，反而略有加速。
- 与 tombstone 增量从 +1,090K 反弹至 +1,556K 一致——**事件源是持续运行的进程**。

### QUERYADV_GE500 加速

- R99 以来 6 轮：+3, +2, +2, +3, +3 = 共 +13 次 >=500ms 查询。
- 对比 R60-R99（39 轮）：56 次（1.44/轮）。
- **R100-R103（4 轮）**：+13（**3.25/轮**——正常频率的 2.3 倍）。

## 6. 问题清单

### Critical：tombstone 逼近半数——46.64%（5,028,362 条，~1,508MB）

- **单轮 +1,555,522 条——增量从 R102 的 +1,090K 反弹至 +1,556K。**
- **recordCount 10.78M 中 5.03M 为 tombstone——每次查询扫描近半数无效记录。**
- **~1.5GB 额外内存占用**——是 R99 (170MB) 的 8.9 倍。
  - R99 (170MB) → 全路径 I/O 饱和
  - R101 (714MB) → 系统危机
  - R102 (1,042MB) → 突破 1GB
  - **R103 (1,508MB) → 1.5GB，系统页面缓存被严重侵蚀**
- 连续第 43 轮超 2%，首轮超过 46%。
- **按当前增速预测**：下一轮可能突破 50%（tombstone 超过 liveRecord）。

### Critical：Max 升至 1,833ms——I/O 恶化加速

- R101(766ms) → R102(980ms) → **R103(1,833ms)**——**Max 连续 3 轮倍增**。
- `path:/Users`=1,833ms 是 linear-gcd 路径历史极端值（前最差 573ms 的 3.2 倍）。
- `.pdf`=1,725ms 恢复趋势被打断——1.5GB tombstone 下缓存预热已失效。
- **趋势**：如果 tombstone 继续增长，下一轮 Max 可能达到 3,000-5,000ms。

### Critical：lockWait 随机性增加

- 最高 48.4ms 出现在 `main.cpp`（85 候选集）——最简单的查询也可能遭遇长锁。
- 锁竞争的不可预测性使得**任何查询都无法保证响应时间**。
- R86 灾难风险持续存在——如果某次 lockWait 累积至数百毫秒，即触发灾难。

### Critical：SKIPFLUSH +59——事件风暴第 3 轮且微增

- 连续 3 轮（+56, +56, +59）——事件源持续运行且略有加速。
- 可能来源：Spotlight mdworker（持续索引活动）、Time Machine 后台备份、或某个持续运行的构建/监控进程。

### High：`.pdf` 恢复趋势被打断

- R100(840ms) → R101(766ms) → R102(484ms) → **R103(1,725ms)**——从持续恢复到强力反弹。
- 1.5GB tombstone 下系统已**无法通过查询电池预热维持缓存稳定**——任何路径的 I/O 性能都不可预测。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（46.64%，~1.5GB——系统存活级别）

- **43 轮连续超标。** tombstone 4 轮内从 568K 增长到 5,028K（8.8 倍），内存从 170MB 增至 1.5GB。
- **46.64% tombstone 意味着查询效率降低近半**——每次扫描有 46.64% 的 I/O 浪费在无效记录上。
- **预计收益**：compaction 释放 ~1.5GB + 消除 5.03M 无效记录 → 所有查询路径立即恢复正常。
- 修复方案：定期 compaction（tombstoneRatio > 0.02 时触发），或基于 tombstoneRatio 的强制 compaction。

### P0 紧急：修复 WAL flush 阈值机制

- FLUSH=1（43 轮仅触发 1 次）——机制近乎失效。
- 方案：时间触发器（每 N 秒强制 flush）+ tombstoneRatio 触发器。

### P0 紧急：lockWait 升级防护
### P0 紧急：`size:>100m` 全表扫描超时保护
### P0 紧急：short-query-cache GCD 隔离
### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. tombstone 是否突破 50%（tombstone > liveRecord）。
2. Max 是否继续倍增趋势（3,000-5,000ms 区间）。
3. SKIPFLUSH 增量是否开始下降（事件源是否减速）。
4. lockWait 是否再次出现 >60ms 或更高值。

## 9. 结论

**tombstone 第四次爆发——逼近半数记录为无效，内存负担达 1.5GB。** tombstone 从 3,473K (37.65%) 增至 **5,028K (46.64%，~1,508MB)**——**单轮 +1,556K 条**（增量从 R102 +1,090K 反弹，事件源非但未停止反而加速）。`path:/Users`=1,833ms（linear-gcd 历史极端值，前最差 3.2 倍），`.pdf`=1,725ms（恢复趋势被打断），`hello world`=629ms，3/16 >500ms。P50=205ms，**Max=1,833ms（连续 3 轮倍增：766→980→1,833）**。lockWait 最高 48.4ms 出现在仅 85 候选集的 `main.cpp`——锁竞争完全随机化，任何查询都无法保证响应时间。15/16 查询有 lockWait。SKIPFLUSH +59（连续第 3 轮事件风暴，微增）。recordCount 10.78M 中 **46.64% 为 tombstone**——查询扫描近半无效记录。1.5GB tombstone 下缓存预热已失效——`.pdf` 从连续 3 轮恢复（840→766→484）反弹至 1,725ms。**下一轮 tombstone 可能突破 50%**——即 tombstone 数量超过 liveRecord，系统进入前所未有的异常状态。
