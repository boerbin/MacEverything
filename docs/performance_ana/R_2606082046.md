# MacEverything 性能分析报告 R_2606082046

## 1. 本轮范围

- 任务启动时间：2026-06-08 20:46
- 报告文件：`docs/performance_ana/R_2606082046.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5408`
  - `SEG_LINES=4466`
  - `LAST_LOG_TS=2026-06-08 23:45`

## 2. 执行摘要

**连续第 2 轮重度事件——4/16 >500ms，Max=1567ms，lockWait=12ms，P50=182ms，极佳比例 1/16。** `.pdf`=1567ms（系列第二高，仅次 R181=7193ms），`path:/Users`=1072ms，`中文测试`=765ms，`*.swift`=680ms。`x`=119ms 暴露极端进程调度延迟（正常 <10ms）。

**与 R197 对比——lockWait 从 567ms 大幅降至 12ms（↓98%），但 page cache 压力更广泛。** R197 的核心问题是 flush 锁竞争（lockWait=567ms），本轮核心问题是全面 page cache 淘汰+系统级资源竞争。`x`=119ms 证明进程被系统 I/O 严重挤压——不仅是 mmap 区域冷，连进程内存缓存的查询都需要 119ms。

QUERYADV_GE500 从 25 跳至 29（+4）——说明 battery 之外的日志查询中也出现了大量 >500ms 事件。

tombstone 24.71%——增速极低，仅 ~78/min（深夜）。距 25% 约 6.2 小时（约明天 03:00）。**此前预计 21:10 触发的预测未实现——夜间增速远低于预期。**

### 关键指标对比

| 指标 | R197 | R198 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 4/16 | **4/16** | 持平（连续 2 轮 4） |
| Max | 2158ms | **1567ms** | ↓ 27% |
| P50 | 206ms | **182ms** | ↓ 12% |
| lockWait max | 567ms | **12ms** | **↓ 98%** |
| 极佳比例(<50ms) | 2/16 | **1/16** | ↓1 |
| tombstone | 1,690,873 (24.67%) | **1,695,242 (24.71%)** | ↑（极慢） |
| tombstone 增速 | ~246/min | **~78/min** | **↓ 68%（深夜极低！）** |
| recordCount | 6,827,788 | **6,832,535** | +4.7K（低） |
| liveRecordCount | 5,136,915 | **5,137,293** | +378 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 4057 | **4071** | +14（间隔短） |
| COMPACTION | 2 | **2** | **不变——compaction 未触发** |
| QUERYADV_GE500 | 25 | **29** | **↑4** |

### 注意：日志时间跨度异常——20:17 到 23:45

- **最后日志时间 23:45——距 battery 执行（~20:50）约 3 小时。**
- **这意味着 R197→R198 之间经历了较长时间间隔。**
- **QUERYADV_GE500 +4 中的大部分可能来自此间隔内的其他查询。**

### `x`=119ms——极端进程调度延迟

- **`x` 使用 short-query-cache（纯进程内存，0 I/O）。**
- **119ms 意味着 ~117ms 的进程调度/唤醒延迟。**
- **此前最高 R190=37ms——本轮 3.2x。**
- **系统资源竞争极其严重——进程被 I/O 密集操作严重降低优先级。**
- **这解释了为什么 `verylongquery`=2.7ms（含 lockWait=2.5ms）反而正常——执行时系统已恢复。**

### `.pdf`=1567ms——系列第二高

- **phase2=1557ms，lockWait=0——纯 page cache 淘汰。**
- **系列排名**：R181=7193ms → **R198=1567ms** → R190=630ms → R194=619ms。
- **仅次于 R181 的选择性淘汰灾难。**
- **trigram postings 区域被系统 I/O 大面积淘汰。**

### `path:/Users`=1072ms——第四次 >1000ms

- **phase2=1025ms，lockWait=0——纯 page cache。**
- **系列排名**：R176=2217ms → R190=2043ms → R197=1803ms → **R198=1072ms**。
- **从 R197=1803ms 有所恢复（↓41%），但仍在 >1000ms。**

### `中文测试`=765ms——系列新高

- **phase2=736ms，lockWait=0——纯 page cache（名称数组区域）。**
- **此前系列最高 R197=1406ms（但含大量 overhead），纯 phase2 来看 R198=736ms > R197=363ms。**
- **名称数组区域的 page cache 淘汰比 R197 更严重。**

### `*.swift`=680ms——首次 >500ms

- **phase2=670ms，lockWait=0——trigram postings 区域被淘汰。**
- **此前最高 R195=254ms——本轮 2.7x。**
- **9345 candidates 的大范围 postings 查询受 page cache 压力影响大。**

### lockWait=12ms——从 567ms 大幅恢复

| Query | lockWait (ms) |
|-------|----------:|
| `ext:cpp` | **12.0** |
| `main.cpp` | 2.6 |
| `verylongquery` | 2.5 |
| 其他 13 项 | 0 |

- **最高 12ms（R197=567ms，↓98%）——flush 锁竞争已消散。**
- **R197 的 567ms 确认为一次性事件——flush timer 恰好在 battery 期间触发。**
- **但 12ms 和 2.5-2.6ms 说明轻度竞争仍存在——COW flush 建议仍有效。**

### Trigram 路径——全面冷态

| Query | R197 | R198 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 248ms | **1567ms** | **↑532%** | **>500ms** |
| `hello world` | 342ms | **371ms** | ↑8% | 冷态 |
| `*.swift` | 207ms | **680ms** | **↑229%** | **>500ms** |
| `main.cpp` | 27ms | **72ms** | ↑167% | 正常 |
| `abc def...` | 194ms | **436ms** | ↑125% | 冷态 |
| `README` | 188ms | **105ms** | ↓44% | 正常 |
| `test` | 172ms | **270ms** | ↑57% | 冷态 |
| `MacEverything` | 21ms | **28ms** | ↑33% | 极佳 |
| Trigram 均值 | 175ms | **441ms** | **↑152%** | 全面冷态 |

- **Trigram 均值 175→441ms（↑152%）——全面恶化。**
- **`.pdf`=1567ms 和 `*.swift`=680ms 都 >500ms——两个高 candidates 查询同时受灾。**
- **`README`=105ms（R197=188ms，↓44%）——部分区域保持温态。**

### 全表扫描——仍在高位但低于 R197

| Query | R197 | R198 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 2158ms | **148ms** | **↓93%（恢复！）** |
| `path:/Users` | 1803ms | **1072ms** | ↓41% |
| `中文测试` | 1406ms | **765ms** | ↓46% |
| **总和** | **5367ms** | **1985ms** | **↓63%** |

- **`content:import`=148ms——从 2158ms（lockWait=567ms）恢复至正常！**
- **全表扫描总和 5367→1985ms（↓63%）——主要因 lockWait 消散和 content:import 恢复。**
- **`path:/Users` 和 `中文测试` 仍偏高但在恢复趋势中。**

### tombstone 24.71%——深夜增速极低

- **tombstone=1,695,242（24.71%）。**
- **增速 ~78/min（R197=246/min，↓68%）——深夜极低！**
- **距 25% 需要额外 7,958 tombstone。**
- **按 78/min 计算：~102 分钟 ≈ 1.7 小时——但实际日志已到 23:45。**
- **⚠️ 此前预计 21:10 触发的预测未实现——夜间增速远低于午后。**
- **修正预测：按深夜 ~50-80/min，距 25% 约 2-3 小时（约 01:00-02:00）。**
- **或可能一直到明天上午活动恢复时才触发。**

### tombstone 增速趋势（完整——含夜间）

| 轮次 | 时间 | 增速(/min) | tombstone% |
|------|------|----------:|----------:|
| R193 | 18:16 | 195 | 24.40% |
| R194 | 18:46 | 214 | 24.46% |
| R195 | 19:16 | 199 | 24.54% |
| R196 | 19:46 | 223 | 24.62% |
| R197 | 20:16 | 246 | 24.67% |
| **R198** | **20:46** | **78** | **24.71%** |

- **深夜增速急剧降至 78/min——文件操作几乎停止。**
- **25% 阈值可能要到明天日间活动恢复后才触发。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 119.2 | 0.0 | short-query-cache | 0 | 0 | **进程延迟 119ms** |
| `.pdf` | 5 | 1567.0 | 1556.7 | advanced-trigram | 0 | 9842 | **>500ms** |
| `main.cpp` | 5 | 72.4 | 54.4 | advanced-trigram | 2.6 | 82 | 正常 |
| `*.swift` | 5 | 680.4 | 670.1 | advanced-trigram | 0 | 9345 | **>500ms** |
| `ext:cpp` | 5 | 182.5 | 163.8 | advanced-ext-index | 12.0 | 3571 | 正常 |
| `size:>100m` | 5 | 181.7 | 171.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 370.9 | 348.9 | advanced-trigram | 0 | 3639 | 冷态 |
| `abc def ghi jkl mno pqr` | 0 | 435.6 | 419.2 | advanced-trigram | 0 | 14334 | 冷态 |
| `README` | 5 | 104.7 | 74.0 | advanced-trigram | 0 | 16725 | 正常 |
| `test` | 5 | 269.7 | 170.8 | advanced-trigram | 0 | 116732 | 冷态 |
| `MacEverything` | 5 | 28.0 | 1.7 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 89.8 | 66.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 147.8 | 138.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（恢复！） |
| `path:/Users` | 5 | 1071.7 | 1025.4 | advanced-linear-gcd | 0 | 0 | **>500ms** |
| `中文测试` | 0 | 764.6 | 736.4 | advanced-linear-gcd | 0 | 0 | **>500ms** |
| `verylongquery...` | 0 | 2.7 | 0.0 | advanced-trigram | 2.5 | 0 | 极佳 |

**P50 = 181.7ms，P90 = 1071.7ms，Max = 1567.0ms。4/16 查询 >500ms。lockWait max = 12.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R197 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 1/16 | 2.7-28ms | R197=2/16（↓1） |
| **正常**（50-500ms） | 11/16 | 72-436ms | R197=10/16（↑1） |
| **超标**（>500ms） | **4/16** | 680-1567ms | R197=4/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,832,535，liveRecordCount=5,137,293（tombstone=**1,695,242**，**24.71%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=308  QUERYADV_GE500=29（+4！）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4071
```

- **QUERYADV_GE500 +4——说明 battery 之外的查询也出现了 >500ms 事件。**
- **COMPACTION 仍为 2——compaction 尚未触发。**

## 6. 问题清单

### P0 紧急：tombstone 24.71%——但深夜增速极低

- **距 25% 仅 0.29 个百分点（7,958 tombstone）。**
- **深夜增速 78/min——可能要到明天日间才触发。**
- **修正预测：明天上午活动恢复后（~09:00-10:00）触发。**

### 观察项：连续 2 轮 4/16 >500ms——夜间 page cache 压力

- **R197 和 R198 连续 2 轮 4/16 >500ms——但机制不同。**
- **R197：lockWait=567ms 主导。R198：page cache 淘汰+进程调度延迟主导。**
- **夜间可能有系统级维护任务（Time Machine、Spotlight、macOS 后台任务）大量占用 I/O。**

### `x`=119ms——进程调度延迟极端

- **`x` 使用纯内存缓存，119ms = ~117ms 调度延迟。**
- **证明系统级 I/O 密集操作严重挤压进程资源。**

## 7. 优化建议

### P0：compaction rewrite 崩溃修复

- **tombstone 24.71%，明天上午可能触发。**
- **现在有更长的时间窗口（深夜增速极低）——但仍需修复。**

### P0.5：flush rewrite 改为 COW 方式

- **R197 的 lockWait=567ms 是最强证据。**
- **R198 的 lockWait=12ms 证明 R197 是一次性事件，但轻度竞争持续存在。**

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. 4/16 >500ms 是否恢复（预计需 1-2 轮）。
2. `x` 的进程调度延迟是否恢复至 <10ms。
3. tombstone 是否在深夜继续缓慢增长。
4. COMPACTION 计数是否仍为 2。

## 9. 结论

**连续第 2 轮重度事件——4/16 >500ms，Max=1567ms，lockWait=12ms（从 567ms 恢复），`x`=119ms 暴露极端进程调度延迟。** `.pdf`=1567ms（系列第二高），`path:/Users`=1072ms，`中文测试`=765ms，`*.swift`=680ms。机制从 R197 的 flush 锁竞争转为全面 page cache 淘汰+系统级资源竞争。夜间可能有系统维护任务（Time Machine/Spotlight）大量占用 I/O。

**tombstone 24.71%——深夜增速降至 78/min，compaction 推迟至明天上午。** 此前预计 21:10 触发的预测未实现——夜间增速远低于午后。P0 compaction 修复有更长时间窗口，但仍需尽快处理。
