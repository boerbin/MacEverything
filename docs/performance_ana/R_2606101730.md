# MacEverything 性能分析报告 R_2606101730

## 1. 本轮范围

- 任务启动时间：2026-06-10 17:30
- 报告文件：`docs/performance_ana/R_2606101730.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9131`
  - `SEG_LINES=8189`
  - `LAST_LOG_TS=2026-06-10 15:47`

## 2. 执行摘要

**⭐ `x` 恢复 short-query-cache！17ms（第 5 轮重建成功）。零和翻转——trigram 回升，全表扫描大幅改善。** 0/16 >500ms，Max=159ms（**post-compaction 最佳！**），P50=29.6ms，lockWait=0（连续 4 轮）。**极佳 11/16。** `x` 从 R257-R260 连续 4 轮 linear-gcd 回归 short-query-cache（17ms vs R260=246ms——↓93%）。全表扫描总和 427ms（↓34% from R260=648ms——显著改善）。部分 trigram 因零和效应回升（`.pdf`=67ms, `hello world`=59ms）。FLUSH 21→26（+5）。tombstone=42,626（0.81%——健康）。COMPACTION 4→6（+2——新增 compaction 日志事件）。

| 指标 | R260 | R261 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 266ms | **159ms** | **↓40%（post-compaction 最佳！）** |
| P50 | 11ms | **29.6ms** | ↑169%（零和翻转） |
| lockWait max | 0ms | **0ms** | 完美（连续 4 轮） |
| 极佳比例(<50ms) | 12/16 | **11/16** | ↓1（零和翻转） |
| tombstone | 26,599 (0.50%) | **42,626 (0.81%)** | ↑（正常增长） |
| recordCount | 5,268,501 | **5,286,245** | +17.7K（偏高） |
| liveRecordCount | 5,241,902 | **5,243,619** | +1,717（正常） |
| FLUSH 累计 | 21 | **26** | +5（首次非 +6） |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 4 | **6** | +2（新增日志事件） |
| QUERYADV_GE500 | 75 | **75** | 不变 |

### ⭐ 里程碑：`x` 恢复 short-query-cache！

| 轮次 | `x` 路径 | `x` 时间 | 说明 |
|------|---------|--------:|------|
| R256 | short-query-cache | 1ms | compaction 前 |
| R257 | advanced-linear-gcd | 587ms | compaction 导致缓存失效 |
| R258 | advanced-linear-gcd | 297ms | 未重建 |
| R259 | advanced-linear-gcd | 260ms | 未重建 |
| R260 | advanced-linear-gcd | 246ms | 未重建（确认缺陷） |
| **R261** | **short-query-cache** | **17ms** | **✅ 恢复！第 5 轮重建** |

- **经过 4 轮 linear-gcd 后，short-query-cache 终于重建！**
- **17ms vs 246ms（↓93%）——缓存命中带来数量级改善。**
- **重建延迟分析：**
  1. **高频 flush（每轮 +5~6）导致缓存反复失效——但最终积累足够查询次数触发重建。**
  2. **或者：cache 重建有固定延迟/冷却期——4 轮约 2 小时。**
  3. **P1 缺陷部分缓解：缓存最终能重建，但 4 轮延迟仍不理想。**
- **修正评级：P1→P2（可重建，但重建延迟需优化）。**

### 零和翻转——trigram ↑，全表扫描 ↓

**`x` 恢复 short-query-cache 意味着缓存重建过程预热了 SoA 数据区域，改善全表扫描性能，但部分 trigram posting list 页被淘汰。**

#### Trigram 路径——零和回升

| Query | R260 | R261 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 3ms | **67ms** | ↑2133%（零和淘汰） | 正常 |
| `hello world` | 8ms | **59ms** | ↑638% | 正常 |
| `*.swift` | 6ms | **36ms** | ↑500% | 极佳 |
| `main.cpp` | 5ms | **15ms** | ↑200% | 极佳 |
| `abc def...` | 5ms | **30ms** | ↑500% | 极佳 |
| `README` | 6ms | **6ms** | 持平 | 极佳 |
| `test` | 18ms | **14ms** | ↓22% | 极佳 |
| `MacEverything` | 18ms | **7ms** | ↓61% | 极佳 |
| Trigram phase2均值 | ~2.6ms | **~25ms** | ↑（零和效应） |

- **`.pdf`=67ms 和 `hello world`=59ms——大 postings 集的 page cache 被淘汰。**
- **小 postings 集仍极佳：README=6ms, MacEverything=7ms, test=14ms。**
- **这是经典零和模式——预计下轮 battery 查询将重新预热 trigram 页面并恢复。**

#### 全表扫描——显著改善

| Query | R260 | R261 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 152ms | **118ms** | **↓22%** |
| `path:/Users` | 230ms | **150ms** | **↓35%** |
| `中文测试` | 266ms | **159ms** | **↓40%** |
| **总和** | **648ms** | **427ms** | **↓34%** |

- **全表扫描 648→427ms（↓34%）——SoA 数据区域 page cache 改善。**
- **`中文测试`=159ms（↓40%）——post-compaction 全表扫描新稳态接近 150-200ms。**
- **Max=159ms（post-compaction 最佳！R257=587ms, R258=297ms, R259=260ms, R260=266ms, R261=159ms）。**

### 零和总量守恒验证

| 类别 | R260 | R261 | 变化 |
|------|-----:|-----:|------|
| Trigram 总和 | ~64ms | ~234ms | +170ms |
| 全表扫描总和 | 648ms | 427ms | -221ms |
| **Battery 总和** | ~1024ms | ~765ms | **↓25%** |

- **零和翻转但总量下降——整体性能改善。**
- **全表扫描改善（-221ms）大于 trigram 回升（+170ms）——净改善 51ms。**
- **说明 compaction 后紧凑索引的全局效益正在显现。**

### 辅助路径

| Query | R260 | R261 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 246ms | **17ms** | **↓93%（恢复！）** | 极佳 |
| `ext:cpp` | 1ms | **12ms** | ↑（零和波动） | 极佳 |
| `size:>100m` | 11ms | **29ms** | ↑164% | 极佳 |
| `dm:today` | 55ms | **46ms** | ↓16% | 极佳 |
| `verylongquery...` | 0.2ms | **0.2ms** | 持平 | 极佳 |

### FLUSH +5 & COMPACTION +2

- **FLUSH 21→26（+5——首次非 +6，可能因为一个 tick 的 WAL entries < 100）。**
- **COMPACTION 4→6（+2——tombstone 0.81% 远低于 25% 阈值，这 2 条可能是 compaction check 日志而非实际 compaction）。**
- **recordCount +17.7K（偏高——大量文件系统活动）。**
- **liveRecordCount +1.7K（正常——大部分新 records 替换了旧版本，产生 tombstone）。**
- **tombstone 增速：(42,626-26,599)/~30min ≈ 534/min（偏高，因 recordCount +17.7K）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 17.1 | 0.0 | short-query-cache | 0 | 0 | **⭐ 恢复！** |
| `.pdf` | 5 | 66.7 | 60.9 | advanced-trigram | 0 | 9783 | 正常（零和） |
| `main.cpp` | 5 | 15.2 | 7.2 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 35.5 | 33.3 | advanced-trigram | 0 | 9402 | 极佳 |
| `ext:cpp` | 5 | 12.1 | 11.6 | advanced-ext-index | 0 | 3612 | 极佳 |
| `size:>100m` | 5 | 29.4 | 28.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 58.9 | 53.1 | advanced-trigram | 0 | 3929 | 正常（零和） |
| `abc def ghi jkl mno pqr` | 0 | 29.8 | 29.2 | advanced-trigram | 0 | 14450 | 极佳 |
| `README` | 5 | 5.8 | 3.6 | advanced-trigram | 0 | 17430 | 极佳 |
| `test` | 5 | 14.2 | 12.2 | advanced-trigram | 0 | 117720 | 极佳 |
| `MacEverything` | 5 | 6.6 | 0.6 | advanced-trigram | 0 | 6923 | 极佳 |
| `dm:today` | 5 | 46.1 | 46.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 118.0 | 107.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 150.2 | 143.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 158.7 | 158.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 29.6ms，P90 = 150.2ms，Max = 158.7ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R260 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **11/16** | 0.2-46ms | R260=12/16（↓1，零和翻转） |
| **正常**（50-500ms） | 5/16 | 59-159ms | R260=4/16（↑1） |
| **超标**（>500ms） | **0/16** | — | R260=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,286,245，liveRecordCount=5,243,619（tombstone=**42,626**，**0.81%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=674  QUERYADV_GE500=75（不变）
FLUSH=26（+5）  COMPACTION=6（+2）  SKIPFLUSH=7266
```

## 6. 问题清单

### P0：flush 写锁阻塞

- **FLUSH +5（连续 4 轮高频 flush——26 次累计）。**

### P0：compaction 触发机制缺陷（根因未修复）

### P2（降级）：short-query-cache 重建延迟

- **缓存在第 5 轮（R261）恢复——确认可自动重建。**
- **但 4 轮延迟（~2 小时）仍不理想——降级为 P2。**
- **建议：flush 后增量更新 cache 而非全量失效，减少重建延迟。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（增加独立定时检查）

### P0：进程 QoS / I/O 优先级保护

### P2：short-query-cache 重建优化（flush 后增量更新）

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. `x` 缓存是否稳定维持 short-query-cache。
2. 零和翻转后 trigram 是否回恢复（`.pdf`, `hello world`）。
3. tombstone 增速趋势（0.81%，534/min——偏高，与 recordCount +17.7K 相关）。
4. COMPACTION +2 是否为实际 compaction 事件。

## 9. 结论

**⭐ `x` 恢复 short-query-cache——第 5 轮重建成功！** 17ms（↓93% from R260=246ms）。连续 4 轮 linear-gcd 后缓存终于重建——P1 缺陷降级为 P2（可重建，重建延迟需优化）。0/16 >500ms，Max=159ms（**post-compaction 最佳！**），lockWait=0（连续 4 轮）。

**零和翻转：trigram ↑（.pdf 3→67ms），全表扫描 ↓（648→427ms，↓34%）。** 缓存重建预热了 SoA 数据区域，部分 trigram posting list 页被淘汰——经典零和模式，预计下轮恢复。**整体性能仍改善——battery 总和 1024→765ms（↓25%），Max=159ms 创 post-compaction 新低。**

FLUSH +5（21→26），COMPACTION +2（4→6）。tombstone=42,626（0.81%——健康）。recordCount +17.7K（偏高——文件系统活动活跃期）。
