# MacEverything 性能分析报告 R_2606031309

## 1. 本轮范围

- 任务启动时间：2026-06-03 13:09
- 报告文件：`docs/performance_ana/R_2606031309.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21289`
  - `SEG_LINES=263`
  - `LAST_LOG_TS=2026-06-03 14:41`

## 2. 执行摘要

服务正常运行（第 11 轮连续可用）。`x` 短查询缓存恢复正常（19ms，确认前轮 254ms 为偶发）。`.pdf`=490ms 连续第 2 轮逼近 500ms 阈值。所有查询连续第 8 轮 < 500ms。**tombstone 逼近 2% 临界点**（114,858 条，1.97%），预计下一轮突破。

### 横向对比（最近 5 轮）

| Query | R55 | R56 | R57 | R58 | R59 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 4 | 5 | 4 | 254 | **19** | 恢复正常 |
| `.pdf` | 115 | 98 | 137 | 488 | **490** | **连续 2 轮逼近 500ms** |
| `hello world` | 128 | 70 | 326 | 425 | **430** | 偏慢 |
| `path:/Users` | 167 | 250 | 497 | 312 | **164** | 恢复 |
| `ext:cpp` | — | 53 | 89 | 89 | **245** | 异常偏慢 |
| P50 | 56 | 52 | 76 | 163 | **176** | 偏慢 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 18.9 | 0.0 | short-query-cache | 0 | 0 | 恢复正常 |
| `.pdf` | 5 | 489.8 | 479.4 | advanced-trigram | 0 | 9944 | **逼近 500ms** |
| `main.cpp` | 5 | 53.5 | 34.8 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 226.4 | 217.1 | advanced-trigram | 0 | 9356 | 偏慢 |
| `ext:cpp` | 5 | 245.0 | 241.3 | advanced-ext-index | 0 | 3702 | 偏慢 |
| `size:>100m` | 5 | 214.4 | 209.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 偏慢 |
| `hello world` | 5 | 429.9 | 418.4 | advanced-trigram | 0 | 3613 | 偏慢 |
| `abc def ghi jkl mno pqr` | 0 | 176.4 | 170.5 | advanced-trigram | 0 | 15972 | 正常 |
| `README` | 5 | 21.9 | 16.0 | advanced-trigram | 0 | 17717 | 快 |
| `test` | 5 | 50.7 | 28.4 | advanced-trigram | 0 | 129960 | 正常 |
| `MacEverything` | 5 | 21.8 | 1.3 | advanced-trigram | 0 | 6802 | 快 |
| `dm:today` | 5 | 76.7 | 76.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 173.8 | 158.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 164.1 | 159.9 | advanced-linear-gcd | 0 | 0 | 恢复 |
| `中文测试` | 0 | 190.9 | 190.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 176ms，P90 ≈ 430ms，Max = 490ms。全部查询连续第 8 轮 < 500ms。lockWaitMs 全部为 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,838,974，liveRecordCount=5,724,116（tombstone=**114,858**，**1.97%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=84  QUERYADV_GE500=5
FLUSH=1  COMPACTION=2  SKIPFLUSH=60
DIAG_TRIGRAM_SKIP=64
```

- QUERYADV 从 75 增至 84（本轮 battery 贡献 9 次日志记录）。
- QUERYADV_GE500 保持 5（`.pdf` 490ms 未超阈值）。
- SKIPFLUSH 从 48 增至 60（+12，间隔约 5 分钟持续跳过）。
- DIAG_TRIGRAM_SKIP 从 63 增至 64（+1）。

## 6. 问题清单

### Critical：tombstone 逼近 2% 临界点（1.97%，114,858 条）

- 证据：十一轮轨迹 4K → 17K → 45K → 67K → 83K → 101K → **115K**。
- 本轮增量 13,862 条（前轮 17,682 条）。
- 占比 1.97%，**下一轮几乎必定突破 2%**。
- SKIPFLUSH=60，flush-triggered compaction 完全休眠。
- recordCount 增速约 13K/轮，liveRecordCount 仅增 ~1.4K/轮，差值即新 tombstone。
- **tombstone 增长模式**：FSWatcher 捕获的文件变更（删除+重建）产生 tombstone，而 WAL flush 阈值（100）未被触及，compaction 无法自然触发。

### High：`.pdf` 连续 2 轮逼近 500ms（488ms → 490ms）

- 前两轮：488ms、490ms。candidates=9944 不变。
- phase2=479ms 占总延迟 98%。I/O 负载偏高时 phase2 验证变慢。
- 结构性根因未变（trigram 候选过多）。

### Medium：`ext:cpp` 异常偏慢（245ms，历史通常 50-90ms）

- phase2=241ms，candidates=3702。
- 可能与本轮整体 I/O 负载有关，非结构性问题。

### Medium：`x` 前轮异常已恢复

- 本轮 19ms，确认 R58 的 254ms 为偶发。short-query-cache 路径正常。

### Low：SKIPFLUSH 加速（+12/轮）

- SKIPFLUSH 间隔从 5 分钟缩短，但 WAL 条目仍为 68，不影响功能。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（极度紧急）

- tombstone 1.97%，**下一轮即将突破 2%**。
- SKIPFLUSH=60，flush-triggered compaction 完全不可用。
- **建议立即实现**：定时检查 `(recordCount - liveRecordCount) / recordCount`，超过阈值（如 1.5%）时自动触发增量 compaction。
- 不实施此修复，tombstone 将无限增长直到影响查询性能和内存使用。

### P0：补齐进程生命周期监控（承前轮）

### P1：将 `.xxx` 扩展名模式路由到 ext-index（承前轮）

- `.pdf` 连续 2 轮逼近 500ms，路由到 ext-index 可立即将延迟降至 ~50-100ms。

### P2：降低 phase2 对 I/O 负载的敏感度（承前轮）

## 8. 下一轮重点

1. **tombstone 是否突破 2%** — 这是最关键的观察指标。
2. `.pdf` 是否突破 500ms，终结 <500ms 连续记录。
3. 确认 `ext:cpp` 偏慢是否持续。

## 9. 结论

服务连续第 11 轮正常运行，所有查询连续第 8 轮 <500ms（`.pdf`=490ms 仅差 10ms）。`x` 短查询缓存恢复正常（19ms），确认前轮 254ms 为偶发。**最紧迫问题是 tombstone 逼近 2% 临界点（114,858 条，1.97%），下一轮几乎必定突破**。SKIPFLUSH=60 意味着 flush-triggered compaction 完全休眠，必须引入基于占比的主动 compaction 触发机制。`.pdf` 连续 2 轮逼近 500ms（488→490ms），ext-index 路由优化的收益也愈发明显。
