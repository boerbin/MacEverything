# MacEverything 性能分析报告 R_2606130717

## 1. 本轮范围

- 任务启动时间：2026-06-13 07:17
- 报告文件：`docs/performance_ana/R_2606130717.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=9244`
  - `SESSION_START_TS=2026-06-10 17:02:22.835`
  - `TOTAL_LINES=11724`
  - `SEG_LINES=2481`
  - `LAST_LOG_TS=2026-06-13 07:17`

## 2. 执行摘要

**本轮前台仍未超标：16-query battery 中 0/16 >500ms，Max 从 372.6ms 降至 338.1ms。** P50 从约 33.1ms 降至约 20.8ms，P90 从约 303.1ms 降至约 251.7ms；大多数 trigram/indexed 查询继续保持低延迟，但扫描类路径仍构成尾部。最高耗时为 `content:import`=338.1ms，其次是 `dm:today`=251.7ms、`path:/Users`=250.2ms、`中文测试`=249.6ms。

本轮最明显的新问题是锁等待放大：`content:import` 出现 lockWait=67.6ms，top-level=338.1ms、phase2=249.6ms，说明除了扫描成本外，查询还被后台写入/flush/update 路径阻塞了一段可感知时间。后台结构性风险继续恶化：recordCount=6,062,012，liveRecordCount=5,293,706，tombstone=768,306（12.67%），较上一轮增加 7,030。latest session `FLUSH=646`（较上一轮 +12），`SKIPFLUSH=0`。最新 flush 从 3444ms 升至 4203ms，仍是秒级完整重写并继续污染 page cache。

| 指标 | R_2606130647 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 372.6ms | **338.1ms** | ↓9% |
| P50 | ≈33.1ms | **≈20.8ms** | ↓37% |
| P90 | ≈303.1ms | **≈251.7ms** | ↓17% |
| lockWait max | 0.5ms | **67.6ms** | 明显恶化 |
| 极佳比例(<50ms) | 10/16 | **12/16** | +2 |
| 全表扫描总和 | 1178.8ms | **1119.5ms** | ↓5% |
| recordCount | 6,054,805 | **6,062,012** | +7,207 |
| liveRecordCount | 5,293,529 | **5,293,706** | +177 |
| tombstone | 761,276 (12.57%) | **768,306 (12.67%)** | +7,030 |
| 最新段 FLUSH | 634 | **646** | +12 |
| 最新段 QUERYADV_GE500 | 12 | **12** | 无新增 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 2.3 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 18.5 | 15.8 | advanced-trigram | 0.0 | 9858 | 极佳 |
| `main.cpp` | 5 | 8.2 | 1.2 | advanced-trigram | 0.0 | 93 | 极佳 |
| `*.swift` | 5 | 13.6 | 10.5 | advanced-trigram | 0.0 | 9517 | 极佳 |
| `ext:cpp` | 5 | 13.5 | 13.1 | advanced-ext-index | 0.0 | 3736 | 极佳 |
| `size:>100m` | 5 | 29.9 | 29.8 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 36.3 | 16.2 | advanced-trigram | 0.0 | 3932 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 29.9 | 29.8 | advanced-trigram | 0.0 | 14594 | 极佳 |
| `README` | 5 | 13.3 | 7.3 | advanced-trigram | 0.0 | 17532 | 极佳 |
| `test` | 5 | 23.0 | 14.3 | advanced-trigram | 0.0 | 117911 | 极佳 |
| `MacEverything` | 5 | 13.7 | 0.9 | advanced-trigram | 0.0 | 7004 | 极佳 |
| `dm:today` | 5 | 251.7 | 205.1 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常（高位） |
| `content:import` | 5 | 338.1 | 249.6 | advanced-pure-filter-soa-gcd | 67.6 | 0 | 正常（本轮最高，明显 lockWait） |
| `path:/Users` | 5 | 250.2 | 240.9 | advanced-linear-gcd | 0.0 | 0 | 正常（偏高） |
| `中文测试` | 0 | 249.6 | 249.2 | advanced-linear-gcd | 0.0 | 0 | 正常（偏高） |
| `verylongquery...` | 0 | 0.7 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈20.8ms，P90≈251.7ms，Max=338.1ms。0/16 查询 >500ms。lockWait max=67.6ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **12/16** | 0.7-36.3ms | short-query-cache、trigram、ext-index 与轻量 pure-filter 表现健康 |
| 正常（50-500ms） | **4/16** | 249.6-338.1ms | pure-filter 与 linear-gcd 扫描类路径构成本轮尾部 |
| 超标（>500ms） | **0/16** | — | 本轮无前台超标查询 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,062,012；
- liveRecordCount=5,293,706；
- tombstone=768,306（**12.67%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=9244
SESSION_START_TS=2026-06-10 17:02:22.835
TOTAL_LINES=11724
SEG_LINES=2481
QUERYADV=201
QUERYADV_GE500=12
FLUSH=646
SKIPFLUSH=0
COMPACTION=2
LAST_LINE=[2026-06-13 07:17:57.193] QueryAdvanced "中文测试" total=249ms | path=advanced-linear-gcd
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 12，本轮没有新增日志级慢查询。最近新增慢查询仍是 04:47 的 `中文测试` 608ms；本轮主要变化不是新增慢查询，而是 `content:import` 的 lockWait=67.6ms 暴露出前台查询和后台写入/flush/update 的交叠风险。

```text
[2026-06-10 17:17:09.559] QueryAdvanced "path:/Users" total=525ms | path=advanced-linear-gcd
[2026-06-10 17:25:15.434] QueryAdvanced "lllllllllllllllllllllllllllllllllllllllllllll" total=1154ms | path=advanced-trigram
[2026-06-10 17:47:13.642] QueryAdvanced "中文测试" total=716ms | path=advanced-linear-gcd
[2026-06-10 23:44:25.566] QueryAdvanced "test" total=676ms | path=advanced-trigram
[2026-06-12 09:17:32.430] QueryAdvanced "中文测试" total=555ms | path=advanced-linear-gcd
[2026-06-12 13:53:09.945] QueryAdvanced ".pdf" total=1955ms | path=advanced-trigram
[2026-06-12 13:53:12.523] QueryAdvanced "path:/Users" total=590ms | path=advanced-linear-gcd
[2026-06-12 14:48:11.578] QueryAdvanced "hello world" total=760ms | path=advanced-trigram
[2026-06-12 16:21:11.617] QueryAdvanced ".pdf" total=529ms | path=advanced-trigram
[2026-06-12 16:21:12.485] QueryAdvanced "ext:cpp" total=591ms | path=advanced-ext-index
[2026-06-12 16:21:16.446] QueryAdvanced "hello world" total=3724ms | path=advanced-trigram
[2026-06-13 04:47:58.351] QueryAdvanced "中文测试" total=608ms | path=advanced-linear-gcd
```

### flush 极端长尾

最新 session 内 `FLUSH=646`，rewrite 范围 **1380ms - 2285476ms**，total 最大 **2285536ms**。

最新 flush：

```text
[2026-06-13 07:16:46.440] Flushed v6 flat index, lastEventId=357174038,
liveRecords=5293693 | timing: walSwap=0ms rewrite=4198ms sqcache=2ms total=4203ms
```

Top flush outliers：

```text
[2026-06-12 13:02:35.416] rewrite=2285476ms sqcache=6ms total=2285536ms
[2026-06-12 03:15:02.833] rewrite=895940ms sqcache=2ms total=896053ms
[2026-06-10 23:24:50.749] rewrite=725091ms sqcache=18ms total=735223ms
[2026-06-12 19:06:52.432] rewrite=106972ms sqcache=34ms total=107008ms
[2026-06-11 14:30:37.596] rewrite=58273ms sqcache=5ms total=101726ms
```

## 6. 问题清单

### P0：lockWait 明显放大

- 证据：`content:import` lockWait=67.6ms，top-level=338.1ms、phase2=249.6ms。
- 影响：前台查询出现可感知等待，说明后台写入/flush/update 与查询路径存在交叠。
- 判断：这不是本轮 Max 的唯一来源，但它把 pure-filter 查询额外推高近 70ms，是需要优先关联时序的信号。

### P0：tombstone 继续在 12% 以上上行

- 证据：tombstone=768,306（12.67%），较上一轮增加 7,030。
- 影响：无效记录比例继续升高，会放大扫描成本、索引重写成本与 compaction 压力。
- 判断：tombstone 已接近 12.7%，清理/compaction 触发明显偏晚。

### P0：flush 高频且没有退避

- 证据：`FLUSH=646`，较上一轮 +12；`SKIPFLUSH=0`；最新 flush total=4203ms。
- 影响：完整重写持续污染 page cache，也可能解释本轮 lockWait 放大和扫描路径高位波动。

### P0：latest session 仍包含灾难级 flush outlier

- 证据：13:02 flush total=2,285,536ms，03:15 flush total=896,053ms，23:24 flush total=735,223ms，19:06 flush total=107,008ms。
- 影响：持久化路径缺少切分、退避和前台保护，是当前最主要的系统性风险。

### P1：扫描类路径仍在中高位

- 证据：`content:import`=338.1ms，`dm:today`=251.7ms，`path:/Users`=250.2ms，`中文测试`=249.6ms。
- 影响：本轮未超标，但 scanning/filtering 仍占据 P90/Max，并随后台状态波动。

## 7. 优化建议

### P0：补充 lockWait 与后台状态关联观测

- 记录每次查询锁等待对应的 flush/update 状态、持锁线程、等待阶段和持锁耗时。
- 验收：确认 `content:import` 的 67.6ms lockWait 是 flush、索引更新还是其他写路径导致。

### P0：立即提前或分段 compaction

- 不等待更高的全局 tombstone 阈值；结合绝对 tombstone 数、segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 优先处理 tombstone 密集 segment，避免 scan/filter 路径继续扫描大量无效记录。
- 验收：tombstone 率从 12%+ 回落，`content:import`、`中文测试` 与 `path:/Users` 稳定 <300ms。

### P0：flush 改为增量/COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页或持锁窗口。
- 验收：常规 flush total <200ms，latest session 不再出现分钟级 rewrite outlier，lockWait 不再出现 50ms+。

### P0：增加 flush I/O 压力退避和前台保护

- 当最近 flush 超过阈值时，提高下一次 flush 间隔或降低 batch 频率。
- 前台查询活跃或 I/O 延迟升高时推迟大 flush。
- 持久化线程使用低 QoS，避免冲掉搜索路径 page cache。

### P1：为 scan/filter 路径增加剪枝

- 对中文、路径、content/filter 类查询记录扫描记录数和跳过原因，评估轻量字符 n-gram、路径分段、预过滤位图或 segment-level skip list。
- 要求保持内存增长可控，优先使用按需或压缩结构，而不是常驻大索引。
- 验收：`content:import`、`path:/Users`、`中文测试` 稳定低于 300ms，并能解释每次扫描规模。

### P1：拆分扫描路径和 trigram 路径耗时

- 对扫描路径记录扫描记录数、tombstone 跳过数、过滤耗时、排序耗时和结果组装耗时。
- 对 trigram 路径记录 postings 读取、候选集交并、排序、路径加载和结果组装耗时。
- 验收：能解释 scan/filter 250ms+ 与 trigram 偶发差值。

### P2：保护搜索热数据 page cache

- 对 flush 大顺序写考虑 `F_NOCACHE` 或同类策略。
- 对 full-scan/SoA/postings 热区使用预热或访问提示，减少 flush 后冷启动波动。

## 8. 下一轮重点

1. lockWait 是否继续出现，尤其是否再次超过 50ms。
2. tombstone 是否继续从 12.67% 向 12.8% 增长。
3. `content:import` 是否继续是 Max 或突破 500ms。
4. 最新 flush 是否继续维持 4 秒级，或出现新的分钟级 outlier。
5. `QUERYADV_GE500` 是否继续增加。

## 9. 结论

**本轮前台未超标：0/16 >500ms，Max=338.1ms，P50≈20.8ms，P90≈251.7ms。** 但 `content:import` 出现 lockWait=67.6ms，是本轮最值得关注的新信号，说明后台写入/flush/update 已经对前台查询产生可感知等待。

**后台根因继续恶化：tombstone=768,306（12.67%），`FLUSH=646`，最新 flush=4203ms，latest session 仍保留 total=2,285,536ms、896,053ms、735,223ms、107,008ms 的极端 outlier。** 下一步应优先推进 lockWait 关联观测、提前/分段 compaction、增量/COW flush、flush 退避，以及 scan/filter 路径剪枝。
