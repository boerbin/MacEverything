# MacEverything 性能分析报告 R_2606031254

## 1. 本轮范围

- 任务启动时间：2026-06-03 12:54
- 报告文件：`docs/performance_ana/R_2606031254.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21252`
  - `SEG_LINES=226`
  - `LAST_LOG_TS=2026-06-03 13:41`

## 2. 执行摘要

服务正常运行（第 9 轮连续可用）。本轮性能从前轮历史最佳有所回落：P50 ≈ 76ms（前轮 52ms），`path:/Users`=497ms（勉强保持 <500ms），`hello world`=326ms（前轮 70ms），`size:>100m`=165ms（前轮 40ms）。所有 battery 查询连续第 6 轮 < 500ms（但 `path:/Users` 仅差 3ms）。tombstone 继续增长至 82,996 条（1.43%）。

### 九轮横向对比（选取关键 query）

| Query | R52 | R53 | R54 | R55 | R56 | R57 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 21 | 6.5 | 16 | 4 | 5 | **4** | 稳定极快 |
| `.pdf` | 446 | 229 | 313 | 115 | 98 | **137** | 正常波动 |
| `hello world` | 334 | 177 | 270 | 128 | 70 | **326** | 回升明显 |
| `path:/Users` | 201 | 438 | 132 | 167 | 250 | **497** | 接近阈值 |
| `size:>100m` | 64 | 54 | 101 | 69 | 40 | **165** | 波动 |
| P50 | 93 | 65 | 95 | 56 | 52 | **76** | 回升 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 4.2 | 0.0 | short-query-cache | 0 | 0 | 极快 |
| `.pdf` | 5 | 137.3 | 133.4 | advanced-trigram | 0 | 9944 | 正常 |
| `main.cpp` | 5 | 14.0 | 9.6 | advanced-trigram | 0 | 85 | 极快 |
| `*.swift` | 5 | 95.5 | 92.0 | advanced-trigram | 0 | 9356 | 正常 |
| `ext:cpp` | 5 | 58.6 | 57.8 | advanced-ext-index | 0 | 3702 | 快 |
| `size:>100m` | 5 | 165.4 | 164.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 偏慢 |
| `hello world` | 5 | 326.0 | 317.8 | advanced-trigram | 0 | 3613 | 回升 |
| `abc def ghi jkl mno pqr` | 0 | 67.9 | 67.1 | advanced-trigram | 0 | 15984 | 正常 |
| `README` | 5 | 56.7 | 51.2 | advanced-trigram | 0 | 17717 | 正常 |
| `test` | 5 | 48.0 | 40.0 | advanced-trigram | 0 | 129960 | 正常 |
| `MacEverything` | 5 | 7.6 | 0.6 | advanced-trigram | 0 | 6802 | 极快 |
| `dm:today` | 5 | 76.4 | 76.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 215.2 | 207.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 497.1 | 466.2 | advanced-linear-gcd | 0 | 0 | **接近 500ms** |
| `中文测试` | 0 | 304.1 | 295.1 | advanced-linear-gcd | 0 | 0 | 偏慢 |
| `verylongquery...` | 0 | 0.6 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 76ms，P90 ≈ 326ms，Max = 497ms。全部查询连续第 6 轮 < 500ms（`path:/Users` 仅差 3ms）。lockWaitMs 全部为 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,806,147，liveRecordCount=5,723,151（tombstone=82,996，**1.43%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=68  QUERYADV_GE500=5
FLUSH=1  COMPACTION=2  SKIPFLUSH=41
DIAG_TRIGRAM_SKIP=62
```

- QUERYADV 从 62 增至 68（本轮 battery 贡献 6 次日志记录）。
- QUERYADV_GE500 保持 5（本轮 `path:/Users` 日志记录 471ms，未超 500ms）。
- SKIPFLUSH 从 35 增至 41（+6，WAL 条目仍为 68）。
- DIAG_TRIGRAM_SKIP 从 61 增至 62（+1，本轮 `中文测试`）。

## 6. 问题清单

### High：tombstone 增长至 1.43%（82,996 条）

- 证据：九轮轨迹持续上升，本轮增量 7,776 条。
- 占比 1.43%，从 R49 的 0.07% 已增长 20 倍。
- SKIPFLUSH=41，flush-triggered compaction 路径完全休眠。
- **预计约 10-15 轮后将突破 2% 阈值。**

### High：`path:/Users` 接近 500ms 阈值

- 证据：497ms（phase2=466ms），日志记录 471ms。距 500ms 仅差 3ms。
- 九轮数据方差极大（132-578ms），advanced-linear-gcd 路径受系统负载影响显著。
- 下一轮可能突破 500ms 连续记录。

### Medium：整体性能从历史最佳回落

- `hello world` 从 70ms 回升至 326ms（+366%）。
- `size:>100m` 从 40ms 升至 165ms（+313%）。
- `content:import` 从 165ms 升至 215ms。
- 所有查询 lockWaitMs=0，回落非锁竞争导致，更可能与系统 I/O 负载有关。

### Low：DIAG-TRIGRAM SKIP 持续累积（62 次）

- 增速稳定（+1/轮）。

## 7. 优化建议

### P0：补齐进程生命周期监控（承前轮）

### P0：实现基于占比的主动 tombstone compaction（升级为 P0）

- tombstone 1.43% 且增速不减，SKIPFLUSH 完全休眠。
- 必须引入不依赖 WAL flush 的 compaction 触发路径。
- 建议：每 N 分钟检查 `(recordCount - liveRecordCount) / recordCount`，超过 2% 时自动触发增量 compaction。

### P1：将 `.xxx` 扩展名模式路由到 ext-index（承前轮）

### P1：`path:/Users` 路径前缀索引

- 线性扫描路径波动至 497ms，已接近可用性边界。需引入路径前缀索引以降低 worst-case。

### P2：调查本轮 I/O 负载回升根因

- 无 lockWait 但多个查询延迟倍增，可能与后台 FSWatcher 批量处理或 macOS 系统活动有关。

## 8. 下一轮重点

1. 追踪 `path:/Users` 是否突破 500ms。
2. tombstone 增长——是否会在 2% 前触发自然 compaction。
3. 确认本轮性能回落是否为偶发。

## 9. 结论

服务连续第 9 轮正常运行。所有查询连续第 6 轮 <500ms，但 `path:/Users`=497ms 仅差 3ms。性能从前轮历史最佳有所回落（P50: 52→76ms），`hello world` 和 `size:>100m` 延迟倍增，lockWaitMs=0 排除锁竞争，回落与系统 I/O 负载相关。tombstone 增长至 82,996 条（1.43%），SKIPFLUSH=41 意味着 flush-triggered compaction 完全休眠，主动 tombstone 回收已从 P1 升级为 P0。
