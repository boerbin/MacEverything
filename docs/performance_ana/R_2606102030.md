# MacEverything 性能分析报告 R_2606102030

## 1. 本轮范围

- 任务启动时间：2026-06-10 20:30
- 报告文件：`docs/performance_ana/R_2606102030.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9331`
  - `SEG_LINES=8389`
  - `LAST_LOG_TS=2026-06-10 18:47`

## 2. 执行摘要

**稳定运行——I/O 风暴后连续 2 轮平稳。** 0/16 >500ms，Max=194ms，P50=34.8ms，lockWait=0。**极佳 10/16。** `x`=5ms（缓存稳定，连续 6 轮）。Trigram 保持良好：phase2 均值 ~19ms。`size:>100m`=107ms（异常偏高——通常 <40ms）。全表扫描总和 527ms（↑20% from R266=441ms——正常波动，稳态区间内）。FLUSH 55→61（+6，恢复 +6 节奏）。tombstone=121,278（2.31%——增速持续）。recordCount +15.0K（连续 4 轮偏高）。

| 指标 | R266 | R267 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 162ms | **194ms** | ↑20% |
| P50 | 28ms | **34.8ms** | ↑24% |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 12/16 | **10/16** | ↓2 |
| tombstone | 106,674 (2.03%) | **121,278 (2.31%)** | ↑（增速持续） |
| recordCount | 5,352,582 | **5,367,580** | +15.0K（连续 4 轮偏高） |
| liveRecordCount | 5,245,908 | **5,246,302** | +394（正常） |
| FLUSH 累计 | 55 | **61** | +6（恢复 +6 节奏） |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 8 | **8** | 不变 |
| QUERYADV_GE500 | 78 | **78** | 不变 |

### `x` 缓存——连续 6 轮稳定

| 轮次 | `x` 路径 | `x` 时间 |
|------|---------|--------:|
| R261 | short-query-cache | 17ms |
| R262 | short-query-cache | 1ms |
| R263 | short-query-cache | 2ms |
| R264 | short-query-cache | 3ms |
| R265 | short-query-cache | 137ms* |
| R266 | short-query-cache | 2ms |
| **R267** | **short-query-cache** | **5ms** |

*R265=137ms 为系统 overhead（I/O 风暴期间），非缓存问题。

### `size:>100m`=107ms 异常

- **R266=29ms → R267=107ms（↑269%）。**
- **phase2=104ms（几乎全部是搜索时间——SoA 过滤扫描）。**
- **此查询走 advanced-pure-filter-soa-gcd，需要扫描全部 SoA 数据检查文件大小属性。**
- **107ms 表明 SoA 数据区域部分 page cache 被淘汰——与全表扫描走势一致。**
- **单次异常——不影响整体评估。**

### Trigram 路径——保持良好

| Query | R266 | R267 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 39ms | **55ms** | ↑41% | 正常 |
| `hello world` | 35ms | **47ms** | ↑34% | 极佳 |
| `*.swift` | 24ms | **24ms** | 持平 | 极佳 |
| `main.cpp` | 6ms | **7ms** | ↑17% | 极佳 |
| `abc def...` | 28ms | **33ms** | ↑18% | 极佳 |
| `README` | 9ms | **7ms** | ↓22% | 极佳 |
| `test` | 15ms | **12ms** | ↓20% | 极佳 |
| `MacEverything` | 10ms | **3ms** | ↓70% | 极佳 |
| Trigram phase2均值 | ~17ms | **~19ms** | ↑12%（轻微） |

- **7/8 trigram 极佳——仅 `.pdf`=55ms 略超 50ms 阈值。**
- **phase2 均值 17→19ms（↑12%——轻微波动，稳定区间内）。**

### 全表扫描——正常波动

| Query | R266 | R267 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 151ms | **157ms** | ↑4% |
| `path:/Users` | 128ms | **176ms** | ↑38% |
| `中文测试` | 162ms | **194ms** | ↑20% |
| **总和** | **441ms** | **527ms** | **↑20%** |

- **全表扫描 441→527ms（↑20%——正常波动，稳态 400-600ms 区间内）。**

### tombstone 增速趋势

| 轮次 | recordCount Δ | tombstone | tombstone% | 增量 |
|------|------------:|----------:|---------:|------:|
| R262 | +7.2K | 49,554 | 0.94% | — |
| R263 | +8.9K | 58,042 | 1.10% | +8,488 |
| R264 | +15.4K | 72,277 | 1.37% | +14,235 |
| R265 | +18.7K | 90,255 | 1.72% | +17,978 |
| R266 | +16.1K | 106,674 | 2.03% | +16,419 |
| **R267** | **+15.0K** | **121,278** | **2.31%** | **+14,604** |

- **tombstone 增速 ~15K/轮——与 recordCount 增速一致。**
- **recordCount 连续 4 轮 +15K 以上——文件系统持续活跃。**
- **liveRecordCount 几乎不变（+394）——大部分新 records 是路径更新（旧→tombstone）。**
- **按 ~15K/轮增速，tombstone 达 25% 阈值需 ~85 轮——暂无 compaction 风险。**
- **但 tombstone 累积增大索引文件 → flush rewrite 时间持续增长。**

### 辅助路径

| Query | R266 | R267 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 2ms | **5ms** | ↑150%（微小） | 极佳 |
| `ext:cpp` | 6ms | **35ms** | ↑483%（偶发波动） | 极佳 |
| `size:>100m` | 29ms | **107ms** | ↑269%（异常） | 正常 |
| `dm:today` | 72ms | **41ms** | ↓43% | 极佳 |
| `verylongquery...` | 0.2ms | **0.3ms** | 持平 | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 4.7 | 0.0 | short-query-cache | 0 | 0 | 极佳（稳定） |
| `.pdf` | 5 | 55.1 | 38.6 | advanced-trigram | 0 | 9788 | 正常 |
| `main.cpp` | 5 | 6.8 | 2.1 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 23.9 | 20.3 | advanced-trigram | 0 | 9405 | 极佳 |
| `ext:cpp` | 5 | 34.8 | 7.7 | advanced-ext-index | 0 | 3611 | 极佳 |
| `size:>100m` | 5 | 106.6 | 103.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（异常偏高） |
| `hello world` | 5 | 47.4 | 46.2 | advanced-trigram | 0 | 3930 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 32.6 | 30.7 | advanced-trigram | 0 | 14457 | 极佳 |
| `README` | 5 | 7.0 | 4.7 | advanced-trigram | 0 | 17432 | 极佳 |
| `test` | 5 | 12.2 | 8.8 | advanced-trigram | 0 | 117728 | 极佳 |
| `MacEverything` | 5 | 2.9 | 0.7 | advanced-trigram | 0 | 6943 | 极佳 |
| `dm:today` | 5 | 40.9 | 40.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 156.5 | 145.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 176.1 | 164.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 194.3 | 194.0 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.3 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 34.8ms，P90 = 176.1ms，Max = 194.3ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R266 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **10/16** | 0.3-47ms | R266=12/16（↓2） |
| **正常**（50-500ms） | 6/16 | 55-194ms | R266=4/16（↑2） |
| **超标**（>500ms） | **0/16** | — | R266=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,367,580，liveRecordCount=5,246,302（tombstone=**121,278**，**2.31%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=700  QUERYADV_GE500=78（不变）
FLUSH=61（+6）  COMPACTION=8  SKIPFLUSH=7266
```

## 6. 问题清单

### P0（紧急）：flush rewrite 加速恶化（2617→3981ms）

- **本轮未捕获新 flush rewrite 日志——但趋势未变。**
- **tombstone +14.6K/轮 → 索引体积持续增长。**

### P0：compaction 触发机制缺陷（根因未修复）

### P0：I/O 优先级保护

## 7. 优化建议

### P0（紧急）：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. flush rewrite 时间是否继续恶化（上轮 3981ms）。
2. `size:>100m`=107ms 异常是否复现。
3. tombstone 增速趋势（2.31%，~15K/轮）。
4. recordCount 增速是否回归正常。

## 9. 结论

**稳定运行——I/O 风暴后连续 2 轮平稳。** 0/16 >500ms，Max=194ms，P50=35ms，lockWait=0。极佳 10/16。`x`=5ms（缓存稳定，连续 6 轮）。Trigram 7/8 极佳（`.pdf`=55ms 略超阈值）。全表扫描 527ms（稳态区间内）。`size:>100m`=107ms 为单次异常（SoA page cache 部分淘汰）。

**tombstone 持续增长（2.31%，~15K/轮）——与 recordCount 连续 4 轮 +15K 同步。** 虽然距 25% 阈值尚远，但 tombstone 累积增大索引文件 → flush rewrite 时间持续增长的恶性循环仍在。FLUSH +6（55→61）。P0 COW flush 改造紧急性不变。
