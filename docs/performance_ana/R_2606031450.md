# MacEverything 性能分析报告 R_2606031450

## 1. 本轮范围

- 任务启动时间：2026-06-03 14:50
- 报告文件：`docs/performance_ana/R_2606031450.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21765`
  - `SEG_LINES=739`
  - `LAST_LOG_TS=2026-06-03 23:41`

## 2. 执行摘要

**I/O 压力持续但正在恢复。** P50 ≈ 165ms（前轮 316ms），2/16 查询 >500ms（前轮 4/16）。`.pdf`=1169ms（前轮 1733ms，-33%），正在恢复。**`x`=677ms 出现第 7 次短查询缓存异常**（新历史最高，前纪录 676ms R72），连续 I/O 压力期间 GCD 线程调度延迟再次被证实。

tombstone 急升至 276,415 条（4.60%），本轮增量 **12,917 条**（历史最大单轮增幅），SKIPFLUSH +12。连续第 18 轮超过 2%。

### 横向对比（最近 5 轮）

| Query | R73 | R74 | R75 | R76 | R77 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 8 | 25 | 1 | 24 | **677** | **第 7 次异常（新高）** |
| `.pdf` | 307 | 34 | 179 | 1733 | **1169** | 恢复中 |
| `*.swift` | 132 | 21 | 48 | 860 | **165** | **已恢复** |
| `hello world` | 137 | 48 | 45 | 1542 | **357** | **已恢复** |
| `path:/Users` | 83 | 179 | 339 | 1245 | **376** | **已恢复** |
| P50 | 61 | 31 | 48 | 316 | **165** | 恢复中 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 677.2 | 0.0 | short-query-cache | 0 | 0 | **第 7 次异常（新高）** |
| `.pdf` | 5 | 1169.0 | 1021.5 | advanced-trigram | 59.0 | 9949 | 恢复中 |
| `main.cpp` | 5 | 66.9 | 21.3 | advanced-trigram | 23.0 | 85 | 正常 |
| `*.swift` | 5 | 165.2 | 155.6 | advanced-trigram | 2.8 | 9356 | 恢复 |
| `ext:cpp` | 5 | 119.3 | 80.3 | advanced-ext-index | 6.3 | 3701 | 正常 |
| `size:>100m` | 5 | 59.8 | 50.2 | advanced-pure-filter-soa-gcd | 0.7 | 0 | 正常 |
| `hello world` | 5 | 357.3 | 347.8 | advanced-trigram | 5.0 | 3613 | 恢复 |
| `abc def ghi jkl mno pqr` | 0 | 315.5 | 312.1 | advanced-trigram | 0.8 | 15996 | 偏慢 |
| `README` | 5 | 31.2 | 24.9 | advanced-trigram | 0 | 17720 | 快 |
| `test` | 5 | 69.7 | 57.1 | advanced-trigram | 0 | 129964 | 正常 |
| `MacEverything` | 5 | 15.8 | 1.4 | advanced-trigram | 0 | 6803 | 极快 |
| `dm:today` | 5 | 97.0 | 96.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 221.3 | 213.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 375.9 | 362.4 | advanced-linear-gcd | 0 | 0 | 恢复 |
| `中文测试` | 0 | 284.3 | 283.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 1.6 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 165ms，P90 ≈ 677ms，Max = 1169ms。2/16 查询 >500ms。lockWaitMs 最高 59ms（`.pdf`），多个查询有微量 lockWait。**

### I/O 压力恢复追踪（R76 事件）

| 指标 | R76（峰值） | R77 本轮 | 变化 |
|------|----------:|--------:|------|
| P50 | 316ms | **165ms** | -48% |
| Max | 1733ms | **1169ms** | -33% |
| >500ms | 4/16 | **2/16** | -50% |
| lockWaitMs max | 77ms | **59ms** | -23% |

恢复速度符合预期（中等事件 1-2 轮恢复），预计下轮完全恢复。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,008,104，liveRecordCount=5,731,689（tombstone=**276,415**，**4.60%**）；
- 进程正常运行。
- **recordCount 首次突破 600 万。**

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=227  QUERYADV_GE500=37
FLUSH=0  COMPACTION=2  SKIPFLUSH=231
DIAG_TRIGRAM_SKIP=82
```

- QUERYADV 从 220 增至 227（+7）。
- QUERYADV_GE500 从 36 增至 37（+1，`.pdf` 1169ms；`x` 677ms 走缓存不记录日志）。
- SKIPFLUSH 从 219 增至 231（+12，FSWatcher 持续高活动）。
- DIAG_TRIGRAM_SKIP 从 81 增至 82（+1）。

## 6. 问题清单

### Critical：tombstone 急升至 4.60%（276,415 条，历史最大单轮增幅）

- 增长轨迹：
  ```
  R74: 247K (4.13%) → R75: 255K (4.26%) → R76: 263K (4.40%)
  → R77: 276K (4.60%)
  ```
- 本轮增量 **12,917 条**（历史最大，前最高 ~13.7K R69）。
- **累计内存开销**：~276K × 300 字节 ≈ 83MB。
- SKIPFLUSH=231（+12），compaction 完全不可触发。连续第 18 轮超 2%。
- **按当前增速（~10-13K/轮），约 7 轮后将达 5%。**
- recordCount 首次突破 600 万——总数据量在持续增长。

### Critical：`x` 短查询缓存第 7 次异常（677ms，新历史最高）

- **异常历史**（25 轮中 7 次，28% 异常率）：
  ```
  R58: 254ms → R62: 93ms → R65: 364ms → R67: 600ms
  → R68: 545ms → R72: 676ms → R77: 677ms
  ```
- 趋势：异常值持续升级（93→254→364→545→600→676→677ms）。
- **本轮特征**：发生在 I/O 压力期间（lockWaitMs 在其他查询上 59ms），与 R67（I/O 压力+600ms）和 R72（I/O 微压+676ms）模式一致。
- **根因确认**：I/O 压力 → GCD 线程池被 FSWatcher 写入占用 → 缓存查询 dispatch 延迟。
- **修复方案**：将缓存查询隔离到独立高优先级 GCD 队列（userInteractive QoS）。

### High：`.pdf` 持续 >500ms（I/O 压力尾部）

- R76=1733ms → R77=1169ms，正在恢复但仍远超正常（34-179ms）。
- lockWaitMs=59ms 仍在影响。
- 预计下轮回到 <500ms。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（4.60%，连续 18 轮超标，历史最大增幅）

- tombstone 276K 条，83MB 额外内存。
- 当前增速 ~13K/轮，约 7 轮后达 5%。
- **紧迫性极高**——每多拖一轮，内存和扫描开销持续恶化。

### P0：short-query-cache GCD 隔离（第 7 次异常，28% 异常率）

- `x` 异常率 28%（25 轮中 7 次），值持续升级至 677ms。
- 根因已确认：GCD 线程调度被 FSWatcher 写入挤占。
- **修复**：缓存查询使用独立 userInteractive QoS 队列，不与 FSWatcher 共享。

### P0：将 `.pdf`/`*.swift` 路由到 ext-index

- `.pdf` 在 I/O 压力下 1169-1733ms；ext-index 路径同等条件下 119-317ms。
- 路由后预计将 `.pdf` I/O 压力下延迟从 1000-2000ms 降至 200-400ms。

### P1：实现 phase2 超时降级

## 8. 下一轮重点

1. I/O 压力是否完全消退——`.pdf` 和 lockWaitMs 是否回归正常。
2. tombstone 增速是否持续——SKIPFLUSH +12 是否维持。
3. `x` 异常是否为 I/O 压力期间的确定性行为。

## 9. 结论

**I/O 压力持续但正在恢复**（P50: 316→165ms，>500ms: 4→2/16）。`*.swift`、`hello world`、`path:/Users` 已恢复正常，仅 `.pdf`=1169ms 仍偏高。**`x`=677ms 出现第 7 次缓存异常（新历史最高）**，25 轮中 7 次（28%），值持续升级——根因确认为 I/O 压力期间 GCD 线程调度延迟。tombstone 急升至 276,415 条（4.60%），本轮增量 12,917 条为历史最大。SKIPFLUSH=231（+12），recordCount 首次突破 600 万。预计下轮完全恢复，但 tombstone compaction 和 GCD 隔离修复已极度紧迫。
