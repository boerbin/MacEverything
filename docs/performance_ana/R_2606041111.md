# MacEverything 性能分析报告 R_2606041111

## 1. 本轮范围

- 任务启动时间：2026-06-04 11:11
- 报告文件：`docs/performance_ana/R_2606041111.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=22026`
  - `SEG_LINES=999`
  - `LAST_LOG_TS=2026-06-04 11:11`

## 2. 执行摘要

**日间 I/O 压力事件。** `.pdf`=820ms（>500ms），`*.swift`=430ms，`hello world`=412ms。**1/16 查询 >500ms**。P50 ≈ 131ms，Max=820ms。lockWaitMs 最高 2.2ms（轻微）。

**tombstone 增幅再创历史新高**：+25,546 条（超过 R85 的 23,853），达 479,574 条（**7.72%**，144MB）。**日间用户活动驱动的加速增长确认**——R91 已预警的日间风险窗口在 1 轮后即兑现。

### 横向对比（最近 5 轮）

| Query | R88 | R89 | R90 | R91 | R92 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 6 | 52 | 3 | 12 | **5** | 正常 |
| `.pdf` | 353 | 225 | 54 | 192 | **820** | **I/O 压力** |
| `*.swift` | 149 | 117 | 32 | 125 | **430** | **偏高** |
| `hello world` | 252 | 218 | 62 | 273 | **412** | **偏高** |
| `path:/Users` | 307 | 216 | 65 | 314 | **222** | 正常 |
| P50 | 112 | 116 | 23 | 126 | **131** | 正常 |

### I/O 压力事件严重程度对比

| 指标 | R76 中等 | R85 中度 | R86 灾难 | **R92 本轮** |
|------|--------:|--------:|--------:|---------:|
| P50 | 316ms | 104ms | 361ms | **131ms** |
| Max | 1733ms | 1648ms | 2,731,551ms | **820ms** |
| >500ms | 4/16 | 1/16 | 5/16 | **1/16** |
| lockWait max | 77ms | 13.6ms | 1,010ms | **2.2ms** |
| tombstone % | 4.4% | 6.63% | 6.68% | **7.72%** |

**判定：轻度 I/O 压力事件。** 仅 `.pdf` 1 个查询突破 500ms，lockWait 极低。但此事件发生在 R86 灾难恢复仅 5 轮后——**事件频率正在加速**。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 5.1 | 0.0 | short-query-cache | 0 | 0 | 正常 |
| `.pdf` | 5 | 819.8 | 806.9 | advanced-trigram | 0 | 9950 | **>500ms** |
| `main.cpp` | 5 | 74.0 | 56.3 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 430.0 | 424.5 | advanced-trigram | 0 | 9356 | 接近 500ms |
| `ext:cpp` | 5 | 173.1 | 169.4 | advanced-ext-index | 2.2 | 3701 | 正常 |
| `size:>100m` | 5 | 131.3 | 131.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 411.7 | 407.2 | advanced-trigram | 0 | 3613 | 接近 500ms |
| `abc def ghi jkl mno pqr` | 0 | 107.7 | 106.9 | advanced-trigram | 0 | 16029 | 正常 |
| `README` | 5 | 27.0 | 18.1 | advanced-trigram | 0 | 17679 | 快 |
| `test` | 5 | 33.4 | 23.3 | advanced-trigram | 0 | 130096 | 正常 |
| `MacEverything` | 5 | 30.3 | 0.9 | advanced-trigram | 0 | 6803 | 正常 |
| `dm:today` | 5 | 100.9 | 100.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 163.8 | 155.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 222.1 | 209.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 278.8 | 278.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.9 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 131ms，P90 ≈ 430ms，Max = 820ms。1/16 查询 >500ms。lockWaitMs 最高 2.2ms。**

### 受影响查询特征

| 查询 | candidates | phase2Ms | 说明 |
|------|----------:|---------:|------|
| `.pdf` | 9,950 | 807ms | 高候选集 + trigram 索引页换入 |
| `*.swift` | 9,356 | 425ms | 同上，略低于阈值 |
| `hello world` | 3,613 | 407ms | 候选集适中但多 token 匹配 |

**共同特征**：trigram 路径 + 需要大量索引页面访问。全表扫描路径（linear-gcd、pure-filter）不受影响——它们的数据已在 R89-R91 的多次访问中预热。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,217,594，liveRecordCount=5,738,020（tombstone=**479,574**，**7.72%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=320  QUERYADV_GE500=45
FLUSH=0  COMPACTION=2  SKIPFLUSH=380
DIAG_TRIGRAM_SKIP=100
```

- QUERYADV 从 309 增至 320（+11）。
- QUERYADV_GE500 从 44 增至 45（+1，即 `.pdf`=820ms）。
- SKIPFLUSH 从 369 增至 380（+11）。
- DIAG_TRIGRAM_SKIP 从 96 增至 100（+4，**跳增**——trigram 索引跳过增多与 I/O 压力一致）。

### DIAG_TRIGRAM_SKIP +4 分析

- 正常增速 +1/轮，本轮 +4——**最大单轮增幅**。
- trigram 索引命中检查失败次数增多，表明 trigram 索引区域有更多页面未在缓存中。
- 与 `.pdf`=820ms 的 I/O 压力表现一致——日间活动挤出了 trigram 索引页面。

## 6. 问题清单

### Critical：tombstone 7.72%（479,574 条，144MB），日间加速增长

- 增长轨迹（近 5 轮，排除 R90 极短间隔）：
  ```
  R88: 430K (+12.7K) → R89: 442K (+12.1K) → R91: 454K (+10.5K)
  → R92: 480K (+25.5K)
  ```
- **日间增速飙升至 +25.5K**——新历史最高，超过之前所有夜间大增幅（R83=18.9K, R84=23.8K, R85=23.9K）。
- **原因**：日间用户文件操作（编辑、保存、编译、下载等）产生大量 FSWatcher 事件。
- **累计内存开销**：~480K × 300 字节 ≈ **144MB**。
- 连续第 32 轮超 2%，第 13 轮超过 5%，第 8 轮超过 6%，第 5 轮超过 7%。

### Critical：I/O 压力事件频率加速

- **事件间隔缩短**：
  - R67 → R76：9 轮间隔
  - R76 → R84-R86：8 轮间隔（渐进恶化 3 轮）
  - R86 → **R92：仅 6 轮间隔**（含 3 轮恢复 + 2 轮正常 + 本轮事件）
- **趋势**：随 tombstone 增长，I/O 事件间隔从 9→8→6 轮递减——**下一次事件可能仅 4-5 轮后**。

### Medium：DIAG_TRIGRAM_SKIP 跳增至 100（+4）

- 正常 +1/轮，本轮 +4。trigram 索引缓存命中率下降的信号。
- 与 tombstone 内存占用（144MB）争抢页面缓存直接相关。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（7.72%，144MB，事件频率加速）

- **I/O 事件间隔从 9→8→6 轮递减。** 按此趋势，下一次事件仅 4-5 轮后。
- **日间增速 +25.5K 创新高**——日间用户活动是 tombstone 增长的最大驱动力。
- compaction 后释放 144MB → trigram 索引常驻 → 消除 I/O 压力事件根因。

### P0 紧急：`size:>100m` 全表扫描超时保护
### P0：short-query-cache GCD 隔离
### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. I/O 压力是否持续或加剧（R84-R86 模式：轻度→中度→灾难）。
2. tombstone 增速是否因日间活动维持 >20K/轮。
3. DIAG_TRIGRAM_SKIP 是否继续跳增。

## 9. 结论

**日间 I/O 压力事件。** `.pdf`=820ms（>500ms），1/16 查询突破阈值。**tombstone 增幅再创历史新高**（+25,546，达 479,574 条，7.72%，144MB）——日间用户活动驱动加速增长。**I/O 事件间隔从 9→8→6 轮递减**，频率正在加速。R91 预警的日间风险窗口在 1 轮后即兑现。DIAG_TRIGRAM_SKIP 跳增 +4（正常 +1）确认 trigram 索引缓存命中率下降。**R84-R86 恶化模式（轻度→中度→灾难）可能正在重演**——R92 即为轻度阶段，下一轮需密切关注是否向中度演进。
