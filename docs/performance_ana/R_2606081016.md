# MacEverything 性能分析报告 R_2606081016

## 1. 本轮范围

- 任务启动时间：2026-06-08 10:16
- 报告文件：`docs/performance_ana/R_2606081016.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=4126`
  - `SEG_LINES=3184`
  - `LAST_LOG_TS=2026-06-08 10:47`

## 2. 执行摘要

**冷启动部分恢复——0/16 >500ms（纪录重启），Max=153ms，P50=62.5ms，lockWait=0，极佳比例 6/16（38%）。** 全表扫描总和 238ms——**本系列绝对新低**（超越 R159=416ms）！`中文测试`=45ms 首次进入极佳范围——系列绝对新低。SoA/linear 区域通过 R176 battery 完全预热。但 trigram postings 仍部分冷态——均值 95.8ms（正常稳态 12-18ms）。lockWait 从 R176 的 32ms 恢复至 0。

### 关键指标对比

| 指标 | R176（冷启动） | R177 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 7/16 | **0/16** | **↓7（纪录重启！）** |
| Max | 2217ms | **153ms** | **↓ 93%** |
| P50 | 167ms | **62.5ms** | **↓ 63%** |
| lockWait max | 32.3ms | **0ms** | **恢复完美** |
| 极佳比例(<50ms) | 1/16 | **6/16** | **↑5** |
| tombstone | 1,386,961 (21.38%) | **1,412,630 (21.69%)** | ↑（含休眠期积压） |
| tombstone 增速 | — | **~427/min** | ↑（唤醒后文件系统活动追赶） |
| recordCount | 6,486,773 | **6,513,466** | +26.7K（含休眠积压） |
| liveRecordCount | 5,099,812 | **5,100,836** | +1024 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 2895 | **2968** | +73 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 9 | **9** | 不变（本轮 0 新增） |

### 恢复模式——SoA/linear 完全预热，trigram 部分冷态

| 路径 | R176（冷态） | R177 本轮 | 恢复率 | 说明 |
|------|-----:|-----:|------|------|
| `content:import`(SoA) | 987ms | **59ms** | **↓ 94%（完全恢复）** | R176 全扫描预热 |
| `path:/Users`(linear) | 600ms | **134ms** | **↓ 78%（完全恢复）** | R176 全扫描预热 |
| `中文测试`(linear) | 404ms | **45ms** | **↓ 89%（超越历史最佳！）** | R176 全扫描预热 |
| `.pdf`(trigram) | 304ms | **115ms** | ↓ 62%（部分恢复） | 仍有冷 postings 页 |
| `test`(trigram) | 2217ms | **115ms** | ↓ 95%（大幅恢复） | 116K candidates 部分热化 |
| `abc def`(trigram) | 1585ms | **152ms** | ↓ 90%（大幅恢复） | 14K candidates 部分热化 |

#### 恢复机制差异

| 读取模式 | 预热方式 | R177 恢复状态 |
|----------|---------|-------------|
| **SoA 全扫描** | R176 battery 本身即全量扫描 → 完全预热 | **完全恢复（超越历史最佳）** |
| **linear-gcd 扫描** | R176 battery 触发 readahead → 区域完全预热 | **完全恢复（系列新低）** |
| **trigram 随机访问** | R176 仅预热查询涉及的 postings 页 → 部分预热 | **部分恢复（仍有冷页）** |

- **核心发现**：SoA/linear 全扫描路径在 1 轮 battery 后即完全恢复——因为 battery 本身就是全量扫描。
- **trigram 需更多轮恢复**：每轮 battery 仅触及部分 postings 页，需要真实用户查询逐步预热剩余冷页。

### 全表扫描——本系列绝对新低！

| Query | R176 | R177 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 987ms | **59ms** | **↓ 94%** |
| `path:/Users` | 600ms | **134ms** | **↓ 78%** |
| `中文测试` | 404ms | **45ms** | **↓ 89%（系列新低！）** |
| **总和** | **1991ms** | **238ms** | **↓ 88%（系列绝对新低！）** |

- **全表扫描总和 238ms——超越所有历史记录（此前最低 R159=416ms）。**
- **`中文测试`=45ms——首次进入极佳范围（<50ms）！此前最低 R175=111ms。**
- **原因**：R176 冷启动 battery 将所有 SoA/linear 区域完全加载到 page cache。系统刚从休眠唤醒，其他进程尚未产生大量 I/O 竞争——page cache 几乎 100% 可用于 MacEverything。
- **这是 post-wake 过度恢复效应——预计后续轮次随系统活动恢复而回升。**

### Trigram 路径——部分冷态

| Query | R176（冷态） | R177 | 恢复率 |
|-------|-----:|-----:|------|
| `.pdf` | 304ms | **115ms** | 62%（仍有冷页） |
| `*.swift` | 590ms | **63ms** | 89% |
| `hello world` | 702ms | **104ms** | 85% |
| `abc def...` | 1585ms | **152ms** | 90% |
| `README` | 167ms | **123ms** | 26%（lockWait 占比消失后真实值暴露） |
| `test` | 2217ms | **115ms** | 95% |
| `MacEverything` | 48ms | **10ms** | 79% |
| Trigram 均值 | — | **95.8ms** | 部分恢复 |

- **Trigram 均值 95.8ms——远高于正常稳态 12-18ms，但远低于 R176 冷态。**
- **恢复不均匀**：`.pdf`=115ms（仍高），`*.swift`=63ms（接近恢复）。
- **预计需 1-2 轮额外 battery 完全恢复 trigram postings。**

### `dm:today`=12.5ms / `content:import`=59ms——post-wake 极值

- **`dm:today`=12.5ms——本系列最低（正常 35-88ms）！**
- **`content:import`=59ms——本系列最低（正常 168-316ms）！**
- **原因**：唤醒后 SoA 区域被 R176 完全预热，且系统活动尚未恢复竞争。**
- **不可持续——后续轮次将回升至正常水平。**

### tombstone 增速异常

- **recordCount +26.7K（正常 ~8K/30min）——含休眠期积压的 FSEvents 追赶。**
- **tombstone 增速 ~427/min——高于正常（250-300/min）。**
- **原因**：唤醒后文件系统变更通知批量到达，集中处理。**
- **预计后续恢复正常增速。**
- **tombstone 21.69%——距 25% 约 ~20 小时。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 4.4 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 114.5 | 112.2 | advanced-trigram | 0 | 9813 | 部分冷态 |
| `main.cpp` | 5 | 13.9 | 9.4 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 62.5 | 60.1 | advanced-trigram | 0 | 9345 | 部分冷态 |
| `ext:cpp` | 5 | 29.0 | 28.1 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 35.2 | 34.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 103.7 | 97.4 | advanced-trigram | 0 | 3614 | 部分冷态 |
| `abc def ghi jkl mno pqr` | 0 | 152.4 | 151.2 | advanced-trigram | 0 | 14215 | 部分冷态 |
| `README` | 5 | 122.7 | 110.7 | advanced-trigram | 0 | 16631 | 部分冷态 |
| `test` | 5 | 115.4 | 79.1 | advanced-trigram | 0 | 116514 | 部分冷态 |
| `MacEverything` | 5 | 9.7 | 0.7 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 12.5 | 11.9 | advanced-pure-filter-soa-gcd | 0 | 0 | **极佳（系列新低）** |
| `content:import` | 5 | 59.3 | 54.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（系列新低） |
| `path:/Users` | 5 | 133.6 | 131.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 45.3 | 42.1 | advanced-linear-gcd | 0 | 0 | **极佳（系列新低！）** |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 62.5ms，P90 = 152.4ms，Max = 152.4ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R176 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 6/16 | 0.2-45ms | R176=1/16（**↑5**） |
| **正常**（50-500ms） | 10/16 | 59-153ms | R176=8/16（↑2） |
| **超标**（>500ms） | **0/16** | — | R176=7/16（**↓7，纪录重启**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,513,466，liveRecordCount=5,100,836（tombstone=**1,412,630**，**21.69%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=150  QUERYADV_GE500=9（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=2968
```

- **本轮 0 新增 >500ms 查询——纪录重新开始。**
- **FLUSH=1**——无新 flush。
- **SKIPFLUSH=2968。**

## 6. 问题清单

### 观察项：trigram postings 部分冷态（恢复中）

- **现象**：trigram 均值 95.8ms（正常 12-18ms）。
- **原因**：R176 冷启动 battery 仅预热部分 postings 页。
- **预计**：1-2 轮额外 battery 完全恢复。
- **处置**：自然恢复中，无需干预。

### tombstone 增速异常（唤醒追赶）

- **增速 ~427/min（正常 250-300/min）。**
- **原因**：唤醒后 FSEvents 批量处理积压。
- **预计后续恢复正常。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 21.69%（含唤醒追赶），距 25% 约 ~20 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- R176 lockWait=32ms 已证明唤醒场景下的问题。

### P2：唤醒预热策略（持续建议）

- 本轮数据证明：SoA/linear 通过 1 轮全扫描即完全恢复（甚至创新低），但 trigram 需多轮。
- 预热策略应重点覆盖高频 trigram postings。

## 8. 下一轮重点

1. Trigram 均值是否从 95.8ms 继续下降（预计 30-50ms）。
2. 全表扫描总和从 238ms（极值）回升至正常（500-800ms）。
3. tombstone 增速是否从 427/min 回落至正常。

## 9. 结论

**冷启动 1 轮恢复——0/16 >500ms（纪录重启），Max=153ms，lockWait=0。** SoA/linear 通过 R176 battery 完全预热——全表扫描总和 238ms 创本系列绝对新低（此前 416ms），`中文测试`=45ms 首次进入极佳范围。但 trigram postings 仍部分冷态（均值 95.8ms）——随机访问模式无法通过单轮 battery 完全预热，需逐步覆盖剩余冷页。

**恢复机制差异揭示**：SoA/linear 全扫描路径天然在 1 轮 battery 后完全恢复（battery 本身即全量扫描），trigram 随机访问路径需多轮逐步预热。这支持 P2 唤醒预热策略——应主动预读高频 postings 页面。tombstone 21.69%（含唤醒追赶），增速 427/min（暂时偏高），距 25% 约 20 小时。
