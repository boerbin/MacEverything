# MacEverything 性能分析报告 R_2605300911

**时间**: 2026-05-30 09:11  
**索引规模**: 5,769,085 总记录 / 5,384,990 活跃记录  
**Tombstone**: 384,095 (**6.66%**)  
**日志**: session 起于 11:04:48（同一长会话，已运行 ~22.1h），WARN=0 / ERROR=0  
**轮次**: 第 82 轮（重启后第 30 轮，同一 11:04:48 长会话）  

---

## 🔵 确认轮：R81「分区页温度」在更细粒度复现——字符串层内部 path 与 content 独立升降

**R81 提出「页温度按访问区域独立升降」（散落 trigram 候选区冷 / 顺序 SoA 扫描区暖 同轮并存）。本轮（09:11）在更细粒度复现同一机制：连字符串层内部不同串池都各自独立升降。** 上一轮 `path:/Users` 与 `content:import` 同刷八轮新低（25.74 / 23.56 ns/槽）；本轮 `path:/Users` 单轮跳回 **36.99 ns/槽（×1.44）**，而 `content:import` 几乎不动（25.92，×1.10）、`中文测试` 几乎不动（33.18 vs 33.38）。三条字符串路径**同一电池、相隔数秒**却只有 path 大幅冷化——`path:/Users` 解引用**路径串池**、`中文测试` 解引用**名字串池**、`content:import` 走内容标志过滤，三者落在不同内存区，故可单独冷化。这把 R81 的「trigram 区 vs 线性区」decoupling 推进到「线性区内部各串池亦 decoupling」，**进一步收紧 mlock P0：常驻不能只钉「线性扫描区」一个整块，须覆盖路径串池 / 名字串池等各子区**。同轮另见 `main.cpp` **trig=19.51ms**（平时 1–7ms）——trigram **索引页**本身冷化（与候选行冷化不同的第三类冷区）。绝对二层分裂本轮仍成立（**9/9**）：数值层 dm **9.84（九样本新低）**/ size 12.56 ≪ 字符串层 content 25.92 / 中文 33.18 / path 36.99（层间隙 2.06×，无重叠）。Tombstone 越过 **6.66%**（连涨 15 轮，晨间 ~21.6K/h）；安全阀整会话 335 次 skip「78 entries」、0 触发、0 回收——R67 根因在 22.1h 持续硬证。

---

## 1. 本轮查询电池（09:11，limit=100，混合温度）

| 查询 | qTimeMs | totalMs | phase2Ms | trigMs | sortMs | lockHeldMs | candidates | results | searchPath |
|------|---------|---------|----------|--------|--------|-----------|-----------|---------|-----------|
| `"x"` | 14.81 | 1.08 | 0 | 0 | 0 | 0 | 0 | 99 | short-query-cache |
| `".pdf"` | 48.43 | 46.18 | 44.04 | 2.01 | 0.02 | 46.10 | 10,117 | 100 | advanced-trigram |
| `"main.cpp"` | 61.30 | 31.49 | 11.90 | **19.51** | 0.01 | 31.42 | **71** | 71 | advanced-trigram |
| `"*.swift"` | 111.48 | 107.66 | 106.66 | 0.93 | 0.02 | 107.59 | 9,213 | 100 | advanced-trigram |
| `"ext:cpp"` | 62.36 | 52.92 | 52.15 | 0 | 0.01 | 52.86 | 2,987 | 100 | advanced-ext-index |
| `"size:>100m"` | 83.46 | 72.46 | 72.43 | 0 | 0 | 72.43 | 0 | 100 | advanced-pure-filter-soa-gcd |
| `"hello world"` | 160.88 | 144.30 | 143.52 | 0.74 | 0.01 | 144.26 | 2,340 | 100 | advanced-trigram |
| `"abc…"`(6词) | 136.73 | 136.64 | 136.52 | 0.06 | 0 | 136.59 | 15,917 | 0 | advanced-trigram |
| `"README"` | 40.72 | 33.79 | 29.57 | 4.15 | 0.05 | 33.73 | 16,457 | 100 | advanced-trigram |
| `"test"` | 53.23 | 41.14 | 29.89 | 10.69 | 0.45 | 40.59 | **136,231** | 100 | advanced-trigram |
| `"MacEverything"` | 14.62 | 9.40 | 2.85 | 6.50 | 0.03 | 9.35 | 6,693 | 100 | advanced-trigram |
| `"dm:today"` | 58.06 | 56.79 | 56.73 | 0 | 0.01 | 56.74 | 0 | 100 | advanced-pure-filter-soa-gcd |
| `"content:import"` | 151.53 | 149.53 | 141.35 | 0 | 8.13 | 141.35 | 0 | 100 | advanced-pure-filter-soa-gcd |
| `"path:/Users"` | 215.61 | 213.40 | **207.15** | 0 | 6.15 | 207.15 | 0 | 100 | advanced-linear-gcd |
| `"中文测试"` | 192.40 | 191.39 | 191.17 | 0.13 | 0 | 191.36 | 0 | 0 | advanced-linear-gcd |
| `"verylong"`(43w) | 0.34 | 0.34 | 0 | 0.33 | 0 | 0.33 | 0 | 0 | advanced-trigram |

**混合温度**：trigram 侧较 R81 部分回暖（ext:cpp 120→52、abc 291→136、.pdf 52→44），但 `*.swift` 仍冷（106ms）、`hello world` 144ms；`main.cpp` trig 飙到 19.51ms（trigram 索引页冷）。lockHeld/total ~99%（结构性持锁全程，lockWait=0）。

---

## 2. 🔑 分区页温度细粒度复现：字符串层内部 path 单独冷化

R81→R82 三条字符串路径 ns/槽变化（同一电池、相隔数秒）：

| 查询 | 解引用区域 | R81 ns/槽 | R82 ns/槽 | 单轮变化 |
|------|-----------|----------|----------|---------|
| `path:/Users` | 路径串池 | 25.74（新低） | **36.99** | **×1.44 ↑（冷）** |
| `content:import` | 内容标志过滤 | 23.56（新低） | 25.92 | ×1.10（不动） |
| `中文测试` | 名字串池 | 33.38 | 33.18 | ×0.99（不动） |

### 解读

- **同属「字符串/线性层」，但只有 path 单独大幅冷化**——路径串池、名字串池、内容标志分属不同内存区，独立驻留/淘汰。这正是 R81「分区页温度」在**层内更细粒度**的复现：decoupling 不止存在于「trigram 区 vs 线性区」，也存在于「线性区内部各串池之间」。
- **第三类冷区证据**：`main.cpp` 本轮 **trig=19.51ms**（平时 1–7ms），是 trigram **索引页**（posting list / trigram→docID 映射）冷化，与「候选行冷化」「串池冷化」并列为第三个独立冷区。
- **对 P0 的影响**：mlock/madvise 常驻不能按「一个线性扫描大区」粗粒度钉，须覆盖**路径串池 / 名字串池 / trigram 索引 / 散落候选行**等多个独立子区——任一未钉都会单独转冷（本轮 path 即活样本）。

---

## 3. 🔑 绝对二层分裂：九样本 9/9 不破（唯一稳健结论）

ns/槽（=totalMs×1e6/总记录数）九读数（R75-01:11 / R75-01:14 / R76 / R77 / R78 / R79 / R80 / R81 / R82）：

| 查询 | 层 | 11 | 14 | R76 | R77 | R78 | R79 | R80 | R81 | **R82** | 极差 |
|------|----|----|----|-----|-----|-----|-----|-----|-----|---------|------|
| `size:>100m` | 数值 | 7.95 | 8.18 | 8.58 | 9.22 | 7.63 | 6.06 | 16.16 | 11.17 | **12.56** | 2.67× |
| `dm:today` | 数值 | 12.8 | 20.0 | 17.7 | 22.3 | 16.8 | 9.96 | 12.23 | 13.23 | **9.84** | 2.27× |
| `content:import` | 字符串 | 28.2 | 35.9 | 28.4 | 31.7 | 38.7 | 26.6 | 27.79 | 23.56 | **25.92** | 1.64× |
| `path:/Users` | 字符串 | 36.3 | 45.0 | 29.8 | 33.6 | 36.7 | 33.7 | 31.47 | 25.74 | **36.99** | 1.75× |
| `中文测试` | 字符串 | 54.1 | 31.2 | 43.5 | 48.2 | 41.4 | 39.4 | 53.15 | 33.38 | **33.18** | 1.74× |

### 解读（9/9 全样本一致）

- **每一轮，最高数值层 < 最低字符串层**：R82 数值层 max=size 12.56 < 字符串层 min=content 25.92（层间隙 2.06×）。九轮层间隙 1.42×–2.67×，从未重叠。**这是七轮反复证伪后唯一扛过全部样本的结论**：纯数值路径（单 i64 列比较、零解引用）≪ 字符串路径（解引用散落串池）。
- **dm:today 刷新九样本新低 9.84**（前低 R79 9.96）——数值层底部继续随页温度浮动，但未挑战分裂边界。
- 层内方差仍由页温度（分区）主导、无固定排序；R76「数值最稳」、R79「1.5–2.2× 界」均已被先前轮次证伪。**只有层间绝对分裂是机制级稳健量。**

---

## 4. trigram/候选 每候选成本：本轮仍与候选数反相关

| 查询 | candidates | phase2Ms | 每候选 (μs) | 温度 |
|------|-----------|----------|------------|------|
| `test` | 136,231 | 29.89 | **0.219** | 暖 |
| `MacEverything` | 6,693 | 2.85 | 0.426 | 暖 |
| `README` | 16,457 | 29.57 | 1.797 | 温 |
| `.pdf` | 10,117 | 44.04 | 4.353 | 冷 |
| `*.swift` | 9,213 | 106.66 | 11.578 | 冷 |
| `abc…`(6词) | 15,917 | 136.52 | 8.577 | 冷 |
| `ext:cpp` | 2,987 | 52.15 | 17.46 | 冷 |
| `hello world` | 2,340 | 143.52 | 61.33 | 很冷 |
| `main.cpp` | 71 | 11.90 | 167.6 | 很冷 |

**每候选成本与候选数反相关**（高候选 0.219μs ↔ 低候选 167.6μs，跨度 765×）：高候选查询命中**热门/常驻**行，低候选查询命中**稀有/散落**行。mode-A 冷缓存特征延续（对照 R63 `main.cpp` 71 候选冷态 1665ms）——每候选成本由候选行页驻留主导，与候选数无关。

---

## 5. 🔴 安全阀整会话不可达（R67 根因 22.1h 硬证）

| 证据 | 值 | 含义 |
|------|-----|------|
| 实际 flush | **1 次**（11:12:29，rewrite=10331ms，持锁 10.3s） | 整 22.1h 仅启动后那一次 |
| Skipping flush | **335 次**，全 `only 78 entries (threshold=100)` | pending 冻结 78<100，命中 `IndexPersistence.cpp:142` 早退 |
| `triggering full compaction` | **0 次** | tombstone 比例阀（`:159`，0.25）从未求值——结构性不可达 |
| `Reclaimed` | **0 次** | 整会话零回收 |
| tombstone 走势 | 0（11:12 flush 后）→ **6.66%** | 单调累积、无任何在线回收 |

`:142` 的 `entryCount()<kCompactThreshold` 早退把 `:159` 的 tombstone 比例阀挡在门外——22.1h 内 335 skip / 0 trigger / 0 reclaim，硬证根因为「检查顺序」缺陷。

---

## 6. Tombstone 趋势（任务起点快照）

| 轮次 | 时刻 | Tombstone | 比例 | Δ | 区间增速 |
|------|------|-----------|------|---|------|
| R78 | 02:41 | 341,283 | 5.96% | — | — |
| R79 | 03:11 | 348,021 | 6.07% | +6,738 | — |
| R80 | 04:50 | 355,489 | 6.19% | +7,468 | — |
| R80(log) | 08:24 | 366,288 | 6.37% | +10,799 | ~3.0K/h（隔夜低） |
| R81 | 08:34 | 370,788 | 6.44% | +4,500 | ~27K/h（晨间） |
| **R82** | **09:11** | **384,095** | **6.66%** | **+13,307** | **~21.6K/h（晨间持续）** |

**15 轮严格单调递增。** R81→R82 37min 增 13,307（~21.6K/h），延续晨间高活动；活跃记录仅净增 152（5,384,838→5,384,990），总记录增 13,459——**~98.9% 新记录转 tombstone**。再坐实 R80「增速与活动量成正比」（隔夜 ~3K/h → 晨间 ~21–27K/h）。当前 6.66%，距 R43 崩溃前 12.97% 约半程，增速受当日活动支配，不宜单一 ETA。

---

## 7. 索引状态

| 指标 | R81(08:34) | R82(09:11) | Δ |
|------|-----|-----|---|
| 总记录 | 5,755,626 | 5,769,085 | +13,459 |
| 活跃记录 | 5,384,838 | 5,384,990 | +152 |
| Tombstone | 370,788 | 384,095 | +13,307 |
| Tombstone 比例 | 6.44% | **6.66%** | +0.22pp |
| 实际 flush | 1 | 1 | +0 |
| Skip flush | 321 | 335 | +14 |
| QueryAdvanced(>100ms) | 202 | 208 | +6 |

---

## 8. 日志延迟分布（QueryAdvanced=208，整会话）

| 区间 | 计数 |
|------|------|
| 100–200ms | 71 |
| 200–500ms | 95 |
| 500–1000ms | 14 |
| 1–5s | 21 |
| **>5s** | **7** |

**Top 7 峰值与 R76/R81 逐字节相同**：18582 / 17928 / 15749 / 8205 / 7544 / 6811 / 6061 ms——会话早段冷缓存 trigram 历史尖峰，冻结在过去。R81→R82 增量（202→208，+6）全部落在 100–500ms 带（66+94→71+95），>1s 各档（21/7）零增长——**本轮暖态电池未新增任何 >1s 条目**。

---

## 9. 数据窗口说明

本轮 status / battery / log 分析均在 **09:11–09:12 同一窗口内**完成（无时间涂抹），ns/槽 一律以 status 时刻总记录 5,769,085 为分母，无跨时外推。

---

## 10. 优化建议

| 优先级 | 项目 | 状态 |
|--------|------|------|
| **P0🔥** | 修复 flush 检查顺序（tombstone 安全阀前置/并联）+ 阈值下调 <12.97% | R67 根因；22.1h「335 skip/0 trigger/0 reclaim」硬证 |
| **P0🔥** | 关键数据页 mlock 常驻——**须覆盖路径串池/名字串池/trigram 索引/散落候选行多个独立子区** | R81/R82：各子区独立淘汰，本轮 path 单独冷化 + main.cpp trig 索引冷 |
| **P0🔥** | 无锁快照 flush | lockHeld/total ~99% + 启动 flush rewrite 持锁 10.3s |
| **P0🔥** | 查询超时保护 | 历史 .pdf 峰值 18.6s；本轮 path 213ms / 中文 191ms |
| **P0** | HTTP 并发处理 | R64 单线程 acceptLoop |
| **P1🔥** | 字符串路径方差治理 | 九样本层内方差由分区页温度主导（本轮 path ×1.44 冷化） |
| P1 | CJK trigram 索引化 | `中文测试` 无 trigram → 0 命中全比对，名字串池最大暴露面 |
| P1 | 日志 total 含 materialization | R64/R72 |

---

## 11. 总结评分

| 维度 | 评分 | 趋势 |
|------|------|------|
| 查询 (trigram, 暖) | 7/10 | → (部分回暖 ext:cpp 52/abc 136，但 *.swift 106ms/hello 144ms 仍冷) |
| 查询 (线性/过滤) | 6/10 | → (dm 刷新低 9.84，但 path ×1.44 冷化至 36.99，分区抖动) |
| 延迟稳定性 | 5/10 | → (暖态本身稳，分区温度波动 + 历史 .pdf 18.6s 均在) |
| 索引健康度 | 3/10 | ↓ (6.66%, 15 轮单调，安全阀 22.1h 零触发零回收) |
| 系统稳定性 | 5/10 | → (代码级缺陷已定位待修) |

**第 82 轮结论**：**🔵 确认轮——R81「分区页温度」在更细粒度复现**。本轮证据把「页温度按访问区域独立升降」从 R81 的「trigram 区 vs 线性区」推进到「线性区内部各串池之间」：同一电池数秒内，`path:/Users` 单轮跳回 36.99 ns/槽（×1.44 冷化），而 `content:import`（25.92，×1.10）、`中文测试`（33.18，×0.99）几乎不动——路径串池、名字串池、内容标志分属不同内存区，故可单独冷化。同轮 `main.cpp` trig=19.51ms 暴露第三类冷区（trigram 索引页本身）。这把 mlock P0 的要求从「钉一个线性大区」收紧为「须覆盖路径串池/名字串池/trigram 索引/散落候选行多个独立子区」。绝对二层分裂九样本 **9/9** 不破（数值 dm 9.84【新低】/size 12.56 ≪ 字符串 content 25.92/中文 33.18/path 36.99，层间隙 2.06×），仍是七轮证伪后唯一机制级稳健结论。每候选成本与候选数反相关（0.219μs↔167.6μs，765× 跨度）延续 mode-A。Tombstone 6.66%（连涨 15 轮），R81→R82 37min 增 13,307（~21.6K/h 晨间持续，~98.9% 新记录转 tombstone），再坐实增速与活动量成正比；安全阀 335 skip「78 entries」/0 触发/0 回收，启动唯一 flush 持锁 10.3s——R67 + 无锁 flush 双 P0 在 22.1h 持续硬证。**工程优先级维持四项 P0🔥（R67 检查顺序、mlock 多子区常驻【本轮强化为细粒度】、无锁 flush、查询超时）；字符串路径方差与 CJK trigram 为 P1🔥。**
