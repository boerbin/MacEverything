# MacEverything 性能分析报告 R_2606091830

## 1. 本轮范围

- 任务启动时间：2026-06-09 18:30
- 报告文件：`docs/performance_ana/R_2606091830.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6451`
  - `SEG_LINES=5509`
  - `LAST_LOG_TS=2026-06-09 13:47`

## 2. 执行摘要

**系统级资源竞争事件——3/16 >500ms，Max=975ms，P50=191ms，lockWait=18.2ms（系列最高！），极佳比例 3/16。** `x`=620ms（short-query-cache 命中但 queryTime=620ms——进程调度延迟灾难级！），`.pdf`=975ms（lockWait=18.2ms），`size:>100m`=648ms。**这不是简单的 page cache 淘汰——进程调度延迟和锁竞争同时飙升，说明系统级资源（CPU+I/O）正在被严重竞争。**

| 指标 | R214 | R215 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 1/16 | **3/16** | **↑2（系统级竞争）** |
| Max | 524ms | **975ms** | ↑86% |
| P50 | 179ms | **191ms** | ↑7% |
| lockWait max | 2.5ms | **18.2ms** | **↑628%（系列最高！）** |
| 极佳比例(<50ms) | 2/16 | **3/16** | ↑1 |
| tombstone | 1,849,220 (26.33%) | **1,871,930 (26.55%)** | ↑ |
| tombstone 增速 | ~486/min | **~757/min** | **↑56%（本会话最高！）** |
| recordCount | 7,024,096 | **7,050,190** | **+26.1K（本会话最高！）** |
| liveRecordCount | 5,174,876 | **5,178,260** | +3.4K |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 4933 | **4993** | +60 |
| COMPACTION | 2 | **2** | **不变——第 16 轮确认缺陷** |
| QUERYADV_GE500 | 50 | **52** | **↑2** |

### `x`=620ms——进程调度延迟灾难级！

- **phase2=0（short-query-cache 立即命中），queryTime=620ms——全是进程调度 overhead！**
- **620ms 是此前纪录（R214=180ms）的 3.4 倍！**
- **这意味着 MacEverything 进程在查询开始前等待了约 620ms 才获得 CPU 时间片。**
- **系统 CPU 被其他进程严重占用——可能是 Spotlight 重建索引、Time Machine 备份、或大型编译任务。**

### 进程调度延迟历史

| 轮次 | `x` queryTime | 说明 |
|------|-------------:|------|
| R201-R207 | 1-5ms | 稳态 |
| R208 | 18ms | 午后轻度 |
| R209 | 2ms | 恢复 |
| R210 | 16ms | 午后轻度 |
| R211 | **137ms** | 纪录 |
| R212 | 7ms | 恢复 |
| R213 | 1.4ms | 恢复 |
| R214 | **180ms** | 新纪录 |
| **R215** | **620ms** | **灾难级！↑244%** |

### `.pdf`=975ms——lockWait=18.2ms！

- **phase2=758ms，lockWait=18.2ms——page cache 淘汰 + 锁竞争双重打击。**
- **lockWait=18.2ms 是系列绝对最高！此前最高 R199=761ms（但那是 lockHeldMs，不是 lockWaitMs）。**
- **`*.swift` lockWait=5.4ms，`main.cpp` lockWait=2.1ms——多个查询出现锁竞争。**
- **锁竞争说明后台有频繁的索引更新操作——与 recordCount +26.1K 一致。**

### `size:>100m`=648ms——SoA 区域再次被淘汰

- **phase2=647ms，lockWait=0——纯 page cache 淘汰。**
- **第 2 次 >500ms（R210=742ms，R215=648ms）——确认 SoA 区域在 I/O 压力下脆弱。**

### lockWait 分布——多查询锁竞争

| Query | lockWait | 说明 |
|-------|--------:|------|
| `.pdf` | **18.2ms** | 系列最高 |
| `*.swift` | 5.4ms | 显著 |
| `main.cpp` | 2.1ms | 轻度 |
| 其余 13 查询 | 0ms | 正常 |

- **3/16 查询出现锁竞争——说明后台索引更新频率极高。**
- **recordCount +26.1K（本会话单轮最高）——大量 FSEvents 推送。**

### Trigram 路径——受压但分化

| Query | R214 | R215 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 524ms | **975ms** | ↑86% | **>500ms** |
| `hello world` | 281ms | **330ms** | ↑17% | 正常 |
| `*.swift` | 462ms | **208ms** | ↓55% | 正常（恢复） |
| `main.cpp` | 30ms | **314ms** | ↑947% | 正常（lockWait 影响） |
| `abc def...` | 179ms | **131ms** | ↓27% | 正常 |
| `README` | 58ms | **47ms** | ↓19% | 极佳 |
| `test` | 130ms | **181ms** | ↑39% | 正常 |
| `MacEverything` | 142ms | **19ms** | ↓87% | 极佳（恢复） |
| Trigram 均值 | 226ms | **276ms** | **↑22%** | 受压 |

- **Trigram 均值 226→276ms（↑22%）——持续受压。**
- **`.pdf`=975ms（↑86%）——连续 3 轮 >500ms（R214=524ms，R215=975ms）。**
- **`*.swift`=208ms（↓55% from 462ms）——恢复。**
- **`main.cpp`=314ms（lockWait=2.1ms 影响）。**

### 全表扫描——意外好转！

| Query | R214 | R215 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 224ms | **191ms** | ↓15% |
| `path:/Users` | 320ms | **192ms** | **↓40%** |
| `中文测试` | 282ms | **163ms** | **↓42%** |
| **总和** | **826ms** | **546ms** | **↓34%** |

- **全表扫描总和 826→546ms（↓34%）——意外好转！**
- **线性扫描区域被 battery 有效预热——即使在系统级竞争下仍保持良好。**
- **对比 trigram 区域恶化——说明 page cache 竞争主要影响 trigram postings（分散读取），而线性扫描（连续读取）更容易被预热。**

### tombstone 26.55%——增速创本会话最高

- **tombstone=1,871,930（26.55%）——超阈值 1.55%。**
- **增速 ~757/min——本会话最高！**
- **recordCount +26.1K——本会话单轮最高增量。**
- **liveRecordCount +3.4K——大量文件操作但净新增较少。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 619.7 | 0.0 | short-query-cache | 0 | 0 | **>500ms（调度延迟灾难！）** |
| `.pdf` | 5 | 974.5 | 757.6 | advanced-trigram | 18.2 | 9860 | **>500ms** |
| `main.cpp` | 5 | 314.0 | 292.2 | advanced-trigram | 2.1 | 82 | 正常（lockWait） |
| `*.swift` | 5 | 207.7 | 195.8 | advanced-trigram | 5.4 | 9345 | 正常 |
| `ext:cpp` | 5 | 56.0 | 55.4 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 648.1 | 646.7 | advanced-pure-filter-soa-gcd | 0 | 0 | **>500ms** |
| `hello world` | 5 | 329.7 | 325.1 | advanced-trigram | 0 | 3669 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 131.3 | 130.3 | advanced-trigram | 0 | 14360 | 正常 |
| `README` | 5 | 46.9 | 39.4 | advanced-trigram | 0 | 17052 | 极佳 |
| `test` | 5 | 180.8 | 167.4 | advanced-trigram | 0 | 116991 | 正常 |
| `MacEverything` | 5 | 19.2 | 1.5 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 120.1 | 117.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 191.3 | 182.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 191.7 | 186.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 163.3 | 162.3 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 191.3ms，P90 = 619.7ms，Max = 974.5ms。3/16 查询 >500ms。lockWait max = 18.2ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R214 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **3/16** | 0.2-47ms | R214=2/16（↑1） |
| **正常**（50-500ms） | 10/16 | 56-330ms | R214=13/16（↓3） |
| **超标**（>500ms） | **3/16** | 620-975ms | R214=1/16（**↑2**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,050,190，liveRecordCount=5,178,260（tombstone=**1,871,930**，**26.55%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=412  QUERYADV_GE500=52（↑2——.pdf, size:>100m；x 未计入因其为 cache 命中）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4993
```

## 6. 问题清单

### P0 确认（第 16 轮）：compaction 触发机制缺陷

- **tombstone 26.55%——超阈值 1.55%。增速 757/min（本会话最高）。**

### 系统级资源竞争——新发现级别

- **`x`=620ms（cache 命中但等 620ms）——进程几乎被饿死。**
- **lockWait=18.2ms（系列最高）——后台索引更新频率极高。**
- **recordCount +26.1K（本会话单轮最高）——大量 FSEvents 推送。**
- **三重压力叠加：进程调度延迟 + page cache 淘汰 + 锁竞争。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 16 轮确认）

### P0.5：flush rewrite 改为 COW 方式

- **lockWait=18.2ms 再次证明锁竞争的严重性——COW 方案可将写锁时间降至最低。**

### P1：进程优先级或 QoS 设置

- **`x`=620ms 的进程调度延迟是新问题——考虑设置 MacEverything 进程的 QoS 优先级。**
- **macOS 提供 `DISPATCH_QUEUE_PRIORITY_HIGH` 或 `QOS_CLASS_USER_INTERACTIVE`。**

### P2：madvise MADV_WILLNEED 热区维护（持续建议）

## 8. 下一轮重点

1. 3/16 >500ms 是否恢复。
2. `x` 进程调度延迟是否消退（620ms 是否为一次性事件）。
3. lockWait 是否回落。
4. tombstone 增长趋势。

## 9. 结论

**系统级资源竞争事件——3/16 >500ms，Max=975ms（`.pdf`），P50=191ms。** `x`=620ms（cache 命中但进程等待 620ms——灾难级调度延迟！此前纪录 180ms），lockWait=18.2ms（系列最高！），`size:>100m`=648ms。三重压力叠加：进程调度延迟 + page cache 淘汰 + 锁竞争。

**tombstone 26.55%（第 16 轮确认 compaction 缺陷）——增速 757/min（本会话最高），recordCount +26.1K（单轮最高）。** 全表扫描意外好转（546ms，↓34%）——线性连续读取比 trigram 分散读取更耐 page cache 竞争。新增 P1 建议：考虑进程 QoS 优先级设置。
