# MacEverything 性能分析报告 R_2606040810

## 1. 本轮范围

- 任务启动时间：2026-06-04 08:10
- 报告文件：`docs/performance_ana/R_2606040810.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21936`
  - `SEG_LINES=909`
  - `LAST_LOG_TS=2026-06-04 08:10`

## 2. 执行摘要

**R86 灾难事件后恢复中（第 1 轮）。** P50 ≈ 114ms，Max=474ms，**全部 16/16 查询 <500ms**。lockWaitMs 全部为 0。`.pdf`=474ms 仍偏高（接近 500ms 阈值），但已从灾难级 10,989ms 大幅恢复。

tombstone=417,135（6.82%），本轮增量 +8,257——**显著低于近 3 轮的 19-24K/轮**。SKIPFLUSH=332（仅 +1，远低于稳态 +12），确认 R86 灾难事件的 45 分钟锁持有导致 FSWatcher 事件处理严重积压。

### 横向对比（最近 5 轮）

| Query | R83 | R84 | R85 | R86 | R87 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 3 | 1 | 174 | 367 | **14** | **恢复** |
| `.pdf` | 38 | 303 | 1648 | 10989 | **474** | **恢复中** |
| `*.swift` | 19 | 90 | 118 | 4072 | **224** | **恢复中** |
| `hello world` | 69 | 272 | 72 | 450 | **230** | 恢复中 |
| `path:/Users` | 305 | 277 | 224 | 218 | **282** | 正常 |
| P50 | 49 | 86 | 104 | 361 | **114** | **恢复中** |

### R86 灾难事件恢复追踪

| 轮次 | P50 | Max | >500ms | lockWait max | 状态 |
|------|----:|----:|-------:|------------:|------|
| R84 (轻度) | 86ms | 310ms | 0/16 | 0ms | 轻度压力 |
| R85 (中度) | 104ms | 1648ms | 1/16 | 13.6ms | 中度 |
| **R86 (灾难)** | **361ms** | **2,731,551ms** | **5/16** | **1,010ms** | **灾难** |
| **R87 本轮** | **114ms** | **474ms** | **0/16** | **0ms** | **恢复中** |

**1 轮恢复速度远快于 R67 灾难（6 轮恢复）。** 但 `.pdf`=474ms 仍接近 500ms 阈值，尚未完全恢复到正常水平（正常值 34-171ms）。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 13.6 | 0.0 | short-query-cache | 0 | 0 | 正常（GCD 异常消失） |
| `.pdf` | 5 | 473.6 | 460.9 | advanced-trigram | 0 | 9949 | **接近 500ms** |
| `main.cpp` | 5 | 64.5 | 52.7 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 224.0 | 198.0 | advanced-trigram | 0 | 9356 | 偏高 |
| `ext:cpp` | 5 | 114.8 | 102.2 | advanced-ext-index | 0 | 3701 | 正常 |
| `size:>100m` | 5 | 111.2 | 110.8 | advanced-pure-filter-soa-gcd | 0 | 0 | **完全恢复** |
| `hello world` | 5 | 230.1 | 219.0 | advanced-trigram | 0 | 3613 | 偏高 |
| `abc def ghi jkl mno pqr` | 0 | 75.6 | 71.6 | advanced-trigram | 0 | 16006 | 正常 |
| `README` | 5 | 9.5 | 6.3 | advanced-trigram | 0 | 17679 | 极快 |
| `test` | 5 | 63.5 | 55.1 | advanced-trigram | 0 | 129957 | 正常 |
| `MacEverything` | 5 | 44.6 | 1.0 | advanced-trigram | 0 | 6803 | 正常 |
| `dm:today` | 5 | 72.8 | 71.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 226.2 | 215.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 281.7 | 277.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 221.4 | 220.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 1.3 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 114ms，P90 ≈ 282ms，Max = 474ms。全部 16/16 查询 <500ms。lockWaitMs 全部为 0。**

### 恢复速度对比：R67 vs R86

| 指标 | R67 灾难 | R86 灾难 |
|------|---------|---------|
| 峰值 Max | 10,902ms | 2,731,551ms |
| 灾难后第 1 轮 Max | 1169ms (R68) | **474ms (R87)** |
| 灾难后第 1 轮 >500ms | 2/16 (R68) | **0/16 (R87)** |
| 灾难后第 1 轮 lockWait | 59ms (R68) | **0ms (R87)** |
| 完全恢复轮数 | 6 轮 | **进行中（预估 1-2 轮）** |

**R86 灾难虽然峰值远超 R67（250×），但恢复速度更快。** 原因：R86 事件集中在 `size:>100m` 一个查询的 45 分钟锁持有，不是广泛的系统性 I/O 饱和。一旦该查询完成释放锁，系统迅速恢复。R67 是广泛 I/O 饱和（13/16 查询 >500ms），需要更久的缓存重建。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,150,592，liveRecordCount=5,733,457（tombstone=**417,135**，**6.82%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=286  QUERYADV_GE500=44
FLUSH=0  COMPACTION=2  SKIPFLUSH=332
DIAG_TRIGRAM_SKIP=92
```

- QUERYADV 从 278 增至 286（+8）。
- QUERYADV_GE500 保持 44（本轮 Max=474ms，无新增）。
- SKIPFLUSH 从 331 增至 332（**+1，远低于稳态 +12**）。
- DIAG_TRIGRAM_SKIP 从 91 增至 92（+1）。

### SKIPFLUSH +1 分析

- 连续 10 轮 +12/轮后，R86 为 +4，**R87 为 +1**。
- **原因**：R86 灾难事件（45 分钟锁持有）严重阻塞 FSWatcher 事件处理。事件结束后系统在消化积压，但 SKIPFLUSH 增速尚未恢复到稳态。
- **预期**：下轮 SKIPFLUSH 将恢复到 +12 或出现补偿性高值。
- **tombstone 增量也受影响**：本轮 +8,257（vs 近 3 轮 19-24K），部分事件仍在积压队列。

## 6. 问题清单

### Critical：tombstone 6.82%（417,135 条，125MB），灾难事件后增速暂缓

- 增长轨迹（近 5 轮）：
  ```
  R83: 358K (+18.9K) → R84: 382K (+23.8K) → R85: 406K (+23.9K)
  → R86: 409K (+2.7K) → R87: 417K (+8.3K)
  ```
- 增速下降是 R86 事件副作用（锁阻塞 FSWatcher），非真实减速。预计恢复后回到 12-24K/轮。
- **累计内存开销**：~417K × 300 字节 ≈ **125MB**。
- 连续第 27 轮超 2%，第 8 轮超过 5%，第 3 轮超过 6%。

### High：`.pdf` 恢复不完全（474ms，接近 500ms）

- 正常范围 34-171ms，本轮 474ms 仍显著偏高。
- **原因**：灾难事件后页面缓存仍未完全重建，trigram 索引部分页面仍需从磁盘换入。
- **预计**：1-2 轮内恢复到正常区间。

### Medium：`*.swift` 和 `hello world` 偏高

- `*.swift`=224ms（正常 19-172ms，偏高端）。
- `hello world`=230ms（正常 34-228ms，轻微超上限）。
- 同属灾难事件后缓存重建期的残余效应。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（6.82%，125MB，**已导致灾难级事件**）

- **R86 事件已实证**：tombstone 积累 → 内存压力 → 全表扫描 45 分钟 → 服务不可用。
- 当前增速暂缓是假象（事件副作用），恢复后将继续加速。
- **一行代码修复**：SKIPFLUSH 分支增加 `tombstoneRatio > 0.02` 检查。
- 预估收益：释放 125MB，消除 I/O 压力事件的根本驱动力。

### P0 紧急：`size:>100m` 全表扫描超时保护

- **R86 证明**：单查询可持锁 45 分钟，阻塞所有后续查询。
- 最小修复：phase1 遍历增加 5 秒超时，超时返回部分结果。

### P0：short-query-cache GCD 隔离

- 第 9 次异常（R86），33% 出现率。本轮恢复正常（14ms）。

### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. `.pdf` 是否恢复到 <200ms（完全恢复标志）。
2. SKIPFLUSH 是否出现补偿性跳增（积压事件释放）。
3. tombstone 增速是否回到 12-24K/轮。

## 9. 结论

**R86 灾难事件后恢复中（第 1 轮）。** P50 ≈ 114ms，Max=474ms，全部 16/16 <500ms，lockWaitMs=0。`.pdf`=474ms 仍接近阈值但已从 10,989ms 大幅恢复。恢复速度快于 R67 灾难（R87 第 1 轮即 0/16 >500ms，R67→R68 仍有 2/16）。原因：R86 灾难集中在单查询长时间持锁，而非 R67 式的广泛系统 I/O 饱和。tombstone=417,135（6.82%，125MB），增量 +8,257 为事件副作用暂缓（SKIPFLUSH 仅 +1），恢复后将回归加速期增速。**compaction 修复不再是预防——R86 已证明 tombstone 积累可导致 45 分钟级服务不可用。**
