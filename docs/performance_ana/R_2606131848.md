# MacEverything 性能分析报告 R_2606131848

## 1. 本轮范围

- 任务启动时间：2026-06-13 18:48
- 报告文件：`docs/performance_ana/R_2606131848.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=12287`
  - `SESSION_START_TS=2026-06-13 12:03:04.944`
  - `TOTAL_LINES=12848`
  - `SEG_LINES=562`
  - `LAST_LOG_TS=2026-06-13 18:48:02.543`

## 2. 执行摘要

**本轮 16-query battery 中 0/16 >500ms，Max 从 365.0ms 回落到 262.38ms，P90 从约 208.0ms 回落到 161.62ms。** P50≈30.92ms，极佳查询数仍为 11/16；所有 lockWait 均为 0ms。最高项仍是 `中文测试`=262.38ms，其次是 `path:/Users`=187.19ms、`content:import`=161.62ms、`dm:today`=81.29ms、`hello world`=65.0ms。

相比 18:18，本轮前台尾部整体改善：`中文测试` 从 365.0ms 降至 262.38ms，`path:/Users` 从 208.0ms 降至 187.19ms，`content:import` 从 166.8ms 小降至 161.62ms；但 P50 从约 25.7ms 升至约 30.92ms，说明低中位查询略有抬升。后台结构性风险继续恶化：recordCount=6,281,787，liveRecordCount=5,315,068，tombstone=966,719（15.39%），较上一轮增加 6,923；最新 session `FLUSH=80`，`SKIPFLUSH=0`，最新 flush total=2657ms，session 最大 flush total 仍为 9092ms。

| 指标 | R_2606131818 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 365.0ms | **262.38ms** | ↓28% |
| P50 | ≈25.7ms | **≈30.92ms** | ↑20% |
| P90 | ≈208.0ms | **≈161.62ms** | ↓22% |
| lockWait max | 0.0ms | **0.0ms** | 持平 |
| 极佳比例(<50ms) | 11/16 | **11/16** | 持平 |
| 扫描/过滤路径总和 | 818.3ms | **725.82ms** | ↓11% |
| recordCount | 6,274,360 | **6,281,787** | +7,427 |
| liveRecordCount | 5,314,564 | **5,315,068** | +504 |
| tombstone | 959,796 (15.30%) | **966,719 (15.39%)** | +6,923 |
| 最新 session FLUSH | 74 | **80** | +6 |
| 最新 session QUERYADV_GE500 | 0 | **0** | 持平 |
| 最新 flush total | 3361ms | **2657ms** | ↓21% |
| session 最大 flush total | 9092ms | **9092ms** | 持平 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 0.16 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 20.03 | 18.51 | advanced-trigram | 0.0 | 10158 | 极佳 |
| `main.cpp` | 5 | 16.12 | 12.33 | advanced-trigram | 0.0 | 103 | 极佳 |
| `*.swift` | 5 | 30.68 | 22.39 | advanced-trigram | 0.0 | 9622 | 极佳 |
| `ext:cpp` | 5 | 13.21 | 12.94 | advanced-ext-index | 0.0 | 3891 | 极佳 |
| `size:>100m` | 5 | 33.34 | 33.24 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 65.0 | 63.76 | advanced-trigram | 0.0 | 3932 | 正常，较上一轮抬升 |
| `abc def ghi jkl mno pqr` | 0 | 34.9 | 34.73 | advanced-trigram | 0.0 | 14624 | 极佳，较上一轮回落 |
| `README` | 5 | 13.29 | 11.87 | advanced-trigram | 0.0 | 17590 | 极佳 |
| `test` | 5 | 31.15 | 27.57 | advanced-trigram | 0.0 | 118022 | 极佳 |
| `MacEverything` | 5 | 5.34 | 1.71 | advanced-trigram | 0.0 | 7041 | 极佳 |
| `dm:today` | 5 | 81.29 | 81.09 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常，较上一轮抬升 |
| `content:import` | 5 | 161.62 | 150.0 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常高位 |
| `path:/Users` | 5 | 187.19 | 177.79 | advanced-linear-gcd | 0.0 | 0 | 正常高位，较上一轮改善 |
| `中文测试` | 0 | 262.38 | 261.77 | advanced-linear-gcd | 0.0 | 0 | 正常高位，本轮最高但明显回落 |
| `verylongquery...` | 0 | 1.12 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈30.92ms，P90≈161.62ms，Max=262.38ms。0/16 查询 >500ms。lockWait max=0.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **11/16** | 0.16-34.9ms | short-query-cache、轻量 trigram、ext-index 与 `size:>100m` 健康 |
| 正常（50-500ms） | **5/16** | 65.0-262.38ms | trigram、pure-filter、linear-gcd 仍有中高位项 |
| 超标（>500ms） | **0/16** | — | 本轮无正式超标查询 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,281,787；
- liveRecordCount=5,315,068；
- tombstone=966,719（**15.39%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=12287
SESSION_START_TS=2026-06-13 12:03:04.944
TOTAL_LINES=12848
SEG_LINES=562
QUERYADV=53
QUERYADV_GE500=0
FLUSH=80
SKIPFLUSH=0
COMPACTION=2
LAST_QUERYADV=[2026-06-13 18:48:02.543] [INFO ] [SearchEngine] QueryAdvanced "中文测试" total=262ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6281789
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 0，本轮没有日志级慢查询。`中文测试` 的日志 total=262ms，与 query battery 的 262.38ms 对齐；该项从上一轮 365.0ms 明显回落，但仍是本轮最高尾部。

```text
[2026-06-13 18:48:02.543] QueryAdvanced "中文测试" total=262ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6281789
```

### flush 情况

最新 session 内 `FLUSH=80`，较上一轮继续 +6；`SKIPFLUSH=0`。最新 flush total=2657ms，低于上一轮 3361ms，但仍是秒级完整重写；session 最大 flush total 仍为 **9092ms**。后台仍没有可见 backoff 或 skip 证据。

最新 flush：

```text
[2026-06-13 18:43:23.894] Flushed v6 flat index, lastEventId=363389365,
liveRecords=5315033 | timing: walSwap=0ms rewrite=2655ms total=2657ms
```

Top flushes：

```text
[2026-06-13 16:43:30.341] rewrite=9087ms total=9092ms
[2026-06-13 12:38:27.299] rewrite=3809ms total=6164ms
[2026-06-13 16:23:26.885] rewrite=5714ms total=5716ms
[2026-06-13 16:28:26.841] rewrite=5670ms total=5672ms
[2026-06-13 17:23:26.451] rewrite=5318ms total=5320ms
```

## 6. 问题清单

### P0：tombstone 继续上行，接近 97 万

- 证据：tombstone=966,719（15.39%），较上一轮增加 6,923。
- 影响：无效记录比例继续升高，会放大 scan/filter、linear-gcd 与 full-index rewrite 的实际工作量。
- 判断：虽然本轮前台尾部改善，但 tombstone 没有改善，长期仍会推高扫描类查询和 flush 成本。

### P0：full-index flush 继续按半小时 +6 累积

- 证据：`FLUSH=80`，较上一轮 +6；`SKIPFLUSH=0`；最新 flush total=2657ms，session 最大 total=9092ms。
- 影响：秒级完整重写持续制造 I/O 和 page-cache 干扰，并持续与前台查询共享系统资源。
- 判断：后台持久化策略仍无可见退避，属于最高优先级结构性风险。

### P0：linear-gcd 仍是最高前台尾部来源

- 证据：`中文测试`=262.38ms，phase2=261.77ms；`path:/Users`=187.19ms，phase2=177.79ms。
- 影响：中文/路径查询虽然本轮改善，但仍明显高于 trigram/ext-index 路径。
- 判断：lockWait=0ms，瓶颈明确在 phase2 扫描/过滤，不是锁等待。

### P1：pure-filter 仍稳定维持中高位

- 证据：`content:import`=161.62ms，`dm:today`=81.29ms，`size:>100m`=33.34ms。
- 影响：pure-filter 已成为 P90 附近稳定来源；不同 filter 的选择性差异较大。

### P1：trigram 多词查询有小尾部波动

- 证据：`hello world`=65.0ms，较上一轮 55.9ms 上升；`abc def ghi jkl mno pqr`=34.9ms，较上一轮 58.3ms 回落。
- 影响：trigram 总体健康，但多词 postings 交并和候选评分仍受系统状态波动影响。

## 7. 优化建议

### P0：为 full-index flush 增加硬性前台保护与退避

- 对 flush rewrite 超过 2s/5s/8s 建立分级退避：延后下一次 flush、降低 batch 频率、前台查询活跃时禁止大 rewrite。
- 持久化线程使用低 QoS，并评估大顺序写的 `F_NOCACHE` 或同类策略，减少对搜索热页的冲刷。
- 在日志中记录触发、跳过和退避原因，避免 `SKIPFLUSH=0` 长期无法解释。
- 验收：出现 5s+ flush 后下一周期必须看到 backoff 或 skip 证据；常规查询 P90 不再随 flush 抬升。

### P0：将 full-index flush 改为增量或 COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页或持锁窗口。
- 验收：常规 flush total <200ms，新 session 不再出现 3-9s 级完整 rewrite。

### P0：提前或分段 compaction，先阻止 tombstone 逼近 100 万

- 以 tombstone 率 14%+ 或绝对 tombstone 900k+ 作为明确触发信号；当前 15.39% 已超过应急阈值。
- 结合 segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 优先处理 tombstone 密集 segment，避免 scan/filter 与 flush rewrite 继续放大。
- 验收：tombstone 率从 15.39% 回落；`中文测试`、`path:/Users`、`content:import` 稳定 <200ms。

### P0：为 advanced-linear-gcd 与 pure-filter 增加轻量剪枝

- 对中文、路径、content/filter 类查询记录扫描记录数、tombstone 跳过数和 segment 分布。
- 评估轻量字符 n-gram、路径分段索引、预过滤位图或 segment-level skip list，以减少无结果或低选择性查询的全量扫描。
- 要求保持内存增长可控，优先使用压缩或按需结构，不引入大常驻索引。
- 验收：`中文测试` 与 `path:/Users` 稳定低于 200ms，`content:import` 不再维持 150ms+。

### P1：细分 top-level timing 与 scan/filter timing

- 对 pure-filter 和 linear-gcd 记录扫描记录数、跳过 tombstone 数、命中 live 数、segment 数、metadata 加载耗时和结果组装耗时。
- 对 `advanced-trigram` 记录 postings 读取、候选交并、候选评分、路径加载、结果组装和 page-cache miss 近似指标。
- 对所有路径增加 queueWait、candidate materialization、sort/rank、HTTP serialization/write 等 top-level timing。
- 验收：`queryTimeMs - phase2Ms - lockWaitMs` 不再是不可解释的大黑盒。

## 8. 下一轮重点

1. tombstone 是否继续从 966,719 上行并逼近 1,000,000。
2. `FLUSH` 是否继续每 30 分钟 +6。
3. `SKIPFLUSH` 是否仍为 0。
4. 是否继续出现 5s+ flush。
5. `中文测试` 是否能稳定低于 300ms，或重新接近 500ms。
6. `path:/Users` 是否能稳定低于 200ms。
7. `content:import` 是否继续维持 150ms+。
8. 新 session 中 `QUERYADV_GE500` 是否开始累积。

## 9. 结论

**本轮仍无正式慢查询：0/16 >500ms，P50≈30.92ms，P90≈161.62ms，Max=262.38ms。** `中文测试`、`path:/Users`、`content:import` 是主要中高位尾部，其中 `中文测试` 和 `path:/Users` 较上一轮改善，但瓶颈仍集中在 phase2 而非 lockWait。

**后台根因继续恶化：tombstone=966,719（15.39%），最新 session `FLUSH=80`，最大 flush total=9092ms，最新 flush total=2657ms，且 `SKIPFLUSH=0`。** 下一步最高优先级仍是 full-index flush 前台保护/退避与增量/COW flush，同时推进提前/分段 compaction、advanced-linear-gcd/pure-filter 剪枝，以及更细粒度 timing 拆分。
