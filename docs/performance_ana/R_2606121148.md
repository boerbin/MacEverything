# MacEverything 性能分析报告 R_2606121148

## 1. 本轮范围

- 任务启动时间：2026-06-12 11:48
- 报告文件：`docs/performance_ana/R_2606121148.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=9244`
  - `SESSION_START_TS=2026-06-10 17:02:22.835`
  - `TOTAL_LINES=10324`
  - `SEG_LINES=1081`
  - `LAST_LOG_TS=2026-06-12 11:48`

## 2. 执行摘要

**本轮出现明确前台回退，但尚未突破 500ms：full-scan 总和从 567.4ms 升至 978.3ms，trigram/filter 多路径同步变慢。** 16-query battery 中 0/16 >500ms，Max=368.8ms，P50≈54.5ms，lockWait=0。极佳比例从 11/16 降到 6/16。`content:import` 从 140.0ms 升到 368.8ms，`path:/Users` 从 218.2ms 升到 323.1ms，`中文测试` 从 209.2ms 升到 286.4ms；`.pdf`、`*.swift`、`hello world`、`dm:today` 也同步回升。

最新运行段日志显示：`QUERYADV_GE500=5` 无新增，`FLUSH=298`（较上一轮 +12），`SKIPFLUSH=0`。最新 flush 为 rewrite=4009ms、total=4016ms，常规 flush 从 3105ms 进一步升至 4016ms。历史灾难 outlier 仍存在：rewrite 最大 895,940ms，total 最大 896,053ms。tombstone=490,860（8.52%），继续扩大。

| 指标 | R_2606121117 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 218.2ms | **368.8ms** | ↑69% |
| P50 | 31.8ms | **54.5ms** | ↑71% |
| lockWait max | 0ms | **0ms** | 持平 |
| 极佳比例(<50ms) | 11/16 | **6/16** | -5 |
| 全表扫描总和 | 567.4ms | **978.3ms** | ↑72% |
| recordCount | 5,746,603 | **5,763,917** | +17,314 |
| liveRecordCount | 5,271,893 | **5,273,057** | +1,164 |
| tombstone | 474,710 (8.26%) | **490,860 (8.52%)** | +16,150 |
| 最新段 FLUSH | 286 | **298** | +12 |
| 最新段 QUERYADV_GE500 | 5 | **5** | 无新增 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 2.9 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 120.3 | 115.6 | advanced-trigram | 0.0 | 9836 | 正常（回升） |
| `main.cpp` | 5 | 20.1 | 9.1 | advanced-trigram | 0.0 | 85 | 极佳 |
| `*.swift` | 5 | 84.1 | 82.2 | advanced-trigram | 0.0 | 9430 | 正常（回升） |
| `ext:cpp` | 5 | 24.4 | 23.4 | advanced-ext-index | 0.0 | 3616 | 极佳 |
| `size:>100m` | 5 | 52.2 | 52.0 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `hello world` | 5 | 107.0 | 93.4 | advanced-trigram | 0.0 | 3930 | 正常（回升） |
| `abc def ghi jkl mno pqr` | 0 | 56.7 | 56.0 | advanced-trigram | 0.0 | 14517 | 正常 |
| `README` | 5 | 12.1 | 5.7 | advanced-trigram | 0.0 | 17383 | 极佳 |
| `test` | 5 | 40.1 | 24.9 | advanced-trigram | 0.0 | 117814 | 极佳 |
| `MacEverything` | 5 | 38.4 | 1.4 | advanced-trigram | 0.0 | 6945 | 极佳 |
| `dm:today` | 5 | 145.4 | 132.1 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常（继续回升） |
| `content:import` | 5 | 368.8 | 356.0 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常（偏高） |
| `path:/Users` | 5 | 323.1 | 315.0 | advanced-linear-gcd | 0.0 | 0 | 正常（偏高） |
| `中文测试` | 0 | 286.4 | 279.8 | advanced-linear-gcd | 0.0 | 0 | 正常（回升） |
| `verylongquery...` | 0 | 1.0 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈54.5ms，P90≈323.1ms，Max=368.8ms。0/16 查询 >500ms。lockWait=0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **6/16** | 1.0-40.1ms | short-query-cache、小候选 trigram 与 ext-index 仍健康 |
| 正常（50-500ms） | **10/16** | 52.2-368.8ms | full-scan、filter 与部分 trigram 查询同步变慢 |
| 超标（>500ms） | **0/16** | — | 本轮无前台超时 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=5,763,917；
- liveRecordCount=5,273,057；
- tombstone=490,860（**8.52%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=9244
SESSION_START_TS=2026-06-10 17:02:22.835
TOTAL_LINES=10324
SEG_LINES=1081
QUERYADV=80
QUERYADV_GE500=5
FLUSH=298
SKIPFLUSH=0
COMPACTION=2
```

### 慢查询证据

最新 session 内慢查询数量仍为 5 条，本轮未新增 `>=500ms` 慢查询：

```text
[2026-06-10 17:17:09.559] QueryAdvanced "path:/Users" total=525ms | path=advanced-linear-gcd
[2026-06-10 17:25:15.434] QueryAdvanced "lllllllllllllllllllllllllllllllllllllllllllll" total=1154ms | path=advanced-trigram
[2026-06-10 17:47:13.642] QueryAdvanced "中文测试" total=716ms | path=advanced-linear-gcd
[2026-06-10 23:44:25.566] QueryAdvanced "test" total=676ms | path=advanced-trigram
[2026-06-12 09:17:32.430] QueryAdvanced "中文测试" total=555ms | path=advanced-linear-gcd
```

### flush 极端长尾

最新 session 内 `FLUSH=298`，rewrite 范围 **1440ms - 895940ms**，total 最大 **896053ms**。

最新 flush：

```text
[2026-06-12 11:47:04.261] Flushed v6 flat index, lastEventId=348496505,
liveRecords=5273107 | timing: walSwap=0ms rewrite=4009ms sqcache=5ms total=4016ms
```

Top flush outliers：

```text
[2026-06-12 03:15:02.833] rewrite=895940ms sqcache=2ms total=896053ms
[2026-06-10 23:24:50.749] rewrite=725091ms sqcache=18ms total=735223ms
[2026-06-11 14:30:37.596] rewrite=58273ms sqcache=5ms total=101726ms
[2026-06-10 21:16:27.157] rewrite=32497ms sqcache=5ms total=230084ms
[2026-06-10 23:26:19.499] rewrite=27557ms sqcache=10ms total=27834ms
```

## 6. 问题清单

### P0：前台查询出现系统性回退

- 证据：P50 31.8ms → 54.5ms，极佳比例 11/16 → 6/16，full-scan 总和 567.4ms → 978.3ms。
- 影响：尚未超过 500ms，但已经接近此前 `中文测试` 和 `path:/Users` 慢查询形态。
- 判断：不是单点查询异常，而是 trigram、pure-filter、linear-gcd 多路径同步变慢。

### P0：flush 完整重写常规成本升至 4 秒级

- 证据：最新 flush total=4016ms，最近几轮为 2737ms → 3105ms → 4016ms。
- 影响：即使不看分钟级 outlier，常规 flush 对 page cache 和 I/O 的扰动也在加重。

### P0：flush 完整重写仍有分钟级灾难长尾

- 证据：rewrite 最大 895,940ms，total 最大 896,053ms。
- 根因：完整索引重写在 APFS COW、系统 I/O 压力、睡眠/唤醒或后台任务竞争下被放大。

### P0：flush 频率继续上升且没有退避

- 证据：最新 session `FLUSH=298`，较上一轮 +12；`SKIPFLUSH=0`。
- 影响：高频完整重写持续污染 page cache，增加 filter/full-scan 波动概率。

### P0：tombstone 增长加速并突破 8.5%

- 证据：tombstone=490,860（8.52%），较上一轮增加 16,150。
- 影响：索引体积、flush rewrite 成本和扫描成本继续扩大。

## 7. 优化建议

### P0：flush 改为增量/COW 页写入

- 分离 stable segment 与 delta segment；常规 flush 只追加 delta。
- 后台低优先级合并，不阻塞前台查询。
- 验收：flush rewrite max <200ms，latest session 不再出现分钟级 outlier。

### P0：为 flush 增加 I/O 压力退避和前台保护

- flush 超过阈值后降低下一轮频率。
- 前台查询活跃时推迟大 flush。
- 持久化线程使用更低 QoS，减少对搜索热路径的干扰。

### P0：降低 flush 频率并合并 WAL 批次

- 引入最小 flush 间隔和最大合并窗口。
- 对高 churn 场景减少完整重写次数。

### P1：提前或分段 compaction

- 不等待全局 25% tombstone 阈值。
- 按 segment 或绝对 tombstone 数触发局部 compaction。
- 验收：tombstone 率从 8.5%+ 回落，并且常规 flush rewrite 时间下降。

### P2：保护搜索热数据 page cache

- 对 flush 大顺序写考虑 `F_NOCACHE` 或同类策略。
- 对搜索热区使用 `madvise` 预热/保持。

## 8. 下一轮重点

1. `content:import` 是否继续接近或突破 500ms。
2. full-scan 总和是否继续高于 900ms。
3. 最新 flush 是否继续高于 4 秒。
4. tombstone 是否继续向 9% 靠近。
5. latest session 慢查询计数是否从 5 增长。

## 9. 结论

**本轮前台出现系统性回退：P50≈54.5ms、Max=368.8ms、极佳比例降至 6/16、full-scan 总和升至 978.3ms。** 当前仍没有 >500ms 查询，但 `content:import`=368.8ms、`path:/Users`=323.1ms 已接近风险区间。

**后台结构性风险继续恶化：FLUSH=298、SKIPFLUSH=0、tombstone=490,860（8.52%），最新 flush=4016ms，历史最大 rewrite 895,940ms。** 当前最优先的修复仍是 COW/增量 flush、I/O 退避、降低 flush 频率、提前或分段 compaction。
