# MacEverything 性能分析报告 R_2606101230

## 1. 本轮范围

- 任务启动时间：2026-06-10 12:30
- 报告文件：`docs/performance_ana/R_2606101230.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=8734`
  - `SEG_LINES=7792`
  - `LAST_LOG_TS=2026-06-10 10:47`

## 2. 执行摘要

**⚠️⚠️ 系列最严重——R249 记录再次被刷新！** `ext:cpp`=3097ms（新系列记录），`.pdf`=2829ms，**7/16 >500ms（系列最多）**。Max=3097ms，P50=313ms，lockWait max=49.8ms（回归）。**`x`=285ms（short-query-cache——系统级阻塞再次爆发）**。QUERYADV_GE500 从 65 跳至 71（+6）。**I/O 风暴未消退——正在加剧！R249→R250"部分恢复"只是暂态。**

| 指标 | R250 | R251 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 1/16 | **7/16** | **↑6——系列新记录！** |
| Max | 511ms | **3097ms** | **↑506%——系列新记录！** |
| P50 | 110ms | **313ms** | ↑185% |
| lockWait max | 0ms | **49.8ms** | **再次回归** |
| 极佳比例(<50ms) | 3/16 | **1/16** | ↓2 |
| tombstone | 2,202,324 (29.71%) | **2,226,784 (29.95%)** | **↑——即将突破 30%！** |
| tombstone 增速 | ~278/min | **~815/min** | **↑193%——爆发性增长！** |
| recordCount | 7,412,757 | **7,442,309** | **+29.6K（通常 +7K）** |
| liveRecordCount | 5,210,433 | **5,215,525** | **+5.1K（通常 +200）** |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 6959 | **7021** | +62 |
| COMPACTION | 2 | **2** | **不变——第 52 轮确认缺陷** |
| QUERYADV_GE500 | 65 | **71** | **+6** |

### ⚠️⚠️ 连续两轮 >500ms——系统级 I/O 风暴持续

**R249→R250→R251 构成三轮事件链：**

| 轮次 | >500ms | Max | `x` (cache) | lockWait | QUERYADV_GE500 |
|------|------:|------:|------:|------:|------:|
| R248 | 0/16 | 306ms | 3ms | 0ms | 60 |
| R249 | 4/16 | 2511ms | 153ms | 11.2ms | 64 |
| R250 | 1/16 | 511ms | 13ms | 0ms | 65 |
| **R251** | **7/16** | **3097ms** | **285ms** | **49.8ms** | **71** |

- **R250 的"部分恢复"只是暂态——I/O 风暴仍在持续。**
- **R251 比 R249 更严重：7/16 >500ms（vs 4/16），Max=3097ms（vs 2511ms）。**
- **`x`=285ms（vs R249=153ms）——系统级阻塞进一步加剧。**

### recordCount 爆发性增长

- **recordCount +29.6K（通常 +7K）——4.2× 正常增速。**
- **liveRecordCount +5.1K（通常 +200）——25× 正常增速。**
- **tombstone 增速 ~815/min（vs ~278/min）——3× 增速。**
- **这意味着系统正在经历大规模文件变更事件（可能是 Xcode 构建、npm install、大批量解压等）。**
- **大量文件变更 → 大量 FSEvents → 大量索引更新 → I/O 争用 → page cache 淘汰。**

### tombstone 即将突破 30%

- **tombstone=2,226,784（29.95%）——距 30% 仅 0.05%！**
- **以当前 ~815/min 增速，约 3-5 分钟内将突破 30%。**

### lockWait 回归分析

| Query | lockWaitMs | 备注 |
|-------|----------:|------|
| `main.cpp` | 23.5 | 中等 |
| `abc def...` | 20.0 | 中等 |
| `MacEverything` | **49.8** | **本轮最高** |
| `中文测试` | 19.0 | 中等 |

- **4/16 查询出现 lockWait——max=49.8ms（`MacEverything`）。**
- **仍远低于 R232=286.6ms——这不是 flush 写锁。**
- **lockWait 分布在 battery 中间和末尾——系统级互斥锁争用。**

### Trigram 路径——极端恶化

| Query | R250 | R251 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 511ms | **2829ms** | ↑454% | **⚠️ >500ms** |
| `hello world` | 375ms | **1905ms** | ↑408% | **⚠️ >500ms** |
| `*.swift` | 459ms | **810ms** | ↑76% | **⚠️ >500ms** |
| `main.cpp` | 70ms | **134ms** | ↑91% | 正常 |
| `abc def...` | 83ms | **800ms** | ↑863% | **⚠️ >500ms** |
| `README` | 44ms | **93ms** | ↑111% | 正常 |
| `test` | 95ms | **1230ms** | ↑1195% | **⚠️ >500ms** |
| `MacEverything` | 22ms | **73ms** | ↑232% | 正常 |
| Trigram 均值 | 195ms | **984ms** | **↑405%** |

- **Trigram 均值 195→984ms——5/8 trigram >500ms。**
- **`test`=1230ms（phase2=137ms，overhead=1093ms）——1 秒 overhead！系统级调度灾难。**

### 全表扫描——反向改善（零和）

| Query | R250 | R251 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 169ms | **103ms** | **↓39%** |
| `path:/Users` | 203ms | **147ms** | **↓28%** |
| `中文测试` | 213ms | **73ms** | **↓66%** |
| **总和** | **585ms** | **323ms** | **↓45%** |

- **全表扫描总和 585→323ms（↓45%）——极端零和：trigram 灾难性恶化 vs 全表扫描改善。**
- **SoA 连续扫描由于系统 I/O 实际上被缓冲了。**

### 辅助路径

| Query | R250 | R251 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 13ms | **285ms** | ↑2092%（系统阻塞） | 正常 |
| `ext:cpp` | 110ms | **3097ms** | ↑2715% | **⚠️ >500ms 系列记录** |
| `size:>100m` | 368ms | **1244ms** | ↑238% | **⚠️ >500ms** |
| `dm:today` | 61ms | **313ms** | ↑413% | 正常 |
| `verylongquery...` | 1ms | **0.5ms** | ↓50% | 极佳 |

- **`ext:cpp`=3097ms（phase2=2752ms）——ext-index 也被 I/O 风暴影响。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 285.4 | 0.0 | short-query-cache | 0 | 0 | **异常（系统阻塞）** |
| `.pdf` | 5 | **2829.0** | 2591.5 | advanced-trigram | 0 | 9860 | **⚠️ >500ms** |
| `main.cpp` | 5 | 134.4 | 96.8 | advanced-trigram | 23.5 | 82 | 正常 |
| `*.swift` | 5 | **810.3** | 691.3 | advanced-trigram | 0 | 9353 | **⚠️ >500ms** |
| `ext:cpp` | 5 | **3097.1** | 2751.5 | advanced-ext-index | 0 | 3571 | **⚠️ >500ms 系列记录** |
| `size:>100m` | 5 | **1244.4** | 1087.6 | advanced-pure-filter-soa-gcd | 0 | 0 | **⚠️ >500ms** |
| `hello world` | 5 | **1904.5** | 1806.4 | advanced-trigram | 0 | 3671 | **⚠️ >500ms** |
| `abc def ghi jkl mno pqr` | 0 | **799.9** | 677.6 | advanced-trigram | 20.0 | 14485 | **⚠️ >500ms** |
| `README` | 5 | 93.0 | 83.5 | advanced-trigram | 0 | 17153 | 正常 |
| `test` | 5 | **1230.0** | 136.8 | advanced-trigram | 0 | 117176 | **⚠️ >500ms** |
| `MacEverything` | 5 | 72.7 | 7.0 | advanced-trigram | 49.8 | 6859 | 正常 |
| `dm:today` | 5 | 313.1 | 28.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 103.0 | 68.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 147.2 | 132.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 72.8 | 49.2 | advanced-linear-gcd | 19.0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 313.1ms，P90 = 1904.5ms，Max = 3097.1ms。7/16 查询 >500ms。lockWait max = 49.8ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R250 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **1/16** | 0.5ms | R250=3/16（↓2） |
| **正常**（50-500ms） | 8/16 | 73-313ms | R250=12/16（↓4） |
| **超标**（>500ms） | **7/16** | 800-3097ms | R250=1/16（**↑6——系列最多**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,442,309，liveRecordCount=5,215,525（tombstone=**2,226,784**，**29.95%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=631  QUERYADV_GE500=71（+6！）
FLUSH=1  COMPACTION=2  SKIPFLUSH=7021
```

## 6. 问题清单

### P0 确认（第 52 轮）：compaction 触发机制缺陷

- **tombstone=2,226,784（29.95%）——即将突破 30%！超阈值 4.95%。**
- **52 轮连续确认，COMPACTION=2 始终不变。**

### P0：系统级 I/O 风暴（持续加剧）

- **R249→R251 三轮事件链——不是一次性事件！**
- **`x`=285ms（系统阻塞），recordCount +29.6K（4.2× 正常）。**
- **大量文件变更导致 FSEvents → 索引更新 → I/O 争用 → 全面性能恶化。**

### P0：flush 写锁阻塞

- **lockWait max=49.8ms（再次回归——系统级争用）。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（第 52 轮——tombstone 即将突破 30%！）

### P1（升级为 P0）：进程 QoS / I/O 优先级保护

- **连续 3 轮系统级 I/O 风暴——P1 严重程度不足。**
- **`x`=285ms 证明纯内存操作被系统阻塞——进程需要 I/O 优先级保护。**

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. I/O 风暴是否消退（3 轮持续，能否在 R252 恢复？）。
2. tombstone 是否突破 30%（当前 29.95%——几乎肯定）。
3. recordCount 增长是否回归正常（+29.6K vs 正常 +7K）。

## 9. 结论

**⚠️⚠️ 系列最严重——R249 记录全面刷新！** `ext:cpp`=3097ms（新系列记录），7/16 >500ms（系列最多），Max=3097ms，P50=313ms。系统级 I/O 风暴持续加剧——`x`=285ms（纯内存操作被阻塞），recordCount +29.6K（4.2× 正常增速，大量文件变更触发 FSEvents 风暴）。lockWait max=49.8ms（再次回归）。极端零和：trigram 984ms 灾难 vs 全表扫描 323ms（↓45% 改善）。QUERYADV_GE500 从 65 跳至 71（+6）。

**tombstone 29.95%——即将突破 30%！** 增速 ~815/min（爆发性 3× 增长）。第 52 轮 compaction 缺陷确认。**建议将 I/O 优先级保护从 P1 升级为 P0。**
