# MacEverything 性能分析报告 R_2605310841

- **任务启动时间**：2026-05-31 08:47（cadence 边界 08:41 → 文件名 R_2605310841）
- **日志范围**：最近一次启动（line 10631，`2026-05-31 00:09:38.774`，app 死亡后重启）至今，连续运行 ~9.2h
- **状态快照**：任务始 @08:47 total `6,347,729` / live `5,416,557` / tomb `931,172`（**14.67%**）；测末 @09:19 total `6,370,711` / live `5,417,638` / tomb `953,073`（**14.96%**）
- **本轮定性**：🟡 **全系列最干净测量环境 + 分裂 36/36 稳健，但 tombstone 越过 12.97% 参考线而 app 仍稳定（参考线性质需重校准）**——本轮日志 0 skip-gate / 0 WARN / 0 ERROR / 0 非测量越线，电池 16 项全部 <100ms，无任何污染 marker（系列最干净）。分裂 36/36 四估计法全 HOLDS（字面间隙 2.35×）。机制隔离重大进展：**skip-gate=0 但 compaction 仍=0**——flush 每 300s 正常发生（entryCount 阀 `:142` 已满足），却仍零回收，**干净证明 `:159` 比例阀（0.25）是唯一阻断点**（比 R101 更纯的隔离）。tombstone **14.96%** 已越过 12.97% 参考线而 app 稳定运行 9.2h——**参考线更像相关性巧合而非硬崩溃阈值**（诚实修正）。R100 的 linear-gcd 非平稳本轮**转移到中文**（path 反而极平稳 1.00×），再证 page-residency 伪影性质。

---

## 1. 原始电池（系列最干净，零污染 marker）

| query | path | total(ms) | phase2 | trig | lockHeld | cand | 备注 |
|---|---|---|---|---|---|---|---|
| `x` | short-query-cache | 3.04 | 0 | 0 | 0 | 0 | 干净 |
| `.pdf` | advanced-trigram | 43.18 | 39.63 | 2.04 | 41.92 | 10183 | 干净 |
| `main.cpp` | advanced-trigram | 17.16 | 10.66 | 6.49 | 17.16 | 83 | 干净（floor 0.27）|
| `*.swift` | advanced-trigram | 46.25 | 43.88 | 2.33 | 46.21 | 9309 | 干净 |
| `ext:cpp` | advanced-ext-index | 27.59 | 27.16 | 0 | 27.55 | 3122 | 干净（<500 无污染）|
| `size:>100m` | pure-filter-soa-gcd | 17.76 | 17.74 | 0 | 17.74 | 0 | 数值（floor 8.02）|
| `hello world` | advanced-trigram | 63.92 | 61.94 | 1.95 | 63.90 | 2387 | 干净 |
| `abc def ghi jkl mno pqr` | advanced-trigram | 51.58 | 51.23 | 0.34 | 51.57 | 15965 | 干净 |
| `README` | advanced-trigram | 16.41 | 10.35 | 6.02 | 16.37 | 16726 | 干净 |
| `test` | advanced-trigram | 30.13 | 16.91 | 13.15 | 30.07 | 138293 | 干净（floor 5.71）|
| `MacEverything` | advanced-trigram | 18.13 | 1.26 | 16.84 | 18.11 | 6751 | 干净 |
| `dm:today` | pure-filter-soa-gcd | 23.44 | 23.36 | 0 | 23.36 | 0 | 数值（floor 18.40，紧致）|
| `content:import` | pure-filter-soa-gcd | 72.45 | 65.01 | 0 | 65.02 | 0 | 字符串（平稳锚 1.36×）|
| `path:/Users` | advanced-linear-gcd | 76.45 | 72.61 | 0 | 72.61 | 0 | 字符串（floor 极平稳 1.00×）|
| `中文测试` | advanced-linear-gcd | 51.53 | 50.94 | 0.13 | 51.52 | 0 | 字符串（floor 非平稳 3.11×）|
| `verylongquery…really` | advanced-trigram | 1.81 | 0 | 1.80 | 1.80 | 0 | |

**无 marker#3**：本轮 `main.cpp` 仅 17.16ms/83cand（vs R101 597ms/71cand），floor 复测 0.27ms。本轮**零写者饿死读者签名**——电池全项 <100ms，是 R67 系列开始以来最干净的一次组织查询。

---

## 2. 复测消歧（标志 marker 全部干净）

| query | 电池值 | probe1 | probe2 | probe3 | probe4 | 判定 |
|---|---|---|---|---|---|---|
| `main.cpp`（83 cand）| 17.16 | 23.60 | 32.90 | 0.27 | 0.88 | ✅ floor 0.27（64× 恢复，probe1/2 为并发 floor/probe 互扰）|
| `test`（138K cand）| 30.13 | 171.76 | 5.71 | 12.69 | 11.50 | ✅ floor 5.71（probe1 冷/争用瞬时）|

无稳态 marker，环境可评估。

---

## 3. FLOOR-METHOD 双窗口（各 8×，R100 平稳性协议）

W1（撞 09:12 ingest 突发，warm-up 污染）+ W2（干净）：

**窗口内 spread（W2 干净窗口）：**

| query | 类型 | W2 min | W2 med | W2 max | spread | 平稳性 |
|---|---|---|---|---|---|---|
| `size` | 数值 pure-filter | 8.02 | ~11.9 | 43.41 | 1.81×（去冷首）| 较紧 |
| `dm` | 数值 pure-filter | 18.40 | ~23.7 | 28.54 | **1.55×** | **紧致** |
| `content` | 字符串 pure-filter | 63.13 | ~67.3 | 81.13 | **1.29×** | **平稳** |
| `path` | 字符串 linear-gcd | 54.70 | ~63.3 | 104.48 | 1.91× | 较紧 |
| `中文` | 字符串 linear-gcd | 43.29 | ~55.1 | 61.36 | **1.42×** | **紧致** |

**跨窗口 floor 摆动（R100 非平稳检验）：**

| query | 类型 | W1 floor | W2 floor | 摆动 |
|---|---|---|---|---|
| `size` | pure-filter | 12.39 | 8.02 | 1.55× |
| `dm` | pure-filter | 33.23 | 18.40 | 1.81× |
| `content` | pure-filter | 46.47 | 63.13 | 1.36×（反向，平稳）|
| `path` | linear-gcd | 54.44 | 54.70 | **1.00×（极平稳）** |
| `中文` | linear-gcd | 134.52 | 43.29 | **3.11×（非平稳）** |

**关键观察——非平稳从 path 转移到中文**：本轮 W1 中文 8 次全 134-192ms（page-cold），W2 降至 43-61ms（暖），摆动 3.11×；而 path 跨窗口 1.00× 极平稳——与 R100/R101（path 摆动）方向相反。证明：**linear-gcd 非平稳是 page-residency 伪影**——窗口启动时哪个串池冷，哪个就摆动，非算法性、非固定路径。W1 size 两次撞突发（2354/1902ms）亦为 ingest 瞬时。

---

## 4. 洁净 ns/槽（floor 法，records=6,347,729，floor=双窗口 min）

| 路径 | 类型 | floor(ms) | 洁净值(ns/槽) | 说明 |
|---|---|---|---|---|
| `size:>100m` | 数值 | 8.02 | **1.26** | 会话新低 |
| `dm:today` | 数值 | 18.40 | **2.90** | 紧致簇（1.55×），高置信 |
| `中文测试` | 字符串 | 43.29 | **6.82** | ⚠️ 非平稳 floor（字面 min）|
| `content:import` | 字符串 | 46.47 | **7.32** | 平稳锚（1.36×）|
| `path:/Users` | 字符串 | 54.44 | **8.58** | 极平稳（1.00×）|

数值层 dm 2.90（紧致簇佐证真热态）。字符串层本轮整体偏暖（中文 6.82 / content 7.32 / path 8.58），低于 R101（path 10.48 / content 12.28）。

---

## 5. 绝对二层分裂：36/36（四估计法全 HOLDS）

| 解读 | 最低字符串 | 最高数值 | 间隙 | 对抗检验 |
|---|---|---|---|---|
| **字面绝对 min** | 中文 6.82ns | dm 2.90ns | **2.35×** | ✅ HOLDS |
| **平稳锚（content）** | content 7.32ns | dm 2.90ns | **2.52×** | ✅ HOLDS |
| **平稳锚（path 1.00×）** | path 8.58ns | dm 2.90ns | **2.96×** | ✅ HOLDS |
| **dm 用中位** | 中文 6.82ns | dm-median 3.73ns | **1.83×** | ✅ HOLDS |

- **数值层**：size 1.26、dm 2.90 → 最高数值 **dm 2.90**
- **字符串层**：中文 6.82（字面 min，非平稳）/ content 7.32（平稳锚）/ path 8.58（极平稳）→ 最低 **中文 6.82**
- **对抗检验（dm 最差洁净 28.54ms=4.50ns）**：< 中文 floor 6.82 **HOLDS**、< content floor 7.32 **HOLDS** ✅

四种估计法全部不破，**分裂 36/36**。本轮间隙 2.35×（字面）较 R101（4.05×）收窄，主因数值层略不及 R101 暖（dm 2.90 vs 2.59）**且**字符串层本轮更暖（中文 6.82 vs R101 path 10.48）——是数值层与字符串层相对暖度变化，非机制变化。仍属机制级分裂（纯数值零解引用 ≪ 字符串解引用散布串池）。

---

## 6. 阀门证据：本轮干净隔离 ratio-valve 为唯一阻断点

| 指标 | 值 |
|---|---|
| `Skipping flush — only … entries` | **0 次**（R101 761）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush（v6 flat rewrite）| 每 ~300s 一次，rewrite **1503-3173ms** |

**机制隔离（比 R101 更纯）**：本轮 skip-gate=0 意味着 entryCount 阀（`:142`，阈值 100）**每周期都被满足**——flush 确实每 5min 发生（日志 `Flushed v6 flat index` 连续可见，liveRecords 5,412,681→5,413,089 爬升）。**然而 compaction 仍=0、Reclaimed 仍=0**。

→ R101 无法区分「skip-gate 阻断 flush」与「ratio-valve 阻断 compaction」两种可能；**本轮 flush 正常发生却仍零回收，干净证明 `:159` 比例阀（0.25）是唯一阻断点**，与 entryCount 阀无关。根因 R106 确认：0.25 > 当前 0.1496，结构性永不可达。每次 flush 全量重写 6.37M 记录（含 953K=15% tombstone），15% I/O 浪费（R107 rewrite-bloat）。

---

## 7. tombstone 趋势：14.96%，越过 12.97% 参考线而 app 稳定

| 轮次 | 时间 | tombstone | ratio | 备注 |
|---|---|---|---|---|
| R101 | 05-30 18:41 | 622,035 | 10.32% | |
| R108 | 05-30 22:11 | (~) | 12.47% | 彼时系列最干净 |
| (R109) | 05-30 22:41 | — | — | 服务死亡，报告未写 |
| (restart) | 05-31 00:09:38 | — | — | app 重启 |
| **本轮始** | 05-31 08:41 | **931,172** | **14.67%** | **越过 12.97% 参考线** |
| 本轮末 | 05-31 09:19 | 953,073 | 14.96% | 窗口 +21,901，95.3% 入墓 |

**诚实修正——12.97% 参考线性质需重校准**：R43 的 12.97%（~800K）一直被当作崩溃参考。但本轮 app 在 **14.96%** 稳定运行 9.2h，**已决定性越过该线而未崩溃**。结合时间线：R108@22:11(12.47%) → ~22:41 服务死亡 → 00:09:38 重启 → 现 14.96%。死亡点（~12.5-13%）与 12.97% **巧合接近**，但：(1) 死因未证（可能与 tombstone 无关）；(2) 重启后载入持久化索引仍含高 tombstone，却稳定爬到 15%。**更可能的解读：12.97% 是相关性巧合而非因果硬阈值**——但 ingest-without-reclaim 单调累积仍是会话内唯一真实退化轴，I/O 浪费随比例线性恶化，修复仍紧迫（不因「未崩溃」而降级）。窗口内 rec +22,982 / live +1,081 → **95.3% 新增入墓**，再确认 churn 模式。

---

## 8. 日志健康度（系列最干净）

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **73 条**
- 延迟分布（QueryAdvanced-only）：>5s=**0**、1-5s=**2**、0.5-1s=**0**、0.1-0.5s=**71**、中位落在 0.1-0.5s
- **越线条目枚举**：仅 2 条 >500ms，**全部为本轮 W1 floor 协议自致**——09:12:26 **2354ms** + 09:12:29 **1902ms**（均 `size:>100m`，撞 09:12 ingest 突发 totalRecords=6,365,774→6,365,801）。**零非测量越线**——这是 R67 系列以来最干净的有机日志（超越 R108）。
- 启动基建事件须排除：Phase 2 `total=19079ms`、sqcache load `21268ms`、周期 flush rewrite 1.5-3.2s
- Top-10 非查询峰值 `[6215, 6248, 6520, 6577, 6683, 7385, 7443, 7447, 19079, 21268]` —— 均为启动+早期 flush 事件

---

## 9. 优化建议（P0｜ratio-valve 升为明确 #1）

1. **🔥 P0｜R67/R106 ratio-valve 修复（本轮升为明确 #1）**：本轮干净隔离——flush 正常发生（skip-gate=0）却仍零 compaction/零 Reclaim，**证明 `:159` 比例阀（0.25）是唯一阻断点**。调换 `:142`/`:159` + 阈值下调（建议 8–10%）。当前 **14.96%**，已越参考线，I/O 浪费 15%。
2. **🔥 P0｜无锁快照 flush**：flush rewrite 1.5-3.2s 持锁，全量重写 6.37M 记录（含 953K tombstone）。
3. **🔥 P0｜mlock/madvise 钉住散布热区**（R100+R101+本轮 三轮证据）：本轮非平稳转移到中文 linear-gcd（跨窗口摆动 3.11×，page-cold W1）。钉住 path/name 串池可消除该 page-residency 伪影。
4. **🔥 P0｜读写锁分离 / 无锁快照读**：本轮无写者饿死读者 marker（环境干净），但 ingest 振荡六轮指向同一根因，标准 P0。
5. **🔥 P0｜查询超时保护**：W1 size 2354ms 突发示 ingest 可瞬时拖慢读者，超时熔断 + 降级返回必要。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1｜CJK trigram 索引**（维持 R99 降级）：本轮中文 floor 6.82ns **低于** content 7.32/path 8.58（暖态），慢主要是页冷非算法——再次佐证降级。

---

## 10. 结论

- **定性**：🟡 全系列最干净测量环境 + 分裂 36/36 稳健，但 tombstone 越过 12.97% 参考线而 app 仍稳定。本轮 0 skip-gate / 0 WARN / 0 ERROR / 0 非测量越线，电池 16 项全 <100ms，无 marker。
- **机制隔离重大进展**：skip-gate=0（R101 761）但 compaction 仍=0——flush 正常发生却零回收，**干净证明 `:159` ratio-valve（0.25）是唯一阻断点**，比 R101 更纯的隔离，ratio-valve 修复升为明确 P0 #1。
- **分裂 36/36**：四估计法（字面 min 2.35× / content 锚 2.52× / path 锚 2.96× / dm 中位 1.83×）+ 对抗检验全 HOLDS。间隙较 R101 收窄归因于两层相对暖度变化，非机制变化。
- **R100 发现第三轮验证 + 性质细化**：linear-gcd 非平稳本轮**从 path 转移到中文**（path 反而极平稳 1.00×），证明非平稳是 page-residency 伪影（哪个串池冷哪个摆动），非固定路径——为 mlock P0 提供第三轮证据。
- **诚实修正——参考线重校准**：tombstone **14.96%** 决定性越过 12.97% R43 参考线而 app 稳定运行 9.2h。结合 R108→死亡→重启时间线，死亡点与 12.97% **巧合接近但因果未证**，**更可能是相关性巧合而非硬崩溃阈值**。但 ingest-without-reclaim 单调累积（窗口 95.3% 入墓）仍是唯一真实退化轴，修复不因「未崩溃」降级。
- **诚实修正纪律**：本轮明确标注 2 条越线均为测量自致、区分 dm 真暖（紧致簇）与中文假低（非平稳 floor）、呈现四估计法、对 12.97% 参考线给出「相关非因果」的保守解读而非过度断言崩溃。贯彻「不轻易下结论、每个结论需逻辑与数据支撑」。
