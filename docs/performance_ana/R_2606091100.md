# MacEverything 性能分析报告 R_2606091100

## 1. 本轮范围

- 任务启动时间：2026-06-09 11:00
- 报告文件：`docs/performance_ana/R_2606091100.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5680`
  - `SEG_LINES=4738`
  - `LAST_LOG_TS=2026-06-09 01:47`

## 2. 执行摘要

**连续第 2 轮系列顶尖——0/16 >500ms，Max=325ms，P50=22ms，lockWait=0，极佳比例 10/16。** 全表扫描总和从 R201=798ms 收敛至 738ms（↓8%），trigram 均值从 17ms 微升至 16ms（持平）。稳态运行，无异常。

| 指标 | R201 | R202 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 383ms | **325ms** | **↓15%** |
| P50 | 26ms | **22ms** | **↓15%** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 11/16 | **10/16** | ↓1（`dm:today` 升至 102ms） |
| tombstone | 1,741,672 (25.23%) | **1,748,016 (25.30%)** | ↑（持续增长） |
| tombstone 增速 | ~245/min | **~211/min** | ↓14%（回落中） |
| recordCount | 6,902,530 | **6,909,037** | +6.5K |
| liveRecordCount | 5,160,858 | **5,161,021** | +163 |
| FLUSH 累计 | 1 | **1** | **不变** |
| SKIPFLUSH | 4255 | **4315** | +60 |
| COMPACTION | 2 | **2** | **不变——第 3 轮连续确认缺陷** |
| QUERYADV_GE500 | 41 | **41** | 不变（0 新增） |

### 连续 2 轮系列顶尖

| 指标 | R201 | R202 | 对比 |
|------|-----:|-----:|------|
| Max | 383ms | **325ms** | R202 更优 |
| P50 | 26ms | **22ms** | R202 更优 |
| 极佳比例 | 11/16 | 10/16 | R201 更优 |
| Trigram 均值 | 17ms | **16ms** | 持平 |
| 全表扫描总和 | 798ms | **738ms** | R202 更优 |

- **Max 和 P50 进一步下降——系统完全温态。**
- **极佳比例 11→10——`dm:today`=102ms（R201=37ms，↑176%），SoA 元数据区域轻度波动。**

### Trigram 路径——连续第 2 轮全部极佳

| Query | R201 | R202 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 26ms | **22ms** | ↓15% | 极佳 |
| `hello world` | 38ms | **35ms** | ↓8% | 极佳 |
| `*.swift` | 16ms | **13ms** | ↓19% | 极佳 |
| `main.cpp` | 3ms | **6ms** | ↑100% | 极佳 |
| `abc def...` | 33ms | **24ms** | ↓27% | 极佳 |
| `README` | 7ms | **7ms** | 持平 | 极佳 |
| `test` | 13ms | **17ms** | ↑31% | 极佳 |
| `MacEverything` | 3ms | **5ms** | ↑67% | 极佳 |
| Trigram 均值 | 17ms | **16ms** | **↓6%** | 全部极佳 |

- **8/8 trigram 全部 <50ms——连续第 2 轮。**
- **微小波动均在正常范围——page cache 完全温态。**

### 全表扫描——稳定收敛

| Query | R201 | R202 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 151ms | **195ms** | ↑29% |
| `path:/Users` | 265ms | **218ms** | ↓18% |
| `中文测试` | 383ms | **325ms** | ↓15% |
| **总和** | **798ms** | **738ms** | **↓8%** |

- **全表扫描总和 798→738ms（↓8%）——继续收敛。**
- **`中文测试`=325ms（↓15%）——名称数组区域 page cache 恢复中。**
- **`path:/Users`=218ms（↓18%）——路径数组区域同步恢复。**
- **`content:import`=195ms（↑29%）——SoA 区域轻度波动，但仍正常。**

### `dm:today`=102ms——SoA 元数据轻度波动

- **R201=37ms → R202=102ms（↑176%）。**
- **phase2=67ms，queryTime=102ms——overhead 35ms。**
- **SoA 修改时间字段的 page cache 被上午 I/O 活动部分淘汰。**
- **仍在正常范围——非结构性问题。**

### tombstone 25.30%——第 3 轮连续确认 compaction 缺陷

| 轮次 | tombstone% | COMPACTION | FLUSH | SKIPFLUSH |
|------|----------:|----------:|------:|----------:|
| R199 | 24.93% | 2 | 1 | 4136 |
| R200 | 25.15% | 2 | 1 | 4195 |
| R201 | 25.23% | 2 | 1 | 4255 |
| **R202** | **25.30%** | **2** | **1** | **4315** |

- **tombstone 连续 3 轮超过 25% 阈值（25.15% → 25.23% → 25.30%）。**
- **COMPACTION 始终为 2——compaction 从未触发。**
- **FLUSH 始终为 1——整个 session（近 3 天）无新 flush。**
- **SKIPFLUSH 均匀增长（+60/轮）——flush timer 正常运行但始终跳过。**
- **结论：compaction 触发机制缺陷已确凿——需要代码修复。**

### tombstone 增速趋势——上午回落

| 轮次 | 时间 | 增速(/min) | tombstone% |
|------|------|----------:|----------:|
| R199 | 09:43 | 25 | 24.93% |
| R200 | 10:00 | ~1336 | 25.15% |
| R201 | 10:30 | ~245 | 25.23% |
| **R202** | **11:00** | **~211** | **25.30%** |

- **从 R200 的脉冲 1336/min 回落至 211/min——上午初始活动（Spotlight/IDE）已稳定。**
- **211/min 是正常日间水平——与 R193-R197 傍晚水平一致。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 8.5 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 22.4 | 19.1 | advanced-trigram | 0 | 9842 | 极佳 |
| `main.cpp` | 5 | 5.6 | 3.9 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 13.0 | 12.3 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 10.0 | 9.4 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 37.1 | 36.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 35.4 | 34.3 | advanced-trigram | 0 | 3669 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 23.8 | 23.7 | advanced-trigram | 0 | 14344 | 极佳 |
| `README` | 5 | 6.5 | 5.4 | advanced-trigram | 0 | 17006 | 极佳 |
| `test` | 5 | 16.8 | 13.7 | advanced-trigram | 0 | 116884 | 极佳 |
| `MacEverything` | 5 | 5.3 | 0.9 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 102.3 | 66.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 195.3 | 174.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 218.0 | 207.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 325.1 | 324.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 22.4ms，P90 = 218.0ms，Max = 325.1ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R201 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **10/16** | 0.2-37ms | R201=11/16（↓1） |
| **正常**（50-500ms） | 6/16 | 102-325ms | R201=5/16（↑1） |
| **超标**（>500ms） | **0/16** | — | R201=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,909,037，liveRecordCount=5,161,021（tombstone=**1,748,016**，**25.30%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=332  QUERYADV_GE500=41（不变——0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4315
```

## 6. 问题清单

### P0 确认（第 3 轮）：compaction 触发机制缺陷

- **tombstone 25.30%（超阈值 0.30%），COMPACTION 仍为 2。**
- **连续 3 轮确认——这不是时序问题，是确凿的代码缺陷。**
- **tombstone 以 ~211/min 增速持续增长——内存浪费加剧。**
- **修复方案：在 skipflush 路径中增加 tombstone 阈值检查。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 3 轮确认）

- **缺陷确凿：compaction 检查仅在 flush 执行时运行，skipflush 时跳过。**
- **tombstone 25.30% 且持续增长——系统浪费约 25% 内存用于死记录。**
- **修复方案：skipflush 路径中检查 tombstone 比例，超阈值时强制执行 compaction。**

### P0.5：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. 稳态是否维持。
2. tombstone 增长趋势。
3. COMPACTION 和 FLUSH 计数变化。

## 9. 结论

**连续第 2 轮系列顶尖——0/16 >500ms，Max=325ms，P50=22ms，极佳 10/16。** 全部 trigram 查询 <50ms（连续第 2 轮），全表扫描总和 738ms（↓8%，收敛中）。系统处于完全温态稳定运行。

**⚠️ compaction 触发缺陷连续第 3 轮确认——tombstone 25.30%（超阈值 0.30%），COMPACTION 仍为 2，FLUSH 仍为 1。** 这已从"推断"升级为"确凿缺陷"——需要代码层面修复。tombstone 以 ~211/min 增速持续增长，系统约 25% 内存用于存储死记录。
