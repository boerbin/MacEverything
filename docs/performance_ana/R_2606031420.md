# MacEverything 性能分析报告 R_2606031420

## 1. 本轮范围

- 任务启动时间：2026-06-03 14:20
- 报告文件：`docs/performance_ana/R_2606031420.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21509`
  - `SEG_LINES=483`
  - `LAST_LOG_TS=2026-06-03 20:11`

## 2. 执行摘要

I/O 压力继续缓解，大部分查询恢复正常。P50 ≈ 88ms（前轮 171ms），14/16 查询 <500ms。lockWaitMs 全部为 0。但 `.pdf`=877ms 和 `path:/Users`=816ms 仍显著偏高——`.pdf` 连续第 2 轮 >500ms，`path:/Users` 连续第 4 轮 >500ms（自 R67 I/O 事件以来）。

tombstone 继续攀升至 222,222 条（3.73%），连续第 11 轮超过 2%。QUERYADV_GE500=25。

### 横向对比（最近 5 轮）

| Query | R66 | R67 | R68 | R69 | R70 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 11 | 600 | 545 | 15 | **7** | 恢复正常 |
| `.pdf` | 329 | 10902 | 23901 | 502 | **877** | **连续 2 轮 >500ms** |
| `*.swift` | 150 | 1101 | 1657 | 390 | **266** | 恢复 |
| `hello world` | 359 | 1644 | 863 | 269 | **180** | 恢复 |
| `path:/Users` | 269 | 3753 | 2221 | 1370 | **816** | 持续恢复但仍偏高 |
| P50 | 127 | 1131 | 641 | 171 | **88** | **接近正常** |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 6.7 | 0.0 | short-query-cache | 0 | 0 | 极快 |
| `.pdf` | 5 | 876.6 | 864.0 | advanced-trigram | 0 | 9947 | **连续 2 轮 >500ms** |
| `main.cpp` | 5 | 65.8 | 48.4 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 266.0 | 259.3 | advanced-trigram | 0 | 9356 | 正常 |
| `ext:cpp` | 5 | 100.1 | 98.1 | advanced-ext-index | 0 | 3702 | 正常 |
| `size:>100m` | 5 | 75.9 | 75.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 179.5 | 176.9 | advanced-trigram | 0 | 3613 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 62.5 | 62.0 | advanced-trigram | 0 | 15990 | 快 |
| `README` | 5 | 19.4 | 12.9 | advanced-trigram | 0 | 17717 | 极快 |
| `test` | 5 | 36.9 | 21.9 | advanced-trigram | 0 | 129962 | 快 |
| `MacEverything` | 5 | 38.2 | 0.7 | advanced-trigram | 0 | 6802 | 快 |
| `dm:today` | 5 | 64.2 | 64.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 194.8 | 183.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 815.5 | 801.4 | advanced-linear-gcd | 0 | 0 | **连续 4 轮 >500ms** |
| `中文测试` | 0 | 262.1 | 261.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 32.5 | 0.0 | advanced-trigram | 0 | 0 | 快 |

**P50 ≈ 88ms，P90 ≈ 816ms，Max = 877ms。14/16 查询 <500ms。lockWaitMs 全部为 0。**

### I/O 压力恢复追踪（R67 事件后）

| 指标 | R67 峰值 | R68 | R69 | R70 本轮 |
|------|--------:|----:|----:|--------:|
| P50 | 1131ms | 641ms | 171ms | **88ms** |
| Max | 10,902ms | 23,901ms | 1370ms | **877ms** |
| >500ms 查询 | 13/16 | 10/16 | 2/16 | **2/16** |
| lockWaitMs max | 583ms | 2269ms | 0.5ms | **0ms** |
| `.pdf` | 10,902ms | 23,901ms | 502ms | **877ms** |
| `path:/Users` | 3,753ms | 2,221ms | 1370ms | **816ms** |

P50 和 lockWaitMs 已完全恢复。`.pdf` 和 `path:/Users` 是最后两个未完全恢复的查询。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,950,344，liveRecordCount=5,728,122（tombstone=**222,222**，**3.73%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=179  QUERYADV_GE500=25
FLUSH=0  COMPACTION=2  SKIPFLUSH=174
DIAG_TRIGRAM_SKIP=75
```

- QUERYADV 从 173 增至 179（+6）。
- QUERYADV_GE500 从 23 增至 25（+2，`.pdf` 877ms + `path:/Users` 816ms）。
- SKIPFLUSH 从 162 增至 174（+12）。
- DIAG_TRIGRAM_SKIP 从 74 增至 75（+1）。

## 6. 问题清单

### Critical：tombstone 逼近 4%（222,222 条，3.73%）

- 增长轨迹：
  ```
  R66: 185K (3.13%) → R67: 195K (3.29%) → R68: 199K (3.36%)
  → R69: 213K (3.58%) → R70: 222K (3.73%)
  ```
- 本轮增量 9,316 条。
- **累计内存开销**：~222K × 300 字节 ≈ 67MB。
- SKIPFLUSH=174，compaction 完全不可触发。连续第 11 轮超 2%。
- **按 ~10K/轮，约 3 轮后达 4%。**

### High：`.pdf` 连续 2 轮 >500ms（502ms → 877ms）

- phase2=864ms，candidates=9947。I/O 残留效应持续影响高候选数 trigram 查询。
- `*.swift`（9356 candidates）=266ms 已恢复，说明 `.pdf` 的额外 600ms 来自 I/O 层面的残留热点。
- ext-index 路由可彻底解决。

### Medium：`path:/Users` 连续 4 轮 >500ms

- R67=3753 → R68=2221 → R69=1370 → R70=816ms。持续改善但恢复缓慢。
- linear-gcd 全表扫描对系统缓存状态高度敏感。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（连续 11 轮超标）

- tombstone 3.73%，约 3 轮后达 4%。最小方案不变。

### P0：将 `.pdf`/`*.swift` 路由到 ext-index（`.pdf` 连续 2 轮 >500ms）

- `.pdf` 走 trigram 时在正常负载下 84-490ms，I/O 压力下可达 24s。
- 路由到 ext-index 后预计 50-130ms，彻底消除 500ms 风险。

### P0：实现 phase2 超时降级（承前轮）

### P1：优化 `path:/Users` linear-gcd 恢复速度

- 连续 4 轮 >500ms，恢复最慢。考虑增加 path 前缀索引或缓存已验证路径结果。

## 8. 下一轮重点

1. `.pdf` 是否回到 <500ms——连续 2 轮 >500ms 是否会延续。
2. `path:/Users` 是否降至 <500ms（当前 816ms，趋势改善）。
3. tombstone 是否突破 4%。

## 9. 结论

I/O 压力继续缓解，P50 恢复至 88ms（接近正常），lockWaitMs 全部归零。但 `.pdf`=877ms 连续第 2 轮 >500ms，`path:/Users`=816ms 连续第 4 轮 >500ms——这两个查询是 I/O 压力事件的最后残留。tombstone 增至 222,222 条（3.73%），连续第 11 轮超 2%，约 3 轮后将达 4%。I/O 恢复追踪显示：P50 和 lockWaitMs 已完全恢复，`.pdf` 和 `path:/Users` 是最后需要恢复的查询。
