# MacEverything 性能分析报告 R_2606131517

## 1. 本轮范围

- 任务启动时间：2026-06-13 15:17
- 报告文件：`docs/performance_ana/R_2606131517.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=12287`
  - `SESSION_START_TS=2026-06-13 12:03:04.944`
  - `TOTAL_LINES=12566`
  - `SEG_LINES=280`
  - `LAST_LOG_TS=2026-06-13 15:18`

## 2. 执行摘要

**本轮 16-query battery 中 0/16 >500ms，Max 从 415.1ms 回落到 344.4ms，但 pure-filter 的 `content:import` 升至 296.4ms。** 最高项为 `path:/Users`=344.4ms，其次是 `content:import`=296.4ms、`中文测试`=286.6ms、`dm:today`=105.5ms、`hello world`=80.9ms。P50≈30.2ms，P90≈286.6ms，所有 lockWait 均为 0ms。

相比 14:48，Max 下降约 17%，P50 从约 42.1ms 降至约 30.2ms，但 P90 从约 176.8ms 升至约 286.6ms，说明中位层恢复但尾部更集中在 pure-filter 与 linear-gcd。扫描/过滤路径总和从 1045.6ms 升至 1069.7ms。后台结构性风险继续恶化：recordCount=6,229,711，liveRecordCount=5,311,577，tombstone=918,134（14.74%），较上一轮增加 6,364；最新 session `FLUSH=38`，较上一轮 +6，`SKIPFLUSH=0`，session 最大 flush total 仍为 6164ms。

| 指标 | R_2606131448 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 415.1ms | **344.4ms** | ↓17% |
| P50 | ≈42.1ms | **≈30.2ms** | ↓28% |
| P90 | ≈176.8ms | **≈286.6ms** | ↑62% |
| lockWait max | 0.0ms | **0.0ms** | 持平 |
| 极佳比例(<50ms) | 11/16 | **10/16** | -1 |
| 扫描/过滤路径总和 | 1045.6ms | **1069.7ms** | ↑2% |
| recordCount | 6,222,933 | **6,229,711** | +6,778 |
| liveRecordCount | 5,311,163 | **5,311,577** | +414 |
| tombstone | 911,770 (14.65%) | **918,134 (14.74%)** | +6,364 |
| 最新 session FLUSH | 32 | **38** | +6 |
| 最新 session QUERYADV_GE500 | 0 | **0** | 持平 |
| 最新 flush total | 2631ms | **2733ms** | ↑4% |
| session 最大 flush total | 6164ms | **6164ms** | 持平 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 0.3 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 56.6 | 49.7 | advanced-trigram | 0.0 | 10157 | 正常，略高于 50ms |
| `main.cpp` | 5 | 5.4 | 3.3 | advanced-trigram | 0.0 | 103 | 极佳 |
| `*.swift` | 5 | 23.7 | 22.3 | advanced-trigram | 0.0 | 9622 | 极佳 |
| `ext:cpp` | 5 | 14.1 | 12.7 | advanced-ext-index | 0.0 | 3891 | 极佳 |
| `size:>100m` | 5 | 36.8 | 36.7 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 80.9 | 54.1 | advanced-trigram | 0.0 | 3932 | 正常，trigram 尾部 |
| `abc def ghi jkl mno pqr` | 0 | 47.2 | 47.1 | advanced-trigram | 0.0 | 14618 | 极佳，贴近 50ms |
| `README` | 5 | 20.4 | 18.6 | advanced-trigram | 0.0 | 17586 | 极佳 |
| `test` | 5 | 23.2 | 19.3 | advanced-trigram | 0.0 | 118021 | 极佳 |
| `MacEverything` | 5 | 14.9 | 3.7 | advanced-trigram | 0.0 | 7037 | 极佳 |
| `dm:today` | 5 | 105.5 | 104.6 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 296.4 | 277.6 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常高位，本轮 pure-filter 最高 |
| `path:/Users` | 5 | 344.4 | 312.9 | advanced-linear-gcd | 0.0 | 0 | 正常高位，本轮最高 |
| `中文测试` | 0 | 286.6 | 285.9 | advanced-linear-gcd | 0.0 | 0 | 正常高位 |
| `verylongquery...` | 0 | 0.8 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈30.2ms，P90≈286.6ms，Max=344.4ms。0/16 查询 >500ms。lockWait max=0.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **10/16** | 0.3-47.2ms | short-query-cache、轻量 trigram、ext-index 与部分 pure-filter 仍健康 |
| 正常（50-500ms） | **6/16** | 56.6-344.4ms | pure-filter、linear-gcd 与部分 trigram 构成主要尾部 |
| 超标（>500ms） | **0/16** | — | 本轮无正式超标查询 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,229,711；
- liveRecordCount=5,311,577；
- tombstone=918,134（**14.74%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=12287
SESSION_START_TS=2026-06-13 12:03:04.944
TOTAL_LINES=12566
SEG_LINES=280
QUERYADV=30
QUERYADV_GE500=0
FLUSH=38
SKIPFLUSH=0
COMPACTION=2
LAST_LINE=[2026-06-13 15:18:00.909] [INFO ] [SearchEngine] QueryAdvanced "中文测试" total=286ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6229711
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 0，本轮没有日志级慢查询。`中文测试` 的日志 total=286ms，与 query battery 的 286.6ms 对齐；本轮最高项切换到 `path:/Users`=344.4ms，而 `content:import`=296.4ms 明显抬升，说明尾部在 linear-gcd 与 pure-filter 之间摆动。

```text
[2026-06-13 15:18:00.909] QueryAdvanced "中文测试" total=286ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6229711
```

### flush 情况

最新 session 内 `FLUSH=38`，rewrite 范围 **2579ms - 4809ms**，total 最大 **6164ms**。`SKIPFLUSH=0`，说明 session 内完整 flush 从上一轮的 32 次继续累积到 38 次，但没有任何可见 backoff。

最新 flush：

```text
[2026-06-13 15:13:23.889] Flushed v6 flat index, lastEventId=361781807,
liveRecords=5311547 | timing: walSwap=0ms rewrite=2730ms total=2733ms
```

Top flushes：

```text
[2026-06-13 12:08:25.921] rewrite=4809ms total=4812ms
[2026-06-13 13:48:25.498] rewrite=4395ms total=4396ms
[2026-06-13 12:38:27.299] rewrite=3809ms total=6164ms
[2026-06-13 13:43:24.572] rewrite=3471ms total=3472ms
[2026-06-13 12:13:24.551] rewrite=3435ms total=3437ms
```

## 6. 问题清单

### P0：tombstone 已接近 920k 且继续上行

- 证据：tombstone=918,134（14.74%），较上一轮增加 6,364。
- 影响：无效记录比例继续升高，会放大扫描成本、索引重写成本与 compaction 压力。
- 判断：连续多轮 tombstone 上行，说明 compaction/清理策略仍未追上更新与删除积累速度。

### P0：new session 内 flush 已累积到 38 次且无退避

- 证据：`FLUSH=38`，较上一轮 +6；`SKIPFLUSH=0`；session 最大 flush total=6164ms。
- 影响：完整重写持续制造 I/O 与 page-cache 干扰，容易让 linear-gcd、pure-filter、trigram 在不同采样点出现尾部抬升。
- 判断：flush 频率继续按固定节奏累积，未看到对 3-6s rewrite 的保护机制。

### P0：pure-filter 尾部明显抬升

- 证据：`content:import`=296.4ms，phase2=277.6ms；`dm:today`=105.5ms，phase2=104.6ms。
- 影响：pure-filter 从上一轮的 176.8ms 升至 296.4ms，已接近 linear-gcd 尾部水平，说明 scan/filter 系列风险不只在路径与中文查询。
- 判断：lockWait=0ms，主要瓶颈仍在 phase2 扫描/过滤阶段。

### P0：linear-gcd 仍维持 280-350ms 尾部

- 证据：`path:/Users`=344.4ms，phase2=312.9ms；`中文测试`=286.6ms，phase2=285.9ms。
- 影响：虽然未接近 500ms，但仍处于用户可感知区间，并会随 tombstone 与 page-cache 状态波动。
- 判断：路径和中文查询仍缺少有效候选剪枝。

### P1：trigram 多数健康但仍有中位以上项

- 证据：`.pdf`=56.6ms，`hello world`=80.9ms，`abc def ghi jkl mno pqr`=47.2ms。
- 影响：trigram 本轮明显好于 pure-filter/linear-gcd，但多词或大候选查询仍可能进入 50-100ms 区间。

## 7. 优化建议

### P0：立即提前或分段 compaction

- 以 tombstone 率 14%+ 或绝对 tombstone 900k+ 作为明确触发信号，不再等待更高阈值。
- 结合 segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 优先处理 tombstone 密集 segment，避免 scan/filter 路径继续扫描大量无效记录。
- 验收：tombstone 率从 14.74% 回落，`content:import`、`path:/Users`、`中文测试` 稳定 <200ms。

### P0：flush 改为增量/COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页或持锁窗口。
- 验收：常规 flush total <200ms，新 session 不再出现 3-6s 级完整 rewrite。

### P0：增加 flush I/O 压力退避和前台保护

- 当最近 flush 超过阈值时，提高下一次 flush 间隔、降低 batch 频率，或前台查询活跃时推迟大 flush。
- 持久化线程使用低 QoS，并评估大顺序写的 `F_NOCACHE`/同类策略，减少对搜索热页的冲刷。
- 验收：`SKIPFLUSH` 不再长期为 0，3-6s flush 后不再出现 300ms+ scan/filter 尾部。

### P0：为 advanced-linear-gcd 与 pure-filter 增加轻量剪枝

- 对中文、路径、content/filter 类查询记录扫描记录数、tombstone 跳过数和 segment 分布。
- 评估轻量字符 n-gram、路径分段索引、预过滤位图或 segment-level skip list，以减少无结果或低选择性查询的全量扫描。
- 要求保持内存增长可控，优先使用压缩或按需结构，不引入大常驻索引。
- 验收：`content:import`、`path:/Users` 与 `中文测试` 不再稳定落在 280ms+ 区间。

### P1：细分 pure-filter 与 top-level timing

- 对 pure-filter 记录扫描记录数、跳过 tombstone 数、命中 live 数、segment 数、metadata 加载耗时和结果组装耗时。
- 对所有路径增加 queueWait、phase2、candidate materialization、path/file metadata load、sort/rank、HTTP serialization/write 等阶段耗时。
- 验收：`content:import` 的 296.4ms 能归因到具体阶段，而不是只看到 phase2 大黑盒。

### P1：细分 trigram/postings 路径

- 对 `advanced-trigram` 记录 postings 读取、候选交并、候选评分、路径加载、结果组装、page-cache miss 近似指标。
- 重点解释 `.pdf` 与 `hello world` 在 50-100ms 区间的成本来源。
- 验收：trigram 查询在 flush 后仍稳定 <100ms，或至少能解释每个阶段的成本。

## 8. 下一轮重点

1. tombstone 是否继续从 918,134 上行并突破 920k。
2. `content:import` 是否继续接近 300ms，或回落到 200ms 以下。
3. `path:/Users` 是否继续维持 300ms+。
4. `中文测试` 是否重新接近 400-500ms。
5. scan/filter 路径总和是否继续高于 1000ms。
6. 新 session 中 `QUERYADV_GE500` 是否开始累积。
7. 新 session flush 是否继续出现 4-6s outlier。
8. `SKIPFLUSH` 是否仍为 0。

## 9. 结论

**本轮没有 >500ms 查询，Max=344.4ms，P50≈30.2ms，但 P90≈286.6ms，尾部集中在 `path:/Users`、`content:import` 与 `中文测试`。** 与 14:48 相比，最高项回落，但 pure-filter 的 `content:import` 抬升到 296.4ms，说明 scan/filter 系列整体仍不稳定。

**后台根因继续恶化：tombstone=918,134（14.74%），新 session `FLUSH=38`，最大 flush total=6164ms，且 `SKIPFLUSH=0`。** 下一步仍应优先推进提前/分段 compaction、增量/COW flush、flush 前台保护/退避、advanced-linear-gcd/pure-filter 剪枝，以及 pure-filter、trigram 与 top-level timing 拆分。
