# MacEverything 性能分析报告 R_2606080816

## 1. 本轮范围

- 任务启动时间：2026-06-08 08:16
- 报告文件：`docs/performance_ana/R_2606080816.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=3925`
  - `SEG_LINES=2983`
  - `LAST_LOG_TS=2026-06-08 08:17`

## 2. 执行摘要

**Trigram 完全恢复+全表扫描创新高——0/16 >500ms，Max=402ms，P50=19.5ms，lockWait=0，极佳比例 10/16（63%）。** Trigram 均值从 R173 的 38.5ms 恢复至 18.2ms——确认 R173 为一次性压力事件。但全表扫描总和达 1089ms——本系列最高（此前最高 R158=820ms）。`dm:today`=106ms 也处于本系列高位。日间 08:00 时段 page cache 竞争继续影响 SoA/linear 区域，但 trigram 已通过查询自预热完全恢复。

### 关键指标对比

| 指标 | R173（30 min 前） | R174 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 400ms | **402ms** | 持平 |
| P50 | 31ms | **19.5ms** | **↓ 37%（trigram 恢复）** |
| lockWait max | 1.7ms | **0ms** | 恢复完美 |
| 极佳比例(<50ms) | 8/16 | **10/16** | **↑2（trigram 恢复）** |
| tombstone | 1,362,977 (21.07%) | **1,371,476 (21.19%)** | ↑ 正常 |
| tombstone 增速 | ~296/min | **~283/min** | ↓ 回落 |
| recordCount | 6,462,244 | **6,470,737** | +8.5K |
| liveRecordCount | 5,099,267 | **5,099,261** | -6（正常波动） |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 2730 | **2790** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 31 轮 0 新增） |

### Trigram 恢复确认

| Query | R173（压力事件） | R174 本轮 | 恢复率 |
|-------|-----:|-----:|------|
| `.pdf` | 137ms | **17ms** | **↓ 88%（完全恢复）** |
| `*.swift` | 31ms | **12ms** | ↓ 61% |
| `hello world` | 21ms | **23ms** | 持平 |
| `abc def...` | 43ms | **40ms** | 持平 |
| `README` | 16ms | **10ms** | ↓ 38% |
| `test` | 21ms | **20ms** | 持平 |
| `MacEverything` | 5ms | **6ms** | 持平 |
| Trigram 均值 | **38.5ms** | **18.2ms** | **↓ 53%（恢复）** |

- **Trigram 均值 18.2ms——从 R173 的 38.5ms 大幅回落。**
- **`.pdf`=17ms（R173=137ms，↓88%）——postings 页面通过 R173 查询完全自预热回 page cache。**
- **18.2ms 高于 R172 的 14.4ms——日间活跃期 page cache 竞争仍存在轻微影响。**
- **结论**：R173 的 trigram 压力确认为一次性事件——自预热机制有效。

### 全表扫描——本系列新高

| Query | R173 | R174 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 244ms | **316ms** | **↑ 29%** |
| `path:/Users` | 235ms | **371ms** | **↑ 58%** |
| `中文测试` | 400ms | **402ms** | 持平 |
| **总和** | **879ms** | **1089ms** | **↑ 24%（本系列新高）** |

- **全表扫描总和 1089ms——超越深夜维护峰值（R158=820ms，R163=822ms）！**
- **`path:/Users`=371ms（R173=235ms，↑58%）——涨幅最大。**
- **`content:import`=316ms（R173=244ms，↑29%）——SoA 区域持续受压。**
- **原因**：08:00 是用户登录后的首个完全活跃小时——应用启动潮、Spotlight 完整索引增量、浏览器多 tab 恢复等同时进行。
- **所有三项仍 <500ms——无超标。**

### 日间全表扫描趋势——首次突破 1000ms

| 时段 | 轮次 | 总和 | 环境 |
|------|------|-----:|------|
| 04:47 | R167 | 467ms | 凌晨极静默 |
| 06:16 | R170 | 693ms | 早晨开始 |
| 06:46 | R171 | 767ms | 日间活跃 |
| 07:16 | R172 | 781ms | 日间稳态 |
| 07:46 | R173 | 879ms | page cache 压力 |
| **08:16** | **R174** | **1089ms** | **日间峰值** |

- **持续上升曲线**：从凌晨 467ms 到日间峰值 1089ms——增幅 133%。
- **首次突破 1000ms——但仍无单项 >500ms。**
- **预计**：08:30-09:00 后趋于稳定（启动潮结束）。

### SoA Filter——`dm:today` 达本系列最高

| Query | R173 | R174 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 74ms | **31ms** | **↓ 58%（恢复）** |
| `dm:today` | 40ms | **106ms** | **↑ 165%（本系列最高）** |

- **`dm:today`=106ms——超越此前最高（R171=88ms）！**
- **`size:>100m`=31ms（R173=74ms，↓58%）——SoA 文件大小区域恢复。**
- **时间戳 filter（dm:today）与文件大小 filter（size:>100m）零和翻转。**
- **两项总和**：R173=114ms → R174=137ms——↑ 20%。

### lockWait 恢复

- **本轮全部 16 查询 lockWait=0——从 R173 的 1.7ms 恢复。**
- **R173 的 lockWait=1.7ms 确认为一次性（I/O 高峰下 flush timer 竞争）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 3.3 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 16.6 | 14.9 | advanced-trigram | 0 | 9812 | 极佳 |
| `main.cpp` | 5 | 1.4 | 0.9 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 12.3 | 11.6 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 8.9 | 7.0 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 31.0 | 30.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 23.3 | 21.3 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 40.1 | 36.6 | advanced-trigram | 0 | 14215 | 极佳 |
| `README` | 5 | 9.6 | 8.8 | advanced-trigram | 0 | 16631 | 极佳 |
| `test` | 5 | 19.5 | 17.3 | advanced-trigram | 0 | 116513 | 极佳 |
| `MacEverything` | 5 | 5.7 | 1.6 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 106.3 | 104.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 315.5 | 295.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 371.4 | 363.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 401.7 | 401.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 19.5ms，P90 = 371.4ms，Max = 401.7ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R173 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 10/16 | 0.4-40ms | R173=8/16（**↑2**） |
| **正常**（50-500ms） | 6/16 | 106-402ms | R173=8/16（↓2） |
| **超标**（>500ms） | 0/16 | — | R173=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,470,737，liveRecordCount=5,099,261（tombstone=**1,371,476**，**21.19%**）；
- 进程正常运行（当前会话，运行约 34.1 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=130  QUERYADV_GE500=4（不变——连续 31 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=2790
```

- **连续 31 轮 0 个新增 >500ms 查询（R144-R174）。**
- **FLUSH=1**——无新 flush（34.1 小时仅启动时 1 次）。
- **SKIPFLUSH=2790**——系统持续空闲。

## 6. 问题清单

### 观察项：全表扫描总和首次突破 1000ms（非 bug）

- **现象**：全表扫描总和 1089ms——本系列最高。
- **原因**：08:00 日间用户活跃峰值——应用启动潮、Spotlight、浏览器恢复。
- **影响**：无单项 >500ms，用户无感知（全表扫描非主流查询路径）。
- **处置**：预计 08:30-09:00 后自然回落。无需修复。

### `dm:today`=106ms（本系列最高）

- **原因**：SoA 时间戳区域 page cache 被日间活动竞争。
- **仍远低于 500ms 阈值。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 21.19%，增速 ~283/min。
- 距 25% 阈值约 ~22 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 日间 I/O 高峰下 lockWait 偶现——COW 可消除。

## 8. 下一轮重点

1. 全表扫描总和是否从 1089ms 回落（启动潮消退）。
2. `dm:today` 是否从 106ms 回落。
3. Trigram 均值是否继续稳定在 <20ms。

## 9. 结论

**Trigram 完全恢复+全表扫描日间峰值——0/16 >500ms，Max=402ms，P50=20ms，lockWait=0，极佳比例 10/16。** R173 的 trigram 压力确认为一次性——postings 通过查询自预热完全恢复（均值 38→18ms）。全表扫描总和首次突破 1000ms（1089ms），超越深夜维护峰值——日间 08:00 用户活跃峰值。

**连续 31 轮 0 新增慢查询，34.1 小时无异常。** 核心发现：trigram 路径具备自预热能力——即使 postings 被完全淘汰，下一轮查询即恢复。SoA/linear 路径无此机制（每次都是全量扫描），只能被动等待系统 I/O 竞争消退。tombstone 21.19%（1.37M），增速 283/min，距 25% 约 22 小时。
