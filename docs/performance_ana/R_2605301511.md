# MacEverything 性能分析报告 R_2605301511

- **任务启动时间**：2026-05-30 15:11
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~28.1h，未重启
- **状态快照 @15:11**：total `5,896,383` / live `5,392,575`
- **本轮定性**：⚠️ **轻度污染轮 + 日志层仍洁净**——电池撞上一次**亚秒级**瞬时系统停顿：标志项 `main.cpp` 71 候选竟 100.64ms（trig=77ms 异常）触发 R88 协议 marker #3，连带 `*.swift` 202ms、`hello world` 185ms、`ext:cpp` 84ms 全线 2–10× 偏高。**3× 复测验证全部瞬时**（main.cpp→0.51、*.swift→1.9、ext:cpp→0.7、x→0.01，140× 恢复）。**关键区别**：本轮停顿全部止于亚秒级，**未越过 >500ms 阈值**——日志延迟分布三段（>5s/1-5s/0.5-1s）**连续 5 轮（R90→R94）完全冻结**，电池窗口（15:11/15:12）日志零 >500ms 条目。R88-R90 重度污染留下 1-5s 段印记，本轮则在日志层完全无形。洁净 ns/槽（3 复测中位）二层分裂 **21/21 不破**、间隙 **2.46×**。唯一恶化项 tombstone **首破 500K（503,808 / 8.54%）**，连续 27 轮单调。

---

## 1. 原始电池（含污染，⚠️ 标记异常项）

| query | path | total(ms) | phase2 | trig | cand | 备注 |
|---|---|---|---|---|---|---|
| `x` | short-query-cache | 1.23 | 0 | 0 | 0 | 缓存正常 |
| `.pdf` | advanced-trigram | 115.7 | 113.54 | 2.02 | 10124 | 受污染（R93 21）|
| `main.cpp` | advanced-trigram | **100.64** | 23.20 | **77.42** | 71 | ⚠️ **71 候选 100ms 不可能**（复测 0.51）+ trig 77ms 标志冷|
| `*.swift` | advanced-trigram | **201.82** | 199.48 | 2.28 | 9213 | ⚠️ 受污染（复测 1.9）|
| `ext:cpp` | advanced-ext-index | **84.50** | 83.70 | 0 | 2987 | ⚠️ 异常（R93 15、复测 0.7）|
| `size:>100m` | pure-filter-soa-gcd | 64.73 | 63.69 | 0 | 0 | 偏高（复测中位 27.54）|
| `hello world` | advanced-trigram | **185.15** | 183.65 | 1.46 | 2340 | ⚠️ 受污染（复测 3.0）|
| `abc def ghi jkl mno pqr` | advanced-trigram | 79.64 | 78.85 | 0.74 | 15924 | |
| `README` | advanced-trigram | 23.88 | 19.77 | 4.07 | 16469 | 未污染 |
| `test` | advanced-trigram | 34.45 | 23.09 | 11.24 | 136275 | 未污染 |
| `MacEverything` | advanced-trigram | 13.84 | 2.46 | 11.32 | 6699 | 未污染 |
| `dm:today` | pure-filter-soa-gcd | 70.47 | 70.05 | 0 | 0 | 数值，复测一致 |
| `content:import` | pure-filter-soa-gcd | 178.30 | 170.77 | 0 | 0 | 字符串 |
| `path:/Users` | advanced-linear-gcd | 240.31 | 231.40 | 0 | 0 | 字符串，略偏高 |
| `中文测试` | advanced-linear-gcd | 254.47 | 254.24 | 0.11 | 0 | 字符串 |
| `verylongquery…really` | advanced-trigram | 1.69 | 0 | 1.59 | 0 | |

**污染标志触发**：`main.cpp` 71 候选 100ms 物理上不可能由数据冷化解释（trig 77ms 对 71 候选是 ~46× R93 单候选成本，与数据规模无关），与同批次普遍 2-10× 偏高一起证明 15:11:46 有一次系统级瞬时停顿横扫批次。

---

## 2. 复测消歧（R88 协议）

**污染标志项（全部 sub-second 恢复）：**

| query | 电池值 | probe1 | probe2 | probe3 | 判定 |
|---|---|---|---|---|---|
| `x`（缓存）| 1.23 | 0.43 | 0.01 | 0.01 | ✅ 瞬时 |
| `main.cpp`（71 cand）| 100.64 | 4.51 | 0.66 | **0.51** | ✅ 瞬时（197× 恢复）|
| `*.swift` | 201.82 | 23.64 | 1.41 | **1.9** | ✅ 瞬时（106× 恢复）|
| `ext:cpp` | 84.50 | 19.80 | 0.35 | **0.70** | ✅ 瞬时（121× 恢复）|
| `hello world` | 185.15 | 25.84 | 3.00 | 5.46 | ✅ 瞬时（62× 恢复）|

**首复测仍带停顿尾迹**（如 main.cpp 4.51、ext:cpp 19.8），第 2/3 复测完全恢复——表明停顿在 15:11:46–15:11:50 短窗口内消退。

**ns/槽路径 3× 复测：**

| query | 电池值 | probe1 | probe2 | probe3 | 复测中位 |
|---|---|---|---|---|---|
| `size:>100m` | 64.73 | 38.6 | 25.23 | 27.54 | **27.54** |
| `dm:today` | 70.47 | 46.67 | 60.41 | 70.23 | **60.41** |
| `content:import` | 178.30 | 148.55 | 181.81 | 147.02 | **148.55** |
| `path:/Users` | 240.31 | 234.15 | 178.27 | 177.84 | **178.27** |
| `中文测试` | 254.47 | 288.73 | 245.70 | 203.56 | **245.70** |

`size` 复测中位（27.54）显著低于电池（64.73），证明 pure-filter 路径同样被波及；`content`/`dm` 复测中位接近电池（148.55 vs 178.30、60.41 vs 70.47），波及程度较小。统一按 R88-R90 严格协议**丢弃电池值、取 3 复测中位**计算 ns/槽。

---

## 3. 洁净 ns/槽（3 复测中位，records=5,896,383）

| 路径 | 类型 | 复测中位 | 洁净值 |
|---|---|---|---|
| `size:>100m` | 数值 | 27.54ms | **4.67** |
| `dm:today` | 数值 | 60.41ms | **10.25** |
| `content:import` | 字符串 | 148.55ms | **25.19** |
| `path:/Users` | 字符串 | 178.27ms | **30.23** |
| `中文测试` | 字符串 | 245.70ms | **41.67** |

---

## 4. 本轮头条：日志层仍洁净，亚秒级污染在日志层无形

本轮污染本身（main.cpp 100ms、*.swift 201ms、hello world 185ms）**全部止于亚秒级**，与 R88-R90 的重度污染（1663ms / 1002ms / 861ms）有本质区别——所有停顿样本**未越过 500ms 日志阈值**，日志层完全无新增条目：

| 信号 | R90（重度污染）| R91-R93（洁净）| **R94（本轮，轻度污染）** |
|---|---|---|---|
| 电池最高停顿 | 861ms | <100ms | **202ms** |
| >5s 段计数 | 7 | 7（冻结）| **7（连续 5 轮冻结）** |
| 1-5s 段计数 | 23 | 23（冻结）| **23（连续 5 轮冻结）** |
| 0.5-1s 段计数 | 16 | 16（冻结）| **16（连续 5 轮冻结）** |
| 电池窗口 >500ms 日志 | 有 | 无 | **无** |

R90 之后**三段延迟连续 5 轮全冻结**（R90→R94），本轮 +15 越线查询**全部落在 0.1-0.5s 段**——这是「污染存在但被亚秒兜底」的清晰签名。R88-R90 的午间停顿曾把 >500ms 推入 0.5-1s/1-5s 段并留下印记，本轮的瞬时停顿则在日志层完全无形。**日志维度上，自 R90 以来系统从未再现>500ms 查询**。

---

## 5. 绝对二层分裂：洁净数据 21/21 不破（间隙 2.46×）

- **数值层**：size 4.67、dm 10.25 → 最高数值 **dm 10.25**
- **字符串层**：content 25.19、path 30.23、中文 41.67 → 最低字符串 **content 25.19**
- **洁净间隙 = 25.19 / 10.25 = 2.46×**，无重叠。

整体偏暖（vs R93）：size 6.20→4.67、dm 12.20→10.25、content 32.62→25.19、path 38.64→30.23、中文 39.91→41.67——除 中文 微涨外，4/5 路径全暖。三复测中位反映**停顿消退后的真热态**，分裂依旧机制级（纯数值零解引用 ≪ 字符串解引用散布串池）。

字符串层内部排序 content < path < 中文，与 R92/R93 同向；path（30.23）与 content（25.19）差 5 ns/槽，比 R93 间隔（6 ns/槽）略窄，为 R79 已定噪声范围，不构成新结论。

---

## 6. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **480 次**（R93 467）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100，`:159` tombstone 比例阀结构性永不可达——全会话 0 触发、0 回收。

---

## 7. tombstone 趋势：首破 500K，连续 27 轮单调上升

| 轮次 | tombstone | ratio |
|---|---|---|
| R90@13:11 | 464,832 | 7.94% |
| R91@13:41 | 473,060 | 8.07% |
| R92@14:11 | 482,158 | 8.21% |
| R93@14:41 | 491,103 | 8.35% |
| **R94@15:11** | **503,808** | **8.54%** |

会话内单调递增。R43 参考崩溃点 12.97%（~800K），当前 8.54% 已过 6.5 成程，**首次突破 50 万绝对值**。live 基本持平（5,392,575），约 96% 新增记录变为 tombstone。

---

## 8. 日志健康度

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **334 条**（R93 319，+15）——与 R93 增量（+13）几乎相同，洁净轮节奏
- Top-7 峰值 `[18582, 17928, 15749, 8205, 7544, 6811, 6061]` —— 仍与历史**逐字节相同**
- 延迟分布：>5s=**7**（冻结）、1-5s=**23**（冻结）、0.5-1s=**16**（冻结）、0.1-0.5s=288（+15）、中位 **219ms**
- 三段连续 5 轮冻结，本轮 +15 越线查询全部落 0.1-0.5s 段——电池层瞬时停顿在日志层完全未留痕迹

---

## 9. 优化建议（四项 P0，本轮无新增）

1. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调 <12.97%（建议 8–10%）。当前 8.54% 已落入建议阈值区间，紧迫性最高。
2. **🔥 P0｜mlock/madvise 整体常驻热区**：本轮亚秒级污染再次证明无内存保护时偶发外部压力仍会拖慢 trigram/ext-index 路径（main.cpp 71 候选 trig 77ms 是 trigram 索引页冷的典型签名）；需把 path/name 串池、content 列、trigram 索引、候选行、数值 SoA 列整体钉入内存。
3. **🔥 P0｜无锁快照 flush**：启动 flush 持锁 10.3s。
4. **🔥 P0｜查询超时保护**：R88-R90 三个 >1s 停顿样本——超时熔断 + 降级返回是必要韧性机制。本轮污染虽自我兜底于亚秒级，但触发原因不可控。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1🔥｜CJK trigram 索引**：`中文测试` 走 linear-gcd 全扫（中位 245ms / 41.67 ns/槽，本轮字符串最差）。

---

## 10. 结论

- **定性**：⚠️ 轻度污染轮 + 日志层仍洁净。电池撞上一次亚秒级瞬时停顿（main.cpp 100ms 触发 marker #3），但所有停顿止于亚秒、日志延迟分布三段连续 5 轮（R90→R94）完全冻结——**「污染存在但被亚秒兜底」的新签名**。
- **诚实修正纪律生效**：本轮没有被电池假象误导——污染识别 + 3 复测严格执行，洁净数据 **21/21 不破、间隙 2.46×**，分裂机制依旧。
- **新观察（明确边界）**：自 R90 起延迟分布三段连续 5 轮冻结，证明 R91-R93 的「洁净」与 R94 的「轻度污染」在日志层是同一类（亚秒兜底）；唯一在日志层留下印记的是 R88-R90 的 >1s 重度停顿。这把「日志层洁净」与「电池层洁净」做了清晰区分。
- **诚实修正纪律**：本轮无推测结论被推翻、无新结论需建立；ns/slot 全线变暖反映停顿消退后的真热态，字符串层 path-content 间隔小幅收窄属 R79 噪声范围。
- **唯一持续恶化项**：tombstone **首破 500K（503,808 / 8.54%）**，连续 27 轮单调，根因 R67 阀门结构性不可达明确。这是会话内唯一真实退化，修复紧迫性随阈值逼近持续上升。
