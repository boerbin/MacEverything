# MacEverything 性能分析报告 R_2606131247

## 1. 本轮范围

- 任务启动时间：2026-06-13 12:47
- 报告文件：`docs/performance_ana/R_2606131247.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=12287`
  - `SESSION_START_TS=2026-06-13 12:03:04.944`
  - `TOTAL_LINES=12360`
  - `SEG_LINES=74`
  - `LAST_LOG_TS=2026-06-13 12:47`

## 2. 执行摘要

**本轮 16-query battery 中 0/16 >500ms，Max=337.4ms，最高项从上一轮 `中文测试` 转移到 `path:/Users`。** P50 从约 47.6ms 降至约 25.0ms，说明中位层恢复；但 P90 从约 210.4ms 升至约 279.4ms，说明尾部仍未稳定。所有 lockWait 均为 0ms，主要耗时仍集中在 phase2 扫描、过滤或 postings 处理。

后台结构性风险继续恶化：recordCount=6,193,947，liveRecordCount=5,309,309，tombstone=884,638（14.28%），较上一轮增加 7,173。最新 session `FLUSH=8`，`SKIPFLUSH=0`，flush total 最大升至 6164ms；这说明 12:03 重启后的新 session 已经重新积累完整重写压力，且仍没有可见退避。

| 指标 | R_2606131217 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 366.3ms | **337.4ms** | ↓8% |
| P50 | ≈47.6ms | **≈25.0ms** | ↓47% |
| P90 | ≈210.4ms | **≈279.4ms** | ↑33% |
| lockWait max | 0.0ms | **0.0ms** | 持平 |
| 极佳比例(<50ms) | 9/16 | **11/16** | +2 |
| 扫描/过滤路径总和 | 904.2ms | **952.8ms** | ↑5% |
| recordCount | 6,186,329 | **6,193,947** | +7,618 |
| liveRecordCount | 5,308,864 | **5,309,309** | +445 |
| tombstone | 877,465 (14.18%) | **884,638 (14.28%)** | +7,173 |
| 最新 session FLUSH | 2 | **8** | +6 |
| 最新 session QUERYADV_GE500 | 0 | **0** | 持平 |
| 最新 flush total | 3437ms | **2998ms** | ↓13% |
| session 最大 flush total | 4812ms | **6164ms** | ↑28% |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 1.7 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 16.7 | 12.6 | advanced-trigram | 0.0 | 10155 | 极佳 |
| `main.cpp` | 5 | 5.9 | 4.2 | advanced-trigram | 0.0 | 103 | 极佳 |
| `*.swift` | 5 | 28.0 | 27.2 | advanced-trigram | 0.0 | 9622 | 极佳 |
| `ext:cpp` | 5 | 12.6 | 11.8 | advanced-ext-index | 0.0 | 3891 | 极佳 |
| `size:>100m` | 5 | 31.7 | 31.6 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 117.0 | 115.4 | advanced-trigram | 0.0 | 3932 | 正常，仍是 trigram 尾部 |
| `abc def ghi jkl mno pqr` | 0 | 44.9 | 44.5 | advanced-trigram | 0.0 | 14615 | 极佳 |
| `README` | 5 | 18.9 | 15.7 | advanced-trigram | 0.0 | 17581 | 极佳 |
| `test` | 5 | 22.0 | 18.2 | advanced-trigram | 0.0 | 118019 | 极佳 |
| `MacEverything` | 5 | 9.1 | 2.5 | advanced-trigram | 0.0 | 7036 | 极佳 |
| `dm:today` | 5 | 66.5 | 66.0 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 237.8 | 228.2 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常高位，pure-filter 尾部 |
| `path:/Users` | 5 | 337.4 | 330.6 | advanced-linear-gcd | 0.0 | 0 | 正常高位，本轮最高 |
| `中文测试` | 0 | 279.4 | 276.5 | advanced-linear-gcd | 0.0 | 0 | 正常高位 |
| `verylongquery...` | 0 | 1.2 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈25.0ms，P90≈279.4ms，Max=337.4ms。0/16 查询 >500ms。lockWait max=0.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **11/16** | 1.2-44.9ms | short-query-cache、轻量 trigram、ext-index 与 `size:>100m` 均健康 |
| 正常（50-500ms） | **5/16** | 66.5-337.4ms | `hello world`、pure-filter、linear-gcd 构成尾部 |
| 超标（>500ms） | **0/16** | — | 本轮无正式超标查询 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,193,947；
- liveRecordCount=5,309,309；
- tombstone=884,638（**14.28%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=12287
SESSION_START_TS=2026-06-13 12:03:04.944
TOTAL_LINES=12360
SEG_LINES=74
QUERYADV=9
QUERYADV_GE500=0
FLUSH=8
SKIPFLUSH=0
COMPACTION=2
LAST_LINE=[2026-06-13 12:47:55.471] [INFO ] [SearchEngine] QueryAdvanced "中文测试" total=277ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6193942
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 0，本轮没有日志级慢查询。需要继续注意：12:03 之前旧 session 的慢查询不属于本次分析范围，当前 `QUERYADV_GE500=0` 只能说明新 session 暂未累积 >500ms 日志级慢查询。

本轮日志末尾可见 `中文测试`：

```text
[2026-06-13 12:47:55.471] QueryAdvanced "中文测试" total=277ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6193942
```

这与 query battery 的 `中文测试`=279.4ms 对齐；但本轮 query battery 的最高项是 `path:/Users`=337.4ms，说明 linear-gcd 尾部在中文与路径查询之间摆动。

### flush 情况

最新 session 内 `FLUSH=8`，rewrite 范围 **2622ms - 4809ms**，total 最大 **6164ms**。`SKIPFLUSH=0`，说明即使新 session 内已有 6.164s flush，仍未出现可见 flush backoff。

最新 flush：

```text
[2026-06-13 12:43:24.099] Flushed v6 flat index, lastEventId=360579645,
liveRecords=5309281 | timing: walSwap=0ms rewrite=2969ms total=2998ms
```

Top flushes：

```text
[2026-06-13 12:08:25.921] rewrite=4809ms total=4812ms
[2026-06-13 12:38:27.299] rewrite=3809ms total=6164ms
[2026-06-13 12:13:24.551] rewrite=3435ms total=3437ms
[2026-06-13 12:23:24.104] rewrite=3015ms total=3018ms
[2026-06-13 12:33:24.094] rewrite=2986ms total=2989ms
```

## 6. 问题清单

### P0：tombstone 在 14% 以上继续上行

- 证据：tombstone=884,638（14.28%），较上一轮增加 7,173。
- 影响：无效记录比例继续升高，会放大扫描成本、索引重写成本与 compaction 压力。
- 判断：上一轮刚突破 14%，本轮继续上行，说明 compaction/清理策略仍未追上更新与删除积累速度。

### P0：linear-gcd 尾部重新转向 path 查询

- 证据：`path:/Users`=337.4ms，phase2=330.6ms；`中文测试`=279.4ms，phase2=276.5ms。
- 影响：路径与中文查询都没有有效候选剪枝，仍会在 tombstone 和 page-cache 状态变化下成为用户可感知尾部。
- 判断：lockWait=0ms，主要瓶颈仍在 phase2 扫描阶段。

### P0：新 session 内 flush 数量快速累积，且没有退避

- 证据：新 session `FLUSH=8`，较 12:17 的 2 次增加到 8 次；`SKIPFLUSH=0`；最大 flush total=6164ms。
- 影响：完整重写继续制造 I/O 与 page-cache 干扰，可能解释 trigram 与 scan/filter 查询的周期性波动。

### P0：pure-filter 尾部仍稳定存在

- 证据：`content:import`=237.8ms、`dm:today`=66.5ms，均主要落在 phase2。
- 影响：即使 linear-gcd 是本轮最高，pure-filter 仍持续贡献 200ms+ 尾部。

### P1：trigram 路径仍有中位以上波动

- 证据：`hello world`=117.0ms，上一轮为 210.4ms；`abc def ghi jkl mno pqr`=44.9ms，候选数 14615。
- 影响：trigram 当前未超标，但受 postings 读取、候选处理或 page-cache 状态影响时会抬升。

## 7. 优化建议

### P0：立即提前或分段 compaction

- 以 tombstone 率 14%+ 作为明确触发信号，不再等待更高阈值。
- 结合绝对 tombstone 数、segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 优先处理 tombstone 密集 segment，避免 scan/filter 路径继续扫描大量无效记录。
- 验收：tombstone 率从 14.28% 回落，`path:/Users`、`中文测试`、`content:import` 稳定 <200ms。

### P0：flush 改为增量/COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页或持锁窗口。
- 验收：常规 flush total <200ms，新 session 不再出现 3-6s 级完整 rewrite。

### P0：增加 flush I/O 压力退避和前台保护

- 当最近 flush 超过阈值时，提高下一次 flush 间隔、降低 batch 频率，或前台查询活跃时推迟大 flush。
- 持久化线程使用低 QoS，并评估大顺序写的 `F_NOCACHE`/同类策略，减少对搜索热页的冲刷。
- 验收：`SKIPFLUSH` 不再长期为 0，6s+ flush 后不再出现 200-300ms 扫描尾部。

### P0：为 advanced-linear-gcd 与 pure-filter 增加轻量剪枝

- 对中文、路径、content/filter 类查询记录扫描记录数、tombstone 跳过数和 segment 分布。
- 评估轻量字符 n-gram、路径分段索引、预过滤位图或 segment-level skip list，以减少无结果或低选择性查询的全量扫描。
- 要求保持内存增长可控，优先使用压缩或按需结构，不引入大常驻索引。
- 验收：`path:/Users` 不再成为稳定 Max，`中文测试` 和 `content:import` 稳定低于 200ms。

### P1：细分 trigram/postings 路径

- 对 `advanced-trigram` 记录 postings 读取、候选交并、候选评分、路径加载、结果组装、page-cache miss 近似指标。
- 重点解释 `hello world` 在 117-210ms 区间波动的主因。
- 验收：trigram 查询在 flush 后仍稳定 <100ms，或至少能解释每个阶段的成本。

### P1：拆分 top-level query timing

- 对所有路径增加 queueWait、phase2、candidate materialization、path/file metadata load、sort/rank、result assembly、HTTP serialization/write 等阶段耗时。
- 验收：`queryTimeMs - phase2Ms - lockWaitMs` 不再是不可解释的大黑盒。

## 8. 下一轮重点

1. tombstone 是否继续从 14.28% 上行。
2. `path:/Users` 是否继续超过 `中文测试` 成为最高项。
3. `content:import` 是否继续维持 200ms+。
4. `hello world` 是否再次回到 200ms+。
5. 新 session 中 `QUERYADV_GE500` 是否开始累积。
6. 新 session flush 是否继续出现 6s+ outlier。
7. `SKIPFLUSH` 是否仍为 0。

## 9. 结论

**本轮没有 >500ms 查询，Max=337.4ms，P50≈25.0ms，P90≈279.4ms。** 中位层较上一轮恢复，但尾部仍由 `path:/Users`、`中文测试`、`content:import` 和 `hello world` 构成，主要耗时仍集中在 phase2。

**后台根因继续恶化：tombstone=884,638（14.28%），新 session `FLUSH=8`，最大 flush total=6164ms，且 `SKIPFLUSH=0`。** 下一步仍应优先推进提前/分段 compaction、增量/COW flush、flush 前台保护/退避、advanced-linear-gcd/pure-filter 剪枝，以及 trigram 与 top-level timing 拆分。
