# MacEverything 性能分析报告 R_2606081546

## 1. 本轮范围

- 任务启动时间：2026-06-08 15:46
- 报告文件：`docs/performance_ana/R_2606081546.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=4874`
  - `SEG_LINES=3932`
  - `LAST_LOG_TS=2026-06-08 16:17`

## 2. 执行摘要

**`path:/Users`=688ms 突破 500ms（1/16 >500ms），trigram 轻度改善（149→133ms），lockWait=0，极佳比例 3/16（19%）。** 与 R187 对比：Max 从 349ms 升至 688ms，但 P50 从 141ms 降至 107ms——变化方向不一致。`path:/Users` 的 linear-gcd 全扫描区域受到集中的 page cache 压力（682ms phase2），而 trigram 路径反而略好。tombstone 增速从 569/min 回落至 394/min——午后高增速消退。QUERYADV_GE500 +1。

### 关键指标对比

| 指标 | R187（午后压力） | R188 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **1/16** | ↑1 |
| Max | 349ms | **688ms** | ↑ 97% |
| P50 | 141ms | **107ms** | **↓ 24%** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 2/16 | **3/16** | ↑1 |
| tombstone | 1,566,899 (23.47%) | **1,583,193 (23.63%)** | ↑ |
| tombstone 增速 | ~569/min | **~394/min** | **↓ 31%（回落！）** |
| recordCount | 6,681,619 | **6,699,849** | +18.2K（偏高） |
| liveRecordCount | 5,114,720 | **5,116,656** | +1,936 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3568 | **3628** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 19 | **20** | **↑1（path:/Users=688ms）** |

### `path:/Users`=688ms——linear-gcd 首次单独 >500ms

- **phase2=682ms，lockWait=0，candidates=0（全扫描）。**
- **此前 `path:/Users` 最高**：R184=248ms，R187=349ms——本轮 688ms 为该查询系列最高。
- **`path:/Users` 执行 linear-gcd 全扫描——扫描整个路径字符串数组。**
- **688ms 说明路径数组的 page cache 区域被大量淘汰——可能是系统有大量路径相关 I/O（文件管理器浏览、Finder 预览等）竞争了相同的 page cache 区域。**

### 与 `中文测试`=294ms 对比

- **两者都走 linear-gcd 路径，但扫描不同数据区域。**
- **`path:/Users`=688ms vs `中文测试`=294ms（2.3x 差距）。**
- **说明路径数组区域的 page cache 淘汰比其他 SoA 区域更严重——选择性压力。**

### Trigram 路径——轻度改善

| Query | R187 | R188 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 227ms | **190ms** | ↓ 16% | 冷态 |
| `hello world` | 298ms | **252ms** | ↓ 15% | 冷态 |
| `*.swift` | 136ms | **126ms** | ↓ 7% | 冷态 |
| `main.cpp` | 146ms | **27ms** | **↓ 82%（极佳）** | 恢复 |
| `abc def...` | 194ms | **259ms** | ↑ 33% | 冷态 |
| `README` | 61ms | **76ms** | ↑ 25% | 正常 |
| `test` | 148ms | **107ms** | ↓ 28% | 正常 |
| `MacEverything` | 23ms | **28ms** | ↑ 22% | 极佳 |
| Trigram 均值 | 149ms | **133ms** | **↓ 11%** | 轻度改善 |

- **Trigram 均值 133ms（R187=149ms，↓11%）——轻度改善但仍远高于稳态（27ms）。**
- **`main.cpp`=27ms（R187=146ms，↓82%）——小 candidates（82）查询恢复极快。**
- **`abc def...`=259ms（R187=194ms，↑33%）——部分 postings 区域反向恶化。**
- **不均匀恢复——部分区域预热（main.cpp），部分区域被新 I/O 再次淘汰（abc def）。**

### 全表扫描——`path:/Users` 异常突出

| Query | R187 | R188 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 236ms | **194ms** | ↓ 18% |
| `path:/Users` | 349ms | **688ms** | **↑ 97%（>500ms！）** |
| `中文测试` | 219ms | **294ms** | ↑ 34% |
| **总和** | **804ms** | **1176ms** | **↑ 46%** |

- **全表扫描总和 1176ms——被 `path:/Users`=688ms 拉高。**
- **`content:import`=194ms（↓18%）——SoA 区域反而改善。**
- **`path:/Users` 独自贡献了总和增量的绝大部分。**

### SoA Filter——稳定

| Query | R187 | R188 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 86ms | **74ms** | ↓ 14% |
| `dm:today` | 65ms | **77ms** | ↑ 18% |

- **两项均在正常范围。**

### tombstone 增速回落——394/min

- **增速 394/min（R187=569/min，↓31%）——午后高增速消退！**
- **但仍高于正常（~300/min）——文件操作减少但未完全恢复。**
- **tombstone 23.63%（1.58M），距 25% 约 11-13 小时。**
- **recordCount +18.2K——仍偏高但趋势向下。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 13.6 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 189.6 | 179.7 | advanced-trigram | 0 | 9842 | 冷态 |
| `main.cpp` | 5 | 26.5 | 17.2 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 126.3 | 117.5 | advanced-trigram | 0 | 9345 | 冷态 |
| `ext:cpp` | 5 | 59.6 | 58.5 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 73.8 | 72.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 252.1 | 247.9 | advanced-trigram | 0 | 3638 | 冷态 |
| `abc def ghi jkl mno pqr` | 0 | 258.6 | 257.5 | advanced-trigram | 0 | 14261 | 冷态 |
| `README` | 5 | 75.9 | 51.7 | advanced-trigram | 0 | 16638 | 正常 |
| `test` | 5 | 106.8 | 99.4 | advanced-trigram | 0 | 116539 | 正常 |
| `MacEverything` | 5 | 28.4 | 1.6 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 76.7 | 75.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 194.0 | 168.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 687.6 | 681.9 | advanced-linear-gcd | 0 | 0 | **>500ms** |
| `中文测试` | 0 | 294.4 | 293.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 4.8 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 106.8ms，P90 = 258.6ms，Max = 687.6ms。1/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R187 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 3/16 | 4.8-28ms | R187=2/16（↑1） |
| **正常**（50-500ms） | 12/16 | 60-294ms | R187=14/16（↓2） |
| **超标**（>500ms） | **1/16** | 688ms | R187=0/16（↑1） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,699,849，liveRecordCount=5,116,656（tombstone=**1,583,193**，**23.63%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=227  QUERYADV_GE500=20（+1——path:/Users=688ms）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3628
```

- **QUERYADV_GE500 +1（path:/Users=688ms）。**
- **SKIPFLUSH +60——正常节奏。**

## 6. 问题清单

### 观察项：`path:/Users` linear-gcd 路径首次单独 >500ms

- **此前 `path:/Users` 的 >500ms 仅在冷启动（R176=600ms）和 I/O 风暴（R179=1145ms）时出现。**
- **本轮 688ms 在日间正常运行中出现——说明路径数组的 page cache 区域在午后竞争中特别脆弱。**
- **影响有限——single query，不影响其他路径。**

### tombstone 增速回落中

- **394/min（R187=569/min，↓31%）——午后高峰消退。**
- **tombstone 23.63%（1.58M），距 25% 约 11-13 小时。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（紧迫！）

- tombstone 23.63%（1.58M），距 25% 约 11-13 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. `path:/Users` 是否从 688ms 恢复至 <500ms。
2. trigram 均值是否从 133ms 继续收敛。
3. tombstone 增速是否继续回落至正常。

## 9. 结论

**`path:/Users`=688ms 突破 500ms（1/16 >500ms），trigram 轻度改善（149→133ms），lockWait=0，极佳 3/16。** linear-gcd 路径数组区域受到集中的 page cache 压力——此前仅在冷启动/I/O 风暴时 >500ms，本轮首次在日间出现。P50=107ms 反而低于 R187（141ms）——混合信号表明不同区域的 page cache 冷热分布在午后持续波动。

**tombstone 增速从 569/min 回落至 394/min——午后高增速消退中。** tombstone 23.63%（1.58M），距 25% 约 11-13 小时。P0 compaction 修复紧迫。
