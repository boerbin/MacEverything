# MacEverything 性能分析报告 R_2606081716

## 1. 本轮范围

- 任务启动时间：2026-06-08 17:16
- 报告文件：`docs/performance_ana/R_2606081716.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5082`
  - `SEG_LINES=4140`
  - `LAST_LOG_TS=2026-06-08 17:47`

## 2. 执行摘要

**完全恢复——0/16 >500ms，Max=324ms，P50=114ms，lockWait=0，极佳比例 4/16。** `path:/Users` 从 R190 的 2043ms 恢复至 184ms（↓91%），`.pdf` 从 630ms 恢复至 324ms（↓49%）——**确认 1 轮恢复模式**，这是系列第四次灾难性事件后的 1 轮完全恢复（R176→R177 睡眠/唤醒，R179→R180 I/O 风暴，R181→R182 选择性淘汰，R190→R191 日间重度淘汰）。tombstone 24.19%——距 25% 仅 0.81 个百分点，增速 404/min，**预计 ~2.2 小时触达阈值**。

### 关键指标对比

| 指标 | R190 | R191 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 2/16 | **0/16** | **↓2（完全恢复）** |
| Max | 2043ms | **324ms** | **↓ 84%** |
| P50 | 172ms | **114ms** | **↓ 34%** |
| lockWait max | 3.6ms | **0ms** | **完美** |
| 极佳比例(<50ms) | 2/16 | **4/16** | **↑2** |
| tombstone | 1,624,967 (24.07%) | **1,637,077 (24.19%)** | **↑** |
| tombstone 增速 | ~549/min | **~404/min** | **↓ 26%（傍晚回落）** |
| recordCount | 6,752,508 | **6,766,352** | +13.8K |
| liveRecordCount | 5,127,541 | **5,129,275** | +1,734 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3748 | **3808** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 22 | **22** | **不变（本轮 0 新增）** |

### 1 轮恢复——第四次确认

| 事件 | 灾难轮 | 恢复轮 | 灾难 Max | 恢复 Max | 恢复率 |
|------|--------|--------|------:|------:|------|
| 睡眠/唤醒 | R176 | R177 | 2217ms | 55ms | 97% |
| I/O 风暴 | R179 | R180 | 1290ms | 324ms | 75% |
| 选择性淘汰 | R181 | R182 | 7193ms | 180ms | 97% |
| **日间重度淘汰** | **R190** | **R191** | **2043ms** | **324ms** | **84%** |

- **四次灾难性事件均 1 轮恢复——系统自恢复能力极强。**
- **恢复率 75-97%，本轮 84%——Max 从 2043ms 降至 324ms。**
- **battery 预热效果稳定——16 个查询足以将关键区域重新拉入 page cache。**

### `path:/Users` 完全恢复

- **2043ms → 184ms（↓91%）——从冷启动级别回到正常。**
- **phase2=178ms——路径数组区域已重新进入 page cache。**
- **系列排名**：R176=2217ms → R179=1145ms → R188=688ms → R190=2043ms → **R191=184ms（正常）**。

### `.pdf` 恢复至冷态正常

- **630ms → 324ms（↓49%）——从 >500ms 恢复至正常冷态水平。**
- **仍高于稳态（~17ms），但在日间午后典型范围。**

### Trigram 路径——恢复中

| Query | R190 | R191 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 630ms | **324ms** | **↓49%** | 冷态（恢复） |
| `hello world` | 358ms | **309ms** | ↓14% | 冷态 |
| `*.swift` | 298ms | **225ms** | ↓24% | 冷态 |
| `main.cpp` | 38ms | **58ms** | ↑53% | 正常 |
| `abc def...` | 181ms | **114ms** | ↓37% | 正常 |
| `README` | 86ms | **61ms** | ↓29% | 正常 |
| `test` | 98ms | **81ms** | ↓17% | 正常 |
| `MacEverything` | 46ms | **20ms** | **↓57%** | 极佳 |
| Trigram 均值 | 217ms | **149ms** | **↓31%** | 恢复中 |

- **Trigram 均值 217→149ms（↓31%）——整体恢复。**
- **`.pdf`=324ms 和 `hello world`=309ms 仍是主要慢项——高 candidates 查询恢复需更多轮。**
- **`main.cpp`=58ms（R190=38ms，↑53%）——小幅波动，candidates 仅 82 故影响有限。**
- **`MacEverything`=20ms（R190=46ms，↓57%）——恢复至极佳。**

### 全表扫描——完全恢复

| Query | R190 | R191 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 262ms | **184ms** | ↓30% |
| `path:/Users` | 2043ms | **184ms** | **↓91%** |
| `中文测试` | 249ms | **168ms** | ↓33% |
| **总和** | **2554ms** | **536ms** | **↓79%** |

- **全表扫描总和 2554→536ms（↓79%）——从灾难恢复至日间正常。**
- **三项均在 170-184ms 范围——非常均匀，说明 SoA/linear 区域 page cache 均匀恢复。**

### SoA Filter——正常偏高

| Query | R190 | R191 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 173ms | **196ms** | ↑13% |
| `dm:today` | 79ms | **42ms** | **↓47%（极佳）** |

- **`dm:today`=42ms 进入极佳范围。**
- **`size:>100m`=196ms 轻度偏高——SoA 全扫描区域受轻度压力。**

### ⚠️ tombstone 24.19%——距 25% 仅 0.81 个百分点！

- **tombstone=1,637,077（24.19%）。**
- **增速 404/min（R190=549/min，↓26%）——傍晚回落中。**
- **距 25% 需要额外 54,511 个 tombstone。**
- **按 404/min 计算：~135 分钟 ≈ 2.2 小时。**
- **这是系列最接近 25% 阈值的时刻——compaction 触发可能在今晚。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 8.6 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 323.9 | 314.8 | advanced-trigram | 0 | 9842 | 冷态 |
| `main.cpp` | 5 | 58.1 | 48.4 | advanced-trigram | 0 | 82 | 正常 |
| `*.swift` | 5 | 224.9 | 220.7 | advanced-trigram | 0 | 9345 | 冷态 |
| `ext:cpp` | 5 | 114.1 | 113.2 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 196.0 | 193.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 309.4 | 304.7 | advanced-trigram | 0 | 3638 | 冷态 |
| `abc def ghi jkl mno pqr` | 0 | 113.8 | 113.3 | advanced-trigram | 0 | 14299 | 正常 |
| `README` | 5 | 61.4 | 50.4 | advanced-trigram | 0 | 16691 | 正常 |
| `test` | 5 | 81.1 | 70.5 | advanced-trigram | 0 | 116588 | 正常 |
| `MacEverything` | 5 | 19.6 | 2.2 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 41.8 | 41.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 183.6 | 175.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 183.8 | 178.3 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 167.8 | 166.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.9 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 114.0ms，P90 = 309.4ms，Max = 323.9ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R190 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 4/16 | 0.9-42ms | R190=2/16（**↑2**） |
| **正常**（50-500ms） | 12/16 | 58-324ms | R190=12/16（持平） |
| **超标**（>500ms） | **0/16** | — | R190=2/16（**↓2**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,766,352，liveRecordCount=5,129,275（tombstone=**1,637,077**，**24.19%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=253  QUERYADV_GE500=22（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3808
```

- **QUERYADV_GE500 不变（本轮 0 新增 >500ms 查询）。**
- **SKIPFLUSH +60——正常节奏。**

## 6. 问题清单

### P0 极度紧急：tombstone 24.19%——距 25% 仅 ~2.2 小时！

- **距 25% 仅 0.81 个百分点（54,511 tombstone）。**
- **增速 404/min——预计 ~2.2 小时（约 19:30-20:00）触达。**
- **compaction 触发后若 rewrite 崩溃，索引可能不可用。**
- **这是系列最紧急时刻——今晚可能触发 compaction。**

### 观察项：1 轮恢复模式第四次确认

- **R190=2043ms → R191=324ms（Max，↓84%）。**
- **系统自恢复能力极强——所有灾难性事件均 1 轮恢复。**
- **无需架构级干预。**

## 7. 优化建议

### P0：立即排查 compaction rewrite 崩溃根因

- **tombstone 24.19%，距 25% 仅 ~2.2 小时。**
- **今晚极可能触发 compaction——必须确认 rewrite 安全。**
- **紧迫度：小时级，今晚。**

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. tombstone 是否突破 24.5%，距 25% 的时间窗口。
2. trigram 均值是否从 149ms 继续收敛。
3. 是否出现 compaction 相关日志。

## 9. 结论

**完全恢复——0/16 >500ms，Max=324ms，P50=114ms，lockWait=0，极佳 4/16。** `path:/Users` 从 2043ms 恢复至 184ms（↓91%），`.pdf` 从 630ms 恢复至 324ms（↓49%）——第四次确认 1 轮恢复模式。系统自恢复能力极强。

**⚠️ tombstone 24.19%——距 25% 仅 0.81 个百分点，预计 ~2.2 小时触达。** 增速 404/min（傍晚回落中），但仍足以在今晚触发 compaction。P0 compaction rewrite 崩溃修复已进入最后几小时倒计时。如果不修复，compaction 触发时可能导致索引不可用。
