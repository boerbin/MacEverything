# MacEverything 性能分析报告 R_2606101700

## 1. 本轮范围

- 任务启动时间：2026-06-10 17:00
- 报告文件：`docs/performance_ana/R_2606101700.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9091`
  - `SEG_LINES=8149`
  - `LAST_LOG_TS=2026-06-10 15:17`

## 2. 执行摘要

**稳定运行——trigram 极致，`x` 缓存第 4 轮未重建（确认为缺陷）。** 0/16 >500ms，Max=266ms，P50=11.0ms，lockWait=0（连续 3 轮）。**极佳 12/16。** Trigram phase2 均值 ~2.6ms（持续极致）。`x` 仍走 advanced-linear-gcd（246ms——**第 4 轮未重建——确认 short-query-cache 重建缺陷**）。全表扫描总和 648ms（↑20% from R259=542ms——轻微零和波动）。FLUSH 15→21（+6，**连续 3 轮 +6**）。tombstone=26,599（0.50%——健康）。

| 指标 | R259 | R260 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 260ms | **266ms** | ↑2% |
| P50 | 12ms | **11.0ms** | ↓8% |
| lockWait max | 0ms | **0ms** | 完美（连续 3 轮） |
| 极佳比例(<50ms) | 12/16 | **12/16** | 持平 |
| tombstone | 19,217 (0.37%) | **26,599 (0.50%)** | ↑（正常增长） |
| recordCount | 5,260,815 | **5,268,501** | +7.7K（正常） |
| liveRecordCount | 5,241,598 | **5,241,902** | +304（正常） |
| FLUSH 累计 | 15 | **21** | +6（连续 3 轮！） |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 4 | **4** | 不变 |
| QUERYADV_GE500 | 75 | **75** | 不变 |

### ⚠️ 确认：short-query-cache 重建缺陷

| 轮次 | `x` 路径 | `x` 时间 | FLUSH 增量 |
|------|---------|--------:|----------:|
| R256 | short-query-cache | 1ms | — |
| R257 | advanced-linear-gcd | 587ms | +2（compaction） |
| R258 | advanced-linear-gcd | 297ms | +6 |
| R259 | advanced-linear-gcd | 260ms | +6 |
| **R260** | **advanced-linear-gcd** | **246ms** | **+6** |

- **连续 4 轮 `x` 未恢复 short-query-cache——确认为缺陷，非暂态。**
- **246ms 时间趋势：587→297→260→246（逐轮下降——page cache 预热改善全表扫描速度，但路径未切回 cache）。**
- **根因分析：**
  1. **高频 flush（每轮 +6）可能每次都失效 short-query-cache。**
  2. **cache 重建可能依赖"连续 N 次相同查询"条件——battery 每轮只查 `x` 一次。**
  3. **或者 cache 重建依赖"无 flush 期间"——但当前每 30s 都在 flush。**
- **升级为 P1 缺陷：flush 后应保留或立即重建高频查询的 cache。**

### FLUSH 连续 3 轮 +6

- **R257→R260：每轮 +6，SKIPFLUSH 始终为 7266（+0）。**
- **这意味着 compaction 后每个 30s tick 都有 ≥100 WAL entries → 触发 flush。**
- **tombstone 增速：(26,599-19,217)/30min ≈ 246/min（与 pre-compaction 正常水平一致）。**
- **recordCount +7.7K（正常——文件系统活动回归正常）。**

### Trigram 路径——极致稳定

| Query | R259 | R260 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 3ms | **3ms** | 持平 | 极佳 |
| `hello world` | 6ms | **8ms** | ↑33% | 极佳 |
| `*.swift` | 2ms | **6ms** | ↑200%（微小波动） | 极佳 |
| `main.cpp` | 5ms | **5ms** | 持平 | 极佳 |
| `abc def...` | 4ms | **5ms** | ↑25% | 极佳 |
| `README` | 12ms | **6ms** | ↓50% | 极佳 |
| `test` | 18ms | **18ms** | 持平 | 极佳 |
| `MacEverything` | 10ms | **18ms** | ↑80%（overhead） | 极佳 |
| Trigram phase2均值 | ~3.3ms | **~2.6ms** | ↓21% |

- **全部 8/8 trigram 极佳——连续 4 轮。**
- **phase2 均值 2.6ms——compaction 后索引紧凑效应完全稳定。**

### 全表扫描——轻微波动

| Query | R259 | R260 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 152ms | **152ms** | 持平 |
| `path:/Users` | 174ms | **230ms** | ↑32% |
| `中文测试` | 216ms | **266ms** | ↑23% |
| **总和** | **542ms** | **648ms** | **↑20%** |

- **全表扫描 542→648ms（↑20%）——轻微零和波动。**
- **`path:/Users`=230ms 和 `中文测试`=266ms 回升——page cache 被其他活动部分淘汰。**
- **正常波动范围——compaction 后新稳态可能在 400-650ms 区间。**

### 辅助路径

| Query | R259 | R260 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 260ms | **246ms** | ↓5%（仍 linear-gcd） | 正常 |
| `ext:cpp` | 1ms | **1ms** | 持平 | 极佳 |
| `size:>100m` | 17ms | **11ms** | ↓35% | 极佳 |
| `dm:today` | 59ms | **55ms** | ↓7% | 正常 |
| `verylongquery...` | 0.7ms | **0.2ms** | ↓71% | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 245.5 | 214.1 | advanced-linear-gcd | 0 | 0 | 正常（缓存未重建） |
| `.pdf` | 5 | 3.1 | 2.3 | advanced-trigram | 0 | 9783 | 极佳 |
| `main.cpp` | 5 | 5.3 | 0.1 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 5.7 | 0.8 | advanced-trigram | 0 | 9401 | 极佳 |
| `ext:cpp` | 5 | 1.0 | 0.5 | advanced-ext-index | 0 | 3612 | 极佳 |
| `size:>100m` | 5 | 11.0 | 10.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 7.8 | 3.2 | advanced-trigram | 0 | 3929 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 4.9 | 4.9 | advanced-trigram | 0 | 14445 | 极佳 |
| `README` | 5 | 6.4 | 0.9 | advanced-trigram | 0 | 17428 | 极佳 |
| `test` | 5 | 17.7 | 7.6 | advanced-trigram | 0 | 117720 | 极佳 |
| `MacEverything` | 5 | 17.5 | 0.6 | advanced-trigram | 0 | 6922 | 极佳 |
| `dm:today` | 5 | 55.3 | 55.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 152.2 | 138.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 230.0 | 221.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 266.3 | 262.3 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 11.0ms，P90 = 230.0ms，Max = 266.3ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R259 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **12/16** | 0.2-18ms | R259=12/16（持平） |
| **正常**（50-500ms） | 4/16 | 55-266ms | R259=4/16（持平） |
| **超标**（>500ms） | **0/16** | — | R259=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,268,501，liveRecordCount=5,241,902（tombstone=**26,599**，**0.50%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=671  QUERYADV_GE500=75（不变）
FLUSH=21（+6！连续 3 轮）  COMPACTION=4  SKIPFLUSH=7266
```

## 6. 问题清单

### P0：flush 写锁阻塞

- **连续 3 轮 FLUSH +6——高频 flush 增加写锁争用概率。**

### P0：compaction 触发机制缺陷（根因未修复）

### P1（确认）：short-query-cache 重建缺陷

- **连续 4 轮 `x` 走 linear-gcd——确认为缺陷。**
- **高频 flush 可能导致 cache 反复失效。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（增加独立定时检查）

### P1：修复 short-query-cache 重建机制

- **flush 后不应失效 cache，或应立即重建。**
- **建议：flush 后仅增量更新 cache，而非全量失效。**

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. `x` 能否在第 5 轮恢复（若仍未恢复则为持久性缺陷）。
2. 全表扫描稳态区间确认（400-650ms？）。
3. tombstone 增速趋势（0.50%，~246/min——正常增长）。

## 9. 结论

**稳定运行——trigram 极致，短查询缓存缺陷确认。** 0/16 >500ms，Max=266ms，P50=11ms，lockWait=0（连续 3 轮）。极佳 12/16。Trigram 全部 8/8 极佳（连续 4 轮），phase2 均值 2.6ms。**`x` 第 4 轮仍走 linear-gcd（246ms）——确认 short-query-cache 重建缺陷（升级 P1）。** 全表扫描 648ms（↑20%——正常零和波动）。

**FLUSH +6（连续 3 轮——15→21）——compaction 后 WAL 持续活跃。** tombstone=26,599（0.50%——健康，增速 ~246/min 正常）。recordCount +7.7K（正常）。
