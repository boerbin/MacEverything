# MacEverything 性能分析报告 R_2605281110

**时间**: 2026-05-28 11:10  
**索引规模**: 无法获取（服务离线）  
**Tombstone**: 无法获取（R43 最后已知值: ~814,772 / 12.97%）  
**日志**: 与 R43 相同（6815 行, INFO=6814, WARN=0, ERROR=0）——无新日志  
**轮次**: 第 44 轮  

---

## ⚠️⚠️⚠️ 服务持续离线

**MacEverything 服务自 R43（10:33:20）崩溃后未恢复。无进程、无端口监听、无新日志。本轮无法执行查询电池。**

---

## 1. 服务状态确认

| 检查项 | 结果 |
|--------|------|
| `pgrep -x MacEverything` | NO_PROCESS |
| `curl localhost:19860/api/status` | 连接拒绝 (exit 7) |
| 最后日志时间 | 2026-05-28 10:33:20 |
| 最后日志内容 | `QueryAdvanced "very long query..." total=56922ms` |
| 新 "Log session started" | 无——未重启 |

**服务已离线 ~37 分钟**（从 10:33:20 到 11:10）。

---

## 2. 日志分析（仍为 R43 会话）

日志无变化，最后会话的关键数据：

| 指标 | 值 |
|------|---|
| 总行数 | 6815 |
| INFO | 6814 |
| WARN | 0 |
| ERROR | 0 |
| Flush 总数 | 2680 |
| Skip 总数 | 567 |
| 最后 Flush | 10:32:17（6,282,507 records, 5,467,831 live）|
| 最后查询 | 10:33:20（very long query, 56,922ms）|

### 崩溃后无信号

- 无 WARN/ERROR 日志
- 无 crash handler 输出
- 无 graceful shutdown 日志（如 "Server stopping" 等）

这表明进程被**外部终止**（macOS watchdog、OOM killer）或发生了**未捕获的 fatal error**（如 SIGSEGV/SIGBUS）。

---

## 3. 崩溃原因分析（更新）

基于 R43 事件重建和 37 分钟无恢复的事实：

| 假设 | 可能性 | 证据 |
|------|--------|------|
| macOS 看门狗终止 | **高** | 56s 无响应足以触发 watchdog，无 crash log |
| OOM (内存耗尽) | 中 | 56s 查询可能累积大量中间结果 |
| 线程死锁后强杀 | 中 | 极端锁竞争 + GCD 队列堆积 |
| SIGSEGV/SIGBUS | 低 | 无 crash reporter 输出（但可能未配置） |
| 用户手动关闭 | 低 | 无 graceful shutdown 日志，且用户未重启 |

**最可能场景**: 56,922ms 的查询在多个 flush 周期中反复被锁阻塞，导致 HTTP 服务端无法响应（包括 macOS 的 health check），系统 watchdog 或 launchd 将其判定为无响应并强杀。

---

## 4. 停机影响评估

| 维度 | 影响 |
|------|------|
| 文件搜索功能 | **完全不可用** |
| 索引更新 | **停止**——文件系统变更未被追踪 |
| Tombstone 增长 | 暂停（但重启后会有补偿性爆发） |
| 数据完整性 | **可能受损**——最后一次 flush 在 10:32:17，之后的索引变更未持久化 |

### 重启后预期

1. 从最后 flush 点恢复（6,282,507 records）
2. 37+ 分钟的文件系统变更将作为积压批量处理
3. 大量 tombstone 产生（补偿性爆发）
4. Flush 频率短期升高
5. **锁竞争风险极高**——积压处理期间 flush 频繁且数据量大

---

## 5. 历史可用性

| 轮次 | 时间 | 服务状态 | 主要事件 |
|------|------|----------|----------|
| R39 | 08:40 | 正常 | 零锁等待，trigram 偏高 |
| R40 | 09:10 | 正常 | 活跃度跃升，零锁 |
| R41 | 09:40 | 正常 | 高峰 III，content 139ms |
| R42 | 10:10 | **受损** | 锁竞争回归，2 超时 |
| R43 | 10:40 | **崩溃** | lockMs=6877, 56s 查询, 进程终止 |
| **R44** | **11:10** | **离线** | **无进程，37+ 分钟停机** |

**连续 3 轮性能退化/服务故障** (R42→R43→R44)。

---

## 6. Tombstone 趋势（冻结）

| 轮次 | Tombstone | 比例 | Δ |
|------|-----------|------|---|
| R40 | 787,319 | 12.61% | +12,924 |
| R41 | 801,380 | 12.79% | +14,061 |
| R42 | 809,464 | 12.90% | +8,084 |
| R43 | ~814,772 | ~12.97% | ~+5,308 |
| **R44** | **~814,772** | **~12.97%** | **+0 (离线)** |

Tombstone 增长暂停（服务不运行=无新变更入队），但**重启后将出现补偿性爆发**——37+ 分钟的文件系统事件积压将一次性处理。

---

## 7. 优化建议（紧迫性再次升级）

| 优先级 | 项目 | 预期效果 | 紧迫性 |
|--------|------|----------|--------|
| **P0 #0** | **服务重启** | **恢复基本功能** | **⚠️⚠️⚠️ 立即（已停机 37 分钟）** |
| **P0 #1** | **无锁快照刷写** | **消除崩溃根因** | **⚠️⚠️ 生产阻塞（已导致崩溃）** |
| **P0 #2** | **增量刷写** | 将 flush 从 628 万全量降至 delta | **极其紧急** |
| **P0 #3** | Tombstone compaction | 减少 flush 数据量 30%+ | 紧急 |
| P0 #4 | 查询超时保护 | 防止单查询耗尽系统资源 | 紧急（新增） |
| P1 | Crash handler / watchdog log | 捕获崩溃原因 | 高（诊断用） |

### 新增建议：查询超时保护

R43 的 56,922ms 查询说明缺乏查询级超时保护。建议：

```
实现方案:
1. 查询硬超时: 单次查询最大执行时间 10s（可配置）
2. 锁等待超时: 等锁最长 2s，超时返回 "service busy" 而非无限等待
3. 资源限制: 单查询最大候选数 / 最大内存使用
```

这不是根本修复（根本修复是无锁快照刷写），但能防止单个查询拖垮整个服务。

---

## 8. 总结

| 维度 | 评分 | 趋势 |
|------|------|------|
| 查询 (trigram) | **N/A** | — (服务离线) |
| 查询 (线性/过滤) | **N/A** | — (服务离线) |
| 延迟稳定性 | **N/A** | — (服务离线) |
| 索引健康度 | **0/10** | ↓ (冻结于 12.97%，重启后将爆发) |
| 系统稳定性 | **0/10** | ↓↓↓ (**服务离线 37+ 分钟，无自动恢复**) |

**第 44 轮结论**: **⚠️⚠️⚠️ 服务持续离线**。MacEverything 自 R43 崩溃后未恢复，已停机 37+ 分钟。无进程、无端口监听、无新日志、无 crash handler 输出。最后日志为 10:33:20 的 56,922ms 查询。本轮无法执行查询电池。连续 3 轮故障退化（R42 锁竞争→R43 崩溃→R44 持续离线）。根本原因确认：全量刷写架构在 6.28M 记录规模下导致灾难性锁竞争 → 查询阻塞 → 服务无响应 → 被系统强杀。**需要：(1) 立即重启服务恢复功能；(2) 实施无锁快照刷写防止再次崩溃；(3) 增加查询超时保护防止单查询拖垮服务。**
