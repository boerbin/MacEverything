# MacEverything 性能分析报告 R_2606101200

## 1. 本轮范围

- 任务启动时间：2026-06-10 12:00
- 报告文件：`docs/performance_ana/R_2606101200.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=8659`
  - `SEG_LINES=7717`
  - `LAST_LOG_TS=2026-06-10 10:16`

## 2. 执行摘要

**部分恢复——系统级 I/O 风暴消退，但 page cache 仍未完全预热。** 1/16 >500ms（`.pdf`=511ms——临界），Max=511ms，P50=110ms，lockWait=0（恢复）。Trigram 均值 195ms（↓68% from R249=612ms——大幅恢复），全表扫描总和 585ms（↓58% from R249=1379ms——接近 R247=521ms 水平）。**系统级阻塞消退（`x`=13ms 恢复正常，`verylongquery`=1ms 恢复正常），但 postings 区域 page cache 仍冷——需要 1-2 轮进一步恢复。** QUERYADV_GE500=65（+1——`.pdf`=511ms 刚过线）。

| 指标 | R249 | R250 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 4/16 | **1/16** | **↓3** |
| Max | 2511ms | **511ms** | **↓80%** |
| P50 | 248ms | **110ms** | **↓56%** |
| lockWait max | 11.2ms | **0ms** | **恢复为 0** |
| 极佳比例(<50ms) | 1/16 | **3/16** | ↑2 |
| tombstone | 2,193,983 (29.63%) | **2,202,324 (29.71%)** | ↑——接近 29.8% |
| tombstone 增速 | ~238/min | **~278/min** | **↑17%——加速！** |
| recordCount | 7,403,842 | **7,412,757** | +8.9K |
| liveRecordCount | 5,209,859 | **5,210,433** | +574 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 6931 | **6959** | +28（异常低——见分析） |
| COMPACTION | 2 | **2** | **不变——第 51 轮确认缺陷** |
| QUERYADV_GE500 | 64 | **65** | +1 |

### 系统级 I/O 风暴消退证据

| 指标 | R249 | R250 | 结论 |
|------|-----:|-----:|------|
| `x` (cache) | 153ms | **13ms** | **恢复——系统阻塞消退** |
| `verylongquery` | 29ms | **1ms** | **恢复——overhead 消退** |
| lockWait max | 11.2ms | **0ms** | **恢复** |
| SKIPFLUSH delta | +54 | **+28** | **异常低（见下文）** |

### SKIPFLUSH +28 异常

- **正常每轮 +60（每 30 分钟 60 次 tick，每次 skipflush）。**
- **R249=+54（缺 6 次），R250=+28（缺 32 次）。**
- **缺 32 次 tick 意味着约 16 分钟进程处于调度延迟状态。**
- **但日志时间跨度 08:18→10:16（约 2 小时），覆盖了 R249 的 I/O 风暴尾部。**
- **R249 I/O 风暴导致的调度延迟跨越了两轮日志区间。**

### tombstone 增速突然加速

- **tombstone 增速 ~278/min（↑17%——从 ~238/min 加速）。**
- **liveRecordCount +574（通常 +150-300）——活跃记录增长加速。**
- **recordCount +8.9K（通常 +7K）——总记录增长也加速。**
- **可能原因：工作日活动高峰（文件创建/删除/移动频率增加）。**

### Trigram 路径——部分恢复

| Query | R249 | R250 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 2511ms | **511ms** | ↓80% | >500ms（临界） |
| `hello world` | 1216ms | **375ms** | ↓69% | 正常 |
| `*.swift` | 414ms | **459ms** | ↑11% | 正常（仍偏高） |
| `main.cpp` | 45ms | **70ms** | ↑56% | 正常 |
| `abc def...` | 318ms | **83ms** | ↓74% | 正常 |
| `README` | 137ms | **44ms** | ↓68% | 极佳 |
| `test` | 186ms | **95ms** | ↓49% | 正常 |
| `MacEverything` | 67ms | **22ms** | ↓67% | 极佳 |
| Trigram 均值 | 612ms | **195ms** | **↓68%（部分恢复）** |

- **Trigram 均值 612→195ms（↓68%）——大幅恢复但仍远高于正常水平（30-50ms）。**
- **`.pdf`=511ms（↓80% from 2511ms）——仍 >500ms，postings 区域最冷。**
- **`*.swift`=459ms（↑11%——仍偏高）——large postings 集恢复最慢。**
- **2/8 trigram 极佳（`README`=44ms，`MacEverything`=22ms）——小集恢复。**

### 全表扫描——大幅恢复

| Query | R249 | R250 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 310ms | **169ms** | **↓45%** |
| `path:/Users` | 571ms | **203ms** | **↓64%** |
| `中文测试` | 498ms | **213ms** | **↓57%** |
| **总和** | **1379ms** | **585ms** | **↓58%** |

- **全表扫描总和 1379→585ms（↓58%）——接近 R247=521ms 水平。**
- **三路均在 169-213ms——均匀分布，恢复完成。**
- **全表扫描比 trigram 恢复更快——SoA 连续数组被连续扫描自然预热。**

### 辅助路径——恢复

| Query | R249 | R250 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 153ms | **13ms** | ↓92%（恢复） | 极佳 |
| `ext:cpp` | 148ms | **110ms** | ↓26% | 正常 |
| `size:>100m` | 645ms | **368ms** | ↓43% | 正常（仍偏高） |
| `dm:today` | 121ms | **61ms** | ↓50% | 正常 |
| `verylongquery...` | 29ms | **1ms** | ↓97%（恢复） | 极佳 |

- **`size:>100m`=368ms（↓43%——仍偏高，SoA size 区域 page cache 仍冷）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 12.8 | 0.0 | short-query-cache | 0 | 0 | 极佳（恢复） |
| `.pdf` | 5 | **511.2** | 498.8 | advanced-trigram | 0 | 9860 | **⚠️ >500ms（临界）** |
| `main.cpp` | 5 | 70.2 | 42.9 | advanced-trigram | 0 | 82 | 正常 |
| `*.swift` | 5 | 459.1 | 452.0 | advanced-trigram | 0 | 9353 | 正常（偏高） |
| `ext:cpp` | 5 | 110.3 | 108.8 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 367.8 | 366.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（偏高） |
| `hello world` | 5 | 375.2 | 367.2 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 83.4 | 82.7 | advanced-trigram | 0 | 14478 | 正常 |
| `README` | 5 | 44.0 | 34.0 | advanced-trigram | 0 | 17151 | 极佳 |
| `test` | 5 | 94.7 | 83.6 | advanced-trigram | 0 | 117136 | 正常 |
| `MacEverything` | 5 | 21.5 | 1.2 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 61.2 | 60.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 168.8 | 158.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 203.2 | 195.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 212.7 | 197.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 1.1 | 0.0 | advanced-trigram | 0 | 0 | 极佳（恢复） |

**P50 = 110.3ms，P90 = 459.1ms，Max = 511.2ms。1/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R249 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **3/16** | 1-44ms | R249=1/16（↑2） |
| **正常**（50-500ms） | 12/16 | 61-459ms | R249=11/16（↑1） |
| **超标**（>500ms） | **1/16** | 511ms | R249=4/16（**↓3**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,412,757，liveRecordCount=5,210,433（tombstone=**2,202,324**，**29.71%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=620  QUERYADV_GE500=65（+1——`.pdf`=511ms）
FLUSH=1  COMPACTION=2  SKIPFLUSH=6959
```

## 6. 问题清单

### P0 确认（第 51 轮）：compaction 触发机制缺陷

- **tombstone=2,202,324（29.71%）——超阈值 4.71%，接近 30%！**
- **51 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **lockWait 恢复为 0。R249 的 lockWait 是系统级事件，非 flush 写锁。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（第 51 轮——tombstone 接近 30%！）

### P1：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. trigram 能否完全恢复（从 195ms→<50ms——预计需要 1-2 轮）。
2. tombstone 是否突破 30%（当前 29.71%，增速 ~278/min——约 2 小时内可能突破）。

## 9. 结论

**部分恢复——系统级 I/O 风暴消退，page cache 仍在预热中。** 1/16 >500ms（`.pdf`=511ms 临界），Max=511ms（↓80%），P50=110ms（↓56%），lockWait=0（恢复）。系统级阻塞完全消退（`x`=13ms，`verylongquery`=1ms 均恢复正常）。Trigram 均值 195ms（↓68%——大幅恢复但仍需 1-2 轮），全表扫描总和 585ms（↓58%——接近正常）。

**tombstone 29.71%（2,202,324，第 51 轮确认 compaction 缺陷）——接近 30%！** 增速 ~278/min（↑17%——工作日活动加速）。COMPACTION=2 始终不变。
