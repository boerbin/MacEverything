# MacEverything 性能分析报告 R_2606091200

## 1. 本轮范围

- 任务启动时间：2026-06-09 12:00
- 报告文件：`docs/performance_ana/R_2606091200.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5807`
  - `SEG_LINES=4865`
  - `LAST_LOG_TS=2026-06-09 02:47`

## 2. 执行摘要

**连续第 4 轮系列顶尖——0/16 >500ms，Max=375ms，P50=22ms，lockWait=0，极佳比例 11/16。** 稳态持续，全部 trigram <50ms（连续第 4 轮）。全表扫描总和从 R203=974ms 收敛至 833ms（↓14%）。`hello world`=17ms 创该查询系列最优。

| 指标 | R203 | R204 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平（连续 4 轮） |
| Max | 381ms | **375ms** | ↓2% |
| P50 | 37ms | **22ms** | **↓41%** |
| lockWait max | 0ms | **0ms** | 完美（连续 6 轮） |
| 极佳比例(<50ms) | 12/16 | **11/16** | ↓1（`size:>100m`=56ms 微出） |
| tombstone | 1,754,182 (25.37%) | **1,760,065 (25.43%)** | ↑（持续增长） |
| tombstone 增速 | ~205/min | **~196/min** | ↓4%（午间平稳） |
| recordCount | 6,915,259 | **6,921,201** | +5.9K |
| liveRecordCount | 5,161,077 | **5,161,136** | +59 |
| FLUSH 累计 | 1 | **1** | **不变** |
| SKIPFLUSH | 4374 | **4434** | +60 |
| COMPACTION | 2 | **2** | **不变——第 5 轮连续确认缺陷** |
| QUERYADV_GE500 | 41 | **41** | 不变（0 新增，连续 4 轮） |

### Trigram 路径——连续第 4 轮全部极佳

| Query | R203 | R204 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 27ms | **27ms** | 持平 | 极佳 |
| `hello world` | 36ms | **17ms** | **↓53%（查询最优）** | 极佳 |
| `*.swift` | 23ms | **22ms** | ↓4% | 极佳 |
| `main.cpp` | 4ms | **2ms** | ↓50% | 极佳 |
| `abc def...` | 41ms | **25ms** | ↓39% | 极佳 |
| `README` | 23ms | **9ms** | ↓61% | 极佳 |
| `test` | 40ms | **19ms** | ↓53% | 极佳 |
| `MacEverything` | 9ms | **4ms** | ↓56% | 极佳 |
| Trigram 均值 | 25ms | **16ms** | **↓36%** | 全部极佳 |

- **8/8 trigram 全部 <50ms——连续第 4 轮。**
- **均值 25→16ms（↓36%）——回到 R201 的系列最优水平。**
- **`hello world`=17ms——从 R199=3128ms 的完全恢复，创该查询系列最优。**

### 全表扫描——继续收敛

| Query | R203 | R204 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 214ms | **213ms** | ↓0.5% |
| `path:/Users` | 379ms | **375ms** | ↓1% |
| `中文测试` | 381ms | **245ms** | **↓36%** |
| **总和** | **974ms** | **833ms** | **↓14%** |

- **全表扫描总和 974→833ms（↓14%）——持续收敛。**
- **`中文测试`=245ms（↓36%）——名称数组区域 page cache 恢复。**
- **`path:/Users`=375ms（持平）——路径数组区域稳定但偏高。**
- **线性扫描区域受 tombstone 增长影响——25.43% 死记录意味着扫描需要遍历更多无效数据。**

### SoA Filter——`dm:today` 偏高

| Query | R203 | R204 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 43ms | **56ms** | ↑30% |
| `dm:today` | 46ms | **88ms** | ↑91% |

- **`dm:today`=88ms（R203=46ms，↑91%）——SoA 元数据区域受午间 I/O 影响。**
- **`size:>100m`=56ms（微出极佳线）——轻度波动。**
- **两项仍在正常范围。**

### tombstone 25.43%——第 5 轮确认 compaction 缺陷

| 轮次 | tombstone% | 超阈值 | COMPACTION | FLUSH | SKIPFLUSH |
|------|----------:|-------:|----------:|------:|----------:|
| R200 | 25.15% | +0.15% | 2 | 1 | 4195 |
| R201 | 25.23% | +0.23% | 2 | 1 | 4255 |
| R202 | 25.30% | +0.30% | 2 | 1 | 4315 |
| R203 | 25.37% | +0.37% | 2 | 1 | 4374 |
| **R204** | **25.43%** | **+0.43%** | **2** | **1** | **4434** |

- **tombstone 连续 5 轮超过 25% 阈值——从 25.15% 增至 25.43%。**
- **COMPACTION 始终为 2，FLUSH 始终为 1——没有任何变化。**
- **SKIPFLUSH 每轮均匀增长 ~60——flush timer 正常运行但始终跳过。**
- **增速 ~196/min（午间稳定）——按此速度 24 小时后 tombstone 将达 ~29.5%。**

### 新观察：tombstone 增长对线性扫描的影响

- **当前 tombstone=25.43%——线性扫描路径需要遍历 ~25% 的无效记录。**
- **`path:/Users`=375ms 和 `content:import`=213ms 持续偏高——部分原因是死记录增加了扫描量。**
- **如果 tombstone 继续增长至 30%+，线性扫描性能将进一步恶化。**
- **这是 compaction 缺陷的直接性能后果。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 2.1 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 26.8 | 25.6 | advanced-trigram | 0 | 9842 | 极佳 |
| `main.cpp` | 5 | 2.2 | 1.3 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 21.8 | 20.2 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 5.2 | 4.8 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 56.2 | 56.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 16.7 | 16.2 | advanced-trigram | 0 | 3669 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 25.4 | 25.1 | advanced-trigram | 0 | 14344 | 极佳 |
| `README` | 5 | 8.6 | 7.1 | advanced-trigram | 0 | 17006 | 极佳 |
| `test` | 5 | 18.8 | 15.7 | advanced-trigram | 0 | 116884 | 极佳 |
| `MacEverything` | 5 | 3.9 | 1.3 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 87.8 | 87.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 212.9 | 200.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 374.8 | 367.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 244.9 | 244.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.3 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 21.8ms，P90 = 244.9ms，Max = 374.8ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R203 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **11/16** | 0.3-27ms | R203=12/16（↓1） |
| **正常**（50-500ms） | 5/16 | 56-375ms | R203=4/16（↑1） |
| **超标**（>500ms） | **0/16** | — | R203=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,921,201，liveRecordCount=5,161,136（tombstone=**1,760,065**，**25.43%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=338  QUERYADV_GE500=41（不变——连续 4 轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=4434
```

## 6. 问题清单

### P0 确认（第 5 轮）：compaction 触发机制缺陷——开始产生性能后果

- **tombstone 25.43%，线性扫描需要遍历 25% 无效数据。**
- **`path:/Users` 持续偏高（375ms）——部分归因于 tombstone 膨胀。**
- **不再仅是内存问题——已开始影响查询性能。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 5 轮确认）

- **tombstone 已开始影响线性扫描性能——不再仅是内存浪费。**
- **修复方案不变：skipflush 路径中检查 tombstone 比例。**
- **紧迫度：高——每天增长 ~4 个百分点，性能影响将加速恶化。**

### P0.5：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

## 8. 下一轮重点

1. `path:/Users` 是否随 tombstone 增长继续恶化。
2. tombstone 增长趋势。
3. COMPACTION 和 FLUSH 计数。

## 9. 结论

**连续第 4 轮系列顶尖——0/16 >500ms，Max=375ms，P50=22ms，极佳 11/16。** Trigram 全部 <50ms（连续第 4 轮），均值 16ms，`hello world`=17ms 创该查询最优。全表扫描总和 833ms（↓14%，继续收敛）。

**⚠️ compaction 触发缺陷连续第 5 轮确认——tombstone 25.43%（超阈值 0.43%），COMPACTION=2，FLUSH=1。** tombstone 膨胀开始产生性能后果——线性扫描需要遍历 25% 无效记录，`path:/Users` 持续偏高。按当前增速（~196/min），24 小时后 tombstone 将达 ~29.5%。compaction 缺陷已从"数据问题"升级为"性能问题"。
