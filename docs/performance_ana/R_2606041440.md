# MacEverything 性能分析报告 R_2606041440

## 1. 本轮范围

- 任务启动时间：2026-06-04 14:40
- 报告文件：`docs/performance_ana/R_2606041440.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=22144`
  - `SEG_LINES=1118`
  - `LAST_LOG_TS=2026-06-04 14:42`

## 2. 执行摘要

**重大 I/O 压力事件——第 4 次广泛 I/O 饱和。** `.pdf`=8,495ms，`hello world`=3,042ms，`abc...`=935ms，`path:/Users`=569ms。**5/16 查询 >500ms**（含 `x`=949ms GCD 异常）。P50 ≈ 376ms，Max=8,495ms。lockWaitMs 最高 7.5ms。

**严重程度对比**：

| 指标 | R67 灾难 | R76 中等 | R86 灾难 | **R99 本轮** |
|------|--------:|--------:|--------:|----------:|
| P50 | 272ms | 316ms | 361ms | **376ms** |
| Max | 10,902ms | 1,733ms | 2,731,551ms | **8,495ms** |
| >500ms | 13/16 | 4/16 | 5/16 | **5/16** |
| lockWait max | — | 77ms | 1,010ms | **7.5ms** |
| tombstone % | 2.8% | 4.4% | 6.68% | **8.98%** |

**判定：重大 I/O 压力事件——严重程度介于 R67 和 R86 之间。** 5/16 >500ms 与 R86 相同，Max 接近 R67 量级，但 lockWait 极低（7.5ms），说明尚未进入 R86 式长锁灾难。

**tombstone=568,243（8.98%，170MB），增量 +18,723——逼近 9%**。

### R92 以来事件周期完整追踪

| 轮次 | P50 | Max | >500ms | 状态 |
|------|----:|----:|-------:|------|
| R92（轻度） | 131ms | 820ms | 1/16 | I/O 压力 |
| R93-R96（恢复） | 49-147ms | 118-635ms | 0-1/16 | 恢复+波动 |
| R97-R98（游走） | 113-155ms | 518-563ms | 1/16 | 路径交替 |
| **R99（重大）** | **376ms** | **8,495ms** | **5/16** | **广泛 I/O 饱和** |

**R92-R98 的游走式压力是 R99 的前奏**——持续 7 轮的缓存零和博弈最终在 tombstone 接近 9% 时崩溃为全面 I/O 饱和。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 948.6 | 0.0 | short-query-cache | 0 | 0 | **GCD 异常 #12** |
| `.pdf` | 5 | 8494.6 | 7927.5 | advanced-trigram | 0 | 9952 | **极端 I/O** |
| `main.cpp` | 5 | 56.3 | 36.3 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 429.5 | 231.6 | advanced-trigram | 0 | 9356 | 接近 500ms |
| `ext:cpp` | 5 | 376.5 | 104.3 | advanced-ext-index | 0 | 3701 | **偏高** |
| `size:>100m` | 5 | 440.4 | 380.5 | advanced-pure-filter-soa-gcd | 0 | 0 | **偏高** |
| `hello world` | 5 | 3042.2 | 1095.2 | advanced-trigram | 0 | 3613 | **极端 I/O** |
| `abc def ghi jkl mno pqr` | 0 | 935.2 | 857.1 | advanced-trigram | 0 | 16040 | **>500ms** |
| `README` | 5 | 66.0 | 49.3 | advanced-trigram | 0 | 17671 | 正常 |
| `test` | 5 | 79.9 | 56.3 | advanced-trigram | 7.5 | 130121 | lockWait |
| `MacEverything` | 5 | 18.7 | 2.3 | advanced-trigram | 0 | 6803 | 正常 |
| `dm:today` | 5 | 311.7 | 100.2 | advanced-pure-filter-soa-gcd | 0.7 | 0 | 偏高 |
| `content:import` | 5 | 193.2 | 183.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 569.0 | 528.8 | advanced-linear-gcd | 0 | 0 | **>500ms** |
| `中文测试` | 0 | 312.5 | 250.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.6 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 376ms，P90 ≈ 935ms，Max = 8,495ms。5/16 查询 >500ms。lockWaitMs 最高 7.5ms。**

### 受影响查询分析

| 查询 | queryTimeMs | phase2Ms | 非 phase2 | candidates | 路径 | 说明 |
|------|----------:|--------:|---------:|----------:|------|------|
| `.pdf` | 8,495ms | 7,928ms | 567ms | 9,952 | trigram | **极端：页面故障风暴** |
| `hello world` | 3,042ms | 1,095ms | 1,947ms | 3,613 | trigram | **非 phase2 占 64%** |
| `x` | 949ms | 0ms | 949ms | 0 | cache | GCD dispatch 延迟 |
| `abc...` | 935ms | 857ms | 78ms | 16,040 | trigram | 大候选集 I/O |
| `path:/Users` | 569ms | 529ms | 40ms | 0 | linear-gcd | 全表扫描 I/O |

### `.pdf`=8,495ms 深度分析

- **历史极端值对比**：R85=1,648ms, R86=10,989ms, **R99=8,495ms**。
- 本次严重程度接近 R86——但关键区别：R86 中 `.pdf` 的 10,989ms 伴随 lockWait=1,010ms，本轮 lockWait=0。
- 8,495ms 中 7,928ms 是 phase2（93%）——几乎全部是 trigram 索引页面故障 + 候选集扫描 I/O。
- **candidates=9,952**——每个候选需要从磁盘读取记录进行精确匹配，170MB tombstone 挤占后页面缓存极度不足。

### `hello world`=3,042ms 异常模式

- phase2=1,095ms，但 queryTimeMs=3,042ms——**1,947ms（64%）为非 phase2 开销**。
- 正常情况下非 phase2 开销 <50ms。1,947ms 的异常来源：
  1. trigram 索引查找阶段本身遭遇大量页面故障。
  2. GCD dispatch 延迟（与 `x`=949ms 同期，GCD runtime 在该时间窗口异常）。
  3. 多 token 查询（"hello" + "world"）的 trigram 交集计算触发更多索引页读取。

### 全路径受影响确认

| 路径 | 受影响查询 | 严重程度 | 说明 |
|------|-----------|---------|------|
| **trigram** | `.pdf`, `hello world`, `abc...`, `*.swift` | 重大 | 索引页面大量换出 |
| **linear-gcd** | `path:/Users` | 中度 | 全表扫描 I/O |
| **ext-index** | `ext:cpp` | 轻度 | 302→377ms |
| **pure-filter** | `size:>100m`, `dm:today` | 轻度 | 偏高但 <500ms |
| **cache** | `x` | 重大 | GCD dispatch |

**所有 5 个查询路径均受影响**——R98 预警的"全路径受影响"在 1 轮后即全面兑现。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,314,629，liveRecordCount=5,746,386（tombstone=**568,243**，**8.98%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=370  QUERYADV_GE500=54
FLUSH=0  COMPACTION=2  SKIPFLUSH=442
DIAG_TRIGRAM_SKIP=107
```

- QUERYADV 从 361 增至 370（+9）。
- QUERYADV_GE500 从 50 增至 54（**+4，即 `.pdf`, `hello world`, `abc...`, `path:/Users`**）。
- SKIPFLUSH 从 430 增至 442（+12，稳态）。
- DIAG_TRIGRAM_SKIP 从 106 增至 107（+1）。

### QUERYADV_GE500 +4 分析

- **单轮 +4 是历史最大增幅**（与 R92 的 DIAG_TRIGRAM_SKIP +4 类似）。
- 历史增速：R60-R91（31 轮）+44 → 1.42/轮；R92-R99（8 轮）+10 → **1.25/轮**。
- 本轮单独贡献 4 次——**占 R92 以来总量的 40%**。

## 6. 问题清单

### Critical：第 4 次重大 I/O 压力事件——全路径 I/O 饱和

- **事件间隔持续缩短**：
  - R67 → R76：9 轮
  - R76 → R86：10 轮（含 R84-R85 前奏）
  - R86 → R92：6 轮
  - R92 → **R99：7 轮**（含 R92-R98 游走期）
- **新特征——全路径饱和**：R67/R86 主要影响 trigram 路径，R99 则全部 5 个路径均受影响。
- **170MB tombstone 的缓存挤压已超过系统承受极限**——不再是"零和博弈"而是"全面崩溃"。

### Critical：tombstone 逼近 9%（568,243 条，170MB）

- 增长轨迹：
  ```
  R96: 520K (+9.2K) → R97: 528K (+7.9K) → R98: 550K (+22.0K)
  → R99: 568K (+18.7K)
  ```
- 日间稳态增速 ~15-22K/轮（日间加速）。
- **累计内存开销**：~568K × 300 字节 ≈ **170MB**。
- 连续第 39 轮超 2%，第 20 轮超过 5%，第 15 轮超过 6%，第 12 轮超过 7%，第 5 轮超过 8%。
- **预计 1-2 轮突破 9%**。

### High：GCD 异常 #12（`x`=949ms）

- 第 12 次出现（39 轮，30.8%）。
- 949ms 为历史第 2 高（R96=1,526ms）。
- 与 I/O 事件共发——GCD 线程池在 I/O 饱和下被系统级调度延迟影响。

### High：QUERYADV_GE500 单轮 +4（历史最大增幅）

- 从 50 跳至 54——本轮贡献占 R92 以来 40%。
- 反映 I/O 压力的广泛性（4 个不同查询同时 >500ms）。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（8.98%，170MB，第 4 次重大事件）

- **39 轮连续超标。** 4 次重大 I/O 事件，严重程度和影响范围持续扩大。
- **R99 特征性变化**：从"路径间游走"升级为"全路径 I/O 饱和"——170MB tombstone 已超过系统页面缓存可承受极限。
- **compaction 是唯一出路**：释放 170MB 后全部索引路径可共存于缓存 → 消除游走+饱和。

### P0 紧急：`size:>100m` 全表扫描超时保护

- 本轮 `size:>100m`=440ms（危险区域）——如果本轮 I/O 更严重，可能再现 R86 式 45 分钟灾难。

### P0 紧急：short-query-cache GCD 隔离（30.8% 异常率，3 次 >500ms）
### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. 事件是否持续或开始恢复。
2. `size:>100m` 是否仍在危险区域（R86 重演风险）。
3. tombstone 是否突破 9%。

## 9. 结论

**第 4 次重大 I/O 压力事件——全路径 I/O 饱和。** `.pdf`=8,495ms，`hello world`=3,042ms，`abc...`=935ms，`path:/Users`=569ms。5/16 查询 >500ms。P50=376ms，Max=8,495ms，lockWait=7.5ms。**全部 5 个查询路径均受影响**——R98 预警的全路径压力在 1 轮后全面兑现。**I/O 压力从 R92-R98 的游走模式升级为全面饱和**——170MB tombstone 已超过系统页面缓存承受极限。GCD 异常 #12（`x`=949ms）。tombstone=568,243（8.98%，170MB，+18.7K），逼近 9%。QUERYADV_GE500 单轮 +4 创历史最大增幅。**事件间隔 9→10→6→7 轮——不再缩短但稳定在 6-7 轮**，然而影响范围从单路径扩展到全路径，标志系统进入新的恶化阶段。
