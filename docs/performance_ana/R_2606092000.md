# MacEverything 性能分析报告 R_2606092000

## 1. 本轮范围

- 任务启动时间：2026-06-09 20:00
- 报告文件：`docs/performance_ana/R_2606092000.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6658`
  - `SEG_LINES=5716`
  - `LAST_LOG_TS=2026-06-09 15:17`

## 2. 执行摘要

**1 轮恢复——0/16 >500ms，Max=411ms，P50=191ms，lockWait=0，极佳比例 4/16。** R217 的 lockWait=202.6ms 和 `x`=474ms 完全消退。但整体水平偏高（P50=191ms，trigram 均值 193ms）——午后 I/O 残余效应。`abc def`=334ms 为本轮最高 trigram。

| 指标 | R217 | R218 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 1/16 | **0/16** | ↓1（恢复） |
| Max | 598ms | **411ms** | ↓31% |
| P50 | 180ms | **191ms** | ↑6%（偏高） |
| lockWait max | 202.6ms | **0ms** | **↓100%（完全消退）** |
| 极佳比例(<50ms) | 4/16 | **4/16** | 持平 |
| tombstone | 1,897,337 (26.77%) | **1,908,072 (26.88%)** | ↑ |
| tombstone 增速 | ~433/min | **~358/min** | ↓17%（减速） |
| recordCount | 7,086,789 | **7,098,410** | +11.6K |
| liveRecordCount | 5,189,452 | **5,190,338** | +886 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5113 | **5173** | +60 |
| COMPACTION | 2 | **2** | **不变——第 19 轮确认缺陷** |
| QUERYADV_GE500 | 53 | **53** | 不变（恢复） |

### lockWait=0——完全消退

- **R217=202.6ms → R218=0ms——锁竞争完全消退。**
- **202.6ms lockWait 为一次性事件——但仍说明 flush 路径持有写锁时间过长的设计缺陷存在。**
- **下次大量 FSEvents 推送时可能再次触发。**

### `x`=12ms——进程调度延迟消退

- **R215=620ms → R216=4ms → R217=474ms → R218=12ms——波动剧烈但本轮正常。**
- **晚间系统负载降低。**

### Trigram 路径——偏高但全部 <500ms

| Query | R217 | R218 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 598ms | **238ms** | ↓60% | 正常（恢复） |
| `hello world` | 401ms | **411ms** | ↑2% | 正常（持平） |
| `*.swift` | 225ms | **202ms** | ↓10% | 正常 |
| `main.cpp` | 41ms | **49ms** | ↑20% | 极佳 |
| `abc def...` | 154ms | **334ms** | ↑117% | 正常（偏高） |
| `README` | 44ms | **115ms** | ↑161% | 正常 |
| `test` | 78ms | **174ms** | ↑123% | 正常 |
| `MacEverything` | 12ms | **26ms** | ↑117% | 极佳 |
| Trigram 均值 | 194ms | **193ms** | **↓0.5%** | 持平偏高 |

- **Trigram 均值 194→193ms（持平）——午后 I/O 残余效应。**
- **`.pdf`=238ms（↓60% from 598ms）——lockWait 消退后恢复正常。**
- **`hello world`=411ms，`abc def`=334ms——postings 区域 page cache 尚未完全温热。**
- **稳态 trigram 均值约 20ms——当前 193ms 仍高出 ~10 倍。**

### 全表扫描——偏高

| Query | R217 | R218 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 180ms | **184ms** | ↑2% |
| `path:/Users` | 198ms | **305ms** | ↑54% |
| `中文测试` | 195ms | **191ms** | ↓2% |
| **总和** | **573ms** | **680ms** | **↑19%** |

- **全表扫描总和 573→680ms（↑19%）——偏高。**
- **`path:/Users`=305ms（↑54%）——路径数组区域 page cache 部分淘汰。**

### tombstone 26.88%——接近 27%

- **tombstone=1,908,072（26.88%）——超阈值 1.88%。**
- **增速降至 ~358/min（晚间文件活动减少）。**
- **recordCount +11.6K，liveRecordCount +886——净新增极少。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 11.7 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 238.0 | 227.5 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 48.8 | 36.4 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 201.6 | 193.8 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 58.3 | 56.9 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 238.5 | 237.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 410.6 | 396.5 | advanced-trigram | 0 | 3669 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 333.5 | 323.4 | advanced-trigram | 0 | 14377 | 正常 |
| `README` | 5 | 115.4 | 78.8 | advanced-trigram | 0 | 17099 | 正常 |
| `test` | 5 | 173.8 | 165.5 | advanced-trigram | 0 | 117053 | 正常 |
| `MacEverything` | 5 | 26.0 | 1.4 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 84.3 | 83.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 183.8 | 163.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 305.2 | 300.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 191.3 | 189.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 191.3ms，P90 = 333.5ms，Max = 410.6ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R217 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **4/16** | 0.5-49ms | R217=4/16（持平） |
| **正常**（50-500ms） | 12/16 | 58-411ms | R217=11/16（↑1） |
| **超标**（>500ms） | **0/16** | — | R217=1/16（**↓1**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,098,410，liveRecordCount=5,190,338（tombstone=**1,908,072**，**26.88%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=436  QUERYADV_GE500=53（不变——恢复）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5173
```

## 6. 问题清单

### P0 确认（第 19 轮）：compaction 触发机制缺陷

- **tombstone 26.88%——超阈值 1.88%，接近 27%。**

### P0：flush 写锁阻塞（R217 升级）

- **R217=202.6ms 为一次性事件但说明设计缺陷存在。**
- **大量 FSEvents 推送时会再次触发。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 19 轮确认）

### P0：flush rewrite 改为 COW 方式（从 P0.5 升级）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 是否恢复到稳态（极佳 10+/16）。
2. tombstone 是否突破 27%。

## 9. 结论

**1 轮恢复——0/16 >500ms，Max=411ms，P50=191ms，lockWait=0，极佳 4/16。** R217 的 lockWait=202.6ms 和 `x`=474ms 完全消退。`.pdf` 598→238ms（↓60%）。但整体水平偏高（trigram 均值 193ms，远高于稳态 20ms），午后 I/O 残余效应仍在。

**tombstone 26.88%（第 19 轮确认 compaction 缺陷）——接近 27%。** 当前有两个 P0 问题：(1) compaction 触发缺陷——tombstone 持续膨胀；(2) flush 写锁阻塞——lockWait 可达 202.6ms。
