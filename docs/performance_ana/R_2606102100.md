# MacEverything 性能分析报告 R_2606102100

## 1. 本轮范围

- 任务启动时间：2026-06-10 21:00
- 报告文件：`docs/performance_ana/R_2606102100.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=9349`
  - `SEG_LINES=8407`
  - `LAST_LOG_TS=2026-06-10 19:17`

## 2. 执行摘要

**全表扫描回升——`中文测试`=422ms（接近 500ms 阈值）。flush rewrite=2688ms（回落但仍高）。** 0/16 >500ms，Max=422ms，P50=44.4ms，lockWait=0。**极佳 8/16（偏低）。** `x`=1ms（缓存稳定，连续 7 轮）。全表扫描总和 834ms（↑58% from R267=527ms——零和效应回摆）。Trigram 保持良好（`.pdf`=45ms, `hello world`=44ms）。FLUSH 61→68（+7——再次 +7）。tombstone=135,551（2.56%——增速持续）。recordCount +14.6K（连续 5 轮偏高）。

| 指标 | R267 | R268 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 194ms | **422ms** | **↑118%** |
| P50 | 35ms | **44.4ms** | ↑27% |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 10/16 | **8/16** | ↓2 |
| tombstone | 121,278 (2.31%) | **135,551 (2.56%)** | ↑（增速持续） |
| recordCount | 5,367,580 | **5,382,181** | +14.6K（连续 5 轮偏高） |
| liveRecordCount | 5,246,302 | **5,246,630** | +328（正常） |
| FLUSH 累计 | 61 | **68** | **+7（再次 +7！）** |
| SKIPFLUSH | 7266 | **7266** | +0 |
| COMPACTION | 8 | **8** | 不变 |
| QUERYADV_GE500 | 78 | **78** | 不变 |

### flush rewrite=2688ms——回落但仍高

**日志末行：**
```
[2026-06-10 19:17:29.961] Flushed v6 flat index, lastEventId=340033439,
liveRecords=5246630 | timing: walSwap=0ms rewrite=2688ms sqcache=2ms total=2692ms
```

| 轮次 | flush rewrite | 变化 |
|------|------------:|------|
| R265 | 2617ms | 首次捕获 |
| R266 | 3981ms | ↑52%（纪录） |
| **R268** | **2688ms** | **↓32%（回落）** |

- **2688ms——从 3981ms 回落 32%，但仍高于 R265=2617ms。**
- **回落原因：磁盘 I/O 负载降低（非索引体积减小——recordCount 仍在增长）。**
- **flush rewrite 时间波动范围：2600-4000ms——受磁盘 I/O 负载影响。**
- **核心问题不变：每次 flush 重写完整索引（~500MB），耗时 2.6-4.0 秒。**

### `中文测试`=422ms——接近 500ms 阈值

- **R267=194ms → R268=422ms（↑117%）。**
- **phase2=416ms——纯搜索时间，非 overhead。**
- **lockWait=0——非锁等待，是 page cache 淘汰导致的 I/O。**
- **与 flush rewrite=2688ms 时间上相关——flush I/O 冲刷了 SoA 数据区域。**
- **422ms 接近 500ms 阈值但未超标——处于临界状态。**

### FLUSH +7（再次！）

- **R265=+7, R266=+5, R267=+6, R268=+7。**
- **+7 意味着部分 30s tick 内发生了 2 次 flush（WAL 在 tick 内再次累积超过 100）。**
- **recordCount 连续 5 轮 +14K 以上——高 WAL 生成速率。**

### `x` 缓存——连续 7 轮稳定

- **1ms——完美。连续 7 轮 short-query-cache（R261-R268）。**

### Trigram 路径——良好

| Query | R267 | R268 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 55ms | **45ms** | ↓18% | 极佳 |
| `hello world` | 47ms | **44ms** | ↓6% | 极佳 |
| `*.swift` | 24ms | **66ms** | ↑175%（overhead） | 正常 |
| `main.cpp` | 7ms | **4ms** | ↓43% | 极佳 |
| `abc def...` | 33ms | **59ms** | ↑79% | 正常 |
| `README` | 7ms | **10ms** | ↑43% | 极佳 |
| `test` | 12ms | **14ms** | ↑17% | 极佳 |
| `MacEverything` | 3ms | **7ms** | ↑133% | 极佳 |
| Trigram phase2均值 | ~19ms | **~22ms** | ↑16%（轻微） |

- **`.pdf`=45ms 和 `hello world`=44ms——恢复至极佳。**
- **`*.swift`=66ms（phase2=17ms 但 overhead=50ms——系统负载波动）。**
- **`abc def`=59ms（phase2=58ms——14K candidates postings 页部分淘汰）。**

### 全表扫描——零和回摆

| Query | R267 | R268 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 157ms | **189ms** | ↑20% |
| `path:/Users` | 176ms | **223ms** | ↑27% |
| `中文测试` | 194ms | **422ms** | **↑117%** |
| **总和** | **527ms** | **834ms** | **↑58%** |

- **全表扫描 527→834ms（↑58%——`中文测试`=422ms 拉高）。**
- **`content:import` 和 `path:/Users` 也回升 20-27%——page cache 竞争加剧。**
- **与 flush rewrite=2688ms 时间关联——flush I/O 冲刷 page cache。**

### 辅助路径

| Query | R267 | R268 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 5ms | **1ms** | ↓80% | 极佳 |
| `ext:cpp` | 35ms | **12ms** | ↓66% | 极佳 |
| `size:>100m` | 107ms | **51ms** | ↓52%（回归正常） | 正常 |
| `dm:today` | 41ms | **85ms** | ↑107% | 正常 |
| `verylongquery...` | 0.3ms | **2ms** | ↑（lockWait=0，overhead） | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1.0 | 0.0 | short-query-cache | 0 | 0 | 极佳（稳定） |
| `.pdf` | 5 | 44.8 | 43.4 | advanced-trigram | 0 | 9788 | 极佳 |
| `main.cpp` | 5 | 4.2 | 1.1 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 66.3 | 16.6 | advanced-trigram | 0 | 9405 | 正常（overhead） |
| `ext:cpp` | 5 | 11.6 | 11.3 | advanced-ext-index | 0 | 3611 | 极佳 |
| `size:>100m` | 5 | 50.6 | 50.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 44.0 | 39.2 | advanced-trigram | 0 | 3930 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 59.1 | 58.2 | advanced-trigram | 0 | 14456 | 正常 |
| `README` | 5 | 9.7 | 8.0 | advanced-trigram | 0 | 17432 | 极佳 |
| `test` | 5 | 14.3 | 11.7 | advanced-trigram | 0 | 117728 | 极佳 |
| `MacEverything` | 5 | 6.9 | 0.8 | advanced-trigram | 0 | 6943 | 极佳 |
| `dm:today` | 5 | 85.0 | 83.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 189.1 | 173.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 222.7 | 211.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 421.7 | 416.3 | advanced-linear-gcd | 0 | 0 | 正常（接近阈值！） |
| `verylongquery...` | 0 | 1.8 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 44.4ms，P90 = 222.7ms，Max = 421.7ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R267 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **8/16** | 1-45ms | R267=10/16（↓2） |
| **正常**（50-500ms） | 8/16 | 51-422ms | R267=6/16（↑2） |
| **超标**（>500ms） | **0/16** | — | R267=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,382,181，liveRecordCount=5,246,630（tombstone=**135,551**，**2.56%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=703  QUERYADV_GE500=78（不变）
FLUSH=68（+7！再次）  COMPACTION=8  SKIPFLUSH=7266
```

**关键日志：**
```
Flushed v6 flat index | rewrite=2688ms sqcache=2ms total=2692ms
```

## 6. 问题清单

### P0（紧急）：flush rewrite 2.6-4.0 秒

- **rewrite=2688ms（回落但仍高——波动范围 2.6-4.0 秒）。**
- **每次 flush 冲刷 page cache → 查询性能恶化。**

### P0：compaction 触发机制缺陷（根因未修复）

### P0：I/O 优先级保护

### 观察项：`中文测试`=422ms 接近 500ms 阈值

- **连续波动：194→422ms——page cache 争用加剧的前兆。**

## 7. 优化建议

### P0（紧急）：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制

### P0：进程 QoS / I/O 优先级保护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. `中文测试`=422ms 是否突破 500ms（临界状态）。
2. flush rewrite 趋势（2688ms——是否继续波动）。
3. recordCount 增速（连续 5 轮 +14K 以上——何时回归正常）。

## 9. 结论

**全表扫描回升——`中文测试`=422ms（接近 500ms 阈值）。** 0/16 >500ms，Max=422ms，P50=44ms，lockWait=0。极佳 8/16（偏低）。`x`=1ms（缓存稳定，连续 7 轮）。Trigram `.pdf`=45ms, `hello world`=44ms（恢复至极佳）。全表扫描 527→834ms（↑58%——flush I/O 冲刷 page cache）。

**flush rewrite=2688ms（回落但仍高——波动范围 2.6-4.0 秒）。** FLUSH +7（再次——部分 tick 内发生 2 次 flush）。tombstone=135,551（2.56%——增速 ~14K/轮持续）。recordCount +14.6K（连续 5 轮偏高）。P0 COW flush 改造紧急性不变。
