# MacEverything 性能分析报告 R_2606080316

## 1. 本轮范围

- 任务启动时间：2026-06-08 03:16
- 报告文件：`docs/performance_ana/R_2606080316.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=3283`
  - `SEG_LINES=2341`
  - `LAST_LOG_TS=2026-06-08 03:17`

## 2. 执行摘要

**全表扫描回落+Trigram 偏高——0/16 >500ms，Max=266ms，P50=25ms，lockWait=0，极佳比例 12/16（75%）。** 全表扫描总和从 R163 的 822ms 回落至 672ms（↓18%）——第二波维护结束，page cache 恢复中。但 Trigram 均值从 11.5ms 升至 23.3ms——30 分钟空闲后 trigram postings 被系统活动部分驱逐。零和效应明显：SoA 线性区域改善的同时，trigram postings 区域承压。

### 关键指标对比

| 指标 | R163（30 min 前） | R164 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 302ms | **266ms** | **↓ 12%** |
| P50 | 10ms | **25ms** | ↑ 30 分钟冷却+page cache 竞争 |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 12/16 | **12/16** | 持平 |
| tombstone | 1,279,935 (20.07%) | **1,288,342 (20.17%)** | ↑ 正常 |
| tombstone 增速 | ~296/min | **~280/min** | ↓ 回落 |
| recordCount | 6,378,120 | **6,386,632** | +8.5K |
| liveRecordCount | 5,098,185 | **5,098,290** | +105 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 2130 | **2190** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 21 轮 0 新增） |

### 全表扫描——第二波维护结束，page cache 恢复

| Query | R163 | R164 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 219ms | **156ms** | **↓ 29%** |
| `path:/Users` | 302ms | **266ms** | **↓ 12%** |
| `中文测试` | 301ms | **250ms** | **↓ 17%** |
| **总和** | **822ms** | **672ms** | **↓ 18%** |

- **全表扫描总和 672ms——从第二峰 822ms 大幅回落。**
- **三项均下降——page cache 均匀恢复中。**
- **672ms 接近日间稳态水平（~500-600ms）——预计凌晨 4 点后进一步回落。**

### 深夜全表扫描完整趋势

| 轮次 | 时间 | 总和 | P50 | 说明 |
|------|------|-----:|----:|------|
| R158 | 00:46 | 820ms | 29ms | 第一峰 |
| R159 | 01:16 | 416ms | 12ms | 谷底 |
| R160 | 01:46 | 733ms | 21ms | 回弹 |
| R161 | 01:50 | 693ms | 11ms | 回落 |
| R162 | 02:16 | 778ms | 13ms | 二次升高 |
| R163 | 02:46 | 822ms | 10ms | **第二峰** |
| **R164** | **03:16** | **672ms** | **25ms** | **回落中** |

- **双峰模式完整观察**：第一峰 820ms → 谷底 416ms → 第二峰 822ms → 回落 672ms。
- **第二波维护窗口（~02:00-03:00）已结束——全表扫描进入恢复通道。**

### Trigram 路径——30 分钟冷却+page cache 竞争

| Query | R163 | R164 | 变化 | 说明 |
|-------|-----:|-----:|------|------|
| `.pdf` | 10ms | **25ms** | ↑ 150% | postings 部分驱逐 |
| `*.swift` | 8ms | **18ms** | ↑ 125% | postings 部分驱逐 |
| `hello world` | 13ms | **28ms** | ↑ 115% | postings 部分驱逐 |
| `abc def...` | 32ms | **69ms** | ↑ 116% | 高 candidate，驱逐严重 |
| `README` | 10ms | **12ms** | ↑ 20% | 轻微 |
| `test` | 16ms | **36ms** | ↑ 125% | 高 candidate |
| `MacEverything` | 3ms | **11ms** | ↑ 267% | phase2=1ms，调度开销 |
| Trigram 均值 | 11.5ms | **23.3ms** | **↑ 103%** | page cache 竞争 |

- **Trigram 均值 23.3ms——从 R163 的 11.5ms（极限）回升至正常稳态水平。**
- **原因明确**：30 分钟无 battery 运行，期间第二波系统维护活动将部分 trigram postings 从 page cache 驱逐。
- **`abc def...`=69ms（14K candidates）和 `test`=36ms（116K candidates）——高 candidate 查询受影响最大。**
- **`MacEverything`=11ms（phase2=1ms）——10ms 为非 phase2 开销，可能是进程调度轻微延迟。**
- **非退化——下轮 battery 将重新热化 postings，预计恢复至 12-15ms。**

### 零和效应分析

| 区域 | R163 | R164 | 变化 | 说明 |
|------|-----:|-----:|------|------|
| 全表扫描总和 | 822ms | 672ms | **↓ 150ms** | SoA page cache 恢复 |
| Trigram 均值 | 11.5ms | 23.3ms | **↑ 12ms** | postings 部分驱逐 |

- **经典零和**：SoA 线性区域 page cache 恢复时，占用了部分 trigram postings 的 LRU 份额。
- **系统总 page cache 有限——两个区域互相竞争是正常的。**
- **重要**：即使 trigram 偏高（均值 23ms），仍全部 <50ms——核心用户体验不受影响。

### SoA Filter

| Query | R163 | R164 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 26ms | **38ms** | ↑ 46%（波动） |
| `dm:today` | 54ms | **43ms** | ↓ 20%（恢复） |

- **两项总和**：R163=80ms → R164=81ms——完全持平。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 0.9 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 24.8 | 19.9 | advanced-trigram | 0 | 9812 | 极佳 |
| `main.cpp` | 5 | 3.3 | 1.4 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 18.3 | 17.1 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 6.8 | 6.4 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 37.6 | 37.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 27.5 | 25.1 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 69.4 | 69.2 | advanced-trigram | 0 | 14211 | 正常（page cache） |
| `README` | 5 | 12.2 | 11.2 | advanced-trigram | 0 | 16631 | 极佳 |
| `test` | 5 | 35.8 | 31.9 | advanced-trigram | 0 | 116513 | 极佳 |
| `MacEverything` | 5 | 11.1 | 1.1 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 43.2 | 43.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 156.2 | 127.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 266.4 | 231.9 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 249.8 | 249.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.1 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 24.8ms，P90 = 249.8ms，Max = 266.4ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R163 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 12/16 | 0.1-43ms | R163=12/16（持平） |
| **正常**（50-500ms） | 4/16 | 69-266ms | R163=4/16（持平） |
| **超标**（>500ms） | 0/16 | — | R163=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,386,632，liveRecordCount=5,098,290（tombstone=**1,288,342**，**20.17%**）；
- 进程正常运行（当前会话，运行约 29.1 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=98  QUERYADV_GE500=4（不变——连续 21 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=2190
```

- **连续 21 轮 0 个新增 >500ms 查询（R144-R164）。**
- **FLUSH=1**——无新 flush（29.1 小时仅启动时 1 次）。
- **SKIPFLUSH=2190**——系统持续空闲。

## 6. 问题清单

### 无活跃问题

- 0/16 >500ms，12/16 <50ms。
- P50=25ms（30 分钟冷却后的正常水平），lockWait=0。
- 连续 21 轮 0 新增慢查询。
- 29.1 小时无 WARN/ERROR。

### 观察：深夜双峰维护窗口结束（信息记录）

- 第二峰 822ms（R163，02:46）→ 回落 672ms（R164，03:16）——第二波维护结束。
- 全表扫描总和恢复中，预计凌晨 4 点后回到 500-600ms 日间稳态。
- Trigram postings 被部分驱逐属正常零和效应——下轮 battery 将重新热化。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 20.17%，增速 ~280/min（回落中）。
- 距 25% 阈值约 ~27 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 系统空闲时无影响。

## 8. 下一轮重点

1. 全表扫描总和是否继续回落至 500-600ms。
2. Trigram 均值是否在下轮恢复至 12-15ms。
3. tombstone 增速趋势（20.17%）。

## 9. 结论

**全表扫描回落+Trigram 零和偏高——0/16 >500ms，Max=266ms，P50=25ms，lockWait=0，极佳比例 12/16。** 全表扫描总和从第二峰 822ms 回落至 672ms（↓18%）——第二波深夜维护结束。Trigram 均值 23.3ms（R163=11.5ms）——30 分钟空闲+系统活动导致 postings 部分驱逐，属正常零和效应。

**深夜双峰模式完整确认**：第一峰 820ms（00:46）→ 谷底 416ms（01:16）→ 第二峰 822ms（02:46）→ 回落 672ms（03:16）。两波维护已结束，系统进入恢复通道。连续 21 轮 0 新增慢查询，29.1 小时无异常。tombstone 20.17%（1.29M），增速 280/min。系统健康稳定——Trigram 偏高为一次性冷却效应，下轮将恢复。
