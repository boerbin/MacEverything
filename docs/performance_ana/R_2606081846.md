# MacEverything 性能分析报告 R_2606081846

## 1. 本轮范围

- 任务启动时间：2026-06-08 18:46
- 报告文件：`docs/performance_ana/R_2606081846.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5211`
  - `SEG_LINES=4269`
  - `LAST_LOG_TS=2026-06-08 19:17`

## 2. 执行摘要

**`.pdf`=619ms 再次突破 500ms（1/16 >500ms），trigram 路径从近稳态回弹，lockWait=0，极佳比例 4/16。** 从 R193 的近稳态（Max=185ms，P50=58ms）劣化：Max ↑234%（185→619ms），P50 ↑64%（58→96ms）。`.pdf` 的 trigram postings 区域再次被 page cache 淘汰——这是 `.pdf` 在本系列第四次 >500ms（R181=7193ms，R184=575ms，R190=630ms，R194=619ms）。`path:/Users`=311ms 也明显上升但未超标。

tombstone 24.46%——增速 ~214/min（夜间水平维持），距 25% 约 4.3 小时。QUERYADV_GE500 +1（.pdf=619ms）。

### 关键指标对比

| 指标 | R193 | R194 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **1/16** | **↑1（.pdf=619ms）** |
| Max | 185ms | **619ms** | **↑ 234%** |
| P50 | 58ms | **96ms** | ↑ 64% |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 7/16 | **4/16** | **↓3** |
| tombstone | 1,652,843 (24.40%) | **1,658,821 (24.46%)** | ↑ |
| tombstone 增速 | ~195/min | **~214/min** | ↑ 10%（微升） |
| recordCount | 6,783,379 | **6,789,819** | +6.4K（低） |
| liveRecordCount | 5,130,536 | **5,130,998** | +462 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3884 | **3916** | +32 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 22 | **23** | **↑1（.pdf=619ms）** |

### `.pdf`=619ms——第四次 >500ms

| 轮次 | .pdf (ms) | 事件类型 | 恢复轮数 |
|------|--------:|---------|------:|
| R181 | 7193 | 选择性 trigram 淘汰 | 1 |
| R184 | 575 | 午后压力 | 1 |
| R190 | 630 | 日间重度淘汰 | 1 |
| **R194** | **619** | **傍晚压力回弹** | **待验证** |

- **phase2=597ms，lockWait=0，candidates=9842。**
- **四次 >500ms 均为 trigram postings 区域被 page cache 淘汰。**
- **`.pdf` 查询的 postings 区域与系统 I/O 活跃区域高度重叠——是 page cache 竞争的"哨兵查询"。**
- **前三次均 1 轮恢复——预计本轮也将 1 轮恢复。**

### 从近稳态回弹——傍晚 I/O 突发

- **R193 是傍晚低活动窗口的近稳态——P50=58ms，极佳 7/16。**
- **R194 回弹说明傍晚仍有间歇性 I/O 活动（可能是系统维护、后台下载等）。**
- **`x`=23ms（R193=3ms，↑667%）——进程调度延迟增加，说明 CPU/I/O 竞争存在。**

### Trigram 路径——从近稳态回弹

| Query | R193 | R194 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 145ms | **619ms** | **↑327%（>500ms！）** | 重度冷态 |
| `hello world` | 185ms | **373ms** | ↑102% | 冷态 |
| `*.swift` | 85ms | **138ms** | ↑62% | 正常 |
| `main.cpp` | 18ms | **70ms** | ↑289% | 正常 |
| `abc def...` | 130ms | **101ms** | ↓22% | 正常 |
| `README` | 43ms | **31ms** | **↓28%** | 极佳 |
| `test` | 67ms | **255ms** | **↑281%** | 冷态 |
| `MacEverything` | 12ms | **15ms** | ↑25% | 极佳 |
| Trigram 均值 | 77ms | **200ms** | **↑160%** | 回弹 |

- **Trigram 均值 77→200ms（↑160%）——从近稳态大幅回弹。**
- **`.pdf`=619ms 和 `test`=255ms 是主要慢项。**
- **`test`=255ms（R193=67ms，↑281%）——116K candidates 的大范围 postings 查询，冷态影响显著。**
- **`README`=31ms（R193=43ms，↓28%）——部分区域仍保持热态，说明淘汰是选择性的。**
- **`abc def...`=101ms（R193=130ms，↓22%）——也保持温态。**
- **不均匀回弹——`.pdf`/`test`/`hello world` 区域被淘汰，`README`/`abc def`/`MacEverything` 保持热态。**

### 全表扫描——上升但可控

| Query | R193 | R194 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 88ms | **177ms** | ↑101% |
| `path:/Users` | 100ms | **311ms** | **↑211%** |
| `中文测试` | 50ms | **172ms** | ↑244% |
| **总和** | **238ms** | **660ms** | **↑177%** |

- **全表扫描总和 238→660ms（↑177%）——从近稳态回到日间正常上端。**
- **`path:/Users`=311ms——上升但远低于 R190=2043ms，无灾难性事件。**
- **三项均匀上升——SoA/linear 区域受到均匀的 page cache 压力。**

### SoA Filter——正常

| Query | R193 | R194 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 59ms | **82ms** | ↑39% |
| `dm:today` | 17ms | **47ms** | ↑176% |

- **`dm:today`=47ms（R193=17ms，↑176%）——从极佳回到正常边缘。**
- **SoA 区域受轻度 page cache 压力。**

### tombstone 24.46%——增速微升但仍在夜间水平

- **tombstone=1,658,821（24.46%）。**
- **增速 ~214/min（R193=195/min，↑10%）——微升但仍在夜间水平。**
- **距 25% 需要额外 36,634 个 tombstone。**
- **按 214/min 计算：~171 分钟 ≈ 2.9 小时（约 21:45）。**
- **按夜间低速 150/min 计算：~4.1 小时（约 22:55）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 23.2 | 0.0 | short-query-cache | 0 | 0 | 极佳（调度延迟） |
| `.pdf` | 5 | 619.3 | 596.7 | advanced-trigram | 0 | 9842 | **>500ms** |
| `main.cpp` | 5 | 69.6 | 45.8 | advanced-trigram | 0 | 82 | 正常 |
| `*.swift` | 5 | 138.3 | 132.6 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 96.6 | 93.9 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 81.9 | 80.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 373.0 | 368.2 | advanced-trigram | 0 | 3638 | 冷态 |
| `abc def ghi jkl mno pqr` | 0 | 100.7 | 99.9 | advanced-trigram | 0 | 14307 | 正常 |
| `README` | 5 | 31.0 | 29.0 | advanced-trigram | 0 | 16658 | 极佳 |
| `test` | 5 | 254.5 | 251.0 | advanced-trigram | 0 | 116588 | 冷态 |
| `MacEverything` | 5 | 14.6 | 0.8 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 47.4 | 47.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳边缘 |
| `content:import` | 5 | 176.7 | 169.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 310.7 | 299.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 172.4 | 171.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 96.6ms，P90 = 310.7ms，Max = 619.3ms。1/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R193 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 4/16 | 0.2-47ms | R193=7/16（**↓3**） |
| **正常**（50-500ms） | 11/16 | 70-373ms | R193=9/16（↑2） |
| **超标**（>500ms） | **1/16** | 619ms | R193=0/16（**↑1**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,789,819，liveRecordCount=5,130,998（tombstone=**1,658,821**，**24.46%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=270  QUERYADV_GE500=23（+1——.pdf=619ms）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3916
```

- **QUERYADV_GE500 +1（.pdf=619ms）。**
- **SKIPFLUSH +32。**

## 6. 问题清单

### P0 紧急：tombstone 24.46%——距 25% 约 3-4 小时

- **距 25% 仅 0.54 个百分点（36,634 tombstone）。**
- **增速 214/min（夜间水平）——约 2.9-4.1 小时触达。**
- **compaction 可能在今晚 21:45-23:00 之间触发。**

### `.pdf`——page cache 竞争的"哨兵查询"

- **第四次 >500ms（R181/R184/R190/R194）。**
- **trigram postings 区域与系统 I/O 热区高度重叠。**
- **前三次均 1 轮恢复——预计本轮也将 1 轮恢复。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因

- **tombstone 24.46%，今晚极可能触发 compaction。**
- **紧迫度：小时级。**

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

- `.pdf` 的 postings 区域应纳入热区维护——系列四次 >500ms。

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. `.pdf` 是否从 619ms 1 轮恢复至 <500ms。
2. tombstone 增速是否继续维持夜间水平（~200/min）。
3. 是否出现 compaction 相关日志。

## 9. 结论

**`.pdf`=619ms 再次突破 500ms（1/16 >500ms），傍晚从近稳态回弹。** `.pdf` 是 page cache 竞争的"哨兵查询"——trigram postings 区域与系统 I/O 热区高度重叠，系列四次 >500ms。前三次均 1 轮恢复，预计本轮同样。lockWait=0，`path:/Users`=311ms（远低于 R190=2043ms）——非灾难性事件。

**tombstone 24.46%——距 25% 仅 0.54 个百分点，今晚可能触发 compaction。** 增速 214/min（夜间水平），预计 21:45-23:00 之间触达。P0 compaction rewrite 修复进入最后几小时倒计时。
