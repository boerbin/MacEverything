# MacEverything 性能分析报告 R_2606100430

## 1. 本轮范围

- 任务启动时间：2026-06-10 04:30
- 报告文件：`docs/performance_ana/R_2606100430.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=7709`
  - `SEG_LINES=6767`
  - `LAST_LOG_TS=2026-06-10 01:17`

## 2. 执行摘要

**Trigram 路径接近历史最佳！均值 21ms（R224=29ms 被超越）。但全表扫描持续偏高。** 0/16 >500ms，Max=399ms，P50=27ms，lockWait=0，极佳比例 10/16。Trigram 8 路查询全部 <45ms——深夜 4:30 page cache 充裕，postings 区域完全预热。全表扫描总和 1070ms（↑24%——`path:/Users`=399ms，`content:import`=377ms）。

| 指标 | R234 | R235 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 346ms | **399ms** | ↑15%（全表扫描主导） |
| P50 | 74ms | **27ms** | **↓64%** |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 4/16 | **10/16** | **↑6** |
| tombstone | 2,094,570 (28.69%) | **2,100,602 (28.72%)** | ↑ |
| tombstone 增速 | ~289/min | **~201/min** | **↓30%（凌晨新低！）** |
| recordCount | 7,301,514 | **7,307,747** | +6.2K |
| liveRecordCount | 5,206,944 | **5,207,145** | +201 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 6037 | **6097** | +60 |
| COMPACTION | 2 | **2** | **不变——第 36 轮确认缺陷** |
| QUERYADV_GE500 | 60 | **60** | 不变 |

### Trigram 均值 21ms——超越 R224 成为历史最佳！

| Query | R234 | R235 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 147ms | **24ms** | **↓84%** | 极佳 |
| `hello world` | 226ms | **45ms** | **↓80%** | 极佳 |
| `*.swift` | 127ms | **21ms** | **↓83%** | 极佳 |
| `main.cpp` | 21ms | **5ms** | **↓76%** | 极佳 |
| `abc def...` | 131ms | **27ms** | **↓79%** | 极佳 |
| `README` | 34ms | **23ms** | ↓32% | 极佳 |
| `test` | 74ms | **21ms** | **↓72%** | 极佳 |
| `MacEverything` | 17ms | **5ms** | ↓71% | 极佳 |
| Trigram 均值 | 97ms | **21ms** | **↓78%（历史最佳！）** |

- **Trigram 均值 21ms——超越 R224 的 29ms 成为系列历史最佳！**
- **8/8 trigram 查询全部 <50ms（极佳）！此前最高 R224=6/8。**
- **`.pdf`=24ms（↓84%）——R232 lockWait=286.6ms 的阴影完全消散。**
- **`hello world`=45ms（↓80%）——postings 区域完全预热。**
- **凌晨 4:30 page cache 充裕、无 I/O 竞争——trigram postings 索引完全驻留内存。**

### Trigram 均值历史 Top 5

| 轮次 | Trigram 均值 | 时段 | 极佳 trigram 数 |
|------|----------:|------|------------:|
| **R235** | **21ms** | **凌晨 4:30** | **8/8** |
| R224 | 29ms | 深夜 23:00 | 6/8 |
| R226 | 83ms | 深夜 00:00 | 4/8 |
| R228 | 84ms | 凌晨 01:00 | 2/8 |
| R231 | 93ms | 凌晨 02:30 | 3/8 |

### 全表扫描——持续偏高（与 trigram 形成反差）

| Query | R234 | R235 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 249ms | **377ms** | ↑51%（偏高） |
| `path:/Users` | 346ms | **399ms** | ↑15%（偏高） |
| `中文测试` | 269ms | **294ms** | ↑9%（偏高） |
| **总和** | **865ms** | **1070ms** | **↑24%** |

- **全表扫描总和 865→1070ms（↑24%）——继续偏高。**
- **与 trigram 21ms 形成极端反差——trigram 依赖 postings 索引（小区域、完全预热），全表扫描依赖大 SoA 数组（大区域、page cache 压力大）。**
- **`content:import`=377ms（↑51%）——SoA fileSize 数组扫描。**
- **`path:/Users`=399ms——路径数组 ~7.3M records × 路径长度，page cache 需求大。**
- **本轮证明：trigram 路径和全表扫描路径的 page cache 竞争是零和博弈——trigram 极佳时全表扫描偏高。**

### 辅助路径

| Query | R234 | R235 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 3ms | **7ms** | ↑133% | 极佳 |
| `ext:cpp` | 61ms | **31ms** | ↓49% | 极佳 |
| `size:>100m` | 65ms | **80ms** | ↑23% | 正常 |
| `dm:today` | 47ms | **57ms** | ↑21% | 正常 |
| `verylongquery...` | 2ms | **0.4ms** | ↓80% | 极佳 |

- **`ext:cpp`=31ms（↓49%）——极佳。**
- **`size:>100m`=80ms（↑23%）——SoA fileSize 区域轻微波动。**

### tombstone 增速创新低

- **tombstone=2,100,602（28.72%）——持续攀升。**
- **增速 ~201/min——本会话新低！凌晨 4:30 文件活动几乎为零。**
- **recordCount +6.2K（本会话最低），liveRecordCount +201。**
- **增速衰减曲线：939→614→414→289→201/min。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 6.5 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 23.9 | 22.2 | advanced-trigram | 0 | 9860 | 极佳 |
| `main.cpp` | 5 | 5.3 | 3.0 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 20.8 | 18.8 | advanced-trigram | 0 | 9353 | 极佳 |
| `ext:cpp` | 5 | 31.3 | 30.4 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 80.0 | 43.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 44.6 | 43.8 | advanced-trigram | 0 | 3671 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 27.4 | 26.8 | advanced-trigram | 0 | 14458 | 极佳 |
| `README` | 5 | 23.4 | 20.3 | advanced-trigram | 0 | 17151 | 极佳 |
| `test` | 5 | 21.2 | 19.5 | advanced-trigram | 0 | 117136 | 极佳 |
| `MacEverything` | 5 | 4.7 | 0.9 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 57.1 | 56.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 377.0 | 357.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（偏高） |
| `path:/Users` | 5 | 398.8 | 386.7 | advanced-linear-gcd | 0 | 0 | 正常（偏高） |
| `中文测试` | 0 | 294.4 | 293.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 27.4ms，P90 = 377.0ms，Max = 398.8ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R234 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **10/16** | 0.4-45ms | R234=4/16（**↑6**） |
| **正常**（50-500ms） | 6/16 | 57-399ms | R234=12/16（↓6） |
| **超标**（>500ms） | **0/16** | — | R234=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,307,747，liveRecordCount=5,207,145（tombstone=**2,100,602**，**28.72%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=546  QUERYADV_GE500=60（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=6097
```

## 6. 问题清单

### P0 确认（第 36 轮）：compaction 触发机制缺陷

- **tombstone=2,100,602（28.72%）——超阈值 3.72%。**
- **36 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **连续 3 轮 lockWait=0。历史证据充分（R232=286.6ms 系列新纪录）。**

### 发现：Trigram vs 全表扫描 page cache 零和博弈

- **本轮证明 trigram 极佳（21ms）时全表扫描偏高（1070ms）。**
- **trigram postings 索引（小区域、热点集中）完全预热后，大 SoA 数组（fileSize、路径等）page cache 被挤压。**
- **两者竞争同一有限 page cache 空间——零和博弈。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（第 36 轮确认）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护（本轮进一步证实）

本轮的 trigram vs 全表扫描反差进一步证实 madvise MADV_WILLNEED 的价值——对 SoA 热区做主动维护可以缓解 page cache 零和博弈。

## 8. 下一轮重点

1. trigram 极佳状态是否持续。
2. 全表扫描是否回落（或 trigram 回升后全表扫描改善）。
3. tombstone 增速是否触底。

## 9. 结论

**Trigram 均值 21ms——超越 R224 成为系列历史最佳！8/8 trigram 全部极佳。** 0/16 >500ms，Max=399ms，P50=27ms，lockWait=0，极佳 10/16。凌晨 4:30 page cache 充裕，postings 区域完全预热。但全表扫描总和 1070ms（↑24%——`path:/Users`=399ms，`content:import`=377ms）——与 trigram 形成极端反差，证实 page cache 零和博弈。

**tombstone 28.72%（第 36 轮确认 compaction 缺陷）。** 增速降至 ~201/min（本会话新低——凌晨活动几乎为零）。COMPACTION=2 始终不变。
