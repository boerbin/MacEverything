# MacEverything 性能分析报告 R_2606101100

## 1. 本轮范围

- 任务启动时间：2026-06-10 11:00
- 报告文件：`docs/performance_ana/R_2606101100.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=8556`
  - `SEG_LINES=7614`
  - `LAST_LOG_TS=2026-06-10 07:47`

## 2. 执行摘要

**工作日活动冲击——全面回调至 R244 级别。** 0/16 >500ms，Max=306ms，P50=72ms，lockWait=0（连续 16 轮）。Trigram 均值 96ms（↑92%——page cache 大面积淘汰），全表扫描总和 783ms（↑50%）。极佳 4/16（↓2——R236 以来最低并列）。**`verylongquery`=32ms（通常 <3ms）——异常偏高，可能有短暂调度延迟或 overhead。** 预期下轮 1-round recovery。

| 指标 | R247 | R248 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 202ms | **306ms** | ↑51% |
| P50 | 62ms | **72ms** | ↑16% |
| lockWait max | 0ms | **0ms** | 完美（连续 16 轮） |
| 极佳比例(<50ms) | 6/16 | **4/16** | ↓2 |
| tombstone | 2,179,927 (29.51%) | **2,186,848 (29.57%)** | ↑ |
| tombstone 增速 | ~203/min | **~231/min** | ↑14%（回升） |
| recordCount | 7,389,479 | **7,396,582** | +7.1K |
| liveRecordCount | 5,209,552 | **5,209,734** | +182 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 6817 | **6877** | +60 |
| COMPACTION | 2 | **2** | **不变——第 49 轮确认缺陷** |
| QUERYADV_GE500 | 60 | **60** | 不变 |

### 工作日活动冲击

- **极佳 6→4/16（↓2）——与 R236=4/16 并列本会话近期最低。**
- **R236→R237：4/16→11/16 恢复。R244→R245：5/16→9/16 恢复。**
- **基于 12/12 次 1-round recovery 先例（100%），预期 R249 将回升。**

### `verylongquery`=32ms 异常

- **通常 <3ms（R240=1ms，R243=1ms，R245=0.2ms）。**
- **phase2=0ms——搜索本身瞬间完成，32ms 全部是 overhead。**
- **可能原因：短暂调度延迟或系统级 context switch。**
- **此前 R239 也出现过 12ms 异常——一次性事件，无持续性。**

### Trigram 路径——全面回调

| Query | R247 | R248 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 93ms | **176ms** | ↑89% | 正常 |
| `hello world` | 62ms | **280ms** | ↑352% | 正常 |
| `*.swift` | 87ms | **133ms** | ↑53% | 正常 |
| `main.cpp` | 10ms | **61ms** | ↑510% | 正常 |
| `abc def...` | 85ms | **56ms** | ↓34% | 正常 |
| `README` | 17ms | **22ms** | ↑29% | 极佳 |
| `test` | 23ms | **33ms** | ↑43% | 极佳 |
| `MacEverything` | 21ms | **8ms** | ↓62% | 极佳 |
| Trigram 均值 | 50ms | **96ms** | **↑92%** |

- **Trigram 均值 50→96ms（↑92%）——与 R244=99ms 接近。**
- **`hello world`=280ms（↑352%）——与 R244=295ms 接近（重复模式）。**
- **`main.cpp`=61ms（↑510%）——小 candidate 集也受影响（82 candidates）——系统级 page cache 清洗。**
- **3/8 trigram 极佳——仅 `README`、`test`、`MacEverything`（高频查询缓存效应）。**

### 全表扫描——同步回调

| Query | R247 | R248 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 175ms | **252ms** | ↑44% |
| `path:/Users` | 144ms | **225ms** | ↑56% |
| `中文测试` | 202ms | **306ms** | ↑51% |
| **总和** | **521ms** | **783ms** | **↑50%** |

- **全表扫描总和 521→783ms（↑50%）——三路同步回调。**
- **`中文测试`=306ms（↑51%——本轮 Max）。**
- **分布相对均匀（225-306ms）——非单点极端。**

### 辅助路径——回调

| Query | R247 | R248 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 1ms | **3ms** | ↑200% | 极佳 |
| `ext:cpp` | 38ms | **72ms** | ↑89% | 正常 |
| `size:>100m` | 85ms | **166ms** | ↑95% | 正常 |
| `dm:today` | 76ms | **41ms** | ↓46% | 极佳 |
| `verylongquery...` | 0.1ms | **32ms** | ↑异常（overhead） | 极佳 |

- **`size:>100m`=166ms（↑95%）——SoA size 区域受影响。**
- **`dm:today`=41ms（↓46%）——反向改善（page cache 重分配）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 3.2 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 175.7 | 169.7 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 61.2 | 56.2 | advanced-trigram | 0 | 82 | 正常 |
| `*.swift` | 5 | 133.4 | 130.5 | advanced-trigram | 0 | 9353 | 正常 |
| `ext:cpp` | 5 | 72.0 | 70.7 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 165.9 | 164.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 280.0 | 277.2 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 56.0 | 55.2 | advanced-trigram | 0 | 14473 | 正常 |
| `README` | 5 | 21.7 | 16.9 | advanced-trigram | 0 | 17151 | 极佳 |
| `test` | 5 | 32.9 | 29.9 | advanced-trigram | 0 | 117136 | 极佳 |
| `MacEverything` | 5 | 8.0 | 0.8 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 41.3 | 41.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 252.1 | 243.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 224.9 | 211.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 306.1 | 305.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 32.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳（异常偏高） |

**P50 = 72.0ms，P90 = 252.1ms，Max = 306.1ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R247 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **4/16** | 3-41ms | R247=6/16（↓2） |
| **正常**（50-500ms） | 12/16 | 56-306ms | R247=10/16（↑2） |
| **超标**（>500ms） | **0/16** | — | R247=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,396,582，liveRecordCount=5,209,734（tombstone=**2,186,848**，**29.57%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=600  QUERYADV_GE500=60（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=6877
```

## 6. 问题清单

### P0 确认（第 49 轮）：compaction 触发机制缺陷

- **tombstone=2,186,848（29.57%）——超阈值 4.57%，持续攀升。**
- **49 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **连续 16 轮 lockWait=0。历史证据充分。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（第 49 轮确认——即将第 50 轮里程碑）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 1-round recovery 是否再次确认（第 13 次）。
2. 第 50 轮 compaction 缺陷里程碑。

## 9. 结论

**工作日活动冲击——极佳 4/16（↓2），trigram 96ms（↑92%），全表扫描 783ms（↑50%）。** 0/16 >500ms，Max=306ms，P50=72ms，lockWait=0（连续 16 轮）。与 R244 模式一致——工作日系统活动大面积淘汰 page cache。`verylongquery`=32ms（overhead 异常——一次性调度延迟）。基于 12/12 次先例，预期 R249 1-round recovery。

**tombstone 29.57%（2,186,848，第 49 轮确认 compaction 缺陷——下轮将达第 50 轮里程碑）。** 增速 ~231/min（↑14%——回升）。COMPACTION=2 始终不变。
