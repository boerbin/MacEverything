# MacEverything 性能分析报告 R_2606081616

## 1. 本轮范围

- 任务启动时间：2026-06-08 16:16
- 报告文件：`docs/performance_ana/R_2606081616.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=4943`
  - `SEG_LINES=4001`
  - `LAST_LOG_TS=2026-06-08 16:47`

## 2. 执行摘要

**线性路径恢复、trigram 再降温、tombstone 急剧逼近阈值。** 本轮 0/16 >500ms，Max=355ms，P50=100ms，lockWait=0，极佳比例 4/16。`path:/Users` 从 R188 的 688ms 恢复到 249ms（↓64%），全表扫描总和从 1176ms 降到 681ms（↓42%）。但 trigram 均值从 133ms 回升至 168ms（↑26%），`.pdf`=355ms、`hello world`=326ms，说明 postings 区域再次被 page cache 压力影响。最大风险转为 tombstone：23.86%，增速约 790/min，若持续，25% 阈值只剩约 2-3 小时量级。

### 关键指标对比

| 指标 | R188 | R189 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 1/16 | **0/16** | **↓1** |
| Max | 688ms | **355ms** | **↓48%** |
| P50 | 107ms | **100ms** | ↓7% |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 3/16 | **4/16** | ↑1 |
| tombstone | 1,583,193 (23.63%) | **1,606,881 (23.86%)** | **↑23.7K** |
| tombstone 增速 | ~394/min | **~790/min** | **↑100%（严重）** |
| recordCount | 6,699,849 | **6,733,295** | +33.4K（异常偏高） |
| liveRecordCount | 5,116,656 | **5,126,414** | +9.8K |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3628 | **3688** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 20 | **20** | 不变（本轮 0 新增） |

### 路径压力从 linear 转回 trigram

| 路径类别 | R188 | R189 | 变化 | 结论 |
|---------|-----:|-----:|------|------|
| Trigram 均值 | 133ms | **168ms** | ↑26% | 再次降温 |
| 全表扫描总和 | 1176ms | **681ms** | ↓42% | 明显恢复 |
| `path:/Users` | 688ms | **249ms** | ↓64% | linear-gcd 恢复 |
| Max | 688ms | **355ms** | ↓48% | 风险降低 |

- **R188 的 `path:/Users` 线性路径异常已恢复。**
- **本轮压力重新集中在 trigram postings 区域。**
- **这是午后 page cache 冷热区域继续迁移的表现：不同 mmap/文件区域轮流被系统 I/O 淘汰。**

### Trigram 路径——再次变冷

| Query | R188 | R189 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 190ms | **355ms** | ↑87% | 冷态 |
| `hello world` | 252ms | **326ms** | ↑29% | 冷态 |
| `*.swift` | 126ms | **195ms** | ↑54% | 冷态 |
| `main.cpp` | 27ms | **47ms** | ↑77% | 极佳边缘 |
| `abc def...` | 259ms | **146ms** | ↓44% | 恢复 |
| `README` | 76ms | **71ms** | ↓7% | 正常 |
| `test` | 107ms | **172ms** | ↑61% | 冷态 |
| `MacEverything` | 28ms | **29ms** | 持平 | 极佳 |
| Trigram 均值 | 133ms | **168ms** | ↑26% | 再次变冷 |

- **`.pdf`=355ms、`hello world`=326ms 是本轮主要慢项。**
- **`abc def...` 从 259ms 恢复到 146ms，说明不是全局 trigram 文件冷态，而是 postings 区域冷热不均。**
- **`MacEverything` 仍保持 29ms，证明部分高频 postings 区域仍在热页中。**

### 全表扫描——从 R188 异常恢复

| Query | R188 | R189 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 194ms | **195ms** | 持平 |
| `path:/Users` | 688ms | **249ms** | **↓64%** |
| `中文测试` | 294ms | **236ms** | ↓20% |
| **总和** | **1176ms** | **681ms** | **↓42%** |

- **`path:/Users` 已从 >500ms 恢复到正常范围。**
- **全表扫描总和 681ms，回到日间正常范围。**
- **R188 的 linear-gcd 路径数组压力确认为短期 page cache 波动。**

### SoA Filter——正常偏高

| Query | R188 | R189 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 74ms | **100ms** | ↑35% |
| `dm:today` | 77ms | **77ms** | 持平 |

- **`size:>100m` 接近 100ms，SoA 区域受到轻度 page cache 压力。**
- **仍在正常范围内。**

### tombstone 23.86%——临近 compaction 阈值

- **tombstone=1,606,881，占 23.86%。**
- **本轮新增 tombstone 23,688，约 790/min，较 R188 翻倍。**
- **recordCount +33.4K，liveRecordCount +9.8K，说明文件系统活动非常密集，且包含大量替换/删除。**
- **距离 25% 阈值约 1.14 个百分点；若 790/min 持续，约 2-3 小时可能触达。**
- **这是当前最高优先级风险。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 24.4 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 354.5 | 336.7 | advanced-trigram | 0 | 9842 | 冷态 |
| `main.cpp` | 5 | 47.1 | 31.5 | advanced-trigram | 0 | 82 | 极佳边缘 |
| `*.swift` | 5 | 195.1 | 179.0 | advanced-trigram | 0 | 9345 | 冷态 |
| `ext:cpp` | 5 | 79.8 | 77.0 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 99.9 | 99.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常偏高 |
| `hello world` | 5 | 326.3 | 308.6 | advanced-trigram | 0 | 3638 | 冷态 |
| `abc def ghi jkl mno pqr` | 0 | 146.2 | 144.5 | advanced-trigram | 0 | 14281 | 正常 |
| `README` | 5 | 70.6 | 61.6 | advanced-trigram | 0 | 16689 | 正常 |
| `test` | 5 | 171.7 | 149.2 | advanced-trigram | 0 | 116584 | 冷态 |
| `MacEverything` | 5 | 28.8 | 1.9 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 76.7 | 75.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 195.2 | 187.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 249.2 | 242.2 | advanced-linear-gcd | 0 | 0 | 正常（恢复） |
| `中文测试` | 0 | 236.4 | 233.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 1.6 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 99.9ms，P90 = 249.2ms，Max = 354.5ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R188 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 4/16 | 1.6-47ms | R188=3/16（↑1） |
| **正常**（50-500ms） | 12/16 | 71-355ms | R188=12/16（持平） |
| **超标**（>500ms） | **0/16** | — | R188=1/16（↓1） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,733,295，liveRecordCount=5,126,414（tombstone=**1,606,881**，**23.86%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=235  QUERYADV_GE500=20（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3688
```

- **本轮 0 新增 >500ms 查询。**
- **SKIPFLUSH +60——正常节奏。**
- **无新 FLUSH、无新 COMPACTION。**

## 6. 问题清单

### P0 风险：tombstone 逼近 25% 阈值

- **23.86%，距 25% 仅约 1.14 个百分点。**
- **本轮增速约 790/min，为近期最高。**
- **如果持续，可能在 2-3 小时内触发 compaction 阈值。**
- **此前已观察到 compaction rewrite 崩溃风险，因此这是当前最紧急事项。**

### 观察项：page cache 压力从 linear 转移到 trigram

- **R188 的 `path:/Users` 已恢复；本轮慢项集中在 `.pdf`、`hello world`、`*.swift`、`test`。**
- **说明午后系统 I/O 对不同 mmap 区域的淘汰在持续迁移。**
- **短期内无功能风险，但会造成 100-350ms 的体验波动。**

## 7. 优化建议

### P0：立即排查 compaction rewrite 崩溃根因

- **tombstone 23.86%，按本轮速率 25% 阈值已进入小时级倒计时。**
- **建议优先处理 compaction 的 rewrite crash，至少先确认触发 compaction 时不会导致索引不可用。**

### P1：flush rewrite 改为 COW 方式（持续建议）

- 本轮 lockWait=0，但 R176/R179/R184 已证明锁竞争会在压力场景出现。

### P2：热区维护策略（持续建议）

- `.pdf`、`hello world`、`*.swift` 反复成为冷态慢项，应纳入热区维护或主动预热名单。

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. tombstone 是否继续以 >700/min 增长，是否接近或超过 24%。
2. 是否出现 compaction 相关日志或崩溃迹象。
3. trigram 慢项是否从 `.pdf`/`hello world` 恢复。

## 9. 结论

**R189 运行稳定但 tombstone 风险急剧上升。** 查询侧 0/16 >500ms，`path:/Users` 从 688ms 恢复至 249ms，P50 小幅改善至 100ms；但 trigram postings 再次变冷，`.pdf`=355ms、`hello world`=326ms。整体属于正常午后 page cache 波动。

**真正的 P0 是 tombstone：23.86%，增速约 790/min，25% 阈值可能进入 2-3 小时倒计时。** 如果 compaction rewrite 崩溃问题仍未解决，应立即优先处理或准备安全降级方案。
