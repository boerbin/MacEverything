# MacEverything 性能分析报告 R_2606100130

## 1. 本轮范围

- 任务启动时间：2026-06-10 01:30
- 报告文件：`docs/performance_ana/R_2606100130.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=7306`
  - `SEG_LINES=6364`
  - `LAST_LOG_TS=2026-06-09 22:17`

## 2. 执行摘要

**lockWait 再次出现——3/16 查询有锁竞争，`hello world` lockWait=46.2ms（系列第 3 高）。** 0/16 >500ms，Max=381ms，P50=165ms，极佳比例 3/16。整体偏高但全部 <500ms。**tombstone 突破 2M 里程碑：2,009,885（27.87%）！**

| 指标 | R228 | R229 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 170ms | **381ms** | ↑124% |
| P50 | 64ms | **165ms** | ↑158% |
| lockWait max | 0ms | **46.2ms** | **↑∞（系列第 3 高！）** |
| 极佳比例(<50ms) | 7/16 | **3/16** | ↓4 |
| tombstone | 1,997,938 (27.68%) | **2,009,885 (27.87%)** | **↑——突破 2M！** |
| tombstone 增速 | ~110/min | ~398/min | ↑262%（回升） |
| recordCount | 7,196,236 | **7,210,222** | +14.0K |
| liveRecordCount | 5,198,298 | **5,200,337** | +2.0K |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5709 | **5738** | +29 |
| COMPACTION | 2 | **2** | **不变——第 30 轮确认缺陷（里程碑！）** |
| QUERYADV_GE500 | 60 | **60** | 不变 |

### tombstone 突破 2M——里程碑！

- **tombstone=2,009,885（27.87%）——首次突破 2M！**
- **从首次超 25%（R200）到突破 2M（R229）历经 30 轮。**
- **增速回升至 ~398/min——凌晨有后台活动。**

### tombstone 里程碑全景

| 里程碑 | 轮次 | tombstone | tombstone% |
|--------|------|----------:|----------:|
| 首次超 25% | R200 | ~1,748K | 25.15% |
| 首次超 26% | R212 | ~1,841K | 26.14% |
| 首次超 27% | R220 | ~1,929K | 27.08% |
| **首次超 2M** | **R229** | **2,009,885** | **27.87%** |

### lockWait=46.2ms——系列第 3 高

| 轮次 | lockWait max | 触发查询 | 时段 |
|------|------------:|---------|------|
| R217 | **202.6ms** | `.pdf` | 午后 |
| R227 | **65.3ms** | `中文测试` | 深夜 |
| **R229** | **46.2ms** | **`hello world`** | **凌晨** |
| R215 | 18.2ms | `.pdf` | 午后 |

- **凌晨 1:30 出现锁竞争——3/16 查询有 lockWait。**
- **`hello world`=46.2ms，`abc def`=4.1ms，`main.cpp`=1.7ms。**
- **lockWait 事件现已覆盖午后、深夜、凌晨——不再是特定时段问题。**
- **进一步强化 COW flush P0 优先级。**

### lockWait 分布

| Query | lockWait | queryTime | phase2 | overhead |
|-------|--------:|----------:|-------:|---------:|
| `hello world` | **46.2ms** | 328ms | 275ms | 7ms |
| `abc def...` | 4.1ms | 302ms | 296ms | 2ms |
| `main.cpp` | 1.7ms | 105ms | 94ms | 9ms |
| 其余 13 查询 | 0ms | — | — | — |

### Trigram 路径——偏高

| Query | R228 | R229 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 89ms | **166ms** | ↑87% | 正常 |
| `hello world` | 170ms | **328ms** | ↑93%（lockWait=46.2ms） | 正常 |
| `*.swift` | 64ms | **214ms** | ↑234% | 正常 |
| `main.cpp` | 26ms | **105ms** | ↑304%（lockWait=1.7ms） | 正常 |
| `abc def...` | 97ms | **302ms** | ↑211%（lockWait=4.1ms） | 正常 |
| `README` | 66ms | **51ms** | ↓23% | 正常 |
| `test` | 137ms | **126ms** | ↓8% | 正常 |
| `MacEverything` | 20ms | **11ms** | ↓45% | 极佳 |
| Trigram 均值 | 84ms | **163ms** | **↑94%** | 偏高 |

- **Trigram 均值 84→163ms（↑94%）——偏高但全部 <330ms。**
- **`abc def`=302ms（↑211%）——lockWait + postings 区域波动。**

### 全表扫描——偏高

| Query | R228 | R229 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 89ms | **183ms** | ↑106% |
| `path:/Users` | 109ms | **381ms** | ↑250% |
| `中文测试` | 60ms | **194ms** | ↑223% |
| **总和** | **258ms** | **758ms** | **↑194%** |

- **全表扫描总和 258→758ms（↑194%）——偏高。**
- **`path:/Users`=381ms——路径数组波动（但远低于 R227=720ms）。**

### 辅助路径

| Query | R228 | R229 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 2ms | **2ms** | 持平 | 极佳 |
| `ext:cpp` | 34ms | **175ms** | ↑415% | 正常 |
| `size:>100m` | 35ms | **162ms** | ↑363% | 正常 |
| `dm:today` | 18ms | **61ms** | ↑239% | 正常 |
| `verylongquery...` | 0.5ms | **0.2ms** | ↓60% | 极佳 |

- **`ext:cpp`=175ms（↑415%）——ext-index 区域 page cache 波动。**
- **`size:>100m`=162ms（↑363%）——SoA fileSize 区域同步波动。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 2.1 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 165.7 | 162.6 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 105.2 | 93.6 | advanced-trigram | **1.7** | 82 | 正常 |
| `*.swift` | 5 | 213.7 | 205.8 | advanced-trigram | 0 | 9346 | 正常 |
| `ext:cpp` | 5 | 175.1 | 174.5 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 162.2 | 145.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 328.4 | 275.1 | advanced-trigram | **46.2** | 3671 | 正常（lockWait!） |
| `abc def ghi jkl mno pqr` | 0 | 302.0 | 296.0 | advanced-trigram | **4.1** | 14410 | 正常 |
| `README` | 5 | 50.7 | 46.3 | advanced-trigram | 0 | 17138 | 正常 |
| `test` | 5 | 125.5 | 108.7 | advanced-trigram | 0 | 117097 | 正常 |
| `MacEverything` | 5 | 11.4 | 0.6 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 61.4 | 58.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 183.1 | 173.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 380.9 | 367.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 193.8 | 187.0 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 165.7ms，P90 = 328.4ms，Max = 380.9ms。0/16 查询 >500ms。lockWait max = 46.2ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R228 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **3/16** | 0.2-11ms | R228=7/16（↓4） |
| **正常**（50-500ms） | 13/16 | 51-381ms | R228=9/16（↑4） |
| **超标**（>500ms） | **0/16** | — | R228=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,210,222，liveRecordCount=5,200,337（tombstone=**2,009,885**，**27.87%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=509  QUERYADV_GE500=60（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5738
```

## 6. 问题清单

### P0 确认（第 30 轮——里程碑！tombstone 突破 2M）：compaction 触发机制缺陷

- **tombstone=2,009,885（27.87%）——首次突破 2M！超阈值 2.87%。**
- **30 轮连续确认——里程碑。**

### P0：flush 写锁阻塞——全时段出现

- **lockWait 事件覆盖午后（R215/R217）、深夜（R227）、凌晨（R229）——全时段问题。**
- **系列 Top 3：202.6ms、65.3ms、46.2ms。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 30 轮确认——tombstone 突破 2M）

### P0：flush rewrite 改为 COW 方式（全时段 lockWait 证据）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. lockWait 是否消退。
2. tombstone 增长趋势（已突破 2M）。

## 9. 结论

**lockWait=46.2ms（系列第 3 高）+ 整体偏高——0/16 >500ms，Max=381ms，P50=165ms，极佳 3/16。** 3/16 查询出现锁竞争（`hello world`=46.2ms，`abc def`=4.1ms，`main.cpp`=1.7ms）。**lockWait 事件现已覆盖午后、深夜、凌晨——全时段问题，进一步强化 COW flush P0 优先级。**

**tombstone 突破 2M 里程碑！2,009,885（27.87%，第 30 轮确认 compaction 缺陷）。** 从 R200 首次超 25% 到 R229 突破 2M——30 轮。增速回升至 398/min。COMPACTION=2 始终不变。
