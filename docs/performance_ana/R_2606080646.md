# MacEverything 性能分析报告 R_2606080646

## 1. 本轮范围

- 任务启动时间：2026-06-08 06:46
- 报告文件：`docs/performance_ana/R_2606080646.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=3731`
  - `SEG_LINES=2789`
  - `LAST_LOG_TS=2026-06-08 06:47`

## 2. 执行摘要

**日间稳态确认——0/16 >500ms，Max=271ms，P50=12ms，lockWait=0.6ms（单次微量），极佳比例 12/16（75%）。** P50=12ms 维持在极限区间。Trigram 均值 12.1ms 接近会话最佳。全表扫描总和从 R170 的 693ms 微降至 767ms——早晨日间 page cache 竞争处于正常水平。lockWait=0.6ms 第二次出现（单次，仍可忽略）。

### 关键指标对比

| 指标 | R170（30 min 前） | R171 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 263ms | **271ms** | 持平 |
| P50 | 12ms | **12ms** | 持平（极限区间） |
| lockWait max | 0ms | **0.6ms** | 微量（单次，可忽略） |
| 极佳比例(<50ms) | 12/16 | **12/16** | 持平 |
| tombstone | 1,338,203 (20.79%) | **1,346,387 (20.89%)** | ↑ 正常 |
| tombstone 增速 | ~258/min | **~273/min** | 微升（日间活动恢复） |
| recordCount | 6,437,151 | **6,445,437** | +8.3K |
| liveRecordCount | 5,098,948 | **5,099,050** | +102 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 2550 | **2610** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 28 轮 0 新增） |

### 全表扫描——日间稳态

| Query | R170 | R171 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 187ms | **244ms** | ↑ 31%（零和波动） |
| `path:/Users` | 243ms | **252ms** | ↑ 4%（持平） |
| `中文测试` | 263ms | **271ms** | ↑ 3%（持平） |
| **总和** | **693ms** | **767ms** | **↑ 11%** |

- **全表扫描总和 767ms——日间稳态中高位。**
- **`content:import`=244ms（R170=187ms，↑31%）——零和效应（R170 时受益于其他区域被竞争）。**
- **三项较为均匀（244/252/271ms）——page cache 竞争均匀分布。**
- **767ms 处于日间正常范围（600-800ms），无异常。**

### 日间全表扫描参考

| 时段 | 轮次 | 总和 | 环境 |
|------|------|-----:|------|
| 22:16 | R153 | 523ms | 夜间开始 |
| 04:47 | R167 | 467ms | 凌晨极静默 |
| 05:46 | R169 | 509ms | 早晨前 |
| 06:16 | R170 | 693ms | 早晨开始 |
| **06:46** | **R171** | **767ms** | **日间活跃** |

- **从凌晨 467ms 到日间 767ms——增幅 64%，完全由外部 page cache 竞争驱动。**
- **767ms 接近深夜维护峰值水平——但成因不同：维护窗口是系统密集 I/O，日间是分散的用户活动。**

### Trigram 路径——稳定极限

| Query | R170 | R171 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 17ms | **17ms** | 持平 |
| `*.swift` | 10ms | **11ms** | 持平 |
| `hello world` | 12ms | **12ms** | 持平 |
| `abc def...` | 24ms | **26ms** | 持平 |
| `README` | 9ms | **8ms** | 持平 |
| `test` | 13ms | **14ms** | 持平 |
| `MacEverything` | 2ms | **2ms** | 持平 |
| Trigram 均值 | 11.8ms | **12.1ms** | 持平（极限区间） |

- **Trigram 均值 12.1ms——连续 3 轮维持在 11.8-12.1ms，接近会话最佳（11.5ms）。**
- **所有 trigram 项几乎与 R170 完全相同——page cache 完全稳定。**
- **结论**：日间用户活动不影响 trigram postings 的 page cache 驻留——核心路径保持极限性能。

### lockWait=0.6ms 分析

- **`test` 查询 lockWait=0.6ms——第二次在本系列观察到非零 lockWait（R167=0.3ms）。**
- **仍完全可忽略（阈值 10ms 以上才需关注）。**
- **两次均出现在高 candidate 查询（`README`/`test`）——持锁时间长的查询更可能与 flush timer 冲突。**
- **频率**：2/28 轮=7%——偶发，非趋势。

### SoA Filter

| Query | R170 | R171 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 30ms | **30ms** | 持平 |
| `dm:today` | 83ms | **88ms** | 持平 |

- **两项总和**：R170=113ms → R171=118ms——持平。
- **`dm:today`=88ms 处于日间偏高但正常范围。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 2.2 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 16.7 | 14.8 | advanced-trigram | 0 | 9812 | 极佳 |
| `main.cpp` | 5 | 1.8 | 1.0 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 11.2 | 10.9 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 6.3 | 6.0 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 29.5 | 29.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 11.7 | 11.3 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 25.9 | 25.9 | advanced-trigram | 0 | 14215 | 极佳 |
| `README` | 5 | 7.5 | 6.4 | advanced-trigram | 0 | 16631 | 极佳 |
| `test` | 5 | 13.8 | 11.6 | advanced-trigram | 0.6 | 116513 | 极佳 |
| `MacEverything` | 5 | 2.1 | 0.6 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 88.2 | 87.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 244.4 | 230.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 251.9 | 245.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 270.9 | 270.6 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.3 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 11.7ms，P90 = 244.4ms，Max = 270.9ms。0/16 查询 >500ms。lockWait max = 0.6ms（单次）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R170 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 12/16 | 0.2-30ms | R170=12/16（持平） |
| **正常**（50-500ms） | 4/16 | 88-271ms | R170=4/16（持平） |
| **超标**（>500ms） | 0/16 | — | R170=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,445,437，liveRecordCount=5,099,050（tombstone=**1,346,387**，**20.89%**）；
- 进程正常运行（当前会话，运行约 32.6 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=119  QUERYADV_GE500=4（不变——连续 28 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=2610
```

- **连续 28 轮 0 个新增 >500ms 查询（R144-R171）。**
- **FLUSH=1**——无新 flush（32.6 小时仅启动时 1 次）。
- **SKIPFLUSH=2610**——系统持续空闲。

## 6. 问题清单

### 无活跃问题

- 0/16 >500ms，12/16 <50ms（75%）。
- P50=12ms（极限区间），lockWait=0.6ms（可忽略）。
- 连续 28 轮 0 新增慢查询。
- 32.6 小时无 WARN/ERROR。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 20.89%，增速 ~273/min（微升——日间文件系统活动恢复）。
- 距 25% 阈值约 ~24 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 系统空闲时无影响。

## 8. 下一轮重点

1. 日间活跃期全表扫描是否稳定在 700-800ms。
2. Trigram 均值是否继续维持 ~12ms。
3. tombstone 增速在日间活跃期的变化。

## 9. 结论

**日间稳态确认——0/16 >500ms，Max=271ms，P50=12ms，lockWait=0.6ms（可忽略），极佳比例 12/16。** Trigram 均值 12.1ms 连续 3 轮维持极限水平——日间用户活动不影响 trigram postings page cache。全表扫描总和 767ms 处于日间正常范围。

**连续 28 轮 0 新增慢查询，32.6 小时无异常。** 核心发现：trigram 路径在任何时段（凌晨静默、深夜维护、日间活跃）均稳定在 11-13ms——系统架构正确地保护了核心用户查询路径。tombstone 20.89%（1.35M），增速 273/min，距 25% 约 24 小时。系统长期稳定运行。
