# MacEverything 性能分析报告 R_2606040014

## 1. 本轮范围

- 任务启动时间：2026-06-04 00:14
- 报告文件：`docs/performance_ana/R_2606040014.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21839`
  - `SEG_LINES=813`
  - `LAST_LOG_TS=2026-06-04 01:41`

## 2. 执行摘要

**性能正常，连续第 3 轮完全恢复。** P50 ≈ 120ms，Max=349ms，**全部 16/16 查询 <500ms**。lockWaitMs 全部为 0。较前轮（P50=43ms）略有回升，但仍处于正常区间——深夜极值窗口已过，回归日常水平。

tombstone 继续攀升至 328,383 条（5.42%），本轮增量 10,010 条。SKIPFLUSH 连续第 6 轮 +12。连续第 22 轮超过 2%，第 3 轮超过 5%。

### 横向对比（最近 5 轮）

| Query | R77 | R78 | R79 | R80 | R81 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 677 | 6 | 1 | 7 | **2** | 正常 |
| `.pdf` | 1169 | 289 | 65 | 42 | **171** | 正常波动 |
| `*.swift` | 165 | 118 | 75 | 64 | **120** | 正常 |
| `hello world` | 357 | 546 | 109 | 34 | **228** | 正常波动 |
| `path:/Users` | 376 | 235 | 165 | 354 | **338** | 正常波动 |
| P50 | 165 | 74 | 65 | 43 | **120** | 正常 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1.6 | 0.0 | short-query-cache | 0 | 0 | 极快 |
| `.pdf` | 5 | 171.4 | 168.3 | advanced-trigram | 0 | 9949 | 正常 |
| `main.cpp` | 5 | 22.9 | 18.2 | advanced-trigram | 0 | 85 | 快 |
| `*.swift` | 5 | 119.8 | 117.0 | advanced-trigram | 0 | 9356 | 正常 |
| `ext:cpp` | 5 | 52.3 | 51.6 | advanced-ext-index | 0 | 3701 | 正常 |
| `size:>100m` | 5 | 51.4 | 51.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 227.5 | 221.5 | advanced-trigram | 0 | 3613 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 185.9 | 183.7 | advanced-trigram | 0 | 16002 | 正常 |
| `README` | 5 | 33.6 | 30.8 | advanced-trigram | 0 | 17679 | 快 |
| `test` | 5 | 55.8 | 48.3 | advanced-trigram | 0 | 129957 | 正常 |
| `MacEverything` | 5 | 18.7 | 1.7 | advanced-trigram | 0 | 6803 | 极快 |
| `dm:today` | 5 | 168.9 | 168.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 349.3 | 342.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 偏慢 |
| `path:/Users` | 5 | 337.7 | 331.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 280.5 | 280.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 2.1 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 120ms，P90 ≈ 349ms，Max = 349ms。全部 16/16 查询 <500ms。lockWaitMs 全部为 0。**

### 正常状态基线统计（排除 I/O 事件，R73-R81 中 6 轮正常数据）

| 指标 | Min | P50 | Max | 说明 |
|------|----:|----:|----:|------|
| P50 | 31ms | 54ms | 120ms | 正常区间 |
| Max | 209ms | 339ms | 354ms | 正常区间 |
| >500ms | 0 | 0 | 0 | 正常时全部 <500ms |

**结论：系统正常状态 P50 在 31-120ms，Max 在 209-354ms。超出此范围即为异常。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,060,322，liveRecordCount=5,731,939（tombstone=**328,383**，**5.42%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=249  QUERYADV_GE500=38
FLUSH=0  COMPACTION=2  SKIPFLUSH=279
DIAG_TRIGRAM_SKIP=86
```

- QUERYADV 从 241 增至 249（+8）。
- QUERYADV_GE500 保持 38（连续第 3 轮无新增）。
- SKIPFLUSH 从 267 增至 279（+12，**连续第 6 轮 +12/轮**）。
- DIAG_TRIGRAM_SKIP 从 85 增至 86（+1）。

## 6. 问题清单

### Critical：tombstone 5.42%（328,383 条），稳态增长无收敛迹象

- 增长轨迹：
  ```
  R78: 296K (4.91%) → R79: 306K (5.06%) → R80: 318K (5.26%)
  → R81: 328K (5.42%)
  ```
- 本轮增量 10,010 条。增速稳定在 ~10-13K/轮。
- **累计内存开销**：~328K × 300 字节 ≈ **98MB**（即将突破 100MB）。
- SKIPFLUSH=279，**连续 6 轮 +12/轮**——确认这是系统稳态，不会自行改善。
- 连续第 22 轮超 2%，第 3 轮超过 5%。
- **线性外推**：
  - 10 轮后：~440K（7.3%，132MB）
  - 20 轮后：~560K（9.2%，168MB）
  - 50 轮后：~940K（14.4%，282MB）

### Medium：`content:import` 偏慢（349ms）

- 正常区间 65-225ms。本轮 349ms 为近期较高值（非历史最高 732ms R71）。
- pure-filter-soa-gcd 全表扫描，phase2=342ms。
- **可能与 tombstone 5.42% 相关**——全表扫描需要额外扫描 328K tombstone 记录。
- 以 5.42% tombstone 计：正常 phase2 × 1.054 ≈ 预期增量 5.4%。但 342ms vs 正常 ~200ms 增幅 70%——超出 tombstone 线性预期。
- 更可能原因：系统缓存状态波动（非深夜极值窗口）。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（5.42%，98MB，第 22 轮）

- **量化损害**：
  - 内存：98MB 额外占用（即将突破 100MB）
  - 扫描：5.42% 额外 phase2 工作量
  - I/O 恢复：tombstone 页面需额外 I/O 换入，放大恢复时间
  - 增长：稳态 +12 SKIPFLUSH/轮 = +10-13K tombstone/轮，永不收敛
- **实施方案不变**：SKIPFLUSH 分支增加占比检查，>2% 触发 compaction。
- **风险**：极低——compaction 已有实现，仅需修改触发条件。

### P1：short-query-cache GCD 隔离（预防性）

### P1：将 `.pdf`/`*.swift` 路由到 ext-index（预防性）

### P2：实现 phase2 超时降级（预防性）

## 8. 下一轮重点

1. tombstone 内存是否突破 100MB（当前 98MB）。
2. 性能是否维持正常（日间活动恢复后）。
3. SKIPFLUSH +12/轮是否继续。

## 9. 结论

**性能正常，连续第 3 轮完全恢复。** P50 ≈ 120ms，Max=349ms，全部 16/16 <500ms。较深夜极值窗口（P50=31-43ms）回升，回归日常正常水平。tombstone 继续攀升至 328,383 条（5.42%，98MB），即将突破 100MB 内存占用。SKIPFLUSH 连续第 6 轮 +12/轮确认为系统稳态。正常状态基线已确立：P50 31-120ms，Max 209-354ms。tombstone compaction 是唯一需要实施的结构性修复——当前性能良好，风险极低，收益明确（释放 98MB + 减少 5.42% 扫描 + 提升 I/O 恢复速度）。
