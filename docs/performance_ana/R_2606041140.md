# MacEverything 性能分析报告 R_2606041140

## 1. 本轮范围

- 任务启动时间：2026-06-04 11:40
- 报告文件：`docs/performance_ana/R_2606041140.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=22054`
  - `SEG_LINES=1028`
  - `LAST_LOG_TS=2026-06-04 11:41`

## 2. 执行摘要

**I/O 压力加剧——R84-R86 恶化模式正在重演。** `README`=1,071ms（新受影响查询），`.pdf`=630ms（持续 >500ms 第 2 轮）。**2/16 查询 >500ms。lockWaitMs 最高 46.1ms。** P50 ≈ 171ms，Max=1,071ms。

**关键恶化信号**：
1. **>500ms 查询从 1/16 增至 2/16**——与 R84→R85 演化路径一致。
2. **lockWait 从 0 增至 46.1ms**——日间 FSWatcher 锁竞争显著上升。
3. **新查询受影响**：`README`（R92-R93 正常 14-27ms）本轮 1,071ms——trigram 索引页面被大量换出。

**tombstone=494,169（7.92%，148MB），增量 +11,866**——回到稳态增速，接近 8% 里程碑。

### 横向对比（最近 5 轮）

| Query | R90 | R91 | R92 | R93 | R94 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 3 | 12 | 5 | 75 | **11** | 正常 |
| `.pdf` | 54 | 192 | 820 | 635 | **630** | **持续 >500ms** |
| `*.swift` | 32 | 125 | 430 | 22 | **226** | 恶化中 |
| `hello world` | 62 | 273 | 412 | 352 | **240** | 改善 |
| `README` | 14 | 126 | 27 | 14 | **1071** | **异常飙升** |
| `path:/Users` | 65 | 314 | 222 | 144 | **218** | 正常 |
| P50 | 23 | 126 | 131 | 57 | **171** | **上升** |

### I/O 压力事件演化对比

| 阶段 | R84-R86 周期 | R92-R94 周期（当前） |
|------|-------------|-------------------|
| 轻度（第 1 轮） | R84: P50=86, Max=310, 0/16 >500 | R92: P50=131, Max=820, 1/16 >500 |
| **中度（第 2-3 轮）** | R85: P50=104, Max=1648, 1/16 >500 | **R94: P50=171, Max=1071, 2/16 >500** |
| 灾难（第 3-4 轮） | R86: P50=361, Max=2.7M, 5/16 >500 | **???** |

**R94 处于中度阶段**——与 R85 高度相似（Max 量级相当，lockWait 出现）。**如果模式继续，下一轮（R95）可能是灾难阶段。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 11.2 | 0.0 | short-query-cache | 0 | 0 | 正常 |
| `.pdf` | 5 | 629.9 | 620.4 | advanced-trigram | 0 | 9950 | **>500ms 连续第 2 轮** |
| `main.cpp` | 5 | 36.9 | 26.8 | advanced-trigram | 0 | 85 | 正常 |
| `*.swift` | 5 | 226.0 | 222.0 | advanced-trigram | 0 | 9356 | 恶化中 |
| `ext:cpp` | 5 | 96.5 | 94.9 | advanced-ext-index | 0 | 3701 | 正常 |
| `size:>100m` | 5 | 85.6 | 80.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（R93 偏高已恢复） |
| `hello world` | 5 | 239.8 | 235.6 | advanced-trigram | 0 | 3613 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 170.6 | 169.7 | advanced-trigram | 0 | 16031 | 正常 |
| `README` | 5 | 1070.8 | 765.7 | advanced-trigram | 0 | 17680 | **>500ms 异常飙升** |
| `test` | 5 | 182.0 | 73.0 | advanced-trigram | **46.1** | 130096 | **lockWait** |
| `MacEverything` | 5 | 73.6 | 15.2 | advanced-trigram | **32.2** | 6803 | **lockWait** |
| `dm:today` | 5 | 93.4 | 93.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 220.4 | 208.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 218.2 | 213.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 390.8 | 389.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 1.6 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 171ms，P90 ≈ 630ms，Max = 1,071ms。2/16 查询 >500ms。lockWaitMs 最高 46.1ms。**

### `README`=1,071ms 异常分析

- **历史数据**：R88=16ms, R89=54ms, R90=14ms, R91=126ms, R92=27ms, R93=14ms, **R94=1,071ms**。
- **正常范围 14-126ms → 突然 1,071ms**——这不是渐进恶化，是突发性缓存换出。
- candidates=17,680（较高），phase2Ms=765.7ms（phase2 占比 71%）——大量候选集需要扫描未缓存的索引页面。
- queryTimeMs - phase2Ms = 305ms——**非 phase2 部分也耗时异常**，表明 trigram 索引查找阶段本身也经历了大量页面故障。
- **对比 `.pdf`**：`.pdf` candidates=9,950, phase2=620ms；`README` candidates=17,680, phase2=766ms。`README` 候选集更大但 phase2 比例更高——因为 `README` 的 trigram 索引分布更广，涉及更多冷页面。

### lockWait 出现模式

| 查询 | lockWaitMs | 位置 | 说明 |
|------|----------:|------|------|
| `test` | 46.1ms | 电池第 10 位 | FSWatcher flush 竞争 |
| `MacEverything` | 32.2ms | 电池第 11 位 | 同上 |

- lockWait 出现在电池中后段（第 10-11 位）——**前面的 `.pdf` 和 `README` 长查询执行期间，FSWatcher 积累了需要 flush 的事件**。
- R91 lockWait=15.8ms → R92=2.2ms → R93=0 → **R94=46.1ms**——**日间 FSWatcher 活动显著增加**。
- lockWait 46ms 本身不严重，但它是 R86 灾难（lockWait=1,010ms）的前兆信号。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,233,237，liveRecordCount=5,739,068（tombstone=**494,169**，**7.92%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=335  QUERYADV_GE500=48
FLUSH=0  COMPACTION=2  SKIPFLUSH=392
DIAG_TRIGRAM_SKIP=102
```

- QUERYADV 从 326 增至 335（+9）。
- QUERYADV_GE500 从 46 增至 48（**+2，即 `.pdf`=630ms 和 `README`=1071ms**）。
- SKIPFLUSH 从 382 增至 392（**+10，略低于稳态 +12**）。
- DIAG_TRIGRAM_SKIP 从 101 增至 102（+1）。

### SKIPFLUSH +10 分析

- 稳态为 +12/轮。本轮 +10 略低。
- R92→R93 仅 +2（短间隔），本轮间隔恢复正常但增速略低——可能因 `README` 和 `.pdf` 长查询期间 FSWatcher 事件被锁阻塞延迟记录。
- 与 R86 灾难期的 +4 (+1) 不同——当前下降幅度很小，不构成异常。

### QUERYADV_GE500 +2 分析

- 连续 5 轮无新增（R87-R91）→ R92 +1 → R93 +1 → **R94 +2**——**加速中**。
- 48 次 >=500ms 查询中，R92-R94 贡献了 4 次——**3 轮贡献了近 9% 的历史慢查询**。

## 6. 问题清单

### Critical：I/O 压力事件正在加剧——R84-R86 模式重演中

- **R92→R93→R94 演化路径**：
  - R92：轻度（1/16 >500ms, lockWait=2.2ms）
  - R93：改善假象（1/16 >500ms, lockWait=0, 但 .pdf 仍 >500ms）
  - **R94：中度（2/16 >500ms, lockWait=46.1ms, 新查询受影响）**
- **与 R84→R85→R86 对比**：
  - R84→R85：1 轮内从轻度升至中度
  - R92→R94：2 轮内从轻度升至中度（R93 的热缓存效应掩盖了 1 轮恶化）
- **下一轮预警**：如果 `README` 或 `.pdf` 继续 >500ms 且 lockWait 进一步增加，R95 可能进入灾难阶段。

### Critical：tombstone 7.92%（494,169 条，148MB），逼近 8%

- 增长轨迹（近 5 轮，排除短间隔）：
  ```
  R91: 454K (+10.5K) → R92: 480K (+25.5K) → R94: 494K (+11.9K)
  ```
- 日间稳态增速 ~12K/轮，含加速窗口可达 25K/轮。
- **累计内存开销**：~494K × 300 字节 ≈ **148MB**。
- 连续第 34 轮超 2%，第 15 轮超过 5%，第 10 轮超过 6%，第 7 轮超过 7%。
- **预计 1-2 轮后突破 8%**。

### High：lockWait 46.1ms——日间 FSWatcher 竞争显著

- lockWait 轨迹（近 5 轮）：R91=15.8ms → R92=2.2ms → R93=0 → **R94=46.1ms**。
- 46.1ms 是 R91 以来的最高值（R86 灾难时为 1,010ms）。
- 日间用户活动（编辑、编译、保存等）产生大量 FSWatcher 事件，与长查询竞争锁资源。
- **lockWait + 长查询 = R86 灾难的两个必要条件——目前两者同时存在**。

### Medium：`README` 查询异常（14ms → 1,071ms）

- 正常范围 14-126ms，本轮 1,071ms——**76 倍偏差**。
- candidates=17,680 + 大量冷 trigram 索引页面 = 严重页面故障。
- 反映 tombstone 148MB 对页面缓存的持续挤压——之前被 `.pdf` 和 `*.swift` 暴露的问题正在扩散到更多查询。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（7.92%，148MB，灾难模式正在重演）

- **R84-R86 恶化模式正在重演**：轻度（R92）→ 中度（R94）→ 灾难（R95?）。
- **两个灾难必要条件同时满足**：lockWait=46ms + 长查询（README 1,071ms）。
- compaction 后释放 148MB → trigram 索引常驻 → 消除 I/O 压力事件根因。

### P0 紧急：`size:>100m` 全表扫描超时保护

- 本轮 `size:>100m`=86ms 正常，但 R86 已证明此路径可产生 45 分钟灾难。
- 如果 R95 进入灾难阶段且 `size:>100m` 遭遇，后果将比 R86 更严重（tombstone 从 6.68% 增至 7.92%）。

### P0：short-query-cache GCD 隔离
### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. **是否进入灾难阶段**：>500ms 查询是否增至 3/16+，lockWait 是否突破 100ms。
2. `README` 是否恢复或继续异常。
3. tombstone 是否突破 8%。

## 9. 结论

**I/O 压力加剧——R84-R86 恶化模式正在重演。** `README`=1,071ms（76 倍异常飙升），`.pdf`=630ms（连续第 2 轮 >500ms），2/16 查询 >500ms。lockWaitMs=46.1ms（R91 以来最高）。P50=171ms（R93 的 57ms 上升 3 倍）。**R92→R94 演化路径与 R84→R85 高度一致**——轻度→中度。R86 灾难的两个必要条件（长查询 + lockWait）同时存在。tombstone=494,169（7.92%，148MB），逼近 8% 里程碑。QUERYADV_GE500 加速增长（3 轮 +4）。**下一轮（R95）需高度警惕灾难阶段到来**。
