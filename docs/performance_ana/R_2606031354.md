# MacEverything 性能分析报告 R_2606031354

## 1. 本轮范围

- 任务启动时间：2026-06-03 13:54
- 报告文件：`docs/performance_ana/R_2606031354.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21424`
  - `SEG_LINES=398`
  - `LAST_LOG_TS=2026-06-03 17:43`

## 2. 执行摘要

服务正常运行（第 18 轮连续可用）。`x` 从前轮历史最严重异常（364ms）恢复正常（10.5ms），确认为偶发。性能中等（P50 ≈ 127ms，Max=359ms）。所有查询连续第 15 轮 < 500ms。

**tombstone 首次突破 3% 大关**：185,214 条（3.13%），连续第 7 轮超过 2%。本轮增量 16,072 条，为近 8 轮最大单轮增幅。

### 横向对比（最近 5 轮）

| Query | R62 | R63 | R64 | R65 | R66 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 93 | 6 | 2 | 364 | **11** | 恢复正常 |
| `.pdf` | 325 | 415 | 84 | 446 | **329** | 波动 |
| `*.swift` | 488 | 233 | 29 | 133 | **150** | 正常 |
| `hello world` | 297 | 274 | 120 | 222 | **359** | 偏慢 |
| `path:/Users` | 255 | 307 | 380 | 203 | **269** | 正常 |
| P50 | 148 | 106 | 34 | 120 | **127** | 正常 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 10.5 | 0.0 | short-query-cache | 0 | 0 | 恢复正常 |
| `.pdf` | 5 | 328.6 | 323.7 | advanced-trigram | 0 | 9945 | 正常 |
| `main.cpp` | 5 | 38.5 | 21.2 | advanced-trigram | 0 | 85 | 快 |
| `*.swift` | 5 | 150.2 | 145.7 | advanced-trigram | 0 | 9356 | 正常 |
| `ext:cpp` | 5 | 63.5 | 62.2 | advanced-ext-index | 0 | 3702 | 正常 |
| `size:>100m` | 5 | 58.5 | 58.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 359.2 | 356.0 | advanced-trigram | 0 | 3613 | 偏慢 |
| `abc def ghi jkl mno pqr` | 0 | 323.9 | 322.9 | advanced-trigram | 0 | 15987 | 偏慢 |
| `README` | 5 | 77.4 | 67.2 | advanced-trigram | 0 | 17717 | 正常 |
| `test` | 5 | 103.3 | 59.9 | advanced-trigram | 0 | 129962 | 正常 |
| `MacEverything` | 5 | 27.6 | 9.4 | advanced-trigram | 0 | 6802 | 快 |
| `dm:today` | 5 | 72.1 | 72.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 180.9 | 169.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 268.5 | 257.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 348.3 | 343.5 | advanced-linear-gcd | 0 | 0 | 偏慢 |
| `verylongquery...` | 0 | 3.6 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 127ms，P90 ≈ 348ms，Max = 359ms。全部查询连续第 15 轮 < 500ms。lockWaitMs 全部为 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,912,616，liveRecordCount=5,727,402（tombstone=**185,214**，**3.13%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=139  QUERYADV_GE500=5
FLUSH=0  COMPACTION=2  SKIPFLUSH=133
DIAG_TRIGRAM_SKIP=71
```

- QUERYADV 从 131 增至 139（+8）。
- QUERYADV_GE500 保持 5（本轮 Max=359ms）。
- SKIPFLUSH 从 120 增至 133（+13），恢复每轮 ~12 次增速。
- DIAG_TRIGRAM_SKIP 从 70 增至 71（+1）。

## 6. 问题清单

### Critical：tombstone 首次突破 3%（3.13%，185,214 条）

- 增长轨迹：
  ```
  R61: 138K (2.36%) → R62: 149K (2.54%) → R63: 158K (2.69%)
  → R64: 160K (2.71%) → R65: 169K (2.87%) → R66: 185K (3.13%)
  ```
- 本轮增量 16,072 条——**近 8 轮最大单轮增幅**（此前最高 14,180 条）。
- **累计内存开销**：~185K × 300 字节 ≈ 56MB 额外内存。
- **查询开销**：phase2 扫描 totalRecords 包含 185K tombstone（3.13% 额外扫描）。
- **加速趋势**：R64 增量仅 1.3K（异常低），但 R65=9.5K、R66=16K，恢复甚至超过正常增速。
- SKIPFLUSH=133，compaction 完全不可触发。
- **按当前速率（~12K/轮），约 5 轮后将达 4%。**

### High：`x` 短查询缓存异常频率升级

- 18 轮中 4 次异常（R58=254ms、R62=93ms、R65=364ms），异常率 22%。
- 本轮恢复正常（10.5ms），但趋势显示异常严重程度在升级（19→93→254→364ms）。
- 需要增加缓存路径诊断日志以定位根因。

### Medium：`hello world` 偏慢（359ms）

- 18 轮数据中 4 次 >300ms。candidates=3613，phase2=356ms。
- I/O 负载偏高时多词 trigram 查询容易退化。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（紧急——已突破 3%）

- tombstone 3.13%，增速恢复并加快。
- **最小实施方案**：在 SKIPFLUSH 分支增加一行 tombstone 占比检查：
  ```
  if (tombstoneRatio > 0.02) triggerCompaction();
  ```
- 复用现有 compaction 代码路径，零新增复杂度。
- **预期收益**：tombstone 从 185K 降至接近 0，节省 ~56MB 内存，减少 3.13% 的 phase2 扫描开销。

### P0：short-query-cache 诊断日志（承前轮，优先级已升至 P0）

### P0：补齐进程生命周期监控（承前轮）

### P1：将 `.xxx`/`*.xxx` 扩展名模式路由到 ext-index（承前轮）

## 8. 下一轮重点

1. tombstone 增速是否继续加快——16K/轮是异常还是新常态。
2. `x` 缓存异常是否在下一轮再次出现。
3. 整体性能是否维持在合理区间。

## 9. 结论

服务连续第 18 轮正常运行，所有查询连续第 15 轮 <500ms（Max=359ms）。`x` 从前轮历史最严重异常（364ms）恢复正常（10.5ms），但 18 轮中已出现 4 次异常且严重性逐步升级。**tombstone 首次突破 3% 大关（185,214 条，3.13%）**，本轮增量 16,072 条为近 8 轮最高，SKIPFLUSH=133，compaction 完全不可触发。按当前增速约 5 轮后将达 4%，修复紧迫性进一步升高。
