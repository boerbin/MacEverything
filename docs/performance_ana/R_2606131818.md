# MacEverything 性能分析报告 R_2606131818

## 1. 本轮范围

- 任务启动时间：2026-06-13 18:18
- 报告文件：`docs/performance_ana/R_2606131818.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=12287`
  - `SESSION_START_TS=2026-06-13 12:03:04.944`
  - `TOTAL_LINES=12808`
  - `SEG_LINES=522`
  - `LAST_LOG_TS=2026-06-13 18:18`

## 2. 执行摘要

**本轮 16-query battery 中 0/16 >500ms，但 Max 从 283.1ms 升至 365.0ms，最高项仍为 `中文测试`。** P50≈25.7ms，P90≈208.0ms，极佳查询数仍为 11/16；所有 lockWait 均为 0ms。`中文测试`=365.0ms、`path:/Users`=208.0ms、`content:import`=166.8ms 仍构成主要前台尾部。

相比 17:48，本轮 P90 从约 228.7ms 回落到约 208.0ms，但 Max 上升约 29%，主要由 `中文测试` 从 283.1ms 升至 365.0ms 驱动。pure-filter 略有改善：`dm:today` 从 94.0ms 降至 44.5ms，`content:import` 从 172.4ms 小降至 166.8ms；`path:/Users` 从 228.7ms 降至 208.0ms。后台结构性风险继续恶化：recordCount=6,274,360，liveRecordCount=5,314,564，tombstone=959,796（15.30%），较上一轮增加 7,018；最新 session `FLUSH=74`，`SKIPFLUSH=0`，最新 flush total=3361ms。

| 指标 | R_2606131748 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 283.1ms | **365.0ms** | ↑29% |
| P50 | ≈24.9ms | **≈25.7ms** | ↑3% |
| P90 | ≈228.7ms | **≈208.0ms** | ↓9% |
| lockWait max | 0.0ms | **0.0ms** | 持平 |
| 极佳比例(<50ms) | 11/16 | **11/16** | 持平 |
| 扫描/过滤路径总和 | 811.5ms | **818.3ms** | ↑1% |
| recordCount | 6,266,883 | **6,274,360** | +7,477 |
| liveRecordCount | 5,314,105 | **5,314,564** | +459 |
| tombstone | 952,778 (15.20%) | **959,796 (15.30%)** | +7,018 |
| 最新 session FLUSH | 68 | **74** | +6 |
| 最新 session QUERYADV_GE500 | 0 | **0** | 持平 |
| 最新 flush total | 4086ms | **3361ms** | ↓18% |
| session 最大 flush total | 9092ms | **9092ms** | 持平 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 0.2 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 29.4 | 24.4 | advanced-trigram | 0.0 | 10158 | 极佳 |
| `main.cpp` | 5 | 2.5 | 1.9 | advanced-trigram | 0.0 | 103 | 极佳 |
| `*.swift` | 5 | 22.1 | 21.5 | advanced-trigram | 0.0 | 9622 | 极佳 |
| `ext:cpp` | 5 | 14.7 | 14.1 | advanced-ext-index | 0.0 | 3891 | 极佳 |
| `size:>100m` | 5 | 34.0 | 33.9 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 55.9 | 55.3 | advanced-trigram | 0.0 | 3932 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 58.3 | 58.2 | advanced-trigram | 0.0 | 14624 | 正常 |
| `README` | 5 | 10.1 | 7.7 | advanced-trigram | 0.0 | 17589 | 极佳 |
| `test` | 5 | 16.1 | 12.2 | advanced-trigram | 0.0 | 118022 | 极佳 |
| `MacEverything` | 5 | 3.7 | 1.5 | advanced-trigram | 0.0 | 7040 | 极佳 |
| `dm:today` | 5 | 44.5 | 44.2 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳，较上一轮明显回落 |
| `content:import` | 5 | 166.8 | 151.5 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常高位 |
| `path:/Users` | 5 | 208.0 | 194.4 | advanced-linear-gcd | 0.0 | 0 | 正常高位 |
| `中文测试` | 0 | 365.0 | 361.7 | advanced-linear-gcd | 0.0 | 0 | 正常高位，本轮最高且明显抬升 |
| `verylongquery...` | 0 | 0.9 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈25.7ms，P90≈208.0ms，Max=365.0ms。0/16 查询 >500ms。lockWait max=0.0ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **11/16** | 0.2-44.5ms | short-query-cache、轻量 trigram、ext-index 与部分 pure-filter 健康 |
| 正常（50-500ms） | **5/16** | 55.9-365.0ms | trigram、pure-filter、linear-gcd 均有中高位项，linear-gcd 仍最高 |
| 超标（>500ms） | **0/16** | — | 本轮无正式超标查询 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=6,274,360；
- liveRecordCount=5,314,564；
- tombstone=959,796（**15.30%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=12287
SESSION_START_TS=2026-06-13 12:03:04.944
TOTAL_LINES=12808
SEG_LINES=522
QUERYADV=50
QUERYADV_GE500=0
FLUSH=74
SKIPFLUSH=0
COMPACTION=2
LAST_QUERYADV=[2026-06-13 18:18:10.084] [INFO ] [SearchEngine] QueryAdvanced "中文测试" total=362ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6274361
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 0，本轮没有日志级慢查询。`中文测试` 的日志 total=362ms，与 query battery 的 365.0ms 对齐；该项从上一轮 283.1ms 抬升到 365.0ms，是本轮 Max 上升的主要原因。

```text
[2026-06-13 18:18:10.084] QueryAdvanced "中文测试" total=362ms | path=advanced-linear-gcd candidates=0 results=0 totalRecords=6274361
```

### flush 情况

最新 session 内 `FLUSH=74`，较上一轮继续 +6；`SKIPFLUSH=0`。最新 flush total=3361ms，虽然低于上一轮 4086ms，但仍是秒级完整重写；session 最大 flush total 仍为 **9092ms**。后台仍没有可见 backoff 或 skip 证据。

最新 flush：

```text
[2026-06-13 18:13:24.578] Flushed v6 flat index, lastEventId=363162584,
liveRecords=5314541 | timing: walSwap=0ms rewrite=3358ms total=3361ms
```

Top flushes：

```text
[2026-06-13 16:43:30.341] rewrite=9087ms total=9092ms
[2026-06-13 12:38:27.299] rewrite=3809ms total=6164ms
[2026-06-13 16:23:26.885] rewrite=5714ms total=5716ms
[2026-06-13 16:28:26.841] rewrite=5670ms total=5672ms
[2026-06-13 17:23:26.451] rewrite=5318ms total=5320ms
```

## 6. 问题清单

### P0：tombstone 继续上行，接近 96 万

- 证据：tombstone=959,796（15.30%），较上一轮增加 7,018。
- 影响：无效记录比例继续升高，会放大 scan/filter、linear-gcd 与 full-index rewrite 的实际工作量。
- 判断：tombstone 每轮约 6k-8k 增长，若无提前 compaction，短期将突破 100 万。

### P0：full-index flush 继续按半小时 +6 累积

- 证据：`FLUSH=74`，较上一轮 +6；`SKIPFLUSH=0`；最新 flush total=3361ms，session 最大 total=9092ms。
- 影响：秒级完整重写持续制造 I/O 和 page-cache 干扰，并持续与前台查询共享系统资源。
- 判断：后台持久化策略仍无可见退避，属于最高优先级结构性风险。

### P0：linear-gcd 的中文查询尾部重新抬升

- 证据：`中文测试`=365.0ms，phase2=361.7ms，较上一轮 283.1ms 上升约 29%。
- 影响：中文/非拉丁查询属于用户可感知路径，365ms 已接近明显卡顿区间。
- 判断：lockWait=0ms，瓶颈明确在 phase2 扫描/过滤，不是锁等待。

### P1：path 与 content 仍在 160-210ms 区间

- 证据：`path:/Users`=208.0ms，`content:import`=166.8ms。
- 影响：虽然比上一轮略有改善，但仍是 P90 附近的稳定成本来源。

### P1：trigram 中位健康，但多词查询仍有 55-60ms 小尾部

- 证据：`hello world`=55.9ms，`abc def ghi jkl mno pqr`=58.3ms。
- 影响：当前不是最高风险，但多词 trigram 在 flush/page-cache 波动下仍可能进入百毫秒级。

## 7. 优化建议

### P0：为 full-index flush 增加硬性前台保护与退避

- 对 flush rewrite 超过 2s/5s/8s 建立分级退避：延后下一次 flush、降低 batch 频率、前台查询活跃时禁止大 rewrite。
- 持久化线程使用低 QoS，并评估大顺序写的 `F_NOCACHE` 或同类策略，减少对搜索热页的冲刷。
- 在日志中记录触发、跳过和退避原因，避免 `SKIPFLUSH=0` 长期无法解释。
- 验收：出现 5s+ flush 后下一周期必须看到 backoff 或 skip 证据；常规查询 P90 不再随 flush 抬升。

### P0：将 full-index flush 改为增量或 COW 页写入

- 将 stable segment 与 delta segment 分离，常规 flush 只追加或替换小段 delta。
- 大段合并放到后台低优先级任务，不与前台查询争抢热页或持锁窗口。
- 验收：常规 flush total <200ms，新 session 不再出现 3-9s 级完整 rewrite。

### P0：提前或分段 compaction，先阻止 tombstone 逼近 100 万

- 以 tombstone 率 14%+ 或绝对 tombstone 900k+ 作为明确触发信号；当前 15.30% 已超过应急阈值。
- 结合 segment tombstone 率和 flush rewrite 成本触发局部 compaction。
- 优先处理 tombstone 密集 segment，避免 scan/filter 与 flush rewrite 继续放大。
- 验收：tombstone 率从 15.30% 回落；`中文测试`、`path:/Users`、`content:import` 稳定 <200ms。

### P0：为 advanced-linear-gcd 与 pure-filter 增加轻量剪枝

- 对中文、路径、content/filter 类查询记录扫描记录数、tombstone 跳过数和 segment 分布。
- 评估轻量字符 n-gram、路径分段索引、预过滤位图或 segment-level skip list，以减少无结果或低选择性查询的全量扫描。
- 要求保持内存增长可控，优先使用压缩或按需结构，不引入大常驻索引。
- 验收：`中文测试` 与 `path:/Users` 稳定低于 200ms，`content:import` 不再维持 150ms+。

### P1：细分 top-level timing 与 scan/filter timing

- 对 pure-filter 和 linear-gcd 记录扫描记录数、跳过 tombstone 数、命中 live 数、segment 数、metadata 加载耗时和结果组装耗时。
- 对 `advanced-trigram` 记录 postings 读取、候选交并、候选评分、路径加载、结果组装和 page-cache miss 近似指标。
- 对所有路径增加 queueWait、candidate materialization、sort/rank、HTTP serialization/write 等 top-level timing。
- 验收：`queryTimeMs - phase2Ms - lockWaitMs` 不再是不可解释的大黑盒。

## 8. 下一轮重点

1. tombstone 是否继续从 959,796 上行并逼近 1,000,000。
2. `FLUSH` 是否继续每 30 分钟 +6。
3. `SKIPFLUSH` 是否仍为 0。
4. 是否继续出现 5s+ flush。
5. `中文测试` 是否继续维持 350ms+ 或接近 500ms。
6. `path:/Users` 是否能回落到 200ms 以下。
7. `content:import` 是否继续维持 150ms+。
8. 新 session 中 `QUERYADV_GE500` 是否开始累积。

## 9. 结论

**本轮仍无正式慢查询：0/16 >500ms，P50≈25.7ms，P90≈208.0ms。** 但 Max 升至 365.0ms，`中文测试` 明显抬升，`path:/Users` 与 `content:import` 仍维持中高位，瓶颈集中在 phase2 而非 lockWait。

**后台根因继续恶化：tombstone=959,796（15.30%），最新 session `FLUSH=74`，最大 flush total=9092ms，最新 flush total=3361ms，且 `SKIPFLUSH=0`。** 下一步最高优先级仍是 full-index flush 前台保护/退避与增量/COW flush，同时推进提前/分段 compaction、advanced-linear-gcd/pure-filter 剪枝，以及更细粒度 timing 拆分。
