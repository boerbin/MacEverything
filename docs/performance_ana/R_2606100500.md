# MacEverything 性能分析报告 R_2606100500

## 1. 本轮范围

- 任务启动时间：2026-06-10 05:00
- 报告文件：`docs/performance_ana/R_2606100500.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=7778`
  - `SEG_LINES=6836`
  - `LAST_LOG_TS=2026-06-10 01:47`

## 2. 执行摘要

**均衡回归——trigram 回升，全表扫描大幅恢复。** 0/16 >500ms，Max=262ms，P50=104ms，lockWait=0，极佳 4/16。R235 的极端 trigram/全表扫描反差消失——两路性能都回到正常区间。全表扫描总和 680ms（↓37%），trigram 均值 115ms（↑448% from R235=21ms——正常回调）。

| 指标 | R235 | R236 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 399ms | **262ms** | **↓34%** |
| P50 | 27ms | **104ms** | ↑285%（正常回调） |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 10/16 | **4/16** | ↓6（trigram 正常回调） |
| tombstone | 2,100,602 (28.72%) | **2,107,054 (28.79%)** | ↑ |
| tombstone 增速 | ~201/min | **~215/min** | ↑7%（微升） |
| recordCount | 7,307,747 | **7,314,404** | +6.7K |
| liveRecordCount | 5,207,145 | **5,207,350** | +205 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 6097 | **6157** | +60 |
| COMPACTION | 2 | **2** | **不变——第 37 轮确认缺陷** |
| QUERYADV_GE500 | 60 | **60** | 不变 |

### Page cache 均衡化——零和博弈自平衡

- **R235：trigram 21ms（极佳）+ 全表扫描 1070ms（偏高）——postings 占据 page cache。**
- **R236：trigram 115ms（正常）+ 全表扫描 680ms（正常）——page cache 重新均衡。**
- **从极端反差回到均衡——系统在没有外部干预的情况下自平衡。**
- **这是 page cache LRU 的自然行为：全表扫描的连续大区域访问将 trigram postings 部分淘汰。**

### Trigram 路径——正常回调

| Query | R235 | R236 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 24ms | **126ms** | ↑425% | 正常 |
| `hello world` | 45ms | **259ms** | ↑475% | 正常 |
| `*.swift` | 21ms | **97ms** | ↑362% | 正常 |
| `main.cpp` | 5ms | **25ms** | ↑400% | 极佳 |
| `abc def...` | 27ms | **223ms** | ↑726% | 正常 |
| `README` | 23ms | **51ms** | ↑122% | 正常 |
| `test` | 21ms | **105ms** | ↑400% | 正常 |
| `MacEverything` | 5ms | **33ms** | ↑560% | 极佳 |
| Trigram 均值 | 21ms | **115ms** | **↑448%（正常回调）** |

- **Trigram 均值 21→115ms——从历史最佳正常回调。**
- **百分比看起来惊人，但绝对值全部 <260ms——全部正常。**
- **`hello world`=259ms——postings 区域 page cache 被全表扫描部分淘汰。**

### 全表扫描——大幅恢复

| Query | R235 | R236 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 377ms | **262ms** | **↓31%** |
| `path:/Users` | 399ms | **248ms** | **↓38%** |
| `中文测试` | 294ms | **170ms** | **↓42%** |
| **总和** | **1070ms** | **680ms** | **↓37%** |

- **全表扫描总和 1070→680ms（↓37%）——大幅恢复。**
- **`中文测试`=170ms（↓42%）——名称数组重新预热。**
- **`path:/Users`=248ms（↓38%）——路径数组改善。**
- **三路扫描均在 170-262ms——均衡分布，无极端值。**

### 辅助路径

| Query | R235 | R236 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 7ms | **5ms** | ↓29% | 极佳 |
| `ext:cpp` | 31ms | **39ms** | ↑26% | 极佳 |
| `size:>100m` | 80ms | **102ms** | ↑28% | 正常 |
| `dm:today` | 57ms | **126ms** | ↑121% | 正常 |
| `verylongquery...` | 0.4ms | **0.7ms** | ↑75% | 极佳 |

- **`dm:today`=126ms（↑121%）——SoA modTime 区域波动。**

### tombstone 增速微升

- **tombstone=2,107,054（28.79%）——持续攀升。**
- **增速 ~215/min（微升 7%——仍处凌晨低位）。**
- **recordCount +6.7K，liveRecordCount +205——6.5K 为 tombstone。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 5.0 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 126.0 | 122.3 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 25.1 | 18.8 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 97.0 | 95.1 | advanced-trigram | 0 | 9353 | 正常 |
| `ext:cpp` | 5 | 39.2 | 38.4 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 101.9 | 101.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 258.6 | 254.6 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 223.4 | 223.1 | advanced-trigram | 0 | 14461 | 正常 |
| `README` | 5 | 50.7 | 42.4 | advanced-trigram | 0 | 17151 | 正常 |
| `test` | 5 | 104.6 | 91.3 | advanced-trigram | 0 | 117136 | 正常 |
| `MacEverything` | 5 | 33.4 | 1.5 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 126.0 | 125.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 261.6 | 253.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 248.3 | 243.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 170.3 | 168.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.7 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 104.6ms，P90 = 258.6ms，Max = 261.6ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R235 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **4/16** | 0.7-39ms | R235=10/16（↓6——正常回调） |
| **正常**（50-500ms） | 12/16 | 51-262ms | R235=6/16（↑6） |
| **超标**（>500ms） | **0/16** | — | R235=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,314,404，liveRecordCount=5,207,350（tombstone=**2,107,054**，**28.79%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=554  QUERYADV_GE500=60（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=6157
```

## 6. 问题清单

### P0 确认（第 37 轮）：compaction 触发机制缺陷

- **tombstone=2,107,054（28.79%）——超阈值 3.79%。**
- **37 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **连续 4 轮 lockWait=0。历史证据充分。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式

### P0：修复 compaction 触发机制（第 37 轮确认）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 是否维持均衡状态。
2. tombstone 增速趋势。

## 9. 结论

**均衡回归——R235 的极端 trigram/全表扫描反差自平衡。** 0/16 >500ms，Max=262ms（↓34%），P50=104ms，极佳 4/16。Trigram 均值 115ms（从 R235=21ms 正常回调），全表扫描总和 680ms（↓37%——大幅恢复）。两路性能都回到正常区间——page cache LRU 自然均衡化。lockWait 连续 4 轮为 0。

**tombstone 28.79%（第 37 轮确认 compaction 缺陷）。** 增速 ~215/min（凌晨低位）。COMPACTION=2 始终不变。
