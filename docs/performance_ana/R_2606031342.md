# MacEverything 性能分析报告 R_2606031342

## 1. 本轮范围

- 任务启动时间：2026-06-03 13:42
- 报告文件：`docs/performance_ana/R_2606031342.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21383`
  - `SEG_LINES=357`
  - `LAST_LOG_TS=2026-06-03 16:46`

## 2. 执行摘要

服务正常运行（第 16 轮连续可用）。**本轮为历史最佳性能**：P50 ≈ 34ms（前轮 106ms），Max=380ms，几乎所有查询创历史新低或接近最佳。`.pdf` 从前轮 415ms 大幅降至 84ms，`*.swift` 从 233ms 降至 29ms，均为历史最佳。所有查询连续第 13 轮 < 500ms。tombstone 微增至 159,681 条（2.71%），连续第 5 轮超过 2%。

### 横向对比（最近 5 轮）

| Query | R60 | R61 | R62 | R63 | R64 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 14 | 7 | 93 | 6 | **2** | **历史最佳** |
| `.pdf` | 349 | 231 | 325 | 415 | **84** | **历史最佳** |
| `*.swift` | 179 | 172 | 488 | 233 | **29** | **历史最佳** |
| `hello world` | 436 | 252 | 297 | 274 | **120** | **大幅改善** |
| `path:/Users` | 278 | 227 | 255 | 307 | **380** | 波动偏慢 |
| P50 | 178 | 147 | 148 | 106 | **34** | **历史最佳** |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 1.6 | 0.0 | short-query-cache | 0 | 0 | **历史最佳** |
| `.pdf` | 5 | 84.3 | 79.0 | advanced-trigram | 0 | 9944 | **历史最佳** |
| `main.cpp` | 5 | 12.2 | 9.9 | advanced-trigram | 0 | 85 | 极快 |
| `*.swift` | 5 | 28.9 | 27.1 | advanced-trigram | 0 | 9356 | **历史最佳** |
| `ext:cpp` | 5 | 24.4 | 23.9 | advanced-ext-index | 0 | 3702 | **历史最佳** |
| `size:>100m` | 5 | 39.0 | 38.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 极快 |
| `hello world` | 5 | 119.6 | 116.9 | advanced-trigram | 0 | 3613 | **历史最佳** |
| `abc def ghi jkl mno pqr` | 0 | 37.3 | 36.7 | advanced-trigram | 0 | 15982 | **历史最佳** |
| `README` | 5 | 10.7 | 6.4 | advanced-trigram | 0 | 17717 | 极快 |
| `test` | 5 | 30.7 | 19.3 | advanced-trigram | 0 | 129960 | 极快 |
| `MacEverything` | 5 | 7.8 | 0.9 | advanced-trigram | 0 | 6802 | 极快 |
| `dm:today` | 5 | 84.3 | 84.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 179.5 | 170.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 380.1 | 373.0 | advanced-linear-gcd | 0 | 0 | 偏慢 |
| `中文测试` | 0 | 264.0 | 263.1 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 34ms，P90 ≈ 264ms，Max = 380ms。全部查询连续第 13 轮 < 500ms。lockWaitMs 全部为 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,884,949，liveRecordCount=5,725,268（tombstone=**159,681**，**2.71%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=123  QUERYADV_GE500=5
FLUSH=0  COMPACTION=2  SKIPFLUSH=110
DIAG_TRIGRAM_SKIP=69
```

- QUERYADV 从 119 增至 123（+4）。
- QUERYADV_GE500 保持 5（本轮 Max=380ms）。
- **FLUSH 归零**：从前轮的 1 降至 0（初始 flush 已被覆盖或日志计数变化）。
- SKIPFLUSH 从 109 增至 110（+1，增速显著放缓）。
- DIAG_TRIGRAM_SKIP 从 68 增至 69（+1）。

## 6. 问题清单

### Critical：tombstone 连续 5 轮超过 2%（2.71%，159,681 条）

- 增长轨迹：
  ```
  R60: 128K (2.19%) → R61: 138K (2.36%) → R62: 149K (2.54%)
  → R63: 158K (2.69%) → R64: 160K (2.71%)
  ```
- 本轮增量仅 1,324 条（前轮 ~10K），增速大幅放缓。
- **累计内存开销**：~160K × 300 字节 ≈ 48MB 额外内存。
- **查询开销**：phase2 扫描 totalRecords 包含 160K tombstone（2.71% 额外扫描）。
- 尽管增速放缓，总量仍在持续积累，compaction 仍未触发。

### Low：`path:/Users` 波动仍大（164-497ms 范围）

- 本轮 380ms，前轮 307ms。linear-gcd 扫描路径对 I/O 负载敏感。
- 16 轮数据中最高 497ms（R57），尚未突破 500ms。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（已超阈值 5 轮）

- tombstone 2.71%，虽增速放缓但总量持续积累。
- **最小实施方案**：在 `IndexPersistence` 的 SKIPFLUSH 分支中增加一行检查：
  ```
  if (tombstoneRatio > 0.02) triggerCompaction();
  ```
- 复用现有 compaction 代码路径，零新增复杂度。

### P0：补齐进程生命周期监控（承前轮）

### P1：将 `.xxx`/`*.xxx` 扩展名模式路由到 ext-index（承前轮）

- 本轮数据进一步证实：`.pdf` 走 trigram 为 84ms，而 `ext:cpp` 走 ext-index 仅 24ms。
- 当 I/O 负载高时，trigram 路径容易波动到 400ms+。

### P2：short-query-cache 偶发延迟根因（承前轮，本轮正常——连续 2 轮正常）

## 8. 下一轮重点

1. tombstone 增速放缓是否持续——本轮仅增 1.3K 条（前几轮 ~10K）。
2. 本轮历史最佳性能是否可重复。
3. `path:/Users` 是否继续在 300-400ms 区间波动。

## 9. 结论

服务连续第 16 轮正常运行，所有查询连续第 13 轮 <500ms。**本轮为历史最佳性能**：P50 ≈ 34ms（前轮 106ms），Max=380ms。几乎所有 trigram 查询创历史新低——`.pdf`=84ms（前轮 415ms）、`*.swift`=29ms（前轮 233ms）、`hello world`=120ms（前轮 274ms）。I/O 负载极低时的性能表现证明引擎本身性能优异，瓶颈在于系统 I/O 负载波动。tombstone 增至 159,681 条（2.71%），但增速显著放缓（本轮仅增 1,324 条）。SKIPFLUSH=110（+1），compaction 仍未触发。
