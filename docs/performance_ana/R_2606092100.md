# MacEverything 性能分析报告 R_2606092100

## 1. 本轮范围

- 任务启动时间：2026-06-09 21:00
- 报告文件：`docs/performance_ana/R_2606092100.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6796`
  - `SEG_LINES=5854`
  - `LAST_LOG_TS=2026-06-09 16:17`

## 2. 执行摘要

**持续恢复——0/16 >500ms，Max=415ms，P50=130ms，lockWait=0，极佳比例 5/16。** 连续第 3 轮 0/16 >500ms。`hello world`=415ms 偏高（postings 区域波动）。**tombstone 突破 27%——27.08%（里程碑！第 21 轮确认 compaction 缺陷）。**

| 指标 | R219 | R220 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平（连续 3 轮） |
| Max | 347ms | **415ms** | ↑20%（`hello world` 波动） |
| P50 | 145ms | **130ms** | ↓10% |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 5/16 | **5/16** | 持平 |
| tombstone | 1,918,280 (26.98%) | **1,928,515 (27.08%)** | **↑——突破 27%！** |
| tombstone 增速 | ~340/min | **~341/min** | 持平（晚间稳定） |
| recordCount | 7,109,668 | **7,122,394** | +12.7K |
| liveRecordCount | 5,191,388 | **5,193,879** | +2.5K |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5233 | **5293** | +60 |
| COMPACTION | 2 | **2** | **不变——第 21 轮确认缺陷** |
| QUERYADV_GE500 | 53 | **53** | 不变（连续 3 轮 0 新增） |

### tombstone 突破 27%——里程碑！

- **tombstone=1,928,515（27.08%）——超阈值 2.08%！**
- **首次突破 27%——第 21 轮确认 compaction 缺陷。**
- **增速 ~341/min（晚间稳定——与 R219 持平）。**
- **recordCount +12.7K，liveRecordCount +2.5K——净新增少，10.2K 为 tombstone。**

### tombstone 增长全景

| 里程碑 | 轮次 | tombstone% | 距本轮 |
|--------|------|----------:|--------|
| 首次超 25% | R200 | 25.15% | 21 轮前 |
| 首次超 26% | R212 | 26.14% | 9 轮前 |
| 首次超 27% | **R220** | **27.08%** | **当前** |

- **从 25% → 27% 历经 21 轮——平均每轮增长 ~0.09 个百分点。**
- **照此速率，约 11 轮后达 28%。**

### Trigram 路径——轻微回升但全部 <500ms

| Query | R219 | R220 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 145ms | **229ms** | ↑58% | 正常（波动） |
| `hello world` | 227ms | **415ms** | ↑83% | 正常（本轮最高） |
| `*.swift` | 243ms | **130ms** | ↓47% | 正常（恢复） |
| `main.cpp` | 29ms | **41ms** | ↑41% | 极佳 |
| `abc def...` | 149ms | **147ms** | ↓1% | 正常（稳定） |
| `README` | 43ms | **51ms** | ↑19% | 正常（微升） |
| `test` | 74ms | **132ms** | ↑78% | 正常 |
| `MacEverything` | 14ms | **16ms** | ↑14% | 极佳 |
| Trigram 均值 | 115ms | **145ms** | **↑26%** | 轻微回升 |

- **Trigram 均值 115→145ms（↑26%）——轻微回升，但全部 <500ms。**
- **`hello world`=415ms（↑83%）——postings 区域 page cache 波动。**
- **`*.swift`=130ms（↓47% from 243ms）——恢复。**
- **距稳态均值 ~20ms 仍有 ~7 倍差距。**

### 全表扫描——平稳

| Query | R219 | R220 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 347ms | **294ms** | ↓15% |
| `path:/Users` | 147ms | **285ms** | ↑94% |
| `中文测试` | 250ms | **171ms** | ↓32% |
| **总和** | **744ms** | **750ms** | **↑1%（平稳）** |

- **全表扫描总和 744→750ms（↑1%）——本质平稳。**
- **`path:/Users` 与 `content:import` 交替波动——零和效应持续。**

### 辅助路径——整体改善

| Query | R219 | R220 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 8ms | **7ms** | ↓14% | 极佳 |
| `ext:cpp` | 78ms | **47ms** | ↓40% | 极佳 |
| `size:>100m` | 104ms | **52ms** | ↓50% | 正常 |
| `dm:today` | 76ms | **55ms** | ↓28% | 正常 |
| `verylongquery...` | 0.3ms | **2.7ms** | ↑800% | 极佳 |

- **`ext:cpp`=47ms（↓40%）——回到极佳。**
- **`size:>100m`=52ms（↓50%）——SoA 区域预热。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 7.4 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 229.3 | 221.7 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 40.6 | 25.8 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 129.6 | 124.9 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 47.2 | 45.5 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 51.6 | 51.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 415.2 | 411.1 | advanced-trigram | 0 | 3671 | 正常（本轮最高） |
| `abc def ghi jkl mno pqr` | 0 | 146.6 | 145.3 | advanced-trigram | 0 | 14387 | 正常 |
| `README` | 5 | 51.0 | 43.0 | advanced-trigram | 0 | 17137 | 正常 |
| `test` | 5 | 131.6 | 119.9 | advanced-trigram | 0 | 117078 | 正常 |
| `MacEverything` | 5 | 15.9 | 1.7 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 55.2 | 54.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 294.2 | 281.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 284.5 | 262.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 171.1 | 169.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 2.7 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 129.6ms，P90 = 284.5ms，Max = 415.2ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R219 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **5/16** | 2.7-47ms | R219=5/16（持平） |
| **正常**（50-500ms） | 11/16 | 51-415ms | R219=11/16（持平） |
| **超标**（>500ms） | **0/16** | — | R219=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,122,394，liveRecordCount=5,193,879（tombstone=**1,928,515**，**27.08%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=452  QUERYADV_GE500=53（不变——连续 3 轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5293
```

## 6. 问题清单

### P0 确认（第 21 轮——tombstone 突破 27%）：compaction 触发机制缺陷

- **tombstone 27.08%——首次突破 27%！超阈值 2.08%。**
- **21 轮连续确认——COMPACTION 始终为 2。**

### P0：flush 写锁阻塞

- **R217=202.6ms 证据。COW flush 是正确修复路径。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 21 轮确认——tombstone 突破 27%）

### P0：flush rewrite 改为 COW 方式

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. tombstone 增长趋势（已突破 27%，观察 28% 进程）。
2. 是否恢复到稳态（极佳 10+/16）。

## 9. 结论

**持续恢复——0/16 >500ms（连续第 3 轮），Max=415ms（↑20%），P50=130ms（↓10%），极佳 5/16。** `hello world`=415ms 偏高（postings 区域波动），其余 trigram 均 <300ms。lockWait=0，`x`=7ms——系统级资源竞争完全消退。辅助路径整体改善：`ext:cpp` 回到极佳，`size:>100m` ↓50%。

**tombstone 突破 27%——27.08%（里程碑！第 21 轮确认 compaction 缺陷）。** 从 R200 的 25.15% → R220 的 27.08%——21 轮增长 1.93 个百分点。增速 ~341/min（晚间稳定）。COMPACTION=2 始终不变。当前两个 P0 问题：compaction 触发缺陷 + flush 写锁阻塞。
