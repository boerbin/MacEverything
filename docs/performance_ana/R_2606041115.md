# MacEverything 性能分析报告 R_2606041115

## 1. 本轮范围

- 任务启动时间：2026-06-04 11:15
- 报告文件：`docs/performance_ana/R_2606041115.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=22034`
  - `SEG_LINES=1008`
  - `LAST_LOG_TS=2026-06-04 11:16`

## 2. 执行摘要

**I/O 压力持续但改善中。** `.pdf`=635ms（>500ms），较 R92 的 820ms 下降 23%。`x`=74.8ms（short-query-cache GCD 异常再现，第 10 次）。P50 ≈ 57ms，Max=635ms。**1/16 查询 >500ms。lockWaitMs 全部为 0。**

**tombstone=482,303（7.76%，145MB），增量 +2,729**（R92→R93 间隔仅 ~5 分钟，增量极低属正常）。

### 横向对比（最近 5 轮）

| Query | R89 | R90 | R91 | R92 | R93 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 52 | 3 | 12 | 5 | **75** | **GCD 异常** |
| `.pdf` | 225 | 54 | 192 | 820 | **635** | **改善中** |
| `*.swift` | 117 | 32 | 125 | 430 | **22** | **热缓存** |
| `hello world` | 218 | 62 | 273 | 412 | **352** | 偏高 |
| `path:/Users` | 216 | 65 | 314 | 222 | **144** | 正常 |
| P50 | 116 | 23 | 126 | 131 | **57** | **偏低（热缓存）** |

### I/O 压力事件恢复追踪

| 轮次 | P50 | Max | >500ms | `.pdf` | 状态 |
|------|----:|----:|-------:|------:|------|
| **R92 (事件)** | **131ms** | **820ms** | **1/16** | **820ms** | **轻度 I/O 压力** |
| **R93 (恢复1)** | **57ms** | **635ms** | **1/16** | **635ms** | **改善中，未完全恢复** |

R92 I/O 事件尚未完全恢复——`.pdf` 仍 >500ms。但 P50 从 131ms 降至 57ms（热缓存效应：R92 查询电池预热了部分索引页面），且 `*.swift` 从 430ms 骤降至 22ms。

**与 R84-R86 模式对比**：R84→R85→R86 是连续 3 轮加剧（轻度→中度→灾难）。R92→R93 为改善趋势——**未进入恶化螺旋**。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 74.8 | 0.0 | short-query-cache | 0 | 0 | **GCD 异常** |
| `.pdf` | 5 | 635.4 | 559.7 | advanced-trigram | 0 | 9950 | **>500ms** |
| `main.cpp` | 5 | 5.6 | 3.6 | advanced-trigram | 0 | 85 | 极快 |
| `*.swift` | 5 | 21.9 | 19.6 | advanced-trigram | 0 | 9356 | **热缓存恢复** |
| `ext:cpp` | 5 | 11.4 | 10.9 | advanced-ext-index | 0 | 3701 | 极快 |
| `size:>100m` | 5 | 316.8 | 186.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 偏高 |
| `hello world` | 5 | 351.5 | 348.4 | advanced-trigram | 0 | 3613 | 偏高 |
| `abc def ghi jkl mno pqr` | 0 | 62.0 | 61.9 | advanced-trigram | 0 | 16029 | 正常 |
| `README` | 5 | 13.6 | 9.9 | advanced-trigram | 0 | 17679 | 极快 |
| `test` | 5 | 15.2 | 11.8 | advanced-trigram | 0 | 130096 | 极快 |
| `MacEverything` | 5 | 17.0 | 0.6 | advanced-trigram | 0 | 6803 | 极快 |
| `dm:today` | 5 | 51.6 | 51.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 218.4 | 210.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 144.0 | 140.0 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 233.8 | 232.0 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.2 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 57ms，P90 ≈ 317ms，Max = 635ms。1/16 查询 >500ms。lockWaitMs 全部为 0。**

### 查询分层分析

**热缓存受益查询**（R92 查询电池预热效应）：

| 查询 | R92 | R93 | 变化 | 原因 |
|------|----:|----:|-----:|------|
| `*.swift` | 430ms | 22ms | -95% | trigram 索引页面被 R92 预热 |
| `main.cpp` | 74ms | 6ms | -92% | 同上 |
| `README` | 27ms | 14ms | -48% | 同上 |
| `test` | 33ms | 15ms | -55% | 同上 |

**仍受 I/O 压力影响的查询**：

| 查询 | R92 | R93 | 变化 | 原因 |
|------|----:|----:|-----:|------|
| `.pdf` | 820ms | 635ms | -23% | trigram 索引区域仍部分冷 |
| `hello world` | 412ms | 352ms | -15% | 多 token 匹配需要更多索引页 |
| `size:>100m` | 131ms | 317ms | +142% | **全表扫描路径波动** |

### `size:>100m` 异常分析

- R92: 131ms（正常）→ R93: 317ms（偏高），queryTimeMs - phase2Ms = 130ms 非 phase2 开销。
- `size:>100m` 走 pure-filter-soa-gcd 全表扫描路径，结果 candidates=0（符合预期）。
- 偏高原因：日间系统 I/O 活动与全表扫描竞争页面缓存。phase2Ms=187ms 但查询总时间 317ms——**130ms 的差值来自 GCD dispatch + 初始化开销**。
- 尚未达到危险水平（R86 为 2,731,551ms），但作为 R86 灾难路径的观测指标需持续关注。

### `x`=74.8ms short-query-cache GCD 异常

- **第 10 次出现**（共 33 轮，30.3%）。
- 出现记录：R67(155), R69(68), R71(103), R73(120), R76(190), R80(88), R85(174), R86(367), R89(52), **R93(75)**。
- phase2=0 确认问题在 GCD dispatch 层，非搜索本身。
- 本次 75ms 属中等偏低（范围 52-367ms）。
- **与 I/O 事件关联**：10 次中有 4 次发生在 I/O 压力轮次（R67, R76, R85, R86）——但也有 6 次在正常轮次出现，说明 GCD 异常是独立问题。

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,220,395，liveRecordCount=5,738,092（tombstone=**482,303**，**7.76%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=326  QUERYADV_GE500=46
FLUSH=0  COMPACTION=2  SKIPFLUSH=382
DIAG_TRIGRAM_SKIP=101
```

- QUERYADV 从 320 增至 326（+6）。
- QUERYADV_GE500 从 45 增至 46（+1，即 `.pdf`=635ms）。
- SKIPFLUSH 从 380 增至 382（+2，R92→R93 仅 ~5 分钟间隔）。
- DIAG_TRIGRAM_SKIP 从 100 增至 101（+1，回归正常增速）。

### DIAG_TRIGRAM_SKIP 增速对比

| 轮次 | 值 | 增量 | 说明 |
|------|---:|-----:|------|
| R89-R91 | 94→96 | +1/轮 | 稳态 |
| **R92** | **100** | **+4** | **日间 I/O 压力跳增** |
| R93 | 101 | +1 | **回归稳态** |

R92 的 +4 跳增为一次性 I/O 压力释放，本轮回归 +1 表明缓存状态正在恢复。

## 6. 问题清单

### Critical：tombstone 7.76%（482,303 条，145MB），持续增长

- 增长轨迹（近 5 轮）：
  ```
  R89: 442K (+12.1K) → R90: 444K (+1.6K) → R91: 454K (+10.5K)
  → R92: 480K (+25.5K) → R93: 482K (+2.7K)
  ```
- R90 和 R93 增量极低均因间隔短（~3-5 分钟）。
- **稳态增速仍为 ~12K/轮**（含日间加速窗口可达 25K/轮）。
- **累计内存开销**：~482K × 300 字节 ≈ **145MB**。
- 连续第 33 轮超 2%，第 14 轮超过 5%，第 9 轮超过 6%，第 6 轮超过 7%。

### Critical：I/O 压力事件尚未完全恢复

- R92 事件后第 1 轮：`.pdf` 仍 >500ms（635ms）。
- 但趋势向好（820→635ms，-23%），且 `*.swift` 已完全恢复（430→22ms）。
- **未进入 R84-R86 恶化螺旋**——锁等待为 0，P50 大幅下降。
- 预计 1-2 轮内完全恢复。

### Medium：short-query-cache GCD 异常（第 10 次，30.3%）

- `x`=74.8ms，phase2=0，问题完全在 GCD dispatch 层。
- 10/33 轮出现率 30.3%，接近每 3 轮出现 1 次。
- 本次严重程度：中低（75ms，范围 52-367ms）。

### Low：`size:>100m` 偏高（317ms）

- R92 正常（131ms）→ R93 偏高（317ms）——queryTimeMs 与 phase2Ms 差值 130ms 异常。
- 作为 R86 灾难路径的早期指标需持续观测。
- 当前远未达危险水平。

## 7. 优化建议

### P0 紧急：实现 tombstone compaction（7.76%，145MB，事件频率加速）

- **33 轮连续超标。** I/O 事件间隔从 9→8→6 轮递减。
- **日间增速 +25.5K（R92）创新高**——日间用户活动是最大驱动力。
- compaction 后释放 145MB → trigram 索引常驻 → 消除 I/O 压力事件根因。

### P0 紧急：`size:>100m` 全表扫描超时保护
### P0：short-query-cache GCD 隔离（30.3% 异常率）
### P1：将 `.pdf`/`*.swift` 路由到 ext-index
### P2：实现 phase2 超时降级

## 8. 下一轮重点

1. `.pdf` 是否恢复到 <500ms（完全恢复标志）。
2. `size:>100m` 是否回到正常区间（<150ms）。
3. tombstone 增速是否维持稳态。

## 9. 结论

**I/O 压力持续但改善中。** `.pdf`=635ms（>500ms，R92 的 820ms 下降 23%），1/16 查询突破阈值。P50 ≈ 57ms（R92 查询电池热缓存效应），lockWaitMs 全部为 0。**未进入 R84-R86 恶化螺旋**——`*.swift` 已完全恢复（430→22ms），P50 大幅下降，锁等待归零。short-query-cache GCD 异常第 10 次出现（30.3%），`x`=74.8ms。tombstone=482,303（7.76%，145MB），增量 +2,729（短间隔所致）。`size:>100m`=317ms 偏高需关注但远未达危险水平。预计 1-2 轮完全恢复。
