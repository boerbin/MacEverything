# MacEverything 性能分析报告 R_2606081416

## 1. 本轮范围

- 任务启动时间：2026-06-08 14:16
- 报告文件：`docs/performance_ana/R_2606081416.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=4671`
  - `SEG_LINES=3729`
  - `LAST_LOG_TS=2026-06-08 14:47`

## 2. 执行摘要

**午后压力 1 轮恢复——0/16 >500ms，Max=165ms，P50=62ms，lockWait=0，极佳比例 7/16（44%）。** 从 R184 的午后压力（P50=134ms，1/16 >500ms）大幅恢复。Trigram 均值从 168ms 降至 82ms（↓51%）。全表扫描总和 219ms——**接近 R177 的 238ms（post-wake 纪录），为正常日间运行的绝对新低！** lockWait 从 19.8ms 恢复至 0。tombstone 增速 371/min——正常范围。

### 关键指标对比

| 指标 | R184（午后压力） | R185 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 1/16 | **0/16** | **↓1（恢复）** |
| Max | 575ms | **165ms** | **↓ 71%** |
| P50 | 134ms | **62ms** | **↓ 54%** |
| lockWait max | 19.8ms | **0ms** | **恢复** |
| 极佳比例(<50ms) | 2/16 | **7/16** | **↑5** |
| tombstone | 1,518,959 (22.92%) | **1,533,912 (23.07%)** | ↑ |
| tombstone 增速 | ~361/min | **~371/min** | 稳定 |
| recordCount | 6,629,150 | **6,645,633** | +16.5K（偏高） |
| liveRecordCount | 5,110,191 | **5,111,721** | +1,530 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3388 | **3448** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 19 | **19** | 不变（本轮 0 新增） |

### Trigram 恢复——部分热化

| Query | R184 | R185 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 575ms | **134ms** | **↓ 77%** | 正常 |
| `hello world` | 296ms | **165ms** | ↓ 44% | 正常 |
| `*.swift` | 286ms | **124ms** | ↓ 57% | 正常 |
| `main.cpp` | 64ms | **38ms** | ↓ 41% | 极佳 |
| `abc def...` | 232ms | **102ms** | ↓ 56% | 正常 |
| `README` | 80ms | **41ms** | ↓ 49% | 极佳 |
| `test` | 134ms | **79ms** | ↓ 41% | 正常 |
| `MacEverything` | 16ms | **13ms** | ↓ 19% | 极佳 |
| Trigram 均值 | 168ms | **82ms** | **↓ 51%** | 恢复中 |

- **所有 trigram 查询均改善——R184 battery 预热效果均匀。**
- **`.pdf`=134ms（R184=575ms，↓77%）——1 轮恢复至正常范围。**
- **均值 82ms——仍高于稳态最佳（R183=27ms），但属正常日间波动。**
- **恢复率 ~50% 整体一致——再 1 轮预计可回到 30-50ms 水平。**

### 全表扫描——日间运行新低！

| Query | R184 | R185 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 219ms | **74ms** | **↓ 66%** |
| `path:/Users` | 248ms | **84ms** | **↓ 66%** |
| `中文测试` | 271ms | **61ms** | **↓ 77%** |
| **总和** | **738ms** | **219ms** | **↓ 70%（日间新低！）** |

- **全表扫描总和 219ms——仅次于 R177 的 238ms（post-wake 纪录），且为正常日间运行的绝对新低！**
- **`中文测试`=61ms——从 R184 的 271ms 暴降 77%。**
- **`content:import`=74ms——本系列日间最低（此前最低 R183=202ms）。**
- **原因**：午后 page cache 竞争消退——可能是午休或低活动窗口，SoA/linear 区域获得充分 page cache。

### SoA Filter——极佳！

| Query | R184 | R185 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 78ms | **44ms** | **↓ 44%（极佳）** |
| `dm:today` | 74ms | **20ms** | **↓ 73%（极佳）** |

- **`dm:today`=20ms——日间第二低（仅次 R177=12.5ms post-wake 极值）。**
- **两项均回到极佳范围——SoA 区域 page cache 充分。**

### tombstone 23.07%——首次突破 23%

- **tombstone 1,533,912（23.07%）——首次突破 23% 大关。**
- **增速 371/min——正常范围。**
- **recordCount +16.5K——偏高（正常 ~8K/30min），可能午后有批量文件操作。**
- **距 25% 约 14-16 小时。**

### 日志新发现：DIAG-TRIGRAM 诊断日志

```
DIAG-TRIGRAM key="中文测试" SKIP reason=allFound=false phase2Pending=0 
indexEmpty=0 indexBuckets=96920 allFound=0 rawCands=0 threshold=664548 totalSize=6645484
```

- **`中文测试` 触发了 trigram SKIP 诊断——`rawCands=0`（trigram 过滤后 0 个候选）。**
- **`threshold=664548`（totalSize 的 10%）——说明 trigram 过滤阈值设为总记录数的 10%。**
- **正常行为——中文查询的 trigram 命中率太低，正确回退到 linear-gcd 路径。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 6.0 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 133.7 | 129.6 | advanced-trigram | 0 | 9841 | 正常 |
| `main.cpp` | 5 | 38.1 | 14.9 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 123.5 | 119.5 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 38.9 | 38.0 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 44.2 | 43.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 164.6 | 156.3 | advanced-trigram | 0 | 3626 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 101.8 | 101.2 | advanced-trigram | 0 | 14233 | 正常 |
| `README` | 5 | 40.6 | 36.7 | advanced-trigram | 0 | 16637 | 极佳 |
| `test` | 5 | 78.7 | 73.1 | advanced-trigram | 0 | 116530 | 正常 |
| `MacEverything` | 5 | 12.7 | 1.1 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 20.4 | 19.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 73.6 | 67.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 83.7 | 81.2 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 61.4 | 58.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 61.4ms，P90 = 133.7ms，Max = 164.6ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R184 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 7/16 | 0.5-44ms | R184=2/16（**↑5**） |
| **正常**（50-500ms） | 9/16 | 61-165ms | R184=13/16（↓4） |
| **超标**（>500ms） | 0/16 | — | R184=1/16（**↓1**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,645,633，liveRecordCount=5,111,721（tombstone=**1,533,912**，**23.07%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=207  QUERYADV_GE500=19（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3448
```

- **本轮 0 新增 >500ms 查询。**
- **SKIPFLUSH +60——正常节奏。**
- **首次出现 DIAG-TRIGRAM 诊断日志——正常行为。**

## 6. 问题清单

### 已解决：R184 午后压力——1 轮恢复

- **确认为午后日间波动——非持续性问题。**
- **lockWait=19.8ms 已恢复至 0——但日间出现的事实仍支持 P1 COW。**

### tombstone 23.07%——首次突破 23%

- **里程碑**：23% 大关突破。
- **增速 371/min（正常），距 25% 约 14-16 小时。**
- **P0 compaction 修复紧迫度不变。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（紧迫）

- tombstone 23.07%（1.53M），距 25% 约 14-16 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. trigram 均值是否继续从 82ms 收敛至 30-50ms。
2. 全表扫描总和是否保持 219ms 极低水平（或回升至正常 600-800ms）。
3. tombstone 增速是否保持正常（~300-370/min）。

## 9. 结论

**午后压力 1 轮恢复——0/16 >500ms，Max=165ms，P50=62ms，lockWait=0，极佳 7/16（44%）。** R184 的午后压力确认为日间正常波动——1 轮恢复。全表扫描总和 219ms 创日间运行新低（接近 R177 的 post-wake 纪录 238ms）——午后低活动窗口使 SoA/linear 区域获得充分 page cache。Trigram 均值 82ms（R184=168ms，↓51%），恢复中。

**恢复模式继续一致——所有压力事件均在 1 轮 battery 后恢复。** tombstone 23.07%（首次突破 23%，1.53M），增速 371/min（正常），距 25% 约 14-16 小时。P0 compaction 修复持续紧迫。
