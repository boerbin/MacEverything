# MacEverything 性能分析报告 R_2606081446

## 1. 本轮范围

- 任务启动时间：2026-06-08 14:46
- 报告文件：`docs/performance_ana/R_2606081446.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=4734`
  - `SEG_LINES=3792`
  - `LAST_LOG_TS=2026-06-08 15:17`

## 2. 执行摘要

**持续收敛——0/16 >500ms，Max=137ms，P50=52ms，lockWait≈0，极佳比例 8/16（50%）。** 连续第 2 轮从 R184 午后压力恢复，所有指标继续改善。Trigram 均值 82→67ms（↓18%），全表扫描总和 189ms（**系列绝对新低！超越 R177=238ms 和 R185=219ms**），`中文测试`=45ms 再次进入极佳范围。tombstone 突破 23.2%。

### 关键指标对比

| 指标 | R185 | R186 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（连续 3 轮 0） |
| Max | 165ms | **137ms** | ↓ 17% |
| P50 | 62ms | **52ms** | **↓ 16%** |
| lockWait max | 0ms | **0.2ms** | 微量（忽略） |
| 极佳比例(<50ms) | 7/16 | **8/16** | **↑1** |
| tombstone | 1,533,912 (23.07%) | **1,552,020 (23.30%)** | ↑ |
| tombstone 增速 | ~371/min | **~603/min** | **↑ 63%（午后突增！）** |
| recordCount | 6,645,633 | **6,664,483** | +18.9K（偏高） |
| liveRecordCount | 5,111,721 | **5,112,463** | +742 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3448 | **3508** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 19 | **19** | 不变（本轮 0 新增） |

### Trigram 持续收敛

| Query | R185 | R186 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 134ms | **119ms** | ↓ 11% | 正常 |
| `hello world` | 165ms | **137ms** | ↓ 17% | 正常 |
| `*.swift` | 124ms | **81ms** | ↓ 35% | 正常 |
| `main.cpp` | 38ms | **20ms** | ↓ 47% | 极佳 |
| `abc def...` | 102ms | **87ms** | ↓ 15% | 正常 |
| `README` | 41ms | **35ms** | ↓ 15% | 极佳 |
| `test` | 79ms | **58ms** | ↓ 27% | 正常 |
| `MacEverything` | 13ms | **9ms** | ↓ 31% | 极佳 |
| Trigram 均值 | 82ms | **67ms** | **↓ 18%** | 收敛中 |

- **所有 trigram 查询继续改善——稳定的逐轮收敛。**
- **`.pdf`=119ms、`hello world`=137ms——仍高于正常（17ms/15ms），但从 R181 的 7193ms/4166ms 已恢复 98%+。**
- **`*.swift`=81ms（R185=124ms，↓35%）——恢复速度最快。**
- **均值 67ms——距稳态 27ms（R183）还有差距，但方向一致。**

### 全表扫描——系列绝对新低！

| Query | R185 | R186 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 74ms | **61ms** | ↓ 18% |
| `path:/Users` | 84ms | **84ms** | 持平 |
| `中文测试` | 61ms | **45ms** | **↓ 26%（极佳！）** |
| **总和** | **219ms** | **189ms** | **↓ 14%（系列绝对新低！）** |

- **全表扫描总和 189ms——超越 R185=219ms 和 R177=238ms，系列绝对新低！**
- **`中文测试`=45ms——再次进入极佳范围（<50ms），与 R177=45ms 并列最低！**
- **`content:import`=61ms——连续 2 轮日间极低值（R185=74ms→R186=61ms）。**
- **午后低活动窗口持续——SoA/linear 区域 page cache 充分。**

### SoA Filter——优秀

| Query | R185 | R186 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 44ms | **51ms** | ↑ 16%（正常） |
| `dm:today` | 20ms | **14ms** | **↓ 30%（极佳）** |

- **`dm:today`=14ms——日间最低（R177=12.5ms post-wake 之后第二低）！**
- **`size:>100m`=51ms——轻微上升，从极佳边缘到正常边缘。**

### ⚠️ tombstone 增速午后突增——603/min

- **增速 603/min（R185=371/min，↑63%）——午后突增！**
- **recordCount +18.9K（R185=+16.5K）——持续偏高。**
- **liveRecordCount 仅 +742——说明大量是替换/删除操作（tombstone 为主）。**
- **可能原因**：午后大量文件操作（应用更新、缓存清理、IDE 重建索引等）产生密集的文件替换。
- **tombstone 23.30%（1.55M）——距 25% 约 10-12 小时（增速加快！）。**
- **如果增速持续 ~600/min，P0 compaction 修复时间窗口缩短！**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 5.6 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 118.8 | 112.5 | advanced-trigram | 0 | 9841 | 正常 |
| `main.cpp` | 5 | 19.7 | 13.8 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 81.2 | 78.4 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 40.9 | 40.3 | advanced-ext-index | 0 | 3571 | 极佳 |
| `size:>100m` | 5 | 51.2 | 50.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 136.6 | 132.7 | advanced-trigram | 0.2 | 3626 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 87.4 | 86.7 | advanced-trigram | 0 | 14238 | 正常 |
| `README` | 5 | 34.8 | 29.7 | advanced-trigram | 0 | 16637 | 极佳 |
| `test` | 5 | 58.0 | 51.8 | advanced-trigram | 0 | 116530 | 正常 |
| `MacEverything` | 5 | 8.6 | 0.6 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 13.5 | 13.1 | advanced-pure-filter-soa-gcd | 0 | 0 | **极佳** |
| `content:import` | 5 | 60.5 | 55.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 83.9 | 80.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 44.5 | 44.2 | advanced-linear-gcd | 0 | 0 | **极佳** |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 51.2ms，P90 = 118.8ms，Max = 136.6ms。0/16 查询 >500ms。lockWait max = 0.2ms（忽略）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R185 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **8/16** | 0.5-45ms | R185=7/16（**↑1**） |
| **正常**（50-500ms） | 8/16 | 51-137ms | R185=9/16（↓1） |
| **超标**（>500ms） | 0/16 | — | R185=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,664,483，liveRecordCount=5,112,463（tombstone=**1,552,020**，**23.30%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=209  QUERYADV_GE500=19（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3508
```

- **本轮 0 新增 >500ms 查询（连续 4 轮 0，排除 R184 的 1 个）。**
- **SKIPFLUSH +60——正常节奏。**

## 6. 问题清单

### ⚠️ tombstone 增速午后突增至 603/min

- **R185=371/min → R186=603/min（↑63%）。**
- **tombstone 23.30%（1.55M），距 25% 约 10-12 小时——时间窗口显著缩短！**
- **如果增速持续 ~600/min，距 25% 仅约 10 小时。**
- **P0 compaction 修复紧迫度进一步上升。**

### 观察项：`.pdf`/`hello world` 仍高于正常

- **119ms/137ms——从 R181 的 7193ms/4166ms 恢复 98%+，但距正常稳态（17ms/15ms）仍有差距。**
- **可能是这两个查询的 postings 区域的"结构性温态"——与系统 I/O 活跃区域长期重叠。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（紧迫度升级！）

- **tombstone 23.30%（1.55M），增速突增至 603/min。**
- **距 25% 约 10-12 小时——时间窗口显著缩短！**
- **建议立即启动排查。**

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. tombstone 增速是否从 603/min 回落（验证午后突增是否为一次性）。
2. trigram 均值是否继续从 67ms 收敛至 30-50ms。
3. 全表扫描总和是否保持 <200ms 极低水平。

## 9. 结论

**持续收敛——0/16 >500ms，Max=137ms，P50=52ms，lockWait≈0，极佳 8/16（50%）。** 全表扫描总和 189ms 创系列绝对新低。Trigram 均值 67ms，稳定收敛中。`中文测试`=45ms 再次进入极佳范围。

**⚠️ tombstone 增速午后突增至 603/min——距 25% 约 10-12 小时，时间窗口显著缩短。** 如果增速持续，P0 compaction 修复的紧迫性进一步升级。需要密切关注下一轮增速是否回落。
