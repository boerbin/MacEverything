# MacEverything 性能分析报告 R_2606121017

## 1. 本轮范围

- 任务启动时间：2026-06-12 10:17
- 报告文件：`docs/performance_ana/R_2606121017.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=9244`
  - `SESSION_START_TS=2026-06-10 17:02:22.835`
  - `TOTAL_LINES=10166`
  - `SEG_LINES=923`
  - `LAST_LOG_TS=2026-06-12 10:17`

## 2. 执行摘要

**前台无 >500ms，但 full-scan 仍不稳定；tombstone 接近 8%。** 本轮 16-query battery 中 0/16 >500ms，Max=339.2ms，P50≈28.5ms，lockWait=0。极佳 10/16。`中文测试` 从 244.0ms 回升到 339.2ms，但仍低于 500ms；`path:/Users` 从 326.9ms 回落到 261.6ms。全表扫描总和 820.2ms，较上一轮 751.5ms 上升 9%，仍处于偏高区间。

最新运行段日志显示：`QUERYADV_GE500=5` 无新增，`FLUSH=262`（较上一轮 +12），`SKIPFLUSH=0`。最新 flush 为 rewrite=2369ms、total=2375ms，继续保持 2 秒级完整重写。历史灾难 outlier 仍存在：rewrite 最大 895,940ms，total 最大 896,053ms。tombstone=457,429（7.99%），继续上升并接近 8%。

| 指标 | R_2606120947 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 326.9ms | **339.2ms** | ↑4% |
| P50 | 38.6ms | **28.5ms** | ↓26% |
| lockWait max | 0ms | **0ms** | 持平 |
| 极佳比例(<50ms) | 11/16 | **10/16** | ↓1 |
| 全表扫描总和 | 751.5ms | **820.2ms** | ↑9% |
| recordCount | 5,717,547 | **5,727,213** | +9,666 |
| liveRecordCount | 5,268,797 | **5,269,784** | +987 |
| tombstone | 448,750 (7.85%) | **457,429 (7.99%)** | +8,679 |
| 最新段 FLUSH | 250 | **262** | +12 |
| 最新段 QUERYADV_GE500 | 5 | **5** | 无新增 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 0.6 | 0.0 | short-query-cache | 0.0 | 0 | 极佳 |
| `.pdf` | 5 | 52.5 | 32.7 | advanced-trigram | 0.0 | 9826 | 正常 |
| `main.cpp` | 5 | 8.5 | 2.7 | advanced-trigram | 0.0 | 85 | 极佳 |
| `*.swift` | 5 | 18.2 | 15.0 | advanced-trigram | 0.0 | 9430 | 极佳 |
| `ext:cpp` | 5 | 7.8 | 7.1 | advanced-ext-index | 0.0 | 3616 | 极佳 |
| `size:>100m` | 5 | 41.7 | 41.5 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 极佳 |
| `hello world` | 5 | 29.8 | 27.4 | advanced-trigram | 0.0 | 3930 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 56.7 | 56.1 | advanced-trigram | 0.0 | 14501 | 正常 |
| `README` | 5 | 15.5 | 11.3 | advanced-trigram | 0.0 | 17380 | 极佳 |
| `test` | 5 | 27.1 | 17.8 | advanced-trigram | 0.0 | 117814 | 极佳 |
| `MacEverything` | 5 | 19.6 | 1.8 | advanced-trigram | 0.0 | 6947 | 极佳 |
| `dm:today` | 5 | 53.4 | 53.0 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 219.4 | 206.9 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `path:/Users` | 5 | 261.6 | 253.6 | advanced-linear-gcd | 0.0 | 0 | 正常 |
| `中文测试` | 0 | 339.2 | 338.5 | advanced-linear-gcd | 0.0 | 0 | 正常（偏高） |
| `verylongquery...` | 0 | 0.3 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈28.5ms，P90≈261.6ms，Max=339.2ms。0/16 查询 >500ms。lockWait=0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **10/16** | 0.3-41.7ms | short-query-cache、主要 trigram、ext-index 与 size filter 仍健康 |
| 正常（50-500ms） | **6/16** | 52.5-339.2ms | full-scan 与部分 filter/postings 查询偏高 |
| 超标（>500ms） | **0/16** | — | 本轮无前台超时 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=5,727,213；
- liveRecordCount=5,269,784；
- tombstone=457,429（**7.99%**）。

## 5. 最新日志段分析

```text
SESSION_START_LINE=9244
SESSION_START_TS=2026-06-10 17:02:22.835
TOTAL_LINES=10166
SEG_LINES=923
QUERYADV=69
QUERYADV_GE500=5
FLUSH=262
SKIPFLUSH=0
COMPACTION=2
```

### 慢查询证据

最新 session 内慢查询数量仍为 5 条，本轮未新增 `>=500ms` 慢查询：

```text
[2026-06-10 17:17:09.559] QueryAdvanced "path:/Users" total=525ms | path=advanced-linear-gcd
[2026-06-10 17:25:15.434] QueryAdvanced "lllllllllllllllllllllllllllllllllllllllllllll" total=1154ms | path=advanced-trigram
[2026-06-10 17:47:13.642] QueryAdvanced "中文测试" total=716ms | path=advanced-linear-gcd
[2026-06-10 23:44:25.566] QueryAdvanced "test" total=676ms | path=advanced-trigram
[2026-06-12 09:17:32.430] QueryAdvanced "中文测试" total=555ms | path=advanced-linear-gcd
```

### flush 极端长尾

最新 session 内 `FLUSH=262`，rewrite 范围 **1440ms - 895940ms**，total 最大 **896053ms**。

最新 flush：

```text
[2026-06-12 10:17:02.563] Flushed v6 flat index, lastEventId=347633493,
liveRecords=5269785 | timing: walSwap=0ms rewrite=2369ms sqcache=4ms total=2375ms
```

Top flush outliers：

```text
[2026-06-12 03:15:02.833] rewrite=895940ms sqcache=2ms total=896053ms
[2026-06-10 23:24:50.749] rewrite=725091ms sqcache=18ms total=735223ms
[2026-06-11 14:30:37.596] rewrite=58273ms sqcache=5ms total=101726ms
[2026-06-10 21:16:27.157] rewrite=32497ms sqcache=5ms total=230084ms
[2026-06-10 23:26:19.499] rewrite=27557ms sqcache=10ms total=27834ms
```

## 6. 问题清单

### P0：flush 完整重写仍有分钟级灾难长尾

- 证据：rewrite 最大 895,940ms，total 最大 896,053ms。
- 最新 flush 为 2375ms，仍是秒级完整重写。
- 根因：完整索引重写在 APFS COW、系统 I/O 压力、睡眠/唤醒或后台任务竞争下被放大。

### P0：flush 频率继续上升且没有退避

- 证据：最新 session `FLUSH=262`，较上一轮 +12；`SKIPFLUSH=0`。
- 影响：高频完整重写持续污染 page cache，增加 full-scan 慢查询概率。

### P0：tombstone 接近 8%

- 证据：tombstone=457,429（7.99%），较上一轮增加 8,679。
- 影响：索引体积、flush rewrite 成本和扫描成本继续扩大。

### P1：full-scan 热区仍不稳定

- 证据：全表扫描总和 751.5ms → 820.2ms；`中文测试` 244.0ms → 339.2ms。
- 影响：当前未超 500ms，但已处于偏高区间，下一轮仍有再次超标风险。

### P1：部分 postings/filter 查询轻微偏高

- 证据：`.pdf`=52.5ms，`abc def...`=56.7ms，`dm:today`=53.4ms。
- 判断：postings/SoA 页面可能受到 flush I/O 的部分冷却。

## 7. 优化建议

### P0：flush 改为增量/COW 页写入

- 分离 stable segment 与 delta segment；常规 flush 只追加 delta。
- 后台低优先级合并，不阻塞前台查询。
- 验收：flush rewrite max <200ms，latest session 不再出现分钟级 outlier。

### P0：为 flush 增加 I/O 压力退避和前台保护

- flush 超过阈值后降低下一轮频率。
- 前台查询活跃时推迟大 flush。
- 持久化线程使用更低 QoS，减少对搜索热路径的干扰。

### P0：降低 flush 频率并合并 WAL 批次

- 引入最小 flush 间隔和最大合并窗口。
- 对高 churn 场景减少完整重写次数。

### P1：提前或分段 compaction

- 不等待全局 25% tombstone 阈值。
- 按 segment 或绝对 tombstone 数触发局部 compaction。

### P2：保护搜索热数据 page cache

- 对 flush 大顺序写考虑 `F_NOCACHE` 或同类策略。
- 对搜索热区使用 `madvise` 预热/保持。

## 8. 下一轮重点

1. `中文测试` 是否再次接近或突破 500ms。
2. full-scan 总和是否能回落到 400-700ms 区间。
3. latest session 慢查询计数是否继续保持 5。
4. FLUSH 增长速度是否放缓（当前 262）。
5. tombstone 是否突破 8%（当前 7.99%）。

## 9. 结论

**本轮前台没有新增 >500ms，`中文测试` 保持在 339.2ms，但 full-scan 总和仍偏高。** P50≈28.5ms，极佳 10/16，整体仍可用；但 full-scan 热区不稳定，仍是下一轮最可能触发 >500ms 的路径。

**后台持久化风险继续累积：FLUSH=262、SKIPFLUSH=0、tombstone=457,429（7.99%），最新 flush=2375ms，历史最大 rewrite 895,940ms。** 优先级仍是 COW/增量 flush、I/O 退避、降低 flush 频率、提前或分段 compaction。
