# MacEverything 性能分析报告 R_2606081646

## 1. 本轮范围

- 任务启动时间：2026-06-08 16:46
- 报告文件：`docs/performance_ana/R_2606081646.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=5013`
  - `SEG_LINES=4071`
  - `LAST_LOG_TS=2026-06-08 17:17`

## 2. 执行摘要

**重度 page cache 压力事件——`path:/Users`=2043ms（linear-gcd 系列第二高），`.pdf`=630ms，2/16 >500ms，Max=2043ms，P50=172ms，lockWait=3.6ms，极佳比例 2/16。** 这是继 R176（睡眠/唤醒 Max=2217ms）之后 `path:/Users` 的第二次 >2000ms 事件。与 R188（`path:/Users`=688ms）相比严重程度升级 3x。但与 R176 不同，本轮不是冷启动——SKIPFLUSH 正常增长（+60），是日间系统 I/O 活动导致的 page cache 大规模淘汰。

### 关键指标对比

| 指标 | R189 | R190 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **2/16** | **↑2** |
| Max | 355ms | **2043ms** | **↑ 475%** |
| P50 | 100ms | **172ms** | ↑ 72% |
| lockWait max | 0ms | **3.6ms** | ↑（轻度） |
| 极佳比例(<50ms) | 4/16 | **2/16** | ↓2 |
| tombstone | 1,606,881 (23.86%) | **1,624,967 (23.97%)** | ↑ |
| tombstone 增速 | ~790/min | **~549/min** | **↓ 30%（回落）** |
| recordCount | 6,733,295 | **6,752,508** | +19.2K |
| liveRecordCount | 5,126,414 | **5,127,541** | +1,127 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 3688 | **3748** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 20 | **22** | **↑2（path:/Users, .pdf）** |

### `path:/Users`=2043ms——linear-gcd 重度事件

- **phase2=2009ms，lockWait=0，candidates=0（全扫描）。**
- **系列排名**：R176=600ms → R179=1145ms → R188=688ms → **R190=2043ms**（第二高，仅次 R176 的 2217ms 但那是全冷启动）。
- **日间正常运行中的 2043ms 是系列最严重的 linear-gcd 事件。**
- **2009ms phase2 说明路径数组的 page cache 区域几乎 100% 被淘汰——等效于冷启动。**

### 与其他 linear-gcd 查询对比

| Query | 路径 | R190 | 说明 |
|-------|------|-----:|------|
| `path:/Users` | linear-gcd（路径数组） | **2043ms** | 灾难 |
| `中文测试` | linear-gcd（名称数组） | **249ms** | 正常 |

- **同为 linear-gcd，但扫描不同数据区域——路径数组 vs 名称数组。**
- **路径数组区域被系统 I/O 集中淘汰（可能 Finder/文件管理器操作大量读取路径信息），名称数组区域未受影响。**

### `.pdf`=630ms——trigram 首次日间正常 >500ms（非极端事件）

- **R181=7193ms（选择性淘汰，极端），R184=575ms（午后压力，刚过）。**
- **本轮 630ms——中等严重，phase2=605ms。**
- **与 `path:/Users` 不同的机制：trigram postings 随机访问被 page cache 淘汰。**

### Trigram 路径——全面冷态

| Query | R189 | R190 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 355ms | **630ms** | **↑ 77%（>500ms！）** | 重度冷态 |
| `hello world` | 326ms | **358ms** | ↑ 10% | 冷态 |
| `*.swift` | 195ms | **298ms** | ↑ 53% | 冷态 |
| `main.cpp` | 47ms | **38ms** | ↓ 19% | 极佳 |
| `abc def...` | 146ms | **181ms** | ↑ 24% | 冷态 |
| `README` | 71ms | **86ms** | ↑ 21% | 正常 |
| `test` | 172ms | **98ms** | ↓ 43% | 正常 |
| `MacEverything` | 29ms | **46ms** | ↑ 59% | 极佳边缘 |
| Trigram 均值 | 168ms | **217ms** | **↑ 29%** | 全面恶化 |

- **Trigram 均值 217ms（R189=168ms，↑29%）——持续恶化趋势。**
- **`.pdf`=630ms 突破 500ms，是本轮第二个 >500ms。**
- **`test`=98ms（R189=172ms，↓43%）——不均匀，R189 battery 预热了 test 的 postings 区域。**

### 全表扫描——被 `path:/Users` 拉爆

| Query | R189 | R190 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 195ms | **262ms** | ↑ 34% |
| `path:/Users` | 249ms | **2043ms** | **↑ 720%** |
| `中文测试` | 236ms | **249ms** | ↑ 6% |
| **总和** | **681ms** | **2554ms** | **↑ 275%** |

- **全表扫描总和 2554ms——被 `path:/Users` 的 2043ms 主导。**
- **`content:import`=262ms 和 `中文测试`=249ms 轻度上升——受影响但不严重。**

### SoA Filter——上升

| Query | R189 | R190 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 100ms | **173ms** | ↑ 73% |
| `dm:today` | 77ms | **79ms** | 持平 |

- **`size:>100m`=173ms——SoA 全扫描区域也受压。**

### lockWait=3.6ms 轻度重现

- **`中文测试`=3.1ms，`verylongquery`=3.6ms。**
- **轻度——远低于 R184=19.8ms 和 R179=64ms。**
- **说明 flush timer 在系统 I/O 压力增加时轻度竞争。**

### `x`=37ms——进程调度延迟

- **`x` 使用 short-query-cache（进程内存）。**
- **37ms 表示 ~35ms 的进程调度延迟。**
- **与系统 I/O 压力一致——进程被密集 I/O 降低调度优先级。**

### ⚠️ tombstone 23.97%——逼近 24%

- **tombstone=1,624,967（23.97%）——距 24% 仅 0.03 个百分点。**
- **增速从 R189 的 790/min 回落至 549/min——但仍远高于正常（~300/min）。**
- **距 25% 约 5-7 小时（549/min 下）。**
- **若增速再次飙升，可能更快。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 36.8 | 0.0 | short-query-cache | 0 | 0 | 极佳（调度延迟） |
| `.pdf` | 5 | 629.9 | 605.2 | advanced-trigram | 0 | 9842 | **>500ms** |
| `main.cpp` | 5 | 37.8 | 22.3 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 297.9 | 288.3 | advanced-trigram | 0 | 9345 | 冷态 |
| `ext:cpp` | 5 | 117.3 | 113.0 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 172.7 | 172.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 357.5 | 351.7 | advanced-trigram | 0 | 3638 | 冷态 |
| `abc def ghi jkl mno pqr` | 0 | 181.1 | 179.3 | advanced-trigram | 0 | 14283 | 冷态 |
| `README` | 5 | 85.6 | 74.2 | advanced-trigram | 0 | 16691 | 正常 |
| `test` | 5 | 98.4 | 82.9 | advanced-trigram | 0 | 116588 | 正常 |
| `MacEverything` | 5 | 46.3 | 1.9 | advanced-trigram | 0 | 6859 | 极佳边缘 |
| `dm:today` | 5 | 78.8 | 72.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 261.7 | 250.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 2042.6 | 2009.3 | advanced-linear-gcd | 0 | 0 | **>500ms（系列第二高）** |
| `中文测试` | 0 | 248.8 | 241.4 | advanced-linear-gcd | 3.1 | 0 | 正常 |
| `verylongquery...` | 0 | 6.8 | 0.0 | advanced-trigram | 3.6 | 0 | 极佳 |

**P50 = 172.7ms，P90 = 357.5ms，Max = 2042.6ms。2/16 查询 >500ms。lockWait max = 3.6ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R189 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 2/16 | 6.8-46ms | R189=4/16（↓2） |
| **正常**（50-500ms） | 12/16 | 79-358ms | R189=12/16（持平） |
| **超标**（>500ms） | **2/16** | 630-2043ms | R189=0/16（**↑2**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,752,508，liveRecordCount=5,127,541（tombstone=**1,624,967**，**23.97%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=244  QUERYADV_GE500=22（+2——path:/Users=2043ms, .pdf=630ms）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3748
```

- **QUERYADV_GE500 +2。**
- **SKIPFLUSH +60——正常节奏。**

## 6. 问题清单

### P0 风险：tombstone 23.97%——25% 在 5-7 小时内

- **距 24% 仅 0.03 个百分点——下一轮预计突破 24%。**
- **增速 549/min（回落但仍偏高）——距 25% 约 5-7 小时。**
- **compaction 触发后若 rewrite 崩溃，索引可能不可用。**

### `path:/Users`=2043ms——日间 linear-gcd 极端事件

- **系列日间最严重——等效于冷启动级别的 page cache 淘汰。**
- **路径数组区域与系统文件操作（Finder/IDE）的 I/O 区域高度重叠。**
- **预计 1 轮恢复。**

## 7. 优化建议

### P0：立即排查 compaction rewrite 崩溃根因

- **tombstone 23.97%，25% 在 5-7 小时内。**
- **如果不修复，compaction 触发时可能导致索引不可用。**
- **紧迫度：小时级。**

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：热区维护策略（持续建议）

- `path:/Users` 的路径数组应纳入热区维护——日间多次 >500ms。

### P3：trigram postings 布局优化（持续建议）

## 8. 下一轮重点

1. `path:/Users` 和 `.pdf` 是否从 >500ms 恢复。
2. tombstone 是否突破 24%。
3. 系统 I/O 压力是否随傍晚消退。

## 9. 结论

**重度 page cache 压力事件——`path:/Users`=2043ms（日间最严重 linear-gcd 事件），`.pdf`=630ms，2/16 >500ms，lockWait=3.6ms。** 路径数组区域被系统 I/O 几乎完全淘汰出 page cache——等效冷启动。与 R176 不同，这是日间活动（非睡眠/唤醒）导致的。

**tombstone 23.97%——即将突破 24%，距 25% compaction 阈值约 5-7 小时。** P0 compaction rewrite 崩溃修复已进入小时级倒计时。如果不修复，compaction 触发时可能导致索引不可用。
