# MacEverything 性能分析报告 R_2605301311

- **任务启动时间**：2026-05-30 13:11
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~26.1h，未重启
- **状态快照 @13:11**：total `5,855,871` / live `5,391,039`
- **本轮定性**：⚠️ **连续第三轮污染 + 午间停顿开始消退 + 史上最窄间隙 17/17 不破**——午间停顿越过峰值后回落：`*.swift` 呈 108ms(11:41)→237ms(12:11)→**1002ms(12:41,峰)**→**618ms(13:11,回落)** 的「升后降」签名，本轮停顿（`*.swift` 618、`hello world` 861）**不再越过 1s**。`main.cpp` 71 候选却 192ms、缓存 `x` 3.46ms 触发 R88 复测协议，全部恢复常态。洁净后出现「**双向挤压**」：数值层天花板 dm **真冷 13.56**、字符串层地板 content **偏暖 28.56**，间隙压到史上最窄 **2.11×**，但二层分裂**仍 17/17 不破**——这是迄今对分裂机制级稳健性最严苛的一次通过。

---

## 1. 原始电池（含污染，⚠️ 标记异常项）

| query | path | total(ms) | phase2 | trig | cand | 备注 |
|---|---|---|---|---|---|---|
| `x` | short-query-cache | **3.46** | 0 | 0 | 0 | ⚠️ 缓存常态 <1ms（复测 0.01）|
| `.pdf` | advanced-trigram | 281.73 | 280.25 | 1.02 | 10121 | 受污染偏高 |
| `main.cpp` | advanced-trigram | **192.74** | 60.49 | 132.17 | 71 | ⚠️ 71 候选 192ms 不可能（复测 0.59）|
| `*.swift` | advanced-trigram | **618.88** | 615.69 | 3.07 | 9213 | ⚠️ **午间停顿（复测 1.33）**|
| `ext:cpp` | advanced-ext-index | **245.55** | 244.14 | 0 | 2987 | ⚠️ 常态 ~15ms（复测 0.26）|
| `size:>100m` | pure-filter-soa-gcd | **104.86** | 104.80 | 0 | 0 | ⚠️ 受污染（复测中位 28.5）|
| `hello world` | advanced-trigram | **861.17** | 855.55 | 5.56 | 2340 | ⚠️ **本轮最高停顿**|
| `abc def ghi jkl mno pqr` | advanced-trigram | 58.28 | 58.16 | 0.06 | 15923 | |
| `README` | advanced-trigram | 13.95 | 10.49 | 3.34 | 16463 | 未污染 |
| `test` | advanced-trigram | 28.59 | 15.79 | 12.49 | 136264 | 未污染 |
| `MacEverything` | advanced-trigram | 12.77 | 3.74 | 8.98 | 6697 | |
| `dm:today` | pure-filter-soa-gcd | 48.10 | 48.03 | 0 | 0 | 数值，复测**更高**（真冷）|
| `content:import` | pure-filter-soa-gcd | 180.31 | 171.68 | 0 | 0 | 字符串，复测一致 |
| `path:/Users` | advanced-linear-gcd | 174.11 | 170.44 | 0 | 0 | 字符串，复测一致 |
| `中文测试` | advanced-linear-gcd | 204.68 | 204.22 | 0.18 | 0 | 字符串，复测略高 |
| `verylongquery…really` | advanced-trigram | 1.15 | 0 | 1.14 | 0 | |

---

## 2. 复测消歧（套用 R88 协议）

**污染标志项（确认瞬时，全部恢复）：**

| query | 电池值 | probe1 | probe2 | probe3 | 判定 |
|---|---|---|---|---|---|
| `x`（缓存）| 3.46 | 0.10 | 0.01 | 0.02 | ✅ 瞬时 |
| `main.cpp`（71 cand）| 192.74 | 2.63 | 0.59 | 0.78 | ✅ 瞬时 |
| `*.swift` | 618.88 | 25.66 | 1.81 | 1.33 | ✅ 瞬时（午间停顿）|
| `ext:cpp` | 245.55 | 17.0 | 0.53 | 0.26 | ✅ 恢复常态 |

**数值/纯过滤项（4× 取中位，区分污染 vs 真冷）：**

| query | 电池值 | 复测中位 | 判定 |
|---|---|---|---|
| `size:>100m` | 104.86 | **28.5** | ⚠️ 受污染，恢复数值地板 |
| `dm:today` | 48.10 | **79.4** | 真冷（复测一致偏高，非污染）|
| `content:import` | 180.31 | **167.3** | 真实（复测一致）|
| `path:/Users` | 174.11 | **179.0** | 真实（复测一致）|
| `中文测试` | 204.68 | **243.2** | 真实（复测一致偏高）|

注：`dm` 复测（73–95ms）**高于**电池（48ms），与 R89 同向——电池抓到 dm 暖瞬间、复测抓到其冷态。**dm 连续两轮（R89+R90）真冷**，是数值层内部逐列温度，非污染。污染只砸在与停顿同时执行的 `size`/`*.swift`/`main.cpp`/`ext:cpp`/`x` 上。

---

## 3. 洁净 ns/槽（records=5,855,871）

| 路径 | 类型 | 洁净值 | 来源 |
|---|---|---|---|
| `size:>100m` | 数值 | **4.86** | 复测中位 28.5ms（数值地板）|
| `dm:today` | 数值 | **13.56** | 复测中位 79.4ms（本轮真冷，数值天花板）|
| `content:import` | 字符串 | **28.56** | 复测中位 167.3ms（字符串地板）|
| `path:/Users` | 字符串 | **30.56** | 复测中位 179.0ms |
| `中文测试` | 字符串 | **41.53** | 复测中位 243.2ms |

---

## 4. 绝对二层分裂：洁净数据 17/17 不破（史上最窄间隙，最严苛通过）

- **数值层**：size 4.86、dm 13.56 → 最高数值 **dm 13.56**
- **字符串层**：content 28.56、path 30.56、中文 41.53 → 最低字符串 **content 28.56**
- **洁净间隙 = 28.56 / 13.56 = 2.11×**，无重叠。**刷新历史最窄**（前最窄 R87 2.41×）。

**为何本轮是最严苛通过**：间隙之所以压到 2.11×，是因为本轮恰逢「**双向挤压**」——数值层天花板（dm）正处真冷高位（13.56），字符串层地板（content）正处偏暖低位（28.56），两层的「内缘」同时朝中间靠拢。即便在这种最不利的冷热组合下，**真冷的最高数值（13.56）仍稳稳低于偏暖的最低字符串（28.56）**。这比 R87 的广域冷（两层同向冷化）更严苛，因为本轮是两层「对向逼近」。分裂依然不破，再次证明它是**机制级恒定差**（纯数值零解引用 vs 字符串解引用散布串池），而非冷热样本巧合。

**数值层内部**：dm（13.56）连续两轮为天花板、size（4.86）连续两轮为地板，方向稳定——R81 分区机制下探到单数值列粒度的持续复现（dm 列与 size 列独立冷热，近两轮 dm 恒冷于 size）。补充观察，不构成新结论。

---

## 5. 环境信号：午间停顿越过峰值、开始消退（外部，非 MacEverything 回归）

把 `*.swift`（trigram，候选恒定 9213）四轮日志并排：

| 时刻 | `*.swift` total | 性质 |
|---|---|---|
| 11:41（R87）| 108ms | 正常 |
| 12:11（R88）| 237ms | 轻度停顿 |
| 12:41（R89）| **1002ms** | **重度停顿（峰值）** |
| **13:11（R90）** | **618ms** | **回落中** |

**关键转折**：午间停顿在 12:41 达峰（1002ms）后开始消退，本轮 `*.swift` 已回落到 618ms。更直接的证据是延迟分布——本轮两个停顿尖峰（`*.swift` 618ms、`hello world` 861ms）**双双落在 0.5-1s 段，不再越过 1s**（R88 的 1663ms、R89 的 1002ms 都曾进入 1-5s 段，本轮 1-5s 段计数停在 23 未增）。

这与「外部进程一次性事件」（疑似午间备份 / Spotlight 重建 / 内存压力）的生命周期完全吻合：12:00 起爬升 → 12:41 达峰 → 13:11 消退。**边界不变**：这是 MacEverything 所处**运行环境**的信号，不是代码回归（数据冷化无法解释 ext-index 0.26ms 和缓存 `x` 0.01ms 的瞬时恢复）。但连续三轮命中，持续强化下文 P0 的韧性需求。

---

## 6. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **431 次**（R89 419）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100，`:159` tombstone 比例阀结构性永不可达——全会话 0 触发、0 回收。

---

## 7. tombstone 趋势：连续 23 轮单调上升，逼近 8%

| 轮次 | tombstone | ratio |
|---|---|---|
| R86@11:11 | 417,598 | 7.19% |
| R87@11:41 | 427,763 | 7.36% |
| R88@12:11 | 439,394 | 7.54% |
| R89@12:41 | 455,464 | 7.79% |
| **R90@13:11** | **464,832** | **7.94%** |

会话内单调递增。R43 参考崩溃点 12.97%（~800K），当前 7.94% 已逼近 8%、过 6 成程。live 基本持平（5,391,039），约 96% 新增记录变为 tombstone。

---

## 8. 日志健康度

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **282 条**（R89 261）——连续午间污染推高越线计数
- Top-7 峰值 `[18582, 17928, 15749, 8205, 7544, 6811, 6061]` —— 仍与历史**逐字节相同**；本轮最高 861ms `hello world` 停顿远低于这些冻结历史峰值
- 延迟分布：>5s=**7**（冻结）、1-5s=**23**（R89 23，**本轮未增**——停顿未越 1s）、0.5-1s=**16**（R89 14，+2 即本轮两个停顿尖峰）、0.1-0.5s=236、中位 **222ms**
- 本轮污染尖峰首次**全部止于亚秒级**（618/861ms），与午间停顿消退一致

---

## 9. 优化建议（四项 P0，连续三轮污染持续强化第 2、4 项）

1. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调 <12.97%（建议 8–10%）。当前 7.94% 逼近 8%，紧迫性持续上升。
2. **🔥 P0｜mlock/madvise 整体常驻热区（连续三轮污染强化）**：午间停顿连续三轮命中（R88/R89/R90），证明无内存保护时外部系统压力会反复拖慢任意路径。需把 path/name 串池、content 列、trigram 索引、候选行、数值 SoA 列整体钉入内存，减小被换出/停顿波及面。
3. **🔥 P0｜无锁快照 flush**：启动 flush 持锁 10.3s。
4. **🔥 P0｜查询超时保护（连续三轮实证强化）**：R88（1663ms）+ R89（1002ms）+ R90（861ms）连续三轮真实停顿——超时熔断 + 降级返回是抵御外部系统停顿的必要韧性机制，已有三个真实触发样本。
- **P0｜HTTP 并发**：acceptLoop 单线程，停顿期间串行阻塞放大影响。
- **P1🔥｜CJK trigram 索引**：`中文测试` 走 linear-gcd 全扫（243ms / 41.53 ns/槽，本轮字符串最差）。

---

## 10. 结论

- **定性**：⚠️ 连续第三轮污染 + 午间停顿消退 + 史上最窄间隙最严苛通过。午间停顿越过峰值（12:41 的 1002ms）后回落（13:11 的 618ms），本轮停顿首次全部止于亚秒级。
- **稳健结论最严苛通过 17/17**：本轮「双向挤压」（数值天花板 dm 真冷 13.56 + 字符串地板 content 偏暖 28.56）把间隙压到史上最窄 2.11×，但分裂仍无重叠——比 R87 广域冷更严苛（两层对向逼近 vs 同向冷化），是机制级恒定差的最强证明。
- **诚实修正连续生效**：连续三轮均未把污染样本误记为机制异常；dm 连续两轮真冷为天花板、size 连续两轮为地板，是 R81 分区机制列粒度的稳定复现，不构成新结论。
- **新环境信号（明确边界）**：捕获午间停顿的完整生命周期——爬升(12:00)→达峰(12:41)→消退(13:11)，属运行环境而非代码回归，但三轮连续命中持续强化应用韧性需求（超时保护 + mlock）。
- **唯一持续恶化项**：tombstone 7.94%（连续 23 轮单调，逼近 8%），根因 R67 阀门不可达明确。
