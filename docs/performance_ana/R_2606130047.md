# MacEverything 性能分析报告 R_2606130047

## 1. 本轮范围

- 任务启动时间：2026-06-13 00:47
- 报告文件：`docs/performance_ana/R_2606130047.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=9244`
  - `SESSION_START_TS=2026-06-10 17:02:22.835`
  - `TOTAL_LINES=11310`
  - `SEG_LINES=2067`
  - `LAST_LOG_TS=2026-06-13 00:47`

## 2. 执行摘要

**本轮前台未出现 >500ms 查询：16-query battery 中 0/16 >500ms，Max 从 411.8ms 降至 303.3ms。** 相比 R_2606130017，扫描类路径有所恢复：`content:import` 从 350.5ms 降至 200.8ms，`path:/Users` 从 279.5ms 降至 174.6ms，`中文测试` 从 411.8ms 降至 280.8ms，full-scan 总和从 1041.8ms 降至 656.2ms。但 trigram/postings 路径同步变慢：`.pdf`=119.4ms，`main.cpp`=96.2ms，`*.swift`=303.3ms，`hello world`=115.1ms；本轮还首次在 `.pdf` 观测到 `lockWaitMs=36.5ms`。

后台结构性风险继续升级：tombstone=673,212（11.29%），较上一轮增加 13,488；latest session `FLUSH=490`（较上一轮 +12），`SKIPFLUSH=0`。最新 flush 为 rewrite=2858ms、sqcache=250ms、total=3109ms，仍是秒级完整重写，并且 sqcache 阶段明显高于近期常见的个位数毫秒。latest session 仍包含 13:02 的 2,285,536ms 灾难 flush、03:15 的 896,053ms flush、19:06 的 107,008ms flush。

| 指标 | R_2606130017 | 本轮 | 变化 |
|------|-------------:|-----:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 411.8ms | **303.3ms** | ↓26% |
| P50 | ≈34.3ms | **≈78.5ms** | ↑129% |
| P90 | ≈350.5ms | **≈280.8ms** | ↓20% |
| lockWait max | 0ms | **36.5ms** | 新增 |
| 极佳比例(<50ms) | 10/16 | **7/16** | -3 |
| 全表扫描总和 | 1041.8ms | **656.2ms** | ↓37% |
| recordCount | 5,947,457 | **5,962,606** | +15,149 |
| liveRecordCount | 5,287,733 | **5,289,394** | +1,661 |
| tombstone | 659,724 (11.09%) | **673,212 (11.29%)** | +13,488 |
| 最新段 FLUSH | 478 | **490** | +12 |
| 最新段 QUERYADV_GE500 | 11 | **11** | 无新增 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|-----------:|-----------:|------|
| `x` | 5 | 41.6 | 0.0 | short-query-cache | 0.0 | 0 | 极佳但偏高 |
| `.pdf` | 5 | 119.4 | 65.5 | advanced-trigram | 36.5 | 9856 | 正常（出现锁等待） |
| `main.cpp` | 5 | 96.2 | 66.4 | advanced-trigram | 0.0 | 91 | 正常（偏高） |
| `*.swift` | 5 | 303.3 | 270.7 | advanced-trigram | 0.0 | 9495 | 正常（本轮最高） |
| `ext:cpp` | 5 | 43.4 | 42.3 | advanced-ext-index | 0.0 | 3702 | 极佳 |
| `size:>100m` | 5 | 80.3 | 70.9 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `hello world` | 5 | 115.1 | 98.2 | advanced-trigram | 0.0 | 3930 | 正常（偏高） |
| `abc def ghi jkl mno pqr` | 0 | 76.6 | 76.1 | advanced-trigram | 0.0 | 14576 | 正常 |
| `README` | 5 | 20.8 | 12.1 | advanced-trigram | 0.0 | 17523 | 极佳 |
| `test` | 5 | 34.6 | 11.6 | advanced-trigram | 0.0 | 117880 | 极佳 |
| `MacEverything` | 5 | 18.0 | 0.7 | advanced-trigram | 0.0 | 6980 | 极佳 |
| `dm:today` | 5 | 63.8 | 62.9 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常 |
| `content:import` | 5 | 200.8 | 190.4 | advanced-pure-filter-soa-gcd | 0.0 | 0 | 正常（恢复） |
| `path:/Users` | 5 | 174.6 | 165.6 | advanced-linear-gcd | 0.0 | 0 | 正常（恢复） |
| `中文测试` | 0 | 280.8 | 271.3 | advanced-linear-gcd | 0.0 | 0 | 正常（偏高） |
| `verylongquery...` | 0 | 1.0 | 0.0 | advanced-trigram | 0.0 | 0 | 极佳 |

**P50≈78.5ms，P90≈280.8ms，Max=303.3ms。0/16 查询 >500ms。lockWait max=36.5ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 说明 |
|------|------:|---------:|------|
| 极佳（<50ms） | **7/16** | 1.0-43.4ms | short-query-cache、ext-index、小结果 trigram 和 long miss 仍可用 |
| 正常（50-500ms） | **9/16** | 63.8-303.3ms | trigram/postings、pure-filter、linear-gcd 多路径同步偏高 |
| 超标（>500ms） | **0/16** | — | 本轮无前台超时 |

## 4. 服务状态

- `localhost:19860` HTTP 服务正常；
- recordCount=5,962,606；
- liveRecordCount=5,289,394；
- tombstone=673,212（**11.29%**）；
- contentIndexedFileCount=0。

## 5. 最新日志段分析

```text
SESSION_START_LINE=9244
SESSION_START_TS=2026-06-10 17:02:22.835
TOTAL_LINES=11310
SEG_LINES=2067
QUERYADV=159
QUERYADV_GE500=11
FLUSH=490
SKIPFLUSH=0
COMPACTION=2
LAST_LINE=[2026-06-13 00:47:59.608] QueryAdvanced "中文测试" total=271ms | path=advanced-linear-gcd
```

### 慢查询证据

最新 session 内 `QueryAdvanced >=500ms` 数量仍为 11，本轮未新增日志级慢查询。扫描类路径较上一轮恢复，但 `*.swift` 达到 303.3ms，`.pdf` 出现 36.5ms lockWait，说明本轮波动从 full-scan 转移到 postings/trigram 与局部锁等待：

```text
[2026-06-10 17:17:09.559] QueryAdvanced "path:/Users" total=525ms | path=advanced-linear-gcd
[2026-06-10 17:25:15.434] QueryAdvanced "lllllllllllllllllllllllllllllllllllllllllllll" total=1154ms | path=advanced-trigram
[2026-06-10 17:47:13.642] QueryAdvanced "中文测试" total=716ms | path=advanced-linear-gcd
[2026-06-10 23:44:25.566] QueryAdvanced "test" total=676ms | path=advanced-trigram
[2026-06-12 09:17:32.430] QueryAdvanced "中文测试" total=555ms | path=advanced-linear-gcd
[2026-06-12 13:53:09.945] QueryAdvanced ".pdf" total=1955ms | path=advanced-trigram
[2026-06-12 13:53:12.523] QueryAdvanced "path:/Users" total=590ms | path=advanced-linear-gcd
[2026-06-12 14:48:11.578] QueryAdvanced "hello world" total=760ms | path=advanced-trigram
[2026-06-12 16:21:11.617] QueryAdvanced ".pdf" total=529ms | path=advanced-trigram
[2026-06-12 16:21:12.485] QueryAdvanced "ext:cpp" total=591ms | path=advanced-ext-index
[2026-06-12 16:21:16.446] QueryAdvanced "hello world" total=3724ms | path=advanced-trigram
```

### flush 极端长尾

最新 session 内 `FLUSH=490`，rewrite 范围 **1380ms - 2285476ms**，total 最大 **2285536ms**。

最新 flush：

```text
[2026-06-13 00:46:45.175] Flushed v6 flat index, lastEventId=353788390,
liveRecords=5289383 | timing: walSwap=0ms rewrite=2858ms sqcache=250ms total=3109ms
```

Top flush outliers：

```text
[2026-06-12 13:02:35.416] rewrite=2285476ms sqcache=6ms total=2285536ms
[2026-06-12 03:15:02.833] rewrite=895940ms sqcache=2ms total=896053ms
[2026-06-10 23:24:50.749] rewrite=725091ms sqcache=18ms total=735223ms
[2026-06-12 19:06:52.432] rewrite=106972ms sqcache=34ms total=107008ms
[2026-06-11 14:30:37.596] rewrite=58273ms sqcache=5ms total=101726ms
```

## 6. 问题清单

### P0：tombstone 继续升高到 11.29%

- 证据：tombstone=673,212（11.29%），较上一轮增加 13,488。
- 影响：索引体积、flush rewrite 成本、扫描成本和 compaction 压力继续扩大。
- 判断：tombstone 已持续穿越 11% 区间，必须提前或分段治理。

### P0：flush 高频且没有退避

- 证据：`FLUSH=490`，较上一轮 +12；`SKIPFLUSH=0`；最新 flush total=3109ms。
- 影响：秒级完整重写持续污染 page cache，是前台冷热波动的主要背景风险。

### P0：latest session 仍包含灾难级 flush outlier

- 证据：13:02 flush total=2,285,536ms，03:15 flush total=896,053ms，19:06 flush total=107,008ms。
- 影响：持久化路径在 I/O 压力下可被极端放大，前台查询当前未超标不代表根因消失。

### P1：trigram/postings 路径本轮明显偏高

- 证据：`*.swift`=303.3ms，`.pdf`=119.4ms，`hello world`=115.1ms，`main.cpp`=96.2ms。
- 影响：高候选 postings 受热数据状态影响明显，说明波动不只发生在 full-scan。

### P1：出现局部锁等待

- 证据：`.pdf` lockWaitMs=36.5ms，其余查询 lockWait=0。
- 影响：不是整体锁竞争，但需要跟踪是否由 flush、索引更新或短时读写锁交叠引起。

### P1：sqcache flush 阶段异常升高

- 证据：最新 flush `sqcache=250ms`，显著高于近期常见的个位数到几十毫秒。
- 影响：sqcache 写入也可能在局部时段放大 flush 总耗时，需纳入持久化路径拆分观测。

## 7. 优化建议

### P0：flush 改为增量/COW 页写入

- 分离 stable segment 与 delta segment；常规 flush 只追加 delta。
- 后台低优先级合并，不阻塞前台查询。
- 验收：flush rewrite max <200ms，latest session 不再出现分钟级 outlier。

### P0：为 flush 增加 I/O 压力退避和前台保护

- flush 超过阈值后降低下一轮频率。
- 前台查询活跃时推迟大 flush。
- 持久化线程使用更低 QoS，减少对搜索热路径的干扰。

### P0：提前或分段 compaction

- 不等待全局 25% tombstone 阈值。
- 按 segment 或绝对 tombstone 数触发局部 compaction。
- 验收：tombstone 率从 11%+ 回落，并且常规 flush rewrite 时间下降。

### P0：降低 flush 频率并合并 WAL 批次

- 引入最小 flush 间隔和最大合并窗口。
- 对高 churn 场景减少完整重写次数。

### P1：补充 postings/trigram 与 lockWait 观测

- 对 postings 读取、候选集构造、排序、结果 materialization 分别打点。
- 记录每次查询锁等待对应的后台 flush/update 状态，判断 `.pdf` lockWait 是否偶发。

### P1：拆分 sqcache flush 耗时

- 单独记录 sqcache 序列化、写入、fsync/rename 阶段。
- 验收：能解释 `sqcache=250ms` 是数据量增长、I/O 抖动还是同步写入造成。

### P2：保护搜索热数据 page cache

- 对 flush 大顺序写考虑 `F_NOCACHE` 或同类策略。
- 对 full-scan/SoA/postings 热区使用 `madvise` 预热/保持。

## 8. 下一轮重点

1. tombstone 是否继续高于 11.4%。
2. `*.swift` 是否回落到 <100ms。
3. `.pdf` 是否再次出现 lockWait。
4. 最新 flush 的 sqcache 是否继续高于 100ms。
5. 是否出现新的分钟级 flush outlier。

## 9. 结论

**本轮前台从上一轮 full-scan near-threshold 状态恢复：0/16 >500ms，Max=303.3ms，full-scan 总和=656.2ms。** 但 P50 升至约 78.5ms，`*.swift`、`.pdf`、`hello world` 等 trigram/postings 路径偏高，并出现 `.pdf` lockWait=36.5ms。

**后台风险继续升级：tombstone=673,212（11.29%），`FLUSH=490`，最新 flush=3109ms 且 sqcache=250ms，latest session 仍包含 total=2,285,536ms、896,053ms、107,008ms 的极端 flush outlier。** 当前最优先的修复仍是 COW/增量 flush、I/O 退避、提前或分段 compaction、降低 flush 频率，并补充 postings/lockWait/sqcache 的分阶段观测。
