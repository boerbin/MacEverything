# MacEverything 性能分析报告 R_2606031329

## 1. 本轮范围

- 任务启动时间：2026-06-03 13:29
- 报告文件：`docs/performance_ana/R_2606031329.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=21027`
  - `SESSION_START_TS=2026-06-03 09:17:33.973`
  - `TOTAL_LINES=21355`
  - `SEG_LINES=329`
  - `LAST_LOG_TS=2026-06-03 16:11`

## 2. 执行摘要

服务正常运行（第 14 轮连续可用）。`*.swift`=488ms 成为最接近 500ms 阈值的查询（差 12ms），`x` 再次异常偏慢（93ms，通常 <20ms）。所有查询连续第 11 轮 < 500ms。tombstone 继续增长至 149,178 条（2.54%），连续第 3 轮超过 2%。

### 横向对比（最近 5 轮）

| Query | R58 | R59 | R60 | R61 | R62 本轮 | 趋势 |
|-------|-----:|-----:|-----:|-----:|--------:|------|
| `x` | 254 | 19 | 14 | 7 | **93** | 再次异常 |
| `.pdf` | 488 | 490 | 349 | 231 | **325** | 改善 |
| `*.swift` | — | 226 | 179 | 172 | **488** | **接近阈值** |
| `hello world` | 425 | 430 | 436 | 252 | **297** | 改善 |
| `path:/Users` | 312 | 164 | 278 | 227 | **255** | 正常 |
| P50 | 163 | 176 | 178 | 147 | **148** | 稳定 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 92.8 | 0.0 | short-query-cache | 0 | 0 | 再次偏慢 |
| `.pdf` | 5 | 324.5 | 317.8 | advanced-trigram | 0 | 9944 | 正常 |
| `main.cpp` | 5 | 44.2 | 37.1 | advanced-trigram | 0 | 85 | 快 |
| `*.swift` | 5 | 487.5 | 481.7 | advanced-trigram | 0 | 9356 | **接近 500ms** |
| `ext:cpp` | 5 | 96.1 | 95.3 | advanced-ext-index | 0 | 3702 | 正常 |
| `size:>100m` | 5 | 161.9 | 161.6 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 297.0 | 288.3 | advanced-trigram | 0 | 3613 | 改善 |
| `abc def ghi jkl mno pqr` | 0 | 260.5 | 258.8 | advanced-trigram | 0 | 15981 | 正常 |
| `README` | 5 | 26.6 | 20.1 | advanced-trigram | 0 | 17717 | 快 |
| `test` | 5 | 35.7 | 27.8 | advanced-trigram | 0 | 129960 | 快 |
| `MacEverything` | 5 | 34.0 | 1.4 | advanced-trigram | 0 | 6802 | 快 |
| `dm:today` | 5 | 69.2 | 69.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 147.5 | 135.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 254.9 | 242.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 275.6 | 270.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 5.9 | 0.0 | advanced-trigram | 0 | 0 | 极快 |

**P50 ≈ 148ms，P90 ≈ 325ms，Max = 488ms。全部查询连续第 11 轮 < 500ms（`*.swift` 仅差 12ms）。lockWaitMs 全部为 0。**

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=5,874,235，liveRecordCount=5,725,057（tombstone=**149,178**，**2.54%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=1  ERROR=0
QUERYADV=111  QUERYADV_GE500=5
FLUSH=1  COMPACTION=2  SKIPFLUSH=96
DIAG_TRIGRAM_SKIP=67
```

- QUERYADV 从 103 增至 111（+8）。
- QUERYADV_GE500 保持 5（`*.swift` 488ms 未超阈值）。
- SKIPFLUSH 从 84 增至 96（+12）。
- DIAG_TRIGRAM_SKIP 从 66 增至 67（+1）。

## 6. 问题清单

### Critical：tombstone 连续 3 轮超过 2%（2.54%，149,178 条）

- 增长轨迹：R60=128K(2.19%) → R61=138K(2.36%) → R62=149K(2.54%)。
- 本轮增量 10,888 条。
- SKIPFLUSH=96，compaction 仍然完全不可触发。
- 按当前速率，约 10 轮后将达 3%（~200K 条）。

### High：`*.swift` 接近 500ms 阈值（488ms）

- phase2=482ms，candidates=9356。与 `.pdf`（9944 candidates）同属高候选数查询。
- 这是 `*.swift` 首次接近 500ms，此前通常 60-180ms。
- I/O 负载导致 phase2 验证变慢是直接原因。

### Medium：`x` 短查询缓存再次偏慢（93ms）

- 14 轮数据中出现 3 次异常：254ms（R58）、93ms（本轮）。
- phase2=0，lockWaitMs=0，延迟在缓存层面。
- 可能与 GCD 调度或缓存数据序列化开销有关。

## 7. 优化建议

### P0：实现基于占比的主动 tombstone compaction（已超阈值 3 轮）

- tombstone 2.54%，每轮增量 ~10K 条。
- 建议在 SKIPFLUSH 检查点增加 tombstone 占比检查，>2% 时触发 compaction。

### P0：补齐进程生命周期监控（承前轮）

### P1：将 `.xxx`/`*.xxx` 扩展名模式路由到 ext-index

- `*.swift` 488ms（9356 candidates）和 `.pdf` 325ms（9944 candidates）均应走 ext-index。
- `ext:cpp` 仅 96ms（3702 candidates），证明 ext-index 路径显著更快。

### P1：调查 short-query-cache 偶发延迟

- `x` 出现第 3 次异常（254ms、93ms），需排查缓存重建或 GCD 调度问题。

## 8. 下一轮重点

1. tombstone 增长速率——是否加速。
2. `*.swift` 和 `.pdf` 是否突破 500ms。
3. `x` 缓存路径是否继续偶发偏慢。

## 9. 结论

服务连续第 14 轮正常运行，所有查询连续第 11 轮 <500ms（`*.swift`=488ms 仅差 12ms）。tombstone 连续第 3 轮超过 2%（149,178 条，2.54%），SKIPFLUSH=96。`x` 短查询缓存再次偶发偏慢（93ms，第 3 次异常）。`*.swift` 首次接近 500ms 阈值，与 `.pdf` 同属高候选数 trigram 查询，进一步验证 ext-index 路由优化的必要性。
