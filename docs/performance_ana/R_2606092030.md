# MacEverything 性能分析报告 R_2606092030

## 1. 本轮范围

- 任务启动时间：2026-06-09 20:30
- 报告文件：`docs/performance_ana/R_2606092030.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=6727`
  - `SEG_LINES=5785`
  - `LAST_LOG_TS=2026-06-09 15:47`

## 2. 执行摘要

**持续恢复——0/16 >500ms，Max=347ms，P50=144ms，lockWait=0，极佳比例 5/16。** 连续第 2 轮 0/16 >500ms。trigram 均值从 193ms 降至 115ms（↓40%），逐步向稳态收敛。**tombstone 26.98%——距 27% 仅 0.02%。**

| 指标 | R218 | R219 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平（连续 2 轮） |
| Max | 411ms | **347ms** | ↓16% |
| P50 | 191ms | **144ms** | ↓25% |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 4/16 | **5/16** | ↑1 |
| tombstone | 1,908,072 (26.88%) | **1,918,280 (26.98%)** | ↑ |
| tombstone 增速 | ~358/min | **~340/min** | ↓5%（晚间减速） |
| recordCount | 7,098,410 | **7,109,668** | +11.3K |
| liveRecordCount | 5,190,338 | **5,191,388** | +1.1K |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5173 | **5233** | +60 |
| COMPACTION | 2 | **2** | **不变——第 20 轮确认缺陷** |
| QUERYADV_GE500 | 53 | **53** | 不变（连续 2 轮 0 新增） |

### tombstone 26.98%——距 27% 仅 0.02%！

- **tombstone=1,918,280（26.98%）——超阈值 1.98%。**
- **第 20 轮确认 compaction 缺陷——里程碑。**
- **增速 ~340/min（晚间减速——文件活动降低）。**
- **recordCount +11.3K，liveRecordCount +1.1K——净新增极少，10.2K 为 tombstone。**

### tombstone 增长全景

| 里程碑 | 轮次 | tombstone% | 距本轮 |
|--------|------|----------:|--------|
| 首次超 25% | R200 | 25.15% | 20 轮前 |
| 首次超 26% | R212 | 26.14% | 8 轮前 |
| **即将超 27%** | **R219** | **26.98%** | **当前** |

- **从 25% → 27% 历经约 20 轮——平均每轮增长 ~0.10 个百分点。**
- **预计下 1-2 轮突破 27%。**

### Trigram 路径——持续恢复

| Query | R218 | R219 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 238ms | **145ms** | ↓39% | 正常 |
| `hello world` | 411ms | **227ms** | ↓45% | 正常 |
| `*.swift` | 202ms | **243ms** | ↑20% | 正常（波动） |
| `main.cpp` | 49ms | **29ms** | ↓41% | 极佳 |
| `abc def...` | 334ms | **149ms** | ↓55% | 正常 |
| `README` | 115ms | **43ms** | ↓63% | 极佳 |
| `test` | 174ms | **74ms** | ↓57% | 正常 |
| `MacEverything` | 26ms | **14ms** | ↓46% | 极佳 |
| Trigram 均值 | 193ms | **115ms** | **↓40%** | 持续恢复 |

- **Trigram 均值 193→115ms（↓40%）——持续恢复。**
- **`abc def`=149ms（↓55% from 334ms），`hello world`=227ms（↓45%）——高 candidates 查询恢复明显。**
- **`*.swift`=243ms（↑20%）——轻微波动。**
- **距稳态均值 ~20ms 仍有 ~6 倍差距——完全恢复需要更多轮。**

### 全表扫描——正常

| Query | R218 | R219 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 184ms | **347ms** | ↑89% |
| `path:/Users` | 305ms | **147ms** | **↓52%** |
| `中文测试` | 191ms | **250ms** | ↑31% |
| **总和** | **680ms** | **744ms** | **↑9%** |

- **全表扫描总和 680→744ms（↑9%）——正常范围。**
- **`path:/Users`=147ms（↓52%）——路径数组恢复。**
- **`content:import`=347ms（↑89%）——SoA 区域与路径数组交替波动（零和效应）。**

### 辅助路径

| Query | R218 | R219 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 12ms | **8ms** | ↓33% | 极佳 |
| `ext:cpp` | 58ms | **78ms** | ↑34% | 正常 |
| `size:>100m` | 239ms | **104ms** | ↓56% | 正常 |
| `dm:today` | 84ms | **76ms** | ↓10% | 正常 |
| `verylongquery...` | 0.5ms | **0.3ms** | ↓40% | 极佳 |

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 7.8 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 144.6 | 139.1 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 28.7 | 16.3 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 242.7 | 236.1 | advanced-trigram | 0 | 9345 | 正常 |
| `ext:cpp` | 5 | 77.8 | 76.3 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 103.9 | 102.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 227.2 | 224.1 | advanced-trigram | 0 | 3669 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 148.6 | 146.4 | advanced-trigram | 0 | 14381 | 正常 |
| `README` | 5 | 42.5 | 36.7 | advanced-trigram | 0 | 17123 | 极佳 |
| `test` | 5 | 74.4 | 62.4 | advanced-trigram | 0 | 117073 | 正常 |
| `MacEverything` | 5 | 14.2 | 1.2 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 76.2 | 76.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 347.0 | 337.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 146.5 | 136.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 249.9 | 247.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.3 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 144.6ms，P90 = 249.9ms，Max = 347.0ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R218 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **5/16** | 0.3-43ms | R218=4/16（↑1） |
| **正常**（50-500ms） | 11/16 | 74-347ms | R218=12/16（↓1） |
| **超标**（>500ms） | **0/16** | — | R218=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,109,668，liveRecordCount=5,191,388（tombstone=**1,918,280**，**26.98%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=444  QUERYADV_GE500=53（不变——连续 2 轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5233
```

## 6. 问题清单

### P0 确认（第 20 轮——里程碑）：compaction 触发机制缺陷

- **tombstone 26.98%——超阈值 1.98%，距 27% 仅 0.02%。**
- **20 轮连续确认。**

### P0：flush 写锁阻塞

- **R217=202.6ms 证据。COW flush 是正确修复路径。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 20 轮确认）

### P0：flush rewrite 改为 COW 方式

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 是否恢复到稳态（极佳 10+/16）。
2. tombstone 是否突破 27%。

## 9. 结论

**持续恢复——0/16 >500ms，Max=347ms（↓16%），P50=144ms（↓25%），极佳 5/16。** Trigram 均值 193→115ms（↓40%），逐步向稳态收敛。lockWait=0，`x`=8ms——系统级资源竞争完全消退。

**tombstone 26.98%——距 27% 仅 0.02%（第 20 轮确认 compaction 缺陷，里程碑）。** 从 R200 的 25.15% → R219 的 26.98%——20 轮增长 1.83 个百分点。当前两个 P0 问题：compaction 触发缺陷 + flush 写锁阻塞。
