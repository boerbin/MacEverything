# MacEverything 性能分析报告 R_2606081046

## 1. 本轮范围

- 任务启动时间：2026-06-08 10:46
- 报告文件：`docs/performance_ana/R_2606081046.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=4196`
  - `SEG_LINES=3254`
  - `LAST_LOG_TS=2026-06-08 11:17`

## 2. 执行摘要

**唤醒后系统活动风暴——0/16 >500ms，Max=419ms，P50=119ms，lockWait=0，极佳比例 5/16（31%）。** Trigram 均值从 R177 的 95.8ms 反升至 115.3ms——日间系统活动持续竞争 page cache，阻止 trigram 恢复。SoA/linear post-wake 优势完全消失——全表扫描总和从 R177 的 238ms（系列新低）回升至 788ms（正常日间水平）。`dm:today`=130ms 创本系列最高。tombstone 增速 733/min——FSEvents 唤醒追赶仍在进行。

### 关键指标对比

| 指标 | R177（30 min 前） | R178 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 153ms | **419ms** | ↑（post-wake 优势消失） |
| P50 | 63ms | **119ms** | ↑ 89%（系统活动竞争） |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 6/16 | **5/16** | ↓1 |
| tombstone | 1,412,630 (21.69%) | **1,434,630 (21.93%)** | **↑ 快速（唤醒追赶）** |
| tombstone 增速 | ~427/min | **~733/min** | **↑ 72%（追赶高峰）** |
| recordCount | 6,513,466 | **6,538,436** | +25.0K（追赶持续） |
| liveRecordCount | 5,100,836 | **5,103,806** | +2,970 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 2968 | **3028** | +60（恢复正常节奏） |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 9 | **9** | 不变（本轮 0 新增） |

### 唤醒后系统活动风暴——所有路径受压

| 路径 | R177（post-wake 极值） | R178 本轮 | 变化 | 说明 |
|------|-----:|-----:|------|------|
| `content:import` | 59ms | **177ms** | ↑ 200% | SoA 优势消失 |
| `path:/Users` | 134ms | **419ms** | ↑ 213% | linear 优势消失 |
| `中文测试` | 45ms | **192ms** | ↑ 324% | linear 优势消失 |
| `.pdf`(trigram) | 115ms | **164ms** | ↑ 43% | 更冷 |
| `hello world`(trigram) | 104ms | **201ms** | ↑ 93% | 更冷 |
| `dm:today` | 12.5ms | **130ms** | ↑ 940% | 系列最高 |

- **R177 的极低值确认为 post-wake 过度恢复效应——系统刚唤醒时无竞争，一旦活动恢复即消失。**
- **当前处于唤醒后系统活动风暴期**：FSEvents 追赶（733/min）、Spotlight 增量索引、应用恢复——密集 I/O 持续竞争 page cache。
- **所有路径均受影响，但 0/16 >500ms——架构韧性保持。**

### 全表扫描——回归日间稳态

| Query | R177（极值） | R178 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 59ms | **177ms** | ↑ 200% |
| `path:/Users` | 134ms | **419ms** | ↑ 213% |
| `中文测试` | 45ms | **192ms** | ↑ 324% |
| **总和** | **238ms** | **788ms** | **↑ 231%（回归日间水平）** |

- **全表扫描总和 788ms——回到日间稳态范围（750-800ms）。**
- **R177 的 238ms 确认为不可持续的 post-wake 极值。**
- **`path:/Users`=419ms——接近 500ms 阈值但未超标。**

### Trigram 路径——系统活动阻止恢复

| Query | R177 | R178 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 115ms | **164ms** | ↑ 43%（更冷） |
| `*.swift` | 63ms | **124ms** | ↑ 97%（更冷） |
| `hello world` | 104ms | **201ms** | **↑ 93%（显著更冷）** |
| `abc def...` | 152ms | **119ms** | ↓ 22%（部分恢复） |
| `README` | 123ms | **49ms** | **↓ 60%（大幅恢复）** |
| `test` | 115ms | **129ms** | ↑ 12%（持平） |
| `MacEverything` | 10ms | **21ms** | ↑（微量） |
| Trigram 均值 | 95.8ms | **115.3ms** | **↑ 20%（反向恶化）** |

- **Trigram 均值 115.3ms——从 R177 的 95.8ms 反升——唤醒后系统活动持续淘汰 postings 页。**
- **恢复不均匀**：`README`=49ms（↓60%）但 `hello world`=201ms（↑93%）。
- **原因**：不同 postings 区域的 page cache 页面被系统活动不均匀淘汰。`README` 的 postings 恰好未被竞争，`hello world` 的区域被密集 I/O 命中。
- **预计**：系统活动风暴消退后（FSEvents 追赶完成），trigram 将逐步恢复至正常水平。

### `dm:today`=130ms——本系列最高

- **此前最高**：R174=106ms（08:16 日间启动潮）。
- **本轮 130ms——超越 20%。**
- **原因**：FSEvents 唤醒追赶（733/min 增速）导致 SoA 时间戳区域 page cache 被 FSEvents 写入操作竞争。
- **一旦追赶完成即可恢复。**

### tombstone 增速 733/min——追赶高峰

- **R177=427/min → R178=733/min——唤醒后追赶仍在加速。**
- **tombstone +22,000/30min——正常值 ~8,000/30min 的 2.75x。**
- **liveRecordCount +2,970——正常（实际新文件较少，大部分是替换/删除=tombstone）。**
- **tombstone 21.93%——距 25% 阈值约 ~14 小时（追赶增速下大幅缩短）。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 4.2 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 164.4 | 161.0 | advanced-trigram | 0 | 9813 | 冷态 |
| `main.cpp` | 5 | 15.9 | 10.1 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 123.9 | 120.3 | advanced-trigram | 0 | 9345 | 冷态 |
| `ext:cpp` | 5 | 56.5 | 55.3 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 61.4 | 58.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 200.6 | 197.4 | advanced-trigram | 0 | 3626 | 冷态 |
| `abc def ghi jkl mno pqr` | 0 | 118.5 | 118.3 | advanced-trigram | 0 | 14214 | 冷态 |
| `README` | 5 | 49.1 | 44.2 | advanced-trigram | 0 | 16632 | 极佳 |
| `test` | 5 | 129.3 | 120.0 | advanced-trigram | 0 | 116528 | 冷态 |
| `MacEverything` | 5 | 21.4 | 1.7 | advanced-trigram | 0 | 6860 | 极佳 |
| `dm:today` | 5 | 130.4 | 129.2 | advanced-pure-filter-soa-gcd | 0 | 0 | **系列最高** |
| `content:import` | 5 | 177.4 | 164.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 419.3 | 415.5 | advanced-linear-gcd | 0 | 0 | 正常（临界） |
| `中文测试` | 0 | 191.7 | 189.7 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 118.5ms，P90 = 200.6ms，Max = 419.3ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R177 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 5/16 | 0.5-49ms | R177=6/16（↓1） |
| **正常**（50-500ms） | 11/16 | 57-419ms | R177=10/16（↑1） |
| **超标**（>500ms） | 0/16 | — | R177=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,538,436，liveRecordCount=5,103,806（tombstone=**1,434,630**，**21.93%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=159  QUERYADV_GE500=9（不变——本轮 0 新增）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3028
```

- **本轮 0 新增 >500ms 查询（连续 2 轮）。**
- **SKIPFLUSH 恢复正常增量（+60）——定时器节奏正常。**

## 6. 问题清单

### 观察项：唤醒后系统活动风暴（进行中）

- **现象**：trigram 均值 115ms（反升），`dm:today`=130ms（系列最高），tombstone 增速 733/min。
- **原因**：FSEvents 唤醒追赶+Spotlight/应用恢复密集 I/O。
- **影响**：page cache 持续被竞争，trigram 无法恢复至正常水平。
- **预计**：FSEvents 追赶完成后（增速回落至 ~300/min）性能恢复。
- **处置**：系统级行为，无需干预。

### tombstone 距阈值缩短

- **21.93%，增速 733/min——距 25% 约 14 小时（此前预估 20-22h）。**
- **若追赶完成后增速回落，实际可能 18-20 小时。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（紧迫度上升）

- tombstone 21.93%，唤醒追赶加速至 733/min。
- 距 25% 阈值约 14-18 小时（追赶结束后约 18-20 小时）。
- **紧迫度上升**——应在触发 compaction 前修复 rewrite 崩溃。

### P1：flush rewrite 改为 COW 方式（持续建议）

### P2：唤醒预热策略（持续建议）

- 本轮数据进一步证明：post-wake 系统活动风暴下 trigram 不仅不恢复反而恶化——主动预热更加重要。

## 8. 下一轮重点

1. FSEvents 追赶增速是否开始回落（733→300/min）。
2. Trigram 均值是否随系统活动消退而开始恢复。
3. `dm:today` 是否从 130ms 回落。

## 9. 结论

**唤醒后系统活动风暴持续——0/16 >500ms，Max=419ms，P50=119ms，lockWait=0，极佳比例 5/16。** Trigram 均值从 R177 的 96ms 反升至 115ms——系统活动风暴（FSEvents 733/min、Spotlight、应用恢复）持续竞争 page cache，阻止 trigram 恢复。SoA/linear post-wake 极低值完全消失——全表扫描总和回归日间稳态 788ms。`dm:today`=130ms 创系列最高（FSEvents 写入竞争 SoA 时间戳区域）。

**核心发现**：唤醒后存在"系统活动风暴期"——不仅 page cache 被清空（R176），唤醒后的密集追赶活动还会持续阻止 page cache 重建。trigram 的恢复取决于系统活动何时消退，而非 battery 预热。tombstone 21.93%（1.43M），追赶增速 733/min（临时），距 25% 约 14-18 小时。P0 compaction 修复紧迫度上升。
