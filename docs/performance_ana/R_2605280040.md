# MacEverything 性能分析报告 R_2605280040

**时间**: 2026-05-28 00:40  
**索引规模**: 6,105,059 总记录 / 5,444,952 活跃记录  
**Tombstone**: 660,107 (10.81%)  
**日志**: 4263 行, INFO=4263, WARN=0, ERROR=0  
**轮次**: 第 23 轮  

---

## 1. 本轮查询耗时

| 查询 | totalMs | trigramMs | phase2Ms | lockMs | searchPath |
|------|---------|-----------|---------|--------|------------|
| `"x"` | 0 | 0 | 0 | 0 | short-query-cache |
| `".pdf"` | 20 | 2 | 17 | 0 | advanced-trigram |
| `"main.cpp"` | 6 | 4 | 1 | 0 | advanced-trigram |
| `"*.swift"` | 19 | 2 | 17 | 0 | advanced-trigram |
| `"ext:cpp"` | 7 | 0 | 6 | 0 | advanced-ext-index |
| `"size:>100m"` | 37 | 0 | 37 | 0 | advanced-pure-filter-soa-gcd |
| `"hello world"` | 9 | 1 | 8 | 0 | advanced-trigram |
| `"abc def ghi..."` | 43 | 1 | 42 | 0 | advanced-trigram |
| `"README"` | 12 | 3 | 9 | 0 | advanced-trigram |
| `"test"` | 25 | 9 | 16 | 0 | advanced-trigram |
| `"MacEverything"` | 6 | 5 | 0 | 0 | advanced-trigram |
| `"dm:today"` | 65 | 0 | 65 | 0 | advanced-pure-filter-soa-gcd |
| `"content:import"` | **291** | 0 | 281 | 0 | advanced-pure-filter-soa-gcd |
| `"path:/Users"` | 209 | 0 | 203 | 0 | advanced-linear-gcd |
| `"中文测试"` | 262 | 0 | 261 | 0 | advanced-linear-gcd |
| `"very long query..."` | 7 | 5 | 1 | 0 | advanced-trigram |

### 与上轮对比

| 查询 | R22 | R23 | 变化 | 分析 |
|------|-----|-----|------|------|
| `"content:import"` | 214ms | **291ms** | **+36%** | 常态高位 |
| `"中文测试"` | 174ms | **262ms** | +51% | 常态高位 |
| `"path:/Users"` | 248ms | **209ms** | -16% | 回落 |
| `"MacEverything"` | 15ms | **6ms** | -60% | 极佳 |
| `"README"` | 9ms | 12ms | +33% | 极佳 |
| `"test"` | 24ms | 25ms | +4% | 稳定 |

**本轮特征**: Trigram 全部 ≤9ms（MacEverything 仅 5ms），零锁等待。但线性/过滤路径出现分化：`content:import` 跳升至 291ms（近 13 轮最高），`中文测试` 升至 262ms；而 `path:/Users` 反向回落至 209ms。活跃记录恢复正增长（+308），R22 的净减少为单轮现象。

---

## 2. 锁竞争与 Trigram：持续稳定

**锁竞争**: 0ms，R22 的 1ms 确认为噪声。R16-R23 八轮中仅 R17 (18ms) 和 R22 (1ms) 有微量锁等待。

**Trigram**:

| 查询 | R21 | R22 | R23 |
|------|---------:|---------:|---------:|
| `test` | 10 | 11 | 9 |
| `MacEverything` | 7 | 14 | **5** |
| `README` | 4 | 2 | 3 |
| `main.cpp` | 4 | 3 | 4 |

MacEverything trigramMs 从 R22 的 14ms 降至 5ms，所有查询 ≤9ms。Trigram 索引在 10.81% tombstone 下仍然非常稳定。

---

## 3. 线性扫描路径分化

| 查询 | R20 | R21 | R22 | R23 | 走势 |
|------|-----|-----|-----|-----|------|
| `content:import` | 218 | 191 | 214 | **291** | ↑ |
| `path:/Users` | 147 | 165 | 248 | **209** | ↓ |
| `中文测试` | 206 | 184 | 174 | **262** | ↑ |

`content:import` 291ms 是 R12 (272ms) 以来最高。`中文测试` 262ms 接近常态上限（260ms）。而 `path:/Users` 从 R22 的 248ms 回落至 209ms。

三条路径在同一轮内走势不同，进一步证明线性扫描受即时系统状态影响大，不存在全局统一的"退化"趋势。

### content:import 291ms 分析

`content:import` 走 `advanced-pure-filter-soa-gcd` 路径（非线性扫描），需要遍历所有记录做内容过滤。phase2Ms=281ms 占绝大部分。回顾历史：

| 轮次 | content:import | Tombstone% |
|------|---------------:|--------:|
| R15 | 65 | 9.23% |
| R19 | 70 | 9.96% |
| R21 | 191 | 10.35% |
| R22 | 214 | 10.62% |
| **R23** | **291** | **10.81%** |

在排除极佳窗口（R15/R19）后，R21-R23 呈现 191→214→291 的上升趋势。这可能与 tombstone 累积导致过滤路径需要跳过更多无效记录有关——与线性扫描不同，过滤路径的 tombstone 敏感度可能更高，因为每条记录都需要检查有效性。

---

## 4. Tombstone 二十三轮趋势

| 轮次 | Tombstone | 比例 | Δ |
|------|-----------|------|---|
| R1 | 332,393 | 5.78% | — |
| ... | ... | ... | ... |
| R18 | 585,216 | 9.71% | +23,015 |
| R19 | 602,232 | 9.96% | +17,016 |
| R20 | 618,916 | 10.21% | +16,684 |
| R21 | 628,737 | 10.35% | +9,821 |
| R22 | 647,037 | 10.62% | +18,300 |
| **R23** | **660,107** | **10.81%** | **+13,070** |

### 4.1 Churn 脉冲式波动继续

| 轮次 | Δ | 趋势 |
|------|---|------|
| R18 | +23.0K | 高峰 II 峰值 |
| R19 | +17.0K | ↓ |
| R20 | +16.7K | → |
| R21 | +9.8K | ↓↓ |
| R22 | +18.3K | ↑↑ |
| R23 | +13.1K | ↓ |

R22→R23 从 +18.3K 降到 +13.1K，但仍高于 R16-R17 的低位平台（4-5K）。

### 4.2 活跃记录变化

| 指标 | R22 | R23 | Δ |
|------|-----|-----|---|
| 总记录 | 6,091,681 | 6,105,059 | +13,378 |
| 活跃记录 | 5,444,644 | 5,444,952 | **+308** |
| Tombstone | 647,037 | 660,107 | +13,070 |

活跃记录恢复正增长（+308），R22 的 -127 确认为单轮现象（深夜清理任务）。新增记录中 **97.7%** 变为 tombstone。

### 4.3 刷写统计

| 指标 | R22 | R23 | Δ |
|-----|-----|----|---|
| 刷写 | 1772 | 1829 | +57 |
| 跳过 | 349 | 352 | +3 |

刷写保持高频（+57），跳过略增（+3）。

---

## 5. 10% 以上的性能评估（R20-R23）

Tombstone 在 10% 以上已持续四轮，可以做阶段性评估：

| 维度 | 表现 | 与 <10% 时期对比 |
|------|------|-----------------|
| Trigram | 全部 ≤14ms | 与低位平台（R16-R17）一致 |
| 锁竞争 | 0-1ms | 好于高峰 I（R8-R13） |
| 线性扫描 | 147-262ms | 常态区间，无退化 |
| 过滤路径 | 191-291ms | content:import **可能有轻微退化趋势** |

**初步结论**: 10% tombstone 对 trigram 查询和锁竞争没有明显影响。线性扫描在常态区间。唯一值得关注的是 `content:import` 过滤路径的 phase2Ms 上升趋势（191→214→291），可能是 tombstone 累积对全量过滤路径的早期影响信号。

---

## 6. 优化建议

### 6.1 紧急：Tombstone compaction

10.81%，660K 无效记录。特别关注 `content:import` 过滤路径可能出现的 tombstone 敏感性——如果过滤路径需要逐条检查 tombstone 标记，compaction 将直接减少遍历量。

### 6.2 过滤路径 tombstone 跳过优化

如果 `content:import` 的 phase2 确实受 tombstone 影响，可以在 SOA-GCD 过滤循环中增加 tombstone 批量跳过（如 bitmap 预过滤），避免逐条检查。

### 6.3 工程优先级

1. **Tombstone compaction** — 紧急（10.81%，过滤路径可能开始受影响）
2. **无锁快照刷写** — 高（日间风险）
3. **过滤路径 tombstone 预过滤** — 中（验证 R21-R23 上升趋势后再决定）

---

## 7. 总结

| 维度 | 评分 | 趋势 |
|------|------|------|
| 查询 (trigram) | **10/10** | → (全部 ≤9ms) |
| 查询 (线性) | 6/10 | ↓ (content 291ms, 中文 262ms) |
| 延迟稳定性 | **10/10** | → (零锁等待) |
| 索引健康度 | **0/10** | ↓ (10.81%, 660K tombstone) |
| 系统稳定性 | 8/10 | → |

**第 23 轮结论**: Trigram 全部 ≤9ms（MacEverything 仅 5ms），零锁等待，但 `content:import` 跳升至 291ms（近 13 轮最高），`中文测试` 升至 262ms。Tombstone 达 660,107 (10.81%)，增量 +13.1K（脉冲式波动中的下降相）。活跃记录恢复正增长（+308），R22 的净减少为单轮现象。新发现：`content:import` 过滤路径在 R21-R23 呈 191→214→291 上升趋势，可能是 tombstone 累积对全量过滤路径的早期影响信号——这为 tombstone compaction 增加了新的紧迫理由。
