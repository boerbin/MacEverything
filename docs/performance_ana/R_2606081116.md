# MacEverything 性能分析报告 R_2606081116

## 1. 本轮范围

- 任务启动时间：2026-06-08 11:16
- 报告文件：`docs/performance_ana/R_2606081116.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=4270`
  - `SEG_LINES=3328`
  - `LAST_LOG_TS=2026-06-08 11:47`

## 2. 执行摘要

**第二次重度冷启动/系统 I/O 风暴——6/16 >500ms，Max=1538ms，P50=280ms，lockWait=63.7ms（系列最高），极佳比例 0/16（0%）。** 程度与 R176（睡眠/唤醒 7/16 >500ms）相当但模式不同：lockWait 高达 64ms（R176=32ms 的 2x），`x`(cache-hit)=72ms（R176=31ms 的 2.3x），表明不仅是 page cache 淘汰，还存在严重的进程调度延迟和锁竞争。可能原因：(1) 第二次短暂睡眠/唤醒，或 (2) 极端系统 I/O 风暴（Time Machine 备份、大型编译等）。

### ⚠️ 严重程度评估

| 维度 | R176（首次唤醒） | R179 本轮 | 对比 |
|------|--------:|--------:|------|
| >500ms 数量 | 7/16 | **6/16** | 略好 |
| Max | 2217ms | **1538ms** | 好 |
| lockWait max | 32ms | **64ms** | **差 2x** |
| `x`(cache) | 31ms | **72ms** | **差 2.3x** |
| 极佳比例 | 1/16 | **0/16** | **差** |
| QUERYADV_GE500 新增 | +5 | **+4** | 略好 |

- **lockWait=64ms 和 `x`=72ms 说明问题不仅是 page cache——进程级调度延迟严重。**
- **这指向系统级 I/O 饱和（磁盘 bandwidth 耗尽），不仅是 page cache LRU 淘汰。**

### 关键指标对比

| 指标 | R178（30 min 前） | R179 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **6/16** | **↑6** |
| Max | 419ms | **1538ms** | **↑ 267%** |
| P50 | 119ms | **280ms** | **↑ 135%** |
| lockWait max | 0ms | **63.7ms** | **↑（系列最高！）** |
| 极佳比例(<50ms) | 5/16 | **0/16** | **↓5** |
| tombstone | 1,434,630 (21.93%) | **1,459,175 (22.22%)** | ↑（追赶继续） |
| tombstone 增速 | ~733/min | **~818/min** | **↑（追赶加速）** |
| recordCount | 6,538,436 | **6,565,559** | +27.1K（追赶持续） |
| liveRecordCount | 5,103,806 | **5,106,384** | +2,578 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 3028 | **3089** | +61 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 9 | **13** | **↑4** |

### lockWait 分布——严重锁竞争

| Query | lockWaitMs | 说明 |
|-------|----------:|------|
| `path:/Users` | **63.7ms** | **系列最高——flush timer 重度持锁** |
| `abc def...` | **29.7ms** | 重度 |
| `中文测试` | 4.8ms | 中度 |
| `test` | 4.2ms | 中度 |
| `verylongquery` | 3.9ms | 轻度 |
| `README` | 0.3ms | 微量 |

- **6/16 查询出现非零 lockWait（此前最多 2-3/16）。**
- **lockWait=63.7ms——R176 的 32ms 的 2x。**
- **分布广泛说明 flush timer 线程在系统 I/O 压力下持锁时间显著延长。**
- **COW 方案（P1）必要性进一步证实——在极端 I/O 下锁竞争可达用户可感知级别。**

### 全表扫描

| Query | R178 | R179 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 177ms | **1538ms** | **↑ 769%** |
| `path:/Users` | 419ms | **1145ms** | **↑ 173%** |
| `中文测试` | 192ms | **739ms** | **↑ 285%** |
| **总和** | **788ms** | **3422ms** | **↑ 334%** |

- **全表扫描总和 3422ms——本系列绝对最高（R176=1991ms 的 1.7x）。**
- **`content:import`=1538ms——phase2=1009ms（SoA 全扫描冷启动）+ 非 phase2 开销 529ms（I/O 等待）。**
- **`path:/Users` lockWait=63.7ms 贡献了额外延迟。**

### Trigram 路径

| Query | R178 | R179 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 164ms | **447ms** | ↑ 173% |
| `*.swift` | 124ms | **241ms** | ↑ 94% |
| `hello world` | 201ms | **985ms** | **↑ 390%（>500ms！）** |
| `abc def...` | 119ms | **284ms** | ↑ 139% |
| `README` | 49ms | **313ms** | ↑ 539% |
| `test` | 129ms | **280ms** | ↑ 117% |
| `MacEverything` | 21ms | **78ms** | ↑ 271% |
| Trigram 均值 | 115ms | **375ms** | **↑ 226%** |

- **Trigram 均值 375ms——R176 冷启动水平的回归。**
- **`hello world`=985ms——3.6K candidates 的 postings 几乎全部需要 fault-in。**
- **与 R176 不同**：R176 的 `test`(116K)=2217ms 最慢，本轮 `hello world`(3.6K)=985ms——说明 I/O 延迟（每次 fault 更慢）而非单纯 fault 数量主导。

### SoA Filter

| Query | R178 | R179 | 变化 |
|-------|-----:|-----:|------|
| `size:>100m` | 61ms | **465ms** | ↑ 660% |
| `dm:today` | 130ms | **233ms** | ↑ 79% |

- **`size:>100m`=465ms——接近 500ms 阈值。SoA 全扫描在 I/O 饱和下极慢。**
- **`dm:today`=233ms（R178=130ms）——时间戳区域 page cache 完全被竞争。**

### tombstone 追赶加速——22.22%

- **增速 818/min——从 R178 的 733/min 继续上升。**
- **tombstone 22.22%（1.46M）——距 25% 约 ~13 小时（追赶增速下）。**
- **recordCount +27.1K/30min——正常的 3.4x，FSEvents 追赶仍在继续。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 71.9 | 0.0 | short-query-cache | 0 | 0 | **调度延迟** |
| `.pdf` | 5 | 446.9 | 439.1 | advanced-trigram | 0 | 9841 | 重度冷态 |
| `main.cpp` | 5 | 50.0 | 16.6 | advanced-trigram | 0 | 82 | 正常 |
| `*.swift` | 5 | 241.1 | 232.5 | advanced-trigram | 0 | 9345 | 冷态 |
| `ext:cpp` | 5 | 144.6 | 128.6 | advanced-ext-index | 0 | 3571 | 冷态 |
| `size:>100m` | 5 | 464.5 | 464.2 | advanced-pure-filter-soa-gcd | 0 | 0 | 临界 |
| `hello world` | 5 | 984.8 | 924.7 | advanced-trigram | 0 | 3626 | **>500ms** |
| `abc def ghi jkl mno pqr` | 0 | 284.4 | 252.2 | advanced-trigram | 29.7 | 14216 | lockWait! |
| `README` | 5 | 313.1 | 296.9 | advanced-trigram | 0.3 | 16634 | 冷态 |
| `test` | 5 | 280.4 | 254.6 | advanced-trigram | 4.2 | 116529 | 冷态+lockWait |
| `MacEverything` | 5 | 77.9 | 29.0 | advanced-trigram | 0 | 6860 | 冷态 |
| `dm:today` | 5 | 233.4 | 229.5 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 1537.5 | 1008.8 | advanced-pure-filter-soa-gcd | 0 | 0 | **>500ms** |
| `path:/Users` | 5 | 1145.0 | 1028.0 | advanced-linear-gcd | 63.7 | 0 | **>500ms, lockWait!** |
| `中文测试` | 0 | 739.4 | 732.0 | advanced-linear-gcd | 4.8 | 0 | **>500ms** |
| `verylongquery...` | 0 | 21.1 | 0.0 | advanced-trigram | 3.9 | 0 | lockWait |

**P50 = 280.4ms，P90 = 1145.0ms，Max = 1537.5ms。6/16 查询 >500ms。lockWait max = 63.7ms。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R178 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 0/16 | — | R178=5/16（**↓5**） |
| **正常**（50-500ms） | 10/16 | 50-465ms | R178=11/16（↓1） |
| **超标**（>500ms） | **6/16** | 739-1538ms | R178=0/16（**↑6**） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,565,559，liveRecordCount=5,106,384（tombstone=**1,459,175**，**22.22%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=171  QUERYADV_GE500=13（+4——本轮新增 4 个 >500ms）
FLUSH=1  COMPACTION=2  SKIPFLUSH=3089
```

- **QUERYADV_GE500 +4（hello world, content:import, path:/Users, 中文测试）。**
- **注意**：battery 中 `*.swift`=590ms 和 `size:>100m`=465ms 接近但被日志计为 <500ms 或恰好 >500ms——实际日志记录可能有轻微差异。
- **SKIPFLUSH +61——定时器节奏正常（非休眠）。**

## 6. 问题清单

### 观察项：第二次重度 I/O 风暴（一次性）

- **现象**：6/16 >500ms，lockWait=64ms，`x`=72ms。
- **可能原因**：
  1. 第二次短暂睡眠/唤醒（SKIPFLUSH +61 正常，不支持长休眠——但可能是 <5min 微休眠）
  2. 极端系统 I/O 活动（Time Machine 首次备份、Xcode 索引、大型下载等）
- **影响**：一次性，预计 1-2 轮恢复。
- **与 R176 对比**：lockWait 更严重（64 vs 32ms）但 Max 更低（1538 vs 2217ms）——I/O 延迟更均匀分布。

### lockWait=64ms（严重但一次性）

- **系列最高，首次超过 50ms。**
- **6/16 查询出现 lockWait——影响范围最广。**
- **COW 方案紧迫度进一步上升。**

### tombstone 22.22%——加速中

- **追赶增速 818/min——距 25% 约 13 小时。**
- **若追赶结束回落至 300/min，实际约 16-18 小时。**

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（紧迫）

- tombstone 22.22%（1.46M），追赶增速 818/min。
- 距 25% 阈值约 13-18 小时。

### P1：flush rewrite 改为 COW 方式（紧迫度上升）

- **本轮 lockWait=64ms 已达用户可感知级别。**
- 两次重度事件（R176=32ms，R179=64ms）证明唤醒/I/O 风暴下 lock 竞争是实际问题。
- COW 可完全消除——读路径无锁。

### P2：唤醒预热策略（持续建议）

## 8. 下一轮重点

1. 是否恢复至 0/16 >500ms（验证一次性）。
2. lockWait 是否恢复至 0。
3. tombstone 增速是否开始回落（追赶完成信号）。

## 9. 结论

**第二次重度 I/O 风暴——6/16 >500ms，Max=1538ms，P50=280ms，lockWait=63.7ms（系列最高）。** 所有路径严重受压，0/16 极佳。与 R176（睡眠/唤醒）不同，本轮 SKIPFLUSH 正常增长——可能是短暂微休眠或极端系统 I/O 活动。lockWait=64ms 在 6/16 查询出现——影响范围和严重程度均为系列最高，COW 方案紧迫度上升。

**tombstone 22.22%（1.46M），追赶增速 818/min——距 25% 约 13-18 小时。** P0 compaction 修复日益紧迫。预计下一轮恢复——两次重度事件（R176、R179）均在 1-2 轮后恢复至正常。系统在极端 I/O 下仍无 crash/hang——进程稳定性完全验证。
