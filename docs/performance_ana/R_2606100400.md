# MacEverything 性能分析报告 R_2606100400

## 1. 本轮范围

- 任务启动时间：2026-06-10 04:00
- 报告文件：`docs/performance_ana/R_2606100400.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=7645`
  - `SEG_LINES=6703`
  - `LAST_LOG_TS=2026-06-10 00:47`

## 2. 执行摘要

**稳态偏高——0/16 >500ms，Max=346ms，P50=74ms，lockWait=0，极佳 4/16。** 从 R233 的恢复状态（Max=334ms，P50=76ms）基本持平。全表扫描偏高（总和 865ms），`path:/Users`=346ms 和 `中文测试`=269ms 未恢复到深夜稳态。Trigram 路径略有回升但总体正常。

| 指标 | R233 | R234 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 334ms | **346ms** | ↑4% |
| P50 | 76ms | **74ms** | ↓3% |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 5/16 | **4/16** | ↓1 |
| tombstone | 2,085,892 (28.60%) | **2,094,570 (28.69%)** | ↑ |
| tombstone 增速 | ~414/min | **~289/min** | **↓30%（凌晨最低区间）** |
| recordCount | 7,292,592 | **7,301,514** | +8.9K |
| liveRecordCount | 5,206,700 | **5,206,944** | +244 |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5977 | **6037** | +60 |
| COMPACTION | 2 | **2** | **不变——第 35 轮确认缺陷** |
| QUERYADV_GE500 | 60 | **60** | 不变 |

### tombstone 增速降至 ~289/min——凌晨最低

- **tombstone=2,094,570（28.69%）——持续攀升。**
- **增速 ~289/min——本会话最低！凌晨 4 点文件活动极少。**
- **recordCount +8.9K（本会话最低级别），liveRecordCount +244。**
- **R230 的 939/min 飙升已完全回落（→614→414→289）。**

### Trigram 路径——略升

| Query | R233 | R234 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 125ms | **147ms** | ↑18% | 正常 |
| `hello world` | 128ms | **226ms** | ↑77% | 正常（波动） |
| `*.swift` | 76ms | **127ms** | ↑67% | 正常 |
| `main.cpp` | 10ms | **21ms** | ↑110% | 极佳 |
| `abc def...` | 70ms | **131ms** | ↑87% | 正常 |
| `README` | 23ms | **34ms** | ↑48% | 极佳 |
| `test` | 41ms | **74ms** | ↑80% | 正常 |
| `MacEverything` | 4ms | **17ms** | ↑325% | 极佳 |
| Trigram 均值 | 60ms | **97ms** | **↑62%** | 正常波动 |

- **Trigram 均值 60→97ms（↑62%）——从 R233 的恢复低点正常回升。**
- **`hello world`=226ms（↑77%）——postings 区域波动，但远低于 R232=513ms。**
- **所有 trigram 仍 <230ms——全部正常。**

### 全表扫描——偏高

| Query | R233 | R234 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 334ms | **249ms** | ↓25% |
| `path:/Users` | 207ms | **346ms** | ↑67%（偏高） |
| `中文测试` | 181ms | **269ms** | ↑49%（偏高） |
| **总和** | **722ms** | **865ms** | **↑20%** |

- **全表扫描总和 722→865ms（↑20%）——偏高。**
- **`path:/Users`=346ms（↑67%）——路径数组 page cache 仍有波动。**
- **`content:import`=249ms（↓25%）——SoA 区域部分恢复。**
- **`中文测试`=269ms（↑49%）——名称数组波动。**

### 辅助路径

| Query | R233 | R234 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 19ms | **3ms** | **↓84%** | 极佳（恢复） |
| `ext:cpp` | 36ms | **61ms** | ↑69% | 正常 |
| `size:>100m` | 64ms | **65ms** | ↑2% | 正常 |
| `dm:today` | 116ms | **47ms** | **↓60%** | 极佳 |
| `verylongquery...` | 0.8ms | **2ms** | ↑150% | 极佳 |

- **`x`=3ms（↓84%）——cache 路径恢复正常（R230=18ms，R233=19ms 的开销消退）。**
- **`dm:today`=47ms（↓60%）——SoA modTime 区域恢复。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 3.0 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 146.8 | 139.0 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 20.8 | 15.7 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 126.8 | 124.3 | advanced-trigram | 0 | 9353 | 正常 |
| `ext:cpp` | 5 | 60.5 | 59.7 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 64.8 | 57.0 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 225.9 | 222.8 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 131.2 | 130.0 | advanced-trigram | 0 | 14457 | 正常 |
| `README` | 5 | 34.4 | 30.5 | advanced-trigram | 0 | 17151 | 极佳 |
| `test` | 5 | 74.3 | 69.4 | advanced-trigram | 0 | 117136 | 正常 |
| `MacEverything` | 5 | 17.0 | 1.3 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 46.9 | 46.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 249.1 | 239.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 346.4 | 338.8 | advanced-linear-gcd | 0 | 0 | 正常（偏高） |
| `中文测试` | 0 | 269.2 | 268.3 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 2.0 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 74.3ms，P90 = 269.2ms，Max = 346.4ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R233 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **4/16** | 2-47ms | R233=5/16（↓1） |
| **正常**（50-500ms） | 12/16 | 61-346ms | R233=11/16（↑1） |
| **超标**（>500ms） | **0/16** | — | R233=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,301,514，liveRecordCount=5,206,944（tombstone=**2,094,570**，**28.69%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=543  QUERYADV_GE500=60（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=6037
```

## 6. 问题清单

### P0 确认（第 35 轮）：compaction 触发机制缺陷

- **tombstone=2,094,570（28.69%）——超阈值 3.69%。**
- **35 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **连续 2 轮 lockWait=0。历史证据充分（R232=286.6ms 系列新纪录）。**

## 7. 优化建议

### P0：flush rewrite 改为 COW 方式（R232 lockWait=286.6ms 系列新纪录）

### P0：修复 compaction 触发机制（第 35 轮确认）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 全表扫描是否恢复到深夜稳态（总和 <300ms）。
2. tombstone 增速趋势（289/min 是否为凌晨底部）。

## 9. 结论

**稳态偏高——0/16 >500ms，Max=346ms（↑4%），P50=74ms（↓3%），极佳 4/16。** lockWait 连续 2 轮为 0。Trigram 均值 97ms（↑62%——正常波动）。全表扫描总和 865ms（↑20%——偏高，`path:/Users`=346ms 和 `中文测试`=269ms 未恢复到深夜稳态）。`x`=3ms（恢复正常）。

**tombstone 28.69%（第 35 轮确认 compaction 缺陷）。** 增速降至 ~289/min（本会话最低——凌晨 4 点文件活动极少）。R230 的 939/min 飙升已完全回落（939→614→414→289）。COMPACTION=2 始终不变。
