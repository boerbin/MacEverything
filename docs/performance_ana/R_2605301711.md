# MacEverything 性能分析报告 R_2605301711

- **任务启动时间**：2026-05-30 17:11
- **日志范围**：最近一次启动（line 6815，`2026-05-29 11:04:48`）至今，会话连续运行 ~30.1h，未重启
- **状态快照 @17:11**：total `5,981,124` / live `5,398,652` / tomb `582,472`（**9.74%**）
- **本轮定性**：🔴 **活跃后台写入「再强化」轮（R96 突发型回潮）**——继 R97 收敛后本轮 ingest 强度反弹：电池峰值 403ms→**698ms**，**首次触发缓存标志 `x`=427.88ms**（短查询缓存路径本不该 >1ms，本系列 ingest 以来最强争用签名），tomb 增量 +18,708（91.6% 增长入墓，从 R97 的 +14,827 再加速）。窗口内 totalRecords 实测 +543（5,981,406→5,981,949）证实写入活跃。但**复测全部干净恢复**（5 个标志项 probe3-4 均回落亚毫秒），属 R96 突发型而非 R95 稳态型，分裂可评估。**FLOOR-METHOD（R97）再次应用**：本轮突发落在 size/path/中文/content，dm 反而紧致干净（70-84ms 无突发）；取各路径 min 为真热态地板，**分裂 2.143×（与 R97 2.14× 并列史上最窄对）、24/24 不破**，对抗性检验通过（dm worst-clean 14.10ns < content floor 25.09ns）。

---

## 1. 原始电池（含活跃写入争用，⚠️ 标记异常项）

| query | path | total(ms) | phase2 | trig | lockHeld | cand | 备注 |
|---|---|---|---|---|---|---|---|
| `x` | short-query-cache | **427.88** | 0 | 0 | — | 0 | ⚠️ **缓存标志触发**（应 <1ms，复测 0.02）|
| `.pdf` | advanced-trigram | 191.87 | 189.9 | 1.6 | — | 10124 | 受争用 |
| `main.cpp` | advanced-trigram | **106.20** | 60.85 | **45.31** | — | 71 | ⚠️ **marker#3**（71 候选 106ms，复测 0.3）|
| `*.swift` | advanced-trigram | **683.59** | 506.36 | 2.2 | **519.48** | 9213 | ⚠️ lockHeld≈total（复测 2.1）|
| `ext:cpp` | advanced-ext-index | **399.02** | 398.1 | 0 | 354.76 | 2987 | ⚠️ ext 标志（复测 0.83）|
| `size:>100m` | pure-filter-soa-gcd | **432.42** | 431.6 | 0 | 394.72 | 0 | ⚠️ lockHeld≈total（floor 14.62）|
| `hello world` | advanced-trigram | **495.81** | 494.9 | 0.8 | 495.75 | 2340 | ⚠️ lockHeld≈total（复测 2.36）|
| `abc def ghi jkl mno pqr` | advanced-trigram | 275.36 | 274.4 | 0.85 | — | 15924 | 受争用 |
| `README` | advanced-trigram | 201.41 | 197.2 | 4.1 | — | 16469 | 受争用 |
| `test` | advanced-trigram | 75.31 | 63.9 | 11.2 | — | 136275 | 较干净 |
| `MacEverything` | advanced-trigram | 77.39 | 66.4 | 10.9 | — | 6699 | 较干净 |
| `dm:today` | pure-filter-soa-gcd | 123.65 | 123.6 | 0 | — | 0 | 数值（floor 70.02）|
| `content:import` | pure-filter-soa-gcd | 185.94 | 178.3 | 0 | — | 0 | 字符串（floor 150.07）|
| `path:/Users` | advanced-linear-gcd | **698.49** | 690.6 | 0 | **691.05** | 0 | ⚠️ lockHeld≈total（floor 161.45）|
| `中文测试` | advanced-linear-gcd | **568.54** | 568.4 | 0.1 | 568.48 | 0 | ⚠️ lockHeld≈total（floor 207.05）|
| `verylongquery…really` | advanced-trigram | 6.69 | 0 | 6.6 | — | 0 | |

**活跃写入争用签名（三重证据，本轮最强）**：
1. **缓存标志首次触发**：`x`=427.88ms——短查询缓存路径连查询都不必跑却耗 427ms，唯一解释是写线程持有索引锁、连缓存读取都被饿死。本系列 ingest（R95-R98）首次击穿缓存路径，是「写者饿死读者」最强直接证据。
2. **lockHeld≈total 全线**：*.swift 519/683、size 394/432、hello world 495/495、path 691/698、中文 568/568——查询几乎全程在等锁，phase2≈lockHeld≈total。
3. **窗口内 totalRecords 实测增长**：17:11-17:14 区间 5,981,406→5,981,949（+543），证实电池期间后台持续写入。

---

## 2. 复测消歧（R88 协议 + R97 FLOOR-METHOD）

**标志项复测（全部干净恢复 → 突发型，非稳态）：**

| query | 电池值 | probe1 | probe2 | probe3 | probe4 | 判定 |
|---|---|---|---|---|---|---|
| `x`（缓存）| 427.88 | 12.29 | 0.19 | 0.05 | 0.02 | ✅ 瞬时（2万× 恢复）|
| `main.cpp`（71 cand）| 106.20 | 35.09 | 2.74 | 0.29 | 0.30 | ✅ 瞬时（354× 恢复）|
| `*.swift` | 683.59 | 132.22 | 1.10 | 2.10 | 16.48 | ✅ 瞬时（620× 恢复）|
| `ext:cpp` | 399.02 | 30.72 | 3.07 | 0.83 | 11.46 | ✅ 瞬时（481× 恢复）|
| `hello world` | 495.81 | 80.05 | 2.36 | 29.91 | 2.93 | ✅ 瞬时（210× 恢复）|

首复测仍带争用尾迹（x 12.29、*.swift 132、main.cpp 35），probe2-4 回落亚毫秒——**这是关键区别**：R95 稳态型 ingest 复测 30s 后仍不能干净恢复（分裂被迫 SUSPEND）；本轮如 R96 突发型，突发间隙存在干净窗口，分裂可评估。

**FLOOR-METHOD 路径 6× 复测（取 min 为无偏地板）：**

| query | 全部 6 次复测（升序）| floor(min) | 中位 |
|---|---|---|---|
| `size:>100m` | 14.62 / 21.2 / 29.4 / 34.06 / 93.8 / 432.01 | **14.62** | 31.73 |
| `dm:today` | 70.02 / 73.37 / 73.92 / 74.31 / 75.18 / 84.36 | **70.02** | 74.12 |
| `content:import` | 150.07 / 155.68 / 159.81 / 176.22 / 234.76 / 254.2 | **150.07** | 167.99 |
| `path:/Users` | 161.45 / 172.05 / 205.4 / 222.27 / 298.44 / 525.44 | **161.45** | 213.84 |
| `中文测试` | 207.05 / 229.15 / 252.91 / 301.98 / 442.24 / 460.44 | **207.05** | 277.45 |

**方法论依据（R97 确立）**：活跃写入下锁等待是**纯加性**的——争用只会在真实计算上叠加等待，绝不会减少耗时。故双峰分布中 **min 是无偏的真热态地板**，中位被争用突发系统性抬高。本轮 size/path/中文/content 均双峰（地板 + 突发尾），dm 反而紧致单峰（70-84ms，无突发命中）——印证突发是**逐查询随机命中**而非全局停顿。

---

## 3. 洁净 ns/槽（floor 法，records=5,981,124）

| 路径 | 类型 | floor(ms) | 洁净值(ns/槽) |
|---|---|---|---|
| `size:>100m` | 数值 | 14.62 | **2.44** |
| `dm:today` | 数值 | 70.02 | **11.71** |
| `content:import` | 字符串 | 150.07 | **25.09** |
| `path:/Users` | 字符串 | 161.45 | **26.99** |
| `中文测试` | 字符串 | 207.05 | **34.62** |

---

## 4. 本轮头条：活跃写入「再强化」——缓存标志首破

继 R95（稳态）→R96（突发）→R97（收敛）后，本轮 R98 ingest 强度**反弹回 R96 级突发**，且争用穿透性创本系列新高：

| 信号 | R95（稳态）| R96（突发）| R97（收敛）| **R98（本轮，再强化）** |
|---|---|---|---|---|
| 电池峰值 | 1290ms | 1375ms | 403ms | **698ms** |
| 缓存标志 `x` | 正常 | 正常 | 正常 | **427.88ms（首破！）** |
| tomb 增量(30min) | +21,579 | +23,550 | +14,827 | **+18,708（再加速）** |
| 复测能否干净恢复 | ❌ 否 | ✅ 是 | ✅ 是 | **✅ 是（突发型）** |
| 分裂判定 | SUSPEND | 22/22 | 23/23 | **24/24** |
| 0.5-1s 段 | 22 | 24 | 24(冻结) | **28(+4)** |

**缓存标志首破的意义**：`x`=427.88ms 是本系列 ingest 以来唯一一次短查询缓存路径被击穿。缓存路径不跑 phase2、不解引用、理论恒亚毫秒，其唯一阻塞源是**索引读写锁**——写线程批量提交时持锁，连缓存读取都被排队。这是「写者饿死读者」根因（→ P0 #2 读写锁分离）最干净的单点证据，比 lockHeld≈total 更直接（后者尚可争辩为真实 phase2 慢，前者完全无计算可言）。

---

## 5. 绝对二层分裂：floor 法 24/24 不破（间隙 2.143×）

- **数值层**：size 2.44、dm 11.71 → 最高数值 **dm 11.71**
- **字符串层**：content 25.09、path 26.99、中文 34.62 → 最低字符串 **content 25.09**
- **floor 间隙 = 25.09 / 11.71 = 2.143×**，与 R97（2.14×）**并列史上最窄对**，无重叠。

**对抗性检验（R97 协议）**：即便取 dm 最差洁净样本（84.36ms = 14.10 ns/槽），仍 < content 地板（25.09 ns/槽）——**HOLDS**。分裂在最不利估计下依然成立，机制级（纯数值零解引用 ≪ 字符串解引用散布串池）不可动摇。

字符串层内部 content < path < 中文，与 R97 同向；数值层 size 地板 2.44 创会话新低（R97 2.35 微高），但电池 size 432ms 被突发击中，floor 14.62ms 才是真热态——**地板法剥离争用后数值层依旧贴地**。23→24 连续不破，全区间间隙 2.11×（R90）~3.49×（R84）。

---

## 6. 阀门证据：tombstone 回收阀仍结构性不可达

| 指标 | 值 |
|---|---|
| `Skipping flush — only 78 entries (threshold=100)` | **575 次**（R97 515）|
| `triggering full compaction` | **0** |
| `Reclaimed` | **0** |
| 实际 flush | **1 次**（启动 11:12:29）|

根因不变（R67）：`IndexPersistence.cpp:142` entryCount 冻结 78 < 100 提前 return，`:159` tombstone 比例阀（`kTombstoneCompactRatio=0.25`）结构性永不可达——全会话 0 触发、0 回收。本轮 ingest 强化使 skip-gate 从 515→575（+60，远超 R97 增量），写入压力直接反映在跳过次数上。

---

## 7. tombstone 趋势：9.74%，连续 31 轮单调，增长再加速

| 轮次 | tombstone | ratio | 30min 增量 |
|---|---|---|---|
| R94@15:11 | 503,808 | 8.54% | — |
| R95@15:41 | 525,387 | 8.87% | +21,579 |
| R96@16:11 | 548,937 | 9.23% | +23,550 |
| R97@16:41 | 563,764 | 9.46% | +14,827（收敛）|
| **R98@17:11** | **582,472** | **9.74%** | **+18,708（再加速）** |

会话内单调递增、永不回缩，**连续 31 轮**。R43 参考崩溃点 12.97%（~800K），当前 9.74% 已过 **~75% 程**。本轮 live 仅 +1,709（5,398,652），total +20,417，即 **91.6% 新增记录变为 tombstone**（更新/删除主导）——R67 阀门破损使这些墓碑永不回收，逼近崩溃点的速率随 ingest 波动但方向不可逆。这是会话内唯一真实退化，修复紧迫性随阈值逼近持续最高。

---

## 8. 日志健康度

- WARN = **0**，ERROR = **0**（全会话）
- QueryAdvanced（total>100ms 才记录）= **442 条**（R97 410，+32）——增速回升（R97 +22），与 ingest 再强化一致
- Top-10 峰值 `[3362, 3766, 3807, 6061, 6811, 7544, 8205, 15749, 17928, 18582]` —— 仍与历史**逐字节相同**（均为启动期事件，非本轮查询）
- 延迟分布（QueryAdvanced-only）：>5s=**7**（冻结）、1-5s=**27**（冻结）、0.5-1s=**28（+4）**、0.1-0.5s=380、中位 **221ms**
- **0.5-1s 段 +4 来源已枚举**：电池 *.swift 683 / path 698 / 中文 568 + 复测 path 525——全部为本轮活跃写入争用产生的新 >500ms 条目，与窗口内 totalRecords +543 实测增长吻合
- 启动基建事件须排除：`20765ms`@11:05:13（Phase-2 mutation replay）、`10353ms`@11:12:29（启动 flush，rewrite=10331ms）——非查询，已从波段计数过滤

---

## 9. 优化建议（P0，本轮 #2 紧迫性再升级）

1. **🔥 P0｜读写锁分离 / 无锁快照读**（本轮证据再升级）：缓存标志 `x`=427.88ms 首破，证明写线程持锁时连缓存路径都被饿死——这是「写者饿死读者」根因最干净的单点证据。读写锁分离 / RCU 快照读直击此根因，本系列 ingest 四轮（R95-R98）持续指向同一争用源，应为最高优先级。
2. **🔥 P0｜R67 flush 检查顺序修复**：调换 `:142`/`:159` + 阈值下调 <12.97%（建议 8–10%）。当前 9.74% 已越建议区间，tombstone 永不回收逼近崩溃点。
3. **🔥 P0｜无锁快照 flush**：启动 flush 持锁 10.3s（与 #1 同属锁争用类）。
4. **🔥 P0｜查询超时保护**：本轮 path 698ms / *.swift 683ms 等多个 >500ms 停顿——超时熔断 + 降级返回是必要韧性机制，写入突发不可控。
- **P0｜HTTP 并发**：acceptLoop 单线程。
- **P1🔥｜CJK trigram 索引**：`中文测试` 走 linear-gcd 全扫（floor 207ms / 34.62 ns/槽，本轮字符串最差）。

---

## 10. 结论

- **定性**：🔴 活跃后台写入「再强化」轮。继 R97 收敛后 ingest 反弹回 R96 级突发：电池峰值 403→698ms、**缓存标志 `x`=427.88ms 首破**、tomb 增量 +18,708 再加速、窗口内 totalRecords 实测 +543。
- **突发型而非稳态型**：5 个标志项复测全部干净恢复（probe2-4 亚毫秒），存在干净窗口，分裂可评估——与 R95 稳态型（复测不恢复、SUSPEND）本质不同。
- **FLOOR-METHOD 再奏效**：本轮突发逐查询随机命中 size/path/中文/content（dm 紧致幸免），取各路径 min 为真热态地板。**分裂 2.143×、24/24 不破**，与 R97 并列史上最窄对；对抗性检验（dm worst-clean 14.10ns < content floor 25.09ns）HOLDS——分裂在最不利估计下仍机制级成立。
- **缓存标志首破的诊断价值**：`x`=427ms 是「写者饿死读者」最干净证据（缓存路径无计算可言，唯一阻塞源是索引锁），使 P0 #1 读写锁分离的紧迫性再升级。
- **唯一持续恶化项**：tombstone **9.74%（582,472）**，连续 31 轮单调，~75% 逼近 12.97% 崩溃点，91.6% 新增入墓，根因 R67 阀门结构性不可达明确。
- **诚实修正纪律**：本轮严格区分电池假象（争用污染）与真热态（floor），未被 698ms 峰值误导；dm 单峰幸免如实标注，未强行套用双峰；24/24 在对抗性检验下建立而非乐观估计。
