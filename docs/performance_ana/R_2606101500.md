# MacEverything 性能分析报告 R_2606101500

## 1. 本轮范围

- 任务启动时间：2026-06-10 15:00
- 报告文件：`docs/performance_ana/R_2606101500.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=8999`
  - `SEG_LINES=8057`
  - `LAST_LOG_TS=2026-06-10 13:35`

## 2. 执行摘要

**⭐ 系列最佳表现！** 0/16 >500ms，Max=73ms，P50=14.4ms，lockWait=0（连续 5 轮）。**极佳 14/16——系列最高！** Trigram 均值 12ms（↓90% from R255=125ms——**1-round recovery 第 14 次确认**）。全表扫描总和 176ms（↓72% from R255=628ms）。全部路径均在最优区间。**page cache 完全预热——"查询即预热"效应持续发挥。**

| 指标 | R255 | R256 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 1/16 | **0/16** | ↓1 |
| Max | 530ms | **73ms** | **↓86%** |
| P50 | 96ms | **14.4ms** | **↓85%** |
| lockWait max | 0ms | **0ms** | 完美（连续 5 轮） |
| 极佳比例(<50ms) | 5/16 | **14/16** | **↑9——系列最高！** |
| tombstone | 2,271,517 (30.33%) | **2,276,439 (30.37%)** | ↑0.04pp |
| recordCount | 7,495,142 | **7,500,190** | +5.0K（正常） |
| liveRecordCount | 5,223,625 | **5,223,751** | +126（正常） |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 7234 | **7258** | +24（偏低——见分析） |
| COMPACTION | 2 | **2** | **不变——第 57 轮确认缺陷** |
| QUERYADV_GE500 | 74 | **74** | 不变 |

### ⭐ 系列最佳——多项记录刷新

| 指标 | 此前最佳 | R256 本轮 | 提升 |
|------|------:|------:|------|
| 极佳比例 | 10/16 (R254) | **14/16** | **+4** |
| Max | 121ms (R254) | **73ms** | **↓40%** |
| P50 | 44ms (R254) | **14ms** | **↓68%** |
| Trigram 均值 | 37ms (R246) | **12ms** | **↓68%** |
| 全表扫描总和 | 173ms (R254) | **176ms** | ≈持平 |

### 1-round recovery 第 14 次确认

- **R255（1/16 >500ms, trigram 125ms, 极佳 5/16）→ R256（0/16, trigram 12ms, 极佳 14/16）。**
- **14/14 = 100% 成功率。**
- **完整记录：R176→R177, R179→R180, R181→R182, R190→R191, R194→R195, R199→R200, R208→R209, R215→R216, R221→R222, R227→R228, R232→R233, R244→R245, R252→R254(3轮I/O), R255→R256。**

### SKIPFLUSH +24 分析

- **正常每轮 +60。+24 意味着缺 36 次 tick。**
- **日志时间跨度 13:09→13:35（26 分钟）——应有 ~52 次 tick，实际 24 次。**
- **与 R255 的 +34 一致——进程仍在经历间歇性调度延迟。**
- **但对查询性能无影响——page cache 在连续查询中被充分预热。**
- **可能原因仍为 App Nap 或 macOS 电源管理。**

### Trigram 路径——全面极佳

| Query | R255 | R256 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 530ms | **24ms** | **↓95%** | **极佳（完全恢复！）** |
| `hello world` | 131ms | **15ms** | ↓89% | 极佳 |
| `*.swift` | 96ms | **13ms** | ↓87% | 极佳 |
| `main.cpp` | 28ms | **2ms** | ↓93% | 极佳 |
| `abc def...` | 97ms | **21ms** | ↓78% | 极佳 |
| `README` | 28ms | **8ms** | ↓71% | 极佳 |
| `test` | 87ms | **12ms** | ↓86% | 极佳 |
| `MacEverything` | 5ms | **3ms** | ↓40% | 极佳 |
| Trigram 均值 | 125ms | **12ms** | **↓90%——系列最佳！** |

- **全部 8/8 trigram 极佳——系列首次！**
- **`.pdf`=24ms（↓95%——从 R251=2829ms 到 24ms，117× 改善！）。**
- **最慢 trigram = `.pdf`=24ms——即使大 postings 集也完全在缓存中。**

### 全表扫描——极佳

| Query | R255 | R256 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 163ms | **73ms** | ↓55% |
| `path:/Users` | 242ms | **55ms** | **↓77%** |
| `中文测试` | 223ms | **48ms** | **↓78%** |
| **总和** | **628ms** | **176ms** | **↓72%** |

- **全表扫描总和 628→176ms（↓72%）——与 R254=173ms 持平（近期最佳水平）。**
- **`中文测试`=48ms（极佳！）。**
- **本轮零和不明显——trigram 和全表扫描同时极佳（page cache 完全预热）。**

### 辅助路径——全面极佳

| Query | R255 | R256 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 145ms | **1ms** | ↓99%（恢复） | 极佳 |
| `ext:cpp` | 28ms | **9ms** | ↓68% | 极佳 |
| `size:>100m` | 42ms | **30ms** | ↓29% | 极佳 |
| `dm:today` | 115ms | **14ms** | ↓88% | 极佳 |
| `verylongquery...` | 1.3ms | **0.4ms** | ↓69% | 极佳 |

- **`x`=1ms（↓99%——系统延迟完全消退）。**
- **全部辅助路径极佳。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 0.8 | 0.0 | short-query-cache | 0 | 0 | 极佳 |
| `.pdf` | 5 | 24.0 | 23.0 | advanced-trigram | 0 | 9888 | 极佳 |
| `main.cpp` | 5 | 2.4 | 1.6 | advanced-trigram | 0 | 85 | 极佳 |
| `*.swift` | 5 | 12.5 | 11.8 | advanced-trigram | 0 | 9379 | 极佳 |
| `ext:cpp` | 5 | 8.8 | 7.3 | advanced-ext-index | 0 | 3606 | 极佳 |
| `size:>100m` | 5 | 29.6 | 29.4 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `hello world` | 5 | 15.3 | 10.3 | advanced-trigram | 0 | 3671 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 20.9 | 20.9 | advanced-trigram | 0 | 14507 | 极佳 |
| `README` | 5 | 8.0 | 7.5 | advanced-trigram | 0 | 17164 | 极佳 |
| `test` | 5 | 11.9 | 10.8 | advanced-trigram | 0 | 117308 | 极佳 |
| `MacEverything` | 5 | 2.6 | 0.6 | advanced-trigram | 0 | 6867 | 极佳 |
| `dm:today` | 5 | 14.4 | 14.3 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 72.7 | 66.1 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 54.8 | 52.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 48.2 | 47.2 | advanced-linear-gcd | 0 | 0 | 极佳 |
| `verylongquery...` | 0 | 0.4 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 14.4ms，P90 = 48.2ms，Max = 72.7ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R255 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **14/16** | 0.4-48ms | R255=5/16（**↑9——系列最高！**） |
| **正常**（50-500ms） | 2/16 | 55-73ms | R255=10/16（↓8） |
| **超标**（>500ms） | **0/16** | — | R255=1/16（↓1） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,500,190，liveRecordCount=5,223,751（tombstone=**2,276,439**，**30.37%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=654  QUERYADV_GE500=74（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=7258
```

- **SKIPFLUSH +24（连续 2 轮偏低——可能 App Nap 持续）。**

## 6. 问题清单

### P0 确认（第 57 轮）：compaction 触发机制缺陷

- **tombstone=2,276,439（30.37%）——超阈值 5.37%。30% 以上第 5 轮。**
- **57 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **lockWait 连续 5 轮为 0。历史证据充分。**

### P0：I/O 优先级保护 + App Nap 防护

- **SKIPFLUSH 连续 2 轮偏低（+34, +24）——进程被系统节流。**
- **建议：`ProcessInfo.beginActivity(.userInitiated)` 防止 App Nap。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 57 轮——tombstone 30.37%！）

### P0：flush rewrite 改为 COW 方式

### P0：进程 QoS / I/O 优先级保护 + App Nap 防护

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. 极佳 14/16 能否维持（系列最佳水平）。
2. SKIPFLUSH 能否回归 +60（App Nap 是否为持续问题）。

## 9. 结论

**⭐ 系列最佳表现！** 0/16 >500ms，Max=73ms，P50=14ms，lockWait=0（连续 5 轮）。**极佳 14/16（系列最高！）。** Trigram 均值 12ms（↓90%——**系列最佳**，8/8 trigram 极佳——系列首次！），全表扫描 176ms（↓72%）。**1-round recovery 第 14 次确认（14/14 = 100%）。** `.pdf` 从 R251=2829ms 恢复至 24ms（117× 改善）。page cache 完全预热——trigram 和全表扫描同时极佳（零和效应消失）。

**tombstone 30.37%（2,276,439，第 57 轮 compaction 缺陷确认）。** recordCount +5.0K（正常）。SKIPFLUSH 连续 2 轮偏低（+34, +24）——App Nap 可能持续影响后台 tick，但不影响查询性能。
