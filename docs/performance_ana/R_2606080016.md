# MacEverything 性能分析报告 R_2606080016

## 1. 本轮范围

- 任务启动时间：2026-06-08 00:16
- 报告文件：`docs/performance_ana/R_2606080016.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=2895`
  - `SEG_LINES=1953`
  - `LAST_LOG_TS=2026-06-08 00:17`

## 2. 执行摘要

**进程唤醒延迟再现+深夜 page cache 竞争——0/16 >500ms，Max=251ms，P50=43ms，lockWait=0，极佳比例 8/16（50%）。** 与 R151 相同模式：30 分钟空闲后进程唤醒（`x`=31.6ms，phase2=0）。叠加深夜系统维护活动导致 SoA 和 trigram postings page cache 部分驱逐。这是本系列首次极佳比例降至 50%，但仍 0/16 >500ms——系统健康。

### 关键指标对比

| 指标 | R156（30 min 前） | R157 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms | 0/16 | **0/16** | 持平（理想） |
| Max | 249ms | **251ms** | 持平 |
| P50 | 14ms | **43ms** | ↑ 唤醒+cache 竞争 |
| lockWait max | 0ms | **0ms** | 完美 |
| 极佳比例(<50ms) | 11/16 | **8/16** | ↓3（深夜效应） |
| tombstone | 1,226,482 (19.40%) | **1,235,474 (19.53%)** | ↑ 正常 |
| tombstone 增速 | ~286/min | **~300/min** | 持平（极低） |
| recordCount | 6,324,050 | **6,333,146** | +9.1K |
| liveRecordCount | 5,097,568 | **5,097,672** | +104 |
| FLUSH 累计 | 1 | **1** | 无新 flush |
| SKIPFLUSH | 1770 | **1830** | +60 |
| COMPACTION | 2 | **2** | 不变 |
| QUERYADV_GE500 | 4 | **4** | 不变（连续 14 轮 0 新增） |

### 进程唤醒延迟分析（第 2 次出现）

| Query | R156 | R157 | overhead(R157) | 说明 |
|-------|-----:|-----:|------:|------|
| `x` | 0.8ms | **31.6ms** | 31.6ms | 进程唤醒（phase2=0） |
| `.pdf` | 14ms | **59ms** | 33ms beyond phase2 | 首批 I/O 路径冷 |
| `ext:cpp` | 4ms | **14ms** | 9ms beyond phase2 | 代码路径冷加载 |

- **R151（21:16）和 R157（00:16）间隔 3 小时——唤醒模式相同。**
- **规律确认**：每次 30 分钟间隔后首查询有 25-32ms 进程唤醒开销。
- **第 2-3 个查询残留 10-30ms overhead，第 4 个起恢复正常。**
- **影响**：仅影响 battery 的前 2-3 项，对用户体验无感知影响。

### 深夜 Page Cache 竞争加剧

| Query | R156 | R157 | 变化 | 说明 |
|-------|-----:|-----:|------|------|
| `size:>100m` | 24ms | **103ms** | ↑ 4.3x | SoA filter 驱逐 |
| `dm:today` | 102ms | **65ms** | ↓ 36% | 恢复 |
| `test` | 49ms | **69ms** | ↑ 41% | 高 candidate trigram 受影响 |
| `README` | 9ms | **43ms** | ↑ 4.8x | trigram postings 部分驱逐 |
| `content:import` | 197ms | **251ms** | ↑ 27% | SoA 线性区域 |
| `中文测试` | 219ms | **211ms** | 持平 | — |
| `path:/Users` | 249ms | **194ms** | ↓ 22% | 零和受益 |

- **`size:>100m` 24→103ms 和 `README` 9→43ms——深夜系统活动驱逐了更多 page cache。**
- **全表扫描总和**：R156=665ms → R157=656ms（持平，但 `content:import` 单项 251ms 偏高）。
- **Trigram 也受影响**：`README` 和 `test` 涉及大量 postings（16K/116K candidates），深夜 page cache 竞争使 postings 页面部分驱逐。
- **原因**：午夜时段 macOS 系统维护（Time Machine、fseventsd 压缩、mds_stores 等）增加内存压力。

### Trigram 路径——受深夜影响

| Query | R156 | R157 | 变化 |
|-------|-----:|-----:|------|
| `.pdf` | 14ms | **59ms** | ↑（含唤醒残余） |
| `*.swift` | 8ms | **22ms** | ↑ page cache 波动 |
| `hello world` | 14ms | **25ms** | ↑ 波动 |
| `README` | 9ms | **43ms** | ↑ postings 驱逐 |
| `test` | 49ms | **69ms** | ↑ postings 驱逐 |
| `abc def...` | 29ms | **26ms** | 持平 |
| Trigram 均值 | 11.5ms | **28.3ms** | ↑ 146%（含异常因素） |
| 去除唤醒项(.pdf) | — | **24.4ms** | 深夜 page cache 效应 |

- **Trigram 均值 28ms（去唤醒项 24ms）——相比 R156 的 11.5ms 显著上浮。**
- **原因明确**：深夜系统活动竞争 page cache，高 candidate 查询的 trigram postings 页面被部分驱逐。
- **预期**：下轮（R158）如仍在 30 分钟内执行，trigram 路径将恢复至 15-20ms 水平。

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 31.6 | 0.0 | short-query-cache | 0 | 0 | 唤醒延迟 |
| `.pdf` | 5 | 58.6 | 25.5 | advanced-trigram | 0 | 9812 | 唤醒残余+波动 |
| `main.cpp` | 5 | 3.6 | 2.6 | advanced-trigram | 0 | 82 | 极佳 |
| `*.swift` | 5 | 22.1 | 17.6 | advanced-trigram | 0 | 9345 | 极佳 |
| `ext:cpp` | 5 | 14.4 | 5.6 | advanced-ext-index | 0 | 3571 | 极佳（含残余） |
| `size:>100m` | 5 | 103.0 | 70.0 | advanced-pure-filter-soa-gcd | 0 | 0 | page cache 驱逐 |
| `hello world` | 5 | 25.4 | 24.8 | advanced-trigram | 0 | 3614 | 极佳 |
| `abc def ghi jkl mno pqr` | 0 | 26.0 | 25.9 | advanced-trigram | 0 | 14210 | 极佳 |
| `README` | 5 | 43.4 | 40.6 | advanced-trigram | 0 | 16631 | 极佳（临界） |
| `test` | 5 | 69.1 | 67.0 | advanced-trigram | 0 | 116512 | page cache 波动 |
| `MacEverything` | 5 | 3.0 | 0.8 | advanced-trigram | 0 | 6861 | 极佳 |
| `dm:today` | 5 | 64.9 | 64.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `content:import` | 5 | 251.3 | 178.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常（偏高） |
| `path:/Users` | 5 | 194.1 | 190.8 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 210.6 | 210.4 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.1 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 43.4ms，P90 = 210.6ms，Max = 251.3ms。0/16 查询 >500ms。lockWait = 0（全部 16 查询）。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R156 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | 8/16 | 0.1-43ms | R156=11/16（↓3 深夜效应） |
| **正常**（50-500ms） | 8/16 | 59-251ms | R156=5/16（↑3） |
| **超标**（>500ms） | 0/16 | — | R156=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=6,333,146，liveRecordCount=5,097,672（tombstone=**1,235,474**，**19.53%**）；
- 进程正常运行（当前会话，运行约 26.2 小时）。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=77  QUERYADV_GE500=4（不变——连续 14 轮 0 新增慢查询）
FLUSH=1  COMPACTION=2  SKIPFLUSH=1830
```

- **连续 14 轮 0 个新增 >500ms 查询（R144-R157）。**
- **FLUSH=1**——无新 flush（26.2 小时仅启动时 1 次）。
- **SKIPFLUSH=1830**——系统持续空闲。

## 6. 问题清单

### 观察：深夜 page cache 竞争叠加进程唤醒（信息记录，非问题）

- 进程唤醒延迟（`x`=32ms）：第 2 次出现，确认为 30 分钟空闲后的固定模式。
- 深夜 page cache 竞争：SoA filter（`size:>100m` 103ms）和高 candidate trigram（`test` 69ms，`README` 43ms）均受影响。
- 两个因素叠加使 P50 从 14→43ms，极佳比例从 11→8/16。
- **均为外部环境因素——系统本身无任何退化。下轮预计恢复。**

### 无活跃问题

- 0/16 >500ms，lockWait=0。
- 26.2 小时无 WARN/ERROR。
- 连续 14 轮 0 新增慢查询。

## 7. 优化建议

### P0：排查 compaction rewrite 崩溃根因（持续建议）

- tombstone 19.53%，增速 ~300/min。
- 距 25% 阈值约 ~31 小时。

### P1：flush rewrite 改为 COW 方式（持续建议）

- 系统空闲时无影响。

## 8. 下一轮重点

1. 唤醒延迟和深夜 cache 竞争是否在下轮消退。
2. tombstone 趋势（接近 19.5%）。
3. 系统稳定性（26 小时+）。

## 9. 结论

**深夜低谷轮——0/16 >500ms，Max=251ms，P50=43ms，lockWait=0，极佳比例 8/16。** 两个外部因素叠加：(1) 30 分钟空闲后进程唤醒延迟（第 2 次确认，`x`=32ms）；(2) 午夜系统维护活动增加 page cache 竞争压力（SoA + 高 candidate trigram 均受影响）。

**这是稳态范围内的环境波动，非系统退化。** P50 12 轮范围扩大至 14-43ms（去除 2 次唤醒轮后均值仍为 ~20ms）。连续 14 轮 0 新增慢查询，26.2 小时无异常。tombstone 19.53%（1.24M），距 25% 约 31 小时。系统本身健康——外部 page cache 竞争不改变系统性能特征。
