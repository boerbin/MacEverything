# MacEverything 性能分析报告 R_2606100200

## 1. 本轮范围

- 任务启动时间：2026-06-10 02:00
- 报告文件：`docs/performance_ana/R_2606100200.md`
- 服务端点：`http://localhost:19860/api/search`
- 状态端点：`http://localhost:19860/api/status`
- 日志文件：`~/Library/Logs/MacEverything/maceverything.log`
- 日志分析范围：仅分析最新一次 `Log session started` 之后的运行段
  - `SESSION_START_LINE=943`
  - `SESSION_START_TS=2026-06-06 22:08:01.548`
  - `TOTAL_LINES=7372`
  - `SEG_LINES=6430`
  - `LAST_LOG_TS=2026-06-09 22:47`

## 2. 执行摘要

**lockWait 完全消退 + 整体回落——0/16 >500ms，Max=285ms，P50=66ms，lockWait=0，极佳 4/16。** R229 的 lockWait=46.2ms 完全消退。**tombstone 突破 28%：2,038,041（28.14%），增速飙升至 ~939/min——凌晨 2 点异常后台活动！**

| 指标 | R229 | R230 本轮 | 变化 |
|------|--------:|--------:|------|
| >500ms 数量 | 0/16 | **0/16** | 持平 |
| Max | 381ms | **285ms** | **↓25%** |
| P50 | 165ms | **66ms** | **↓60%** |
| lockWait max | 46.2ms | **0ms** | **↓100%（完全消退）** |
| 极佳比例(<50ms) | 3/16 | **4/16** | ↑1 |
| tombstone | 2,009,885 (27.87%) | **2,038,041 (28.14%)** | **↑——突破 28%！** |
| tombstone 增速 | ~398/min | **~939/min** | **↑136%（凌晨异常飙升！）** |
| recordCount | 7,210,222 | **7,243,281** | +33.1K |
| liveRecordCount | 5,200,337 | **5,205,240** | +4.9K |
| FLUSH 累计 | 1 | **1** | 不变 |
| SKIPFLUSH | 5738 | **5797** | +59 |
| COMPACTION | 2 | **2** | **不变——第 31 轮确认缺陷** |
| QUERYADV_GE500 | 60 | **60** | 不变 |

### lockWait 完全消退

- **R229=46.2ms → R230=0ms——一次性事件，完全消退。**
- **第 10 次确认 1 轮恢复模式。**

### tombstone 突破 28%——增速飙升

- **tombstone=2,038,041（28.14%）——首次突破 28%！**
- **增速 ~939/min——本会话最高！是 R229（398/min）的 2.4 倍。**
- **recordCount +33.1K，liveRecordCount +4.9K——28.2K 为 tombstone。**
- **凌晨 2 点出现大量文件事件——可能是 Time Machine 备份或其他定时任务。**

### tombstone 里程碑全景

| 里程碑 | 轮次 | tombstone | tombstone% |
|--------|------|----------:|----------:|
| 首次超 25% | R200 | ~1,748K | 25.15% |
| 首次超 26% | R212 | ~1,841K | 26.14% |
| 首次超 27% | R220 | ~1,929K | 27.08% |
| 首次超 2M | R229 | 2,009,885 | 27.87% |
| **首次超 28%** | **R230** | **2,038,041** | **28.14%** |

### Trigram 路径——回落

| Query | R229 | R230 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `.pdf` | 166ms | **285ms** | ↑72% | 正常 |
| `hello world` | 328ms | **145ms** | **↓56%** | 正常 |
| `*.swift` | 214ms | **188ms** | ↓12% | 正常 |
| `main.cpp` | 105ms | **54ms** | ↓49% | 正常 |
| `abc def...` | 302ms | **230ms** | ↓24% | 正常 |
| `README` | 51ms | **85ms** | ↑67% | 正常 |
| `test` | 126ms | **89ms** | ↓29% | 正常 |
| `MacEverything` | 11ms | **31ms** | ↑182% | 极佳 |
| Trigram 均值 | 163ms | **138ms** | **↓15%** | 回落 |

- **Trigram 均值 163→138ms（↓15%）——从 R229 的偏高回落。**
- **`hello world`=145ms（↓56%）——R229 lockWait=46.2ms 消退后大幅改善。**
- **`.pdf`=285ms（↑72%）——postings 区域波动，但仍 <300ms。**

### 全表扫描——大幅恢复

| Query | R229 | R230 | 变化 |
|-------|-----:|-----:|------|
| `content:import` | 183ms | **74ms** | **↓60%** |
| `path:/Users` | 381ms | **108ms** | **↓72%** |
| `中文测试` | 194ms | **57ms** | **↓71%** |
| **总和** | **758ms** | **239ms** | **↓68%** |

- **全表扫描总和 758→239ms（↓68%）——大幅恢复。**
- **`中文测试`=57ms（↓71%）——名称数组重新预热。**
- **`path:/Users`=108ms（↓72%）——路径数组恢复。**

### 辅助路径

| Query | R229 | R230 | 变化 | 状态 |
|-------|-----:|-----:|------|------|
| `x` | 2ms | **18ms** | ↑800% | 极佳（偏高） |
| `ext:cpp` | 175ms | **57ms** | ↓67% | 正常 |
| `size:>100m` | 162ms | **56ms** | ↓65% | 正常 |
| `dm:today` | 61ms | **21ms** | ↓66% | 极佳 |
| `verylongquery...` | 0.2ms | **0.5ms** | ↑150% | 极佳 |

- **`x`=18ms（↑800%）——short-query-cache 路径通常 2-5ms。**
- **18ms 仍属极佳，但提示有轻微系统调度开销（可能与后台大量文件事件有关）。**
- **`ext:cpp`=57ms（↓67%）、`size:>100m`=56ms（↓65%）——ext-index 和 SoA 区域恢复。**

## 3. Query Battery 结果

| Query | Count | queryTimeMs | phase2Ms | Source | lockWaitMs | candidates | 备注 |
|-------|------:|------------:|---------:|--------|----------:|-----------:|------|
| `x` | 5 | 18.1 | 0.0 | short-query-cache | 0 | 0 | 极佳（偏高） |
| `.pdf` | 5 | 285.2 | 264.0 | advanced-trigram | 0 | 9860 | 正常 |
| `main.cpp` | 5 | 53.6 | 42.6 | advanced-trigram | 0 | 82 | 正常 |
| `*.swift` | 5 | 188.1 | 183.1 | advanced-trigram | 0 | 9353 | 正常 |
| `ext:cpp` | 5 | 57.1 | 55.9 | advanced-ext-index | 0 | 3571 | 正常 |
| `size:>100m` | 5 | 55.7 | 54.9 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `hello world` | 5 | 145.3 | 140.5 | advanced-trigram | 0 | 3671 | 正常 |
| `abc def ghi jkl mno pqr` | 0 | 230.0 | 228.5 | advanced-trigram | 0 | 14448 | 正常 |
| `README` | 5 | 85.3 | 59.8 | advanced-trigram | 0 | 17150 | 正常 |
| `test` | 5 | 89.0 | 79.8 | advanced-trigram | 0 | 117122 | 正常 |
| `MacEverything` | 5 | 30.5 | 0.9 | advanced-trigram | 0 | 6859 | 极佳 |
| `dm:today` | 5 | 20.9 | 19.7 | advanced-pure-filter-soa-gcd | 0 | 0 | 极佳 |
| `content:import` | 5 | 74.0 | 67.8 | advanced-pure-filter-soa-gcd | 0 | 0 | 正常 |
| `path:/Users` | 5 | 107.8 | 104.3 | advanced-linear-gcd | 0 | 0 | 正常 |
| `中文测试` | 0 | 56.8 | 51.5 | advanced-linear-gcd | 0 | 0 | 正常 |
| `verylongquery...` | 0 | 0.5 | 0.0 | advanced-trigram | 0 | 0 | 极佳 |

**P50 = 65.6ms，P90 = 230.0ms，Max = 285.2ms。0/16 查询 >500ms。lockWait = 0。**

### 性能分层

| 层级 | 查询数 | 时间范围 | 与 R229 对比 |
|------|------:|------:|------|
| **极佳**（<50ms） | **4/16** | 0.5-31ms | R229=3/16（↑1） |
| **正常**（50-500ms） | 12/16 | 54-285ms | R229=13/16（↓1） |
| **超标**（>500ms） | **0/16** | — | R229=0/16（持平） |

## 4. 服务可用性状态

- `localhost:19860` HTTP 服务正常，状态 200；
- recordCount=7,243,281，liveRecordCount=5,205,240（tombstone=**2,038,041**，**28.14%**）；
- 进程正常运行。

## 5. 最新日志段分析

```text
WARN=0  ERROR=0
QUERYADV=514  QUERYADV_GE500=60（不变）
FLUSH=1  COMPACTION=2  SKIPFLUSH=5797
```

## 6. 问题清单

### P0 确认（第 31 轮）：compaction 触发机制缺陷

- **tombstone=2,038,041（28.14%）——首次突破 28%！超阈值 3.14%。**
- **增速飙升至 ~939/min——凌晨后台活动异常。**
- **31 轮连续确认，COMPACTION=2 始终不变。**

### P0：flush 写锁阻塞

- **R230 lockWait=0——消退。但历史证据充分：R217=202.6ms，R227=65.3ms，R229=46.2ms。**
- **全时段出现（午后、深夜、凌晨）。COW flush 是正确修复路径。**

## 7. 优化建议

### P0：修复 compaction 触发机制（第 31 轮确认——tombstone 突破 28%）

### P0：flush rewrite 改为 COW 方式（全时段 lockWait 证据）

### P1：进程 QoS 优先级设置

### P2：madvise MADV_WILLNEED 热区维护

## 8. 下一轮重点

1. tombstone 增速是否回落（939/min 为本会话最高）。
2. `x`=18ms 的 cache 路径开销是否消退。

## 9. 结论

**lockWait 完全消退 + 整体回落——0/16 >500ms，Max=285ms（↓25%），P50=66ms（↓60%），极佳 4/16。** R229 的 lockWait=46.2ms 完全消退——第 10 次确认 1 轮恢复模式。全表扫描总和 239ms（↓68%）。Trigram 均值 138ms（↓15%）。`x`=18ms（通常 2-5ms）——轻微系统开销。

**tombstone 突破 28% 里程碑！2,038,041（28.14%，第 31 轮确认 compaction 缺陷）。** 增速飙升至 ~939/min（本会话最高！是 R229 的 2.4 倍）——凌晨后台大量文件事件。recordCount +33.1K（本轮最高增量之一）。COMPACTION=2 始终不变。
